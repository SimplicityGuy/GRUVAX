"""Admin authentication module for GRUVAX.

Provides PIN hashing/verification (Argon2id via passlib), server-side session
management, and cookie helpers for the admin login flow (ADMN-01, ADMN-02).
"""
