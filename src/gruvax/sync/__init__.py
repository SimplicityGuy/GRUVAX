"""Profile-sync subpackage (P1, Plans 02-03).

Exports:
  - ``encrypt_pat`` / ``decrypt_pat`` — Fernet PAT-at-rest helpers (Plan 02 Task 2).
  - (Plan 03) ``sync_profile`` — staging-swap orchestrator + advisory-lock guard.
"""
