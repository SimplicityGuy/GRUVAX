---
phase: 02-multi-profile-migration-profile-manager
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/integration/test_migrate_0010.py
  - tests/unit/test_cache_registry.py
  - tests/unit/test_profile_state_registry.py
  - tests/integration/test_profile_manager_api.py
  - tests/integration/test_sse_per_profile.py
  - tests/integration/test_session_bootstrap.py
  - tests/conftest.py
autonomous: true
requirements: [PROF-04, PROF-02, API-02, SYN-02]
must_haves:
  truths:
    - "Every Phase 2 requirement (PROF-04, PROF-02, API-02, SYN-02) has at least one failing test file present before implementation begins (Nyquist RED baseline)."
    - "A two-profile DB fixture exists so SLO benchmark and SSE-leakage tests can assert isolation."
  artifacts:
    - path: "tests/integration/test_migrate_0010.py"
      provides: "PROF-04 NOT NULL + composite-PK + round-trip RED tests"
      contains: "def test_composite_pks"
    - path: "tests/unit/test_cache_registry.py"
      provides: "API-02 per-profile registry isolation RED tests"
    - path: "tests/unit/test_profile_state_registry.py"
      provides: "SYN-02 per-profile staleness RED tests"
    - path: "tests/integration/test_profile_manager_api.py"
      provides: "PROF-02 CRUD + connect + 202/poll + collision + soft-delete RED tests"
    - path: "tests/integration/test_sse_per_profile.py"
      provides: "D2-04 SSE 403/400 + no-cross-profile-leakage RED tests"
    - path: "tests/integration/test_session_bootstrap.py"
      provides: "D2-08 auto-bind + D2-10 cookie-independence RED tests"
  key_links:
    - from: "tests/conftest.py"
      to: "gruvax.profiles (second profile row)"
      via: "second_profile fixture seeds a 2nd non-deleted profile"
      pattern: "second_profile"
---

<objective>
Create the Wave 0 test scaffolding (RED baseline) for every Phase 2 requirement so each downstream implementation plan has an automated `<verify>` command that exists from the start (Nyquist rule). No production code is written here — only `pytest` test files and shared fixtures, asserting the *intended* P2 behavior so they fail until the corresponding implementation lands.

Purpose: Guarantee no implementation task ships without a pre-existing automated check. The RESEARCH §"Wave 0 Gaps" list is the authoritative inventory.
Output: 6 new test files + a `second_profile` fixture in `tests/conftest.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-profile-migration-profile-manager/02-RESEARCH.md
@.planning/phases/02-multi-profile-migration-profile-manager/02-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure the new tests build on (verified by codebase read). -->

From tests/conftest.py:
- `db_pool` — session-scoped psycopg AsyncConnectionPool fixture (loop_scope="session").
- `default_profile_uuid` — returns "00000000-0000-0000-0000-000000000001".
- `admin_session(client)` — module-scoped; logs in, returns {cookies, csrf_token}.
- Integration test modules provide their own `client` fixture (LifespanManager + httpx AsyncClient + ASGITransport). Follow the pattern in tests/integration/test_sse.py and test_search.py.

Existing analog tests to mirror structure from:
- tests/integration/test_migrate_0009.py — alembic round-trip via subprocess.run + asyncio.to_thread; seeds tests/fixtures/legacy/synth_collection.sql before the downgrade leg.
- tests/integration/test_sse.py — live-server SSE test against GET /api/events.
- tests/integration/test_search_benchmark.py — pytest-benchmark p95 SLO gate.
- tests/unit/test_event_bus.py — EventBus subscribe/publish/unsubscribe unit test.

Migration ground truth (from migrations/versions/0009_v2_profiles_and_collection_cache.py):
- 7 v1 tables carry nullable profile_id: admin_sessions, boundary_history, cube_boundaries, idempotency_keys, record_stats, segment_overrides, settings.
- P2 tightens 5 to NOT NULL: cube_boundaries, settings, record_stats, segment_overrides, boundary_history.
- admin_sessions + idempotency_keys STAY nullable.
- Composite-PK changes on 4 tables: cube_boundaries → (profile_id, unit_id, row, col); settings → (profile_id, key); record_stats → (profile_id, release_id); segment_overrides → (profile_id, unit_id, row, col, label).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + registry/staleness RED tests + second_profile fixture</name>
  <files>tests/integration/test_migrate_0010.py, tests/unit/test_cache_registry.py, tests/unit/test_profile_state_registry.py, tests/conftest.py</files>
  <read_first>
    - tests/integration/test_migrate_0009.py (alembic subprocess + roundtrip + legacy-seed pattern — copy exactly)
    - tests/unit/test_event_bus.py (unit-test style for an app.state object)
    - tests/conftest.py (db_pool, default_profile_uuid, fixture conventions; find the "P1 Wave 0 fixtures" section to add second_profile nearby)
    - migrations/versions/0009_v2_profiles_and_collection_cache.py (the 7 tables + their PKs)
    - .planning/phases/02-multi-profile-migration-profile-manager/02-RESEARCH.md §Critical Migration Discovery + §Validation Architecture
  </read_first>
  <action>
    Add a session-or-function-scoped `second_profile` fixture to tests/conftest.py (place near the "P1 Wave 0 fixtures" section) that INSERTs a second non-deleted profile row into gruvax.profiles via `db_pool` (display_name 'Sam', a fresh `gen_random_uuid()` captured into the fixture's return value, app_token_encrypted a Fernet placeholder, app_token_revoked TRUE, last_sync_status NULL) and yields its UUID string; teardown soft-deletes or DELETEs it. Use parameterized %s SQL, never f-strings.

    Write tests/integration/test_migrate_0010.py mirroring test_migrate_0009.py's subprocess `_alembic` helper (asyncio.to_thread, never asyncio.run-from-async) and legacy-seed precondition. Tests (all expected RED until Plan 02-01 lands):
    - `test_roundtrip_clean`: `upgrade head → downgrade base → upgrade head` exits 0 after seeding tests/fixtures/legacy/synth_collection.sql.
    - `test_not_null_on_five_data_tables`: after `upgrade head`, query information_schema.columns and assert profile_id is_nullable = 'NO' for cube_boundaries, settings, record_stats, segment_overrides, boundary_history.
    - `test_nullable_stays_on_two_infra_tables`: assert profile_id is_nullable = 'YES' for admin_sessions and idempotency_keys.
    - `test_composite_pks`: query pg_index/pg_constraint and assert the 4 composite PKs include profile_id as a leading column for cube_boundaries (profile_id, unit_id, row, col), settings (profile_id, key), record_stats (profile_id, release_id), segment_overrides (profile_id, unit_id, row, col, label).

    Write tests/unit/test_cache_registry.py asserting the intended registry API (RED until Plan 02-02): construct an object with `boundary_cache_registry: dict[str, BoundaryCache]`, `snapshot_registry`, `segment_cache_registry`, `settings_cache_registry`, `event_bus_registry` keyed by str(uuid); assert lookup by `str(profile_id)` returns the per-profile instance and that two distinct profile keys return distinct instances (isolation by construction). Import the cache classes from gruvax.estimator.*; if a registry-builder helper does not yet exist, assert against the expected app.state attribute names directly via a small fake state object so the test compiles and fails on the missing implementation.

    Write tests/unit/test_profile_state_registry.py asserting the intended `profile_state_registry: dict[str, dict]` shape (RED until Plan 02-02): each entry holds keys `last_sync_at`, `last_sync_status`, `app_token_revoked`; assert two profiles produce two independent entries.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_migrate_0010.py tests/unit/test_cache_registry.py tests/unit/test_profile_state_registry.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest --collect-only -q` collects test_migrate_0010.py::test_composite_pks, ::test_not_null_on_five_data_tables, ::test_nullable_stays_on_two_infra_tables, ::test_roundtrip_clean without collection errors.
    - tests/conftest.py defines a `second_profile` fixture (grep `def second_profile` returns a match).
    - test_cache_registry.py references all five registry attribute names: boundary_cache_registry, snapshot_registry, segment_cache_registry, settings_cache_registry, event_bus_registry.
    - All new tests FAIL or ERROR (RED) when run against current code (no green false-positives).
  </acceptance_criteria>
  <done>Three test files + the second_profile fixture exist, collect cleanly, and are RED against current code.</done>
</task>

<task type="auto">
  <name>Task 2: Profile-manager API + per-profile SSE + session-bootstrap RED tests</name>
  <files>tests/integration/test_profile_manager_api.py, tests/integration/test_sse_per_profile.py, tests/integration/test_session_bootstrap.py</files>
  <read_first>
    - tests/integration/test_sse.py (live-server SSE client pattern + EventSourceResponse consumption)
    - tests/integration/test_admin_auth.py (admin_session usage + CSRF header on mutations)
    - src/gruvax/api/admin/profile_sync.py (current POST /profiles/{id}/sync shape + error taxonomy)
    - src/gruvax/auth/sessions.py (cookie names: gruvax_session, gruvax_csrf — the browse-binding cookie must be a NEW, separate name)
    - .planning/phases/02-multi-profile-migration-profile-manager/02-RESEARCH.md §Validation Architecture (the exact test names) + §Security Domain
    - .planning/phases/02-multi-profile-migration-profile-manager/02-CONTEXT.md (D2-04, D2-08, D2-10, D2-12, D2-13)
  </read_first>
  <action>
    Write tests/integration/test_profile_manager_api.py (RED until Plans 02-05) with a `client` + `admin_session` fixture, all admin mutations sending the X-CSRF-Token header:
    - `test_create_profile`: POST /api/admin/profiles {display_name:"Sam"} → 200/201, returns id; GET /api/admin/profiles lists it with status PENDING (no PAT).
    - `test_connect_pat_flow`: POST /api/admin/profiles/{id}/connect with a valid fake PAT → 200 (synchronous test-sync captures discogsography_user_id, flips app_token_revoked FALSE) and kicks the full sync; subsequent GET /api/admin/profiles/{id} eventually shows last_sync_status 'ok'.
    - `test_sync_202_poll`: POST /api/admin/profiles/{id}/sync → 202 immediately; poll GET /api/admin/profiles/{id} and assert last_sync_status transitions in_progress → ok.
    - `test_user_id_collision`: connecting a second profile with a PAT resolving to an already-used discogsography_user_id → 409 {"type":"user_id_collision"}.
    - `test_soft_delete_evicts`: DELETE /api/admin/profiles/{id} → 200; profile no longer in GET /api/admin/profiles and absent from GET /api/session profiles[].
    - `test_pat_rejected`: connect with a rejected PAT → 401 {"type":"pat_rejected"}.
    Use the fake-discogsography fixture (tests/fixtures/fake_discogsography.py) and its magic-token routing for the error paths, per the P1 pattern.

    Write tests/integration/test_sse_per_profile.py (RED until Plan 02-03) with a live-server `client`:
    - `test_sse_403_on_profile_mismatch`: bind browse cookie to profile A, GET /api/events/{B} → 403.
    - `test_sse_400_on_unbound`: no browse cookie, GET /api/events/{A} → 400.
    - `test_sse_connects_when_bound`: cookie bound to A, GET /api/events/{A} yields the initial connected comment.
    - `test_no_cross_profile_leakage`: subscribe two clients to profiles A and B; publish collection_changed on A's bus (trigger via A's sync or a test hook); assert only A's client receives the event, B's does not.

    Write tests/integration/test_session_bootstrap.py (RED until Plan 02-04):
    - `test_single_profile_auto_binds`: with exactly one active profile, GET /api/session returns profile_count 1, a non-null bound_profile_id, and sets the browse-binding cookie in the response.
    - `test_two_profiles_unbound`: with two active profiles and no cookie, GET /api/session returns profile_count 2 and bound_profile_id null.
    - `test_bind_then_unbind`: POST /api/session/bind {profile_id} sets the cookie; DELETE /api/session/bind clears it.
    - `test_binding_independent_of_admin`: binding/unbinding the browse cookie does NOT affect the gruvax_session admin cookie, and vice versa (assert the two cookie names are distinct and operations on one leave the other intact).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_profile_manager_api.py tests/integration/test_sse_per_profile.py tests/integration/test_session_bootstrap.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `--collect-only -q` collects test_connect_pat_flow, test_sync_202_poll, test_user_id_collision, test_soft_delete_evicts, test_no_cross_profile_leakage, test_single_profile_auto_binds, test_binding_independent_of_admin without collection errors.
    - test_sse_per_profile.py asserts status 403 on mismatch and 400 on unbound (grep for `403` and `400`).
    - test_session_bootstrap.py references a browse-binding cookie name distinct from `gruvax_session`.
    - All tests are RED against current code.
  </acceptance_criteria>
  <done>Three integration test files exist, collect cleanly, and are RED against current code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → DB | Tests connect to the shared dev Postgres; fixtures must clean up rows they insert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Tampering | second_profile fixture leaves orphan rows | mitigate | Fixture teardown DELETEs/soft-deletes the seeded profile so the suite stays order-independent (project memory: integration_test_harness). |
| T-02-00-SC | Tampering | npm/pip installs | accept | No package installs in this plan (RESEARCH §Package Legitimacy Audit: none new). |

No production code paths are introduced here, so no ASVS-relevant runtime threats apply to this plan.
</threat_model>

<verification>
- `uv run pytest --collect-only -q tests/integration/test_migrate_0010.py tests/unit/test_cache_registry.py tests/unit/test_profile_state_registry.py tests/integration/test_profile_manager_api.py tests/integration/test_sse_per_profile.py tests/integration/test_session_bootstrap.py` collects all named tests with zero collection errors.
- Running the suite shows the new tests RED (not accidentally passing).
- `grep -c "def second_profile" tests/conftest.py` returns 1.
</verification>

<success_criteria>
All six RESEARCH §Wave 0 Gap test files exist, collect cleanly, and fail against current code; the `second_profile` two-profile fixture exists for SLO/leakage tests.
</success_criteria>

<output>
Create `.planning/phases/02-multi-profile-migration-profile-manager/02-00-SUMMARY.md` when done.
</output>
