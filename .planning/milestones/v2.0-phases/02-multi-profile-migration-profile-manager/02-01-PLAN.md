---
phase: 02-multi-profile-migration-profile-manager
plan: 01
type: execute
wave: 1
depends_on: ["02-00"]
files_modified:
  - migrations/versions/0010_profile_id_not_null.py
files_modified_test:
  - tests/integration/test_migrate_0010.py
autonomous: true
requirements: [PROF-04]
must_haves:
  truths:
    - "After `alembic upgrade head`, profile_id is NOT NULL on cube_boundaries, settings, record_stats, segment_overrides, and boundary_history (SC#3)."
    - "admin_sessions.profile_id and idempotency_keys.profile_id remain nullable (SC#3 — Pitfall 5/6)."
    - "The 4 composite-PK tables include profile_id as the leading PK column (SC#3, D2-discretion)."
    - "The full `upgrade head → downgrade base → upgrade head` round-trip is clean (v1.0 CI invariant carries over)."
    - "v1 data backfilled to the default profile in 0009 survives the tighten with no NULL-violation failures (PROF-03 carry-forward + PROF-04)."
  artifacts:
    - path: "migrations/versions/0010_profile_id_not_null.py"
      provides: "PROF-04 NOT NULL + composite-PK migration with full downgrade"
      contains: "down_revision: str | None = \"0009\""
      min_lines: 80
  key_links:
    - from: "migrations/versions/0010_profile_id_not_null.py"
      to: "gruvax.cube_boundaries / settings / record_stats / segment_overrides / boundary_history"
      via: "op.execute(SET NOT NULL) on each of the 5 data tables"
      pattern: "ALTER COLUMN profile_id SET NOT NULL"
---

<objective>
Write Alembic migration 0010 that tightens the nullable `profile_id` column added by 0009 to NOT NULL on exactly the 5 per-profile *data* tables (`cube_boundaries`, `settings`, `record_stats`, `segment_overrides`, `boundary_history`), reconstructs the composite PRIMARY KEY on the 4 tables that need profile_id in their key, and leaves `admin_sessions` + `idempotency_keys` nullable (global/infra). The full upgrade↔downgrade round-trip must stay clean (the v1.0 CI invariant). This is PROF-04 and the dependency root for the rest of Phase 2.

Purpose: Make per-profile data isolation structurally enforced at the schema level (SC#3). Highest-risk P2 item (risk #3) — mitigated by 0009's nullable head-start + per-table staged commits during execution.
Output: `migrations/versions/0010_profile_id_not_null.py` (upgrade + full downgrade), turning the Wave 0 `test_migrate_0010.py` tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-profile-migration-profile-manager/02-RESEARCH.md
@.planning/phases/02-multi-profile-migration-profile-manager/02-PATTERNS.md
@migrations/versions/0009_v2_profiles_and_collection_cache.py

<interfaces>
<!-- Migration 0009 ground truth (verified by codebase read). -->

The 7 v1 tables 0009 added nullable profile_id to (all backfilled to the default UUID):
  admin_sessions, boundary_history, cube_boundaries, idempotency_keys, record_stats, segment_overrides, settings

P2 NOT NULL scope = 5 DATA tables: cube_boundaries, settings, record_stats, segment_overrides, boundary_history.
P2 leaves nullable = 2 INFRA tables: admin_sessions, idempotency_keys (Pitfall 5/6).

Existing PKs (from migrations 0001/0004/0005/0008 + RESEARCH §Critical Migration Discovery table):
  cube_boundaries     PK (unit_id, row, col)          → new PK (profile_id, unit_id, row, col)
  settings            PK (key)                        → new PK (profile_id, key)
  record_stats        PK (release_id)                 → new PK (profile_id, release_id)
  segment_overrides   PK (unit_id, row, col, label)   → new PK (profile_id, unit_id, row, col, label)
  boundary_history    PK (id BIGSERIAL) — UNCHANGED (profile_id stays a FK only, SET NOT NULL only)
  admin_sessions      PK (id UUID) — UNCHANGED, profile_id stays NULLABLE
  idempotency_keys    PK (key/id) — UNCHANGED, profile_id stays NULLABLE

Postgres default PK constraint name for raw-DDL tables = "{tablename}_pkey" (Pitfall 1).

0009 conventions to mirror exactly:
  - from __future__ import annotations; from alembic import op
  - revision = "0010"; down_revision = "0009"; branch_labels = None; depends_on = None
  - ALL SQL as module-level string constants; op.execute(_CONST) in upgrade()/downgrade(); never inline triple-quoted strings inside functions; never f-strings/concatenation.
  - Module docstring documents the 5-vs-2 scope split and the round-trip note.

Round-trip dependency (from test_migrate_0009.py): `downgrade base` goes all the way to 0001; tests/fixtures/legacy/synth_collection.sql must be seeded before the downgrade leg so 0009's v_collection re-creation resolves.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0010 upgrade — backfill verify, composite PKs, SET NOT NULL</name>
  <files>migrations/versions/0010_profile_id_not_null.py</files>
  <read_first>
    - migrations/versions/0009_v2_profiles_and_collection_cache.py (module-level _CONST + op.execute pattern; docstring style; _V1_TABLES tuple)
    - .planning/phases/02-multi-profile-migration-profile-manager/02-PATTERNS.md §migrations/versions/0010_profile_id_not_null.py (the per-table constant + upgrade/downgrade skeleton)
    - .planning/phases/02-multi-profile-migration-profile-manager/02-RESEARCH.md §Pattern 3 + §Critical Migration Discovery (exact PK columns) + Pitfall 1/5/6
  </read_first>
  <action>
    Create migrations/versions/0010_profile_id_not_null.py. Set `revision = "0010"`, `down_revision = "0009"`. Write a module docstring that states: scope = NOT NULL on cube_boundaries/settings/record_stats/segment_overrides/boundary_history; admin_sessions + idempotency_keys deliberately stay nullable (Pitfall 5/6 — infra not per-profile data); round-trip CI invariant carries over.

    Define module-level string constants (no f-strings, no concatenation at runtime):
    - Five `_VERIFY_NO_NULLS_<TABLE>` constants: a `DO $$ ... IF EXISTS (SELECT 1 FROM gruvax.<table> WHERE profile_id IS NULL) THEN RAISE EXCEPTION ... END IF; END $$` for cube_boundaries, settings, record_stats, segment_overrides, boundary_history (fail-fast guard against an incomplete 0009 backfill).
    - PK reconstruction for the 4 composite tables: `_DROP_PK_<T>` = `ALTER TABLE gruvax.<t> DROP CONSTRAINT <t>_pkey` and `_ADD_PK_<T>` = `ALTER TABLE gruvax.<t> ADD PRIMARY KEY (profile_id, ...)` with columns exactly: cube_boundaries (profile_id, unit_id, row, col); settings (profile_id, key); record_stats (profile_id, release_id); segment_overrides (profile_id, unit_id, row, col, label).
    - Five `_SET_NOT_NULL_<T>` = `ALTER TABLE gruvax.<t> ALTER COLUMN profile_id SET NOT NULL` for the 5 data tables. Use raw op.execute SQL strings — NOT op.alter_column(nullable=False) — to avoid Alembic's reflection path mis-handling FK constraints on composite-PK tables (RESEARCH anti-pattern).

    upgrade(): run the 5 backfill-verify guards first; then DROP+ADD the 4 composite PKs; then SET NOT NULL on the 5 data tables. Do NOT touch admin_sessions or idempotency_keys profile_id nullability.
  </action>
  <verify>
    <automated>just migrate && uv run pytest tests/integration/test_migrate_0010.py::test_not_null_on_five_data_tables tests/integration/test_migrate_0010.py::test_nullable_stays_on_two_infra_tables tests/integration/test_migrate_0010.py::test_composite_pks -x</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` exits 0; `information_schema.columns.is_nullable = 'NO'` for profile_id on cube_boundaries, settings, record_stats, segment_overrides, boundary_history.
    - `is_nullable = 'YES'` for profile_id on admin_sessions and idempotency_keys.
    - The 4 composite PKs include profile_id as the leading column (test_composite_pks GREEN).
    - `grep -v '^#' migrations/versions/0010_profile_id_not_null.py | grep -c 'op.alter_column'` returns 0 (raw SQL only).
  </acceptance_criteria>
  <done>upgrade() applies cleanly; the three structural migration tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 0010 downgrade + verify full round-trip</name>
  <files>migrations/versions/0010_profile_id_not_null.py</files>
  <read_first>
    - migrations/versions/0009_v2_profiles_and_collection_cache.py (downgrade() reverse-order convention + IF EXISTS defensive idempotence)
    - tests/integration/test_migrate_0010.py (the test_roundtrip_clean expectation + legacy-seed precondition)
    - .planning/phases/02-multi-profile-migration-profile-manager/02-PATTERNS.md §downgrade() pattern
  </read_first>
  <action>
    Add `_DROP_NOT_NULL_<T>` = `ALTER TABLE gruvax.<t> ALTER COLUMN profile_id DROP NOT NULL` for the 5 data tables, and PK-restore constants for the 4 composite tables: `_DROP_NEW_PK_<T>` (drop the {t}_pkey created in upgrade) + `_RESTORE_OLD_PK_<T>` (re-create the original PK: cube_boundaries (unit_id, row, col); settings (key); record_stats (release_id); segment_overrides (unit_id, row, col, label)).

    downgrade(): reverse order — DROP NOT NULL on the 5 data tables first, then DROP the 4 new composite PKs and ADD the original PKs back. This restores the exact nullable-column + original-PK state 0009 left, so `downgrade base` then `upgrade head` is idempotent. Because 0009's backfill seeded all rows to the default profile, the dropped NOT NULL leaves a valid nullable state with no orphan rows.

    Confirm the full CI round-trip gate passes via `just migrate-roundtrip` (it seeds tests/fixtures/legacy/synth_collection.sql before the downgrade leg, per test_migrate_0009.py's documented dependency).
  </action>
  <verify>
    <automated>just migrate-roundtrip && uv run pytest tests/integration/test_migrate_0010.py::test_roundtrip_clean -x</automated>
  </verify>
  <acceptance_criteria>
    - `just migrate-roundtrip` (`upgrade head → downgrade base → upgrade head`) exits 0.
    - test_migrate_0010.py::test_roundtrip_clean is GREEN.
    - After a full round-trip, the 5 data tables are again NOT NULL and the 2 infra tables still nullable (re-running Task 1's structural tests stays GREEN).
  </acceptance_criteria>
  <done>downgrade() fully reverses upgrade(); the round-trip gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → production DB | DDL runs with schema-owner privileges; a botched PK change could lock or corrupt per-profile data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Denial of Service | DROP/ADD PRIMARY KEY on data tables | accept | Household scale, no zero-downtime requirement; the ACCESS EXCLUSIVE lock is sub-second on a ~3k-row table. RESEARCH §Tertiary confirms lock-minimizing patterns are unnecessary here. |
| T-02-01-02 | Tampering | NULL profile_id slipping past the tighten | mitigate | Per-table `_VERIFY_NO_NULLS` DO-block RAISEs EXCEPTION before SET NOT NULL, failing the migration loudly instead of silently corrupting isolation (T-02-01 in VALIDATION map). |
| T-02-01-03 | Tampering | downgrade leaving an inconsistent PK/nullability state | mitigate | `just migrate-roundtrip` CI gate proves upgrade↔downgrade fidelity; test_roundtrip_clean asserts exit 0. |
| T-02-01-SC | Tampering | npm/pip installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: none new). |

No HIGH-severity unmitigated threats.
</threat_model>

<verification>
- `just migrate` then `just migrate-roundtrip` both exit 0.
- `uv run pytest tests/integration/test_migrate_0010.py -x` is fully GREEN (4 tests).
- Manual: `psql -c "\d gruvax.cube_boundaries"` shows PK (profile_id, unit_id, row, col) and profile_id NOT NULL; `\d gruvax.admin_sessions` shows profile_id still nullable.
</verification>

<success_criteria>
Migration 0010 promotes the 5 data tables' profile_id to NOT NULL with the 4 composite PKs reconstructed, leaves the 2 infra tables nullable, fully reverses on downgrade, and passes the round-trip CI gate (SC#3 / PROF-04).
</success_criteria>

<output>
Create `.planning/phases/02-multi-profile-migration-profile-manager/02-01-SUMMARY.md` when done. Record the actual PK constraint names observed (Pitfall 1 / Assumption A1) for downstream reference.
</output>
