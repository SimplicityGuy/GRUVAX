---
phase: 04-sync-polish-diagnostics
plan: 01
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - src/gruvax/sync/nightly.py
  - src/gruvax/app.py
  - src/gruvax/api/admin/settings.py
  - src/gruvax/api/admin/profiles.py
  - src/gruvax/api/session.py
autonomous: true
requirements: [SYN-01, SYN-02]
must_haves:
  truths:
    - "Nightly background sync fires at 03:00 server-local for all non-revoked, non-in-progress profiles via asyncio.create_task(_sync_loop()) started in lifespan; cadence (24h/12h/6h/off) is read each tick from the sync.cadence setting and changing it takes effect without restart"
    - "On startup, a one-shot catch-up sweep syncs any non-revoked profile staler than the cadence, and a one-shot purge sweep removes profile_collection rows for any soft-deleted profile that still has them"
    - "Soft-deleting a profile schedules a profile_collection purge; the profile row, per-profile config, and audit lineage (boundary_history etc.) are retained"
    - "GET /api/session returns needs_reauth for the bound profile derived from its app_token_revoked"
    - "sync.cadence is a global setting under the default-profile UUID, validated against {24h,12h,6h,off} via the existing whitelist, and persists across PUT /api/admin/settings"
  artifacts:
    - path: "src/gruvax/sync/nightly.py"
      provides: "next_fire_after(), now_local(), _read_sync_cadence(), _sync_loop(), _startup_catchup_sweep(), _startup_purge_sweep(), _purge_profile_collection()"
      contains: "def next_fire_after"
    - path: "src/gruvax/app.py"
      provides: "lifespan wiring: startup sweeps (one-shot await) + _sync_loop task registration (CR-01 strong-ref)"
      contains: "_sync_loop"
    - path: "src/gruvax/api/admin/settings.py"
      provides: "sync.cadence in _ALLOWED_SETTINGS_KEYS + key_map + _CADENCE_VALUES validation + GET/PUT handling"
      contains: "sync.cadence"
    - path: "src/gruvax/api/session.py"
      provides: "needs_reauth field on GET /api/session response"
      contains: "needs_reauth"
  key_links:
    - from: "src/gruvax/app.py"
      to: "src/gruvax/sync/nightly.py"
      via: "asyncio.create_task(_sync_loop(...)) + startup sweep awaits in lifespan"
      pattern: "create_task\\(_sync_loop"
    - from: "src/gruvax/sync/nightly.py"
      to: "gruvax.sync.profile_sync.sync_profile"
      via: "per-profile sequential sync call in loop + catch-up sweep"
      pattern: "await sync_profile"
    - from: "src/gruvax/sync/nightly.py"
      to: "gruvax.settings (sync.cadence row)"
      via: "_read_sync_cadence SELECT under DEFAULT_PROFILE_UUID each tick"
      pattern: "key = 'sync.cadence'"
    - from: "src/gruvax/api/admin/profiles.py"
      to: "src/gruvax/sync/nightly.py::_purge_profile_collection"
      via: "background_tasks.add_task in soft_delete_profile"
      pattern: "_purge_profile_collection"
---

<objective>
Backend sync autonomy + surfacing for Phase 4. Build the nightly scheduler, startup sweeps, the `sync.cadence` setting, the soft-delete purge, and the `needs_reauth` session field — and verify the already-wired `app_token_revoked` reset (D4-09) and `app_token_revoked` exposure (D4-07).

Purpose: SYN-01's net-new piece (nightly background scheduler) plus SYN-02's backend surfacing. Most infrastructure exists (advisory-lock `sync_profile`, state machine, 202+poll); this plan adds the orchestration around it.
Output: `src/gruvax/sync/nightly.py` (new), lifespan wiring in `app.py`, `sync.cadence` in `settings.py`, purge wiring in `profiles.py`, `needs_reauth` in `session.py`. NO new Alembic migration (head stays 0011 — verified: all columns exist on 0009; cadence is a settings row; purge is a DELETE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sync-polish-diagnostics/04-RESEARCH.md
@.planning/phases/04-sync-polish-diagnostics/04-PATTERNS.md
@.planning/phases/04-sync-polish-diagnostics/04-CONTEXT.md

<interfaces>
<!-- Symbols downstream tasks/plans consume. Extracted from codebase. -->

Existing (reuse — do not reimplement):
- `gruvax.sync.profile_sync.sync_profile(profile_id: str, app_state) -> ...` — advisory-locked staging-swap sync; sets last_sync_status/last_sync_error/app_token_revoked; refreshes per-profile caches inline.
- `gruvax.db.queries.DEFAULT_PROFILE_UUID` = "00000000-0000-0000-0000-000000000001" (global settings live under this UUID).
- app.py lifespan already sets `app.state.background_tasks = set()` and runs `_refresh_all_profiles_state()` (the 60s per-profile state loop) registered via CR-01 strong-ref (add + add_done_callback(discard)).
- settings.py: `_ALLOWED_SETTINGS_KEYS` frozenset (line 43), `key_map` in update_settings, GET helpers `_get_color/_get_int/_get_bool`, JSON-string storage `f'"{value}"'` pattern (auth.pin_hash precedent, line 280).
- profiles.py: `soft_delete_profile` (lines 593-641) already sets deleted_at, detaches devices, evicts registries; imports BackgroundTasks (line 34). `connect_pat` / `rotate_pat` already set `app_token_revoked = FALSE` (VERIFIED 04-RESEARCH lines 499-510). `list_profiles`/`get_profile` already return `app_token_revoked` (D4-07 already satisfied).
- session.py: `get_session` already fetches `profiles[]` (each with `app_token_revoked`) and resolves `bound_profile_id`.

VERIFIED FK FACT: profile_collection.profile_id REFERENCES profiles(id) ON DELETE CASCADE — but profiles are NEVER hard-deleted (D4-14), so the cascade never fires. No `change_log`/`change_sets` tables exist. Nothing references profile_collection. The purge DELETE is cascade-safe.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: nightly.py — DST scheduler, loop, sweeps, purge helper</name>
  <files>src/gruvax/sync/nightly.py</files>
  <read_first>
    - src/gruvax/app.py lines ~291-356 (EXACT analog: `_refresh_all_profiles_state` while-True loop, `app.state.background_tasks` set, CR-01 strong-ref registration, per-profile try/except, asyncio.sleep — COPY THIS STRUCTURE)
    - src/gruvax/sync/profile_sync.py (sync_profile signature + the advisory lock that guarantees correctness — the skip policy is efficiency on top)
    - src/gruvax/api/admin/settings.py lines 131-137 (settings SELECT under DEFAULT_PROFILE_UUID pattern)
    - 04-RESEARCH.md §Pattern 1 (next_fire_after — authoritative, fold=1, validated Python 3.14), §Pattern 2 (loop), §Pattern 3 (sweeps + purge), §"Critical Factual Confirmations" (no migration), §Pitfalls 1/2/3/4/6
    - 04-PATTERNS.md §"src/gruvax/sync/nightly.py" (imports, core loop, CR-01, skip SQL, cadence read, next_fire_after, purge helper code excerpts)
    - 04-CONTEXT.md D4-01..06, D4-11..14; tests/property/test_nightly_scheduler.py + tests/unit/test_nightly_scheduler.py (from 04-00 — these are your RED→GREEN target)
  </read_first>
  <behavior>
    - next_fire_after(now_aware, hour): always strictly future; successive hour=3 firings 22-26 wall hours apart; DST-safe via fold=1 (test_nightly_scheduler.py property tests).
    - _read_sync_cadence(pool): returns "24h"|"12h"|"6h"|"off"; absent row → "24h" (Pitfall 6).
    - skip policy: profile-selection SQL excludes app_token_revoked=TRUE and last_sync_status='in_progress' (D4-04; test_skip_policy).
    - off cadence: loop parks (sleeps ~60s) and calls sync_profile zero times (D4-06; test_cadence_off).
    - _purge_profile_collection(pool, profile_id): DELETEs only profile_collection rows for that profile via `%s::uuid`; commits.
  </behavior>
  <action>
    Create src/gruvax/sync/nightly.py with: `now_local()` (datetime.now().astimezone()); `next_fire_after(now_aware, hour=3)` exactly per 04-RESEARCH §Pattern 1 (D4-01, fold=1, candidate <= now → tomorrow); `_read_sync_cadence(pool)` SELECT-under-DEFAULT_PROFILE_UUID with "24h" fallback (D4-06); `_sync_loop(pool, app_state)` mirroring `_refresh_all_profiles_state` — outer `while True` with `try/except Exception` (NOT BaseException, so CancelledError propagates per Pitfall 1), read cadence each tick, `off` → sleep 60 and continue (D4-06), else compute fire-hours {"24h":[3],"12h":[3,15],"6h":[3,9,15,21]} (D4-03), sleep to soonest next_fire, then SELECT non-deleted/non-revoked/non-in_progress profiles ORDER BY created_at (D4-04 skip-policy SQL, parameterized `%s`), call `await sync_profile(pid, app_state)` per profile each in its own `try/except Exception` + `logger.warning` (per-profile isolation); `_startup_catchup_sweep(pool, app_state, cadence)` one-shot — sync stale non-revoked profiles (`last_sync_at IS NULL OR last_sync_at < NOW() - INTERVAL %s`) sequentially with the same skip policy (D4-02; mitigates sync-storm per §Pitfall 3 by reusing skip policy); `_startup_purge_sweep(pool)` one-shot — `SELECT DISTINCT p.id FROM profiles p JOIN profile_collection pc ON pc.profile_id=p.id WHERE p.deleted_at IS NOT NULL` then call `_purge_profile_collection` per id (D4-11/D4-12); `_purge_profile_collection(pool, profile_id)` — `DELETE FROM gruvax.profile_collection WHERE profile_id = %s::uuid` + commit (D4-13, parameterized — bandit B608). All logging uses `logger.info`/`logger.warning`; NEVER pass a PAT as a log field (dscg_* redactor already covers Bearer tokens — do not log raw secrets). DECISION: keep catch-up and purge as TWO separate one-shot sweeps (not combined) — independently testable and visible as distinct startup phases (§Pitfall 3); document this in a module docstring.
  </action>
  <verify>
    <automated>uv run pytest tests/property/test_nightly_scheduler.py tests/unit/test_nightly_scheduler.py -x -q && uv run ruff check src/gruvax/sync/nightly.py && uv run mypy --strict src/gruvax/sync/nightly.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/property/test_nightly_scheduler.py tests/unit/test_nightly_scheduler.py -x -q` PASSES (RED→GREEN for SYN-01 scheduler invariants, skip policy, off-parking, cadence fallback)
    - `grep -c "BaseException" src/gruvax/sync/nightly.py` returns 0 in the outer loop handler (only `except Exception`)
    - Purge DELETE uses `%s::uuid` placeholder — `grep -v '^#' src/gruvax/sync/nightly.py | grep -c "DELETE FROM gruvax.profile_collection WHERE profile_id = %s"` ≥ 1; no f-string SQL (`grep -c 'f"DELETE\|f.SELECT' src/gruvax/sync/nightly.py` returns 0)
    - `uv run ruff check src/gruvax/sync/nightly.py` and `uv run mypy --strict src/gruvax/sync/nightly.py` both clean
    - Module docstring documents the two-separate-sweeps decision
  </acceptance_criteria>
  <done>nightly.py exists; scheduler property/unit tests green; lint+type clean; purge is parameterized + cascade-safe.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Lifespan wiring + soft-delete purge + sync.cadence setting</name>
  <files>src/gruvax/app.py, src/gruvax/api/admin/profiles.py, src/gruvax/api/admin/settings.py</files>
  <read_first>
    - src/gruvax/app.py lines 291-356 (where to insert: after `_state_task` registration line ~346; COPY the CR-01 add + add_done_callback(discard) + _log_*_task_exc pattern verbatim)
    - src/gruvax/api/admin/profiles.py lines 34 (BackgroundTasks import), 306-311 (_DEFAULT_SETTINGS seed list — add sync.cadence here per Pitfall 6/A3), 471-484 + 565-577 (connect_pat/rotate_pat app_token_revoked=FALSE — VERIFY, no change), 593-641 (soft_delete_profile — add purge call)
    - src/gruvax/api/admin/settings.py lines 43-113 (_ALLOWED_SETTINGS_KEYS + the _BRIGHTNESS_KEYS/_COLOR_KEYS/_BOOL_KEYS frozenset + validation pattern), 226 (key_map), 280 (auth.pin_hash JSON-string storage), 141-183 (GET helpers)
    - 04-PATTERNS.md §"settings.py" + §"profiles.py" + §"app.py" (exact insertion points + code excerpts), §Shared Patterns (CR-01, parameterized SQL, settings frozenset)
    - 04-CONTEXT.md D4-06, D4-09, D4-11, D4-13; tests/integration/sync/test_purge.py + tests/integration/api/test_admin_settings.py (from 04-00 — RED→GREEN targets)
  </read_first>
  <action>
    app.py: in lifespan, immediately after the existing `_state_task` registration, add — `cadence = await _read_sync_cadence(pool)`; `await _startup_catchup_sweep(pool, app.state, cadence)` (one-shot, D4-02); `await _startup_purge_sweep(pool)` (one-shot, D4-11); then `_sync_task = asyncio.create_task(_sync_loop(pool, app.state))` registered with the CR-01 strong-ref pattern (`app.state.background_tasks.add(_sync_task)` + `_sync_task.add_done_callback(app.state.background_tasks.discard)`) and the `_log_*_task_exc` done-callback. Import the four symbols from `gruvax.sync.nightly`. Use the `# ── N. Description ── (D4-NN)` section-comment style of surrounding lifespan blocks.

    profiles.py — Change 1 (purge, D4-11/D4-13): add `background_tasks: BackgroundTasks` parameter to `soft_delete_profile`; after registries are evicted and before the `return`, call `background_tasks.add_task(_purge_profile_collection, pool=request.app.state.db_pool, profile_id=str(uid))` and `logger.info("profile soft-deleted: id=%s; purge scheduled", str(uid))`. Import `_purge_profile_collection` from `gruvax.sync.nightly`. Change 2 (D4-09 + D4-07 VERIFY, no edit): confirm connect_pat (471-484) and rotate_pat (565-577) both set `app_token_revoked = FALSE`, and list_profiles/get_profile both return `app_token_revoked` — record "verified present, no change" in the SUMMARY. Change 3 (seed, A3/Pitfall 6): add `("sync.cadence", '"24h"')` to the `_DEFAULT_SETTINGS` list (line ~306) so it is seeded under the default profile.

    settings.py: add `"sync.cadence"` to `_ALLOWED_SETTINGS_KEYS`; add `_CADENCE_VALUES = frozenset({"24h","12h","6h","off"})`; add `"sync_cadence": "sync.cadence"` to `key_map`; add a validation `elif db_key == "sync.cadence":` branch that raises 422 `{"type":"invalid_cadence",...}` when value ∉ _CADENCE_VALUES and stores `json_value = f'"{value}"'` (same as auth.pin_hash); add `"sync_cadence"` to the GET return dict via a string-from-JSON read defaulting to `"24h"` (D4-06). Cadence is GLOBAL under DEFAULT_PROFILE_UUID — same composite-PK pattern as auth.pin_hash.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/sync/test_purge.py tests/integration/api/test_admin_settings.py -x -q && uv run ruff check src/gruvax/app.py src/gruvax/api/admin/profiles.py src/gruvax/api/admin/settings.py && uv run mypy --strict src/gruvax/app.py src/gruvax/api/admin/profiles.py src/gruvax/api/admin/settings.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/sync/test_purge.py tests/integration/api/test_admin_settings.py -x -q` PASSES (purge self-clearing + audit untouched + rotate-clears-revoked + sync.cadence persist/validate)
    - app.py registers _sync_loop with strong-ref: `grep -c "create_task(_sync_loop\|background_tasks.add(_sync_task" src/gruvax/app.py` ≥ 1 and `add_done_callback` present
    - settings.py: `grep -c "sync.cadence" src/gruvax/api/admin/settings.py` ≥ 2 (whitelist + key_map/validation) and `_CADENCE_VALUES` defined
    - profiles.py: `soft_delete_profile` takes `background_tasks: BackgroundTasks` and calls `_purge_profile_collection` (`grep -c "_purge_profile_collection" src/gruvax/api/admin/profiles.py` ≥ 1); SUMMARY records D4-09/D4-07 verified-no-change
    - ruff + mypy --strict clean on all three files
  </acceptance_criteria>
  <done>Lifespan runs both startup sweeps then the nightly loop; soft-delete schedules the purge; sync.cadence settable/validated/persisted; D4-09/D4-07 verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: needs_reauth on GET /api/session</name>
  <files>src/gruvax/api/session.py</files>
  <read_first>
    - src/gruvax/api/session.py (get_session: where profiles[] is assembled and bound_profile_id resolved, the content dict — add needs_reauth before it; app_token_revoked already in the active-profiles SELECT)
    - 04-PATTERNS.md §"src/gruvax/api/session.py" (exact derivation code) and 04-RESEARCH.md §Pattern 4 (D4-08 — derive from bound profile's app_token_revoked; no new DB query)
    - 04-CONTEXT.md D4-08, D4-10; tests/unit/test_session.py::test_needs_reauth (from 04-00 — RED→GREEN target)
  </read_first>
  <behavior>
    - GET /api/session returns needs_reauth=true when the bound profile has app_token_revoked=TRUE; false when bound profile is healthy; false when no profile is bound (test_needs_reauth).
  </behavior>
  <action>
    In `get_session`, after `bound_profile_id` is resolved and `profiles` assembled, derive `needs_reauth` server-side (D4-08): default False; if `bound_profile_id`, find the matching profile in `profiles` and set `needs_reauth = bound_profile.get("app_token_revoked", False)`. Add `"needs_reauth": needs_reauth` to the `content` dict. No new DB query (app_token_revoked already fetched). DECISION: surface as an explicit `GET /api/session` field per the locked D4-08 decision (not frontend-only derivation) — this is the criterion's named contract; note in SUMMARY that the frontend MAY additionally derive locally but the session field is authoritative.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_session.py::test_needs_reauth -x -q && uv run ruff check src/gruvax/api/session.py && uv run mypy --strict src/gruvax/api/session.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_session.py::test_needs_reauth -x -q` PASSES
    - `grep -c "needs_reauth" src/gruvax/api/session.py` ≥ 2 (derivation + content dict key)
    - No new DB query added (no new `cur.execute(` calls in get_session — grep diff confirms)
    - ruff + mypy --strict clean
  </acceptance_criteria>
  <done>GET /api/session returns needs_reauth for the bound profile; D4-08 satisfied; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nightly loop → discogsography API | Server-side outbound HTTP carrying the Fernet-decrypted PAT as a Bearer token. |
| admin client → PUT /api/admin/settings | PIN-gated write of sync.cadence (untrusted value crosses here). |
| soft-delete caller → purge DELETE | profile_id flows into a DELETE statement. |
| kiosk/SPA → GET /api/session | needs_reauth exposed per bound profile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | _sync_loop / catch-up sweep logs | mitigate | Existing `dscg_*` structlog redactor covers Bearer tokens; nightly log calls pass only profile_id + status, never the PAT (acceptance criterion forbids raw-secret log fields). |
| T-04-01-02 | Tampering | purge DELETE on profile_collection | mitigate | Parameterized `%s::uuid` only (bandit B608); profile_id is a server-derived UUID, never user-supplied at call sites. VERIFIED no FK cascade into audit tables. |
| T-04-01-03 | Tampering | sync.cadence settings write | mitigate | Validated against `_CADENCE_VALUES` frozenset BEFORE any SQL; stored as JSON string; whitelist `_ALLOWED_SETTINGS_KEYS` gate. |
| T-04-01-04 | Information Disclosure | needs_reauth on /api/session | mitigate | Derived only from the BOUND profile (session/device-scoped); no cross-profile leakage — needs_reauth reflects only the caller's bound profile, not all profiles. |
| T-04-01-05 | Elevation of Privilege | stale app_token_revoked | mitigate | needs_reauth derived from the live per-request profiles read, not an app.state cache. |
| T-04-01-06 | Denial of Service | startup catch-up sync-storm vs rate limit | accept | Sequential per-profile iteration + skip policy (≤4 profiles ≈ 60 req/min); existing DiscogsographyClient 429+Retry-After backoff is the safety valve (04-RESEARCH rate-limit section). |
| T-04-01-SC | Tampering | npm/pip installs | accept | Zero new packages (04-RESEARCH §Package Legitimacy Audit: none). |
</threat_model>

<verification>
- Alembic round-trip unaffected (NO new migration — head stays 0011): `uv run pytest tests/integration/test_migrate_0011.py -x -q` still green.
- Nightly scheduler property + unit tests green; purge + cadence + needs_reauth integration/unit tests green.
- ruff + mypy --strict clean across all five files.
- No raw PAT in any log field added by this plan.
</verification>

<success_criteria>
- `uv run pytest tests/property/test_nightly_scheduler.py tests/unit/test_nightly_scheduler.py tests/unit/test_session.py tests/integration/sync/test_purge.py tests/integration/api/test_admin_settings.py -x -q` all green.
- Full suite (`uv run pytest tests/ -q --tb=short`) green — no Phase 1/2/3 regressions (run after this wave per MEMORY: full sequential suite catches contract regressions).
- Nightly loop registered in lifespan with CR-01 strong-ref; cadence read each tick; off parks.
- Purge cascade-safe (profile_collection only); D4-09/D4-07 verified-no-change recorded.
</success_criteria>

<output>
Create `.planning/phases/04-sync-polish-diagnostics/04-01-SUMMARY.md` when done.
</output>
