---
phase: 03-devices-pairing
plan: 01
type: execute
wave: 2
depends_on: ["03-00"]
files_modified:
  - migrations/versions/0011_devices_and_pairing_codes.py
  - src/gruvax/auth/sessions.py
autonomous: true
requirements: [DEV-01]
user_setup: []

must_haves:
  truths:
    - "gruvax.devices and gruvax.pairing_codes exist after migration 0011 with the spec-locked indexes"
    - "Alembic upgrade -> downgrade -> upgrade round-trip is clean (v1.0 CI invariant)"
    - "The fingerprint cookie is HttpOnly + SameSite=Strict + max_age >= 30 days (NOT a session cookie)"
  artifacts:
    - path: "migrations/versions/0011_devices_and_pairing_codes.py"
      provides: "devices + pairing_codes tables + partial-unique + plain fingerprint indexes"
      contains: "down_revision"
    - path: "src/gruvax/auth/sessions.py"
      provides: "issue_fingerprint_cookie / get_fingerprint / clear_fingerprint_cookie + FINGERPRINT_COOKIE constant"
      contains: "def issue_fingerprint_cookie"
  key_links:
    - from: "src/gruvax/auth/sessions.py"
      to: "secrets.token_urlsafe"
      via: "opaque value generation"
      pattern: "token_urlsafe\\(32\\)"
    - from: "migrations/versions/0011_devices_and_pairing_codes.py"
      to: "gruvax.profiles"
      via: "ON DELETE SET NULL FK"
      pattern: "ON DELETE SET NULL"
---

<objective>
The data + auth foundation for Phase 3: Alembic migration 0011 creating the `devices` and `pairing_codes` tables with the spec-locked indexes, and the HttpOnly fingerprint cookie helpers in `auth/sessions.py`. Everything downstream (endpoints, deps, SSE, frontend, reboot test) builds on these two artifacts.

Purpose: Establish the device persistence model (DEV-01) and the credential-equivalent fingerprint cookie with disk-persistence semantics (max_age) that the reboot requirement depends on.
Output: Migration 0011 (round-trip clean) + three new cookie helpers + two new module constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-devices-pairing/03-RESEARCH.md
@.planning/phases/03-devices-pairing/03-PATTERNS.md

@migrations/versions/0010_profile_id_not_null.py
@src/gruvax/auth/sessions.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 0011 — devices + pairing_codes</name>
  <files>migrations/versions/0011_devices_and_pairing_codes.py</files>
  <read_first>
    - migrations/versions/0010_profile_id_not_null.py (header convention; revision/down_revision; module-level SQL string constants; op.execute pattern; downgrade fully reverses upgrade)
    - .planning/phases/03-devices-pairing/03-PATTERNS.md (migration block — exact DDL constants _CREATE_DEVICES / _CREATE_PAIRING_CODES / the four index constants / upgrade()/downgrade())
    - .planning/phases/03-devices-pairing/03-RESEARCH.md (Pitfall 5 + Open Question 2 — non-partial fingerprint index rationale; Data Model index names)
    - tests/integration/test_migrate_0011.py (the RED round-trip test this turns GREEN)
  </read_first>
  <behavior>
    - After upgrade head: gruvax.devices and gruvax.pairing_codes tables exist
    - devices.profile_id references gruvax.profiles(id) ON DELETE SET NULL
    - pairing_codes.code is CHAR(4) PRIMARY KEY; expires_at NOT NULL; consumed_at nullable
    - Indexes exist: idx_devices_fingerprint_active (UNIQUE, WHERE revoked_at IS NULL), idx_devices_fingerprint (plain, for revoke-guard lookups of revoked rows), idx_devices_profile_active (UNIQUE, WHERE revoked_at IS NULL AND profile_id IS NOT NULL), idx_pairing_codes_expires
    - downgrade to 0010 drops both tables + all four indexes
    - Round-trip upgrade -> downgrade -> upgrade exits 0
  </behavior>
  <action>Create migration 0011 with `revision = "0011"`, `down_revision = "0010"`, `branch_labels = None`, `depends_on = None`, `from __future__ import annotations`, `from alembic import op`. Define ALL SQL as module-level string constants (string concatenation only — never f-strings, never inline triple-quoted strings inside functions; bandit B608). Tables: `gruvax.devices` (id UUID PK DEFAULT gen_random_uuid(), fingerprint TEXT NOT NULL, profile_id UUID REFERENCES gruvax.profiles(id) ON DELETE SET NULL, display_name TEXT NOT NULL DEFAULT 'Unnamed device', revoked_at TIMESTAMPTZ, last_seen_at TIMESTAMPTZ, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()); `gruvax.pairing_codes` (code CHAR(4) PRIMARY KEY, fingerprint TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), expires_at TIMESTAMPTZ NOT NULL, consumed_at TIMESTAMPTZ). Indexes per the PATTERNS block: idx_devices_fingerprint_active (UNIQUE partial WHERE revoked_at IS NULL), idx_devices_fingerprint (plain non-partial — RESEARCH Pitfall 5), idx_devices_profile_active (UNIQUE partial WHERE revoked_at IS NULL AND profile_id IS NOT NULL), idx_pairing_codes_expires. `upgrade()` op.execute each create/index in dependency order; `downgrade()` drops indexes then tables in reverse, using `DROP INDEX IF EXISTS gruvax.<name>` and `DROP TABLE IF EXISTS gruvax.<name>`. Match the 0010 narrative-docstring header style.</action>
  <verify>
    <automated>pytest tests/integration/test_migrate_0011.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head && alembic downgrade -1 && alembic upgrade head` exits 0
    - tests/integration/test_migrate_0011.py passes
    - migration file contains `down_revision: str | None = "0010"` and `ON DELETE SET NULL`
    - No f-strings in any SQL constant (grep for `f"` / `f'` in the file returns nothing inside op.execute args)
  </acceptance_criteria>
  <done>Migration 0011 creates both tables + four indexes; round-trip clean; RED migration test now GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add HttpOnly fingerprint cookie helpers to auth/sessions.py</name>
  <files>src/gruvax/auth/sessions.py</files>
  <read_first>
    - src/gruvax/auth/sessions.py (BROWSE_BINDING_COOKIE constant lines ~42-44; set_browse_binding_cookie / clear_browse_binding_cookie lines ~205-258; confirm `import secrets` already present)
    - .planning/phases/03-devices-pairing/03-PATTERNS.md (sessions.py block — exact new constants + the three new function shapes + CR-04 delete-attribute-match invariant)
    - .planning/phases/03-devices-pairing/03-RESEARCH.md (Pattern 1 — token_urlsafe(32) entropy; Pitfall 1 max_age disk-persistence; Pitfall 7 never log the value)
    - tests/unit/test_fingerprint_cookie.py (the RED unit test this turns GREEN)
  </read_first>
  <behavior>
    - issue_fingerprint_cookie sets cookie with httponly=True, samesite="strict", secure=<param>, max_age=FINGERPRINT_MAX_AGE; returns a secrets.token_urlsafe(32) value (len >= 40)
    - get_fingerprint returns request.cookies.get(FINGERPRINT_COOKIE) or None
    - clear_fingerprint_cookie's delete_cookie attributes (path, httponly, samesite, secure) match set_cookie exactly (CR-04)
  </behavior>
  <action>Add immediately after the existing BROWSE_BINDING_COOKIE block: `FINGERPRINT_COOKIE = "gruvax_device_fp"` and `FINGERPRINT_MAX_AGE = 30 * 24 * 3600` with comments noting HttpOnly=True (JS must never read it — session-equivalent secret), max_age required for Chromium disk persistence (Pitfall 1), Secure=False for LAN HTTP until TLS. Add three functions mirroring set_browse_binding_cookie / clear_browse_binding_cookie: `issue_fingerprint_cookie(response: Response, secure: bool = False) -> str` (generate `secrets.token_urlsafe(32)`, call response.set_cookie with FINGERPRINT_COOKIE, the value, httponly=True, samesite="strict", secure=secure, max_age=FINGERPRINT_MAX_AGE; return the raw token); `get_fingerprint(request: Request) -> str | None` (return request.cookies.get(FINGERPRINT_COOKIE)); `clear_fingerprint_cookie(response: Response, secure: bool = False) -> None` (response.delete_cookie with path="/", httponly=True, samesite="strict", secure=secure — attributes MUST match set_cookie per CR-04). NEVER log the fingerprint value (Pitfall 7). Do not couple any of these to the browse-binding cookie (D3-04).</action>
  <verify>
    <automated>pytest tests/unit/test_fingerprint_cookie.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - sessions.py contains `def issue_fingerprint_cookie(` and `FINGERPRINT_COOKIE = "gruvax_device_fp"`
    - tests/unit/test_fingerprint_cookie.py passes
    - issue_fingerprint_cookie call uses `httponly=True`, `samesite="strict"`, `max_age=FINGERPRINT_MAX_AGE`
    - No `logger`/`logging` call in the new code references the fingerprint value
  </acceptance_criteria>
  <done>Three cookie helpers + two constants added; RED unit test now GREEN; fingerprint never logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk browser → API | fingerprint cookie crosses on every request; credential-equivalent |
| API → Postgres | device + pairing rows persisted; FK to profiles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | fingerprint cookie value | mitigate | secrets.token_urlsafe(32) = 256-bit CSPRNG opaque value (security touchpoint #3); HttpOnly so JS cannot read/forge it |
| T-03-02 | Information Disclosure | fingerprint value in logs | mitigate | Never logged plaintext (Pitfall 7); credential-equivalent redaction discipline matches PAT handling |
| T-03-03 | Tampering | session-cookie downgrade losing persistence | mitigate | max_age=30d enforced + asserted by unit test; Chromium writes to disk only with max_age (Pitfall 1) |
| T-03-04 | Elevation of Privilege | revoked-device fingerprint lookup misses revoked rows | mitigate | non-partial idx_devices_fingerprint added so the revoke guard finds revoked rows (Pitfall 5) |
</threat_model>

<verification>
- Migration round-trip exits 0; both tables + four indexes present after upgrade, absent after downgrade.
- Cookie unit test passes; attributes are HttpOnly + SameSite=Strict + max_age >= 30d.
- `pytest tests/unit/test_fingerprint_cookie.py tests/integration/test_migrate_0011.py -x -q` green.
</verification>

<success_criteria>
- DEV-01 data + cookie foundation complete.
- Alembic round-trip clean (CI invariant preserved).
- Fingerprint cookie persists to disk (max_age) and is never logged.
</success_criteria>

<output>
Create `.planning/phases/03-devices-pairing/03-01-SUMMARY.md` when done.
</output>
