---
phase: 01-walking-skeleton-api-client-single-profile-sync
plan: 05
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/gruvax/api/health.py
  - src/gruvax/app.py
  - frontend/src/api/types.ts
  - services/fake-discogsography/Dockerfile
  - services/fake-discogsography/server.py
  - services/fake-discogsography/seed.yaml
  - compose.yaml
  - justfile
  - tests/integration/api/test_health.py
  - tests/integration/test_compose_smoke.py
requirements: [SYN-02]
autonomous: true
tags: [health, lifespan, compose, fake-discogsography, init-sync]

must_haves:
  truths:
    - "/api/health response includes `discogsography_api_check` field with value strictly in {'ok','failed','stale'} (D-13)."
    - "/api/health response does NOT include the legacy `discogsography_view_check` field."
    - "`discogsography_api_check = 'ok'` iff last_sync_status='ok' AND app_token_revoked=FALSE."
    - "`discogsography_api_check = 'ok'` when last_sync_status='in_progress' (a sync currently running is healthy state, not stale; the 5-min watchdog in Plan 03 marks hung syncs as 'failed')."
    - "`discogsography_api_check = 'failed'` iff last_sync_status='failed' OR app_token_revoked=TRUE."
    - "`discogsography_api_check = 'stale'` iff last_sync_at IS NULL OR now() - last_sync_at > 24h (and status is NOT 'in_progress')."
    - "Overall `status` field is 'degraded' when db_ok=FALSE OR discogsography_api_check != 'ok' (preserves v1 contract semantics)."
    - "`sync_age_seconds` field is now derived from `now() - profiles.last_sync_at` for the default profile (NOT max(v_collection.synced_at))."
    - "Lifespan startup probe targets `gruvax.profile_collection` (NOT `gruvax.v_collection`); never crashes — log + flip flag + continue (CARRIES OVER v1 pattern)."
    - "`fake-discogsography` Compose sibling service serves the contract endpoints, importing from src/gruvax/_internal/fake_discogsography.py (canonical — D-15 single-module mandate; NO duplication, NO `just sync-fake` drift guard)."
    - "`init-sync` one-shot container runs `gruvax-sync default` AFTER `gruvax-api` is healthy and ONLY IF profile_collection is empty for the default profile (D-16 verbatim — `idempotent — skips if already populated`)."
    - "`just compose-smoke` recipe brings up gruvax-api + fake-discogsography + init-sync, verifies init-sync exit code 0 + fake-discogsography returns rows, and tears down with `docker compose down -v`; runs in CI."
    - "TypeScript HealthResponse type updated: discogsography_view_check renamed to discogsography_api_check, union widened to 'ok'|'failed'|'stale'."
  artifacts:
    - path: "src/gruvax/api/health.py"
      provides: "/api/health handler with renamed field + state widening per D-13"
    - path: "src/gruvax/app.py"
      provides: "Lifespan probe targets profile_collection + background task reads profiles.last_sync_at"
    - path: "services/fake-discogsography/Dockerfile"
      provides: "Minimal single-stage fake-discogsography container image — installs gruvax package and imports the canonical module"
    - path: "services/fake-discogsography/server.py"
      provides: "Uvicorn entry script importing gruvax._internal.fake_discogsography.create_fake_app with seed loaded from seed.yaml"
    - path: "services/fake-discogsography/seed.yaml"
      provides: "~3000-row synthetic seed (generated by Plan 01-00's generate_synth_data.py — same source as tests/fixtures/synth_profile_collection.sql)"
    - path: "compose.yaml"
      provides: "fake-discogsography sibling service + init-sync container with idempotency precheck; api depends on fake-discogsography healthy"
    - path: "justfile"
      provides: "compose-smoke recipe — full up/verify/down cycle for CI"
  key_links:
    - from: "src/gruvax/api/health.py"
      to: "app.state.default_profile_last_sync_at + default_profile_last_sync_status + default_profile_app_token_revoked"
      via: "Background-task-populated cached attributes (no live DB probe — preserves v1 D-13 invariant)"
      pattern: "default_profile_last_sync"
    - from: "src/gruvax/app.py::_refresh_default_profile_state"
      to: "gruvax.profiles.last_sync_at"
      via: "60-second polling background task"
      pattern: "gruvax\\.profiles"
    - from: "services/fake-discogsography/server.py"
      to: "src/gruvax/_internal/fake_discogsography.py (canonical)"
      via: "from gruvax._internal.fake_discogsography import create_fake_app"
      pattern: "from gruvax\\._internal\\.fake_discogsography"
    - from: "compose.yaml::init-sync"
      to: "POST /api/admin/profiles/{default-uuid}/sync"
      via: "idempotency precheck (psql count) then `echo $ADMIN_PIN | gruvax-sync --profile default`"
      pattern: "gruvax-sync"
---

<objective>
Finish the wire-side of the data-source swap from v_collection → profile_collection: rename the `/api/health` field per D-13 and re-derive its three states from cached profile state; replace the lifespan v_collection probe with a profile_collection probe; replace the 60s `_refresh_sync_age` background task body with reads against `gruvax.profiles.last_sync_at`; rename the TypeScript HealthResponse field; add the Compose `fake-discogsography` sibling + `init-sync` one-shot per D-16 (idempotent precheck — skips if already populated) so `docker compose up` does a real end-to-end sync the FIRST time and a no-op on subsequent boots; package the canonical fake module (D-15 — NO duplication into services/, NO drift guard) and ship a `just compose-smoke` CI gate.

Purpose: Land all "edge of the system" rewires in one focused plan — health endpoint, lifespan probes, frontend type, Compose, and the smoke gate. Plan 06 still has to rewire the queries.py / collection_snapshot.py read paths; that's the heaviest rewire and gets its own plan to keep this one focused.

Output: 4 modified Python files + 1 TypeScript file + 3 new infra files + compose.yaml updates + justfile recipe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-UI-SPEC.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-VALIDATION.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-00-PLAN.md
@src/gruvax/api/health.py
@src/gruvax/app.py
@src/gruvax/_internal/fake_discogsography.py
@compose.yaml
@justfile

<interfaces>
<!-- /api/health response shape per D-13 + UI-SPEC -->

GET /api/health  (always 200)
Response JSON:
  {
    "status": "ok" | "degraded",
    "db": "ok" | "error",
    "discogsography_api_check": "ok" | "failed" | "stale",   # RENAMED + WIDENED (D-13)
    "mqtt": "ok" | "error",
    "sync_age_seconds": float | null,                         # source swapped; field name unchanged
    "started_at": "<iso8601>",
    "uptime_seconds": float,
    ... (any other v1 fields preserved as-is)
  }

<!-- app.state attributes for the new background task to populate -->

app.state.default_profile_last_sync_at: datetime | None
app.state.default_profile_last_sync_status: str | None   # 'ok' | 'failed' | 'in_progress' | None
app.state.default_profile_app_token_revoked: bool
app.state.sync_age_seconds: float | None                 # derived from default_profile_last_sync_at (kept as v1 attr name)

<!-- Lifespan probe replacement -->

REMOVE (in app.py lifespan step 2):
  SELECT 1 FROM gruvax.v_collection LIMIT 1
  app.state.discogsography_view_ok = True/False

ADD:
  SELECT count(*) FROM gruvax.profile_collection WHERE profile_id = %s::uuid LIMIT 1   # using DEFAULT_PROFILE_UUID
  app.state.profile_collection_ready = True/False
  (Failure path: same "log + flip flag + continue" pattern as v1's v_collection probe — never crash.)

<!-- Background task replacement (app.py step 1c — _refresh_sync_age) -->

OLD body:
  SELECT EXTRACT(EPOCH FROM (now() - max(synced_at))) FROM gruvax.v_collection

NEW body (per D-13 + PATTERNS §Shared Patterns "Background-task lifecycle"):
  SELECT last_sync_at, last_sync_status, app_token_revoked
  FROM gruvax.profiles WHERE id = %s::uuid AND deleted_at IS NULL
Then:
  app.state.default_profile_last_sync_at = row[0]
  app.state.default_profile_last_sync_status = row[1]
  app.state.default_profile_app_token_revoked = bool(row[2])
  app.state.sync_age_seconds = (now - row[0]).total_seconds() if row[0] else None

<!-- D-13 state derivation precedence — updated per Warning #4 RESOLUTION (in_progress → ok) -->

# Precedence:
#   failed > stale > ok
# `in_progress` maps to 'ok' (an active sync is healthy state; if it hangs >5min
# Plan 03's watchdog marks it 'failed'). NEVER invent a 'stale' fallback for
# 'in_progress'.
if last_sync_status == 'failed' or token_revoked:
    api_check = 'failed'
elif last_sync_status == 'in_progress':
    api_check = 'ok'   # active sync is healthy
elif last_sync_at is None or (now - last_sync_at) > timedelta(hours=24):
    api_check = 'stale'
else:  # last_sync_status == 'ok'
    api_check = 'ok'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /api/health field rename + state-widening derivation (in_progress → ok)</name>
  <files>
    src/gruvax/api/health.py,
    frontend/src/api/types.ts,
    tests/integration/api/test_health.py
  </files>
  <read_first>
    src/gruvax/api/health.py (full file — current discogsography_view_check derivation lines 34-66),
    frontend/src/api/types.ts (HealthResponse type definition; if missing, grep frontend/src for the type),
    tests/integration/api/test_health.py (existing v1 health tests — extend, do not rewrite),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md (D-13 verbatim),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-UI-SPEC.md (§Scope row 1 + §Color §/api/health state mapping — the three-state contract),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md (§12 health.py modification)
  </read_first>
  <behavior>
    - Test 1 (field rename): GET /api/health → response.json() has key "discogsography_api_check" and does NOT have key "discogsography_view_check".
    - Test 2 (value enum): the value is one of {"ok","failed","stale"} only (assert with `in` against the set).
    - Test 3 (ok derivation): app.state.default_profile_last_sync_status='ok' AND default_profile_app_token_revoked=FALSE → discogsography_api_check == 'ok'.
    - Test 4 (failed by status): default_profile_last_sync_status='failed' AND revoked=FALSE → 'failed'.
    - Test 5 (failed by revoked): default_profile_last_sync_status='ok' AND revoked=TRUE → 'failed'.
    - Test 6 (stale by null): default_profile_last_sync_at=None (status NOT in_progress and NOT failed) → 'stale'.
    - Test 7 (stale by age): default_profile_last_sync_at=now() - 25h AND status='ok' → 'stale'.
    - Test 8 (in_progress → ok per Warning #4 fix): default_profile_last_sync_status='in_progress' AND revoked=FALSE → discogsography_api_check == 'ok' (an active sync is healthy state; do NOT map to 'stale').
    - Test 9 (in_progress with token_revoked still → failed): default_profile_last_sync_status='in_progress' AND revoked=TRUE → 'failed' (token_revoked takes precedence).
    - Test 10 (overall degraded propagation): discogsography_api_check='failed' → top-level status='degraded'; api_check='ok' AND db='ok' AND mqtt='ok' → status='ok'.
    - Test 11 (sync_age_seconds source swap): app.state.sync_age_seconds is populated from `now() - default_profile_last_sync_at` (NOT max(v_collection.synced_at)). Assert by setting last_sync_at = now() - 60s; sync_age_seconds is in [55, 65] range.
    - Test 12 (TypeScript type renamed): `cat frontend/src/api/types.ts` shows `discogsography_api_check: 'ok' | 'failed' | 'stale'` and does NOT contain `discogsography_view_check`.
  </behavior>
  <action>
    Modify src/gruvax/api/health.py per PATTERNS §12. Preserve:
      - Module docstring "No live probe" rule (lines 35-43).
      - JSONResponse always-200 envelope shape.
      - Existing fields (db, mqtt, sync_age_seconds, started_at, uptime_seconds).

    Replace the `discogsography_view_check` derivation with the D-13 three-state derivation per UI-SPEC §/api/health state mapping table + Warning #4 RESOLUTION:
      ```
      last_sync_at = getattr(request.app.state, "default_profile_last_sync_at", None)
      last_sync_status = getattr(request.app.state, "default_profile_last_sync_status", None)
      token_revoked = getattr(request.app.state, "default_profile_app_token_revoked", True)
      now = datetime.now(UTC)

      # Precedence per D-13 + Warning #4 RESOLUTION:
      #   failed > stale > ok
      # 'in_progress' maps to 'ok' (active sync is healthy state — if hung >5min,
      # Plan 03's watchdog flips it to 'failed').
      if last_sync_status == 'failed' or token_revoked:
          api_check = 'ok' if False else 'failed'   # explicit for readability
          api_check = 'failed'
      elif last_sync_status == 'in_progress':
          api_check = 'ok'   # active sync == healthy
      elif last_sync_at is None or (now - last_sync_at) > timedelta(hours=24):
          api_check = 'stale'
      else:  # last_sync_status == 'ok' (or None with fresh last_sync_at)
          api_check = 'ok'
      ```
    Rename the response body field from `discogsography_view_check` to `discogsography_api_check`. Add a one-line code comment citing Warning #4 RESOLUTION (in_progress → ok mapping) for future readers.

    Update overall-status derivation: `overall = "ok" if db_ok and api_check == "ok" else "degraded"`.

    Locate `frontend/src/api/types.ts` (or the actual HealthResponse type location — grep `discogsography_view_check` across frontend/src/). Rename the field type member from `discogsography_view_check` to `discogsography_api_check` and widen the union from `'ok' | 'failed'` to `'ok' | 'failed' | 'stale'`. If no consumer in the v1.0 frontend reads this field (UI-SPEC §Component Inventory line 140 verified zero consumers), the rename is type-only and no further frontend code change is needed.

    Extend tests/integration/api/test_health.py with Tests 1-12. Use the existing health test fixture pattern (probably `client: AsyncClient` against the in-process app via LifespanManager — read the file first). For Tests 3-9, monkeypatch `app.state.default_profile_*` attributes inside the test before calling GET /api/health.

    For Test 12, do a `Read` of the modified types.ts file and assert via test (`assert "discogsography_api_check" in content` etc.) OR run `tsc --noEmit` in CI (not part of pytest). Simplest: do a Python-level open+read of the TS file inside the test.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/api/test_health.py -x -q &amp;&amp; (! grep -r discogsography_view_check src/gruvax/api/health.py)</automated>
  </verify>
  <done>
    - /api/health returns discogsography_api_check with three-state value per D-13.
    - The legacy discogsography_view_check field is gone from health.py and types.ts.
    - in_progress maps to 'ok' (Warning #4 fix verified by Tests 8 + 9).
    - All 12 tests pass.
    - sync_age_seconds derives from default_profile_last_sync_at.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lifespan rewire — probe profile_collection + populate default_profile_* state</name>
  <files>
    src/gruvax/app.py
  </files>
  <read_first>
    src/gruvax/app.py (full file — lifespan steps 2 + 1c at lines 127-139 and 209-239),
    src/gruvax/db/pool.py (post-Plan-01 — search_path = "gruvax, public"),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md (D-13 + D-14),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md (§Modified files row for app.py; §Shared Patterns "Background-task lifecycle" + "Lifespan-startup probe")
  </read_first>
  <behavior>
    - Test 1 (startup probe success): with profile_collection populated for default UUID, lifespan startup logs "profile_collection probe: OK" and `app.state.profile_collection_ready` is True.
    - Test 2 (startup probe failure non-fatal): with profile_collection empty (or migration not run — drop the table to simulate), lifespan continues; logger.error fired; `app.state.profile_collection_ready` is False. App still serves /api/health.
    - Test 3 (background task populates default_profile_*): after lifespan startup with default-profile row present (last_sync_at set), `app.state.default_profile_last_sync_at`, `default_profile_last_sync_status`, `default_profile_app_token_revoked` are all set within ~1 second (allow asyncio.sleep(60) initial delay is bypassed for tests — see action note on initial fire).
    - Test 4 (background task refresh on schedule): manipulate gruvax.profiles.last_sync_at via direct SQL; after the next 60-second tick, app.state.default_profile_last_sync_at reflects the new value. (Skip the literal 60s sleep in tests via monkeypatching the sleep duration or asserting on the first iteration only.)
    - Test 5 (legacy view_ok attribute removed): no code path in app.py sets app.state.discogsography_view_ok any more (grep returns 0).
  </behavior>
  <action>
    In src/gruvax/app.py lifespan:

    Step 2 (around lines 127-139) — REPLACE the v_collection probe:
      ```
      # ── 2. profile_collection startup probe (P1 D-13) ────────────────────────
      try:
          async with pool.connection() as conn, conn.cursor() as cur:
              await cur.execute(
                  "SELECT 1 FROM gruvax.profile_collection WHERE profile_id = %s::uuid LIMIT 1",
                  (DEFAULT_PROFILE_UUID,),
              )
              await cur.fetchone()
          app.state.profile_collection_ready = True
          logger.info("profile_collection probe: OK")
      except Exception as exc:
          app.state.profile_collection_ready = False
          logger.error(
              "profile_collection probe FAILED — search will return 503 until resolved. "
              "Run `alembic upgrade head` and `gruvax-sync --profile default`. Details: %s",
              exc,
          )
      ```
    DELETE the `app.state.discogsography_view_ok = True/False` assignments (Test 5).

    Define `DEFAULT_PROFILE_UUID = "00000000-0000-0000-0000-000000000001"` as a module-top constant (mirrors migration 0009).

    Step 1c (around lines 209-239) — REPLACE the _refresh_sync_age body per PATTERNS §Shared Patterns "Background-task lifecycle":
      ```
      app.state.default_profile_last_sync_at = None
      app.state.default_profile_last_sync_status = None
      app.state.default_profile_app_token_revoked = True
      app.state.sync_age_seconds = None

      async def _refresh_default_profile_state() -> None:
          while True:
              try:
                  async with pool.connection() as conn, conn.cursor() as cur:
                      await cur.execute(
                          "SELECT last_sync_at, last_sync_status, app_token_revoked "
                          "FROM gruvax.profiles WHERE id = %s::uuid AND deleted_at IS NULL",
                          (DEFAULT_PROFILE_UUID,),
                      )
                      row = await cur.fetchone()
                  if row is not None:
                      app.state.default_profile_last_sync_at = row[0]
                      app.state.default_profile_last_sync_status = row[1]
                      app.state.default_profile_app_token_revoked = bool(row[2])
                      now = datetime.now(UTC)
                      app.state.sync_age_seconds = (
                          (now - row[0]).total_seconds() if row[0] else None
                      )
              except Exception as exc:
                  logger.warning("default profile state refresh failed: %s", exc)
              await asyncio.sleep(60)

      _state_task = asyncio.create_task(_refresh_default_profile_state())
      app.state.background_tasks.add(_state_task)
      _state_task.add_done_callback(app.state.background_tasks.discard)
      def _log_state_task_exc(t: asyncio.Task) -> None:
          if not t.cancelled() and t.exception() is not None:
              logger.warning("default_profile_state background task exited: %s", t.exception())
      _state_task.add_done_callback(_log_state_task_exc)
      logger.info("default_profile_state background refresh task scheduled (60s cadence)")
      ```
    DELETE the old `_refresh_sync_age` function entirely.

    Verify no other lifespan steps reference `discogsography_view_ok` (grep). If any do, update them or remove.

    Extend or write tests in `tests/integration/api/test_health.py` (Task 1's file) for Tests 1-5 here — they overlap heavily with Task 1's testing surface and consolidating prevents fixture duplication.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/api/test_health.py -x -q &amp;&amp; (! grep -n discogsography_view_ok src/gruvax/app.py)</automated>
  </verify>
  <done>
    - Lifespan startup probe targets profile_collection.
    - Background task populates default_profile_last_sync_at/status/app_token_revoked.
    - sync_age_seconds derives from default_profile_last_sync_at.
    - Old _refresh_sync_age function removed.
    - app.state.discogsography_view_ok no longer assigned anywhere in app.py.
    - All 5 lifespan tests pass (in addition to Task 1's health response tests).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose fake-discogsography sibling + idempotent init-sync + `just compose-smoke` CI gate + canonical-module packaging</name>
  <files>
    services/fake-discogsography/Dockerfile,
    services/fake-discogsography/server.py,
    services/fake-discogsography/seed.yaml,
    compose.yaml,
    justfile,
    .github/workflows/build.yml,
    tests/integration/test_compose_smoke.py
  </files>
  <read_first>
    compose.yaml (full file — mosquitto + api service blocks for the sibling pattern),
    src/gruvax/_internal/fake_discogsography.py (Plan 02 canonical — the module this service imports; D-15 mandate),
    tests/fixtures/fake_discogsography.py (Plan 01-00 thin re-export — proves the canonical module is importable),
    Dockerfile (root — for the multi-stage uv reference),
    pyproject.toml (verify gruvax package is installable; check whether a `[fake]` optional extra is needed),
    services/fake-discogsography/seed.yaml (Plan 01-00 output — generator already produced this in Wave 0),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md (D-15, D-16, D-17, D-18),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-RESEARCH.md (§Recommended Project Structure — services/fake-discogsography path; §Pattern 4),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md (§11 services/fake-discogsography analog),
    justfile (existing recipes + style),
    .github/workflows/build.yml (existing matrix + service shapes)
  </read_first>
  <behavior>
    - Behavior 1 (Dockerfile builds + imports canonical): `docker build services/fake-discogsography` produces a runnable image; the image's CMD launches uvicorn against `server:app`; `server.py` imports `from gruvax._internal.fake_discogsography import create_fake_app` (single canonical module — D-15). NO copy of `fake_discogsography.py` lives under `services/`; NO `just sync-fake` drift guard exists.
    - Behavior 2 (seed.yaml present from Wave 0): `services/fake-discogsography/seed.yaml` exists (generated by Plan 01-00's `just regen-synth-data`) and has >=2900 release records — this plan does NOT regenerate it.
    - Behavior 3 (compose includes fake-discogsography): `docker compose config` shows the `fake-discogsography` service block with the right image/build context, healthcheck against /api/user/collection?limit=1 + Bearer dscg_dev_seed, and `networks: [internal]`.
    - Behavior 4 (compose api depends on fake healthy): the `api` service's `depends_on:` adds `fake-discogsography: condition: service_healthy`.
    - Behavior 5 (init-sync container present): the `init-sync` one-shot service exists; depends on api + fake-discogsography healthy; runs an `sh -c` command that performs the idempotency precheck via psql (per D-16 verbatim — `idempotent — skips if profile_collection is already populated for the default profile`) THEN `echo $GRUVAX_ADMIN_PIN | gruvax-sync --profile default`; restart: "no".
    - Behavior 6 (init-sync idempotency observed): on first `docker compose up`, init-sync runs gruvax-sync (logs "running initial sync"); on second `docker compose up` (no `down -v` between), init-sync exits 0 with log line "profile_collection already populated for default profile; skipping initial sync" WITHOUT calling gruvax-sync.
    - Behavior 7 (`just compose-smoke` exists and exits 0 on a clean run): the recipe runs `docker compose up -d gruvax-api fake-discogsography init-sync`, waits ≤60s for init-sync to exit, asserts exit code 0, asserts fake-discogsography returns rows on `/api/user/collection?limit=1`, then `docker compose down -v`. Exit code 0 from the recipe.
    - Behavior 8 (CI runs `just compose-smoke`): .github/workflows/build.yml has a matrix step running `just compose-smoke` in the standard CI runner.
  </behavior>
  <action>
    Verify Plan 01-00 produced `services/fake-discogsography/seed.yaml`. If absent (developer skipped the `just regen-synth-data` invocation), run it now: `just regen-synth-data` from the repo root. The seed is the same source of truth as `tests/fixtures/synth_profile_collection.sql` (per Blocker #4 RESOLUTION — single canonical generator).

    Create `services/fake-discogsography/Dockerfile`. The image installs the gruvax package (read pyproject.toml — if the `gruvax` package can be installed with no extras, do that; if `_internal.fake_discogsography` requires fastapi + pydantic that aren't in the runtime dep list, add a `[project.optional-dependencies] fake = ["fastapi", "uvicorn[standard]", "pyyaml", "pydantic"]` extra and install via `uv pip install -e '.[fake]'`):
      ```
      # syntax=docker/dockerfile:1.7
      FROM python:3.14-slim AS runtime
      WORKDIR /app
      # Copy the gruvax package source (build context = repo root via compose build.context)
      # so `gruvax._internal.fake_discogsography` is importable. Compose entry below
      # passes context: . so the whole repo is available.
      COPY pyproject.toml uv.lock README.md ./
      COPY src/ ./src/
      RUN pip install --no-cache-dir uv && uv pip install --system -e '.[fake]'
      COPY services/fake-discogsography/server.py services/fake-discogsography/seed.yaml ./
      EXPOSE 8004
      HEALTHCHECK --interval=30s --timeout=5s --retries=5 \
          CMD curl -fsS -H "Authorization: Bearer dscg_dev_seed" http://127.0.0.1:8004/api/user/collection?limit=1 || exit 1
      CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8004"]
      ```

    Create `services/fake-discogsography/server.py`:
      ```
      """Compose-sibling fake discogsography. Imports the canonical module (D-15)."""
      from __future__ import annotations
      from pathlib import Path
      import yaml
      from gruvax._internal.fake_discogsography import create_fake_app

      _SEED_PATH = Path(__file__).parent / "seed.yaml"
      _seed = yaml.safe_load(_SEED_PATH.read_text())["releases"]
      app = create_fake_app(seed=_seed, user_id="00000000-0000-0000-0000-DEFAULTDEVUSER")
      ```
    NO copy of `fake_discogsography.py` in this directory; NO drift guard.

    If pyproject.toml does NOT yet have a `[project.optional-dependencies]` block with a `fake` extra, ADD it:
      ```toml
      [project.optional-dependencies]
      fake = ["fastapi>=0.136", "uvicorn[standard]>=0.32", "pyyaml>=6", "pydantic>=2.13"]
      ```

    Modify compose.yaml — ADD the `fake-discogsography` service block (mirror the mosquitto block's shape per PATTERNS §11), changing the build context to repo root so the gruvax package is in scope:
      ```yaml
      fake-discogsography:
        image: gruvax/fake-discogsography:dev
        build:
          context: .
          dockerfile: services/fake-discogsography/Dockerfile
        container_name: gruvax-fake-discogsography
        networks: [internal]
        healthcheck:
          test: ["CMD", "curl", "-fsS", "-H", "Authorization: Bearer dscg_dev_seed", "http://127.0.0.1:8004/api/user/collection?limit=1"]
          interval: 30s
          timeout: 5s
          retries: 5
          start_period: 10s
        restart: unless-stopped
        logging:
          driver: "json-file"
          options: { max-size: "10m", max-file: "3" }
      ```
    Add to the existing `api` service's `depends_on`:
      ```yaml
      fake-discogsography:
        condition: service_healthy
      ```

    ADD the `init-sync` one-shot service block — idempotent per D-16 verbatim (Blocker #6 RESOLUTION):
      ```yaml
      init-sync:
        image: ghcr.io/simplicityguy/gruvax:latest   # reuses main gruvax image
        depends_on:
          api: { condition: service_healthy }
          fake-discogsography: { condition: service_healthy }
        entrypoint: ["/bin/sh", "-c"]
        command:
          - >-
            COUNT=$(psql "$DATABASE_URL" -At -c
            "SELECT COUNT(*) FROM gruvax.profile_collection WHERE profile_id = '00000000-0000-0000-0000-000000000001'");
            if [ "$COUNT" -gt 0 ]; then
              echo "profile_collection already populated for default profile; skipping initial sync";
              exit 0;
            else
              echo "running initial sync";
              echo "$GRUVAX_ADMIN_PIN" | uv run gruvax-sync --profile default;
            fi
        environment:
          GRUVAX_BASE_URL: "http://api:8000"
          DATABASE_URL: "${DATABASE_URL:-postgresql://gruvax:gruvax@gruvax-dev-pg:5432/gruvax}"
          GRUVAX_ADMIN_PIN: "${GRUVAX_ADMIN_PIN:?GRUVAX_ADMIN_PIN must be set in .env for init-sync}"
        restart: "no"
        networks: [internal]
      ```
    Notes:
      - The DATABASE_URL form for psql is `postgresql://...` (NOT `postgresql+psycopg://` which is the SQLAlchemy form); read compose.yaml to see which form is already used and align — psql understands only the plain libpq DSN, so a separate `PSQL_DSN` env var may be needed if the runtime app uses the SQLAlchemy form. Document the chosen approach in the summary.
      - Plan 04 Task 3 already makes `gruvax-sync` read PIN from stdin pipe when `sys.stdin.isatty()` is False (per Blocker #8 fix), so `echo $PIN | gruvax-sync` works without further changes.

    Create the `just compose-smoke` recipe in justfile (Blocker #2 RESOLUTION):
      ```
      # Full compose-up smoke gate: api + fake-discogsography + init-sync; assert init-sync exits 0.
      compose-smoke:
          #!/usr/bin/env bash
          set -euo pipefail
          docker compose up -d gruvax-api fake-discogsography init-sync
          # Wait up to 60s for init-sync to exit
          timeout 60 bash -c 'until [ -n "$(docker compose ps init-sync --status exited --quiet 2>/dev/null)" ]; do sleep 2; done' \
            || { echo "init-sync did not exit within 60s"; docker compose logs init-sync; exit 1; }
          # Assert exit code 0
          EXIT_CODE=$(docker compose ps init-sync --format json | jq -r '.[0].ExitCode // .ExitCode')
          if [ "$EXIT_CODE" != "0" ]; then
              echo "init-sync exited non-zero: $EXIT_CODE"
              docker compose logs init-sync
              exit 1
          fi
          # Assert fake-discogsography serves rows
          docker compose exec -T fake-discogsography curl -fs -H "Authorization: Bearer dscg_dev_seed" \
              "http://127.0.0.1:8004/api/user/collection?limit=1" | jq -e '.releases | length > 0'
          docker compose down -v
      ```

    Add a step to .github/workflows/build.yml that runs `just compose-smoke` in the build matrix. Read the existing workflow for the right job to extend (probably the `build` or `test` job that already runs docker compose). The step:
      ```yaml
      - name: Compose smoke gate
        env:
          GRUVAX_ADMIN_PIN: "1234"
          GRUVAX_SECRET_KEY: ${{ secrets.GRUVAX_SECRET_KEY_TEST }}
          DATABASE_URL: "postgresql://gruvax:gruvax@gruvax-dev-pg:5432/gruvax"
        run: just compose-smoke
      ```

    Create tests/integration/test_compose_smoke.py with Behavior 3-6 assertions: parse compose.yaml via PyYAML and assert the service blocks exist with the expected fields (including the idempotency precheck `sh -c` form for init-sync's command). This is the FAST structural test; the actual compose-up gate is `just compose-smoke` (slow; only in CI).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_compose_smoke.py -x -q &amp;&amp; python3 -c "import yaml,sys; n=len(yaml.safe_load(open('services/fake-discogsography/seed.yaml'))['releases']); sys.exit(0 if n>=2900 else 1)" &amp;&amp; grep -c "compose-smoke" justfile | grep -v 0 &amp;&amp; (! test -f services/fake-discogsography/fake_discogsography.py)</automated>
  </verify>
  <done>
    - services/fake-discogsography/{Dockerfile, server.py} exist; server.py imports from gruvax._internal.fake_discogsography (canonical — D-15).
    - services/fake-discogsography/seed.yaml exists with >=2900 releases (generated by Plan 01-00).
    - NO services/fake-discogsography/fake_discogsography.py file exists (D-15 single-module mandate).
    - NO `just sync-fake` recipe in justfile.
    - compose.yaml has the fake-discogsography service + init-sync one-shot with idempotency precheck + api dependency.
    - justfile has `just compose-smoke` recipe; CI runs it.
    - tests/integration/test_compose_smoke.py asserts compose structure (including init-sync precheck).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/health response → external clients | Cached state; no PII; no PAT |
| compose-time init-sync → admin endpoint | GRUVAX_ADMIN_PIN in env (operator-provided) |
| fake-discogsography container ↔ api container | Internal compose network; not exposed to LAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-health-leak | Information Disclosure | /api/health reveals last_sync_at timestamps | accept | LAN-only deployment (OOS-06); timestamps reveal sync cadence but not PAT or user data; matches v1 contract. |
| T-01-init-sync-pin | Information Disclosure | GRUVAX_ADMIN_PIN in compose env | mitigate | Sourced from `.env` (gitignored per project conv); compose uses `${GRUVAX_ADMIN_PIN:?...}` substitution that fails compose-up clearly if missing; not stored in image. |
| T-01-init-sync-rerun | Tampering | init-sync re-syncs unnecessarily on every boot | mitigate | D-16 verbatim — precheck COUNT(*) on profile_collection; skip if populated. Compose-smoke verifies idempotency in CI. |
| T-01-fake-svc-exposure | Spoofing | fake-discogsography exposed externally | mitigate | Bound to `internal` network only; no `ports:` mapping; only reachable via Compose DNS as `fake-discogsography:8004`. |
| T-01-fake-leak-prod | Tampering | fake-discogsography accidentally runs in prod | mitigate | Distinct compose service name + only built/run in local dev; prod overrides DISCOGSOGRAPHY_BASE_URL to point at the real discogsography. Documented in compose.yaml header comment. |
| T-01-fake-drift | Tampering | Compose sibling and tests/ have separate fake-app implementations | mitigate | Single canonical module at src/gruvax/_internal/fake_discogsography.py (D-15); server.py imports directly; no copy in services/, no drift guard. |
| T-01-lifespan-crash | Denial of Service | profile_collection probe failure crashes startup | mitigate | "try/except + logger.error + proceed" pattern preserved (PATTERNS §Shared Patterns "Lifespan-startup probe"); Test 2 verifies. |
| T-01-fake-token-pollution | Tampering | Production code accidentally uses `dscg_dev_seed` | accept | The token is hard-coded only in the fake service's container; production GRUVAX never has this token in its profile rows. |
</threat_model>

<verification>
- `uv run pytest tests/integration/api/test_health.py tests/integration/test_compose_smoke.py -x -q` is green.
- `grep -rn discogsography_view_check src/gruvax/api/health.py` returns nothing.
- `grep -rn discogsography_view_ok src/gruvax/app.py` returns nothing.
- `grep -rn discogsography_view_check frontend/src/` returns nothing.
- compose.yaml has both `fake-discogsography` and `init-sync` service blocks; init-sync command contains the `psql ... SELECT COUNT(*) ... skipping initial sync` precheck.
- `test ! -f services/fake-discogsography/fake_discogsography.py` (D-15 single-module).
- `grep -c "compose-smoke" justfile` >= 1.
- services/fake-discogsography/seed.yaml has at least 2900 releases.
- CI build.yml runs `just compose-smoke`.
</verification>

<success_criteria>
- /api/health field rename + three-state derivation per D-13 (in_progress → ok per Warning #4 fix).
- Lifespan probe + background task rewired to use profile_collection / profiles.
- Compose stack delivers a real end-to-end sync on first `docker compose up` (D-16) and idempotently skips on subsequent boots.
- `just compose-smoke` exits 0 in CI and tears the stack down clean.
- Canonical fake-discogsography module is single-source (D-15); NO duplication, NO drift guard.
- TypeScript types updated.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-05-SUMMARY.md` when done, listing:
- /api/health response shape post-rewire (sample JSON)
- Lifespan startup log lines (probe + background task scheduled)
- Compose services added (fake-discogsography, init-sync with precheck) + dependency wiring
- Seed row count (from Plan 01-00 generator)
- Frontend type rename location
- Confirmation: services/fake-discogsography/server.py imports from gruvax._internal.fake_discogsography (NO local copy)
- compose-smoke recipe path + CI step location
</output>
</content>
</invoke>
