---
phase: 01-walking-skeleton-api-client-single-profile-sync
plan: 06
type: execute
wave: 4
depends_on: ["01-05"]
files_modified:
  - src/gruvax/db/queries.py
  - src/gruvax/estimator/collection_snapshot.py
  - tests/fixtures/synth_profile_collection.sql
  - compose.yaml
  - tests/integration/db/test_queries_rewire.py
  - tests/integration/test_search.py
  - tests/integration/test_search_benchmark.py
requirements: [API-02, API-03]
autonomous: true
tags: [rewire, queries, snapshot, slo-benchmark, profile-collection]

must_haves:
  truths:
    - "Every query function in src/gruvax/db/queries.py that previously hit gruvax.v_collection now hits gruvax.profile_collection with WHERE profile_id = %s::uuid binding."
    - "src/gruvax/estimator/collection_snapshot.py::load() targets gruvax.profile_collection."
    - "D-17: tests/fixtures/synth_profile_collection.sql (created scaffold + generator-populated in Plan 01-00) is regenerated to the final content for SLO/integration tests that BYPASS sync entirely and seed gruvax.profile_collection directly via this synth SQL; ~3000 rows; shape-variety contract preserved (alpha+digit prefixes BLP/BST/ECM/KC, multi-prefix labels, mixed separators, pure numeric, multi-value catalogs); regression test in Plan 01-00 already asserts YAML/SQL row-count agreement."
    - "All v1.0 SLO benchmarks remain green: p95 /api/search ≤ 200ms; p95 /api/locate ≤ 50ms."
    - "All v1.0 search + locate integration tests remain green after the rewire (no behavioral regression)."
    - "Compose initdb mount (or post-migration seed recipe) points at tests/fixtures/synth_profile_collection.sql so `just up` clean-boot seeds profile_collection."
    - "Legacy fixtures/synth_collection.sql remains at tests/fixtures/legacy/synth_collection.sql (Plan 01-00 already moved it — Plan 06 does NOT need to repeat the move)."
  artifacts:
    - path: "src/gruvax/db/queries.py"
      provides: "All 9 query functions (search_collection, get_release_for_locate, did_you_mean_query, get_distinct_labels, get_catalogs_for_label, cube_exact_match, get_phantom_boundary_count, get_top_searched, get_sync_staleness_seconds) rewired to profile_collection"
    - path: "src/gruvax/estimator/collection_snapshot.py"
      provides: "load() targets profile_collection WHERE profile_id = %s::uuid"
    - path: "tests/fixtures/synth_profile_collection.sql"
      provides: "Final-content synthetic seed for gruvax.profile_collection (regenerated via `just regen-synth-data`)"
  key_links:
    - from: "src/gruvax/db/queries.py::search_collection"
      to: "gruvax.profile_collection (fts_vector + label + catalog_number indexes)"
      via: "FROM gruvax.profile_collection v WHERE v.profile_id = %s::uuid AND ..."
      pattern: "FROM gruvax\\.profile_collection"
    - from: "src/gruvax/estimator/collection_snapshot.py::load"
      to: "gruvax.profile_collection"
      via: "SELECT release_id, label, catalog_number FROM gruvax.profile_collection WHERE profile_id = %s::uuid"
      pattern: "FROM gruvax\\.profile_collection"
---

<objective>
Complete the data-source swap by rewiring every read path that touched `gruvax.v_collection` to instead hit `gruvax.profile_collection WHERE profile_id = <default UUID>`. The legacy v1 dev-DB seed was pre-moved to `tests/fixtures/legacy/synth_collection.sql` in Plan 01-00 (Wave 0); the new fixture `tests/fixtures/synth_profile_collection.sql` was scaffolded and generator-populated in Plan 01-00 — this plan only regenerates it with the final content (in case the schema needs a fresh sweep after migration 0009) and updates the compose initdb path. Verify the v1.0 SLO benchmark gate (p95 /api/search ≤ 200ms, /api/locate ≤ 50ms) AND every existing search/locate integration test remain green post-rewire — this is the proof-of-non-regression that the data-source swap actually preserved Core Value.

Purpose: This is the test that the whole phase succeeds. If the SLO holds + the search results are unchanged for the synthetic dataset, then the rewire is correct. If either fails, the rewire missed something or the indexes are misaligned.

Output: 2 rewired src files + 1 regenerated SQL fixture + compose seed-mount update + 1 new equivalence test + 2 extended existing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-VALIDATION.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-00-PLAN.md
@src/gruvax/db/queries.py
@src/gruvax/estimator/collection_snapshot.py
@tests/fixtures/legacy/synth_collection.sql
@tests/fixtures/synth_profile_collection.sql
@tests/fixtures/generate_synth_data.py
@compose.yaml
@tests/integration/test_search_benchmark.py
@tests/conftest.py

<interfaces>
<!-- The DEFAULT_PROFILE_UUID is THE single value bound into every query call site for P1.
     In P2 it becomes a per-request parameter (from session/device binding); the function
     signatures already need to ACCEPT a profile_id so P2 only flips the call sites. -->

DEFAULT_PROFILE_UUID = "00000000-0000-0000-0000-000000000001"   # constant, NOT user input

Rewire pattern (applied to every query that hits v_collection):
  OLD: FROM gruvax.v_collection
  NEW: FROM gruvax.profile_collection v WHERE v.profile_id = %s::uuid AND ...
       (binding param = DEFAULT_PROFILE_UUID)

Column mapping v_collection → profile_collection (D-04):
  collection_item_id   → REMOVED (drop from SELECT; consumers re-derive or omit)
  release_id           → release_id (unchanged; BIGINT)
  title                → title
  primary_artist       → artist (column name simplified per D-04)
  label                → label
  catalog_number       → catalog_number
  format               → REMOVED (not in P1 profile_collection per D-04)
  year                 → year
  fts_vector           → fts_vector (preserved; weighted A=catalog_number, B=title, C=artist||' '||label)
  synced_at            → synced_at

Where SELECTs referenced `collection_item_id` or `primary_artist` or `format`:
  - collection_item_id: emit NULL or drop the column from the row dict; downstream consumers need rewiring (search results historically include collection_item_id — verify if any consumer uses it; if so, derive from release_id+profile_id or remove).
  - primary_artist: rename to `artist` in row dict keys to match new schema (verify no UI consumers hard-code the old name).
  - format: drop. Verify no UI consumer reads it.

CollectionSnapshot.load() (estimator/collection_snapshot.py):
  OLD:  SELECT release_id, label, catalog_number FROM gruvax.v_collection
  NEW:  SELECT release_id, label, catalog_number FROM gruvax.profile_collection WHERE profile_id = %s::uuid
        (single binding param)
</interfaces>

<query_inventory>
<!-- Per PATTERNS.md §Modified files row for db/queries.py — verify exact list against the file -->

Functions in src/gruvax/db/queries.py that hit gruvax.v_collection in v1 (must be rewired):
  1. search_collection           — FTS + catalog union (heavy; SRCH-08 boost; line ~126)
  2. get_release_for_locate      — single-row fetch by release_id
  3. did_you_mean_query          — trigram suggestion
  4. get_distinct_labels         — autocomplete / admin
  5. get_catalogs_for_label      — autocomplete
  6. cube_exact_match            — cube boundary lookup
  7. get_phantom_boundary_count  — admin diagnostics (OQ7)
  8. get_top_searched            — record_stats JOIN v_collection for human-readable rows
  9. get_sync_staleness_seconds  — staleness derivation (already replaced at app.py-level by Plan 05's background task; queries.py function may now be unused or rewired)

If the executor finds additional v_collection references via `grep -n gruvax.v_collection src/gruvax/db/queries.py`, rewire them all in this task — the inventory above is from PATTERNS.md as of 2026-05-26 (HEAD 4bbd086); drift means the executor's grep is the source of truth.
</query_inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate synth_profile_collection.sql via Plan 01-00 generator + update compose initdb mount</name>
  <files>
    tests/fixtures/synth_profile_collection.sql,
    compose.yaml
  </files>
  <read_first>
    tests/fixtures/legacy/synth_collection.sql (pre-moved by Plan 01-00 — shape-variety contract source),
    tests/fixtures/synth_profile_collection.sql (Plan 01-00 generator output),
    tests/fixtures/generate_synth_data.py (Plan 01-00 generator script),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md (D-17),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md (§10 synth_profile_collection analog),
    compose.yaml (current initdb mount — check what fixtures/synth_collection.sql was mounted to BEFORE Plan 01-00 moved the file)
  </read_first>
  <behavior>
    - Behavior 1: `just regen-synth-data` is re-run from this plan, producing a fresh `tests/fixtures/synth_profile_collection.sql` with >=2900 INSERT rows; Plan 01-00's regression test (`tests/fixtures/test_generator_consistency.py`) still passes (YAML and SQL row-counts equal).
    - Behavior 2: the regenerated SQL fixture executes cleanly against a freshly-migrated database (alembic head, schema gruvax) — running it twice does not error (TRUNCATE + INSERT idempotency).
    - Behavior 3: after running, `SELECT COUNT(*) FROM gruvax.profile_collection WHERE profile_id = '00000000-0000-0000-0000-000000000001'::uuid` returns >=2900 rows.
    - Behavior 4: catalog-number variety preserved — at minimum the alpha+digit prefix shapes (BLP, ECM, KC, BST), multi-prefix labels, mixed separator forms (space, hyphen, contiguous), pure-numeric, and multi-value comma-separated catalog rows from the v1 fixture are represented.
    - Behavior 5: compose.yaml's `gruvax-dev-pg` initdb mount points at the NEW SQL fixture path (tests/fixtures/synth_profile_collection.sql) so `just up` clean-boot seeds the new schema. Any reference to the old `fixtures/synth_collection.sql` path is removed (Plan 01-00 deleted the file).
    - Behavior 6: legacy `tests/fixtures/legacy/synth_collection.sql` remains in place (Plan 01-00 created it; Plan 06 does NOT move or modify it).
  </behavior>
  <action>
    Run `just regen-synth-data` from the repo root. This invokes Plan 01-00's generator:
      ```
      uv run python tests/fixtures/generate_synth_data.py \
          --yaml services/fake-discogsography/seed.yaml \
          --sql tests/fixtures/synth_profile_collection.sql
      ```
    Both artifacts are regenerated from the same deterministic source. Plan 01-00's `tests/fixtures/test_generator_consistency.py` is the regression gate that the YAML/SQL row-counts agree.

    Update compose.yaml `gruvax-dev-pg` service's initdb mount:
      OLD:  `./fixtures/synth_collection.sql:/docker-entrypoint-initdb.d/01-synth-collection.sql:ro`  (the path Plan 01-00 deleted)
      NEW:  `./tests/fixtures/synth_profile_collection.sql:/docker-entrypoint-initdb.d/01-synth-profile-collection.sql:ro`
    NOTE: initdb runs BEFORE alembic. The new fixture references gruvax.profiles + gruvax.profile_collection which only exist AFTER migration. Two-mount approach: keep the existing init order but use a wrapper script that waits for the schema. SIMPLER approach: move the seed loading out of initdb and into a post-migration justfile recipe (e.g., `just seed-synth`) called as part of `just up`. Read compose.yaml's just-up recipe and the existing init flow first; choose the option that survives `docker compose down -v && docker compose up` clean-boot. Document the chosen approach.

    DO NOT re-move `fixtures/synth_collection.sql` — Plan 01-00 already moved it to `tests/fixtures/legacy/synth_collection.sql` and deleted the original. If `fixtures/synth_collection.sql` somehow still exists (developer skipped Plan 01-00), surface as a blocking error.
  </action>
  <verify>
    <automated>test -f tests/fixtures/synth_profile_collection.sql &amp;&amp; test -f tests/fixtures/legacy/synth_collection.sql &amp;&amp; test ! -f fixtures/synth_collection.sql &amp;&amp; grep -c "tests/fixtures/synth_profile_collection.sql" compose.yaml | grep -v 0 &amp;&amp; uv run pytest tests/fixtures/test_generator_consistency.py -x -q</automated>
  </verify>
  <done>
    - tests/fixtures/synth_profile_collection.sql regenerated with final content.
    - compose.yaml seeds the new fixture path on clean boot.
    - Legacy tests/fixtures/legacy/synth_collection.sql in place (Plan 01-00 created).
    - Plan 01-00's generator-consistency regression test still passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire queries.py + collection_snapshot.py from v_collection → profile_collection</name>
  <files>
    src/gruvax/db/queries.py,
    src/gruvax/estimator/collection_snapshot.py,
    tests/integration/db/test_queries_rewire.py
  </files>
  <read_first>
    src/gruvax/db/queries.py (full file — all 9+ functions),
    src/gruvax/estimator/collection_snapshot.py (full file — load() body + invalidate seam at lines 86-94, 109-121),
    tests/fixtures/synth_profile_collection.sql (Task 1 output — for fixture loading in equivalence tests),
    tests/fixtures/legacy/synth_collection.sql (for v1 baseline reference),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md (§Modified files row for queries.py + collection_snapshot.py; §Shared Patterns "SQL parameterization"),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-RESEARCH.md (§Architectural Responsibility Map row "Search / locate against profile_collection")
  </read_first>
  <behavior>
    - Test 1 (zero v_collection references in queries.py post-rewire): `grep -n gruvax.v_collection src/gruvax/db/queries.py` returns 0 lines.
    - Test 2 (zero v_collection references in collection_snapshot.py): `grep -n v_collection src/gruvax/estimator/collection_snapshot.py` returns 0 lines.
    - Test 3 (every rewired function binds DEFAULT_PROFILE_UUID): grep `%s::uuid` in queries.py SELECT statements + verify each call site passes DEFAULT_PROFILE_UUID (or accepts it as a parameter; see action note on signature evolution for P2 readiness).
    - Test 4 (search_collection equivalence): seed profile_collection via the new fixture with the same canonical query "blp4195"; expected top result is the BLP-4195 release with `catalog_number='BLP 4195'`; results match v1 expectations (re-run v1's existing test_search.py against the new data source; results must be identical or measurably equivalent given column rename).
    - Test 5 (CollectionSnapshot.load equivalence): with the new fixture loaded, snapshot.size matches the v1 fixture's row count; snapshot.lookup_by_label("Blue Note") returns the same release_ids as v1.
    - Test 6 (Pitfall C preserved — labels casefolded, never normalize_catalog'd): the collection_snapshot.py rewire MUST preserve the existing Pitfall-C invariant (PATTERNS §Modified files row notes "Pitfall C (D-13 in v1.0 estimator) — labels are casefolded, never normalize_catalog'd. Preserved post-rewire."). Verify by reading the existing snapshot.py and ensuring the label-keying logic is unchanged — only the SOURCE query changes.
    - Test 7 (every search/locate integration test passes): `uv run pytest tests/integration/test_search.py tests/integration/test_locate.py tests/integration/test_search_benchmark.py -x` is green.
  </behavior>
  <action>
    For each function in `<query_inventory>` above, modify the SQL string body:
      - Replace `FROM gruvax.v_collection` (and any aliased form like `gruvax.v_collection v`) with `FROM gruvax.profile_collection v`.
      - Add a `WHERE v.profile_id = %s::uuid AND ...` clause at the appropriate position (preserving any existing WHERE clauses with AND).
      - Update the function signature: add a `profile_id: str = DEFAULT_PROFILE_UUID` keyword parameter to every rewired function. This is the P2-readiness lever — call sites still call without specifying, but the parameter is in place.
      - Update the `cur.execute(sql, (...))` tuple to include the profile_id parameter (insert at the appropriate position matching the new %s order).
      - Where the function returns row dicts, RENAME `primary_artist` → `artist` key (column rename per D-04 — verify downstream consumers in api/search.py + api/locate.py + frontend if any).

    Add a module-level `DEFAULT_PROFILE_UUID = "00000000-0000-0000-0000-000000000001"` constant at the top of queries.py (mirrors migration 0009 and Plan 05's app.py constant). Document via inline comment that this is the P1 single-profile fallback; P2 replaces with per-request binding.

    Specific notes per function:
      - `search_collection`: FROM clause changes; the WITH fts CTE's FROM also changes; the catalog-prefix UNION ALL branch's FROM also changes. The `collection_item_id` column is dropped from SELECTs (cols become release_id, title, artist, label, catalog_number, year — drop format too).
      - `get_release_for_locate`: SELECT label, catalog_number FROM gruvax.profile_collection v WHERE v.profile_id = %s::uuid AND v.release_id = %s LIMIT 1; signature gains profile_id.
      - `did_you_mean_query`: both the label and artist sub-selects in the UNION must be from profile_collection with profile_id binding; signature gains profile_id.
      - `get_distinct_labels`, `get_catalogs_for_label`, `cube_exact_match`, `get_phantom_boundary_count`, `get_top_searched`: same treatment.
      - `get_sync_staleness_seconds`: Plan 05's background task supersedes this; the function can either be retired OR rewired to read `profiles.last_sync_at` for the default profile (mirror the background task). Retire if no consumer; rewire if there are any callers (grep `get_sync_staleness_seconds`).

    Modify src/gruvax/estimator/collection_snapshot.py::load() per PATTERNS §Modified files row + RESEARCH §Architectural Responsibility Map row "Search / locate":
      ```
      OLD: SELECT release_id, label, catalog_number FROM gruvax.v_collection
      NEW: SELECT release_id, label, catalog_number FROM gruvax.profile_collection WHERE profile_id = %s::uuid
      ```
      The `cur.execute(sql, (...))` becomes `cur.execute(sql, (DEFAULT_PROFILE_UUID,))`. Add DEFAULT_PROFILE_UUID constant to collection_snapshot.py module top, OR import from queries.py to keep the constant single-sourced. Preserve invalidate() seam at lines 109-121 unchanged. Preserve the Pitfall-C label-casefold logic verbatim (Test 6).

    Update tests/integration/test_search.py if any test pre-populates `gruvax_dev.collection_items` directly — switch to the new fixture loading path (run synth_profile_collection.sql instead). The existing conftest.py likely has a fixture that runs the SQL file; update the path it loads.

    Update tests/integration/test_search_benchmark.py similarly — the benchmark test relies on a populated collection. Switch its fixture loading to the new SQL file.

    Create tests/integration/db/test_queries_rewire.py implementing Tests 1-6 above. Use direct grep-based assertions for Tests 1-3 (open the source files in pytest and `assert "v_collection" not in source`). Use the loaded fixture for Tests 4-5. Test 6 reads collection_snapshot.py source and asserts the label-casefolding loop is present.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/db/test_queries_rewire.py tests/integration/test_search.py tests/integration/test_locate.py -x -q &amp;&amp; (! grep -rn "gruvax\\.v_collection" src/gruvax/)</automated>
  </verify>
  <done>
    - Zero gruvax.v_collection references remain in src/gruvax/db/queries.py and src/gruvax/estimator/collection_snapshot.py.
    - Every rewired function accepts profile_id parameter (defaulted to DEFAULT_PROFILE_UUID for P1 compatibility).
    - test_queries_rewire.py passes all 6 tests.
    - Existing search + locate integration tests pass against the new fixture.
    - Pitfall C invariant verified (label casefold preserved).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SLO benchmark gate — verify p95 /api/search ≤ 200ms and /api/locate ≤ 50ms hold post-rewire</name>
  <files>
    tests/integration/test_search_benchmark.py,
    justfile,
    .github/workflows/build.yml
  </files>
  <read_first>
    tests/integration/test_search_benchmark.py (full file — current benchmark setup, fixtures, parametrize),
    justfile (existing `just slo` recipe, if present),
    .github/workflows/build.yml (existing v1.0 SLO gate placement),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-VALIDATION.md (§Per-Task Verification Map line 54 — 01-06-01 SLO row)
  </read_first>
  <behavior>
    - Test 1 (existing benchmark file runs against new data source): `uv run pytest tests/integration/test_search_benchmark.py --benchmark-only` completes; benchmark output shows p95 /api/search ≤ 200ms.
    - Test 2 (locate benchmark): if a locate benchmark exists separately, p95 ≤ 50ms; if combined in the search benchmark file, both targets verified.
    - Test 3 (`just slo` recipe exists and runs the SLO benchmark with the new gate values): `just slo` exits 0 when SLOs are met.
    - Test 4 (CI gate updated): .github/workflows/build.yml has a step running `just slo` (or equivalent) AFTER `just migrate-roundtrip` AND after seeding the new fixture; the job fails if SLOs regress.
  </behavior>
  <action>
    Open tests/integration/test_search_benchmark.py and review the current shape (already exists from v1.0 Phase 8). The benchmark loads a fixture, hits /api/search and /api/locate via the in-process httpx client, and records p95. The rewire happened in Task 2; this task verifies the benchmark still passes.

    Update the fixture-loading code in the benchmark test file IF it still references `gruvax_dev.collection_items` or `fixtures/synth_collection.sql` (Task 2 may have already done this; verify and complete if needed). The new path is `tests/fixtures/synth_profile_collection.sql`.

    Add or update a `just slo` recipe in justfile that runs:
      `uv run pytest tests/integration/test_search_benchmark.py --benchmark-only --benchmark-min-rounds=5`
    Read the existing recipe convention; if `just slo` already exists, just verify it picks up the new fixture path. If absent, add it.

    Update .github/workflows/build.yml's existing SLO step (if it exists from v1.0 Phase 8) to ensure it runs AFTER the fixture-seed step. If no SLO step exists, add one to the test job:
      ```yaml
      - name: SLO benchmark gate
        run: just slo
      ```
    The job ordering must be: install deps → start postgres → alembic upgrade head → psql -f tests/fixtures/synth_profile_collection.sql → just slo.

    Verify benchmark actually runs against an in-process app populated from the new fixture. If the existing benchmark used the v1 fixture loaded via initdb in a docker postgres, replicate that flow against synth_profile_collection.sql.

    If the existing benchmark times out or regresses (unlikely — profile_collection has the same indexes as v_collection had via the underlying releases table indexes), investigate:
      - Are the GIN(fts_vector) + (profile_id, label, catalog_number) + GIN trgm indexes from migration 0009 actually created? (They are per Plan 01 task 2.)
      - Is the WHERE profile_id = %s::uuid filter using the composite index? (It should; if not, EXPLAIN ANALYZE the slow query and consider whether the partial index needs to be promoted.)
      - Is the row count comparable? (~3000 in the new fixture vs whatever v1 had.)

    Do NOT relax the SLO thresholds. The phase success criterion (ROADMAP §Phase 1 #2) is "v1.0 SLOs hold: /api/search p95 ≤ 200 ms and /api/locate p95 ≤ 50 ms on synthetic data".
  </action>
  <verify>
    <automated>just slo</automated>
  </verify>
  <done>
    - `just slo` exits 0 with p95 /api/search ≤ 200ms and /api/locate ≤ 50ms.
    - .github/workflows/build.yml runs `just slo` against the new fixture in CI.
    - Benchmark fixture loading switched to synth_profile_collection.sql.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query layer ↔ DB | All SQL parameterized; profile_id is a constant in P1 but binds via %s::uuid |
| test fixture seed → DB | Trusted fixture; no user input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-sqli-rewire | Tampering | Rewire reintroduces f-string SQL | mitigate | All %s placeholders preserved; PATTERNS §Shared Patterns "SQL parameterization" enforced; Task 2 Test 1 + Test 3 verify. |
| T-01-slo-regression | Denial of Service | profile_collection slower than v_collection | mitigate | Migration 0009 ships all required indexes (GIN fts, composite (profile_id, label, catalog_number), GIN trgm); SLO gate verifies. |
| T-01-cross-profile-read | Information Disclosure | Missing profile_id binding leaks other-profile data | mitigate (P2 stress) | Every query binds DEFAULT_PROFILE_UUID; P1 has only one profile so no leak possible by data presence; P2's NOT NULL migration + per-request binding closes the loophole structurally. Task 2 Test 3 verifies every query has the WHERE clause. |
| T-01-fixture-pollution | Tampering | Synthetic fixture leaks into production DB | accept | Fixture is local-dev/test only; production seeds profile_collection only via sync_profile against the real discogsography. Compose mount only runs against the gruvax-dev-pg sibling. |
| T-01-pitfall-c-loss | Tampering | Estimator stops casefolding labels (Pitfall C regression) | mitigate | Test 6 verifies the casefolding loop unchanged post-rewire. |
</threat_model>

<verification>
- `uv run pytest tests/integration/db/test_queries_rewire.py tests/integration/test_search.py tests/integration/test_locate.py tests/integration/test_search_benchmark.py -x -q` is green.
- `just slo` exits 0.
- `grep -rn "gruvax\\.v_collection" src/gruvax/` returns nothing.
- `grep -rn "v_collection" src/gruvax/estimator/collection_snapshot.py` returns nothing.
- tests/fixtures/synth_profile_collection.sql exists and has the shape-variety contract preserved.
- compose.yaml initdb mount points at the new fixture.
- CI build.yml runs `just slo` step.
</verification>

<success_criteria>
- All 9 query functions + CollectionSnapshot.load rewired to profile_collection.
- Synthetic seed fixture regenerated via Plan 01-00's canonical generator + compose mount updated.
- p95 SLOs (search ≤ 200ms / locate ≤ 50ms) hold post-rewire.
- All v1 search/locate integration tests pass unchanged in behavior.
- Pitfall C casefold invariant preserved.
- P2 readiness: every rewired function accepts profile_id parameter (P2 only flips call sites).
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-06-SUMMARY.md` when done, listing:
- Final v_collection reference count (0 in src/gruvax/)
- All rewired function names + signatures (profile_id parameter added)
- SLO benchmark p95 numbers (search + locate) post-rewire
- Synth seed row count (post-regeneration)
- CI gate paths (migrate-roundtrip + slo + compose-smoke)
</output>
</content>
</invoke>
