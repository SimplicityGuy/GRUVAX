---
phase: 02-real-position-estimation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gruvax/estimator/collection_snapshot.py
  - src/gruvax/estimator/constants.py
  - src/gruvax/estimator/algorithm.py
  - src/gruvax/api/deps.py
  - src/gruvax/api/locate.py
  - src/gruvax/app.py
  - tests/unit/test_collection_snapshot.py
  - tests/unit/test_algorithm.py
  - tests/property/test_estimator_props.py
  - tests/integration/test_locate.py
  - fixtures/golden_cases.yaml
  - fixtures/synth_collection.py
  - fixtures/__init__.py
  - pyproject.toml
  - conftest.py
autonomous: true
requirements: [POS-03, POS-05, CUBE-04, CUBE-10, CUBE-03]
nyquist_compliant: true

must_haves:
  truths:
    - "GET /api/locate returns a populated sub_cube_interval (start/end/crosses_boundary/next_cube) for a multi-record label record"
    - "A singleton-label record returns sub_cube_interval start=0.0 end=1.0 at confidence 0.30 (D-02 faint full-cube band backing data)"
    - "A record whose confidence is at or below 0.30 returns sub_cube_interval=null and estimator_version=cube-only-v1 (§4.8 fallback)"
    - "A record whose label spans two cubes returns label_span with >=2 entries (CUBE-03 backing data)"
    - "The estimator computes with zero DB calls — collection records come from the in-memory CollectionSnapshot loaded at startup"
  artifacts:
    - path: "src/gruvax/estimator/collection_snapshot.py"
      provides: "CollectionSnapshot (per-label record lists loaded once at startup) + RecordRow"
      contains: "class CollectionSnapshot"
    - path: "src/gruvax/estimator/constants.py"
      provides: "TEXT_CUE_THRESHOLD + POSITION_HALF_WIDTH + confidence calibration constants"
      contains: "POSITION_HALF_WIDTH"
    - path: "src/gruvax/estimator/algorithm.py"
      provides: "locate_by_index (§4.1) + locate() dispatcher (§4.1 primary, §4.8 fallback)"
      contains: "def locate_by_index"
    - path: "fixtures/synth_collection.py"
      provides: "planted-truth synthetic collection factories (shared by property tests + A/B harness)"
      contains: "def make_uniform_dense"
  key_links:
    - from: "src/gruvax/api/locate.py"
      to: "gruvax.estimator.algorithm.locate"
      via: "dispatcher call replacing locate_cube_only"
      pattern: "locate\\("
    - from: "src/gruvax/app.py"
      to: "gruvax.estimator.collection_snapshot.CollectionSnapshot"
      via: "lifespan startup load → app.state.collection_snapshot"
      pattern: "collection_snapshot"
    - from: "src/gruvax/estimator/algorithm.py"
      to: "gruvax.estimator.normalize.parse_key"
      via: "sort label records by parse_key (raw-string compare forbidden — D-13)"
      pattern: "parse_key"
---

<objective>
Swap the real §4.1 index-based estimator in behind the locked `LocateResult` contract so `/api/locate` answers "where exactly in the cube" — a populated `sub_cube_interval` with a calibrated confidence — while keeping the Phase 1 §4.8 cube-only path as the timeout/low-confidence fallback. This is the first end-to-end backend slice: after this plan, `curl /api/locate?release_id=...` returns a non-null `sub_cube_interval` for multi-record labels.

Purpose: Implements POS-05 (two estimators behind one contract), POS-03 (CPU-only, no DB during compute via an in-memory collection snapshot), and the backend data for CUBE-04 (sub-cube interval), CUBE-10/D-02 (singleton full-cube band), and CUBE-03 (multi-cube label_span).

Output: `CollectionSnapshot` module, `constants.py`, extended `algorithm.py` (`locate_by_index` + `locate` dispatcher + confidence), wired `/api/locate` + lifespan + deps, plus the full Wave-0 test scaffold (unit, property, golden, benchmark) and the shared synthetic generator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-position-estimation/02-CONTEXT.md
@.planning/phases/02-real-position-estimation/02-RESEARCH.md
@.planning/phases/02-real-position-estimation/02-PATTERNS.md
@.planning/phases/02-real-position-estimation/02-VALIDATION.md

<interfaces>
<!-- Frozen contract — DO NOT change these shapes. From src/gruvax/estimator/contract.py -->
SubInterval is a frozen dataclass with FIVE fields (note `cube` is field #1):
  cube: CubeRef
  start: float            # 0..1 within cube width
  end: float             # 0..1 within cube width
  crosses_boundary: bool
  next_cube: CubeRef | None = None

LocateResult (mutable dataclass):
  release_id: int
  primary_cube: CubeRef | None
  label_span: list[CubeRef]
  sub_cube_interval: SubInterval | None
  confidence: float
  generated_at: datetime = field(default_factory=...UTC now)
  estimator_version: str = "cube-only-v1"

CUBE_ONLY_CONFIDENCE: float = 0.30   (in contract.py)
NO_BOUNDARY_CONFIDENCE: float = 0.0  (in contract.py)

<!-- Existing §4.8 estimator — extend, do not rewrite. From src/gruvax/estimator/algorithm.py -->
def locate_cube_only(release_id: int, label: str, catalog_number: str, cache: BoundaryCache) -> LocateResult
  - covering = boundary rows where first_label.casefold() <= label.casefold() <= last_label.casefold()
    AND catalog_in_range(catalog_number, b.first_catalog, b.last_catalog)
  - sorted_span = sorted(covering, key=lambda c: (c.unit_id, c.row, c.col))
  - primary_cube = sorted_span[0]; returns confidence CUBE_ONLY_CONFIDENCE when covered, else NO_BOUNDARY_CONFIDENCE + primary_cube=None

<!-- POS-01 comparator — the ONLY legal comparison path (D-13). From src/gruvax/estimator/normalize.py -->
def parse_key(catalog: str | None) -> tuple[tuple[int, int | str], ...]   # natural-sort key
def catalog_in_range(catalog, first_catalog, last_catalog) -> bool
  # labels are NOT catalog numbers — compare labels with .casefold() only, never normalize_catalog()

<!-- Cache analog to mirror. From src/gruvax/estimator/boundary_cache.py -->
class BoundaryCache: __init__ sets self._rows=[]; async load(pool); _load_rows(rows) testing seam;
  get_boundaries() -> Sequence[BoundaryRow]; invalidate() sets self._rows=[]
BoundaryRow(frozen): unit_id, row, col, first_label, first_catalog, last_label, last_catalog, is_empty
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CollectionSnapshot module + Wave-0 snapshot tests</name>
  <files>src/gruvax/estimator/collection_snapshot.py, tests/unit/test_collection_snapshot.py</files>
  <read_first>
    - src/gruvax/estimator/boundary_cache.py (exact analog — copy lifespan + `_load_rows` seam + `invalidate()` structure)
    - tests/unit/test_algorithm.py (test helper + assertion patterns to mirror; `_make_cache_from_yaml`, `loop_scope="session"` for DB tests)
    - tests/conftest.py (FIXTURE_DIR resolves to repo-root `fixtures/`; `db_pool` session fixture; `boundary_cache` fixture)
    - .planning/phases/02-real-position-estimation/02-PATTERNS.md (`collection_snapshot.py` Pattern Assignment + Testing Seam shared pattern)
    - .planning/phases/02-real-position-estimation/02-RESEARCH.md (§POS-03 In-Memory Collection Snapshot — design + Pitfall C label normalization)
  </read_first>
  <behavior>
    - Test 1 (test_snapshot_load_groups_by_label): after `_load_snapshot(by_label)`, `get_label_records("Blue Note")` returns exactly the records for that label.
    - Test 2 (test_snapshot_label_case_folded): `get_label_records("BLUE NOTE") == get_label_records("blue note")` (Pitfall C — `.casefold()` key, never `normalize_catalog()` on labels).
    - Test 3 (test_snapshot_unknown_label_returns_empty): `get_label_records("NONEXISTENT") == []`.
    - Test 4 (test_snapshot_invalidate_empties): after `invalidate()`, every `get_label_records(...)` returns `[]`.
    - Test 5 (test_snapshot_load_from_db): live-DB load via `db_pool`, `@pytest.mark.asyncio(loop_scope="session")`, asserts a known label resolves to >=1 record.
  </behavior>
  <action>
    Create `CollectionSnapshot` mirroring `BoundaryCache` exactly. Define `@dataclass(frozen=True) class RecordRow` with fields `release_id: int`, `label: str`, `catalog_number: str`. `CollectionSnapshot.__init__` sets `self._by_label: dict[str, list[RecordRow]] = {}`. Implement `async def load(self, pool)` running `SELECT release_id, label, catalog_number FROM gruvax.v_collection` (the ONLY legal source — DEP-02 / Pitfall 5; no schema-qualified direct table access), grouping rows into `_by_label` keyed by `(label or "").casefold()`. Implement `def get_label_records(self, label: str) -> list[RecordRow]` returning `self._by_label.get((label or "").casefold(), [])`. Implement testing seam `def _load_snapshot(self, by_label: dict[str, list[RecordRow]]) -> None`. Implement `def invalidate(self) -> None` setting `self._by_label = {}` (Phase 4 SSE seam — document it as such). Use `.casefold()` for label keys to match `locate_cube_only`'s label comparison (Pitfall C); never apply `normalize_catalog()` to labels. Add the five tests in `tests/unit/test_collection_snapshot.py` following the helper/assertion patterns in `test_algorithm.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_collection_snapshot.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/gruvax/estimator/collection_snapshot.py` contains `class CollectionSnapshot` and `class RecordRow`
    - `collection_snapshot.py` SELECT references only `gruvax.v_collection` (grep: `FROM gruvax.v_collection`; no `releases`/`collection_items` direct table refs)
    - `get_label_records` uses `.casefold()` (grep: `casefold`) and never calls `normalize_catalog`
    - `_load_snapshot` testing seam exists (grep: `def _load_snapshot`)
    - `uv run pytest tests/unit/test_collection_snapshot.py -x -q` exits 0 with >=5 tests collected
  </acceptance_criteria>
  <done>CollectionSnapshot loads/groups/casefolds/invalidates correctly; all five unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: §4.1 locate_by_index + locate() dispatcher + confidence/band constants + unit + benchmark tests</name>
  <files>src/gruvax/estimator/constants.py, src/gruvax/estimator/algorithm.py, tests/unit/test_algorithm.py</files>
  <read_first>
    - src/gruvax/estimator/algorithm.py (existing `locate_cube_only` — extend in place, reuse its covering/sort/error-return patterns)
    - src/gruvax/estimator/contract.py (frozen `SubInterval` has `cube: CubeRef` as first field — the dispatcher must populate it)
    - src/gruvax/estimator/normalize.py (`parse_key` — the only legal sort key; D-13)
    - src/gruvax/estimator/collection_snapshot.py (created in Task 1 — `get_label_records`, `RecordRow`)
    - tests/unit/test_algorithm.py (existing `_make_cache_from_yaml` helper + assertion patterns; benchmark fixture usage)
    - .planning/phases/02-real-position-estimation/02-RESEARCH.md (§POS-05 algorithm sketch + dispatcher + Pitfall A singleton f, Pitfall B StopIteration; §Confidence Calibration `_compute_confidence`; §Benchmark Gate)
    - .planning/phases/02-real-position-estimation/02-UI-SPEC.md (§Threshold constants: CUBE_ONLY_CONFIDENCE=0.30, TEXT_CUE_THRESHOLD=0.50)
  </read_first>
  <behavior>
    - test_locate_by_index_multi_record: a label with k>=4 records returns a populated `SubInterval` with `0 <= start <= end <= 1` and confidence > 0.30; for a single-cube label the band width equals `2*POSITION_HALF_WIDTH` (except clamped at the cube edges).
    - test_singleton_full_cube_band (CUBE-10/D-02): k=1 → `SubInterval(start=0.0, end=1.0)`, confidence == 0.30.
    - test_singleton_confidence: k=1 confidence == CUBE_ONLY_CONFIDENCE (0.30).
    - test_band_width_formula: for a known k>1 record with index `idx`, `start == max(0.0, f - POSITION_HALF_WIDTH)` and `end == min(1.0, f + POSITION_HALF_WIDTH)` where `f = idx/(k-1)`.
    - test_fallback_to_cube_only: when confidence would be <= 0.30 (label has no snapshot records, or target release_id not in snapshot — Pitfall B), `locate()` returns `sub_cube_interval=None` and `estimator_version="cube-only-v1"`.
    - test_monotone_within_label: records sorted by `parse_key` produce non-decreasing `sub_cube_interval.start`.
    - test_locate_benchmark (POS-03): p95 over 100 locate() calls < 50 ms, asserted via `benchmark.stats['percentile_95'] < 50`.
  </behavior>
  <action>
    Create `src/gruvax/estimator/constants.py` exporting:
      - `TEXT_CUE_THRESHOLD: float = 0.50` (D-03 text-cue threshold).
      - `POSITION_HALF_WIDTH: float = 0.05` (half-width of the rendered sub-cube band for non-singleton records — gives a 0.10-wide band centered on `f`; D-01/Pitfall 21 — never a zero-width bar).
      - `def compute_confidence(k: int) -> float` per 02-RESEARCH.md (k<=1 → CUBE_ONLY_CONFIDENCE; k<=3 → 0.35+0.05*(k-2); k<=10 → min(0.70, 0.50+0.05*(k-3)); k<=30 → min(0.82, 0.70+0.012*(k-10)); else 0.85). Import `CUBE_ONLY_CONFIDENCE` from contract for the k<=1 case rather than re-literal-ing 0.30.
    Extend `algorithm.py`: add `locate_by_index(release_id, label, catalog_number, cache, snapshot) -> LocateResult`. First call `locate_cube_only(...)` for `primary_cube`/`label_span`/no-boundary handling; if `primary_cube is None` (no covering boundary), return that result unchanged. Otherwise pull `snapshot.get_label_records(label)`, `sorted_recs = sorted(label_records, key=lambda r: parse_key(r.catalog_number))`, `k = len(sorted_recs)`. SPECIAL-CASE singletons FIRST (Pitfall A): if `k == 1`, build `SubInterval(cube=primary_cube, start=0.0, end=1.0, crosses_boundary=False)` with confidence `CUBE_ONLY_CONFIDENCE`, `estimator_version = "index-v1"`. For k>1: find `idx = next((i for i,r in enumerate(sorted_recs) if r.release_id == release_id), None)`; if `None` (Pitfall B — record not in snapshot), `return locate_cube_only(...)`. Compute `f = idx / (k - 1)`. EXACT BAND FORMULA (single-cube case): `start = max(0.0, f - POSITION_HALF_WIDTH)`, `end = min(1.0, f + POSITION_HALF_WIDTH)`; build `SubInterval(cube=primary_cube, start=start, end=end, crosses_boundary=False)`. MULTI-CUBE-SPAN case (len(label_span) > 1): map `f` across the ordered `label_span` to choose the cube `f` falls in (`span_idx = min(len(label_span)-1, int(f * len(label_span)))`), recompute the within-cube fraction, apply the same `±POSITION_HALF_WIDTH` band; when the band would exceed the chosen cube's right edge, set `crosses_boundary=True` and `next_cube = label_span[span_idx+1]`. Set `confidence = compute_confidence(k)`, `estimator_version = "index-v1"`.
    Add `locate(release_id, label, catalog_number, cache, snapshot) -> LocateResult` dispatcher: if `not snapshot.get_label_records(label)` → `result = locate_cube_only(...)`, set `result.estimator_version = "cube-only-v1"`, return it; else `result = locate_by_index(...)`; if `result.confidence <= CUBE_ONLY_CONFIDENCE` return a `LocateResult` with `sub_cube_interval=None`, `estimator_version="cube-only-v1"` (preserve primary_cube/label_span/confidence). Extend `__all__` with `"locate_by_index"`, `"locate"`.
    Extend `tests/unit/test_algorithm.py` with the unit + benchmark tests in <behavior> (add a `_make_snapshot(records)` helper mirroring `_make_cache_from_yaml`; build small in-memory snapshots via `_load_snapshot`).
    CUBE-10 / D-02 RECONCILIATION (document at the top of `locate_by_index`): REQUIREMENTS.md CUBE-10 literal wording is "tick-mark indicator rather than a width-proportional range bar"; per D-02 the owner overrides this to a faint full-cube band — so singletons return `SubInterval(start=0.0, end=1.0)`, NEVER a zero-width/tick bar (Pitfall 21). Non-singletons use the `±POSITION_HALF_WIDTH` band, also never zero-width.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_algorithm.py -x -q && uv run pytest tests/unit/test_algorithm.py -k benchmark --benchmark-only</automated>
  </verify>
  <acceptance_criteria>
    - `algorithm.py` contains `def locate_by_index(` and `def locate(`
    - `constants.py` contains `TEXT_CUE_THRESHOLD`, `POSITION_HALF_WIDTH`, and `def compute_confidence`
    - Band formula is exact: `start = max(0.0, f - POSITION_HALF_WIDTH)`, `end = min(1.0, f + POSITION_HALF_WIDTH)` (test_band_width_formula passes); no zero-width bars
    - Singleton path returns `start=0.0, end=1.0` and confidence 0.30 (test_singleton_full_cube_band passes)
    - Records not in snapshot fall back: `estimator_version == "cube-only-v1"` and `sub_cube_interval is None` (test_fallback_to_cube_only passes)
    - All catalog ordering uses `parse_key` (grep `parse_key` in algorithm.py; no raw `sorted(...key=lambda r: r.catalog_number)` string sort)
    - Benchmark run reports p95 < 50 ms (POS-03); the test asserts `benchmark.stats['percentile_95'] < 50`
    - `uv run pytest tests/unit/test_algorithm.py -x -q` exits 0
  </acceptance_criteria>
  <done>§4.1 produces calibrated sub-intervals with an exact ±POSITION_HALF_WIDTH band, singletons are full-cube bands, fallback selects §4.8, unit + benchmark tests pass, p95 < 50 ms with no DB calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: planted-truth synth_collection generator + golden_cases fixture + property tests + import-path setup</name>
  <files>fixtures/synth_collection.py, fixtures/__init__.py, fixtures/golden_cases.yaml, conftest.py, pyproject.toml, tests/property/test_estimator_props.py, tests/unit/test_algorithm.py</files>
  <read_first>
    - src/gruvax/estimator/algorithm.py (locate / locate_by_index from Task 2a)
    - src/gruvax/estimator/collection_snapshot.py (RecordRow, `_load_snapshot` seam — Task 1)
    - src/gruvax/estimator/boundary_cache.py (BoundaryRow, `_load_rows` seam)
    - src/gruvax/estimator/normalize.py (`parse_key`)
    - tests/conftest.py (FIXTURE_DIR = repo-root `fixtures/`; `boundary_cache` fixture pattern)
    - tests/property/test_parser_props.py (Hypothesis `@given`/`@settings` style to mirror)
    - fixtures/boundaries.yaml (golden_cases.yaml YAML structure analog)
    - pyproject.toml ([tool.pytest.ini_options] — currently no `pythonpath`)
    - .planning/phases/02-real-position-estimation/02-RESEARCH.md (§POS-06 A/B Harness Design — shape factories + Pitfall F; §Golden Cases; §Hypothesis Invariants)
    - .planning/phases/02-real-position-estimation/02-VALIDATION.md (Wave 0 file list — paths must match what this task creates)
  </read_first>
  <behavior>
    - Property invariants (test_estimator_props.py, INTERPOLATION §7.3): `primary_cube in label_span` when not None; `0 <= start <= end <= 1`; monotone position within a label; stability under cosmetic (case/separator/whitespace) noise.
    - test_golden_cases (parametrized): each entry in `fixtures/golden_cases.yaml` produces the expected start/end/confidence/crosses_boundary within tolerance.
    - `from fixtures.synth_collection import all_shapes` resolves in pytest (path setup correct).
  </behavior>
  <action>
    IMPORT-PATH STRATEGY (single source of truth — must match Plan 02-04 and 02-VALIDATION.md):
      - Add `pythonpath = ["."]` to `[tool.pytest.ini_options]` in `pyproject.toml` so the repo root is on `sys.path` for every pytest run → `from fixtures.synth_collection import ...` and (in 02-04) `from scripts.run_all_algorithms import ...` resolve.
      - Create `fixtures/__init__.py` (empty) so `fixtures` is an importable package.
      - Create a repo-root `conftest.py` (empty or with a one-line comment) so pytest treats the repo root as the rootdir and the `pythonpath` setting applies consistently. Do NOT move or duplicate `tests/conftest.py` — the root conftest only anchors rootdir.
    Create `fixtures/synth_collection.py` (repo-root `fixtures/`, matching conftest `FIXTURE_DIR`): a PLAIN Python module (no pytest), factory functions `make_uniform_dense`, `make_sparse_gappy`, `make_multi_prefix`, `make_singleton`, each returning a `(BoundaryCache, CollectionSnapshot, dict[int, float])` triplet built via `_load_rows`/`_load_snapshot` seams (no DB); the dict maps `release_id → planted physical-shelf position fraction in [0,1]`. Pitfall F (critical): plant truth using the PHYSICAL shelf truth, NOT §4.1's own formula. uniform_dense → truth `idx/(k-1)` (validates §4.1). sparse_gappy → gap-weighted cumulative fraction (so §4.1 shows non-zero MAE; §4.8 null → 0.5 shows higher). singleton → 0.5. Add `def all_shapes() -> dict[str, Callable]` returning the named factories (shared by property tests AND Plan 02-04's harness).
    Create `fixtures/golden_cases.yaml` with entries (id, label, catalog_number, k, expected_start, expected_end, expected_confidence, expected_crosses_boundary) covering: singleton; tight pair; small dense k=5; sparse multi-record k=5; multi-prefix BLP/BST k=6; mixed separators; barcode outlier; two-cube span. Add a parametrized `test_golden_cases` to `tests/unit/test_algorithm.py` loading it via `yaml.safe_load` from `FIXTURE_DIR`.
    Create `tests/property/test_estimator_props.py` with the four Hypothesis invariants using a session-scoped synth snapshot/cache built from `fixtures/synth_collection.py` (`@settings(max_examples=...)` like test_parser_props.py). Resolve Open-Question Q3 by adding a session-scoped `synth_snapshot`/`synth_cache` fixture built without DB.
  </action>
  <verify>
    <automated>uv run pytest tests/property/test_estimator_props.py tests/unit/test_algorithm.py -k "golden or props or estimator" -x -q && uv run python -c "from fixtures.synth_collection import all_shapes, make_sparse_gappy; c,s,t = make_sparse_gappy(); assert isinstance(t, dict) and set(all_shapes()) >= {'uniform_dense','sparse_gappy','multi_prefix','singleton'}"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `[tool.pytest.ini_options]` contains `pythonpath = ["."]`
    - `fixtures/__init__.py` and repo-root `conftest.py` exist
    - `fixtures/synth_collection.py` defines `make_uniform_dense`, `make_sparse_gappy`, `make_multi_prefix`, `make_singleton`, `all_shapes`; returns 3-tuples via `_load_snapshot`/`_load_rows` (grep: `_load_snapshot`; no `pool`/`await` in the module)
    - sparse_gappy planted truth is gap-weighted, NOT `idx/(k-1)` (Pitfall F — documented with a comment)
    - `fixtures/golden_cases.yaml` exists with a singleton entry and a two-cube-span entry
    - `tests/property/test_estimator_props.py` exists with the four invariants
    - `from fixtures.synth_collection import all_shapes` resolves (the verify `uv run python -c ...` exits 0)
    - `uv run pytest tests/property/test_estimator_props.py tests/unit/test_algorithm.py -k "golden or props or estimator" -x -q` exits 0
  </acceptance_criteria>
  <done>The shared planted-truth generator (`all_shapes`), golden-case fixture, and Hypothesis invariants exist in repo-root `fixtures/`, importable via the `pythonpath="."` strategy; property + golden tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire snapshot + dispatcher into /api/locate, lifespan, and deps; populate sub_cube_interval JSON</name>
  <files>src/gruvax/api/deps.py, src/gruvax/api/locate.py, src/gruvax/app.py, tests/integration/test_locate.py</files>
  <read_first>
    - src/gruvax/api/deps.py (existing `get_boundary_cache` — copy its 503-on-missing structure for `get_collection_snapshot`)
    - src/gruvax/api/locate.py (existing endpoint + `_cube_ref_to_dict` serializer + 404 pattern; `sub_cube_interval` currently hardcoded null at line 92)
    - src/gruvax/app.py (lifespan step 3 boundary-cache load at lines 83-92 — insert step 3b after it, same try/except/log/state pattern; router import-inside-create_app pattern)
    - src/gruvax/estimator/contract.py (SubInterval field order: cube, start, end, crosses_boundary, next_cube — but the JSON shape the frontend expects per UI-SPEC is {start, end, crosses_boundary, next_cube}, NO `cube` field)
    - tests/integration/test_locate.py (existing `client` fixture + `loop_scope="session"` assertion pattern; existing COVERED_RELEASE_ID constants)
    - .planning/phases/02-real-position-estimation/02-PATTERNS.md (locate.py / deps.py / app.py Pattern Assignments)
    - .planning/phases/02-real-position-estimation/02-UI-SPEC.md (§TypeScript Type Extension — the SubInterval JSON the frontend consumes)
  </read_first>
  <action>
    In `deps.py` add `get_collection_snapshot(request: Request) -> CollectionSnapshot` identical in structure to `get_boundary_cache` (`getattr(request.app.state, "collection_snapshot", None)`, raise 503 with detail `"Collection snapshot not ready"` on None). Import `CollectionSnapshot`.
    In `locate.py` replace the `locate_cube_only` import with `locate` (dispatcher); add `get_collection_snapshot` to the deps import and `CollectionSnapshot` to the type import. Add `snapshot: CollectionSnapshot = Depends(get_collection_snapshot)` as a dependency on the route. Replace the `locate_cube_only(...)` call with `locate(release_id=release_id, label=label, catalog_number=catalog_number, cache=cache, snapshot=snapshot)`. Add a `_sub_interval_to_dict(si: SubInterval) -> dict` helper serializing `{start, end, crosses_boundary, next_cube}` where `next_cube` is `_cube_ref_to_dict(si.next_cube) if si.next_cube else None` — do NOT emit the `cube` field (UI-SPEC TS shape omits it). Replace the hardcoded `"sub_cube_interval": None` with `_sub_interval_to_dict(result.sub_cube_interval) if result.sub_cube_interval else None`.
    In `app.py` lifespan, immediately after the boundary-cache block (after line 91 `app.state.boundary_cache = cache`), add a step "3b. Collection snapshot (POS-03)": import `CollectionSnapshot`, instantiate, `await snapshot.load(pool)` inside try/except logging `"Collection snapshot loaded (%d labels)"` on success and `"Collection snapshot load failed: %s"` on failure (proceed with empty — locate falls back to cube-only), set `app.state.collection_snapshot = snapshot`.
    Extend `tests/integration/test_locate.py` with: `test_sub_cube_interval_populated` (a multi-record covered release returns non-null `sub_cube_interval`), `test_sub_cube_interval_bounds` (`0 <= start <= end <= 1`, and the JSON has no `cube` key), `test_multi_cube_label_span` (CUBE-03 — a boundary-straddling label returns `len(label_span) >= 2`), `test_singleton_full_cube_band` (singleton release → `sub_cube_interval` start 0.0 end 1.0 confidence 0.30). Use the existing `client` fixture and `@pytest.mark.asyncio(loop_scope="session")`. The existing `test_locate_covered` asserting `sub_cube_interval is None` must be updated/retargeted to a release that now legitimately returns null (a singleton or no-snapshot label) — keep the contract assertions consistent with the dispatcher.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_locate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `deps.py` contains `def get_collection_snapshot`
    - `locate.py` imports `locate` (not `locate_cube_only`) and passes `snapshot=` (grep: `Depends(get_collection_snapshot)`)
    - locate JSON serializer emits `sub_cube_interval` keys `start`, `end`, `crosses_boundary`, `next_cube` and NOT `cube` (test_sub_cube_interval_bounds asserts `"cube" not in body["sub_cube_interval"]`)
    - `app.py` lifespan sets `app.state.collection_snapshot` (grep: `app.state.collection_snapshot`)
    - `uv run pytest tests/integration/test_locate.py -x -q` exits 0 with the four new tests present
    - `uv run pytest tests/ -q` exits 0 (full suite green after wiring)
  </acceptance_criteria>
  <done>/api/locate returns populated sub_cube_interval for multi-record labels, full-cube band for singletons, null fallback for no-snapshot labels, and label_span >=2 for straddling labels; all integration tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/locate | `release_id` query param crosses from untrusted HTTP into the estimator |
| gruvax-api → Postgres (v_collection RO) | startup snapshot load reads the shared discogsography view read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (SQLi) | CollectionSnapshot.load SELECT | mitigate | Static SELECT with no user input; only the literal `FROM gruvax.v_collection`. No interpolation. |
| T-02-02 | Tampering | /api/locate release_id | mitigate | FastAPI typed `release_id: int` → 422 on non-int (existing T-01-09); estimator never builds SQL from it |
| T-02-03 | Denial of Service | locate() compute | accept | §4.1 is O(k log k) over <=~95 records/label in-memory; benchmark gate proves p95 < 50 ms; no pathological input path |
| T-02-04 | Information Disclosure | label_span / sub_cube_interval response | accept | Returns only physical shelf-position metadata for owned records; no PII; home-LAN only |
| T-02-05 | Tampering | stale snapshot vs live DB (Pitfall B) | mitigate | Missing release_id in snapshot falls back to cube-only via try/StopIteration → no 500; Phase 4 adds SSE invalidation |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced (RESEARCH §Package Legitimacy Audit: not applicable); no install task in this plan |
</threat_model>

<verification>
- `uv run pytest tests/ -q` is green (unit + property + integration).
- `uv run pytest tests/unit/test_algorithm.py -k benchmark --benchmark-only` reports p95 < 50 ms (POS-03).
- `curl -s "http://localhost:8000/api/locate?release_id=<multi-record-covered-id>"` returns a non-null `sub_cube_interval` with `start`/`end`/`crosses_boundary` and no `cube` key.
- A singleton release returns `sub_cube_interval` `start:0.0 end:1.0 confidence:0.30`.
</verification>

<success_criteria>
- §4.1 `locate_by_index` + `locate` dispatcher implemented behind the unchanged `LocateResult` contract (POS-05).
- In-memory `CollectionSnapshot` loaded at startup; estimator does zero DB calls during compute; p95 < 50 ms (POS-03).
- Singleton → full-cube band start=0.0 end=1.0 confidence=0.30 (CUBE-10/D-02 reconciled).
- Multi-cube labels return label_span >=2 (CUBE-03 backing data).
- All Wave-0 estimator tests (unit, property, golden, benchmark) pass.
</success_criteria>

<output>
Create `.planning/phases/02-real-position-estimation/02-01-SUMMARY.md` when done.
</output>
