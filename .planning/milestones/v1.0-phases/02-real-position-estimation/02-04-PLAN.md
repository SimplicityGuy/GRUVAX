---
phase: 02-real-position-estimation
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/run_all_algorithms.py
  - scripts/__init__.py
  - tests/integration/test_run_all_algorithms.py
autonomous: true
requirements: [POS-06]
nyquist_compliant: true

must_haves:
  truths:
    - "A developer can run `uv run python scripts/run_all_algorithms.py --ci` and see per-distribution-shape MAE, timing, and confidence for §4.1 vs §4.8"
    - "On every synthetic planted-truth shape, §4.1 MAE is <= §4.8 MAE — proving §4.1 is the right v1 default (D-08 reframed: §4.1 >= §4.8 on synthetic shapes + runtime budget). D-09: this is the evidence that index position = shelf position so §4.1 is correct (§4.10 stays deferred; the Phase-1 prefix-first parser already matches the shelf — no parser change is in scope)"
    - "The harness runs the gitignored local CSV when present and skips it gracefully under --ci or when the CSV is absent (repo-hygiene: CSV never committed)"
    - "D-07: The harness genuinely differentiates the algorithms via a synthetic generator that PLANTS known positions across distribution shapes (uniform/sparse-gappy/multi-prefix/singleton), is CI-gated, and does not score §4.1 = 0.00 MAE on every shape by construction (Pitfall F)"
  artifacts:
    - path: "scripts/run_all_algorithms.py"
      provides: "developer A/B harness CLI (§4.1 vs §4.8, per-shape MAE/timing/confidence, --ci flag)"
      contains: "run_all_algorithms"
    - path: "tests/integration/test_run_all_algorithms.py"
      provides: "CI assertion that §4.1 MAE <= §4.8 MAE on every synthetic shape"
      contains: "test_index_beats_or_ties_cube_only"
  key_links:
    - from: "scripts/run_all_algorithms.py"
      to: "gruvax.estimator.algorithm.locate / locate_cube_only"
      via: "runs both estimators against planted-truth collections"
      pattern: "locate"
    - from: "scripts/run_all_algorithms.py"
      to: "fixtures/synth_collection.py (all_shapes — created by Plan 02-01 Task 2b)"
      via: "imports planted-truth shape factories via the pythonpath=. strategy"
      pattern: "synth_collection"
---

<objective>
Build the developer-facing A/B harness (POS-06) that runs §4.1 (index) and §4.8 (cube-only) against the synthetic planted-truth shapes and the gitignored local CSV, emitting per-distribution-shape error metrics — proving §4.1 >= §4.8 before §4.1 is locked as the v1 default (D-07/D-08). After this plan, `uv run python scripts/run_all_algorithms.py --ci` prints a per-shape comparison table and a CI test asserts §4.1 MAE <= §4.8 MAE on every shape.

Purpose: Implements POS-06 — the only tool that can genuinely differentiate the two algorithms on this data (D-07: a rank-only proxy cannot, because §4.1 is rank-based).

Output: `scripts/run_all_algorithms.py` (a standalone CLI, not in the pytest collection), `scripts/__init__.py` (makes the harness importable by the test), and a CI integration test asserting the §4.1 >= §4.8 invariant.

This slice depends on 02-01 because it imports `locate`/`locate_cube_only` (algorithm.py) and the `all_shapes`/`make_*` planted-truth factories that Plan 02-01 Task 2b creates in `fixtures/synth_collection.py`. Plan 04 CONSUMES that generator — it does NOT recreate it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-position-estimation/02-CONTEXT.md
@.planning/phases/02-real-position-estimation/02-RESEARCH.md
@.planning/phases/02-real-position-estimation/02-PATTERNS.md
@.planning/phases/02-real-position-estimation/02-VALIDATION.md
@.planning/phases/02-real-position-estimation/02-01-SUMMARY.md

<interfaces>
<!-- Estimators from Plan 01. From src/gruvax/estimator/algorithm.py -->
def locate(release_id, label, catalog_number, cache, snapshot) -> LocateResult  # §4.1 primary + §4.8 fallback
def locate_cube_only(release_id, label, catalog_number, cache) -> LocateResult   # §4.8 only
LocateResult.sub_cube_interval may be None (§4.8 / fallback). For MAE, §4.8 null → worst-case 0.5 (center-of-cube).

<!-- Planted-truth factories — CREATED by Plan 02-01 Task 2b, CONSUMED here. From fixtures/synth_collection.py -->
all_shapes() -> dict[str, Callable]   # {"uniform_dense", "sparse_gappy", "multi_prefix", "singleton"}
make_uniform_dense(...) / make_sparse_gappy(...) / make_multi_prefix(...) / make_singleton(...)
  each returns (BoundaryCache, CollectionSnapshot, dict[int, float])  # dict = planted truth position per release_id
Pitfall F: sparse_gappy planted truth is GAP-WEIGHTED (NOT idx/(k-1)) so §4.1 shows non-zero MAE and the harness differentiates.

<!-- IMPORT-PATH STRATEGY (single source of truth, established in Plan 02-01 Task 2b). -->
pyproject.toml [tool.pytest.ini_options] has `pythonpath = ["."]` and repo root has conftest.py + fixtures/__init__.py.
→ In tests: `from fixtures.synth_collection import all_shapes` and `from scripts.run_all_algorithms import run_all_algorithms` resolve.
→ This plan ADDS `scripts/__init__.py` so `scripts` is an importable package for the test.
→ For the STANDALONE script run (`uv run python scripts/run_all_algorithms.py`), sys.path[0] is `scripts/`, NOT repo root,
   so the script itself must insert the repo root onto sys.path before importing `fixtures.*` / `gruvax.*`.

<!-- Repo hygiene (CLAUDE.md). Local CSV is gitignored and NEVER committed. -->
CI path: synthetic shapes only. Local path: read the gitignored RWlodarczyk-collection-*.csv + fixtures/boundaries.yaml.
Guard: skip the local-CSV path under --ci or when the CSV file is absent.

<!-- No analog for a CLI script. From 02-PATTERNS.md "No Analog Found" -->
Use standard argparse + `if __name__ == "__main__":`; --ci flag skips local CSV.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: A/B harness CLI (consumes fixtures/synth_collection.all_shapes) with explicit import-path shim</name>
  <files>scripts/run_all_algorithms.py, scripts/__init__.py</files>
  <read_first>
    - fixtures/synth_collection.py (the `all_shapes` + `make_*` factories created by Plan 02-01 Task 2b — CONSUME, do not recreate)
    - src/gruvax/estimator/algorithm.py (locate / locate_cube_only signatures from Plan 01)
    - src/gruvax/estimator/contract.py (LocateResult.sub_cube_interval may be None — §4.8 → MAE worst-case 0.5)
    - pyproject.toml ([tool.pytest.ini_options] `pythonpath = ["."]` — established by Plan 02-01 Task 2b)
    - .planning/phases/02-real-position-estimation/02-RESEARCH.md (§POS-06 metrics output format; CI vs local split + CSV guard; §4.8 null → worst-case 0.5 MAE)
    - .planning/phases/02-real-position-estimation/02-VALIDATION.md (POS-06 verification command `uv run python scripts/run_all_algorithms.py --ci`; import-path note)
    - CLAUDE.md (repo hygiene — CSV + background/ never committed; CI uses synthetic dataset)
  </read_first>
  <action>
    Create `scripts/__init__.py` (empty) so `scripts` is an importable package for the Task 2 test (combined with the `pythonpath = ["."]` set in Plan 02-01 Task 2b).
    Create `scripts/run_all_algorithms.py`. At the TOP, before importing `fixtures.*`/`gruvax.*`, add the standalone-run import shim: compute the repo root as `Path(__file__).resolve().parent.parent` and `sys.path.insert(0, str(repo_root))` if it is not already on `sys.path` — this lets `uv run python scripts/run_all_algorithms.py` resolve `from fixtures.synth_collection import all_shapes` and `from gruvax.estimator.algorithm import locate, locate_cube_only` even though `sys.path[0]` is `scripts/`. (Document why: pytest uses `pythonpath="."`; the standalone script needs the explicit shim.)
    Standard `argparse` CLI with a `--ci` flag (skip local CSV) and `if __name__ == "__main__":`. Define `run_all_algorithms(ci: bool = False) -> dict` returning a per-shape results structure: `{shape: {"index": {"mae", "p95_ms", "confidence_mean"}, "cube_only": {"mae", "p95_ms", "confidence_mean"}}}`. For each shape in `all_shapes()`: build `(cache, snapshot, truth)`, run BOTH `locate(...)` (§4.1) and `locate_cube_only(...)` (§4.8) for every planted release_id, and compute per-shape: MAE of the predicted position fraction vs planted truth (predicted = midpoint `(start+end)/2` of `sub_cube_interval`; for §4.8 / null interval use worst-case `0.5` center-of-cube), per-call timing (track p95; assert aggregate p95 < 50 ms — POS-03 budget), and mean confidence. Print a per-shape table (`§4.1 MAE=… p95_ms=… conf=…` / `§4.8 MAE=… …`) and an aggregate timing line.
    Local-CSV path (only when NOT `--ci` and the gitignored CSV exists): load the real collection from `RWlodarczyk-collection-*.csv` + `fixtures/boundaries.yaml`, run the same metrics. Guard with `if ci or not local_csv_path.exists(): skip` — NEVER fail when the CSV is absent (repo hygiene; CI has no CSV).
  </action>
  <verify>
    <automated>uv run python scripts/run_all_algorithms.py --ci</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/__init__.py` exists (empty)
    - `scripts/run_all_algorithms.py` contains `def run_all_algorithms`, an `argparse` `--ci` flag, and an `if __name__ == "__main__":` guard
    - The script inserts the repo root onto `sys.path` before importing `fixtures.*`/`gruvax.*` (grep: `sys.path.insert`)
    - The script imports `all_shapes` from `fixtures.synth_collection` (grep: `from fixtures.synth_collection import`) — it does NOT redefine `make_*` factories
    - §4.8 null interval is scored as worst-case `0.5` MAE (grep: a `0.5` center-of-cube fallback)
    - The local-CSV path is guarded so `--ci` (and absent CSV) never reads or requires the gitignored CSV (grep: `--ci` / `.exists()` guard)
    - `uv run python scripts/run_all_algorithms.py --ci` exits 0 and prints a per-shape §4.1-vs-§4.8 table
  </acceptance_criteria>
  <done>The standalone A/B harness runs §4.1 vs §4.8 across the planted-truth shapes (consuming Plan 01's generator), emits per-shape MAE/timing/confidence, stays under the 50 ms budget, and never reads the gitignored CSV under --ci.</done>
</task>

<task type="auto">
  <name>Task 2: CI assertion test — §4.1 MAE <= §4.8 MAE on every shape</name>
  <files>tests/integration/test_run_all_algorithms.py</files>
  <read_first>
    - scripts/run_all_algorithms.py (the `run_all_algorithms(ci=True)` function + result structure from Task 1)
    - fixtures/synth_collection.py (all_shapes — Plan 02-01 Task 2b)
    - tests/integration/test_locate.py (existing integration-test structure; pytest patterns)
    - pyproject.toml ([tool.pytest.ini_options] `pythonpath = ["."]` — makes `from scripts.run_all_algorithms import` resolve)
    - .planning/phases/02-real-position-estimation/02-VALIDATION.md (POS-06 row; import-path note)
    - .planning/phases/02-real-position-estimation/02-RESEARCH.md (D-08 reframe: §4.1 >= §4.8 on planted-truth shapes; §4.8 null → 0.5 worst-case)
  </read_first>
  <action>
    Create `tests/integration/test_run_all_algorithms.py` (resolvable via the `pythonpath = ["."]` strategy + `scripts/__init__.py` from Task 1). Add `from scripts.run_all_algorithms import run_all_algorithms`.
    `test_index_beats_or_ties_cube_only_on_all_shapes`: call `run_all_algorithms(ci=True)` once and assert, for every synthetic shape, `result[shape]["index"]["mae"] <= result[shape]["cube_only"]["mae"]` (D-08 reframed: §4.1 >= §4.8 on planted-truth shapes). Use `pytest.approx` tolerance where ties are expected.
    `test_harness_aggregate_under_budget`: assert the aggregate p95 across shapes is < 50 ms (POS-03). Mark/guard the local-CSV portion as skipped under CI so the test never requires the gitignored CSV. This test file IS collected by pytest (the harness script is not).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_run_all_algorithms.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_run_all_algorithms.py` exists and imports `from scripts.run_all_algorithms import run_all_algorithms`
    - Contains `test_index_beats_or_ties_cube_only_on_all_shapes` asserting `index.mae <= cube_only.mae` for every shape
    - Contains `test_harness_aggregate_under_budget` asserting aggregate p95 < 50 ms
    - The test never reads the gitignored CSV (calls `run_all_algorithms(ci=True)`)
    - `uv run pytest tests/integration/test_run_all_algorithms.py -x -q` exits 0
  </acceptance_criteria>
  <done>A CI test proves §4.1 MAE <= §4.8 MAE on every planted-truth shape and that the aggregate compute stays under the 50 ms budget, without ever requiring the local CSV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CSV file → harness (dev only) | the gitignored owner collection CSV is read locally; never committed (repo hygiene) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Information Disclosure | local collection CSV in CI artifacts | mitigate | `--ci` and absent-file guards prevent the CSV from being read in CI; CLAUDE.md repo hygiene keeps it gitignored; harness output is aggregate metrics, not row data |
| T-02-15 | Tampering | harness imports production estimator | accept | Read-only use of `locate`/`locate_cube_only`; no DB, no mutation; in-memory seams only |
| T-02-16 | Repudiation | A/B result used to lock §4.1 | accept | Result is a developer decision aid (D-08); not a runtime control surface; CI test makes the §4.1>=§4.8 claim reproducible |
| T-02-17 | Tampering | sys.path.insert in harness shim | accept | Inserts only the computed repo root (`Path(__file__).resolve().parent.parent`); no env/user input; dev tooling, not request path |
| T-02-SC | Tampering | pip installs | accept | No new packages — harness uses stdlib argparse + existing estimator (RESEARCH: no additions); no install task |
</threat_model>

<verification>
- `uv run python scripts/run_all_algorithms.py --ci` prints a per-shape §4.1 vs §4.8 table and exits 0.
- `uv run pytest tests/integration/test_run_all_algorithms.py -x -q` is green (§4.1 MAE <= §4.8 MAE on every shape; aggregate p95 < 50 ms).
- `git status` shows no collection CSV staged (repo hygiene preserved).
</verification>

<success_criteria>
- POS-06: developer A/B harness exists, runs §4.1 and §4.8 against synthetic planted-truth shapes (CI) and the local CSV (when present, gitignored), and emits per-distribution-shape MAE/timing/confidence.
- §4.1 >= §4.8 proven on every synthetic shape (D-07/D-08); the harness genuinely differentiates (Pitfall F avoided).
- The harness imports resolve via the single `pythonpath="."` + `scripts/__init__.py` + script sys.path-shim strategy (consistent with Plan 02-01).
- The CSV is never committed and never required in CI.
</success_criteria>

<output>
Create `.planning/phases/02-real-position-estimation/02-04-SUMMARY.md` when done.
</output>
