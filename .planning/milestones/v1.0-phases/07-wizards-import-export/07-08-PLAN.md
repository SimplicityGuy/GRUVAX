---
phase: 07-wizards-import-export
plan: 08
type: execute
wave: 2
depends_on: ["07-07"]
files_modified:
  - frontend/src/api/adminClient.ts
  - frontend/src/routes/admin/Import.tsx
  - frontend/src/routes/admin/Settings.tsx
autonomous: false
gap_closure: true
requirements: [ADMN-05, BAK-02, BAK-01]
must_haves:
  truths:
    - "Import upload runs a DRY-RUN preview (no write); the DB changes ONLY when COMMIT IMPORT is tapped"
    - "Settings import of a freshly exported settings.yaml applies all keys and shows 'Settings applied.'"
    - "Boundaries import works through the real UI path (client + server agree on the body contract)"
    - "After import, History shows source badges and the confirmation REVERT THIS CHANGE SET tap works"
  artifacts:
    - path: "frontend/src/api/adminClient.ts"
      provides: "Raw-body uploads (text/csv | application/x-yaml) + dry_run preview call"
      contains: "application/x-yaml"
    - path: "frontend/src/routes/admin/Import.tsx"
      provides: "Preview wired to dry_run; commit only on COMMIT IMPORT tap"
      contains: "dry_run"
  key_links:
    - from: "frontend/src/routes/admin/Import.tsx"
      to: "POST /api/admin/import/boundaries?dry_run=true"
      via: "preview call returns diff + errors, no write"
      pattern: "dry_run"
    - from: "frontend/src/api/adminClient.ts uploadImportSettings"
      to: "POST /api/admin/import/settings (raw body)"
      via: "body: file with Content-Type application/x-yaml"
      pattern: "application/x-yaml"
---

<objective>
Close the frontend half of the import gaps and re-verify the SC5 items that the import
breakage blocked:

- **G2 (UAT test 3, major):** `Import.tsx` calls the atomic COMMIT endpoint for its "validation
  pass" (clean files commit on upload; COMMIT IMPORT is an idempotency no-op). Wire the preview
  to the new `POST /api/admin/import/boundaries?dry_run=true` (plan 07-07) — a true dry-run that
  returns the diff + per-row errors with NO write — and commit via the same endpoint WITHOUT
  `dry_run` ONLY when COMMIT IMPORT is tapped (D-11 flow: preview → gated COMMIT → atomic replace).

- **G4 (UAT test 5, major):** The clients send MULTIPART FormData but BOTH backend endpoints read
  the RAW request body and `yaml.safe_load` it. Settings import is BROKEN (422 on the multipart
  wrapper); boundaries import is fragile. Fix the CLIENT to send the RAW file bytes with the right
  Content-Type (`text/csv` or `application/x-yaml`, derived from extension/type) for BOTH
  `uploadImportBoundaries` and `uploadImportSettings`, dropping FormData. This matches the existing
  backend raw-body reader AND the host integration tests (which already send raw bytes), so no
  backend or test rewrite is needed.

- **SC5 re-verification fold-in (no new feature):** Once import works end-to-end, re-check that
  History shows the D-04 source badges (CSV IMPORT / YAML IMPORT / WIZARD SETUP / RESHUFFLE) after
  a commit, and that the confirmation's "REVERT THIS CHANGE SET" tap (D-15) navigates to
  `/admin/history?highlight=<id>` and the revert works. These are existing surfaces; this plan only
  ensures they are exercised once the import flow is unblocked (verified at the human checkpoint).

Purpose: ADMN-05 (validate + diff preview before atomic replace, D-11), BAK-02 (settings
export → import round-trip), BAK-01/SC5 (history legibility + keystone undo).

Output: `adminClient.ts` raw-body uploads + a dry_run preview call; `Import.tsx` preview wired to
dry_run with commit gated on the COMMIT IMPORT tap; `Settings.tsx` settings-import round-trip
fixed; a human-verify checkpoint covering the full import → history-badge → revert flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-wizards-import-export/07-UAT.md
@.planning/phases/07-wizards-import-export/07-CONTEXT.md
@.planning/phases/07-wizards-import-export/07-UI-SPEC.md
@.planning/phases/07-wizards-import-export/07-07-SUMMARY.md

# Files being modified (read in full before editing):
@frontend/src/api/adminClient.ts
@frontend/src/routes/admin/Import.tsx
@frontend/src/routes/admin/Settings.tsx

# Consumed contracts:
@frontend/src/api/types.ts

<interfaces>
<!-- Established + new (07-07) contracts the executor uses directly. -->

NEW from plan 07-07 (backend dry_run preview):
- `POST /api/admin/import/boundaries?dry_run=true` with the RAW file bytes as body:
    - On validation errors: HTTP 400 — same shapes Import.tsx already parses
      (`{type, message, near_misses, ...}` or `{errors: [...]}`).
    - On success: HTTP 200 with `{total_cubes, file_cube_count, diff_preview: [{unit_id, row,
      col, delta, will_be_empty}]}` — EXACTLY the shape `Import.tsx`'s `parseDiff()` already reads.
    - dry_run performs NO DB write and mints NO change_set_id.
- `POST /api/admin/import/boundaries` (no dry_run): atomic commit → `{change_set_id, applied, source}`.

From frontend/src/api/adminClient.ts (current — to be fixed):
- `adminFetch(path, options)` injects `X-CSRF-Token` for mutating methods; if `body` is FormData
  it omits Content-Type. For raw-body uploads, PASS an explicit `headers: {'Content-Type': ...}`
  and a non-FormData `body` (the raw `File` is an acceptable fetch body — the browser streams its
  bytes; do NOT wrap it in FormData).
- `uploadImportBoundaries(file, idempotencyKey)` and `uploadImportSettings(file)` currently build
  `FormData` — REPLACE with raw-body sends.
- `downloadBoundariesYaml()` / `downloadSettingsYaml()` already work (do not change).

From frontend/src/routes/admin/Import.tsx (current — to be fixed):
- `runValidation(file)` calls `uploadImportBoundaries` (the COMMIT endpoint) and treats a 200 as
  "validated"; `handleCommit()` then no-ops via the stored commitResult/idempotency key.
- `parseServerErrors(body)` + `parseDiff(body)` already handle the 400 error shapes and the
  diff_preview/file_cube_count/total_cubes 200 shape. KEEP these.
- The confirmation navigates to `/admin/wizard/done?change_set_id=...&applied=...&source=csv|yaml`.

From 07-UI-SPEC.md (locked copy — unchanged):
- Import: `COMMIT IMPORT` enabled only at zero errors; movement counts suffixed `(approx.)`;
  partial-import warning when file_cube_count < total_cubes.
- Settings import success: `Settings applied.` in `--gruvax-success`. Failure copy:
  `Settings could not be applied. Check that the file is a valid GRUVAX settings export.`
- History badges (D-04): csv → CSV IMPORT, yaml → YAML IMPORT, wizard → WIZARD SETUP,
  reshuffle → RESHUFFLE.
- Confirmation revert: `REVERT THIS CHANGE SET` → `/admin/history?highlight=<id>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the import upload contract — raw body, not multipart (G4) + add a dry_run preview call</name>
  <read_first>
    - frontend/src/api/adminClient.ts (the full file — adminFetch, uploadImportBoundaries ~L633, uploadImportSettings ~L666, downloadBoundariesYaml/downloadSettingsYaml)
    - tests/integration/test_import.py + tests/integration/test_settings_import.py (confirm the backend contract: raw bytes + Content-Type text/csv | application/x-yaml)
    - src/gruvax/api/admin/import_.py `_detect_format` (Content-Type / Content-Disposition detection the client must satisfy)
  </read_first>
  <action>
    Make the client body contract match the backend raw-body reader (the contract the host
    integration tests already use). Pick the RECOMMENDED option per the UAT G4 decision: clients
    send RAW file bytes, NOT FormData.

    `uploadImportBoundaries(file, idempotencyKey, dryRun = false)`:
    - Derive the Content-Type from the file extension/type: `.csv` → `text/csv`; `.yaml`/`.yml`
      → `application/x-yaml`. (Use the filename extension; fall back to `file.type` if present.)
    - Send the raw `File` as the request body (`body: file`) with that explicit `Content-Type`
      header. Do NOT use FormData. `adminFetch` still injects `X-CSRF-Token` for the POST.
    - Add a `dryRun` parameter that, when true, targets
      `/api/admin/import/boundaries?dry_run=true` and does NOT require/consume an Idempotency-Key
      (a preview is not idempotency-tracked). When false, target `/api/admin/import/boundaries`
      and send the `Idempotency-Key` header as today.
    - On a 400/422, parse the structured JSON body and throw `BulkSaveError` (existing pattern).
      ERROR-BODY CONTRACT (W6 — LOCKED, do not leave open): add a public readonly `body` property
      to the `BulkSaveError` class carrying the FULL parsed JSON object (`Record<string, unknown>`),
      in ADDITION to the existing `errorType` / `serverMessage`. Set `body` on every throw from both
      upload functions. This is the minimal change to `Import.tsx`'s existing `catch` — it reads
      `err.body` (the structured `{type, message, near_misses, errors, diff_preview, ...}`) instead
      of re-parsing a stringified message. Apply this ONE shape consistently for all 4xx throws in
      this module; document `body` in the `BulkSaveError` docstring. (Do NOT switch to a
      `{ok, status, body}` return shape — keep throwing, just attach `.body`.)

    `uploadImportSettings(file)`:
    - Send the raw `File` as the body with `Content-Type: application/x-yaml` (settings export is
      YAML). Drop FormData. No Idempotency-Key (settings import is idempotent by nature).
    - RETURN TYPE (B2 — the backend returns `{"updated": [...keys...]}`, NOT `applied`): change the
      signature from `Promise<{ applied: string[] }>` to `Promise<{ updated: string[] }>` and parse
      `.updated` from the success body. Do NOT read `.applied` (no such field exists server-side).
    - On non-OK, throw `BulkSaveError` (with `.body` per the contract above) so Settings.tsx can
      show the locked failure copy.

    Keep `downloadBoundariesYaml` / `downloadSettingsYaml` unchanged. Do NOT change `adminFetch`'s
    FormData branch (other callers may rely on it) — just stop using FormData for these two
    upload functions and pass an explicit Content-Type instead. No hardcoded hex (N/A here), all
    strings via the existing error classes; no innerHTML.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "application/x-yaml|text/csv" frontend/src/api/adminClient.ts` returns ≥2 matches
      (raw-body Content-Types).
    - `grep -nE "new FormData\(\)" frontend/src/api/adminClient.ts` returns 0 matches inside
      `uploadImportBoundaries` and `uploadImportSettings` (FormData dropped for both; if FormData
      remains elsewhere for unrelated callers that is fine — confirm the two upload fns no longer use it).
    - `grep -nE "dry_run=true|dryRun" frontend/src/api/adminClient.ts` returns ≥1 (preview call path).
    - `grep -nE "Idempotency-Key" frontend/src/api/adminClient.ts` still present on the COMMIT
      (non-dryRun) boundaries path.
    - B2: `grep -nE "Promise<\{ updated: string\[\] \}>|updated: string\[\]" frontend/src/api/adminClient.ts`
      returns ≥1 — `uploadImportSettings` returns `{ updated: string[] }` (NOT `{ applied: ... }`);
      `grep -nE "applied: string\[\]" frontend/src/api/adminClient.ts` returns 0.
    - W6: `BulkSaveError` carries a `body` property — `grep -nE "\bbody\b" frontend/src/api/adminClient.ts`
      confirms `body` is set on every 4xx throw in the two upload functions (read the class +
      throw sites to confirm `new BulkSaveError(..., body)` / `err.body` is the single agreed shape).
    - `cd frontend && npx tsc --noEmit` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>Both upload functions send raw file bytes with the correct Content-Type; a dry_run preview variant exists for boundaries; settings upload returns {updated} and no longer wraps the file in multipart; BulkSaveError carries the parsed `.body` for callers.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Import preview to dry_run; commit ONLY on COMMIT IMPORT (G2 + D-11)</name>
  <read_first>
    - frontend/src/routes/admin/Import.tsx (the full file — runValidation ~L374, handleCommit ~L519, parseServerErrors, parseDiff, DiffGrid, the COMMIT IMPORT button)
    - frontend/src/api/adminClient.ts (the Task-1 raw-body + dryRun signatures)
    - .planning/phases/07-wizards-import-export/07-UAT.md (G2 gap + the inverted preview-after-write root cause)
    - .planning/phases/07-wizards-import-export/07-UI-SPEC.md §"Surface 2: /admin/import"
  </read_first>
  <action>
    Replace the fake "validation pass = commit" logic with a true dry-run preview, then a real
    gated commit.

    `runValidation(file)` (the upload handler):
    - Call `uploadImportBoundaries(file, <no idempotency key needed>, /*dryRun*/ true)` — the
      dry_run preview. Parse the result:
        - On a 200 preview body: feed it through the EXISTING `parseDiff(body)` to populate
          `diff` / `fileCubeCount` / `totalCubes`, set `phase: 'validated'`, and clear errors.
        - On a 400/422: catch the `BulkSaveError` and read `err.body` (the W6 contract — the FULL
          parsed JSON). Feed `err.body` through the EXISTING `parseServerErrors(body)` for the
          per-row error cards (with did-you-mean chips) AND `parseDiff(err.body)` for any partial
          diff, set `phase: 'validated'`. The COMMIT button stays disabled until errors are fixed.
          Consume `err.body` for BOTH the 200 (diff) and 4xx (errors) paths — do NOT re-parse a
          stringified message.
    - B1 — NO-OP REGRESSION GUARD: `runValidation` MUST NOT assign `state.commitResult` from the
      dry-run preview (the dry-run mints NO change_set_id and writes nothing — a commitResult here
      is exactly what let the old no-op return). Removing only the handleCommit branch is NOT
      sufficient; a stale `commitResult` could re-enable the no-op. Prefer DELETING the
      `commitResult` field from `ImportState` entirely if nothing else needs it after this rewrite
      (handleCommit can navigate directly from the awaited commit response without storing it). If
      the field must stay, the ONLY assignment to `commitResult` in the whole file must be inside
      `handleCommit` after a successful NON-dry-run commit.
    - Remove the misleading implementation-note comments (lines ~388-396) that describe reusing
      the commit endpoint for preview; replace with a short comment pointing at the dry_run contract.

    `handleCommit()`:
    - DELETE the "skip re-post if we already have a commitResult/idempotencyKey" branch — that was
      the no-op bug. The commit MUST be a real write that happens only now.
    - W4 — chip-fixed path still posts: `handleCommit` MUST ALWAYS call the real (non-dryRun)
      `uploadImportBoundaries` even when every error was chip-fixed. Do NOT add any short-circuit
      keyed on `state.errors.every(e => e.fixed)` (or similar) that returns/navigates without
      posting. Applying did-you-mean chips only flips local `fixed` flags to enable the button; it
      does NOT pre-commit anything — the actual write happens here, on the COMMIT IMPORT tap.
    - Generate a fresh `crypto.randomUUID()` Idempotency-Key, set `phase: 'committing'`, and call
      `uploadImportBoundaries(state.file, key, /*dryRun*/ false)` (the atomic COMMIT). On success,
      navigate to the confirmation route with `change_set_id`, `applied`, and `source` (csv|yaml)
      exactly as today. On failure, show the locked copy `Import failed — check your connection and
      try again. Your collection has not changed.`
    - The applied-near-miss flow (did-you-mean chips) currently only flips a `fixed` flag locally.
      Because the dry_run preview now re-runs on the server, ensure the commit reflects fixes: if
      the file content itself is unchanged, applying a suggestion in the UI does not change the
      bytes sent to the server. Keep the existing UI behavior (chips mark errors FIXED to enable
      COMMIT) BUT add a guard so COMMIT is only enabled when there are zero ACTIVE server errors —
      i.e. a fixed-by-chip error must correspond to an actual re-validation pass. Simplest correct
      approach: when the user has applied any near-miss fix, the commit path is NOT a no-op (it now
      really posts); if the server still rejects (because the file bytes were not actually edited),
      surface the returned errors again rather than silently committing. Document this so the
      executor does not reintroduce the no-op shortcut.

    Keep `parseServerErrors`, `parseDiff`, `DiffGrid`, `ErrorCard`, the partial-import warning, the
    `(approx.)` suffix, and the COMMIT-disabled-until-zero-errors gating. All color via
    `--gruvax-*` tokens (no hardcoded hex); all strings via JSX `{}` (never innerHTML).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "dryRun|dry_run" frontend/src/routes/admin/Import.tsx` returns ≥1 (preview uses dry-run).
    - `grep -nE "commitResult && state.idempotencyKey" frontend/src/routes/admin/Import.tsx`
      returns 0 (the no-op short-circuit is removed).
    - B1: `runValidation` MUST NOT assign `state.commitResult` from the dry-run preview. EITHER the
      `commitResult` field is removed from `ImportState` (`grep -nE "commitResult" frontend/src/routes/admin/Import.tsx`
      returns 0), OR the ONLY `commitResult` assignment is inside `handleCommit` after a successful
      NON-dry-run commit — confirm by reading every assignment site: no `commitResult:` appears in
      `runValidation` or its `setState` calls.
    - W4: `grep -nE "errors.every\(.*fixed|every\(e => e.fixed" frontend/src/routes/admin/Import.tsx`
      shows NO early-return/short-circuit inside `handleCommit` keyed on all-fixed — handleCommit
      always reaches the real `uploadImportBoundaries(state.file, key, false)` call.
    - W6: the 4xx preview path reads `err.body` (the BulkSaveError parsed JSON) — `grep -nE "\.body"
      frontend/src/routes/admin/Import.tsx` returns ≥1 and both `parseDiff` and `parseServerErrors`
      are fed from the same `err.body` object on the error path.
    - `grep -nE "uploadImportBoundaries\(" frontend/src/routes/admin/Import.tsx` shows the
      dry-run call in the preview path AND the non-dry-run call in handleCommit.
    - `grep -c "innerHTML" frontend/src/routes/admin/Import.tsx` returns 0.
    - `grep -nE "#[0-9a-fA-F]{3,6}" frontend/src/routes/admin/Import.tsx` returns 0
      (no hardcoded hex; note inline Lucide SVGs use currentColor/stroke — confirm no hex added).
    - `cd frontend && npx tsc --noEmit` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>Uploading a clean file shows the diff with NO write; the DB changes only when COMMIT IMPORT is tapped; phantom files show per-row error cards with did-you-mean chips and a disabled COMMIT.</done>
</task>

<task type="auto">
  <name>Task 3: Fix settings import round-trip in Settings BACKUP & RESTORE (G4 settings)</name>
  <read_first>
    - frontend/src/routes/admin/Settings.tsx (the BACKUP & RESTORE section + the settings-import handler)
    - frontend/src/api/adminClient.ts uploadImportSettings (Task-1 raw-body version)
    - src/gruvax/api/admin/import_.py import_settings (the raw-body YAML reader + allowed-keys validation)
    - .planning/phases/07-wizards-import-export/07-UI-SPEC.md §"Surface 6" settings import copy + §Copywriting Contract
  </read_first>
  <action>
    The settings-import handler in Settings.tsx calls `uploadImportSettings`, which (after Task 1)
    now sends the raw YAML body with `Content-Type: application/x-yaml` — matching the backend.
    Verify and finish the wiring so a freshly EXPORTED settings.yaml round-trips:
    - The IMPORT SETTINGS control reads the chosen `.yaml`/`.yml` file and calls
      `uploadImportSettings(file)`.
    - On success (the backend returns `{updated: [...]}`), show the locked success copy
      `Settings applied.` in `--gruvax-success`. (If the existing code checks for an `applied`
      key but the backend returns `updated`, reconcile the field name so success is detected —
      read the backend response shape: `import_settings` returns `{"updated": [...]}`.)
    - On failure, show the locked copy `Settings could not be applied. Check that the file is a
      valid GRUVAX settings export.`
    - Confirm the in-process settings cache reload on the backend covers propagation (it does —
      import_settings refreshes `app.state.settings_cache`); no extra client refresh needed beyond
      re-reading settings if the page already does so.
    - Do NOT change the EXPORT SETTINGS button (it works and excludes the PIN — verified). Do NOT
      reintroduce FormData. All color via `--gruvax-*` tokens; strings via JSX `{}` only.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "uploadImportSettings" frontend/src/routes/admin/Settings.tsx` returns ≥1.
    - `grep -nE "Settings applied\." frontend/src/routes/admin/Settings.tsx` returns ≥1 (success copy).
    - `grep -nE "Settings could not be applied" frontend/src/routes/admin/Settings.tsx` returns ≥1 (failure copy).
    - B2: `grep -nE "\.updated" frontend/src/routes/admin/Settings.tsx` returns ≥1 — the success
      handler reads the `.updated` field from the `uploadImportSettings` result (so the
      "Settings applied." path actually fires). `grep -nE "\.applied" frontend/src/routes/admin/Settings.tsx`
      must NOT gate the settings-import success on an `applied` field (no such field server-side).
    - The success check matches the backend response field (`updated`) — confirm by reading the
      handler against import_settings' `{"updated": [...]}` return.
    - `grep -c "innerHTML" frontend/src/routes/admin/Settings.tsx` returns 0.
    - `grep -nE "#[0-9a-fA-F]{3,6}" frontend/src/routes/admin/Settings.tsx` returns 0.
    - `cd frontend && npx tsc --noEmit` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>Exporting settings.yaml then IMPORT SETTINGS on that exact file shows 'Settings applied.' (green); a non-YAML file shows the failure copy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify — import dry-run + identity round-trip + settings round-trip + SC5 badges/revert + reshuffle entry</name>
  <read_first>
    - .planning/phases/07-wizards-import-export/07-UAT.md (the 4 gaps + the SC5 fold-in items)
    - .planning/phases/07-wizards-import-export/07-UI-SPEC.md §"Surface 2" + §"Surface 4" + §"Surface 5"
  </read_first>
  <action>
    Rebuild the running stack (the project uses `just up` / Docker Compose on the dev DB; rebuild
    api + frontend if needed), log into /admin, then walk the human-verify steps below against
    SYNTHETIC or dev-v_collection data only (never the real collection CSV). Pause for the owner to
    confirm the five end-to-end flows below before marking the gaps closed.
  </action>
  <what-built>
    Full import flow fixed end-to-end: (a) boundaries import is now a true dry-run preview that
    only writes on COMMIT IMPORT (G2); (b) export → re-import is an identity — a freshly exported
    boundaries.yaml re-imports with zero diff (G3, backend in 07-07); (c) the upload contract is
    raw body, so settings import works through the UI (G4); plus the previously-blocked SC5 checks
    (history source badges D-04, confirmation REVERT THIS CHANGE SET tap D-15).
  </what-built>
  <how-to-verify>
    Run the stack (the project uses `just up` / Docker Compose on the dev DB; rebuild the api +
    frontend if needed). Log into /admin. Then:

    1. IMPORT DRY-RUN (G2): On /admin/cubes, EXPORT BOUNDARIES → save boundaries.yaml. Edit it to
       change exactly ~3 cubes to OTHER catalog numbers that EXIST in your dev v_collection. Go to
       /admin/import, upload the edited file. EXPECT: the AFFECTED CUBES grid lights ~3 cubes
       yellow with deltas suffixed "(approx.)"; COMMIT IMPORT is enabled; NOTHING has changed yet
       — open /admin/cubes in another tab and confirm the boundaries are UNCHANGED before you
       commit. Then tap COMMIT IMPORT → confirmation names a change_set_id; now /admin/cubes
       reflects the change.

    2. EXPORT IDENTITY (G3/SC4): On /admin/cubes, EXPORT BOUNDARIES again (unedited). Re-import
       that exact file at /admin/import. EXPECT: the diff shows ZERO cubes changing and COMMIT
       IMPORT is enabled with zero errors (export → re-import = identity). (No need to commit.)

    3. SETTINGS ROUND-TRIP (G4): /admin/settings → BACKUP & RESTORE → EXPORT SETTINGS (confirm the
       downloaded settings.yaml has NO pin_hash). Then IMPORT SETTINGS with that exact file.
       EXPECT: "Settings applied." in green. Then import a non-YAML file → EXPECT the failure copy.

    4. SC5 BADGES + REVERT (D-04 / D-15): Open /admin/history. EXPECT the recent import shows a
       CSV IMPORT or YAML IMPORT badge (and a prior wizard commit shows WIZARD SETUP / a reshuffle
       shows RESHUFFLE, yellow-tinted). From the import confirmation screen, tap REVERT THIS CHANGE
       SET → EXPECT navigation to /admin/history?highlight=<id> with the revert confirm available;
       complete the revert and confirm the boundaries restore.

    5. RESHUFFLE ENTRY (G1, from plan 07-06): /admin/wizard with no draft shows START SETUP WIZARD
       + START RESHUFFLE; START RESHUFFLE opens reshuffle mode with existing cut points pre-loaded.
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe what failed (which step, observed vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin SPA | User-selected files crossing into upload calls |
| SPA upload call → admin API | Raw file bytes posted to /import/* (admin-gated, CSRF) |
| API 4xx error body → Import.tsx render | Server-supplied error strings (labels/catalogs) rendered in error cards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0708-XSS | Tampering (XSS) | Import.tsx error cards / diff render / Settings result | mitigate | All server strings (labels, catalogs, near-miss text, messages) rendered via JSX `{}` interpolation (textContent semantics); never innerHTML (CLAUDE.md / sketch-findings). |
| T-0708-CSRF | CSRF | raw-body upload POSTs | mitigate | Uploads route through `adminFetch`, which injects `X-CSRF-Token` for mutating methods; do NOT bypass adminFetch for the raw-body sends. |
| T-0708-SIZE | DoS | client upload | mitigate | Client keeps the existing 100 KB pre-flight size check in Import.tsx; backend enforces its own 100 KB cap (defense in depth). |
| T-0708-NOOP-COMMIT | Tampering (integrity) | handleCommit | mitigate | Remove the no-op short-circuit so COMMIT is a real atomic write; the dry-run preview never mints a change_set_id (07-07). Verified at the human checkpoint (cubes unchanged pre-commit). |
| T-0708-PIN-LEAK | Information Disclosure | settings export/import | accept (verified) | EXPORT SETTINGS already excludes pin_hash (allowlist, D-14, verified live); import rejects auth.* keys server-side. No client change affects this. |
| T-0708-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (07-UI-SPEC Registry Safety: no new npm packages in Phase 7). Edits to existing files only. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0; `cd frontend && npm run build` exits 0.
- Backend dry_run + identity behavior is covered by 07-07's integration tests (run together:
  `uv run pytest tests/integration/test_import.py tests/integration/test_import_roundtrip_identity.py tests/integration/test_settings_import.py -q`).
- The human-verify checkpoint confirms the five end-to-end flows (dry-run, identity, settings
  round-trip, SC5 badges + revert, reshuffle entry).
</verification>

<success_criteria>
Boundaries import previews via a true dry-run and only writes on COMMIT IMPORT; a freshly
exported boundaries.yaml re-imports as an identity through the UI; settings export → import
round-trips with "Settings applied."; History shows the D-04 source badges and the confirmation
REVERT tap works; tsc + build are clean.
</success_criteria>

<output>
Create `.planning/phases/07-wizards-import-export/07-08-SUMMARY.md` when done.
</output>
