---
phase: 06-led-contract-over-mqtt-hardware-stubbed
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/unit/test_led_color.py
  - tests/unit/test_admin_led_settings.py
  - src/gruvax/api/admin/settings.py
  - frontend/src/api/types.ts
  - frontend/src/api/adminClient.ts
  - frontend/src/components/ColorBlindPreview.tsx
  - frontend/src/routes/admin/Settings.tsx
  - frontend/src/routes/admin/admin.css
autonomous: true
requirements: [LED-04, LED-05]
nyquist_compliant: true

must_haves:
  truths:
    - "Admin can tune label-span, position, error, setup, all-off, and idle/ambient LED colors via a new LEDs section in the existing Settings page; this LED palette is a SEPARATE surface from the kiosk UI's Nordic Grid colors (kiosk lit cell stays yellow); values persist to gruvax.settings and refresh the settings_cache so the next illuminate uses them (LED-05 / D-04, D-05, D-15, D-19)"
    - "Admin can set THREE brightness tiers — span (label-span ~30-50%), active (position 100%), and ambient (idle baseline) — and the server clamps every payload's brightness to the relevant ceiling; the keys are led_brightness.span / led_brightness.active / led_brightness.ambient and the UI labels never call the label-span tier ambient (LED-04 / D-07, D-24)"
    - "Admin can configure the active-highlight TTL (led_highlight.active_ttl_seconds, default 180), the retain-mode toggle (led_highlight.retain_mode, default false), and the retain timeout (led_highlight.retain_ttl_seconds, default 900) from the LEDs section; these feed the lifecycle slice (LED-04 surface / D-21, D-23, D-25)"
    - "Defaults are accessibility-respecting: position gold #FFD700, span purple #7C3AED — NOT red/green for active/error; brightness is a second information channel (LED-05 / D-05)"
    - "A lightweight color-blind preview (deuteranopia/protanopia/tritanopia matrix simulation) renders next to each color picker, in-SPA with no new packages, so an inaccessible pair is caught at pick time (LED-05 / D-18)"
    - "The LEDs section reuses the existing Settings shell, SaveStatus state machine, and admin.css patterns with ALL-CAPS Barlow labels and design-token swatches — no new route (D-19)"
    - "Transition styles ship as fixed per-state defaults (defined in Plan 06-01) with NO admin transition editor in v1 — the writable settings key allow-list deliberately excludes the led_transition.* keys (D-16, D-17)"
    - "Color settings are stored as JSON strings (#RRGGBB); brightness and TTL stored as integers; retain_mode stored as a boolean; the publisher/lifecycle resolve them at publish time (D-06)"
  artifacts:
    - path: "frontend/src/components/ColorBlindPreview.tsx"
      provides: "In-SPA color-blind simulation preview (3x3 matrix multiply, zero deps)"
      contains: "simulateColorBlindness"
    - path: "src/gruvax/api/admin/settings.py"
      provides: "Extended _ALLOWED_SETTINGS_KEYS + key_map + GET/PUT covering LED color/brightness/ambient/TTL/retain keys"
      contains: "led_color.ambient"
    - path: "tests/unit/test_led_color.py"
      provides: "Tests for color-blind simulation invariants and the gold/purple distinguishability"
      contains: "def test_colorblind_preview"
    - path: "tests/unit/test_admin_led_settings.py"
      provides: "GET/PUT persistence + cache refresh for all LED color/brightness/ambient/TTL/retain keys + the D-24 naming guard"
      contains: "def test_put_led_settings_persists_and_caches"
  key_links:
    - from: "frontend/src/routes/admin/Settings.tsx"
      to: "/api/admin/settings (PUT)"
      via: "putAdminSettings with LED keys"
      pattern: "led_color"
    - from: "src/gruvax/api/admin/settings.py"
      to: "app.state.settings_cache"
      via: "load_settings_cache(pool) after PUT"
      pattern: "load_settings_cache"
    - from: "frontend/src/routes/admin/Settings.tsx"
      to: "ColorBlindPreview"
      via: "JSX render next to each color input"
      pattern: "ColorBlindPreview"
---

<objective>
Deliver the admin LED configuration vertical slice: a new "LEDs" section inside the
existing admin Settings page (no new route) with per-state color pickers (including
the idle/ambient color), three brightness sliders (span / active / ambient), the
active-highlight TTL, a retain-mode toggle, the retain timeout, accessibility-safe
defaults, and an in-SPA color-blind preview — backed by extending the existing
/api/admin/settings GET/PUT to persist all LED keys into gruvax.settings and refresh
the settings_cache so the publisher (Plan 01) and lifecycle (Plan 02) immediately
use the tuned values.

Purpose: Closes LED-04 (the three brightness tiers, correctly named span/active/
ambient per D-24) and LED-05 (per-state colors incl. ambient, accessibility
defaults + color-blind preview), and surfaces the lifecycle knobs (TTL, retain
toggle, retain timeout) the 06-02 slice consumes. After this slice the admin can
change what the LEDs look like, how long highlights last, and whether they
accumulate — and verify accessibility before any hardware exists.

Output: An admin LEDs settings section, a reusable ColorBlindPreview component, and
an extended settings endpoint persisting + caching all LED presentation knobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-CONTEXT.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-RESEARCH.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-PATTERNS.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-01-SUMMARY.md
@./.claude/skills/sketch-findings-gruvax/SKILL.md

<interfaces>
<!-- Extracted from codebase / Plan 01 — use directly. -->

Existing admin/settings.py contract (src/gruvax/api/admin/settings.py):
- _ALLOWED_SETTINGS_KEYS = frozenset({"cube.nominal_capacity", "session.idle_ttl_seconds"})
- GET /settings returns {cube_nominal_capacity, session_idle_ttl_seconds}, reading via WHERE key = ANY(%s)
- PUT /settings uses a key_map {body_key: db_key}, UPDATEs gruvax.settings ::jsonb, then refreshes request.app.state.settings_cache = await load_settings_cache(pool)
- Values stored as JSON: colors as JSON strings ('"#FFD700"'); numbers/booleans as bare JSON. Mirror the existing pin_hash JSON-string storage idiom for colors.

Seeded keys (migration 0006, Plan 01 — already exist): led_color.position/label_span/error/setup/all_off/ambient, led_brightness.span/active/ambient, led_transition.* (NOT admin-editable, D-17), led_highlight.active_ttl_seconds (180), led_highlight.retain_mode (false), led_highlight.retain_ttl_seconds (900).

hex_to_rgb already lives in gruvax.mqtt.publishers (Plan 01) — import + assert it in test_led_color.py.

Frontend admin settings client (frontend/src/api/adminClient.ts): getAdminSettings(): Promise<AdminSettings>; putAdminSettings(payload): Promise<Partial<AdminSettings>> — both wrap adminFetch (CSRF-aware). AdminSettings / AdminSettingsPut types in frontend/src/api/types.ts.

Settings.tsx patterns: type SaveStatus = 'idle'|'saving'|'saved'|'error'; existing section template; setTimeout(() => setStatus('idle'), 2000) reset; load on mount via getAdminSettings.

Color-blind matrices + simulateColorBlindness are given verbatim in RESEARCH.md §Code Examples (deuteranopia/protanopia/tritanopia 3x3 matrices) — copy exactly.
</interfaces>

<naming_contract>
CRITICAL (D-24): the three brightness tiers are led_brightness.span (label-span), led_brightness.active
(position), led_brightness.ambient (idle baseline). In the admin UI, label them:
- "SPAN BRIGHTNESS (LABEL SPAN)"   → led_brightness.span
- "ACTIVE BRIGHTNESS (POSITION)"   → led_brightness.active
- "AMBIENT BRIGHTNESS (IDLE)"      → led_brightness.ambient
NEVER label the label-span tier "ambient". "Ambient" is the idle state only.
</naming_contract>

<dependency_note>
Plan 01 created publishers.fan_out_illuminate (reads led_color.* / led_brightness.span / led_brightness.active)
and migration 0006 seeded all LED keys. Plan 02 created the lifecycle (reads led_color.ambient /
led_brightness.ambient / led_highlight.*). This plan makes ALL those keys ADMIN-EDITABLE and adds the UI.
No publisher or lifecycle change is needed here — they already consume the cache.
</dependency_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 tests — color-blind simulation + admin LED settings persistence for all keys (RED)</name>
  <files>tests/unit/test_led_color.py, tests/unit/test_admin_led_settings.py</files>
  <read_first>
    - tests/conftest.py (admin_session fixture; client fixture; MEMORY: admin_session may be broken — follow the integration pattern that re-seeds the test PIN; tests share dev Postgres, re-seed before client fixture)
    - tests/unit/test_queries.py (mocked-pool style) + any existing settings GET/PUT test for the shape
    - src/gruvax/api/admin/settings.py (GET/PUT contract to assert against)
    - migrations/versions/0006_led_settings_seed.py (Plan 01 — the seeded keys + JSON storage form)
    - .planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-RESEARCH.md §"Validation Architecture" (LED-05 color-blind test row) + §Code Examples (matrices)
  </read_first>
  <action>
    Create two RED test files.

    tests/unit/test_led_color.py — pure-function tests for the color math:
      - test_colorblind_preview: simulating the seeded gold #FFD700 and purple #7C3AED under deuteranopia produces two DISTINCT results (the safe pair stays distinguishable — the Pitfall 18 guarantee) (LED-05). Assert against the matrix math expectations (mirror the TS matrices in a small Python helper or assert the documented expected outputs).
      - test_hex_to_rgb: import hex_to_rgb from gruvax.mqtt.publishers; assert hex_to_rgb("#FFD700") == (255, 215, 0); tolerates missing "#".

    tests/unit/test_admin_led_settings.py — httpx AsyncClient + admin_session, asserting the extended endpoint covers the FULL LED key set:
      - test_get_settings_includes_led_keys: GET /api/admin/settings returns led_color_position, led_color_label_span, led_color_error, led_color_setup, led_color_all_off, led_color_ambient, led_brightness_span, led_brightness_active, led_brightness_ambient, led_highlight_active_ttl_seconds, led_highlight_retain_mode, led_highlight_retain_ttl_seconds (defaults from migration 0006) (LED-04, LED-05, D-25).
      - test_put_led_settings_persists_and_caches: PUT with {led_color_position:"#00FF00", led_brightness_span:64, led_color_ambient:"#112233", led_brightness_ambient:20, led_highlight_active_ttl_seconds:60, led_highlight_retain_mode:true, led_highlight_retain_ttl_seconds:300} returns the updated list; re-GET reflects them; assert app.state.settings_cache["led_color.position"]=="#00FF00", ["led_brightness.span"]==64, ["led_highlight.retain_mode"] is True after the PUT (cache refresh, D-15, D-25).
      - test_span_brightness_key_is_span_not_ambient: assert the body key led_brightness_span maps to the DB key led_brightness.span and that led_brightness_ambient maps to led_brightness.ambient (the D-24 separation — two distinct keys, two distinct tiers).
      - test_put_rejects_unknown_led_key: a non-whitelisted key is ignored (not written).
      - test_put_rejects_malformed_hex: PUT led_color_position:"nothex" returns 422 (Tampering).
      - test_transition_keys_not_writable: PUT led_transition_position_style:"instant" is ignored — transition keys are NOT in the writable allow-list (D-17).

    Both files MUST fail (RED) — the endpoint does not yet return/accept the LED keys.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_led_color.py tests/unit/test_admin_led_settings.py -q 2>&1 | tail -15; echo "EXPECT failures — RED phase (LED keys not yet wired)"</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: tests/unit/test_led_color.py, tests/unit/test_admin_led_settings.py
    - test_led_color.py contains `def test_colorblind_preview` and asserts gold vs purple stay distinct under deuteranopia
    - test_admin_led_settings.py contains `def test_put_led_settings_persists_and_caches`, `def test_span_brightness_key_is_span_not_ambient`, `def test_transition_keys_not_writable`
    - test_put_led_settings_persists_and_caches asserts settings_cache["led_brightness.span"] and ["led_highlight.retain_mode"]
    - Running both files fails (RED) because GET/PUT do not yet handle the LED keys
  </acceptance_criteria>
  <done>Two Wave-0 test files encode LED-04/LED-05 persistence + ambient/TTL/retain keys + color-blind distinguishability + the D-24 span/ambient key separation; they fail because the endpoint is not extended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend admin/settings.py GET/PUT for all LED color/brightness/ambient/TTL/retain keys (GREEN)</name>
  <files>src/gruvax/api/admin/settings.py</files>
  <read_first>
    - src/gruvax/api/admin/settings.py (the EXACT file to modify — _ALLOWED_SETTINGS_KEYS, GET response builder, PUT key_map + cache refresh)
    - migrations/versions/0006_led_settings_seed.py (Plan 01 — the seeded keys + JSON-string/number/boolean storage form)
    - src/gruvax/db/queries.py (load_settings_cache — refreshed after PUT)
    - tests/unit/test_admin_led_settings.py (the RED tests this task turns GREEN)
    - .planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-PATTERNS.md §admin/settings.py MODIFY (the frozenset + key_map extension)
  </read_first>
  <action>
    Extend src/gruvax/api/admin/settings.py (do not change unrelated PIN logic).

    Extend _ALLOWED_SETTINGS_KEYS to add the writable LED keys: led_color.position, led_color.label_span, led_color.error, led_color.setup, led_color.all_off, led_color.ambient, led_brightness.span, led_brightness.active, led_brightness.ambient, led_highlight.active_ttl_seconds, led_highlight.retain_mode, led_highlight.retain_ttl_seconds. Do NOT add the led_transition.* keys (D-17 — fixed defaults, not admin-editable).

    Extend GET /settings to include all those LED keys in the response object, reading from the same settings_map (extend the ANY(%s) key list). Return body adds: led_color_position (default "#FFD700"), led_color_label_span ("#7C3AED"), led_color_error ("#E63946"), led_color_setup ("#0077B6"), led_color_all_off ("#000000"), led_color_ambient ("#0051A2"), led_brightness_span (int 128), led_brightness_active (int 255), led_brightness_ambient (int 40), led_highlight_active_ttl_seconds (int 180), led_highlight_retain_mode (bool false), led_highlight_retain_ttl_seconds (int 900). Colors come back as JSON strings — values stored as '"#FFD700"' deserialize to the Python str "#FFD700".

    Extend the PUT key_map with: led_color_position→led_color.position, led_color_label_span→led_color.label_span, led_color_error→led_color.error, led_color_setup→led_color.setup, led_color_all_off→led_color.all_off, led_color_ambient→led_color.ambient, led_brightness_span→led_brightness.span, led_brightness_active→led_brightness.active, led_brightness_ambient→led_brightness.ambient, led_highlight_active_ttl_seconds→led_highlight.active_ttl_seconds, led_highlight_retain_mode→led_highlight.retain_mode, led_highlight_retain_ttl_seconds→led_highlight.retain_ttl_seconds. For color values, validate the value matches #RRGGBB (6 hex digits) before writing — reject malformed hex with HTTP 422 — and store as a JSON string (wrap in quotes). For brightness/TTL values, validate integer and store the bare integer as ::jsonb (brightness clamped 0..255 server-side at publish; TTL non-negative). For led_highlight_retain_mode, store the bare JSON boolean (true/false). Keep the existing load_settings_cache(pool) refresh after the writes so the publisher (Plan 01) and lifecycle (Plan 02) see new values immediately (D-15).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_admin_led_settings.py tests/unit/test_led_color.py -x -q && grep -c "led_brightness.span\|led_brightness.ambient\|led_color.ambient\|led_highlight.active_ttl_seconds\|led_highlight.retain_mode\|led_highlight.retain_ttl_seconds" src/gruvax/api/admin/settings.py</automated>
  </verify>
  <acceptance_criteria>
    - src/gruvax/api/admin/settings.py _ALLOWED_SETTINGS_KEYS contains led_color.ambient, led_brightness.span, led_brightness.ambient, led_highlight.active_ttl_seconds, led_highlight.retain_mode, led_highlight.retain_ttl_seconds
    - _ALLOWED_SETTINGS_KEYS does NOT contain any led_transition.* key (D-17)
    - GET /settings response includes led_color_ambient, led_brightness_span, led_brightness_ambient, led_highlight_retain_mode keys
    - PUT key_map includes led_brightness_span → led_brightness.span AND led_brightness_ambient → led_brightness.ambient (two distinct keys, D-24)
    - Color hex values are validated against #RRGGBB and stored as JSON strings; brightness/TTL as integers; retain_mode as boolean
    - PUT still calls load_settings_cache(pool) to refresh app.state.settings_cache
    - `uv run pytest tests/unit/test_admin_led_settings.py tests/unit/test_led_color.py -x` exits 0
  </acceptance_criteria>
  <done>GET/PUT /api/admin/settings persist + return the full LED key set (colors incl. ambient, the three brightness tiers, TTL, retain toggle, retain timeout), validate hex, exclude transition keys, refresh the cache, and the admin-settings + color tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Admin LEDs settings section + ColorBlindPreview component (GREEN, UI slice)</name>
  <files>frontend/src/components/ColorBlindPreview.tsx, frontend/src/api/types.ts, frontend/src/api/adminClient.ts, frontend/src/routes/admin/Settings.tsx, frontend/src/routes/admin/admin.css</files>
  <read_first>
    - frontend/src/routes/admin/Settings.tsx (the file to modify — section template; SaveStatus union; useState + load-on-mount; save handler pattern)
    - frontend/src/api/adminClient.ts (getAdminSettings/putAdminSettings — extend payload with LED keys; adminFetch CSRF wrapper)
    - frontend/src/api/types.ts (AdminSettings, AdminSettingsPut — extend with LED fields)
    - frontend/src/routes/admin/admin.css (existing settings-section / settings-field / settings-color-input / settings-range-input class conventions — add any missing classes)
    - ./.claude/skills/sketch-findings-gruvax/SKILL.md (validated admin section structure, CSS patterns, ALL-CAPS Barlow labels, design-token usage, el() vanilla-DOM constraint note — Settings.tsx is React so use JSX, but follow the same label/token language)
    - .planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-PATTERNS.md §Settings.tsx MODIFY (the LEDs section JSX + ColorBlindPreview matrices verbatim)
    - design/gruvax-design-tokens.json (Nordic Grid token swatches offered as presets: blue #0051A2, yellow #FFDA00, off-white #F7F9FC)
  </read_first>
  <action>
    Add the LEDs configuration UI. Reuse the existing Settings shell — NO new route (D-19).

    frontend/src/components/ColorBlindPreview.tsx (new): export a ColorBlindPreview({ hex }: { hex: string }) component rendering three small swatches (DEUTERANOPIA / PROTANOPIA / TRITANOPIA) computed via simulateColorBlindness(hex, type) and the three 3x3 MATRICES copied VERBATIM from RESEARCH.md §Code Examples. Pure math, zero new packages (D-18). Each swatch shows the simulated color + a small mono label. Guard against malformed hex (return original on parse failure).

    frontend/src/api/types.ts (modify): extend AdminSettings and AdminSettingsPut with optional fields: led_color_position, led_color_label_span, led_color_error, led_color_setup, led_color_all_off, led_color_ambient (string); led_brightness_span, led_brightness_active, led_brightness_ambient, led_highlight_active_ttl_seconds, led_highlight_retain_ttl_seconds (number); led_highlight_retain_mode (boolean).

    frontend/src/api/adminClient.ts (modify): no new functions needed — putAdminSettings already accepts AdminSettingsPut; the extended type covers the LED keys. Confirm getAdminSettings return type carries the new fields.

    frontend/src/routes/admin/Settings.tsx (modify): add a new <section className="settings-section" aria-labelledby="leds-heading"> titled "LEDS" (ALL CAPS Barlow per design language) placed after CAPACITY & TIMEOUT and before SEGMENT OVERRIDES. Add useState for the 6 colors (position, label_span, error, setup, all_off, ambient — seeded from getAdminSettings on mount with the gold/purple/error/setup/black/blue defaults), the 3 brightness ceilings (span, active, ambient), the active TTL (number), retain_mode (boolean), retain TTL (number); add a ledsStatus: SaveStatus + ledsError. For EACH of the 6 color states render: an ALL-CAPS label, an <input type="color"> bound to its state, the Nordic Grid token swatches as quick-pick preset buttons (#0051A2, #FFDA00, #F7F9FC) next to the picker, and a <ColorBlindPreview hex={color} />. For brightness render three <input type="range" min=0 max=255> labeled "SPAN BRIGHTNESS (LABEL SPAN)" / "ACTIVE BRIGHTNESS (POSITION)" / "AMBIENT BRIGHTNESS (IDLE)" with a mono <span> showing each value (the span tier is led_brightness.span — never labeled ambient, D-24). Add a "HIGHLIGHT TTL (SECONDS)" number input bound to active_ttl_seconds, a "RETAIN MODE" toggle (checkbox/switch) bound to retain_mode, and a "RETAIN TIMEOUT (SECONDS)" number input bound to retain_ttl_seconds. Add a handleSaveLeds that calls putAdminSettings({led_color_position, ..., led_color_ambient, led_brightness_span, led_brightness_active, led_brightness_ambient, led_highlight_active_ttl_seconds, led_highlight_retain_mode, led_highlight_retain_ttl_seconds}), sets ledsStatus='saved', resets via setTimeout(...2000). Include a "SAVE LED SETTINGS" primary button. Leave a stable settings-actions container in the LEDs section so Plan 06-04 can append the All-off + Run Diagnostic buttons.

    frontend/src/routes/admin/admin.css (modify): add any missing classes used above — settings-color-input, settings-range-input, settings-value-mono, settings-toggle, and a .colorblind-preview row + swatch styles. Consume design tokens (var(--gruvax-*)); never hardcode hex for chrome (the LED colors themselves are user data, not chrome).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -8 && npx eslint src/components/ColorBlindPreview.tsx src/routes/admin/Settings.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ColorBlindPreview.tsx contains simulateColorBlindness and the three matrices (deuteranopia/protanopia/tritanopia)
    - frontend/src/routes/admin/Settings.tsx contains a "LEDS" section, six type="color" inputs, three type="range" brightness inputs labeled SPAN/ACTIVE/AMBIENT, a HIGHLIGHT TTL input, a RETAIN MODE toggle, a RETAIN TIMEOUT input, <ColorBlindPreview, and handleSaveLeds
    - the label-span brightness control is labeled "SPAN ..." and bound to led_brightness_span — NOT "AMBIENT" (D-24); the AMBIENT BRIGHTNESS control is bound to led_brightness_ambient (idle)
    - frontend/src/api/types.ts AdminSettings/AdminSettingsPut include led_color_ambient, led_brightness_span, led_brightness_ambient, led_highlight_retain_mode
    - npm run build exits 0 and eslint reports no errors on the two touched files
    - admin.css adds the color/range/preview/toggle classes (no hardcoded chrome hex; design tokens used)
  </acceptance_criteria>
  <done>The admin Settings page has a LEDs section with six per-state color pickers (incl. ambient), three correctly-named brightness sliders, TTL/retain-mode/retain-timeout controls, token presets, and a color-blind preview; the frontend builds and lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → PUT /api/admin/settings | Authenticated admin input (session + CSRF) writing LED color/brightness/TTL/retain values |
| settings_cache → publishers.fan_out_illuminate + lifecycle | Admin-tuned values flow into published payloads and the revert schedule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Elevation of Privilege | PUT /api/admin/settings (LED keys) | mitigate | Existing require_admin (session + CSRF) on the settings router — inherited from Phase 3 |
| T-06-08 | Tampering | LED color hex value | mitigate | Server validates #RRGGBB (6 hex digits) before write → HTTP 422 on malformed; stored as JSON string |
| T-06-09 | Tampering | Brightness / TTL value out of range | mitigate | Stored as integer; brightness clamp_brightness (Plan 01) bounds 0..255 at publish; TTL validated non-negative |
| T-06-10 | Tampering | Non-whitelisted settings key write (incl. transition keys) | mitigate | _ALLOWED_SETTINGS_KEYS frozenset + key_map allow-list; transition keys excluded (D-17); unknown keys ignored |
| T-06-11 | Info Disclosure | Inaccessible color pair chosen by admin | mitigate | Color-blind preview (D-18) surfaces deuteranopia/protanopia/tritanopia rendering at pick time |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: zero new deps; color-blind preview is zero-dep matrix math) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_led_color.py tests/unit/test_admin_led_settings.py -q` green
- `uv run pytest -q` full suite green (no regression to existing admin settings / PIN tests)
- `cd frontend && npm run build` exits 0; eslint clean on touched files
- No "ambient" labeling the label-span tier: `grep -i 'ambient' frontend/src/routes/admin/Settings.tsx` shows ambient only on the IDLE brightness/color controls, never the SPAN control (manual scan)
- MANUAL (admin UI): open /admin/settings, scroll to LEDS; verify six color pickers (incl. AMBIENT/IDLE), token presets, three brightness sliders (SPAN/ACTIVE/AMBIENT), HIGHLIGHT TTL, RETAIN MODE toggle, RETAIN TIMEOUT, and three color-blind swatches per color; change position color + ambient color + TTL + retain toggle and save; confirm the next kiosk illuminate (Plan 01/02 path) uses the new color and the revert respects the new TTL via mosquitto_sub
</verification>

<success_criteria>
- Admin can tune all six LED colors (incl. ambient) + three brightness tiers (span/active/ambient) + TTL + retain toggle + retain timeout via the LEDs section; values persist and refresh the cache (SC2, SC6, LED-04, LED-05, D-15, D-25)
- Defaults are accessibility-respecting (gold/purple, not red/green) (SC2, LED-05)
- Color-blind preview renders for each color in-SPA with no new packages (LED-05, D-18)
- The label-span tier is named span everywhere; ambient is the idle state only (D-24)
- The LEDs surface is a section in the existing Settings page — no new route (D-19)
</success_criteria>

<output>
Create `.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-03-SUMMARY.md` when done.
</output>
