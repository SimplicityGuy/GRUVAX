---
phase: 08-observability-deployment-hardening
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - src/gruvax/api/health.py
  - src/gruvax/api/search.py
  - src/gruvax/api/locate.py
  - Dockerfile
  - .gitignore
  - tests/integration/test_health.py
autonomous: true
requirements: [OBS-01, OBS-04, OBS-05, OBS-06, OBS-07]
must_haves:
  truths:
    - "GET /api/health returns version = git SHA (not the hardcoded 0.1.0) and sync_age_seconds"
    - "D-04: a search whose top result has a release_id increments that record's search_count server-side (release_id only, no query text)"
    - "D-04: a locate call for a release_id increments that record's selection_count server-side (release_id only, no query text)"
    - "D-07: a /api/search slower than 200 ms records an entry in the slow-query ring buffer with both request-total and DB-time"
    - "A /api/locate slower than 50 ms records an entry in the slow-query ring buffer"
    - "Counter increments never block or fail the response (fire-and-forget with exception logging)"
    - "The Docker image bakes git SHA + build timestamp + environment into src/gruvax/_version.py at build time"
  artifacts:
    - path: "src/gruvax/api/health.py"
      provides: "enriched /api/health with git-SHA version + sync_age_seconds"
      contains: "sync_age_seconds"
    - path: "Dockerfile"
      provides: "ARG GIT_SHA/BUILD_TIMESTAMP/GRUVAX_ENV -> _version.py generation"
      contains: "GIT_SHA"
  key_links:
    - from: "src/gruvax/api/search.py"
      to: "increment_search_count"
      via: "fire-and-forget asyncio.create_task on top result release_id"
      pattern: "increment_search_count"
    - from: "src/gruvax/api/locate.py"
      to: "increment_selection_count"
      via: "fire-and-forget asyncio.create_task on release_id"
      pattern: "increment_selection_count"
    - from: "src/gruvax/api/health.py"
      to: "gruvax._version.GIT_SHA"
      via: "import with ImportError fallback, replaces 0.1.0"
      pattern: "GIT_SHA"
---

<objective>
Surface the observability spine into the user-facing contracts and finish the version injection: enrich `/api/health` with the real git SHA version + `sync_age_seconds` (OBS-01, OBS-04, OBS-06), hook the slow-query recorder and the durable counters into the search/locate request paths (OBS-05, OBS-07), and add the Docker build-time generation of `_version.py` (OBS-04).

Depends on Plan 02 (the `increment_*`, staleness, counter query functions exist) and reads the `app.state` ring buffers + `sync_age_seconds` that Plan 01 created. Owns `search.py`/`locate.py`/`health.py`/`Dockerfile`/`.gitignore` — disjoint from every other plan in Wave 2.

Purpose: this is the vertical slice where instrumentation becomes observable behavior — a search/locate actually moves a counter and a slow request actually lands in the ring buffer.
Output: enriched health.py, instrumented search.py + locate.py, Dockerfile ARG injection, .gitignore entry, enriched test_health.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-observability-deployment-hardening/08-CONTEXT.md
@.planning/phases/08-observability-deployment-hardening/08-RESEARCH.md
@.planning/phases/08-observability-deployment-hardening/08-PATTERNS.md
@.planning/phases/08-observability-deployment-hardening/08-VALIDATION.md
@.planning/phases/08-observability-deployment-hardening/08-01-SUMMARY.md
@.planning/phases/08-observability-deployment-hardening/08-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (app.state) + Plan 02 (queries.py) + codebase. -->

From Plan 01 (already wired in app.py lifespan):
- `app.state.slow_query_ring: deque` (maxlen 50), `app.state.log_ring_buffer: deque` (maxlen 200), `app.state.sync_age_seconds: float | None`, `app.state.background_tasks: set` (CR-01 strong-ref).
- `src/gruvax/middleware/timing.py` exports `SLO_THRESHOLDS_MS` and `record_slow_query(app, path, total_ms, db_ms)`.

From Plan 02 (db/queries.py):
- `async def increment_search_count(pool, release_id: int) -> None`
- `async def increment_selection_count(pool, release_id: int) -> None`
- `async def get_sync_staleness_seconds(pool) -> float | None` (prefer reading app.state.sync_age_seconds in health.py to avoid a per-request DB hit).

From src/gruvax/api/search.py (current):
- `async def search(request: Request, q, limit, pool=Depends(get_pool)) -> dict`; body does `rows, took_ms, did_you_mean = await search_collection(pool, q, limit)`; returns `{items: rows, took_ms, did_you_mean}`. Top result release_id is `rows[0]["release_id"]` when `rows` is non-empty.

From src/gruvax/api/locate.py (current):
- `async def locate_endpoint(request: Request, release_id: int, pool, segment_cache, snapshot) -> JSONResponse`; CPU-only (POS-03 -> db_ms ~ 0); 404s when record is None (do NOT increment selection on a 404).

From src/gruvax/api/health.py (current — enrich):
- body currently has `status, db, discogsography_view_check, mqtt, version: "0.1.0", started_at`. Replace `"0.1.0"` with imported `GIT_SHA`; add `sync_age_seconds`.

From Dockerfile (runtime stage 3, lines 68-104):
- `FROM python:3.14-slim`; non-root `gruvax` user created; `COPY --from=python-builder /build/src /app/src`; `ENV PYTHONPATH="/app/src"`; then `USER gruvax`. ARG injection + `_version.py` RUN must go AFTER the src COPY and BEFORE `USER gruvax` (needs root write to /app/src).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich /api/health with git-SHA version + sync_age_seconds</name>
  <files>src/gruvax/api/health.py, tests/integration/test_health.py</files>
  <read_first>
    - .planning/phases/08-observability-deployment-hardening/08-PATTERNS.md (section health.py lines 44-89 — exact fields to replace/add)
    - .planning/phases/08-observability-deployment-hardening/08-RESEARCH.md (section OBS-01 Enriched Fields lines 690-709)
    - src/gruvax/api/health.py (current body, lines 47-54)
    - tests/integration/test_health.py (existing tests + harness; the required-keys set on line 80)
    - .planning/phases/08-observability-deployment-hardening/08-01-SUMMARY.md (confirm app.state.sync_age_seconds + _version.py placeholder are in place)
  </read_first>
  <action>
    Enrich `src/gruvax/api/health.py`: add `try: from gruvax._version import GIT_SHA` / `except ImportError: GIT_SHA = "dev"` at module top (Pitfall 4 — mandatory fallback). In the response body, replace `"version": "0.1.0"` with `"version": GIT_SHA`, and add `"sync_age_seconds": getattr(request.app.state, "sync_age_seconds", None)` (OBS-06). Keep the no-live-probe rule — read `app.state` only; do NOT add a DB query to the health handler (the value is maintained by Plan 01's background task).

    SECURITY: the health body must continue to leak NO secrets — only the existing subsystem fields + GIT_SHA + sync_age_seconds. Do not add env/DSN/PIN.

    Update `tests/integration/test_health.py`: extend the required-keys set to include `sync_age_seconds`; add a test asserting `body["version"]` equals the imported `gruvax._version.GIT_SHA` value (in dev that is `"dev"`) and is NOT the literal `"0.1.0"` hardcode; add a test asserting `sync_age_seconds` is a float-or-None; add a test asserting no secret keys (`session_secret`, `database_url`, `pin`) appear.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_health.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/health JSON contains `sync_age_seconds` and `version` equal to `gruvax._version.GIT_SHA` (not hardcoded `"0.1.0"`)
    - `grep -n "sync_age_seconds" src/gruvax/api/health.py` returns a match; `grep -n '"0.1.0"' src/gruvax/api/health.py` returns NO match
    - Health body contains none of: `session_secret`, `database_url`, `pin`
    - `uv run pytest tests/integration/test_health.py -x -q` exits 0
  </acceptance_criteria>
  <done>/api/health reports git-SHA version + sync_age_seconds, reading app.state only; no secrets; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument /api/search and /api/locate — counter increments + slow-query recording</name>
  <files>src/gruvax/api/search.py, src/gruvax/api/locate.py</files>
  <read_first>
    - .planning/phases/08-observability-deployment-hardening/08-RESEARCH.md (Open Question 5 DB-time propagation lines 568-583; Open Question 6 hook points lines 628-631; Pitfall 2 fire-and-forget exception logging lines 768-787; Pitfall 3 inline-instrumentation lines 789-808)
    - .planning/phases/08-observability-deployment-hardening/08-PATTERNS.md (Shared Patterns: Background task strong-reference CR-01 lines 1181-1193)
    - src/gruvax/api/search.py (current handler, lines 34-64)
    - src/gruvax/api/locate.py (current handler, lines 62-127)
    - .planning/phases/08-observability-deployment-hardening/08-02-SUMMARY.md (confirm increment_* signatures)
  </read_first>
  <action>
    In `src/gruvax/api/search.py` `search()`: after `rows, took_ms, did_you_mean = await search_collection(...)`, call `record_slow_query(request.app, "/api/search", took_ms, took_ms)` (for search, request-total ~= DB time per RESEARCH Pitfall 3; pass took_ms as both total and db). Then, when `rows` is non-empty, schedule a FIRE-AND-FORGET counter increment for `rows[0]["release_id"]` via `asyncio.create_task(increment_search_count(pool, top_id))`, registering it on `request.app.state.background_tasks` with `add_done_callback(...discard)` AND an exception-logging done-callback (Pitfall 2). Never await it; never let it affect the response or its latency. Import `increment_search_count` from `gruvax.db.queries`, `record_slow_query` from `gruvax.middleware.timing`, plus `asyncio` and a module `logger`.

    In `src/gruvax/api/locate.py` `locate_endpoint()`: measure request-total with `t0 = time.perf_counter()` at handler entry and compute `total_ms = (time.perf_counter() - t0) * 1000` just before returning; call `record_slow_query(request.app, "/api/locate", total_ms, 0.0)` (locate is CPU-only, db_ms ~ 0, POS-03). On the SUCCESS path only (record is not None, after the 404 guard), schedule fire-and-forget `increment_selection_count(pool, release_id)` with the same CR-01 + exception-logging pattern. Do NOT increment on a 404.

    PRIVACY: pass ONLY the integer release_id to the counters — never `q`, `did_you_mean`, label, or catalog text (OBS-07).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_search.py tests/integration/test_locate.py -x -q && uv run mypy --strict src/gruvax/api/search.py src/gruvax/api/locate.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "increment_search_count" src/gruvax/api/search.py` and `grep -n "increment_selection_count" src/gruvax/api/locate.py` each return a match
    - `grep -n "record_slow_query" src/gruvax/api/search.py src/gruvax/api/locate.py` returns matches in both files
    - The counter calls pass only an int release_id (no q/label/catalog text) — verify by reading the call site
    - `uv run pytest tests/integration/test_search.py tests/integration/test_locate.py -x -q` exits 0 (no regression; counters are best-effort)
    - `uv run mypy --strict src/gruvax/api/search.py src/gruvax/api/locate.py` exits 0
  </acceptance_criteria>
  <done>Search increments search_count for the top result; locate increments selection_count on success; both record slow queries with total + DB time; fire-and-forget with exception logging; existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Dockerfile build-time _version.py generation + .gitignore entry</name>
  <files>Dockerfile, .gitignore</files>
  <read_first>
    - .planning/phases/08-observability-deployment-hardening/08-PATTERNS.md (section Dockerfile lines 935-978 — ARG injection placement + RUN command)
    - .planning/phases/08-observability-deployment-hardening/08-RESEARCH.md (Open Question 3 Dockerfile injection lines 351-368; Pitfall 4 lines 810-831)
    - Dockerfile (runtime stage 3, lines 68-104 — insert after `COPY --from=python-builder /build/src /app/src` and `ENV PYTHONPATH`, BEFORE `USER gruvax`)
    - .planning/phases/08-observability-deployment-hardening/08-01-SUMMARY.md (note: Plan 01 committed _version.py placeholder; this task adds the gitignore line + build generation)
  </read_first>
  <action>
    In the runtime stage (stage 3) of `Dockerfile`, after the src COPY + `ENV PYTHONPATH` and BEFORE `USER gruvax`, add `ARG GIT_SHA=unknown`, `ARG BUILD_TIMESTAMP=unknown`, `ARG GRUVAX_ENV=production`, then a `RUN python3 -c "..."` that writes `/app/src/gruvax/_version.py` containing `GIT_SHA = "${GIT_SHA}"`, `BUILD_TIMESTAMP = "${BUILD_TIMESTAMP}"`, `ENVIRONMENT = "${GRUVAX_ENV}"` (use the exact pattern from 08-PATTERNS.md section Dockerfile). The default `ENVIRONMENT` is `production` for the image (discretion: env baked at build, not read at runtime — RESEARCH OQ3).

    Add `src/gruvax/_version.py` to `.gitignore` so the build-generated file (and the local `just build-version` output) are not committed (Pitfall 4). Plan 01 committed the placeholder; run `git rm --cached src/gruvax/_version.py` so the dev fallback in version.py/health.py (`except ImportError`) governs locally even when the file is absent. Confirm `version.py` and `health.py` both have the `try/except ImportError` fallback so the app boots without the file.

    The `just build` build-arg invocation (passing GIT_SHA/BUILD_TIMESTAMP) is owned by Plan 06 (justfile). This task only adds the Dockerfile ARGs + RUN + gitignore line.
  </action>
  <verify>
    <automated>docker build --build-arg GIT_SHA=testsha --build-arg BUILD_TIMESTAMP=2026-05-24T00:00:00Z --build-arg GRUVAX_ENV=production -t gruvax-version-test . && docker run --rm gruvax-version-test python3 -c "from gruvax._version import GIT_SHA; assert GIT_SHA == 'testsha', GIT_SHA; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ARG GIT_SHA" Dockerfile` returns a match in the runtime stage (before `USER gruvax`)
    - `grep -n "_version.py" .gitignore` returns a match
    - Building with `--build-arg GIT_SHA=testsha` produces an image whose `gruvax._version.GIT_SHA == "testsha"`
    - The app still imports cleanly when `_version.py` is absent (ImportError fallback): `uv run python -c "import gruvax.api.version; import gruvax.api.health"` exits 0 after `git rm --cached` removes the local file (or with it gitignored)
  </acceptance_criteria>
  <done>Docker image bakes git SHA/build timestamp/environment into _version.py; file gitignored; dev import works via ImportError fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN client -> /api/health | Public, unauthenticated; reports subsystem status + version + staleness |
| Search/locate request -> record_stats | Server-side counter writes; only int release_id crosses |
| Docker build args -> image filesystem | Build-time metadata baked into _version.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Information Disclosure | /api/health body | mitigate | Only subsystem fields + git SHA + sync_age_seconds; test asserts no session_secret/database_url/pin keys. |
| T-08-10 | Information Disclosure | counter increment args | mitigate | Only int release_id passed to increment_* — never q/label/catalog text (OBS-07, PRIV-02). |
| T-08-11 | Denial of Service | fire-and-forget counter tasks | mitigate | CR-01 strong-ref + exception-logging done-callback; failures logged, never crash or delay the response (Pitfall 2). |
| T-08-12 | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: none); stdlib + installed deps only. |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_health.py tests/integration/test_search.py tests/integration/test_locate.py -q` green
- `uv run mypy --strict src/gruvax/` exits 0
- `uv run ruff check src/ tests/` exits 0
- `docker build --build-arg GIT_SHA=$(git rev-parse --short HEAD) ...` then `GET /api/version` returns that SHA
</verification>

<success_criteria>
- /api/health reports git-SHA version + sync_age_seconds (OBS-01, OBS-04, OBS-06)
- Search increments search_count for the top result; locate increments selection_count on success (OBS-07)
- Slow requests land in the ring buffer with request-total + DB time (OBS-05)
- Docker image bakes build-time version metadata into _version.py (OBS-04)
- No secrets in /api/health; only release_id reaches counters (privacy)
</success_criteria>

<output>
Create `.planning/phases/08-observability-deployment-hardening/08-03-SUMMARY.md` when done.
</output>
