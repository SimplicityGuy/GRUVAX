---
phase: 05-segment-aware-position-precision
plan: "04"
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/gruvax/api/admin/validation.py
  - src/gruvax/api/admin/cubes.py
  - src/gruvax/api/admin/segments.py
  - src/gruvax/api/admin/router.py
  - src/gruvax/db/queries.py
  - tests/unit/test_diff_preview.py
  - tests/unit/test_boundary_validation.py
  - tests/property/test_boundary_validation_property.py
  - tests/unit/test_segment_cache.py
  - tests/integration/test_segment_api.py
  - tests/integration/test_cubes_bulk.py
  - tests/integration/test_change_set.py
  - tests/integration/test_boundary_editor.py
  - tests/integration/test_sse.py
autonomous: true
requirements: [SEG-05, SEG-08]

must_haves:
  truths:
    - "Admin can view, edit, and add cut points and set per-label width overrides via the admin API"
    - "D-09: the save validator rejects a cut-point set that scatters a label across non-adjacent bins (contiguity, SEG-05)"
    - "D-06: a cut insert cascades subsequent cut points as ONE change-set (durable (unit,row,col) coords survive renumber); a cut insert near the end of the shelf with no free trailing cube is blocked with a plain-language shelf_overflow error (end-of-shelf overflow edge behavior)"
    - "D-07: the cut-point editor reuses the Phase 3 validate -> bulk -> invalidate -> SSE machinery wholesale (replacing the Phase 3 per-cube first/last editor); D-04: an orphaned override (whose label is no longer in the bin after an edit) is dropped during the commit; SegmentCache is invalidated + re-derived alongside BoundaryCache on every commit"
    - "Every admin/db consumer of last_label/last_catalog (cubes.py, queries.py, validation.py) is reworked to the cut-point model"
    - "AUTHORITATIVE BACKEND GREEN GATE: this is the LAST backend wave, so after the admin/DB write-path + integration tests are healed, the FULL suite `just lint && just typecheck && just test` (Postgres up) exits 0 — this is the phase's backend green gate that the scoped Wave 2/3 gates deferred to here"
  artifacts:
    - path: "src/gruvax/api/admin/validation.py"
      provides: "validate_contiguity(proposed_updates, segment_cache) -> str | None"
      contains: "def validate_contiguity"
    - path: "src/gruvax/api/admin/segments.py"
      provides: "GET segment data + cut-point/override/insert-cut write endpoints (require_admin, Idempotency-Key)"
      contains: "/segments"
    - path: "src/gruvax/api/admin/cubes.py"
      provides: "BoundaryEdit without last_*; movement counts via SegmentBin; bulk invalidates SegmentCache; suggest uses derived last record"
      contains: "segment_cache"
    - path: "src/gruvax/db/queries.py"
      provides: "write_boundary/get_cube_boundary cut-point shape; history keeps last_* columns nullable as audit artifact"
      contains: "first_catalog"
  key_links:
    - from: "src/gruvax/api/admin/segments.py"
      to: "src/gruvax/estimator/segment_cache.py"
      via: "GET reads SegmentCache.get_bin(); writes invalidate + re-derive"
      pattern: "get_segment_cache"
    - from: "src/gruvax/api/admin/cubes.py"
      to: "src/gruvax/api/admin/validation.py"
      via: "validate endpoint calls validate_contiguity after phantom check"
      pattern: "validate_contiguity"
    - from: "src/gruvax/api/admin/router.py"
      to: "src/gruvax/api/admin/segments.py"
      via: "create_admin_router includes segments_router"
      pattern: "segments"
---

<objective>
Rework the admin write path to the cut-point + width-override model and heal every admin/db orphan
of the dropped `last_*` fields. THIS IS THE LAST BACKEND WAVE — it owns the authoritative full-suite
backend green gate.

GREEN-GATE MODEL (two-tier — this wave is tier 2):
Waves 2 (05-02) and 3 (05-03) ran under SCOPED gates because the `last_*` orphan heal is sequential
(estimator read-path → admin/DB write-path); the FULL `just test` suite could not be green until the
admin/DB write-path was healed. That heal happens HERE. After 05-04's Task 3 heals the admin/validation/
integration tests, `just lint && just typecheck && just test` (full suite, Postgres up) MUST exit 0.
This is the phase's authoritative backend green gate; 05-05 (frontend-only) re-confirms it does not regress.

Backend: add `validate_contiguity()` to `validation.py` (SEG-05 + the shelf_overflow / empty_bin /
contiguity taxonomy from RESEARCH Pattern 6); rework `cubes.py` (drop `last_*` from `BoundaryEdit`,
make `_compute_movement_counts` use a derived `SegmentBin`/`count_records_in_bin`, rework
`suggest_cube_midpoint` to derive a bin's last record from the next cut point via SegmentCache, add the
contiguity check to `/cubes/validate`, and invalidate + re-derive SegmentCache in the single-cube write
and bulk write commit paths); create `segments.py` (GET segment data + cut-edit + override-set +
insert-cut endpoints, all `require_admin`, Idempotency-Key on bulk-style writes); rework `queries.py`
(`write_boundary` + `get_cube_boundary` to the cut-point shape; keep `boundary_history` `last_*`
columns as nullable audit artifact per RESEARCH Runtime State decision; `source='cut_insert'` handling
— the CHECK was already extended in migration 0005); register `segments.py` in `router.py`.

Tests: fill SEG-05 (`test_contiguity_validation`) + SEG-08 (`test_segment_api.py`); rewrite the
`validate_boundary_order`-era validation tests for the cut-point/contiguity model; heal the integration
tests that POST `last_*` in `BoundaryEdit` bodies (`test_cubes_bulk`, `test_change_set`,
`test_boundary_editor`, `test_sse`) to the cut-point shape; heal `test_diff_preview.py`.

Purpose: deliver the admin backend for SEG-08 + the contiguity guard for SEG-05, close the
admin/db half of the orphan inventory, and bring the full backend suite to GREEN. Output: a working
cut-point + override admin API on a green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-segment-aware-position-precision/05-CONTEXT.md
@.planning/phases/05-segment-aware-position-precision/05-RESEARCH.md
@.planning/phases/05-segment-aware-position-precision/05-PATTERNS.md
@.planning/phases/05-segment-aware-position-precision/05-VALIDATION.md
@.planning/phases/05-segment-aware-position-precision/05-REPLAN-NOTES.md
@.planning/phases/05-segment-aware-position-precision/05-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase + 05-02/03 outputs. -->

From src/gruvax/estimator/segment_cache.py (05-02):
```python
class SegmentCache:
    def get_bin(self, unit_id, row, col) -> SegmentBin | None
    def get_bins_for_label(self, label) -> list[SegmentBin]
    def derive(self, cache, snapshot, overrides) -> None
    def invalidate(self) -> None
# SegmentBin.segments: tuple[LabelSegment, ...]; LabelSegment has label, segment_count,
# auto_fraction, applied_fraction, is_override, continues, first_rank_in_label, last_rank_in_label
```

From src/gruvax/estimator/boundary_math.py (05-03):
```python
def count_records_in_bin(bin_: SegmentBin) -> int          # sum of seg.segment_count
def suggest_midpoint(label, first_anchor_release_id, last_anchor_release_id, snapshot) -> RecordRow | None
def sample_records(records_in_range, n=7) -> list[RecordRow]
```

From src/gruvax/api/deps.py (05-03 added get_segment_cache):
```python
def get_segment_cache(request) -> SegmentCache       # app.state.segment_cache; 503 if missing
def get_boundary_cache(request) -> BoundaryCache
def get_collection_snapshot(request) -> CollectionSnapshot
def get_event_bus(request) -> EventBus
def get_pool(request) -> AsyncConnectionPool
def require_admin(request) -> dict[str, Any]          # Phase 3 auth dependency
```

From src/gruvax/api/admin/cubes.py (existing — sites to change):
```python
class BoundaryEdit(BaseModel):   # lines 61-73: REMOVE last_label / last_catalog fields
    unit_id; row; col; first_label; first_catalog; is_empty; force
# _compute_movement_counts (lines 495-541): synthetic BoundaryRow(last_*=...) -> use SegmentBin
# suggest_cube_midpoint (lines 544-619): uses current.last_label/.last_catalog as first anchor
# bulk_write_cubes (lines 671-815) + put_cube_boundary: add segment_cache.invalidate()+derive()
# validate endpoint (lines 377-492): add validate_contiguity after the phantom check
```

From src/gruvax/db/queries.py (existing — sites to change):
```python
# get_cube_boundary (lines ~547-559): SELECT includes last_label, last_catalog -> drop
# write_boundary (lines 562-599): SET last_label/last_catalog -> drop (cut-point only)
# write_history_row (lines 602-654): KEEP prev_last_*/new_last_* columns as nullable audit
#   artifact (RESEARCH Runtime State A1) — pass None for new_last_* going forward
# find_boundary_near_misses / cube_exact_match: phantom helpers — unchanged (D-07 reuse)
```

Save-validation taxonomy (RESEARCH Pattern 6 — implement these dispositions):
  phantom (existing) hard-block | boundary_order_error (existing) | contiguity_violation (new) hard-block
  | empty_bin_after_cut (new) hard-block | shelf_overflow (new) hard-block
  | overrides-not-summing-1.0 -> WARN (allow commit, surface in diff) | orphaned override -> drop + diff row

UI-SPEC error copy (use verbatim for the new blocks):
  shelf_overflow: "No empty cube available. Adding a cut here would move records into a cube that
                   doesn't exist. Free up the last cube first."
  empty_bin:      "Bin {n} would have no records. Adjust the cut point to include at least one record."
  contiguity:     "This cut would split {LABEL} across non-adjacent bins. Reposition the cut or
                   remove the existing boundary."
</interfaces>

@src/gruvax/api/admin/cubes.py
@src/gruvax/api/admin/validation.py
@src/gruvax/api/admin/router.py
@src/gruvax/db/queries.py
@tests/integration/test_segment_api.py
@tests/unit/test_segment_cache.py
@tests/integration/test_boundary_editor.py
@tests/integration/test_change_set.py
@tests/integration/test_sse.py
@tests/integration/test_cubes_bulk.py
@tests/unit/test_diff_preview.py
@tests/unit/test_boundary_validation.py
@tests/property/test_boundary_validation_property.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contiguity validator + cubes.py rework + queries.py cut-point rework</name>
  <files>src/gruvax/api/admin/validation.py, src/gruvax/api/admin/cubes.py, src/gruvax/db/queries.py</files>
  <read_first>
    - src/gruvax/api/admin/validation.py (whole file — validate_boundary_order lines 21-57 to keep/adapt; module docstring)
    - src/gruvax/api/admin/cubes.py — BoundaryEdit (61-81), get_admin_cubes fill-level (127-177), put_cube_boundary (216-374 incl. invalidate ~362), validate endpoint (377-492), _compute_movement_counts (495-541), suggest_cube_midpoint (544-619, esp. current.last_label/last_catalog anchor at 573-619), bulk_write_cubes (671-815 incl. invalidate ~815)
    - src/gruvax/db/queries.py — get_cube_boundary (530-559), write_boundary (562-599), write_history_row (602-654)
    - 05-PATTERNS.md sections "src/gruvax/api/admin/validation.py", "src/gruvax/api/admin/cubes.py", and "Shared Patterns -> Cache invalidation + SSE publish pipeline" (the exact invalidate->derive->publish sequence)
    - 05-RESEARCH.md "Pattern 6: Save-Validation Taxonomy", "Pitfall 3: Cut-insert overflow", "Pitfall 6: count_records_in_boundary", and "Runtime State Inventory" (keep boundary_history last_* nullable)
  </read_first>
  <action>
    `validation.py`: keep `validate_boundary_order` ONLY if a non-contiguity caller still needs it;
    otherwise mark it deprecated/remove (the cut-point model has no stored "last", so the old
    first<=last ordering check applies to cut ordering, not last_*). Add `validate_contiguity(
    proposed_updates: list[BoundaryEdit], segment_cache: SegmentCache) -> str | None`: returns a
    plain-language error string (use the UI-SPEC contiguity copy) when the proposed cut-point set would
    place any label in NON-adjacent bins (compute the would-be bin membership per label and reject if a
    label's bins are not a contiguous (unit,row,col)-sorted run), else None. Also add
    `validate_no_empty_bin(...) -> str | None` and `validate_shelf_overflow(...) -> str | None` (or fold
    the empty-bin + overflow checks into the insert-cut path in segments.py — choose and document)
    returning the exact UI-SPEC plain-language messages.
    `cubes.py`: remove `last_label`/`last_catalog` from `BoundaryEdit` (and any sibling model that
    carries them). Rewrite `_compute_movement_counts` to compute records_before/after from
    `count_records_in_bin(SegmentBin)` — for the proposed edit, get the proposed bin from a SegmentCache
    re-derived against the proposed cut points (or approximate via the current SegmentCache's bin for the
    coordinate and the snapshot slice); do NOT construct a `BoundaryRow(last_*=...)`. Rewrite
    `suggest_cube_midpoint`: the current bin's "last record" anchor is no longer stored — derive it as
    the record immediately before the NEXT bin's cut point within the shared label, using SegmentCache
    (`get_bin` for the current coord -> last segment's last record by rank) and the snapshot; keep
    `suggest_midpoint` (index-space, D-08) as the midpoint engine. In `/cubes/validate`, after the
    existing phantom check, call `validate_contiguity(body.updates, segment_cache)` and return
    `{"type":"contiguity_violation","message":...}` with 400 on failure (before movement counts). In
    BOTH the single-cube write (`put_cube_boundary`) and `bulk_write_cubes` commit paths, after
    `cache.invalidate()` + `await cache.load(pool)`, add `segment_cache.invalidate()` then
    `segment_cache.derive(cache, snapshot, cache.overrides)` BEFORE the `bus.publish("boundary_changed",
    ...)` (invalidate is NEVER inside the DB transaction — Pitfall A). Add `segment_cache` + `snapshot`
    Depends params where needed.
    `queries.py`: drop `last_label, last_catalog` from `get_cube_boundary`'s SELECT (it now returns only
    cut-point columns + is_empty). Change `write_boundary` to UPDATE only `first_label, first_catalog,
    is_empty` (drop the last_* SET + params). KEEP `write_history_row`'s `prev_last_*`/`new_last_*`
    columns (the boundary_history table still has them — they are a nullable audit artifact per RESEARCH
    A1); pass `None` for the new_last_* args going forward and remove the now-unused `new_last_label`/
    `new_last_catalog` params from `write_boundary`'s callers. Ensure any caller that passed last_* to
    write_boundary is updated.
  </action>
  <verify>
    <automated>uv run mypy --strict src/gruvax/ && uv run python -c "import gruvax.app"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run mypy --strict src/gruvax/` exits 0.
    - `grep -n "def validate_contiguity" src/gruvax/api/admin/validation.py` matches.
    - `grep -nE "last_label|last_catalog" src/gruvax/api/admin/cubes.py` returns nothing in BoundaryEdit / _compute_movement_counts / suggest_cube_midpoint (the only permitted residual is in the boundary_history audit columns inside queries.py, which is intentional).
    - `grep -nE "segment_cache.invalidate|segment_cache.derive" src/gruvax/api/admin/cubes.py` matches in both write paths.
    - `write_boundary` SQL no longer sets `last_label`/`last_catalog`; `get_cube_boundary` SELECT no longer fetches them.
    - `uv run python -c "import gruvax.app"` exits 0.
  </acceptance_criteria>
  <done>validate_contiguity + cut-point cubes.py + cut-point queries.py implemented; SegmentCache invalidated/re-derived on commit; mypy --strict clean; app imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create segments.py admin endpoints + register in router; fill SEG-05 + SEG-08 tests</name>
  <files>src/gruvax/api/admin/segments.py, src/gruvax/api/admin/router.py, tests/integration/test_segment_api.py, tests/unit/test_segment_cache.py</files>
  <read_first>
    - src/gruvax/api/admin/cubes.py (the handler shape to mirror: require_admin dependency, JSONResponse, Idempotency-Key check pattern lines ~713-718, invalidate->derive->publish pipeline)
    - src/gruvax/api/admin/router.py (create_admin_router — add segments_router include)
    - 05-PATTERNS.md "src/gruvax/api/admin/segments.py" (imports/router, GET segment data endpoint, write endpoints), "Shared Patterns -> Authentication / Parameterized SQL / Idempotency-Key / Cache invalidation"
    - 05-RESEARCH.md "Pattern 5: Admin API Extensions" (the 4 endpoint signatures + bodies), "Pattern 6" (taxonomy), "Security Domain -> Known Threat Patterns" (phantom override injection, fraction>1.0, shelf overflow)
    - tests/integration/test_segment_api.py (the 9 SEG-08 skip-stubs from 05-01 — un-skip + implement)
    - tests/unit/test_segment_cache.py (test_contiguity_validation skip-stub — un-skip; SEG-05)
    - 05-VALIDATION.md Per-Requirement Verification Map rows SEG-05 + SEG-08
  </read_first>
  <action>
    Create `src/gruvax/api/admin/segments.py` mirroring `cubes.py` conventions. Implement:
    `GET /cubes/{unit_id}/{row}/{col}/segments` (require_admin) -> reads `segment_cache.get_bin(...)`,
    returns `{segments:[{label, fraction, is_override, auto_fraction, continues, segment_count}]}`,
    404 if no bin; no DB access.
    `PUT /cubes/{unit_id}/{row}/{col}/cut` (require_admin) -> body `{first_label, first_catalog, force}`,
    runs POS-01 + phantom check (reuse `cube_exact_match`/`find_boundary_near_misses`), 400 phantom if
    !force; on success writes via the existing change-set/bulk machinery (or a transaction) then
    invalidate+derive+publish.
    `POST /cubes/{unit_id}/{row}/{col}/overrides` (require_admin) -> body `{overrides:[{label, fraction|null}]}`;
    null clears the override; REJECT overrides for labels not present in the bin's derived segments
    (phantom override injection — Tampering mitigation); upsert/delete into `gruvax.segment_overrides`
    with %s placeholders; honor Idempotency-Key; then invalidate+derive+publish.
    `POST /cubes/insert-cut` (require_admin) -> body `{after_unit_id, after_row, after_col,
    new_first_label, new_first_catalog, force}`; run phantom check; run `validate_shelf_overflow` +
    `validate_no_empty_bin`; cascade subsequent cut points as ONE change-set (rides Phase 3 undo);
    record `boundary_history` rows with `source='cut_insert'`; invalidate+derive+publish.
    Register the new router in `router.py` (`from gruvax.api.admin.segments import router as
    segments_router; router.include_router(segments_router)`).
    Then un-skip + implement `test_contiguity_validation` in test_segment_cache.py (SEG-05: a cut set
    scattering a label across non-adjacent bins returns a non-None error; an adjacent multi-bin span
    returns None) and the 9 SEG-08 tests in test_segment_api.py (GET segments shape, cut edit happy +
    phantom, override set + clear + reject-phantom-label, insert-cut happy + shelf_overflow, require_admin
    on each new route). Use the existing integration test client/auth fixtures.
  </action>
  <verify>
    <automated>uv run mypy --strict src/gruvax/api/admin/segments.py && uv run pytest tests/integration/test_segment_api.py "tests/unit/test_segment_cache.py::test_contiguity_validation" -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/gruvax/api/admin/segments.py` defines the 4 endpoints (grep each route path) and every handler has `require_admin` (grep count of `require_admin` >= 4).
    - `grep -n "segments" src/gruvax/api/admin/router.py` shows the include.
    - All 9 SEG-08 tests in test_segment_api.py pass un-skipped; `test_contiguity_validation` passes un-skipped.
    - The override-set endpoint rejects an override whose label is not in the bin's segments (a test asserts 400/422).
    - insert-cut returns the shelf_overflow plain-language error when no trailing free cube exists (a test asserts it).
    - `uv run mypy --strict src/gruvax/api/admin/segments.py` exits 0.
  </acceptance_criteria>
  <done>segments.py admin API created + registered; SEG-05 contiguity + 9 SEG-08 API tests pass un-skipped; mypy clean.</done>
</task>

<task type="auto">
  <name>Task 3: Heal admin/validation/integration test orphans; AUTHORITATIVE full-suite backend green gate</name>
  <files>tests/unit/test_diff_preview.py, tests/unit/test_boundary_validation.py, tests/property/test_boundary_validation_property.py, tests/integration/test_cubes_bulk.py, tests/integration/test_change_set.py, tests/integration/test_boundary_editor.py, tests/integration/test_sse.py</files>
  <read_first>
    - tests/unit/test_boundary_validation.py + tests/property/test_boundary_validation_property.py (validate_boundary_order(last_label=..., last_catalog=...) call sites — rework for the cut-point model)
    - tests/unit/test_diff_preview.py (BoundaryRow(last_*=...) + count_records_in_boundary usage at lines 14, 36, 50-64 — heal to SegmentBin + count_records_in_bin)
    - tests/integration/test_boundary_editor.py (BoundaryEdit POST bodies with last_label/last_catalog at lines 56-57, 90-91, 124-125, 164-165, 217-218 — drop the last_* keys)
    - tests/integration/test_change_set.py (BoundaryEdit POSTs with last_* at 73-74, 116-117, 177-178, 236-237, 267-268, 322-323 — drop last_* keys; preserve change-set/undo assertions)
    - tests/integration/test_sse.py (POST bodies with last_* at 150-151, 158-159 — drop last_* keys; preserve boundary_changed SSE assertions)
    - tests/integration/test_cubes_bulk.py (line 94 asserts last_label NOT in bulk response — keep/strengthen)
  </read_first>
  <action>
    Heal `test_boundary_validation.py` + `test_boundary_validation_property.py`: the cut-point model has
    no stored "last", so the `validate_boundary_order(first_label, first_catalog, last_label,
    last_catalog)` contract no longer applies. Replace those tests' targets — if `validate_boundary_order`
    was removed in Task 1, retarget the Hypothesis property at `validate_contiguity` (property: a label
    placed in adjacent bins validates; a label placed in non-adjacent bins is rejected); if it was kept
    for cut ordering, adapt the call args to the surviving signature. Do NOT delete the coverage — keep
    an equivalent number of behavior assertions.
    Heal `test_diff_preview.py`: replace `BoundaryRow(last_*=...)` + `count_records_in_boundary` with a
    derived `SegmentBin` + `count_records_in_bin` (mirror the make_multi_label_bin fixture pattern);
    assert the movement count reflects a changed cut point.
    Heal `test_boundary_editor.py`, `test_change_set.py`, `test_sse.py`: remove the `last_label`/
    `last_catalog` keys from every `BoundaryEdit` POST body — the edit now sets only the cut point
    (`first_label`/`first_catalog`/`is_empty`). Adjust any assertion that depended on the old last_*
    validation (e.g. a phantom test that put a phantom in last_* must now put it in first_*; an
    inverted-range test must be re-expressed as a contiguity/cut-ordering case or removed if it no longer
    has meaning). Preserve the phantom-block, diff-preview, change-set/undo, and SSE `boundary_changed`
    assertions — those behaviors carry forward (D-07).
    Keep/strengthen `test_cubes_bulk.py` line-94 assertion that the bulk response does not expose last_*.
    After all heals, run the FULL suite and confirm green — this is the phase's authoritative backend
    green gate (Waves 2/3 deferred their full-suite check here per the two-tier model).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_boundary_validation.py tests/property/test_boundary_validation_property.py tests/unit/test_diff_preview.py tests/integration/test_boundary_editor.py tests/integration/test_change_set.py tests/integration/test_sse.py tests/integration/test_cubes_bulk.py -q && just lint && just typecheck && just test</automated>
  </verify>
  <acceptance_criteria>
    - The seven listed test files all pass.
    - AUTHORITATIVE BACKEND GREEN GATE (tier 2 of the two-tier model — Waves 2/3 deferred their full-suite check here): `just lint && just typecheck && just test` (full suite, Postgres up) all exit 0 — 0 failures, 0 errors. Record the result in the SUMMARY. This is the gate that proves the entire `last_*` orphan inventory (read-path from 05-03 + admin/DB write-path from this wave) is fully healed.
    - `grep -rnE "last_label|last_catalog" tests/` returns nothing EXCEPT intentional negative assertions (e.g. test_cubes_bulk asserting `"last_label" not in cube`) — note any residual explicitly in the SUMMARY.
    - Behavior coverage is preserved: phantom-block, diff-preview, change-set/undo, and SSE `boundary_changed` assertions still exist (grep their key markers).
    - Latency check (record in SUMMARY): `uv run pytest tests/integration/test_locate.py --benchmark-only` shows p95 ≤ 50 ms (if the benchmark test exists; if not, note absence).
  </acceptance_criteria>
  <done>All admin/validation/integration test orphans healed to the cut-point model; FULL backend suite green (authoritative backend green gate passed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client -> /api/admin/cubes/*, /segments, /overrides, /insert-cut | authenticated admin writes; untrusted body crosses here |
| request body -> segment_overrides write | fraction + label values persisted; must be validated |
| cut-insert cascade -> cube_boundaries | bulk multi-row write; must be atomic + bounded by physical cube count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Elevation of Privilege | every new admin endpoint | mitigate | `require_admin` dependency on GET segments, PUT cut, POST overrides, POST insert-cut (Phase 3 V2/V4 carry-forward) |
| T-05-04-02 | Tampering | phantom override injection (override for a label not in the bin) | mitigate | server-side validation in POST /overrides rejects overrides whose label is absent from the bin's derived segments |
| T-05-04-03 | Tampering | override fraction > 1.0 or <= 0 | mitigate | Pydantic field bound (0 < fraction <= 1) + DB CHECK `fraction > 0.0 AND fraction <= 1.0` (migration 0005) |
| T-05-04-04 | Denial of Service | cut-insert cascade exceeding physical cube count | mitigate | `validate_shelf_overflow` counts physical cubes before accepting insert; reject with plain-language shelf_overflow error |
| T-05-04-05 | Spoofing | Idempotency-Key replay of cut inserts / override sets | mitigate | reuse Phase 3 Idempotency-Key dedup table (check->execute->store in one transaction, Pitfall 7) |
| T-05-04-06 | Tampering | SQL injection in new segment/override SQL | mitigate | all SQL uses %s placeholders; zero f-string interpolation of user input (T-03-24) |
| T-05-04-07 | Tampering | save validator bypass (contiguity/empty/overflow) | mitigate | validators are server-authoritative in /cubes/validate + insert-cut; UI dry-run and commit share the same path |
| T-05-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this wave (RESEARCH.md: zero new deps) |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_segment_api.py tests/unit/test_segment_cache.py -q` — SEG-05 + SEG-08 pass.
- `uv run python -c "import gruvax.app"` exits 0 (segments router registered, cut-point queries valid).
- `test -z "$(grep -rnE 'last_label|last_catalog' src/gruvax/api/admin/)"` succeeds (zero matches; history audit columns live in queries.py only and are intentional).
- AUTHORITATIVE BACKEND GREEN GATE: `just lint && just typecheck && just test` all exit 0 (FULL suite GREEN — the entire last_* orphan inventory, read-path + admin/DB write-path, is now closed). This is tier 2 of the two-tier green-gate model; Waves 2/3 ran scoped gates and deferred their full-suite check to here.
</verification>

<success_criteria>
- Admin can view/edit/add cut points + set width overrides via the new admin API (SEG-08).
- The save validator rejects non-adjacent label scatter (SEG-05 contiguity) and blocks end-of-shelf overflow.
- Cut/override saves run through validate -> bulk -> invalidate -> SSE; SegmentCache is re-derived on every commit; p95 <= 50 ms preserved.
- All admin/db last_* orphans healed; FULL backend suite GREEN at wave end (authoritative backend green gate).
</success_criteria>

<output>
Create `.planning/phases/05-segment-aware-position-precision/05-04-SUMMARY.md` when done.
MUST record: the AUTHORITATIVE backend green-gate result (`just lint && just typecheck && just test` exit 0
— full suite, 0 failures/0 errors); a grep audit proving zero `last_label`/`last_catalog` in
src/gruvax/api/admin/ (and that the only residual in src/gruvax/db/queries.py is the intentional
boundary_history audit columns); confirmation that the override endpoint rejects phantom-label overrides
and insert-cut blocks shelf overflow; and the locate p95 benchmark result (or note its absence).
</output>
</content>
