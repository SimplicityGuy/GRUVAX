---
milestone: v1.0
audited: 2026-05-25T21:00:54Z
status: gaps_found
scores:
  requirements: 75/84 satisfied (9 documented deferrals)
  phases: 9/9 verified (2 manual-checkpoint pending)
  integration: 2 broken cross-phase flows
  flows: 5/7 key flows complete
gaps:
  requirements:
    - id: "SRCH-09"
      status: "deferred"
      phase: "Phase 4 — Realtime + Offline Resilience"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "not-implemented (SPIDR-deferred 2026-05-21)"
      evidence: "Still listed under '## v1 Requirements' with [ ] and traceability 'Pending'; never moved to v2/backlog. No code, no dead stubs (integration checker confirmed clean absence)."
    - id: "OFF-01"
      status: "deferred"
      phase: "Phase 4 — Realtime + Offline Resilience"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "not-implemented (SPIDR-deferred 2026-05-21)"
      evidence: "Offline banner. Only a clean `bannerVisible:false` stub in store.ts (annotated deferred); no UI consumes it."
    - id: "OFF-02"
      status: "deferred"
      phase: "Phase 4 — Realtime + Offline Resilience"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "not-implemented (SPIDR-deferred 2026-05-21)"
      evidence: "Disable search input while offline. Not implemented; no half-wiring."
    - id: "OFF-03"
      status: "deferred"
      phase: "Phase 4 — Realtime + Offline Resilience"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "not-implemented (SPIDR-deferred 2026-05-21)"
      evidence: "Auto-reconnect with exponential backoff. Not implemented."
    - id: "OFF-04"
      status: "deferred"
      phase: "Phase 4 — Realtime + Offline Resilience"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "not-implemented (SPIDR-deferred 2026-05-21)"
      evidence: "Reconnect success indicator. Not implemented."
    - id: "PRIV-01"
      status: "deferred"
      phase: "Phase 4 — Realtime + Offline Resilience"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "not-implemented (SPIDR-deferred 2026-05-21)"
      evidence: "Session-only search history. No client-side history store exists at all (de-facto satisfied by absence, but unverified as a requirement)."
    - id: "PRIV-02"
      status: "deferred"
      phase: "Phase 4 — Realtime + Offline Resilience"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "not-implemented (SPIDR-deferred 2026-05-21)"
      evidence: "No server-side query text persisted. Integration checker confirms search/locate counters store release_id only — the PRIV-02 intent is effectively met by Phase 8's design, but the requirement is unchecked/Pending."
    - id: "PRIV-03"
      status: "deferred"
      phase: "Phase 4 — Realtime + Offline Resilience"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "not-implemented (SPIDR-deferred 2026-05-21)"
      evidence: "Aggregate-only admin stats. Phase 8 diagnostics return top-N by count, no per-visitor breakdown — intent met, requirement unchecked/Pending."
    - id: "PRIV-04"
      status: "deferred"
      phase: "Phase 4 — Realtime + Offline Resilience"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "not-implemented (SPIDR-deferred 2026-05-21)"
      evidence: "No-PIN 'Reset kiosk' button. No reset-kiosk UI or endpoint exists."
  integration:
    - id: "INT-A"
      severity: "BLOCKER"
      from: "Phase 5 (segments.py cut/override/insert-cut)"
      to: "Phase 4 (kiosk SSE consumer)"
      affected_reqs: ["RTM-01", "ADMN-11", "SEG-07", "SEG-08"]
      issue: "segments.py publishes boundary_changed with payload keys `cubes`/`unit_id`; the kiosk consumer (KioskView.tsx:240) and every other producer (cubes.py:359/816, import_.py:559) use `cube_ids`/`unit`. On a segment edit the kiosk destructures cube_ids=undefined; `['cubes']`/`['units']` still invalidate (main grid updates) but `for (const c of cube_ids)` throws TypeError, so shimmer-clear and relocateActiveSelection() (highlight-follows-record, D-05/D-06) silently fail."
      evidence: "src/gruvax/api/admin/segments.py:295-300 (and 2 more); frontend/src/routes/kiosk/KioskView.tsx:240-258"
      fix: "Rename `cubes`→`cube_ids` and `unit_id`→`unit` in all three segments.py publish calls; drop the redundant top-level `type` key."
    - id: "INT-B"
      severity: "BLOCKER"
      from: "Phase 3 (history.py revert)"
      to: "Phase 5 (SegmentCache) + Phase 4 (SSE)"
      affected_reqs: ["ADMN-09", "RTM-01", "ADMN-11", "SEG-07"]
      issue: "revert_change_set reloads BoundaryCache but never re-derives SegmentCache and never publishes boundary_changed. After an undo: /api/locate returns stale segment positions until next admin write or restart, and the kiosk is never notified (no live re-render after undo). history.py imports neither get_segment_cache, get_event_bus, nor get_collection_snapshot."
      evidence: "src/gruvax/api/admin/history.py:38 (imports), 194-195 (cache reload only, no derive/publish)"
      fix: "Add get_segment_cache + get_collection_snapshot + get_event_bus deps to revert_change_set; after cache.load() add segment_cache.invalidate()+derive() and bus.publish('boundary_changed', {'cube_ids':[...], 'change_set_id': new_id}) — mirror cubes.py:341-362."
  flows:
    - flow: "Admin cut-point/override/insert-cut edit → live kiosk re-render"
      breaks_at: "SSE fan-out payload shape from segments.py (INT-A)"
      impact: "Active-selection highlight does not follow the record after a Phase 5 edit; editing shimmer does not clear on commit (clears via 60s TTL sweeper instead). Main shelf grid still refreshes."
    - flow: "Admin undo (history revert) → estimator update + live kiosk re-render"
      breaks_at: "post-commit SegmentCache re-derive + SSE publish (INT-B)"
      impact: "After an undo, /api/locate sub-cube positions are stale until restart; kiosk never live-updates. Cube-level boundaries are correct (BoundaryCache reloads)."
tech_debt:
  - phase: 02-real-position-estimation
    items:
      - "VALIDATION.md is a stale plan-phase DRAFT (nyquist_compliant:false, TBD task IDs, unchecked Wave-0 boxes) never reconciled post-execution — yet VERIFICATION.md is passed 9/9 with tests confirmed green. Reconcile via /gsd-validate-phase 2."
      - "REQUIREMENTS.md traceability marks CUBE-08 'Pending' despite verified-complete + human-approved (stale doc)."
  - phase: 03-admin-loop-pin-manual-entry-undo
    items:
      - "WR-03: PinOverlay fabricates session expiry (10/30 min) instead of reading server; self-corrects within 30s via /session poll."
      - "WR-05: rate-limit key trusts REMOTE_ADDR (acceptable for single-host LAN, no reverse proxy)."
      - "WR-06: slowapi private-internals coupling (limiter._key_prefix / ._limiter.hit) — fragile across slowapi minor releases."
      - "IN-03: DiffPreviewSheet shows AFTER column only, no BEFORE values."
      - "login.py docstring claims expires_at/hard_cap_at in response body; actual response omits them (doc drift)."
  - phase: 04-realtime-live-updates
    items:
      - "CR-02: put_cube_boundary docstring claims a boundary_history write but no write_history_row call — single-cube edits leave no audit trail (pre-existing)."
      - "CR-03: DiffPreviewSheet isOverstuffed heuristic false-positives on empty→populated cubes."
      - "IN-02: KioskView SSE handlers JSON.parse without try/catch (a malformed/mis-keyed frame can throw — see INT-A)."
  - phase: 05-segment-aware-position-precision
    items:
      - "WR-01/WR-02: validate_contiguity is START-only and unit-scoped — deeper continuation / cross-unit contiguity edge cases are advisory residual risk beyond the enforced SEG-05 contract."
      - "Dead DiffPreviewSheet CSS (admin.css:987/2341/2523) + stale adminClient.ts:396 JSDoc left intentionally."
      - "SEG-01..08 traceability rows still 'Pending' + [ ] in REQUIREMENTS.md despite Phase 5 verified-complete (stale doc — flip to Complete)."
  - phase: 06-led-contract-over-mqtt-hardware-stubbed
    items:
      - "VERIFICATION.md status human_needed: 6 wire-level MQTT 5 checks (QoS/retain/MessageExpiryInterval, span-brightness-on-wire, diagnostic ambient restore, TTL revert timing, all-off idempotency, concurrent-diagnostic guard) require a live broker + MQTT 5 inspection — all PENDING (manual-only)."
      - "IN-01..IN-05: dead now_iso, redundant imports, console.debug artifact, duplicated PIN-length constant, ambiguous duration_ms — info-only, deferred."
  - phase: 07-wizards-import-export
    items:
      - "Hardcoded hex LED-color-picker defaults in Settings.tsx (pre-existing from Phase 6, not token-sourced)."
  - phase: 08-observability-deployment-hardening
    items:
      - "VERIFICATION.md status human_needed: 2 browser checks (admin /diagnostics Nordic-Grid render + REFRESH/RESET flow; kiosk >14d staleness banner) — PENDING (manual-only)."
      - "WR-03: diagnostics stalenessStatus() returns 'ok' when sync seconds is null — misleading 'OK' on unknown state."
      - "IN-01: check_benchmark.py passes (exit 0) when 0 benchmark IDs matched — SLO gate can silently no-op if IDs drift."
      - "IN-02: adminClient.ts console.debug in production path. IN-03: formatSyncAge shows '< 1h ago' for 0 seconds."
  - phase: 09-tooling-and-docs-hardening
    items:
      - "WR-04 deferred with documented rationale. No anti-pattern markers. Clean."
  - phase: cross-cutting (documentation hygiene)
    items:
      - "SUMMARY.md `requirements_completed` frontmatter is empty project-wide — the 3rd cross-reference source for milestone audits is unusable; VERIFICATION.md tables had to carry the full load."
      - "9 deferred v1 requirements (SRCH-09, OFF-01..04, PRIV-01..04) remain under '## v1 Requirements' with [ ] rather than moved to '## v2 / Backlog' — REQUIREMENTS.md header (81) and coverage block (84) also disagree on the requirement count."
nyquist:
  compliant_phases: ["01", "03", "04", "05", "06", "07", "08", "09"]
  partial_phases: ["02"]
  missing_phases: []
  overall: "8/9 compliant; Phase 2 VALIDATION.md is a stale draft (tests exist + pass per VERIFICATION) needing reconciliation"
---

# GRUVAX v1.0 — Milestone Audit

**Audited:** 2026-05-25
**Status:** ⚠️ `gaps_found`
**Milestone scope:** Phases 1–9 (84 v1 product requirements across Phases 1–8; Phase 9 is tooling/docs, no product REQ-IDs). Phases 999.1 / 999.2 are backlog and out of scope.

## North Star — Met ✓

> Type artist / title / label / catalog# → see the right cube (and a sub-cube position estimate) on the touchscreen within ~200 ms.

The Core Value flow is **wired end-to-end and verified**: kiosk search (P1) → `/api/locate` → segment-aware estimator (P5, the wired default; §4.1 cleanly retired to a private fallback) → cube highlight + sub-cube bar (P1/P2) → `/api/illuminate` MQTT fan-out consuming the segment-aware sub-span (P6). The milestone delivers its headline capability. The gaps below are in **admin-edit live-propagation seams** and **deferred resilience/privacy features**, not the kiosk lookup path.

## Why `gaps_found`

Two confirmed cross-phase **integration blockers** (each verified against source, not just reported) and nine **v1 requirements deferred but never relocated to backlog**.

### Cross-phase integration blockers

| ID | Seam | Affected REQs | Verified evidence |
|----|------|---------------|-------------------|
| **INT-A** | P5 segment editor → P4 kiosk SSE | RTM-01, ADMN-11, SEG-07/08 | `segments.py:295-300` publishes `boundary_changed` with `cubes`/`unit_id`; `KioskView.tsx:240` + `cubes.py:359` + `import_.py:559` all use `cube_ids`/`unit`. Mismatch ⇒ `cube_ids` is `undefined` on segment edits; the `for…of` throws before `relocateActiveSelection()` and shimmer-clear run. |
| **INT-B** | P3 history revert → P5 SegmentCache + P4 SSE | ADMN-09, RTM-01, ADMN-11, SEG-07 | `history.py:38` imports only boundary-cache/pool/auth; revert (`:194-195`) reloads BoundaryCache but never re-derives SegmentCache and never publishes `boundary_changed`. Undo leaves locate positions stale until restart and the kiosk silent. |

Both were invisible to per-phase verification because they live **between** phases (P5↔P4, P3-revert↔P4↔P5) — exactly what a milestone integration audit is for. Neither crashes the kiosk page (the main grid still refreshes); both degrade the live-update / undo experience for the owner.

### Broken end-to-end flows

- **Admin cut-point / override / insert-cut → live kiosk** — highlight no longer follows the record; shimmer clears only via the 60s TTL sweeper. (INT-A)
- **Admin undo → estimator + live kiosk** — `/api/locate` returns stale sub-cube positions post-undo; no kiosk live-update. (INT-B)

Five flows are clean: Core Value, Wizard/CSV/YAML import → bulk commit, LED illuminate→TTL→ambient lifecycle, observability (health/version/diagnostics read real subsystems + counters), and estimator supersession.

### Deferred-but-not-relocated requirements (9)

SPIDR-split out of the original "Realtime + Offline Resilience" on 2026-05-21, but still sitting under `## v1 Requirements` with unchecked boxes:

- **SRCH-09** — per-session recently-pulled list
- **OFF-01..04** — offline banner, disabled input, reconnect backoff, reconnect success
- **PRIV-01..04** — session-only history, no server-side query text, aggregate-only stats, no-PIN reset-kiosk

The integration checker confirmed these left **no half-wired dead code** (only one annotated `bannerVisible:false` stub). PRIV-02/03 are *de-facto* satisfied by Phase 8's release_id-only counters, but remain formally unchecked. These are documented deferrals, not silent drops — they need a scope decision (relocate to v2/backlog, or close in v1), not necessarily code.

## Requirements coverage (3-source cross-reference)

| Source | Result |
|--------|--------|
| REQUIREMENTS.md traceability | 67 "Complete", 17 "Pending" (of which 8 SEG are stale-Pending = actually done; 9 are genuine deferrals) |
| Phase VERIFICATION.md tables | Every in-scope REQ marked SATISFIED with code/API evidence across Phases 1–8 |
| SUMMARY.md `requirements_completed` frontmatter | **Empty project-wide** — source unusable; documentation-hygiene gap |

**Net:** **75 / 84 satisfied** (incl. the 8 SEG + CUBE-08 whose traceability rows are stale-Pending but verified-complete). **9 deferred.** 0 truly orphaned (every deferral is documented).

Doc fixes needed: flip SEG-01..08 and CUBE-08 traceability rows to "Complete" and tick their `[x]`; reconcile the REQUIREMENTS.md header count (81 vs 84).

## Phase verification roll-up

| Phase | Status | Score | Note |
|-------|--------|-------|------|
| 1 First Search → Cube Highlight | passed | 16/16 | — |
| 2 Real Position Estimation | passed | 9/9 | VALIDATION.md stale draft (nyquist reconcile) |
| 3 Admin Loop (PIN/Entry/Undo) | passed | 5/5 | undo path is INT-B's origin |
| 4 Realtime Live Updates | passed | 9/9 | kiosk SSE consumer is INT-A's victim |
| 5 Segment-Aware Precision | passed | 8/8 | segments.py SSE payload is INT-A's source |
| 6 LED Contract over MQTT | human_needed | 6/6 auto | 6 live-broker MQTT-5 checks pending (manual) |
| 7 Wizards + Import/Export | passed | 18/18 | — |
| 8 Observability + Deploy Hardening | human_needed | 12/12 auto | 2 browser checks pending (manual) |
| 9 Tooling and Docs Hardening | passed | 10/10 | clean |

All 9 have automated must-haves fully verified. Phases 6 & 8 carry **manual-only** human-verify checkpoints (live MQTT broker / browser visual) that were never formally signed off — not automated gaps, but open acceptance items.

## Nyquist coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 1, 3, 4, 5, 6, 7, 8, 9 | exists | true | none |
| 2 | exists (draft) | false | `/gsd-validate-phase 2` to reconcile the stale draft against the green test suite |

8/9 compliant. Phase 2's non-compliance is a *documentation* artifact (the draft predates the plans and was never reconciled), not a coverage hole — VERIFICATION.md confirms the tests exist and pass.

## Recommended path to close

1. **Fix INT-A** (1-line-per-call): rename `cubes`→`cube_ids`, `unit_id`→`unit` in the three `segments.py` `boundary_changed` publishes; drop the redundant `type` key.
2. **Fix INT-B**: wire `get_segment_cache` + `get_collection_snapshot` + `get_event_bus` into `history.py::revert_change_set`; re-derive SegmentCache and publish `boundary_changed` post-commit (mirror `cubes.py:341-362`). Also consider hardening `KioskView.tsx` SSE handlers with try/catch (IN-02) so a future payload drift degrades gracefully instead of throwing.
3. **Scope-decide the 9 deferrals**: relocate SRCH-09 / OFF-01..04 / PRIV-01..04 to `## v2 / Backlog` (recommended — they were explicitly deferred), or insert closure phases if they must ship in v1.
4. **Doc reconciliation**: flip SEG-01..08 + CUBE-08 traceability to Complete; fix the 81-vs-84 count; backfill SUMMARY `requirements_completed` frontmatter (or accept VERIFICATION.md as the system of record).
5. **Manual checkpoints**: run the Phase 6 live-broker MQTT-5 sweep and Phase 8 browser checks, or formally accept them as hardware/visual acceptance deferred to the hardware milestone.

A single closure phase can absorb items 1, 2, and 4; item 3 is a `/gsd-phase` edit + decision; item 5 is operator UAT.

---
*Audit generated by `/gsd-audit-milestone`. Integration findings independently verified against source before recording.*
