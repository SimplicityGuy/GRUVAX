---
phase: quick-260524-sd6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/0002_v_collection_view.py
  - justfile
  - .planning/todos/pending/2026-05-25-reconcile-gruvax-vs-gruvax-app-postgres-role-naming.md
autonomous: true
requirements: [SD6-RECONCILE-ROLE]
must_haves:
  truths:
    - "Operator-facing grant docs reference role `gruvax`, matching the runtime connection user"
    - "No occurrence of `gruvax_app` remains in migrations/ or justfile"
    - "The captured todo is marked completed (moved to .planning/todos/completed/)"
    - "No runtime behavior, SQL, or migration logic changed — comments/docstrings only"
  artifacts:
    - path: "migrations/versions/0002_v_collection_view.py"
      provides: "GRANT NOTE docstring referencing canonical role `gruvax`"
      contains: "GRANT USAGE ON SCHEMA discogsography TO gruvax;"
    - path: "justfile"
      provides: "provision-db recipe printing grants for canonical role `gruvax`"
      contains: "GRANT USAGE ON SCHEMA discogsography TO gruvax;"
    - path: ".planning/todos/completed/2026-05-25-reconcile-gruvax-vs-gruvax-app-postgres-role-naming.md"
      provides: "Completed todo record (moved from pending via git mv)"
  key_links:
    - from: "migrations/versions/0002_v_collection_view.py GRANT NOTE"
      to: "runtime DATABASE_URL user (gruvax)"
      via: "consistent role name in docs"
      pattern: "TO gruvax;"
---

<objective>
Reconcile the Postgres role naming mismatch between GRUVAX's runtime connection user
and its operator-facing grant documentation. The app connects as `gruvax` (correct,
unchanged), but two doc/comment locations reference a non-existent `gruvax_app` role.

Per locked user decision (D-01): canonical role name is `gruvax`. Replace every
`gruvax_app` occurrence in the migration 0002 GRANT NOTE docstring and the justfile
`provision-db` recipe with `gruvax`. Then mark the captured todo completed.

Purpose: De-risk the production cutover onto the shared discogsography Postgres — an
operator following `just provision-db` would otherwise grant read access to the wrong
role, breaking `gruvax.v_collection` reads in prod.
Output: Two doc/comment edits and one todo file move. No runtime/SQL/migration-logic change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/2026-05-25-reconcile-gruvax-vs-gruvax-app-postgres-role-naming.md

<interfaces>
<!-- Exact text the executor edits. The runtime user `gruvax` is CORRECT and MUST NOT change. -->

migrations/versions/0002_v_collection_view.py — GRANT NOTE docstring (lines ~16-21):
```python
GRANT NOTE (for operator, never run by application code):
    GRANT USAGE ON SCHEMA discogsography TO gruvax_app;
    GRANT SELECT ON discogsography.releases,
                    discogsography.artists,
                    discogsography.collection_items TO gruvax_app;
    -- No INSERT / UPDATE / DELETE granted.
    -- See `just provision-db` for the full provisioning script.
```

justfile — provision-db recipe (lines ~67-72):
```
provision-db:
    @echo "-- Run the following SQL as a superuser on the shared Postgres instance:"
    @echo "-- GRANT USAGE ON SCHEMA discogsography TO gruvax_app;"
    @echo "-- GRANT SELECT ON discogsography.releases, discogsography.artists,"
    @echo "--                  discogsography.collection_items TO gruvax_app;"
    @echo "-- (No INSERT / UPDATE / DELETE granted.)"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace gruvax_app with gruvax in grant docs</name>
  <files>migrations/versions/0002_v_collection_view.py, justfile</files>
  <action>
    Per locked decision D-01 (canonical role = `gruvax`), change every `gruvax_app`
    token to `gruvax` in exactly two locations:

    1. migrations/versions/0002_v_collection_view.py — the GRANT NOTE docstring
       (around lines 16-21). Edit only the docstring text. There are two occurrences:
       `GRANT USAGE ON SCHEMA discogsography TO gruvax_app;` and
       `...collection_items TO gruvax_app;`. Both become `... TO gruvax;`.

    2. justfile — the `provision-db` recipe (around lines 67-72). Two `@echo`'d comment
       lines reference `gruvax_app`; both become `gruvax`.

    Do NOT touch: the `_CREATE_VIEW` SQL or any executable code in the migration; the
    `.env` DATABASE_URL; compose.yaml; src/gruvax/settings.py; the `seed-dev` recipe;
    or any other runtime connection string. The runtime user `gruvax` is already
    correct — this task only makes the DOCS agree with it. No SQL is executed, no
    migration logic is altered, no fresh dev volume is required.
  </action>
  <verify>
    <automated>test -z "$(grep -rn gruvax_app migrations/ justfile)" && grep -q "TO gruvax;" migrations/versions/0002_v_collection_view.py && echo PASS</automated>
  </verify>
  <done>`grep -rn gruvax_app migrations/ justfile` returns zero matches; the GRANT NOTE and provision-db recipe both reference `gruvax`; no executable code changed.</done>
</task>

<task type="auto">
  <name>Task 2: Mark the captured todo completed</name>
  <files>.planning/todos/pending/2026-05-25-reconcile-gruvax-vs-gruvax-app-postgres-role-naming.md</files>
  <action>
    Move the captured todo from pending to completed, preserving the filename, using
    `git mv`:

    `git mv .planning/todos/pending/2026-05-25-reconcile-gruvax-vs-gruvax-app-postgres-role-naming.md .planning/todos/completed/2026-05-25-reconcile-gruvax-vs-gruvax-app-postgres-role-naming.md`

    If `.planning/todos/completed/` does not exist, create it first (`mkdir -p`), then
    run the `git mv`. Do not edit the file contents.
  </action>
  <verify>
    <automated>test -f .planning/todos/completed/2026-05-25-reconcile-gruvax-vs-gruvax-app-postgres-role-naming.md && test ! -f .planning/todos/pending/2026-05-25-reconcile-gruvax-vs-gruvax-app-postgres-role-naming.md && echo PASS</automated>
  </verify>
  <done>Todo file exists under .planning/todos/completed/ and no longer exists under .planning/todos/pending/.</done>
</task>

</tasks>

<verification>
- `grep -rn gruvax_app migrations/ justfile` → zero matches (canonical role enforced).
- `git diff --stat` shows only doc/comment lines changed in the two source files; no
  changes to .env, compose.yaml, settings.py, or any runtime connection string.
- `just provision-db` prints grants targeting `gruvax`.
- The todo is under .planning/todos/completed/.
</verification>

<success_criteria>
- Both grant-doc locations reference `gruvax` (locked D-01); zero `gruvax_app` remain in migrations/ or justfile.
- No runtime behavior, executed SQL, or migration logic changed — comments/docstrings only.
- Captured todo moved to completed via `git mv`, filename preserved.
</success_criteria>

<output>
Create `.planning/quick/260524-sd6-reconcile-gruvax-vs-gruvax-app-postgres-/260524-sd6-SUMMARY.md` when done
</output>
