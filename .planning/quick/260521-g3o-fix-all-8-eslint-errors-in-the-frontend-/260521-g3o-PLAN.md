---
phase: quick-260521-g3o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/admin/AdminShell.tsx
  - frontend/src/routes/admin/PinOverlay.tsx
  - frontend/src/routes/admin/CubeEditor.tsx
  - frontend/src/routes/admin/DiffPreviewSheet.tsx
autonomous: true
requirements: [QUICK-LINT-G3O]

must_haves:
  truths:
    - "`cd frontend && npm run lint` exits 0 with zero errors"
    - "`cd frontend && npm run build` (tsc -b && vite build) stays green"
    - "Admin loop behavior is byte-for-byte identical at runtime: PIN auto-submit on 4th digit, wrong-PIN shake/reset, rate-limit countdown, session countdown tick, 30s session poll, idle expiry → logout, Lock/Logout, cube form reset on boundary load, debounced phantom validation, suggest-midpoint, diff-preview mount validation + commit"
  artifacts:
    - path: "frontend/src/routes/admin/AdminShell.tsx"
      provides: "Lazy nowMs init + scoped/justified disables for poll & idle-expiry effects"
    - path: "frontend/src/routes/admin/PinOverlay.tsx"
      provides: "submitPin declared before auto-submit effect; unused `_` alias removed"
    - path: "frontend/src/routes/admin/CubeEditor.tsx"
      provides: "Boundary-load reset effect + debounced validation effect lint-clean"
    - path: "frontend/src/routes/admin/DiffPreviewSheet.tsx"
      provides: "Mount validation effect lint-clean"
  key_links:
    - from: "frontend/src/routes/admin/PinOverlay.tsx auto-submit effect"
      to: "submitPin useCallback"
      via: "dependency array references submitPin (now declared above)"
      pattern: "\\[digits, submitPin\\]"
---

<objective>
Fix all 8 ESLint errors in `frontend/src/routes/admin/` (React Compiler `react-hooks` rules + one unused var) so `npm run lint` exits 0, while `tsc -b && vite build` stays green and the admin loop's observable behavior is unchanged.

This admin loop already passed full UAT. These are lint-only failures from the React Compiler ruleset (`eslint-plugin-react-hooks@7`). Each fix is the minimal real change the rule requires — no logic, control flow, or UX timing may change.

Purpose: Unblock CI/lint gate without regressing a UAT-verified feature.
Output: Four files edited; `eslint .` clean; build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- The exact 8 errors, verified against current HEAD via `npm run lint`. -->
<!-- ESLint config (frontend/eslint.config.js) extends reactHooks.configs.flat.recommended -->
<!-- (eslint-plugin-react-hooks ^7.1.1) + tseslint recommended. DO NOT relax these in config. -->

1. AdminShell.tsx:39  react-hooks/purity        — `useState(Date.now())` calls impure fn in render
2. AdminShell.tsx:68  react-hooks/set-state-in-effect — `void pollSession()` in effect (async poll)
3. AdminShell.tsx:80  react-hooks/set-state-in-effect — `setIsLocked(false)` in idle-expiry effect
4. CubeEditor.tsx:192 react-hooks/set-state-in-effect — `setFields(...)` resets form on boundary load
5. CubeEditor.tsx:292 react-hooks/set-state-in-effect — `void runValidation(debouncedFields)` in effect
6. DiffPreviewSheet.tsx:65 react-hooks/set-state-in-effect — `setIsValidating(true)` in mount fetch effect
7. PinOverlay.tsx:48   react-hooks/immutability  — `submitPin` used in effect (48), declared at 74
8. PinOverlay.tsx:77   @typescript-eslint/no-unused-vars — `message: _` destructure alias never used
</interfaces>

<rule_guidance>
Per https://react.dev/learn/you-might-not-need-an-effect, prefer (in order):
(a) derive value during render instead of state+effect;
(b) reset state via a `key` prop instead of a reset-in-effect;
(c) move setState into the async callback / promise resolution;
(d) compute-during-render with a store-previous-value pattern.

ONLY where the effect is a legitimate external-system synchronization (timer
subscription, async network poll/fetch) that genuinely cannot be expressed as
derived render state may you use a SINGLE scoped
`// eslint-disable-next-line react-hooks/set-state-in-effect` with a one-line
written justification on the line directly above. Each disable covers exactly
one statement. NEVER disable the rule in eslint.config.js and NEVER add a
file-level disable.
</rule_guidance>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AdminShell.tsx (#1 purity + #2/#3 set-state-in-effect)</name>
  <files>frontend/src/routes/admin/AdminShell.tsx</files>
  <action>
Apply three fixes, behavior-identical:

#1 (line 39, react-hooks/purity): change `const [nowMs, setNowMs] = useState(Date.now())` to a lazy initializer `const [nowMs, setNowMs] = useState(() => Date.now())`. This is the canonical fix — same initial value, computed once on mount, no longer impure-during-render.

#2 (line 68, set-state-in-effect): the effect calls `void pollSession()` immediately, and `pollSession` (a useCallback) calls `setAdminLoggedIn`/`refreshExpiry`/`setAdminLoggedOut`/`setIsLocked` only AFTER `await adminGetSession()` resolves/rejects — i.e. the synchronous body itself does not setState; the lint flag is on the `void pollSession()` call. This is a legitimate async external-system synchronization (30s server session poll) that cannot be expressed as derived render state. Add a SINGLE scoped disable directly above the `void pollSession()` line:
`// eslint-disable-next-line react-hooks/set-state-in-effect -- async server session poll; setState happens in promise resolution, not synchronously`
Keep the `setInterval` poll and cleanup exactly as-is. If lint also flags the interval's `void pollSession()` (line 69), add the same scoped disable above that line with the same justification. Do not change `POLL_INTERVAL_MS`, deps `[isLoggedIn, pollSession]`, or any timing.

#3 (line 80, set-state-in-effect): the idle-expiry effect (lines 76-82) calls `setAdminLoggedOut()` then `setIsLocked(false)` when `nowMs >= sessionExpiresAt`. This is a legitimate timer-driven external synchronization (the per-second tick crossing the expiry boundary forces re-auth) — it must remain in the effect because it reacts to the `nowMs` timer value, not derivable during render without re-introducing the same setState. Add a SINGLE scoped disable directly above the `setAdminLoggedOut()` line (line 79):
`// eslint-disable-next-line react-hooks/set-state-in-effect -- timer-driven idle expiry; forces re-auth when the per-second tick crosses sessionExpiresAt`
If lint additionally flags line 80 (`setIsLocked(false)`) separately, add the same scoped disable above it too. Do NOT remove either setState call and do NOT change the `nowMs >= sessionExpiresAt` condition or the deps array.

Constraint — behavior preservation: countdown tick, poll cadence, idle-expiry → logout, Lock/Logout flows must be unchanged. The only structural change is the lazy `useState` initializer; everything else is a comment-only scoped disable.
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/routes/admin/AdminShell.tsx</automated>
  </verify>
  <done>eslint reports zero errors for AdminShell.tsx; `nowMs` uses `useState(() => Date.now())`; both effects retain their setState calls behind single-line scoped, justified disables; no eslint.config.js change.</done>
</task>

<task type="auto">
  <name>Task 2: Fix PinOverlay.tsx (#7 immutability/order + #8 unused var)</name>
  <files>frontend/src/routes/admin/PinOverlay.tsx</files>
  <action>
Apply two fixes, behavior-identical:

#7 (lines 48/74, react-hooks/immutability "accessed before it is declared"): the auto-submit effect at lines 46-51 calls `void submitPin(digits.join(''))`, but `submitPin` is declared as a `useCallback` further down at lines 74-111. Move the entire `submitPin = useCallback(async (pin: string) => {...}, [setAdminLoggedIn])` declaration (current lines 74-111) to ABOVE the auto-submit effect (so it sits between the `retryTimerRef` ref at line 43 and the auto-submit effect at line 46). Then add `submitPin` to the auto-submit effect's dependency array, making it `[digits, submitPin]`. Remove the now-redundant `// eslint-disable-next-line react-hooks/exhaustive-deps` (current line 50) ONLY if lint passes without it — `submitPin` is referentially stable (its deps are just `[setAdminLoggedIn]`, a stable store action) so the auto-submit effect will not re-fire spuriously, preserving "auto-submit exactly once when the 4th digit lands while status === 'idle'". If removing the disable reintroduces an exhaustive-deps error (e.g. eslint still wants something), keep a single scoped disable rather than expanding deps in a way that changes behavior. Verify the rate-limit countdown effect (currently lines 54-72) and all handlers below remain in place and unmodified.

Behavior guard: the auto-submit condition `if (digits.length === PIN_LENGTH && status === 'idle')` and the `void submitPin(digits.join(''))` call must be byte-identical. submitPin's body (setStatus('submitting'), adminLogin, success/RateLimitError/AuthError/else branches, all setTimeout timings 400ms/2000ms) must not change.

#8 (line 77, @typescript-eslint/no-unused-vars): the destructure `const { csrf_token: csrfToken, message: _ } = await adminLogin(pin)` aliases `message` to `_` which is never used. Remove the unused binding entirely: `const { csrf_token: csrfToken } = await adminLogin(pin)`. (Do not switch to a `_`-ignore pattern; just drop the field — `message` is not consumed anywhere.)

Constraint — behavior preservation: PIN entry, auto-submit-on-4th-digit, wrong-PIN shake+reset (400ms), generic-error reset (2000ms), and rate-limit countdown must all behave identically. This task is pure reordering + dep-array correction + dead-binding removal.
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/routes/admin/PinOverlay.tsx</automated>
  </verify>
  <done>eslint reports zero errors for PinOverlay.tsx; `submitPin` is declared before the auto-submit effect; effect deps are `[digits, submitPin]`; the `message: _` binding is gone; submitPin body and timings unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Fix CubeEditor.tsx + DiffPreviewSheet.tsx (#4/#5 + #6) and run the verification gate</name>
  <files>frontend/src/routes/admin/CubeEditor.tsx, frontend/src/routes/admin/DiffPreviewSheet.tsx</files>
  <action>
Apply three fixes, then run the full lint+build gate.

#4 (CubeEditor.tsx line 192, set-state-in-effect): the effect at lines 190-199 calls `setFields({...})` to populate the local form from the loaded `boundary` query result (resets form when boundary/prop changes). Preferred fix order: this is a server-data → local-editable-form sync (the user then edits `fields` locally), which is the canonical "you can't derive it because it's user-editable after seed" case. Use a SINGLE scoped disable directly above the `setFields({` line:
`// eslint-disable-next-line react-hooks/set-state-in-effect -- seed editable form state from the loaded boundary query; fields are user-edited afterward so cannot be derived during render`
Keep the `if (boundary) {...}` guard and `[boundary]` deps exactly as-is. (Do NOT switch to a `key`-prop reset — that would require restructuring the route element and risks remount/focus behavior changes; the scoped disable is the minimal behavior-preserving fix.)

#5 (CubeEditor.tsx line 292, set-state-in-effect): the effect at lines 285-294 calls `void runValidation(debouncedFields)` when all four debounced fields are set. `runValidation` (useCallback) sets `setIsValidating`/`setPhantomWarnings` inside an async network call (POST /api/admin/cubes/validate). This is a legitimate async external-system sync (debounced dry-run validation). Add a SINGLE scoped disable directly above the `void runValidation(debouncedFields)` line:
`// eslint-disable-next-line react-hooks/set-state-in-effect -- debounced async dry-run validation; setState happens inside the network call, not synchronously`
Keep the four-field guard, the `[debouncedFields, runValidation]` deps, and the 600ms debounce unchanged.

#6 (DiffPreviewSheet.tsx line 65, set-state-in-effect): the mount-only effect at lines 63-110 calls `setIsValidating(true)` then fires parallel `validateBoundary` + boundary fetches and sets results in the `.then()`. This is a legitimate on-mount async fetch/validation sync. The synchronous `setIsValidating(true)` at line 65 is the flagged line. Add a SINGLE scoped disable directly above it:
`// eslint-disable-next-line react-hooks/set-state-in-effect -- on-mount async validate+fetch; loading flag set before the network round-trip`
Keep the existing `// eslint-disable-next-line react-hooks/exhaustive-deps` (line 109) and the empty `[]` deps array as-is (mount-only by design). Do not change the Promise.all logic, error handling, or the early-return guard.

THEN run the verification gate from the frontend directory:
1. `cd frontend && npm run lint` — MUST exit 0 with zero errors (all 8 fixed).
2. `cd frontend && npm run build` — `tsc -b && vite build` MUST succeed (no TS or build regressions).
3. `cd frontend && git diff --stat src/routes/admin/` — confirm only the four target files changed and the diff is limited to: one lazy-init, one reorder, one dead-binding removal, and the scoped/justified disable comments (plus, in PinOverlay, the dep-array edit). No logic/control-flow/timing changes.

If lint or build fails, fix forward within the same constraints (scoped justified disables only for genuine external-sync effects; never relax eslint.config.js; never change runtime behavior) and re-run both gates until both are green.

Constraint — behavior preservation: cube form reset on boundary load, 600ms debounced phantom validation + near-misses, suggest-midpoint, and the diff-preview mount validation + commit flow must behave identically.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run build</automated>
  </verify>
  <done>`npm run lint` exits 0 with zero errors; `npm run build` (tsc -b && vite build) succeeds; `git diff` shows only the four admin files changed with no runtime-logic changes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run lint` → exits 0, "0 problems" (was "8 problems (8 errors)").
- `cd frontend && npm run build` → tsc -b passes, vite build completes.
- `git diff --stat frontend/src/routes/admin/` → exactly 4 files: AdminShell.tsx, PinOverlay.tsx, CubeEditor.tsx, DiffPreviewSheet.tsx.
- `git diff frontend/eslint.config.js` → EMPTY (no rule relaxation).
- Manual diff read confirms: no setState call deleted (except the unused `_` binding); no control flow / condition / timeout duration changed.
</verification>

<success_criteria>
- All 8 originally-reported ESLint errors resolved.
- `eslint .` exits 0 with zero errors.
- `tsc -b && vite build` stays green.
- Zero runtime behavior change to the UAT-verified admin loop.
- Any `set-state-in-effect` disables are single-line, scoped, justified, and only on legitimate external-sync effects; eslint.config.js untouched.
</success_criteria>

<output>
Create `.planning/quick/260521-g3o-fix-all-8-eslint-errors-in-the-frontend-/260521-g3o-SUMMARY.md` when done.
</output>
