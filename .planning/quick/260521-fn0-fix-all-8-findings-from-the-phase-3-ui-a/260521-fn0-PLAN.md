---
phase: quick-260521-fn0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/admin/FillBar.tsx
  - frontend/src/routes/admin/PinOverlay.tsx
  - frontend/src/routes/admin/CubeEditor.tsx
  - frontend/src/routes/admin/AlphaRail.tsx
  - frontend/src/routes/admin/CubesGrid.tsx
  - frontend/src/routes/admin/admin.css
autonomous: true
requirements: [UI-REVIEW-P03]
must_haves:
  truths:
    - "Admin fill bars are blue-light below 80%, yellow at 80–100%, error-red above 100%"
    - "The diff validation-error banner renders with an error-color treatment, not as unstyled black text"
    - "Wrong-PIN message reads exactly 'Incorrect PIN. Try again.'"
    - "Phantom warnings read exactly 'No match in collection. Did you mean one of these?'"
    - "SUGGEST MIDPOINT renders as an underline-on-hover text link, not an outlined button"
    - "The AlphaRail letter the grid is jumped to shows a blue background with white text"
    - "All Phase 3 admin badge/label typography uses declared spec sizes (no 10px); cube-card-label--last is internally consistent"
    - "Phase 3 admin spacing uses --gruvax-space-* tokens (or a documented named exception); no bare gap: 4px"
    - "frontend builds clean (tsc -b && vite build) and lints clean (eslint .)"
  artifacts:
    - path: "frontend/src/routes/admin/admin.css"
      provides: ".diff-validate-error, .alpha-rail-btn--active, midpoint link class, fill over-full class, corrected typography/spacing"
      contains: "diff-validate-error"
    - path: "frontend/src/routes/admin/FillBar.tsx"
      provides: "Three-tier fill color logic matching kiosk FillBar"
    - path: "frontend/src/routes/admin/AlphaRail.tsx"
      provides: "Active-letter indicator wiring (activeLetter prop + --active class)"
  key_links:
    - from: "frontend/src/routes/admin/CubesGrid.tsx"
      to: "frontend/src/routes/admin/AlphaRail.tsx"
      via: "activeLetter prop tracked in handleLetterTap"
      pattern: "activeLetter"
    - from: "frontend/src/routes/admin/DiffPreviewSheet.tsx"
      to: "frontend/src/routes/admin/admin.css"
      via: "diff-validate-error class definition"
      pattern: "diff-validate-error"
---

<objective>
Fix all 8 findings from the Phase 3 UI audit (`03-UI-REVIEW.md`) in the frontend
admin UI: 2 real bugs (inverted FillBar color logic; undefined `diff-validate-error`
CSS class) and 6 UI-SPEC conformance fixes (wrong-PIN copy, phantom copy, SUGGEST
MIDPOINT button style, AlphaRail active-letter indicator, off-spec typography,
off-spec spacing). Conform every change to `03-UI-SPEC.md` and the Nordic Grid
design language.

Purpose: The admin UI currently communicates wrong fill-urgency across the whole
cubes grid, silently fails a commit-blocking error banner with no visual treatment,
and diverges from the locked design contract in copy, button style, active-state
visibility, type scale, and spacing tokens. Closing these restores the audited
19/24 toward full spec conformance.

Output: Updated `FillBar.tsx`, `PinOverlay.tsx`, `CubeEditor.tsx`, `AlphaRail.tsx`,
`CubesGrid.tsx`, and `admin.css`. Frontend builds and lints clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/03-admin-loop-pin-manual-entry-undo/03-UI-REVIEW.md
@.planning/phases/03-admin-loop-pin-manual-entry-undo/03-UI-SPEC.md
@design/gruvax-design-language.md
@design/gruvax-design-tokens.css

<constraints>
- **Never hardcode a hex value.** Every color reference is a `var(--gruvax-*)` token
  or a `color-mix()` over a token. (CLAUDE.md + UI-SPEC §Color.)
- **Type system:** ALL CAPS labels = Barlow Condensed 700 (`--gruvax-font-display`),
  tracked wide; body/instructions = Space Grotesk (`--gruvax-font-ui`);
  catalog/counts/timestamps = DM Mono (`--gruvax-font-mono`). Never Barlow Condensed
  for body copy.
- **Declared Phase 3 type sizes (4 total):** 36px, 16px, 14px, 11px. No 10px tier exists.
  11px tokens: `--gruvax-text-mono-sm` (DM Mono metadata) and `--gruvax-text-label`
  (uppercase micro-labels).
- **Spacing:** use `--gruvax-space-*` token names; `--gruvax-space-1` = 4px,
  `--gruvax-space-2` = 8px. No token covers 2px — document any retained 2px as a
  named CSS exception per the spec ("never write raw pixel values").
- **There is no `--gruvax-font-body` token.** Body font = `--gruvax-font-ui`
  (Space Grotesk). Body text token = `--gruvax-text-body` (16px) /
  `--gruvax-text-body-sm` (14px).
</constraints>

<interfaces>
<!-- Key contracts extracted from the codebase — executor uses these directly, no exploration needed. -->

Spec strings (03-UI-SPEC.md §Copywriting Contract, must match EXACTLY):
- Wrong PIN: "Incorrect PIN. Try again."
- Phantom — no collection match: "No match in collection. Did you mean one of these?"

Spec fill-bar color tiers (03-UI-SPEC.md §C and §I — admin and kiosk identical rules):
- 0% (is_empty)  → no bar
- 1–79%          → var(--gruvax-blue-light)
- 80–100%        → var(--gruvax-yellow)
- >100%          → var(--gruvax-error)

Spec SUGGEST MIDPOINT (03-UI-SPEC.md §D, reserved-accent #5):
- "Barlow Condensed 700 14px --gruvax-blue, underline on hover" — an inline text
  action (link-style), NOT a bordered secondary button.

Spec AlphaRail active letter (03-UI-SPEC.md §D, reserved-accent #6):
- "active letter: --gruvax-blue background + --gruvax-white text"

Reference implementation — kiosk FillBar (frontend/src/routes/kiosk/FillBar.tsx)
already implements the correct three-tier logic via inline style:
```
const color =
  fillLevel < 0.8
    ? 'var(--gruvax-blue-light)'
    : fillLevel <= 1.0
      ? 'var(--gruvax-yellow)'
      : 'var(--gruvax-error)'
```
Note: the admin FillBar uses a track+fill structure with CSS classes
(`.fill-bar-track` wrapping `.fill-bar-fill`); the kiosk FillBar renders a single
`.fill-bar` div. Preserve the admin track/fill structure; change only the color
logic to three tiers.

AlphaRail current contract (frontend/src/routes/admin/AlphaRail.tsx):
```
interface AlphaRailProps {
  activeLetters: Set<string>      // letters that have ≥1 cube
  onLetterTap: (letter: string) => void
}
```
Consumed in CubesGrid.tsx:156 with `handleLetterTap` (CubesGrid.tsx:67) which
scrolls but does NOT currently track which letter is active. The grid's
`data-label-letter` anchors already exist (CubesGrid.tsx:106–114).

admin.css existing patterns to mirror:
- `.diff-warning--empty` (admin.css ~1165): warning-tinted banner via
  `color-mix(in srgb, var(--gruvax-warning) 12%, transparent)` bg + warning text +
  35% border. The new `.diff-validate-error` should mirror this with
  `--gruvax-error`.
- `.diff-warning--overstuffed` (admin.css ~1171): already uses `--gruvax-error`
  at 10% bg / 30% border — a good reference for the error treatment intensity.
- `.alpha-rail-btn` (admin.css ~462) + `.alpha-rail-btn--inactive` (~491):
  add a sibling `.alpha-rail-btn--active`.
- `.fill-bar-fill` (admin.css ~437, currently `--gruvax-yellow`) +
  `.fill-bar-fill--warn` (~443, `--gruvax-error`).
- `.editor-btn-secondary` (admin.css ~933): the outlined style to replace for the
  midpoint action.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix component logic & copy (FillBar tiers, PIN/phantom copy, AlphaRail active wiring, midpoint class)</name>
  <files>
    frontend/src/routes/admin/FillBar.tsx,
    frontend/src/routes/admin/PinOverlay.tsx,
    frontend/src/routes/admin/CubeEditor.tsx,
    frontend/src/routes/admin/AlphaRail.tsx,
    frontend/src/routes/admin/CubesGrid.tsx
  </files>
  <action>
Apply the TSX-side fixes for findings #1, #3, #4, #5 (markup), and #6 (wiring).

(#1 — admin FillBar three-tier color logic) In `admin/FillBar.tsx`:
Replace the 2-tier `const isOverFull = fillLevel > 0.8` logic. Compute the spec's
three tiers (blue-light <0.8, yellow 0.8–1.0, error >1.0) matching the kiosk
FillBar reference in `<interfaces>`. Keep the existing track/fill DOM structure
(`.fill-bar-track` > `.fill-bar-fill`) and the `clampedFill` width math. Drive the
color by applying one of three CSS classes to the fill element — keep the default
`.fill-bar-fill`, replace the single `--warn` toggle with: blue-light tier =>
no extra modifier (default class set to blue-light in admin.css, Task 2),
yellow tier => `.fill-bar-fill--near`, error/over tier => `.fill-bar-fill--over`.
Do NOT switch to inline style; the project keeps admin fill colors in admin.css.
Then fix the JSDoc block at the top of the file (lines ~7–11) which currently
documents the WRONG thresholds ("yellow at ≤80%, error at >80%") — rewrite it to
state the three correct tiers (blue-light 1–79%, yellow 80–100%, error >100%),
matching the kiosk FillBar JSDoc wording.

(#3 — wrong-PIN copy) In `admin/PinOverlay.tsx` line 167: change the literal
`'Incorrect PIN'` to `'Incorrect PIN. Try again.'` (exact spec §A string,
including the trailing period). Leave the `errorMsg` fallback branch unchanged.

(#4 — phantom copy) In `admin/CubeEditor.tsx`, BOTH occurrences (the first-boundary
block ~line 439 and the last-boundary block ~line 519): change
`Not in collection — verify label/catalog.` to
`No match in collection. Did you mean one of these?` (exact spec §Error states
string). Change only the text node inside `.phantom-chip-text`; leave the
near-miss chip mapping and USE ANYWAY button untouched.

(#5 markup — SUGGEST MIDPOINT) In `admin/CubeEditor.tsx` ~line 561: change the
button's `className` from `editor-btn-secondary` to `editor-btn-link` (the new
link-style class added in Task 2). Do not change the button's logic, disabled
state, or label text.

(#6 wiring — AlphaRail active letter) Make the AlphaRail track and display the
currently-jumped-to letter:
  - In `admin/AlphaRail.tsx`: add an optional prop `activeLetter?: string | null`
    to `AlphaRailProps` (document it in the JSDoc props list). When a button's
    letter equals `activeLetter`, append the `alpha-rail-btn--active` class
    (added in Task 2). Order class precedence so an active letter still reads as
    active even though it is by definition a matched (non-inactive) letter.
    Add `aria-current="true"` on the active button for assistive tech.
  - In `admin/CubesGrid.tsx`: add state `const [activeLetter, setActiveLetter] =
    useState<string | null>(null)` (import `useState`). In `handleLetterTap`
    (~line 67), call `setActiveLetter(letter)` when a matching scroll target is
    found (i.e. inside the `if (target)` branch). Pass `activeLetter={activeLetter}`
    to `<AlphaRail .../>` (~line 156).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -20; grep -c "Incorrect PIN. Try again." src/routes/admin/PinOverlay.tsx; grep -c "No match in collection. Did you mean one of these?" src/routes/admin/CubeEditor.tsx; grep -c "editor-btn-link" src/routes/admin/CubeEditor.tsx; grep -c "activeLetter" src/routes/admin/CubesGrid.tsx</automated>
  </verify>
  <done>
    `admin/FillBar.tsx` applies three color tiers (no `isOverFull > 0.8` 2-tier
    logic) and its JSDoc documents the correct thresholds. PinOverlay renders
    "Incorrect PIN. Try again." (grep count ≥ 1). Both CubeEditor phantom blocks
    render "No match in collection. Did you mean one of these?" (grep count = 2).
    SUGGEST MIDPOINT button uses `editor-btn-link`. AlphaRail receives and applies
    `activeLetter`; CubesGrid tracks it in `handleLetterTap`. TypeScript compiles
    with no new errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix admin.css (define classes, typography, spacing)</name>
  <files>frontend/src/routes/admin/admin.css</files>
  <action>
Apply the CSS-side fixes for findings #1, #2, #5, #6, #7, #8. Consume tokens only —
no hardcoded hex.

(#2 — define `.diff-validate-error`) Add a new rule (place near the other diff
warning rules, ~after `.diff-warning--overstuffed` at ~line 1175, OR near the
`.diff-sheet-actions` block where the element is rendered). Mirror the
`.diff-warning--empty` structure (flex, gap `--gruvax-space-2`, padding
`--gruvax-space-2` `--gruvax-space-3`, border-radius `--gruvax-radius-md`,
`--gruvax-font-ui`, `--gruvax-text-body-sm`) but use the ERROR token:
background `color-mix(in srgb, var(--gruvax-error) 10%, transparent)`,
color `var(--gruvax-error)`, border `1px solid color-mix(in srgb,
var(--gruvax-error) 30%, transparent)`. This matches the existing
`.diff-warning--overstuffed` error treatment intensity.

(#1 — fill bar three-tier classes) Update the fill classes to match Task 1:
  - `.fill-bar-fill` (~line 437): change default `background` from
    `var(--gruvax-yellow)` to `var(--gruvax-blue-light)` (this is the 1–79% tier).
  - Add `.fill-bar-fill--near { background: var(--gruvax-yellow); }` (80–100% tier).
  - Rename/replace `.fill-bar-fill--warn` (~line 443) with
    `.fill-bar-fill--over { background: var(--gruvax-error); }` (>100% tier).
    (Task 1 emits `--near` and `--over`; ensure both class names exist and `--warn`
    is removed so no dead rule remains.)

(#5 CSS — midpoint link style) Add `.editor-btn-link` per spec §D: inline text
action, `--gruvax-font-display` (Barlow Condensed), font-weight 700,
font-size `var(--gruvax-text-body-sm, 14px)` (14px), color `var(--gruvax-blue)`,
`letter-spacing: var(--gruvax-tracking-display, 0.14em)`, `text-transform:
uppercase`, background transparent, border none, padding 0 (or
`--gruvax-space-1` block padding only to keep tap target — keep min-height ≥ 44px
via padding for touch), cursor pointer, no underline by default, and
`text-decoration: underline` on `:hover:not(:disabled)`. Add a `:focus-visible`
outline `2px solid var(--gruvax-blue)` offset 2px and a `:disabled { opacity: 0.6;
cursor: not-allowed; }` rule. Do NOT remove `.editor-btn-secondary` (it may be used
elsewhere — grep first; if no other consumer, it may remain harmlessly).

(#6 CSS — AlphaRail active class) Add `.alpha-rail-btn--active` after
`.alpha-rail-btn--inactive` (~line 493) per spec §D: `background:
var(--gruvax-blue); color: var(--gruvax-white);` Add `border-radius:
var(--gruvax-radius-md)` for a clean filled chip. Ensure it visually overrides the
plain `.alpha-rail-btn` (which is transparent bg + blue text) — since both classes
sit on the same element, `--active` declared later in the file wins on equal
specificity; confirm the active rule appears after the base rule.

(#7 — typography) 
  - `.cube-card-empty-badge` (~line 592): change `font-size: 10px` to
    `font-size: var(--gruvax-text-label, 11px)` (uppercase micro-label role).
  - `.history-source-badge` (~line 1385): change `font-size: 10px` to
    `font-size: var(--gruvax-text-label, 11px)`.
  - `.cube-card-label--last` (~lines 622–626): the spec's 14px role is Space
    Grotesk 400 (body), but this class currently inherits Barlow Condensed (display)
    from `.cube-card-label` at 14px weight 700 — neither the spec body role nor the
    display role. Make it internally consistent with the spec's 14px role: set
    `font-family: var(--gruvax-font-ui)` (Space Grotesk), `font-size:
    var(--gruvax-text-body-sm, 14px)`, `font-weight: 400`, and remove
    `text-transform: uppercase` / wide tracking inherited from `.cube-card-label`
    by setting `letter-spacing: normal; text-transform: none;`. (It is a secondary
    record label, not an ALL-CAPS display label.)

(#8 — spacing) 
  - `.diff-mini-grid` (~line 1064): change `gap: 4px` to
    `gap: var(--gruvax-space-1)`.
  - For the three 2px paddings that have no matching token —
    `.cube-card-empty-badge` (~line 599 `padding: 2px 6px`),
    `.history-source-badge` (~line 1392 `padding: 2px 6px`),
    `.history-reverted-pill` (~line 1413 `padding: 2px 8px`): replace the
    horizontal (6px/8px) values with the nearest tokens where they match
    (6px has no token — keep, or use `--gruvax-space-2`/8px for the 8px one);
    for the 2px vertical paddings with no token, add a CSS comment immediately
    above each declaring it a named exception, e.g.
    `/* spacing-exception: 2px vertical pad — below --gruvax-space-1 (4px) for pill density (UI-REVIEW §Pillar 5) */`.
    Prefer tokens wherever a token's value matches; only the sub-4px values stay
    as documented exceptions. Where you keep a raw px, the comment is mandatory.
  </action>
  <verify>
    <automated>cd frontend && grep -c "diff-validate-error" src/routes/admin/admin.css; grep -c "alpha-rail-btn--active" src/routes/admin/admin.css; grep -c "editor-btn-link" src/routes/admin/admin.css; grep -c "fill-bar-fill--near\|fill-bar-fill--over" src/routes/admin/admin.css; grep -vn '^[[:space:]]*/\*' src/routes/admin/admin.css | grep -c "font-size: 10px"; grep -vn '^[[:space:]]*/\*' src/routes/admin/admin.css | grep -c "gap: 4px"</automated>
  </verify>
  <done>
    `.diff-validate-error`, `.alpha-rail-btn--active`, `.editor-btn-link`,
    `.fill-bar-fill--near`, and `.fill-bar-fill--over` are all defined in admin.css
    (each grep ≥ 1; the fill-bar grep ≥ 2). `.fill-bar-fill` default background is
    `--gruvax-blue-light` and `.fill-bar-fill--warn` is gone. Zero `font-size: 10px`
    declarations remain. Zero bare `gap: 4px` declarations remain. The three 2px
    paddings either use tokens or carry an inline `spacing-exception` comment.
    `.cube-card-label--last` uses `--gruvax-font-ui` / 14px / weight 400.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build, lint, and spec-string conformance gate</name>
  <files>frontend/src/routes/admin/admin.css, frontend/src/routes/admin/FillBar.tsx</files>
  <action>
Verify the full set of fixes builds, lints, and conforms to `03-UI-SPEC.md`. This
task makes no source edits unless the build/lint surfaces a regression introduced
by Tasks 1–2 (e.g. an unused import from the AlphaRail/CubesGrid changes, a leftover
`isOverFull` reference, or a removed-class reference). If a regression appears, fix
it in the offending file, then re-run.

1. Run the production build: `npm run build` (= `tsc -b && vite build`). Must exit 0.
2. Run lint: `npm run lint` (= `eslint .`). Must exit 0 with no new errors.
3. No-hardcoded-hex gate on the touched files: grep the admin TSX + admin.css for
   hex literals; expect zero matches (all color via tokens / color-mix).
4. Cross-check each implemented string/threshold against `03-UI-SPEC.md`:
   - "Incorrect PIN. Try again." == §Error states / §A.
   - "No match in collection. Did you mean one of these?" == §Error states.
   - FillBar tiers blue-light/yellow/error at <0.8 / ≤1.0 / >1.0 == §C and §I.
   - AlphaRail active = blue bg + white text == §D.
   - SUGGEST MIDPOINT = Barlow 700 14px blue, underline on hover == §D.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && ! grep -rEn "#[0-9a-fA-F]{3,8}" src/routes/admin/*.tsx src/routes/admin/admin.css && echo "HEX-CLEAN-AND-BUILD-OK"</automated>
  </verify>
  <done>
    `npm run build` and `npm run lint` both exit 0. No hardcoded hex values in any
    touched admin file. Each of the 8 findings' implemented value matches its
    `03-UI-SPEC.md` clause. Final check prints "HEX-CLEAN-AND-BUILD-OK".
  </done>
</task>

</tasks>

<verification>
- `npm run build` (tsc + vite) exits 0.
- `npm run lint` (eslint) exits 0.
- No hardcoded hex in `frontend/src/routes/admin/*.tsx` or `admin.css`.
- All 8 UI-REVIEW findings closed and verified against `03-UI-SPEC.md`:
  1. FillBar 3-tier color logic + corrected JSDoc.
  2. `.diff-validate-error` defined with error treatment.
  3. "Incorrect PIN. Try again." exact copy.
  4. "No match in collection. Did you mean one of these?" exact copy (×2).
  5. SUGGEST MIDPOINT link style (text + underline-on-hover, blue, Barlow 700 14px).
  6. AlphaRail active-letter indicator (blue bg + white text) wired through CubesGrid.
  7. 10px badge sizes → 11px token; `cube-card-label--last` font family/size/weight consistent.
  8. Spacing tokens applied (`gap: var(--gruvax-space-1)`); 2px exceptions documented.
</verification>

<success_criteria>
- All 8 findings from `03-UI-REVIEW.md` are fixed and conform to `03-UI-SPEC.md`.
- Frontend builds and lints clean.
- Zero hardcoded hex; all colors via `--gruvax-*` tokens or `color-mix()`.
- Typography uses declared spec sizes (no 10px); spacing uses tokens or documented exceptions.
</success_criteria>

<output>
Create `.planning/quick/260521-fn0-fix-all-8-findings-from-the-phase-3-ui-a/260521-fn0-SUMMARY.md` when done.
</output>
