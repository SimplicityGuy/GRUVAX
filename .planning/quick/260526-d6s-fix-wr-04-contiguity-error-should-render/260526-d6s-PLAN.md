---
phase: quick-260526-d6s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-segment-aware-position-precision/05-REVIEW.md
autonomous: true
requirements:
  - WR-04
must_haves:
  truths:
    - "validate_contiguity emits the original-case label (e.g. 'Blue Note') in the user-facing contiguity_error message."
    - "The integration test test_put_cut_scatter_rejected_contiguity_error asserts the proper-case label is present AND the casefolded form is absent — regression-locked."
    - "05-REVIEW.md no longer presents WR-04 as an open warning; it is annotated as RESOLVED with the landing commit, so the review file is accurate for future audits."
  artifacts:
    - path: src/gruvax/api/admin/validation.py
      provides: "label_display dict + format(label=label_display[lk]) in validate_contiguity"
      contains: "label_display: dict[str, str]"
    - path: tests/integration/test_segment_api.py
      provides: "regression assertion that the contiguity_error surfaces 'Blue Note' (not 'blue note')"
      contains: '"Blue Note" in raw_msg'
    - path: .planning/phases/05-segment-aware-position-precision/05-REVIEW.md
      provides: "WR-04 annotated RESOLVED in commit 3598c22"
      contains: "WR-04"
  key_links:
    - from: "validate_contiguity (validation.py)"
      to: "_CONTIGUITY_MSG_TEMPLATE format slot"
      via: "label_display[lk] (NOT lk)"
      pattern: "label=label_display\\[lk\\]"
    - from: "test_put_cut_scatter_rejected_contiguity_error"
      to: "contiguity_error response body"
      via: "raw_msg assertions on proper case"
      pattern: '"Blue Note" in raw_msg'
---

<objective>
Close out WR-04 from 05-REVIEW.md: `validate_contiguity` must surface the
original-case label (e.g. "Blue Note") in the user-facing contiguity error
message, never the casefolded key ("blue note").

Purpose: The CLAUDE.md design-language voice rule says labels are proper nouns
from the collection. Showing the owner "split blue note across non-adjacent bins"
reads as a bug. WR-04 in 05-REVIEW.md prescribed the exact fix: introduce a
`label_display: dict[str, str]` map (casefold → first-seen original-case label)
and format the error with `label=label_display[lk]`.

Output: An accurate 05-REVIEW.md (WR-04 marked RESOLVED with landing commit) and
a verified-still-green production codepath.

CRITICAL DISCOVERY DURING PLANNING: The WR-04 production fix AND the integration
test tightening are BOTH ALREADY ON MAIN, landed as part of commit `3598c22`
(the Phase 5 PR squash-merge):

- `src/gruvax/api/admin/validation.py` lines 253-262 build `label_display` from
  the first-seen original-case label per casefold key.
- `src/gruvax/api/admin/validation.py` line 278 returns
  `_CONTIGUITY_MSG_TEMPLATE.format(label=label_display[lk])`.
- `tests/integration/test_segment_api.py` lines 619-628 already assert
  `"Blue Note" in raw_msg` AND `"blue note" not in raw_msg` — both directions.

The only outstanding work is a documentation correction: 05-REVIEW.md still
lists WR-04 as an unresolved warning (its review pre-dated the merge that
included the fix). This plan annotates WR-04 as RESOLVED so the review file is
accurate for future audits, and re-verifies the production fix via grep gates
to confirm no regression has crept in between then and now.

Scope guard from the task brief: ONLY WR-04. Do NOT touch WR-01/WR-02/WR-03,
IN-01/IN-02/IN-03, or refactor `validate_contiguity` further.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<!-- The bug site as it exists on main (already fixed). -->
@src/gruvax/api/admin/validation.py

<!-- The regression-locking integration test as it exists on main (already tightened). -->
@tests/integration/test_segment_api.py

<!-- The review file whose WR-04 entry needs to be marked RESOLVED. -->
@.planning/phases/05-segment-aware-position-precision/05-REVIEW.md

<interfaces>
<!-- Key contract extracted from validation.py. Use directly — no exploration needed. -->

From src/gruvax/api/admin/validation.py (lines 250-280, already on main):

```
# label_start_positions: dict[str, list[int]] — casefold key -> indices in label_sequence
# label_display:         dict[str, str]       — casefold key -> first-seen ORIGINAL-case label
#
# Construction loop:
#   for i, lbl in enumerate(label_sequence):
#       lk = lbl.casefold()
#       if lk not in label_start_positions:
#           label_start_positions[lk] = []
#       if lk not in label_display:
#           label_display[lk] = lbl
#       label_start_positions[lk].append(i)
#
# Emission site (on contiguity gap):
#   return _CONTIGUITY_MSG_TEMPLATE.format(label=label_display[lk])
```

From tests/integration/test_segment_api.py (lines 619-628, already on main):

```
assert "Blue Note" in raw_msg, ...
assert "blue note" not in raw_msg, ...
```

Both forward (proper-case present) and reverse (casefolded absent) assertions
already lock the behavior. No new test is needed; the WR-04 test-coverage gap
is already closed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the WR-04 production fix and regression test are still in place</name>
  <files>src/gruvax/api/admin/validation.py, tests/integration/test_segment_api.py</files>
  <action>
This is a read-only verification step — no source edits. Confirm via grep gates
that the WR-04 fix landed in commit 3598c22 is still on the current HEAD before
touching the review file. If any of these gates fail, STOP and surface the
regression instead of proceeding to Task 2.

Gate A (production code — label_display dict declared with type annotation):
  grep -v '^[[:space:]]*#' src/gruvax/api/admin/validation.py \
    | grep -c 'label_display: dict\[str, str\] = {}'
  Must equal 1.

Gate B (production code — label_display populated with original-case label):
  grep -v '^[[:space:]]*#' src/gruvax/api/admin/validation.py \
    | grep -c 'label_display\[lk\] = lbl'
  Must equal 1.

Gate C (production code — emission site uses label_display[lk], NOT lk):
  grep -v '^[[:space:]]*#' src/gruvax/api/admin/validation.py \
    | grep -c '_CONTIGUITY_MSG_TEMPLATE.format(label=label_display\[lk\])'
  Must equal 1.

Gate D (production code — old buggy form is GONE):
  grep -v '^[[:space:]]*#' src/gruvax/api/admin/validation.py \
    | grep -c '_CONTIGUITY_MSG_TEMPLATE.format(label=lk)'
  Must equal 0.

Gate E (integration test — proper-case present assertion locked in):
  grep -v '^[[:space:]]*#' tests/integration/test_segment_api.py \
    | grep -c '"Blue Note" in raw_msg'
  Must equal 1.

Gate F (integration test — casefolded absent assertion locked in):
  grep -v '^[[:space:]]*#' tests/integration/test_segment_api.py \
    | grep -c '"blue note" not in raw_msg'
  Must equal 1.

All six gates must pass. They confirm that (1) the fix WR-04 prescribed is
implemented as specified, (2) the buggy form is not lurking elsewhere in the
same file, and (3) the integration test asserts both directions of the
behavior. Filter comments with `grep -v '^[[:space:]]*#'` before counting per
GSD grep-gate hygiene rules — the code comments at validation.py lines 253-254
and 276-277 reference WR-04 and would otherwise inflate counts.
  </action>
  <verify>
    <automated>set -e; cd /Users/Robert/Code/public/GRUVAX; A=$(grep -v '^[[:space:]]*#' src/gruvax/api/admin/validation.py | grep -c 'label_display: dict\[str, str\] = {}'); B=$(grep -v '^[[:space:]]*#' src/gruvax/api/admin/validation.py | grep -c 'label_display\[lk\] = lbl'); C=$(grep -v '^[[:space:]]*#' src/gruvax/api/admin/validation.py | grep -c '_CONTIGUITY_MSG_TEMPLATE.format(label=label_display\[lk\])'); D=$(grep -v '^[[:space:]]*#' src/gruvax/api/admin/validation.py | grep -c '_CONTIGUITY_MSG_TEMPLATE.format(label=lk)'); E=$(grep -v '^[[:space:]]*#' tests/integration/test_segment_api.py | grep -c '"Blue Note" in raw_msg'); F=$(grep -v '^[[:space:]]*#' tests/integration/test_segment_api.py | grep -c '"blue note" not in raw_msg'); echo "A=$A B=$B C=$C D=$D E=$E F=$F"; [ "$A" = "1" ] && [ "$B" = "1" ] && [ "$C" = "1" ] && [ "$D" = "0" ] && [ "$E" = "1" ] && [ "$F" = "1" ]</automated>
  </verify>
  <done>All six gates pass. Production fix and regression test are confirmed in place on HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Mark WR-04 RESOLVED in 05-REVIEW.md with landing-commit reference</name>
  <files>.planning/phases/05-segment-aware-position-precision/05-REVIEW.md</files>
  <action>
Update the WR-04 entry in 05-REVIEW.md (currently at lines 159-180) so the
review file accurately reflects that the fix shipped. This is a documentation
correction — the fix landed in commit 3598c22 (Phase 5 squash-merge PR #8) but
the review file's WR-04 entry still reads as an open warning, which makes the
review file misleading for future audits.

Two edits:

EDIT 1 — Change the section heading to include a RESOLVED marker:

  FROM:
    ### WR-04: `validate_contiguity` emits the casefolded label in the user-facing error message

  TO:
    ### WR-04 [RESOLVED in commit 3598c22 (Phase 5 PR #8), verified 2026-05-26]: `validate_contiguity` emits the casefolded label in the user-facing error message

EDIT 2 — Append a "Resolution" subsection at the end of the WR-04 entry,
immediately before the next heading line `## Info` (currently line 182). The
appended block documents what landed, where, and the regression-test guarantee.
Use this exact block:

    **Resolution (verified 2026-05-26 via quick task 260526-d6s):**

    The prescribed fix shipped as part of commit `3598c22` (Phase 5 squash-merge,
    PR #8). On `main` HEAD:

    - `src/gruvax/api/admin/validation.py` builds
      `label_display: dict[str, str]` mapping each casefold key to the
      first-seen original-case label.
    - The emission site returns
      `_CONTIGUITY_MSG_TEMPLATE.format(label=label_display[lk])`, never
      `format(label=lk)`.
    - `tests/integration/test_segment_api.py::test_put_cut_scatter_rejected_contiguity_error`
      now asserts BOTH `"Blue Note" in raw_msg` AND
      `"blue note" not in raw_msg`, locking the casing behavior against
      regression in both directions.

    The bug an owner would have seen ("split blue note across non-adjacent
    bins") is no longer producible by the validation path.

Do NOT touch any other WR-xx or IN-xx entry. Do NOT modify the frontmatter
counts (warning: 4) — even though WR-04 is resolved, the historical review
counts at review-time are preserved as audit data; the per-entry RESOLVED
annotation is the supported way to record post-review closure in this
project's convention.

Do NOT change the body's "Issue" / "Fix" prose — those describe the bug as
found at review time, which is correct historical context. Only the heading
gets a RESOLVED marker; the closure detail goes in the new Resolution
subsection.
  </action>
  <verify>
    <automated>set -e; cd /Users/Robert/Code/public/GRUVAX; grep -c 'WR-04 \[RESOLVED in commit 3598c22' .planning/phases/05-segment-aware-position-precision/05-REVIEW.md | grep -q '^1$' && grep -c '\*\*Resolution (verified 2026-05-26 via quick task 260526-d6s):\*\*' .planning/phases/05-segment-aware-position-precision/05-REVIEW.md | grep -q '^1$' && grep -c 'label_display\[lk\]' .planning/phases/05-segment-aware-position-precision/05-REVIEW.md | (read n; [ "$n" -ge 2 ])</automated>
  </verify>
  <done>05-REVIEW.md WR-04 heading carries the RESOLVED marker with commit hash, and a Resolution subsection documents the landed fix + test + verification date.</done>
</task>

</tasks>

<verification>
After both tasks:

1. `git diff --stat` shows exactly one file changed:
   `.planning/phases/05-segment-aware-position-precision/05-REVIEW.md`.
   No source code or test files are modified by this plan — those edits already
   shipped in commit 3598c22.

2. `grep -c '_CONTIGUITY_MSG_TEMPLATE.format(label=label_display\[lk\])' src/gruvax/api/admin/validation.py`
   returns `1` (production fix in place).

3. `grep -c '"Blue Note" in raw_msg' tests/integration/test_segment_api.py`
   returns `1` (regression test in place).

4. `grep -c 'WR-04 \[RESOLVED in commit 3598c22' .planning/phases/05-segment-aware-position-precision/05-REVIEW.md`
   returns `1` (review file updated).
</verification>

<success_criteria>
- All six grep gates in Task 1 pass on first run (no regressions in the
  Phase 5 fix that landed in commit 3598c22).
- 05-REVIEW.md WR-04 entry is annotated RESOLVED with the landing-commit
  hash and a Resolution block documenting the production fix, the regression
  test guarantee (both directions), and the verification date.
- No source code or test files are touched. The plan delivers a documentation
  correction and a regression check — no behavior change is needed because the
  bug WR-04 described is already fixed on `main`.
- Single atomic commit per the task brief's scope guard.
</success_criteria>

<output>
Create `.planning/quick/260526-d6s-fix-wr-04-contiguity-error-should-render/260526-d6s-SUMMARY.md` when done, documenting:
- Discovery that the WR-04 fix and integration test were already on main (commit 3598c22)
- The grep-gate verification that confirmed no regression
- The 05-REVIEW.md annotation that closed the loop for future audits
- Confirmation that scope guard held: WR-01/WR-02/WR-03/IN-01..IN-03 untouched
</output>
