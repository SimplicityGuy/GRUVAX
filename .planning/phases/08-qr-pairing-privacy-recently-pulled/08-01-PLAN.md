---
phase: 08-qr-pairing-privacy-recently-pulled
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_08_privacy.py
autonomous: true
requirements: [PRIV-02, PRIV-03]

must_haves:
  truths:
    - "Running a search through the ASGI client leaves the raw query term absent from app.state.log_ring_buffer"
    - "The uvicorn.access logger is suppressed to WARNING or higher so query-string URLs never reach stdout"
    - "No gruvax.search_log table exists (record stats stay aggregate-only)"
  artifacts:
    - path: "tests/integration/test_08_privacy.py"
      provides: "PRIV-02 + PRIV-03 CI assertions (query never in logs, uvicorn access suppressed, no search_log table)"
      contains: "test_query_never_in_logs"
      min_lines: 60
  key_links:
    - from: "tests/integration/test_08_privacy.py"
      to: "app.state.log_ring_buffer"
      via: "in-process ring-buffer assertion after a search request"
      pattern: "log_ring_buffer"
    - from: "tests/integration/test_08_privacy.py"
      to: "logging.getLogger('uvicorn.access')"
      via: "level >= WARNING regression guard"
      pattern: "uvicorn\\.access"
---

<objective>
Formalize and CI-lock the two privacy guarantees that are already de-facto true in the
codebase (per L-04): the server never logs raw query text (PRIV-02) and record
statistics are aggregate-only with no per-query log table (PRIV-03). This plan adds
tests only — no behavior change. It exists so a future refactor cannot silently
reintroduce query logging or a search-log table without a red test.

Purpose: Lock PRIV-02 / PRIV-03 against regression. Success criterion 4 of the phase
("`docker logs gruvax-api | grep <term>` returns zero hits after a search") becomes a
CI-runnable, in-process assertion against `app.state.log_ring_buffer`.

Output: `tests/integration/test_08_privacy.py` with three passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-qr-pairing-privacy-recently-pulled/08-CONTEXT.md
@.planning/phases/08-qr-pairing-privacy-recently-pulled/08-RESEARCH.md
@.planning/phases/08-qr-pairing-privacy-recently-pulled/08-PATTERNS.md

<interfaces>
<!-- Canonical analog: tests/integration/test_diagnostics.py — LifespanManager fixture
     yielding (ac, manager.app) and the app.state.log_ring_buffer assertion pattern.
     The executor MUST read it before writing the new test. -->

From src/gruvax/app.py — create_app() builds the FastAPI app; app.state.log_ring_buffer
is the in-process deque populated under LifespanManager (entries are dicts with keys
ts/level/logger/msg).

From src/gruvax/logging_config.py:188 — uvicorn.access is already pinned to WARNING.

From tests/integration/test_diagnostics.py — the diag_client fixture shape:
  app = create_app(); LifespanManager(app) as manager; AsyncClient(ASGITransport(manager.app));
  yield ac, manager.app. The new privacy_client fixture is identical MINUS the
  require_admin dependency_override (the /api/search endpoint is public).

From tests/conftest.py — session-scoped db_pool fixture (auto-loaded).
From tests/integration/conftest.py — module-scoped _seeded_profile_collection autouse fixture.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test_08_privacy.py with PRIV-02 + PRIV-03 assertions</name>
  <files>tests/integration/test_08_privacy.py</files>
  <read_first>
    - tests/integration/test_08_privacy.py (the file being created — confirm it does not yet exist)
    - tests/integration/test_diagnostics.py (canonical analog: diag_client fixture lines ~40-59, ring-buffer assertion lines ~256-295)
    - tests/conftest.py (db_pool fixture signature + scope)
    - tests/integration/conftest.py (autouse seeding fixture)
    - src/gruvax/logging_config.py (confirm uvicorn.access pinned to WARNING at line ~188; confirm log_ring_buffer wiring)
    - src/gruvax/api/search.py (confirm /api/search?q= signature and that q is never logged — PRIVACY comment near line 131)
  </read_first>
  <behavior>
    - test_query_never_in_logs: after `await ac.get("/api/search?q=probe_priv02_xyz&limit=5")`, every entry in `list(app.state.log_ring_buffer)` has the probe term absent from `entry.get("msg", "")`. Fails loudly with the offending entry if found.
    - test_uvicorn_access_log_suppressed: `logging.getLogger("uvicorn.access").level >= logging.WARNING` (regression guard for logging_config.py:188).
    - test_no_search_log_table: `SELECT to_regclass('gruvax.search_log')` returns NULL (no per-query log table — PRIV-03). Use the schema-aware regclass name matching this environment; the dev DB schema is `gruvax_dev` per project memory, so resolve the schema the same way the existing integration tests do rather than hardcoding `gruvax`.
  </behavior>
  <action>
    Create `tests/integration/test_08_privacy.py` mirroring the `test_diagnostics.py` fixture
    pattern. Define a module-scoped `privacy_client` fixture using `pytest_asyncio.fixture`:
    build `create_app()`, wrap in `LifespanManager`, expose `AsyncClient(ASGITransport(manager.app), base_url="http://test")`, and `yield ac, manager.app`. Do NOT install a
    `require_admin` dependency override — `/api/search` is public.

    Implement three tests, each decorated `@pytest.mark.asyncio(loop_scope="session")`:
    `test_query_never_in_logs`, `test_uvicorn_access_log_suppressed`, `test_no_search_log_table`.
    Use a unique probe term constant (`PROBE_TERM = "probe_priv02_xyz"`) so the assertion is
    unambiguous. For the table-existence test, take `db_pool` and run `to_regclass` against the
    schema this suite actually uses — confirm the schema-resolution approach against the
    existing integration conftest rather than assuming `gruvax`.

    No production code changes. This is a test-only, behavior-locking plan (L-04).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_08_privacy.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_08_privacy.py -x -q` exits 0 with 3 passed.
    - `test_query_never_in_logs` actually drives a search request (grep the file for `/api/search` and `log_ring_buffer`).
    - The probe term appears in the test as a single constant, asserted absent from every ring entry.
    - `test_uvicorn_access_log_suppressed` asserts `>= logging.WARNING` on `uvicorn.access`.
    - `test_no_search_log_table` asserts the regclass lookup returns None.
    - No file under `src/gruvax/` is modified by this plan (git diff shows only the new test file).
  </acceptance_criteria>
  <done>All three privacy tests pass; no production code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/search | Untrusted query text (`q`) crosses into the backend; must never be persisted or logged |
| backend → stdout/journald | Log output is the disclosure surface; query text must not reach it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | structlog processor chain (`q` field) | mitigate | `search.py` logs `release_id` only; `test_query_never_in_logs` asserts probe term absent from `app.state.log_ring_buffer` |
| T-08-02 | Information Disclosure | uvicorn.access query-string URLs | mitigate | `logging_config.py:188` pins uvicorn.access to WARNING; `test_uvicorn_access_log_suppressed` is the regression guard |
| T-08-03 | Information Disclosure | persisted per-query history | mitigate | PRIV-03 — `test_no_search_log_table` asserts no `search_log` table; stats stay aggregate-only |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_08_privacy.py -x -q` → 3 passed.
- `git diff --name-only` shows only `tests/integration/test_08_privacy.py`.
- Full suite still green: `uv run pytest -q`.
</verification>

<success_criteria>
- PRIV-02 and PRIV-03 are CI-locked: any future change that logs `q`, re-enables
  uvicorn.access, or adds a `search_log` table turns a test red.
- Phase success criterion 4 ("zero grep hits after a search") is satisfied in-process.
</success_criteria>

<output>
Create `.planning/phases/08-qr-pairing-privacy-recently-pulled/08-01-SUMMARY.md` when done.
</output>
