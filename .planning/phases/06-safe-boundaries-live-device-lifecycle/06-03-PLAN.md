---
phase: 06-safe-boundaries-live-device-lifecycle
plan: 03
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/integration/test_two_profile_isolation.py
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "A boundary edit bound to profile A does not modify profile B's row at the same (unit_id,row,col)"
    - "An admin boundary write with no browse-binding cookie returns 400 session_unbound (no default-profile fallback)"
    - "A scoped write to a position absent for the bound profile returns 404 boundary_not_found"
    - "boundary_changed fans out only to the bound profile's SSE channel, not to other profiles"
    - "admin_editing fans out only to the bound profile's SSE channel, not to other profiles (D-04 shimmer isolation)"
  artifacts:
    - path: "tests/integration/test_two_profile_isolation.py"
      provides: "two-profile boundary-isolation + unbound-admin 400 + 0-row 404 + per-profile boundary_changed AND admin_editing fan-out tests"
      contains: "test_boundary_edit_profile_a_does_not_touch_profile_b"
  key_links:
    - from: "test (admin write bound to A)"
      to: "gruvax.cube_boundaries profile B row"
      via: "PUT /api/admin/cubes/... then SELECT B's row unchanged"
      pattern: "profile_id"
    - from: "test (boundary_changed)"
      to: "profile A SSE channel only"
      via: "two EventSource streams; B receives nothing"
      pattern: "boundary_changed"
    - from: "test (admin_editing)"
      to: "profile A SSE channel only"
      via: "POST /api/admin/editing bound to A; B's stream receives no admin_editing"
      pattern: "admin_editing"
---

<objective>
Deliver success-criterion #3's MANDATORY two-profile integration test plus the
companion isolation assertions for DATA-01. This test is the executable proof that
Plan 01's profile scoping closed the corruption hole.

Purpose: prove a boundary edit on profile A cannot modify profile B's cube at the
same physical position, that an unbound admin write fails loud (D-02), that a 0-row
write returns 404 (D-10), and that boundary_changed fan-out is per-profile (D-04).
Output: tests/integration/test_two_profile_isolation.py, exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-safe-boundaries-live-device-lifecycle/06-CONTEXT.md
@.planning/phases/06-safe-boundaries-live-device-lifecycle/06-01-PLAN.md

<interfaces>
<!-- Existing test patterns to reuse — do NOT invent new harness. -->

From tests/integration/test_sse_per_profile.py:
- `live_server` fixture (module-scoped, real uvicorn in a thread) — copy it or import the pattern.
- BROWSE_BINDING_COOKIE = "gruvax_browse_binding"; default profile UUID
  "00000000-0000-0000-0000-000000000001"; tests scope a request by sending
  cookies={BROWSE_BINDING_COOKIE: profile_id}.
- `@pytest.mark.asyncio(loop_scope="session")`; httpx.AsyncClient over base_url.
- test_no_cross_profile_leakage shows the two-EventSource fan-out assertion shape.

From tests/integration/test_editing.py:79-120:
- login helper: POST the test PIN → returns {"cookies": dict, "csrf_token": str};
  admin writes send those cookies + the X-CSRF-Token header.

From tests/integration/conftest.py:145-205:
- _seed_cube_boundaries inserts into gruvax.cube_boundaries with composite PK
  (profile_id, unit_id, row, col); INSERT ... ON CONFLICT DO UPDATE. Default profile
  rows are already seeded. A SECOND profile + its boundary rows must be created in-test.

From tests/integration/test_migrate_0009.py:233-245:
- INSERT INTO gruvax.profiles (display_name, app_token_encrypted, app_token_revoked)
  VALUES (..., %s::bytea, TRUE) RETURNING id — the way to create profile B; clean it up after.

From src/gruvax/app.py:178-265 + 06-01-PLAN interfaces:
- event_bus_registry is keyed str(profile_id). A newly INSERTed profile B has NO bus
  until app startup re-runs. For the fan-out test, bind both EventSource streams to
  profiles that EXIST at server start; create profile B and its boundary rows BEFORE
  the live_server fixture builds the app, OR assert isolation against the default
  profile vs a profile created and registered at startup. Simplest: seed profile B +
  its boundary row in a session-scoped fixture that runs before live_server's create_app().

Endpoint: PUT /api/admin/cubes/{unit}/{row}/{col} body {is_empty, first_label, first_catalog}
(cubes.py:256 put_cube_boundary) — the per-cube write under test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-profile isolation + unbound-admin 400 + 0-row 404 tests (DATA-01 write scoping)</name>
  <files>tests/integration/test_two_profile_isolation.py</files>
  <read_first>
    - tests/integration/test_editing.py:79-185 (login helper + admin write with cookies + CSRF)
    - tests/integration/conftest.py:145-245 (seed pattern for cube_boundaries; composite PK)
    - tests/integration/test_migrate_0009.py:233-245 (INSERT profile + RETURNING id + cleanup)
    - tests/integration/test_sse_per_profile.py:40-90 (live_server fixture + BROWSE_BINDING_COOKIE)
    - src/gruvax/api/admin/cubes.py:256-378 (PUT /api/admin/cubes/{u}/{r}/{c} request/response shape)
  </read_first>
  <behavior>
    - Seed profile B and a boundary row for B at the SAME (unit_id,row,col) as a default-profile row.
    - PUT bound to the default profile updates only the default row; B's first_label/first_catalog/is_empty unchanged.
    - PUT with NO browse-binding cookie (and no fingerprint) returns 400 session_unbound.
    - PUT bound to profile B for a (unit,row,col) absent for B returns 404 with detail type boundary_not_found.
  </behavior>
  <action>
    Create tests/integration/test_two_profile_isolation.py. Reuse the `live_server` fixture and
    login helper from test_sse_per_profile.py / test_editing.py (copy or import). Add a
    fixture that INSERTs profile B (display_name e.g. 'IsolationTestB', app_token_revoked TRUE,
    app_token_encrypted b'') RETURNING id, and INSERTs a cube_boundaries row for profile B at a
    chosen (unit_id,row,col) that also exists for the default profile, with a sentinel
    first_label (e.g. 'B-SENTINEL'); register a teardown DELETEing B's rows + profile. Write
    `test_boundary_edit_profile_a_does_not_touch_profile_b`: log in, PUT
    /api/admin/cubes/{u}/{r}/{c} with cookies including {BROWSE_BINDING_COOKIE: DEFAULT_PROFILE_UUID}
    setting a new first_label for the default profile; assert 200; then SELECT B's row directly
    and assert first_label still equals 'B-SENTINEL' (unchanged). Write
    `test_unbound_admin_write_returns_400`: PUT with admin auth cookies but NO browse-binding
    cookie and no fingerprint; assert status 400 and detail type 'session_unbound'. Write
    `test_zero_row_write_returns_404`: bind to profile B and PUT a (unit,row,col) that has no B
    row; assert 404 and detail type 'boundary_not_found'. All SQL via the db_pool fixture; no
    f-string SQL. No fenced code in this action.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && uv run pytest tests/integration/test_two_profile_isolation.py -x -q && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - test_two_profile_isolation.py exists with test_boundary_edit_profile_a_does_not_touch_profile_b asserting B's sentinel row is unchanged after an A-scoped write.
    - test_unbound_admin_write_returns_400 asserts 400 + detail type session_unbound.
    - test_zero_row_write_returns_404 asserts 404 + detail type boundary_not_found.
    - `uv run pytest tests/integration/test_two_profile_isolation.py -x` exits 0.
  </acceptance_criteria>
  <done>The mandatory two-profile isolation test passes alongside the unbound-400 and 0-row-404 assertions; profile B is created and cleaned up in-test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-profile boundary_changed fan-out isolation test (DATA-01 SSE scoping)</name>
  <files>tests/integration/test_two_profile_isolation.py</files>
  <read_first>
    - tests/integration/test_sse_per_profile.py:167-230 (test_no_cross_profile_leakage — two-EventSource fan-out assertion)
    - tests/integration/test_editing.py:140-185 (test_editing_fans_out — connect SSE, trigger admin write, read the boundary_changed frame)
    - src/gruvax/api/admin/cubes.py:367-373 (boundary_changed publish payload shape: cube_ids + change_set_id)
  </read_first>
  <behavior>
    - The "other profile" channel GET returns 200 (its bus exists in event_bus_registry at server start) before the silence assertion is trusted.
    - With one client streaming the writing profile's SSE channel and another streaming a different existing profile's channel, an admin boundary write bound to the writing profile delivers boundary_changed on the writing channel only; the other channel receives no boundary_changed.
  </behavior>
  <action>
    Add `test_boundary_changed_fans_out_per_profile` to test_two_profile_isolation.py. MANDATORY
    registry constraint (WARNING-2): the "other profile" streamed for the negative assertion MUST
    have a bus in event_bus_registry at server start — otherwise GET /api/events/{other} returns
    404 profile_not_found and the negative assertion passes vacuously. Satisfy this by EITHER (a)
    making profile B's INSERT happen in a fixture scoped BEFORE the live_server fixture so
    create_app()'s startup loop (app.py:191-265) builds B's bus, OR (b) streaming two profiles
    that both exist in the seed set at startup and writing to one of them. Add an explicit assert
    that the other-channel GET returns 200 (not 404) before relying on its silence, so a missing-
    bus regression fails loudly rather than silently. Open two httpx streaming GETs:
    /api/events/{writing_profile} (cookie bound to it) and /api/events/{other_profile} (cookie
    bound to it). Drain initial server_hello frames. Then log in and PUT a boundary change bound to
    the writing profile. Assert the writing-channel stream yields a `boundary_changed` event within
    a short timeout, and the other-channel stream yields NO boundary_changed in the same window
    (mirror test_no_cross_profile_leakage's timeout-drain assertion). No f-string SQL. No fenced
    code in this action.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && uv run pytest tests/integration/test_two_profile_isolation.py::test_boundary_changed_fans_out_per_profile -x -q && uv run ruff check tests/integration/test_two_profile_isolation.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts the other-profile channel GET returns 200 (bus exists at server start) before relying on its silence.
    - test_boundary_changed_fans_out_per_profile asserts boundary_changed arrives on the writing profile's channel and NOT on the other profile's channel.
    - `uv run ruff check tests/integration/test_two_profile_isolation.py` exits 0 (Ruff hard-match lint, CLAUDE.md).
    - `uv run pytest tests/integration/test_two_profile_isolation.py -x` (full file) exits 0.
  </acceptance_criteria>
  <done>The per-profile boundary_changed fan-out isolation test proves boundary_changed is scoped to the affected profile's bus (D-04, success criterion #4); the other channel is verified registered (200) so the negative assertion is meaningful.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Per-profile admin_editing fan-out isolation test (D-04 shimmer isolation)</name>
  <files>tests/integration/test_two_profile_isolation.py</files>
  <read_first>
    - tests/integration/test_editing.py:140-190 (test_editing_fans_out — opens SSE on a profile channel, POSTs /api/admin/editing with admin cookies + X-CSRF-Token, reads the admin_editing frame)
    - src/gruvax/api/admin/editing.py:60-78 (signal_editing — POST /api/admin/editing body {cube_ids, editing}; publishes admin_editing; scoped to per-profile bus by Plan 01 Task 2)
    - tests/integration/test_sse_per_profile.py:167-230 (two-EventSource timeout-drain negative-assertion pattern)
  </read_first>
  <behavior>
    - The "other profile" channel GET returns 200 (its bus exists at server start) before the silence assertion is trusted.
    - An admin POST /api/admin/editing with browse-binding bound to the writing profile delivers admin_editing on the writing channel only; the other existing-profile channel receives no admin_editing in the same window.
  </behavior>
  <action>
    Add `test_admin_editing_fans_out_per_profile` to test_two_profile_isolation.py, mirroring
    test_editing_fans_out but with TWO SSE readers (writing profile + a different profile that
    exists in event_bus_registry at server start — same WARNING-2 constraint as Task 2: use a
    seed-set profile or a profile B inserted before live_server starts; assert its channel GET is
    200 before trusting silence). Log in (the _login helper); open both streams; drain server_hello;
    POST /api/admin/editing with body {"cube_ids": [{"unit":1,"row":0,"col":0}], "editing": true},
    admin cookies, X-CSRF-Token, AND a browse-binding cookie set to the writing profile (so
    get_write_target resolves that profile). Assert the writing channel yields an `admin_editing`
    frame within a short timeout, and the other channel yields NO admin_editing in the same window
    (timeout-drain). No f-string SQL. No fenced code in this action.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_two_profile_isolation.py::test_admin_editing_fans_out_per_profile -x -q && uv run ruff check tests/integration/test_two_profile_isolation.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - test_admin_editing_fans_out_per_profile asserts the other-profile channel GET returns 200 before relying on its silence.
    - It asserts admin_editing arrives on the writing profile's channel and NOT on the other profile's channel.
    - `uv run ruff check tests/integration/test_two_profile_isolation.py` exits 0.
    - `uv run pytest tests/integration/test_two_profile_isolation.py -x` (full file, all 5 tests) exits 0.
  </acceptance_criteria>
  <done>The admin_editing fan-out isolation test proves the shimmer event is scoped to the affected profile's bus (D-04, second half), closing the gap that boundary_changed-only testing left open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test admin write (profile A) → profile B rows | the isolation boundary under test |
| test SSE stream (profile A) ↔ stream (profile B) | the fan-out isolation boundary under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | cross-profile boundary write | mitigate | This plan's test is the executable verification of T-06-01 (Plan 01) — an A-scoped write must leave B's sentinel row unchanged. |
| T-06-09 | Information Disclosure | cross-profile SSE leakage | mitigate | Fan-out test verifies T-06-03 — boundary_changed reaches only the writing profile's channel. |
| T-06-09b | Information Disclosure | cross-profile admin_editing (shimmer) leakage | mitigate | admin_editing fan-out test verifies the second half of T-06-03/D-04 — a shimmer from an admin editing profile A reaches only A's channel, never profile B. |
| T-06-10 | Repudiation | shared dev DB state contaminating other tests | accept | Profile B + its rows are created and torn down in-fixture; reuses the gruvax-dev-pg shared-DB pattern (MEMORY: integration harness uses shared dev Postgres, suite is order-independent). |
| T-06-SC | Tampering | npm/pip installs | accept | No new dependencies — uses existing pytest/httpx/uvicorn harness. |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_two_profile_isolation.py -x` exits 0 (all five tests: isolation, unbound-400, 0-row-404, boundary_changed fan-out, admin_editing fan-out).
- Run AFTER the full sequential suite to confirm no order dependence (MEMORY: shared dev DB; reseed via just seed-dev / synth_collection.sql if a prior migration-roundtrip test cleared cube_boundaries).
- These tests are RED until Plan 01 lands (depends_on 06-01) and GREEN after.
</verification>

<success_criteria>
- Two-profile boundary-isolation test passes (success criterion #3 — mandatory deliverable).
- Unbound-admin 400 and 0-row 404 tests pass (D-02, D-10).
- Per-profile boundary_changed AND admin_editing fan-out tests pass (success criterion #4, D-04 — both events scoped).
- Profile B fixtures create + clean up; suite remains order-independent.
</success_criteria>

<output>
Create `.planning/phases/06-safe-boundaries-live-device-lifecycle/06-03-SUMMARY.md` when done
</output>
