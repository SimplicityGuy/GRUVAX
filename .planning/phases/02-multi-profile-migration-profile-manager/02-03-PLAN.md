---
phase: 02-multi-profile-migration-profile-manager
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/gruvax/api/events.py
  - src/gruvax/api/search.py
  - src/gruvax/api/locate.py
  - src/gruvax/api/illuminate.py
files_modified_test:
  - tests/integration/test_sse_per_profile.py
  - tests/integration/test_sse.py
  - tests/integration/test_search_benchmark.py
autonomous: true
requirements: [API-02, SYN-02]
must_haves:
  truths:
    - "GET /api/events/{profile_id} validates the path profile_id against the session cookie and returns 403 on mismatch, 400 when unbound, and streams only that profile's bus events (SC#4, D2-04)."
    - "collection_changed and boundary_changed reach ONLY the affected profile's SSE clients — cross-profile leakage is impossible (SC#4)."
    - "search / locate / illuminate carry profile_id, validate it against the session, and route to the correct per-profile cache + scope their queries by profile_id (D2-04, API-02)."
    - "p95 /api/search ≤ 200 ms and /api/locate ≤ 50 ms hold with 2+ profiles cached (SC#5, API-02)."
  artifacts:
    - path: "src/gruvax/api/events.py"
      provides: "Per-profile SSE endpoint GET /api/events/{profile_id}"
      contains: "events/{profile_id}"
    - path: "src/gruvax/api/search.py"
      provides: "Profile-scoped search routing"
      contains: "profile_id"
  key_links:
    - from: "src/gruvax/api/events.py"
      to: "get_bus_for_profile"
      via: "Depends(get_bus_for_profile) — no get_pool (Pitfall 10)"
      pattern: "get_bus_for_profile"
    - from: "src/gruvax/api/search.py"
      to: "search_collection(pool, q, limit, profile_id)"
      via: "session-validated profile_id passed through"
      pattern: "profile_id"
---

<objective>
Convert the kiosk read path to per-profile routing. `GET /api/events` becomes `GET /api/events/{profile_id}` backed by the per-profile EventBus registry, validating the path `profile_id` against the browse-binding session cookie (403 mismatch / 400 unbound, D2-04). Wire `profile_id` through `/api/search`, `/api/locate`, and `/api/illuminate` so each resolves the correct per-profile cache instance and scopes its DB queries by `profile_id`. Re-parameterize the SLO benchmark over `profile_id` so the p95 gate proves search/locate SLOs hold with 2+ profiles cached (SC#5).

Purpose: SC#4 (per-profile SSE isolation, no cross-leakage) + API-02 (positioning/search/locate off the per-profile cache, SLOs preserved) + the kiosk-view-per-profile half of SYN-02.
Output: Modified `events.py`, `search.py`, `locate.py`, `illuminate.py`; the per-profile SSE tests + the updated SSE test + the re-parameterized benchmark GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-multi-profile-migration-profile-manager/02-RESEARCH.md
@.planning/phases/02-multi-profile-migration-profile-manager/02-PATTERNS.md
@src/gruvax/api/events.py
@src/gruvax/api/search.py
@src/gruvax/api/locate.py

<interfaces>
<!-- Verified by codebase read + provided by Plan 02-02 (same wave dependency root). -->

From Plan 02-02 (src/gruvax/api/deps.py) — resolution deps:
  get_boundary_cache_for_profile(profile_id, request) -> BoundaryCache
  get_snapshot_for_profile(profile_id, request) -> CollectionSnapshot
  get_segment_cache_for_profile(profile_id, request) -> SegmentCache
  get_bus_for_profile(profile_id, request) -> EventBus
  (each: 400 unbound / 403 mismatch / 404 missing / 503 not-ready; reads gruvax_browse_binding cookie)

Current endpoint signatures (verified):
  events.py:  @router.get("/events")  stream_events(request, bus=Depends(get_event_bus))  — generator subscribe/yield/unsubscribe; EventSourceResponse(ping=15, X-Accel-Buffering:no, Cache-Control:no-store)
  search.py:  @router.get("/search")  search(request, q, limit, pool=Depends(get_pool)) -> calls search_collection(pool, q, limit)
  locate.py:  @router.get("/locate")  locate_endpoint(request, release_id, pool, segment_cache=Depends(get_segment_cache), snapshot=Depends(get_collection_snapshot)) -> calls get_release_for_locate(pool, release_id) then locate(..., segment_cache, snapshot)
  illuminate.py: @router.post("/illuminate") illuminate(...)

queries.py functions ALREADY accept profile_id (verified): search_collection(pool, q, limit, profile_id=DEFAULT), get_release_for_locate(pool, release_id, profile_id=DEFAULT), did_you_mean_query(..., profile_id=DEFAULT). P2's job is to PASS the session-validated profile_id instead of relying on the default.

Pitfall 10 (HARD): events.py depends ONLY on the bus dep — NEVER get_pool. get_bus_for_profile already does the 400/403 cookie validation itself and reads only app.state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-profile SSE endpoint + update legacy SSE test</name>
  <files>src/gruvax/api/events.py, tests/integration/test_sse.py</files>
  <behavior>
    - GET /api/events/{profile_id} resolves the bus via get_bus_for_profile (which 400s unbound, 403s mismatch); the generator body is unchanged (subscribe/yield/unsubscribe, ping=15, no-store headers).
    - A client bound to profile A subscribing to /api/events/{A} receives A's collection_changed/boundary_changed; a client on B never sees A's events.
    - The legacy test_sse.py is updated to hit /api/events/{profile_id} with a bound cookie (the unparameterized /api/events route no longer exists).
  </behavior>
  <read_first>
    - src/gruvax/api/events.py (full current body — copy the generator verbatim, change only the route + dep)
    - .planning/phases/02-multi-profile-migration-profile-manager/02-PATTERNS.md §src/gruvax/api/events.py (the per-profile route skeleton) + §Shared Patterns "SSE: no get_pool"
    - tests/integration/test_sse.py (current live-server consumer — must be updated to the new URL + bound cookie)
    - tests/integration/test_sse_per_profile.py (Wave 0 RED tests this turns green)
    - .planning/phases/02-multi-profile-migration-profile-manager/02-RESEARCH.md §Code Examples (verified per-profile SSE) + Pitfall 10
  </read_first>
  <action>
    Change the route decorator to `@router.get("/events/{profile_id}")` and the signature to `stream_events(profile_id: str, request: Request, bus: EventBus = Depends(get_bus_for_profile))`. Keep the generator body EXACTLY as-is (subscribe → yield connected comment → loop is_disconnected/wait_for/yield → finally unsubscribe) and the EventSourceResponse(ping=15, headers X-Accel-Buffering:no + Cache-Control:no-store). Do NOT add get_pool (Pitfall 10). Remove the old unparameterized `/events` route.

    Update tests/integration/test_sse.py: set the browse-binding cookie to the default profile UUID before connecting, and GET `/api/events/{default_profile_uuid}`. This replaces the RESEARCH "Open Question 3" shim — update the test in the same plan, no backward-compat alias.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_sse_per_profile.py tests/integration/test_sse.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "events/{profile_id}" src/gruvax/api/events.py` is 1; `grep -c '"/events"' src/gruvax/api/events.py` is 0.
    - `grep -v '^#' src/gruvax/api/events.py | grep -c "get_pool"` is 0 (Pitfall 10).
    - test_sse_per_profile.py is GREEN: 403 on profile mismatch, 400 on unbound, connects when bound, and test_no_cross_profile_leakage passes.
    - test_sse.py (updated) is GREEN.
  </acceptance_criteria>
  <done>The SSE endpoint is per-profile with session validation and no cross-profile leakage; both SSE test files are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Profile-scoped search / locate / illuminate routing + re-parameterized SLO gate</name>
  <files>src/gruvax/api/search.py, src/gruvax/api/locate.py, src/gruvax/api/illuminate.py, tests/integration/test_search_benchmark.py</files>
  <behavior>
    - search / locate / illuminate accept profile_id, validate it against the session cookie (403 mismatch, 400 unbound), resolve the per-profile cache instance, and pass profile_id into the queries layer.
    - The SLO benchmark seeds a second profile (second_profile fixture) and asserts p95 /api/search ≤ 200 ms and /api/locate ≤ 50 ms with 2+ profiles cached in the registry.
  </behavior>
  <read_first>
    - src/gruvax/api/search.py + locate.py + illuminate.py (full current bodies — keep slow-query-ring + background-task increment logic verbatim)
    - src/gruvax/db/queries.py (search_collection / get_release_for_locate / did_you_mean_query signatures — they already take profile_id)
    - tests/integration/test_search_benchmark.py (current p95 gate — add the second-profile dimension)
    - tests/conftest.py (the second_profile fixture from Plan 02-00)
    - .planning/phases/02-multi-profile-migration-profile-manager/02-RESEARCH.md §State of the Art (DEFAULT_PROFILE_UUID removed from call sites) + §Validation Architecture (SC-5 rows)
  </read_first>
  <action>
    Decide the profile_id source per D2-04: take it from the validated session (browse-binding cookie). For search/locate, accept `profile_id` as a path or query param consistent with the SSE URL style and validate path/query == cookie via the per-profile resolution deps (reuse get_segment_cache_for_profile / get_snapshot_for_profile / get_boundary_cache_for_profile so the 400/403 logic is centralized — the dep returns the right per-profile cache AND enforces validation in one shot). Pass the validated `profile_id` into `search_collection(pool, q, limit, profile_id)`, `get_release_for_locate(pool, release_id, profile_id)`, and `did_you_mean_query(..., profile_id)`. For locate, replace the `Depends(get_segment_cache)`/`Depends(get_collection_snapshot)` with the per-profile resolution deps so the estimator runs off the correct profile's caches. For illuminate, scope its boundary/segment lookup to the validated profile_id likewise. Keep the slow-query-ring recording and fire-and-forget increment logic verbatim. Remove reliance on the `DEFAULT_PROFILE_UUID` default at these call sites (the value now comes from the session).

    Update tests/integration/test_search_benchmark.py to seed a second profile (use the `second_profile` fixture + the synth_profile_collection seed for it) so 2+ profiles are cached in the registry, then assert p95 /api/search ≤ 200 ms and /api/locate ≤ 50 ms for a profile-scoped query. Bind the browse cookie to the profile under test in the benchmark client.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_search.py tests/integration/test_locate.py -x -q && just slo</automated>
  </verify>
  <acceptance_criteria>
    - search/locate/illuminate each pass a session-validated profile_id into their queries (grep `profile_id` present in each file).
    - `just slo` exits 0 with p95 /api/search ≤ 200 ms and /api/locate ≤ 50 ms while a second profile is cached.
    - A search/locate request whose path/query profile_id != the browse cookie returns 403 (covered by an assertion in test_search.py or test_locate.py).
    - Existing test_search.py / test_locate.py pass (regression, default profile bound).
  </acceptance_criteria>
  <done>search/locate/illuminate route per-profile with session validation; the SLO benchmark passes with 2+ profiles cached.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk browser → /api/events/{profile_id}, /api/search, /api/locate | Untrusted path/query profile_id crosses here; validated against the browse-binding cookie, never authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | SSE delivering another profile's events | mitigate | Per-profile EventBus registry resolved by validated profile_id; get_bus_for_profile 403s on mismatch; test_no_cross_profile_leakage asserts isolation (SC#4). |
| T-02-03-02 | Spoofing | profile_id in search/locate/illuminate path/query | mitigate | Per-profile resolution deps validate path profile_id == browse cookie before returning the cache (403/400). |
| T-02-03-03 | Information Disclosure | search/locate reading the default profile's collection regardless of binding | mitigate | profile_id from the session is passed into every query; DEFAULT_PROFILE_UUID default no longer relied on at call sites. |
| T-02-03-04 | Elevation of Privilege | SSE endpoint coupling to the DB pool | mitigate | events.py depends only on get_bus_for_profile (Pitfall 10); grep gate asserts zero get_pool. |
| T-02-03-SC | Tampering | npm/pip installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: none new). |

No HIGH-severity unmitigated threats.
</threat_model>

<verification>
- `uv run pytest tests/integration/test_sse_per_profile.py tests/integration/test_sse.py tests/integration/test_search.py tests/integration/test_locate.py` all GREEN.
- `just slo` exits 0 (p95 SLOs hold with 2+ profiles).
- `grep -v '^#' src/gruvax/api/events.py | grep -c get_pool` is 0.
</verification>

<success_criteria>
Kiosk read path (SSE + search + locate + illuminate) routes per-profile with session validation; no cross-profile event leakage; p95 SLOs preserved with 2+ profiles cached (SC#4, SC#5, API-02, SYN-02 kiosk-view half).
</success_criteria>

<output>
Create `.planning/phases/02-multi-profile-migration-profile-manager/02-03-SUMMARY.md` when done. Record the final profile_id transport choice (path vs query) for search/locate so the frontend (Plan 02-06) wires the matching URL shape.
</output>
