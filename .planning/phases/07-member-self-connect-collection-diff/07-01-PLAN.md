---
phase: 07-member-self-connect-collection-diff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/0012_invite_codes_and_first_seen_at.py
  - src/gruvax/sync/profile_sync.py
  - src/gruvax/api/admin/profiles.py
  - src/gruvax/api/admin/diagnostics.py
  - tests/integration/test_invite_codes.py
  - tests/unit/test_profile_sync_diff.py
  - tests/unit/test_fake_discogsography.py
autonomous: true
requirements: [API-04]
user_setup: []

must_haves:
  truths:
    - "After any sync, the collection_changed SSE payload includes new_record_count (int >= 0) and is_initial_import (bool)."
    - "The first-ever sync for a profile reports is_initial_import=true; subsequent syncs report false."
    - "new_record_count equals the number of genuinely new releases this sync (arrivals only, never negative)."
    - "The admin profile/diagnostics API exposes last_new_record_count and last_sync_is_initial, persisted across requests until the next sync."
    - "The admin profiles list/get response exposes a derived has_token boolean and never the raw or encrypted token."
  artifacts:
    - path: "migrations/versions/0012_invite_codes_and_first_seen_at.py"
      provides: "profile_invite_codes table + first_seen_at column + profiles diff columns"
      contains: "profile_invite_codes"
    - path: "src/gruvax/sync/profile_sync.py"
      provides: "first_seen_at on INSERT + new_record_count/is_initial_import computed in swap + extended SSE payload"
      contains: "new_record_count"
    - path: "src/gruvax/api/admin/profiles.py"
      provides: "has_token derivation + last_new_record_count/last_sync_is_initial in list/get response"
      contains: "has_token"
    - path: "src/gruvax/api/admin/diagnostics.py"
      provides: "last_new_record_count + last_sync_is_initial in per-profile diagnostics"
      contains: "last_new_record_count"
    - path: "tests/integration/test_invite_codes.py"
      provides: "Wave-0 integration test scaffold for AUTH-02 + API-04"
      contains: "def test_arrival_count_accuracy"
  key_links:
    - from: "src/gruvax/sync/profile_sync.py::_swap_inside_tx"
      to: "src/gruvax/sync/profile_sync.py::_refresh_profile_caches"
      via: "(new_record_count, is_initial_import) threaded through sync_profile"
      pattern: "new_record_count, is_initial_import"
    - from: "src/gruvax/sync/profile_sync.py::_refresh_profile_caches"
      to: "EventBus.publish collection_changed"
      via: "extended payload dict"
      pattern: "collection_changed"
---

<objective>
Deliver the API-04 backend: migration 0012 (the schema for BOTH this phase's features), the
collection-diff computation inside the staging-swap sync, the extended `collection_changed` SSE
payload, the persisted `last_new_record_count` / `last_sync_is_initial` profile state, the derived
`has_token` boolean on the admin profiles API, and the Wave-0 test scaffolds that Plans 02 and 03
verify against.

Purpose: API-04 requires a per-sync "N new records" count computed atomically inside the existing
staging swap and surfaced on SSE + the admin API. This plan also lands migration 0012 first so the
`profile_invite_codes` table exists before Plan 02 (AUTH-02 invite endpoints) builds on it.

Output: migration 0012, extended `profile_sync.py`, extended admin `profiles.py` + `diagnostics.py`,
and three test files (one integration scaffold, two unit files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-member-self-connect-collection-diff/07-CONTEXT.md
@.planning/phases/07-member-self-connect-collection-diff/07-RESEARCH.md
@.planning/phases/07-member-self-connect-collection-diff/07-PATTERNS.md

<interfaces>
<!-- Contracts extracted from the live codebase. Use directly — no exploration needed. -->

profile_sync.py current swap signature (src/gruvax/sync/profile_sync.py:281-285):
  async def _swap_inside_tx(conn, profile_id: str, row_count: int, user_id: str) -> None
  Body order: DELETE old rows (294) → INSERT from staging (299) → UPDATE profiles (307).
  Caller (sync_profile:505-506): user_id, row_count = await _ingest_into_staging(...); await _swap_inside_tx(conn, profile_id, row_count, user_id)
  Refresh caller (sync_profile:516): await _refresh_profile_caches(profile_id, app_state)

_refresh_profile_caches current publish (src/gruvax/sync/profile_sync.py:354-356):
  await bus.publish("collection_changed", {"profile_id": profile_id})
  EventBus.publish signature (src/gruvax/events/bus.py:63): async def publish(self, name: str, data: dict[str, Any]) -> None

profiles.py list SELECT (src/gruvax/api/admin/profiles.py:194-197):
  "SELECT id, display_name, last_sync_at, last_sync_status, last_sync_item_count, app_token_revoked, last_sync_error FROM gruvax.profiles WHERE deleted_at IS NULL ORDER BY created_at"
  get_profile uses the same column list (247). Both deliberately exclude app_token_encrypted.

diagnostics.py per-profile SELECT (src/gruvax/api/admin/diagnostics.py:106-108):
  "SELECT id::text, display_name, last_sync_at, last_sync_status, last_sync_item_count, last_sync_error, app_token_revoked FROM gruvax.profiles WHERE deleted_at IS NULL ORDER BY created_at"
  Serialized into profile_diagnostics dicts (112-123).

migration 0011 conventions (migrations/versions/0011_devices_and_pairing_codes.py):
  revision/down_revision module constants; ALL SQL as module-level string constants; op.execute(_CONST); no f-strings (bandit B608); downgrade() fully reverses upgrade().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave-0 test scaffolds for AUTH-02 + API-04</name>
  <files>tests/integration/test_invite_codes.py, tests/unit/test_profile_sync_diff.py, tests/unit/test_fake_discogsography.py</files>
  <read_first>
    - tests/integration/test_devices.py (analog — module-scoped ASGI client fixture, login helper, rate-limit reset autouse fixture)
    - .planning/phases/07-member-self-connect-collection-diff/07-RESEARCH.md (§Validation Architecture — the 14-row Phase Requirements → Test Map; copy every test name verbatim)
    - .planning/phases/07-member-self-connect-collection-diff/07-PATTERNS.md (tests/integration/test_invite_codes.py section — fixture + test-name list)
    - src/gruvax/_internal/fake_discogsography.py (confirm the limit=1 path at the get_collection handler)
  </read_first>
  <action>
    Create three test files following the test_devices.py fixture pattern (module-scoped ASGI client via LifespanManager + ASGITransport, `_login` helper posting /api/admin/login, and an autouse `reset_rate_limits` fixture calling `limiter.reset()`).

    In tests/integration/test_invite_codes.py define one async test function per row of the RESEARCH §Validation Architecture map, using EXACTLY these names: test_generate_invite, test_new_invite_voids_prior, test_get_valid_code, test_redeem_success, test_redeem_second_use_rejected, test_redeem_bad_pat, test_redeem_expired, test_profile_has_token_field, test_redeem_rotates_token, test_initial_import_flag, test_arrival_count_accuracy, test_profile_new_record_fields. For tests whose endpoints do not exist yet (Plan 02 ships invite_codes.py), the body may assert the endpoint shape it WILL satisfy; mark those still-unimplemented with `pytest.mark.xfail(reason="endpoint in Plan 02", strict=False)` so the suite stays green while this plan lands only the API-04 pieces. The API-04 tests that this plan CAN satisfy (test_initial_import_flag, test_arrival_count_accuracy, test_profile_new_record_fields, test_profile_has_token_field) must NOT be xfail — they must pass after this plan.

    In tests/unit/test_profile_sync_diff.py write unit tests for the diff computation: test_collection_changed_payload (assert the published collection_changed payload dict contains keys profile_id, new_record_count, is_initial_import), plus a direct test of the arrival-count arithmetic (new_record_count = max(0, row_count - existing_count)) and is_initial_import detection (read last_sync_at IS NULL BEFORE the UPDATE).

    In tests/unit/test_fake_discogsography.py add test_limit_one asserting GET /api/user/collection with limit=1 returns a single-release page without error (it likely already passes — keep it as a regression guard).

    Do not place fenced code in production files; these are test files.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && uv run pytest tests/unit/test_fake_discogsography.py::test_limit_one tests/unit/test_profile_sync_diff.py -q --tb=short --benchmark-skip 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_invite_codes.py` exists and defines all 12 named test functions listed above (grep each name).
    - `tests/unit/test_profile_sync_diff.py` defines `test_collection_changed_payload` and an arrival-count arithmetic test.
    - `tests/unit/test_fake_discogsography.py::test_limit_one` passes.
    - Collecting the integration file does not error: `uv run pytest tests/integration/test_invite_codes.py --collect-only -q` lists ≥12 tests.
    - Tests targeting Plan-02 endpoints are marked `xfail(strict=False)`; API-04 tests are NOT xfail.
  </acceptance_criteria>
  <done>Three test files exist; unit tests green; integration tests collect cleanly with Plan-02 endpoint tests xfail-marked.</done>
</task>

<task type="auto">
  <name>Task 1: Migration 0012 — invite codes table + first_seen_at + profile diff columns</name>
  <files>migrations/versions/0012_invite_codes_and_first_seen_at.py</files>
  <read_first>
    - migrations/versions/0011_devices_and_pairing_codes.py (exact analog — header block, revision constants, module-level SQL string constants, CREATE TABLE / CREATE INDEX / upgrade / downgrade structure)
    - .planning/phases/07-member-self-connect-collection-diff/07-PATTERNS.md (migration 0012 section — full constant-by-constant mapping)
    - migrations/versions/0009_v2_profiles_and_collection_cache.py (confirms profile_collection columns + synced_at, beside which first_seen_at sits)
  </read_first>
  <action>
    Create migration 0012 with `revision = "0012"`, `down_revision = "0011"`, `branch_labels = None`, `depends_on = None`, following 0011's exact conventions (all SQL in module-level string constants, op.execute per constant, no f-strings, downgrade fully reverses upgrade).

    Constants and DDL:
    - `_CREATE_INVITE_CODES`: CREATE TABLE IF NOT EXISTS gruvax.profile_invite_codes (code UUID PRIMARY KEY DEFAULT gen_random_uuid(), profile_id UUID NOT NULL REFERENCES gruvax.profiles(id) ON DELETE CASCADE, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), expires_at TIMESTAMPTZ NOT NULL, consumed_at TIMESTAMPTZ). The ON DELETE CASCADE satisfies D-11 (deleting a profile invalidates its outstanding invite).
    - `_IDX_INVITE_CODES_EXPIRES`: CREATE INDEX IF NOT EXISTS idx_profile_invite_codes_expires ON gruvax.profile_invite_codes (expires_at).
    - `_IDX_INVITE_CODES_PROFILE`: CREATE INDEX IF NOT EXISTS idx_profile_invite_codes_profile ON gruvax.profile_invite_codes (profile_id) — supports the D-09 "void prior invite for this profile" query.
    - `_ADD_FIRST_SEEN_AT`: ALTER TABLE gruvax.profile_collection ADD COLUMN IF NOT EXISTS first_seen_at TIMESTAMPTZ (nullable for online migration — Pitfall 3).
    - `_ADD_PROFILE_DIFF_COLUMNS`: ALTER TABLE gruvax.profiles ADD COLUMN IF NOT EXISTS last_new_record_count BIGINT DEFAULT 0, ADD COLUMN IF NOT EXISTS last_sync_is_initial BOOLEAN DEFAULT FALSE (D-08 stored diff state).

    upgrade(): execute all five constants. downgrade(): DROP both indexes, DROP TABLE profile_invite_codes, DROP COLUMN first_seen_at from profile_collection, DROP both diff columns from profiles — fully reversing upgrade.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` completes with no error (round-trip clean).
    - `revision = "0012"` and `down_revision = "0011"` are present as module-level assignments.
    - After upgrade, `\d gruvax.profile_invite_codes` shows columns code (uuid PK), profile_id (FK ON DELETE CASCADE), created_at, expires_at, consumed_at.
    - After upgrade, gruvax.profile_collection has a first_seen_at TIMESTAMPTZ column and gruvax.profiles has last_new_record_count + last_sync_is_initial columns.
    - No f-strings or inline triple-quoted SQL inside upgrade()/downgrade(): `grep -nE 'f"|f.{0,2}"""' migrations/versions/0012_invite_codes_and_first_seen_at.py | grep -v '^#'` returns nothing.
  </acceptance_criteria>
  <done>Migration 0012 round-trips cleanly; invite table, first_seen_at, and both diff columns exist after upgrade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute arrival count + is_initial_import in the swap; extend SSE payload; surface on admin APIs</name>
  <files>src/gruvax/sync/profile_sync.py, src/gruvax/api/admin/profiles.py, src/gruvax/api/admin/diagnostics.py</files>
  <behavior>
    - Before the profiles UPDATE in _swap_inside_tx, reading last_sync_at IS NULL yields is_initial_import (true only on the first-ever sync).
    - new_record_count = max(0, row_count - existing_count) where existing_count is the count of pre-DELETE profile_collection rows matching staging on (release_id, folder_id). Always >= 0.
    - The INSERT into profile_collection sets first_seen_at = NOW() for the synced rows.
    - The profiles UPDATE writes last_new_record_count and last_sync_is_initial alongside the existing fields.
    - _refresh_profile_caches publishes collection_changed with {profile_id, new_record_count, is_initial_import}.
    - GET /api/admin/profiles (list + single) returns has_token (bool, never the ciphertext) plus last_new_record_count and last_sync_is_initial.
    - GET /api/admin/diagnostics per-profile entries include last_new_record_count and last_sync_is_initial.
  </behavior>
  <read_first>
    - src/gruvax/sync/profile_sync.py (lines 281-356 swap + refresh; 503-532 sync_profile caller chain)
    - src/gruvax/api/admin/profiles.py (lines 184-275 list_profiles + get_profile)
    - src/gruvax/api/admin/diagnostics.py (lines 100-134 per-profile diagnostics)
    - .planning/phases/07-member-self-connect-collection-diff/07-PATTERNS.md (profile_sync.py + profiles.py + diagnostics sections — exact SQL extensions)
    - .planning/phases/07-member-self-connect-collection-diff/07-RESEARCH.md (Pitfall 3, Pitfall 4, Pitfall 7, Pitfall 9 — these are acceptance criteria)
  </read_first>
  <action>
    In profile_sync.py `_swap_inside_tx`: change the return type to `tuple[int, bool]` returning (new_record_count, is_initial_import). FIRST, before any DELETE/UPDATE, SELECT `last_sync_at IS NULL` for this profile and capture it as `is_initial_import` (Pitfall 4 — read BEFORE the UPDATE that sets last_sync_at). Then, still before the DELETE, compute `existing_count` via SELECT COUNT(*) joining gruvax.profile_collection pc to profile_collection_staging s ON pc.release_id = s.release_id AND pc.folder_id IS NOT DISTINCT FROM s.folder_id WHERE pc.profile_id = %s::uuid. Set `new_record_count = max(0, row_count - existing_count)` (Pitfall 9 — retry-safe scalar comparison, NOT per-row first_seen_at). Extend the existing INSERT ... SELECT ... FROM profile_collection_staging to also project NOW() into a new `first_seen_at` column (add `first_seen_at` to the column list and `NOW()` to the SELECT list). Extend the existing profiles UPDATE to also set `last_new_record_count = %s, last_sync_is_initial = %s` (bind new_record_count, is_initial_import). Return (new_record_count, is_initial_import).

    In `sync_profile` (line ~506): capture `new_record_count, is_initial_import = await _swap_inside_tx(...)`. Thread both values into `_refresh_profile_caches` — add parameters `new_record_count: int, is_initial_import: bool` to its signature and pass them at line ~516.

    In `_refresh_profile_caches`: change the publish call to `await bus.publish("collection_changed", {"profile_id": profile_id, "new_record_count": new_record_count, "is_initial_import": is_initial_import})`. Keep the publish AFTER the cache reload (existing Pitfall A ordering comment).

    In profiles.py: extend BOTH the list_profiles and get_profile SELECTs to add `(app_token_encrypted IS NOT NULL AND length(app_token_encrypted) > 1)::bool AS has_token, last_new_record_count, last_sync_is_initial` (Pitfall 7 — derive the boolean in SQL; the ciphertext never leaves Postgres). Add `"has_token"`, `"last_new_record_count"`, `"last_sync_is_initial"` to the serialized response dicts in both endpoints.

    In diagnostics.py: extend the per-profile SELECT (line ~106) with `last_new_record_count, last_sync_is_initial` and add both to each profile_diagnostics dict (line ~112).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && uv run pytest tests/unit/test_profile_sync_diff.py tests/integration/test_invite_codes.py::test_initial_import_flag tests/integration/test_invite_codes.py::test_arrival_count_accuracy tests/integration/test_invite_codes.py::test_profile_new_record_fields tests/integration/test_invite_codes.py::test_profile_has_token_field -q --tb=short --benchmark-skip 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `is_initial_import` is captured by reading `last_sync_at IS NULL` BEFORE the profiles UPDATE: `grep -n "last_sync_at IS NULL" src/gruvax/sync/profile_sync.py` returns a line that appears before the UPDATE that sets `last_sync_at = NOW()`.
    - `new_record_count` uses `max(0, ...)`: `grep -n "max(0" src/gruvax/sync/profile_sync.py` matches.
    - The published payload contains all three keys: `grep -n "new_record_count" src/gruvax/sync/profile_sync.py` and `is_initial_import` both present in the publish dict.
    - `GET /api/admin/profiles` response body contains `has_token` and does NOT contain `app_token_encrypted` (asserted by test_profile_has_token_field).
    - First sync of a fresh profile reports `is_initial_import=true`; a second sync reports `false` (test_initial_import_flag passes).
    - `new_record_count` equals the count of genuinely new releases, never negative (test_arrival_count_accuracy passes).
    - `mypy` and `ruff` clean on the three modified files.
  </acceptance_criteria>
  <done>Diff count + initial-import flag computed atomically in the swap, delivered on SSE, and surfaced via has_token + diff fields on the admin APIs; the four API-04 integration tests + unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /api/admin/profiles, /api/admin/diagnostics | PIN-gated; must never leak the encrypted/raw PAT |
| sync worker → Postgres swap transaction | diff count must be committed atomically with the collection swap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | GET /api/admin/profiles, /api/admin/diagnostics | mitigate | Derive `has_token` in SQL as `(app_token_encrypted IS NOT NULL AND length(...)>1)::bool`; the raw ciphertext is never added to any SELECT projection or response dict (Pitfall 7). |
| T-07-02 | Tampering | _swap_inside_tx arrival count | mitigate | Compute new_record_count + is_initial_import INSIDE the swap transaction and store last_new_record_count atomically with the swap; a second sync starting mid-flight cannot read a half-written count (Pitfall 9). |
| T-07-03 | Repudiation/Integrity | is_initial_import detection | mitigate | Read `last_sync_at IS NULL` BEFORE the UPDATE that sets it (Pitfall 4); prevents every sync falsely reporting initial-import. |
| T-07-04 | Information Disclosure | first_seen_at backfill | accept | Existing rows remain NULL after the ADD COLUMN; the diff count uses scalar pre-DELETE comparison, not per-row first_seen_at, so NULL backfill cannot mis-count (Pitfall 3). Rationale: nullable online migration is the correct Alembic pattern; no PII in the column. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH §Package Legitimacy Audit confirms NO new packages this phase; no install task exists. |
</threat_model>

<verification>
- Migration 0012 round-trips (upgrade → downgrade → upgrade) with no error.
- `uv run pytest tests/unit/test_profile_sync_diff.py -q --benchmark-skip` green.
- `uv run pytest tests/integration/test_invite_codes.py::test_initial_import_flag ::test_arrival_count_accuracy ::test_profile_new_record_fields ::test_profile_has_token_field -q --benchmark-skip` green.
- `uv run mypy src/gruvax/sync/profile_sync.py src/gruvax/api/admin/profiles.py src/gruvax/api/admin/diagnostics.py` and `uv run ruff check` clean.
</verification>

<success_criteria>
- API-04 backend complete: `collection_changed` carries `new_record_count` + `is_initial_import`; admin API exposes `last_new_record_count`, `last_sync_is_initial`, and a derived `has_token` (no ciphertext leak).
- Migration 0012 has landed the `profile_invite_codes` table (consumed by Plan 02), `first_seen_at`, and the profiles diff columns.
- Wave-0 test scaffolds exist; the four API-04 integration tests and both unit-test files pass.
</success_criteria>

<output>
Create `.planning/phases/07-member-self-connect-collection-diff/07-01-SUMMARY.md` when done.
</output>
