---
phase: 07-member-self-connect-collection-diff
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/api/inviteClient.ts
  - frontend/src/routes/redeem/RedeemPage.tsx
  - frontend/src/routes/redeem/RedeemPage.css
  - frontend/src/App.tsx
  - frontend/src/routes/admin/ProfileDrawer.tsx
  - frontend/src/routes/admin/ProfileDiagnosticsCard.tsx
  - frontend/src/routes/kiosk/KioskView.tsx
autonomous: false
requirements: [AUTH-02, API-04]
user_setup: []

must_haves:
  truths:
    - "A member can open /redeem/:code, see which profile they are connecting, paste their PAT, submit, and reach a terminal 'Connected — importing' state."
    - "Expired / used / invalid codes show a plain-language error card with no form."
    - "The owner can generate an invite link in ProfileDrawer, see a TTL countdown, and copy the link to the clipboard (with COPIED! feedback)."
    - "The admin diagnostics card shows a NEW RECORDS (or IMPORTED) row driven by the stored per-profile count."
    - "The kiosk shows a yellow 'N NEW RECORDS' pill after a sync, parsed from the collection_changed SSE payload, which clears on the next sync."
    - "No hardcoded hex anywhere — all colors/spacing/typography come from var(--gruvax-*) tokens."
    - "D-02: the owner obtains the invite link via copy-to-clipboard (no native Web Share API, no QR in this phase), shown with a live TTL countdown."
  artifacts:
    - path: "frontend/src/routes/redeem/RedeemPage.tsx"
      provides: "public member redeem page (loading/active/invalid/submitting/success states)"
      contains: "CONNECTED"
      min_lines: 80
    - path: "frontend/src/api/inviteClient.ts"
      provides: "getInviteCode + redeemInviteCode (public fetch) + generateInvite (adminFetch)"
      contains: "redeemInviteCode"
    - path: "frontend/src/routes/admin/ProfileDrawer.tsx"
      provides: "INVITE LINK section with generate + TTL countdown + copy-to-clipboard"
      contains: "INVITE LINK"
    - path: "frontend/src/routes/kiosk/KioskView.tsx"
      provides: "collection_changed payload parsing + new-records pill"
      contains: "new_record_count"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "RedeemPage at /redeem/:code"
      via: "Route outside the /admin nest"
      pattern: "redeem/:code"
    - from: "frontend/src/routes/kiosk/KioskView.tsx"
      to: "collection_changed SSE payload"
      via: "JSON.parse(e.data).new_record_count"
      pattern: "new_record_count"
    - from: "frontend/src/routes/admin/ProfileDrawer.tsx"
      to: "POST /api/admin/profiles/{id}/invite"
      via: "generateInvite + navigator.clipboard.writeText"
      pattern: "clipboard"
---

<objective>
Deliver the Phase 7 frontend: the public member redeem page at `/redeem/:code`, the owner-side
"Copy invite link" affordance in ProfileDrawer (generate + 1-hour TTL countdown + copy-to-clipboard),
and the "N new records" indicators on both the admin diagnostics card and the kiosk (parsed from the
extended `collection_changed` SSE payload). Plus the shared types and invite API client.

Purpose: completes the user-observable surfaces for AUTH-02 (member self-connect UI) and API-04
(the diff indicator on admin + kiosk). All backend contracts are already shipped by Plans 01–02.

Output: `types.ts` + `inviteClient.ts`, the RedeemPage component + CSS, the App route, and the
three extended admin/kiosk components. Ends with a human-verify checkpoint (the phase is UI-bearing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-member-self-connect-collection-diff/07-CONTEXT.md
@.planning/phases/07-member-self-connect-collection-diff/07-RESEARCH.md
@.planning/phases/07-member-self-connect-collection-diff/07-PATTERNS.md
@.planning/phases/07-member-self-connect-collection-diff/07-UI-SPEC.md
@.planning/phases/07-member-self-connect-collection-diff/07-01-SUMMARY.md
@.planning/phases/07-member-self-connect-collection-diff/07-02-SUMMARY.md
@design/gruvax-design-language.md
@design/gruvax-design-tokens.css

<interfaces>
<!-- Contracts extracted from the live codebase + Plans 01-02. Use directly. -->

Backend endpoints (from Plan 02):
  POST /api/admin/profiles/{id}/invite (PIN+CSRF) -> { code, url, expires_at }
  GET  /api/invite-codes/{code} (public) -> { display_name, expires_at } | 404 { type: "invite_not_found" }
  POST /api/invite-codes/{code}/redeem (public) body { pat } -> { status: "connected", profile_id } | 404 invite_not_found | 401 pat_rejected | 409 user_id_collision | 503 upstream_unavailable | 429 rate_limited

Backend fields added to AdminProfile (Plan 01): has_token: boolean, last_new_record_count: number, last_sync_is_initial: boolean
Backend SSE payload (Plan 01): collection_changed { profile_id, new_record_count, is_initial_import }

adminClient (frontend/src/api/adminClient.ts:66): export async function adminFetch(path, options) — credentials 'same-origin' + CSRF. Use for owner generateInvite ONLY.

App.tsx Routes (frontend/src/App.tsx:135-153): public routes /, /pair, /select sit OUTSIDE the <Route path="/admin"> nest; add /redeem/:code beside them.

ProfileDrawer (frontend/src/routes/admin/ProfileDrawer.tsx): drawerMode state ('view' | 'rotate' | 'rename'); button classes profile-btn-primary / profile-btn-secondary; PAT input pattern profile-field-input--pat with Eye/EyeOff (lucide-react). Section label class profile-field-label.

ProfileDiagnosticsCard (frontend/src/routes/admin/ProfileDiagnosticsCard.tsx): rows use .diag-status-row / .diag-row-label / .diag-cell-mono. Prop type ProfileDiagnosticEntry (frontend/src/api/adminClient.ts:620) — extend with last_new_record_count + last_sync_is_initial.

KioskView (frontend/src/routes/kiosk/KioskView.tsx): boundProfileId from useSessionStore (44); EventSource handlers added 271-356; collection_changed handler at 338-345 (currently no-arg, no e.data parse); main JSX at 544-552 (.kiosk-page > .kiosk-content > .kiosk-search-section at 554). queryClient + resync() in scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + invite API client + public redeem page (AUTH-02 member UI)</name>
  <files>frontend/src/api/types.ts, frontend/src/api/inviteClient.ts, frontend/src/routes/redeem/RedeemPage.tsx, frontend/src/routes/redeem/RedeemPage.css, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/api/types.ts (the AdminProfile interface — extend with has_token, last_new_record_count, last_sync_is_initial; add InviteCodeInfo, GeneratedInvite, RedeemResult)
    - frontend/src/api/adminClient.ts (adminFetch signature for the owner generate call)
    - frontend/src/routes/admin/ProfileDrawer.tsx (PAT input + Eye/EyeOff toggle + mapConnectError pattern — analog for the redeem form + error mapping)
    - frontend/src/App.tsx (Routes block — add the public /redeem/:code route outside /admin)
    - .planning/phases/07-member-self-connect-collection-diff/07-UI-SPEC.md (§Surface 1 redeem page structure, states, copywriting; §Color — PAT focus ring uses --gruvax-yellow; §Animation)
    - .planning/phases/07-member-self-connect-collection-diff/07-PATTERNS.md (RedeemPage.tsx + inviteClient.ts + types.ts + App.tsx sections)
    - design/gruvax-design-tokens.css (token names — consume, never hardcode hex)
  </read_first>
  <action>
    types.ts: extend `AdminProfile` with `has_token: boolean`, `last_new_record_count: number`, `last_sync_is_initial: boolean` (keep `app_token_revoked` for existing status logic). Add `InviteCodeInfo { display_name: string; expires_at: string }`, `GeneratedInvite { code: string; url: string; expires_at: string }`, `RedeemResult { status: 'connected'; profile_id: string }`, and a `RedeemApiError` class carrying `errorType` parsed from the response `detail.type`.

    inviteClient.ts: a `publicFetch(path, options)` helper using `fetch` with `credentials: 'omit'` and JSON content-type (public routes carry NO CSRF/session — per UI-SPEC + Plan 02). Export `getInviteCode(code): Promise<InviteCodeInfo>` and `redeemInviteCode(code, pat): Promise<RedeemResult>` (both throw RedeemApiError on !res.ok, reading detail.type). Export `generateInvite(profileId): Promise<GeneratedInvite>` using `adminFetch` (owner-side, PIN-gated).

    RedeemPage.tsx (route component, reads :code from useParams): a `PageState` union 'loading' | 'invalid' | 'active' | 'submitting' | 'success'. On mount, GET the code → 'active' (store InviteCodeInfo) or 'invalid'. Render per UI-SPEC §Surface 1: GRUVAX icon mark (48px), heading "CONNECT {DISPLAY_NAME}" (uppercased), instruction paragraph, Discogs link to https://www.discogs.com/settings/developers (target=_blank rel="noopener noreferrer"), "PERSONAL ACCESS TOKEN" label, password-type PAT input with Eye/EyeOff toggle (focus ring uses --gruvax-yellow), full-width "CONNECT COLLECTION" CTA (→ "CONNECTING…" + Loader2 while submitting). On submit → redeemInviteCode → 'success' (terminal: CheckCircle2 in --gruvax-success, "CONNECTED" heading, "Your collection is importing. You can close this page.", no further interaction). Map errors with a mapRedeemError function using the exact copy strings from UI-SPEC §Copywriting Contract (pat_rejected, user_id_collision, upstream_unavailable; PAT errors stay inline + form interactive; invalid/expired/used render the error card with no form). Accessibility: role="main", <h1> heading, aria-label on the toggle, role="alert" on the error zone, role="status" on success.

    RedeemPage.css: vanilla CSS module per UI-SPEC §Spacing/§Typography/§Color/§Responsive. min-height: 100dvh; centered card max-width 480px; horizontal padding var(--gruvax-space-5) (var(--gruvax-space-4) under 390px). PAT input focus ring `outline: 2px solid var(--gruvax-yellow)`. CTA `background: var(--gruvax-blue); color: var(--gruvax-white); min-height: 44px; width: 100%`. Success icon/heading use var(--gruvax-success). Mount/success transitions per UI-SPEC §Animation tokens. NEVER hardcode hex — every color/space/font via var(--gruvax-*).

    App.tsx: import RedeemPage; add `<Route path="/redeem/:code" element={<RedeemPage />} />` beside /pair and /select, OUTSIDE the `<Route path="/admin">` nest (D-03 — public, no PIN gate).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX/frontend && npm run lint && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` and `npm run lint` are clean for the new/changed files.
    - `RedeemPage.tsx` renders the terminal success heading "CONNECTED" and the body copy "Your collection is importing. You can close this page." (grep).
    - The redeem route is registered OUTSIDE the /admin nest: `grep -n "redeem/:code" frontend/src/App.tsx` matches and is not inside the `<Route path="/admin">` block.
    - inviteClient public functions use `credentials: 'omit'`; generateInvite uses `adminFetch`: `grep -n "credentials: 'omit'\|adminFetch" frontend/src/api/inviteClient.ts` shows both.
    - No hardcoded hex in RedeemPage.css: `grep -nE '#[0-9a-fA-F]{3,6}' frontend/src/routes/redeem/RedeemPage.css` returns nothing.
    - `AdminProfile` now includes `has_token`, `last_new_record_count`, `last_sync_is_initial`.
  </acceptance_criteria>
  <done>Public redeem page works end-to-end against the Plan-02 endpoints; types + client shipped; route is public; CSS is token-only.</done>
</task>

<task type="auto">
  <name>Task 2: Owner invite affordance + admin diagnostics row + kiosk new-records pill</name>
  <files>frontend/src/routes/admin/ProfileDrawer.tsx, frontend/src/routes/admin/ProfileDiagnosticsCard.tsx, frontend/src/routes/kiosk/KioskView.tsx</files>
  <read_first>
    - frontend/src/routes/admin/ProfileDrawer.tsx (existing drawerMode/connectState state + button classes; insertion point for the INVITE LINK section between action rows and DELETE PROFILE)
    - frontend/src/routes/admin/ProfileDiagnosticsCard.tsx (the .diag-status-row pattern — insert NEW RECORDS between ITEMS and LAST ERROR)
    - frontend/src/routes/kiosk/KioskView.tsx (lines 338-356 collection_changed handler; 544-554 JSX where the pill mounts; boundProfileId at 44)
    - frontend/src/api/adminClient.ts (ProfileDiagnosticEntry at 620 — extend with last_new_record_count + last_sync_is_initial)
    - .planning/phases/07-member-self-connect-collection-diff/07-UI-SPEC.md (§Surface 2 invite affordance + TTL countdown + copy flow; §Surface 3 admin row + kiosk pill; §Interaction Contracts)
    - .planning/phases/07-member-self-connect-collection-diff/07-PATTERNS.md (ProfileDrawer.tsx + ProfileDiagnosticsCard.tsx + KioskView.tsx sections)
  </read_first>
  <action>
    ProfileDrawer.tsx: add invite state (inviteInfo: GeneratedInvite | null, inviteError, isGeneratingInvite, copiedAt, ttlSeconds). Add an INVITE LINK section (shown in drawerMode === 'view') between the existing action rows and DELETE PROFILE. When no active invite: a "GENERATE INVITE LINK" secondary button calling generateInvite(profileId). When an invite exists: a link box showing inviteInfo.url (DM Mono) + an "Expires in MM:SS" countdown driven by a setInterval that recomputes from inviteInfo.expires_at every second (color shifts to --gruvax-warning under 5min, --gruvax-error under 1min, and sets inviteInfo to null at 0 — D-09 one active invite, no history), plus a yellow "COPY LINK" primary CTA. handleCopyLink uses navigator.clipboard.writeText(inviteInfo.url); on success show "COPIED!" for 2000ms; on failure set inviteError "Could not copy. Tap the link to copy manually." (UI-SPEC copy). Clear the interval on unmount / when inviteInfo changes. aria-live="polite" on the countdown; aria-label="Copy invite link" on the copy button. Use the exact copy strings from UI-SPEC §Copywriting Contract. Owner NEVER sees a raw PAT here.

    ProfileDiagnosticsCard.tsx: extend the prop type (ProfileDiagnosticEntry) with `last_new_record_count: number | null` and `last_sync_is_initial: boolean | null`. Insert a new .diag-status-row between ITEMS and LAST ERROR: label "IMPORTED" when last_sync_is_initial else "NEW RECORDS"; value in DM Mono via .diag-cell-mono, colored var(--gruvax-success) when count > 0 else var(--gruvax-text-muted); show "—" when count is null/0 (no yellow on this card — established rule). Use inline style only for the conditional color token (no hardcoded hex).

    KioskView.tsx: change the collection_changed handler from `() => {...}` to `(e) => {...}`, keeping the existing invalidateQueries + resync(), then defensively parse `e.data` (JSON.parse if non-empty; backward-compatible if empty/missing): read new_record_count (number, default 0) and is_initial_import (boolean). Optionally verify payload.profile_id matches boundProfileId before showing. Add `newRecordState` useState ({ count, isInitial } | null); set it when count > 0; the next collection_changed event replaces it (D-08 — clears/replaces on next sync). Wrap parsing in try/catch (graceful degrade — no pill on parse failure; do NOT call es.close() in the handler — Pitfall, the cleanup return is the only close). Render the pill in .kiosk-content (below the search section) when newRecordState.count > 0: a yellow pill (background var(--gruvax-yellow), text var(--gruvax-blue-darker), border-radius var(--gruvax-radius-pill), Barlow Condensed 700 18px), text "{N} NEW RECORDS" or "IMPORTED {N} RECORDS" for initial import; role="status" aria-live="polite" with the aria-label from UI-SPEC; enter/exit opacity transitions per §Animation tokens. Add the pill CSS to the kiosk CSS module (token-only).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX/frontend && npm run lint && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` and `npm run lint` clean.
    - ProfileDrawer renders an "INVITE LINK" section with both "GENERATE INVITE LINK" and "COPY LINK" copy, and a TTL countdown: `grep -n "INVITE LINK\|COPY LINK\|clipboard" frontend/src/routes/admin/ProfileDrawer.tsx` matches.
    - The diagnostics card renders a NEW RECORDS / IMPORTED row driven by last_new_record_count: `grep -n "NEW RECORDS\|IMPORTED\|last_new_record_count" frontend/src/routes/admin/ProfileDiagnosticsCard.tsx` matches.
    - KioskView parses the payload and shows a pill: `grep -n "new_record_count\|newRecordState" frontend/src/routes/kiosk/KioskView.tsx` matches; the collection_changed handler now takes `(e)` and JSON.parses e.data; no new `es.close()` added inside the handler.
    - No hardcoded hex introduced in the changed components/CSS: `grep -nE '#[0-9a-fA-F]{3,6}' frontend/src/routes/kiosk/KioskView.tsx frontend/src/routes/admin/ProfileDrawer.tsx frontend/src/routes/admin/ProfileDiagnosticsCard.tsx` returns nothing (or only pre-existing matches unrelated to this work).
  </acceptance_criteria>
  <done>Owner invite affordance with TTL + copy works; admin NEW RECORDS row + kiosk yellow pill render from the diff data; lint + typecheck clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — invite/redeem flow + diff indicators across 6 surfaces</name>
  <action>Do not write code in this task. After Tasks 1-2 land, run the manual verification steps in &lt;how-to-verify&gt; below and pause for the developer to approve or report issues per surface.</action>
  <what-built>
    AUTH-02 + API-04 frontend: public redeem page (/redeem/:code), owner "Copy invite link" affordance in ProfileDrawer (generate + 1-hour TTL countdown + copy-to-clipboard), admin diagnostics NEW RECORDS row, and the kiosk yellow "N NEW RECORDS" pill driven by the collection_changed SSE payload.
  </what-built>
  <how-to-verify>
    Use the Local UAT recipe (MEMORY: one uvicorn against gruvax-dev-pg; set PIN via gruvax-set-pin; login rate-limiter trips on repeat logins — restart to clear). Start the backend and `cd frontend && npm run dev`.

    1. INVITE GENERATION (admin): Sign in to /admin with the PIN → Profiles → open a profile drawer → tap "GENERATE INVITE LINK". Confirm: a link box appears with the /redeem/{uuid} URL, an "Expires in MM:SS" countdown ticking down from ~60:00, and a yellow "COPY LINK" button. Tap "COPY LINK" → confirm it flashes "COPIED!" for ~2s and the clipboard holds the URL. Generate a second link → confirm the first is superseded (one block shown).
    2. REDEEM (member, separate browser/incognito — no PIN): open the copied /redeem/{code} URL. Confirm: heading "CONNECT {PROFILE NAME}", the Discogs developer-settings link, a password PAT field with working Eye/EyeOff toggle (yellow focus ring). Paste a valid dev PAT → "CONNECT COLLECTION" → "CONNECTING…" → terminal "CONNECTED — Your collection is importing. You can close this page."
    3. SINGLE-USE: reload the same /redeem/{code} URL → confirm the plain-language "already used / expired" error card with NO form.
    4. DIFF INDICATOR (admin): open the profile's diagnostics card → confirm an "IMPORTED N" row (first sync) or "NEW RECORDS N" row (subsequent).
    5. KIOSK PILL: open the kiosk (/) bound to that profile → trigger a "Sync now" (or wait for the redeem's auto-sync) → confirm a yellow "N NEW RECORDS" (or "IMPORTED N RECORDS") pill appears below the search box and clears/replaces on the next sync.
    6. Confirm no raw PAT is ever shown anywhere in the admin UI (owner sees only presence, never the token).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per surface).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| member browser → public redeem page | the redeem page POSTs the PAT to the public endpoint; it must not persist the PAT client-side |
| owner browser → admin invite affordance | owner must never see the raw/encrypted PAT; only presence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Information Disclosure | RedeemPage PAT handling | mitigate | PAT lives only in component state and the POST body; never written to localStorage/sessionStorage/URL; input is type="password" with autocomplete="off". |
| T-07-14 | Information Disclosure | owner invite affordance | mitigate | UI reads only `has_token` (Plan 01); no PAT field or value is rendered in ProfileDrawer for the invite flow. |
| T-07-15 | Information Disclosure | error-card oracle on redeem page | accept | The member-facing copy ("expired" vs "already used") is safe to show — the discriminator is owned by the owner who minted the code, not an attacker; the SERVER returns a uniform 404 regardless (Plan 02, Pitfall 2). |
| T-07-16 | Tampering | kiosk SSE payload parse | mitigate | collection_changed parsing is wrapped in try/catch with graceful degrade; no es.close() added in the handler (cleanup return is the only close). |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH §Package Legitimacy Audit confirms NO new packages this phase; no install task exists. |
</threat_model>

<verification>
- `cd frontend && npm run lint && npx tsc --noEmit` clean.
- Human-verify checkpoint approved across all six surfaces.
- No hardcoded hex in any new/changed CSS or inline style (token-only — CLAUDE.md convention).
</verification>

<success_criteria>
- AUTH-02 member UI: redeem page (loading/active/invalid/submitting/success) + owner invite affordance (generate + TTL + copy) ship and work against the Plan-02 endpoints.
- API-04 UI: admin NEW RECORDS/IMPORTED row + kiosk yellow pill render from the diff data and clear on the next sync.
- Owner never sees the raw PAT; all visuals consume design tokens; lint + typecheck clean; human-verify approved.
</success_criteria>

<output>
Create `.planning/phases/07-member-self-connect-collection-diff/07-03-SUMMARY.md` when done.
</output>
