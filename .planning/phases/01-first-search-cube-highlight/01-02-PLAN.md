---
phase: 01-first-search-cube-highlight
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/gruvax/estimator/__init__.py
  - src/gruvax/estimator/normalize.py
  - src/gruvax/estimator/contract.py
  - src/gruvax/estimator/boundary_cache.py
  - src/gruvax/estimator/algorithm.py
  - tests/unit/__init__.py
  - tests/unit/test_normalize.py
  - tests/unit/test_algorithm.py
  - tests/property/__init__.py
  - tests/property/test_parser_props.py
autonomous: true
requirements: [POS-01, POS-02, POS-04]
must_haves:
  truths:
    - "Catalog numbers are compared via a numeric-aware token-stream key, never raw strings"
    - "parse_key('BLP 9') < parse_key('BLP 10') and parse_key('BLP 4195') == parse_key('blp-4195')"
    - "The LocateResult/SubInterval/CubeRef contract is locked exactly as ARCHITECTURE specifies"
    - "The cube-only estimator returns confidence=0.30, sub_cube_interval=null, estimator_version='cube-only-v1' for a covered record"
    - "A record whose label has no covering boundary yields confidence=0.0, primary_cube=null, label_span=[]"
    - "BoundaryCache loads all cube_boundaries rows at startup and exposes an invalidate() seam"
    - "Implements decisions D-10 (cube-only fallback, sub_cube_interval null, contract locked), D-11 (confidence float 0.30 not enum; estimator_version cube-only-v1), D-12 (real label_span; 404 vs 200-no-boundary semantics), D-13 (POS-01 token-stream parser strategy C; raw compare forbidden); supports D-03 (cache loads from cube_boundaries)"
  artifacts:
    - path: "src/gruvax/estimator/normalize.py"
      provides: "normalize_catalog, parse_key, compare_catalogs, catalog_in_range (POS-01)"
      contains: "def parse_key"
    - path: "src/gruvax/estimator/contract.py"
      provides: "LocateResult, SubInterval, CubeRef dataclasses + CUBE_ONLY_CONFIDENCE constant"
      contains: "class LocateResult"
    - path: "src/gruvax/estimator/algorithm.py"
      provides: "locate_cube_only(): label+catalog → LocateResult (cube-only-v1)"
      contains: "cube-only-v1"
    - path: "src/gruvax/estimator/boundary_cache.py"
      provides: "BoundaryCache.load()/get_boundaries()/invalidate()"
      contains: "def invalidate"
    - path: "tests/property/test_parser_props.py"
      provides: "Hypothesis total-order + cosmetic-stability + monotonic properties"
      contains: "@given"
  key_links:
    - from: "src/gruvax/estimator/algorithm.py"
      to: "src/gruvax/estimator/normalize.py"
      via: "catalog_in_range comparator (no raw string compare)"
      pattern: "catalog_in_range"
    - from: "src/gruvax/estimator/boundary_cache.py"
      to: "gruvax.cube_boundaries"
      via: "SELECT at startup load()"
      pattern: "cube_boundaries"
---

<objective>
Build the POS-01 catalog-number parser/comparator (shared infrastructure), the locked LocateResult
contract, the in-memory BoundaryCache (POS-04), and the Phase-1 cube-only estimator (INTERPOLATION
§4.8). This is the only genuinely novel code in Phase 1 and the prime Hypothesis property-test target.
TDD: tests express the locked behavior first (RED), then the implementation makes them GREEN.

Purpose: POS-01 is reused by the search catalog path (plan 03), every later algorithm (Phase 2), and
the boundary-save validator (Phase 3). Raw-string catalog comparison silently misplaces 35.6% of
multi-record labels — this module forbids it. The contract is locked here for all later phases (D-10/
D-11/D-12); Phase 2 swaps in the §4.1 estimator behind the same shape.
Output: A tested parser, a locked contract, a startup-loaded boundary cache, and the cube-only estimator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-first-search-cube-highlight/01-CONTEXT.md
@.planning/phases/01-first-search-cube-highlight/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/INTERPOLATION.md
@.planning/phases/01-first-search-cube-highlight/01-01-SUMMARY.md

<interfaces>
<!-- Concrete reference implementations live in 01-RESEARCH.md §Pattern 2 + §Pattern 3 + §Pattern 4.
     Implement to these exact signatures. -->

normalize.py:
  normalize_catalog(raw: str | None) -> str           # NFKC + casefold + first-of-comma + separator-collapse
  parse_key(catalog: str | None) -> tuple              # alternating (type_tag, value) tokens; empties sort first
  compare_catalogs(a, b) -> int                        # -1/0/1 total order over parse_key
  catalog_in_range(catalog, first_catalog, last_catalog) -> bool

contract.py (ARCHITECTURE §Position-Estimator Contract; D-10/D-11/D-12):
  CubeRef(unit_id:int, row:int, col:int)               # frozen
  SubInterval(cube:CubeRef, start:float, end:float, crosses_boundary:bool, next_cube:CubeRef|None)
  LocateResult(release_id:int, primary_cube:CubeRef|None, label_span:list[CubeRef],
               sub_cube_interval:SubInterval|None, confidence:float,
               generated_at:datetime, estimator_version:str="cube-only-v1")
  CUBE_ONLY_CONFIDENCE = 0.30   NO_BOUNDARY_CONFIDENCE = 0.0

boundary_cache.py:
  BoundaryRow(unit_id,row,col,first_label,first_catalog,last_label,last_catalog,is_empty)
  BoundaryCache.load(pool) / .get_boundaries() / .invalidate()   # invalidate() is the Phase 4 SSE seam

algorithm.py:
  locate_cube_only(release_id:int, label:str, catalog_number:str, cache:BoundaryCache) -> LocateResult
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POS-01 parser/comparator (normalize.py) — RED then GREEN</name>
  <read_first>
    - .planning/phases/01-first-search-cube-highlight/01-RESEARCH.md (§Pattern 2 token-stream parser — concrete normalize_catalog/parse_key/compare_catalogs/catalog_in_range; §Pattern 2 Hypothesis invariants)
    - .planning/research/INTERPOLATION.md (§3.1 Strategy C; §3.2 normalization pipeline; §3.4 test invariants; §6 edge cases for placeholder/multi-value)
    - .planning/phases/01-first-search-cube-highlight/01-CONTEXT.md (D-13: strategy delegated; raw comparison forbidden)
  </read_first>
  <behavior>
    - parse_key("BLP 9") < parse_key("BLP 10")  (numeric-aware, not lexical)
    - parse_key("BLP 4195") == parse_key("blp-4195") == parse_key("BLP\t4195") == parse_key("blp4195")  (cosmetic stability)
    - parse_key("BLP 4001") != parse_key("BST 4001")  (multi-prefix discriminates)
    - normalize_catalog(normalize_catalog(s)) == normalize_catalog(s)  (idempotent)
    - compare_catalogs(a,b) ∈ {-1,0,1} and == -compare_catalogs(b,a)  (total order, antisymmetric)
    - Placeholder/empty ("none","n/a","",None) sort FIRST; multi-value "BLP-100, BST-200" uses first part only
    - catalog_in_range respects parse_key ordering: catalog_in_range("BLP 4010","BLP 4001","BLP 4020") is True
  </behavior>
  <action>
    Write `tests/unit/test_normalize.py` (golden cases: numeric-aware, cosmetic stability, multi-prefix,
    placeholder, multi-value first-part, NFKC) and `tests/property/test_parser_props.py` (Hypothesis:
    total order/antisymmetry over st.text(), idempotent normalize, numeric-aware monotonicity) FIRST and
    confirm they FAIL (module absent). Then implement `src/gruvax/estimator/normalize.py` with
    `normalize_catalog`, `parse_key`, `compare_catalogs`, `catalog_in_range` per the RESEARCH §Pattern 2
    reference (token regex `([A-Za-z]+|\d+)`, separator-collapse regex `[\s\-_./]+`, digit cap of 12,
    sentinel-first ordering for placeholders). Run until GREEN. Commit RED then GREEN as separate commits.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_normalize.py tests/property/test_parser_props.py -x -q && uv run mypy --strict src/gruvax/estimator/normalize.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_normalize.py tests/property/test_parser_props.py -x` exits 0
    - `src/gruvax/estimator/normalize.py` contains `def parse_key` and `def catalog_in_range`
    - `tests/property/test_parser_props.py` contains `@given` (Hypothesis is actually exercised)
    - `mypy --strict src/gruvax/estimator/normalize.py` exits 0
    - No raw-string `<`/`>` comparison of catalog numbers exists in any non-test estimator file (grep clean)
  </acceptance_criteria>
  <done>Parser passes golden + Hypothesis tests under mypy --strict; comparisons go through parse_key only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LocateResult contract + cube-only estimator + BoundaryCache — RED then GREEN</name>
  <read_first>
    - .planning/phases/01-first-search-cube-highlight/01-RESEARCH.md (§Pattern 3 LocateResult shapes + locate_cube_only; §Pattern 4 BoundaryCache)
    - .planning/research/ARCHITECTURE.md (§Position-Estimator Contract: input/output/error semantics/latency budget)
    - .planning/research/INTERPOLATION.md (§4.8 cube-only fallback; §6 no-covering-boundary edge case)
    - .planning/phases/01-first-search-cube-highlight/01-CONTEXT.md (D-10/D-11/D-12: float confidence, real label_span, error semantics)
    - src/gruvax/estimator/normalize.py (catalog_in_range — created in Task 1)
    - tests/conftest.py (boundary_cache fixture from YAML — created in plan 01)
  </read_first>
  <behavior>
    - For a record whose (label, catalog) falls in a seeded cube: locate_cube_only returns confidence==0.30,
      sub_cube_interval is None, estimator_version=="cube-only-v1", primary_cube is the first cube in span order
    - label_span lists ALL covering cubes sorted by (unit_id,row,col); primary_cube == label_span[0]
    - For a record whose label has NO covering boundary: confidence==0.0, primary_cube is None, label_span==[]
    - BoundaryCache.load(pool) populates get_boundaries() with all 32 seeded rows; invalidate() empties it
    - Covering test uses catalog_in_range (numeric-aware), NOT string compare — verified by a numeric-edge case
  </behavior>
  <action>
    Write `tests/unit/test_algorithm.py` FIRST with: a covered-record case (assert confidence 0.30,
    null sub_cube_interval, version string, primary == span[0]); a no-boundary case (confidence 0.0,
    null primary, empty span); a `test_cache_load` that loads from the seeded DB and asserts
    `len(cache.get_boundaries()) == 32`; and a numeric-edge covering case proving range membership uses
    parse_key. Confirm RED. Then implement `src/gruvax/estimator/contract.py` (CubeRef/SubInterval/
    LocateResult frozen/dataclass exactly per ARCHITECTURE; `CUBE_ONLY_CONFIDENCE = 0.30`,
    `NO_BOUNDARY_CONFIDENCE = 0.0`, documenting D-11 reconciliation in a module docstring),
    `src/gruvax/estimator/boundary_cache.py` (BoundaryRow + BoundaryCache.load/get_boundaries/invalidate,
    with an `invalidate()` docstring marking it the Phase 4 SSE seam), and
    `src/gruvax/estimator/algorithm.py` `locate_cube_only(...)` using case-folded label range +
    `catalog_in_range`, returning the locked contract. Run until GREEN. Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_algorithm.py -x -q && uv run mypy --strict src/gruvax/estimator/</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_algorithm.py -x` exits 0 including `test_cache_load` (32 rows)
    - `src/gruvax/estimator/contract.py` contains `class LocateResult` and the literal `cube-only-v1` and `0.30`
    - `src/gruvax/estimator/boundary_cache.py` contains `def invalidate` with a Phase-4-seam docstring
    - A covered record yields `confidence == 0.30` and `sub_cube_interval is None`; a no-boundary record yields `confidence == 0.0` and `primary_cube is None`
    - `mypy --strict src/gruvax/estimator/` exits 0
  </acceptance_criteria>
  <done>Contract + cache + cube-only estimator pass unit tests under mypy --strict; behavior matches D-10/D-11/D-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none external this plan) | Pure in-process Python; inputs come from DB-resident boundary rows and validated record fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | catalog comparison correctness | mitigate | All comparisons route through parse_key (numeric-aware); Hypothesis total-order + monotonic properties pin correctness (POS-01) |
| T-01-05 | Denial of Service | parse_key on adversarial catalog strings | mitigate | Digit-run cap of 12 prevents barcode-outlier integer blowup; Hypothesis fuzzes arbitrary text without unbounded cost |
| T-01-06 | Information Disclosure | LocateResult error path | accept | Public endpoint returns cube position only; no PII in LocateResult; no-boundary case returns empty span, not an error leak |
</threat_model>

<verification>
- `pytest tests/unit/ tests/property/ -x` green (parser + estimator + cache).
- `mypy --strict src/gruvax/estimator/` passes.
- Hypothesis properties (total order, cosmetic stability, monotonic-in-catalog) hold.
</verification>

<success_criteria>
The POS-01 parser, the locked LocateResult contract, the startup-loadable BoundaryCache with an
invalidate() seam, and the cube-only-v1 estimator all exist and are tested; catalog comparisons never
use raw strings; the contract matches ARCHITECTURE exactly with float confidence per D-11.
</success_criteria>

<output>
Create `.planning/phases/01-first-search-cube-highlight/01-02-SUMMARY.md` when done.
</output>
