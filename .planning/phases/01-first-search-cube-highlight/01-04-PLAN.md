---
phase: 01-first-search-cube-highlight
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/api/types.ts
  - frontend/src/api/client.ts
  - frontend/src/state/store.ts
  - frontend/src/routes/kiosk/KioskView.tsx
  - frontend/src/routes/kiosk/SearchBox.tsx
  - frontend/src/routes/kiosk/ResultsList.tsx
  - frontend/src/routes/kiosk/ResultRow.tsx
  - frontend/src/routes/kiosk/NoResultsRow.tsx
  - frontend/src/routes/kiosk/ShelfGrid.tsx
  - frontend/src/routes/kiosk/ShelfLabel.tsx
  - frontend/src/routes/kiosk/Cube.tsx
  - frontend/src/routes/kiosk/kiosk.css
  - Dockerfile
  - compose.yaml
  - mosquitto/mosquitto.conf
  - README.md
autonomous: false
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05, SRCH-06, CUBE-01, CUBE-02, CUBE-05, CUBE-06, DEP-01]
must_haves:
  truths:
    - "User types a query; results appear ranked; top result auto-highlights its cube; tapping another result re-highlights"
    - "An N×4×4 grid (N=2, 32 cubes) renders with row+col address overlays and desaturated empty cubes"
    - "Search debounces ~250 ms; clear-X empties the field; no-results state renders; loading shows only after ~300 ms"
    - "All component colors/fonts/motion come from design tokens — no hardcoded hex in component source"
    - "docker compose up brings up gruvax-api + mosquitto; gruvax-api serves the built SPA via FastAPI StaticFiles"
    - "mosquitto has no host ports: exposed; both services are healthchecked"
  artifacts:
    - path: "frontend/src/routes/kiosk/Cube.tsx"
      provides: "cube cell with data-state dim/lit/empty + address overlay (CUBE-02/05/06)"
      contains: "data-state"
    - path: "frontend/src/routes/kiosk/SearchBox.tsx"
      provides: "debounced typeahead + clear-X + delayed loading indicator (SRCH-03/05/06)"
      contains: "clear"
    - path: "frontend/src/state/store.ts"
      provides: "Zustand store: query, selectedReleaseId, highlight primaryCube"
      contains: "create"
    - path: "compose.yaml"
      provides: "gruvax-api + mosquitto (no ports on mosquitto, healthchecks)"
      contains: "eclipse-mosquitto"
    - path: "mosquitto/mosquitto.conf"
      provides: "persistence true; internal-only broker config"
      contains: "persistence"
  key_links:
    - from: "frontend/src/routes/kiosk/SearchBox.tsx"
      to: "/api/search"
      via: "TanStack Query fetch on debounced query"
      pattern: "api/search"
    - from: "frontend/src/routes/kiosk/ResultsList.tsx"
      to: "/api/locate"
      via: "fetch on result select → set highlight.primaryCube"
      pattern: "api/locate"
    - from: "frontend/src/main.tsx"
      to: "design/gruvax-design-tokens.css"
      via: "token import at app entry"
      pattern: "gruvax-design-tokens.css"
    - from: "compose.yaml"
      to: "FastAPI StaticFiles"
      via: "built SPA copied into static/ and served at /"
      pattern: "static"
---

<objective>
Build the React/Vite kiosk SPA exactly per 01-UI-SPEC.md — search box (debounce + clear-X + delayed
loading + no-results), ranked results list with top-result auto-highlight, and a 2×(4×4) cube grid with
address overlays + desaturated empty state + primary-cube highlight — consuming `design/
gruvax-design-tokens.css` (never hardcoded hex). Serve the built SPA via FastAPI StaticFiles, finish the
Docker Compose stack (`gruvax-api` + `mosquitto`, no broker ports, healthchecks), and prove the Core
Value end-to-end with `docker compose up`.

Purpose: This closes the walking skeleton — the first slice where a real user types a query and sees the
right cube light up (D-01 "demoable" = `docker compose up` + SPA in any browser). It is the client half
of the loop plus the deployment that makes the whole stack runnable.
Output: A demoable kiosk SPA + a working Compose stack serving it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-first-search-cube-highlight/01-CONTEXT.md
@.planning/phases/01-first-search-cube-highlight/01-RESEARCH.md
@.planning/phases/01-first-search-cube-highlight/01-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
@design/gruvax-design-language.md
@design/gruvax-design-tokens.css
@.planning/phases/01-first-search-cube-highlight/01-03-SUMMARY.md

<interfaces>
<!-- Component contracts are authoritative in 01-UI-SPEC.md. Build to those exact specs. API shapes
     come from plan 03; design tokens from design/gruvax-design-tokens.css. -->

API (plan 03): GET /api/search?q=&limit= → {items:[{release_id, title, primary_artist, label,
  catalog_number, format, year, rank}], took_ms}; GET /api/locate?release_id= → LocateResult
  {primary_cube:{unit_id,row,col}|null, label_span:[...], sub_cube_interval:null, confidence:float,
  estimator_version}; GET /api/units → {units:[{id, display_name, rows, cols, ordering}]}.

UI-SPEC component inventory: KioskView, SearchBox, ResultsList, ResultRow, NoResultsRow, ShelfGrid,
  Cube, ShelfLabel, NoResultsRow. Cube data-state ∈ {dim, lit, empty, hover}. Address scheme:
  Shelf A rows A–D, Shelf B rows E–H, columns 1–4 (e.g. "A1".."H4").

Design tokens (consume; never hardcode): --gruvax-cell-dim/-lit/-empty(+borders), --gruvax-yellow,
  --gruvax-blue, --gruvax-off-white, --gruvax-shadow-led, --gruvax-cell-size-xl (80px),
  --gruvax-cell-gap-xl (12px), --gruvax-cell-radius-xl (8px), --gruvax-led-on/off-duration/-ease,
  --gruvax-font-display/-ui/-mono, --gruvax-text-* sizes, --gruvax-space-* , --gruvax-radius-md.

Stack pins (version-corrected): vite@8, react@19.2, tailwindcss@4.3, @tanstack/react-query@5,
  zustand@5, react-router@7, gsap@3.15, motion@12.39.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vite/React scaffold + tokens wiring + ShelfGrid/Cube (CUBE-01/05/06)</name>
  <read_first>
    - .planning/phases/01-first-search-cube-highlight/01-UI-SPEC.md (§Grid Component Contract; §Layout; §Address Overlay; §Empty State; §Color cube cell states; §Typography)
    - .planning/research/01-RESEARCH.md (§Pattern 6 StaticFiles + Vite layout incl. vite.config.ts proxy + build outDir; §Code Examples Cube component + CSS)
    - design/gruvax-design-tokens.css (exact token names — consume only via var())
    - design/gruvax-design-language.md (Kallax cube atomic unit; lit cell always yellow; never recolor)
    - CLAUDE.md (consume tokens, never hardcode hex; three-font system)
  </read_first>
  <action>
    Scaffold `frontend/` with `npm create vite@latest frontend -- --template react-ts`; install
    react-router@7, @tanstack/react-query@5, zustand@5, gsap@3.15, motion@12.39, and dev tailwindcss@4.3.
    Set `vite.config.ts` to proxy `/api → http://localhost:8000` in dev and build to `outDir: '../static'`
    with `emptyOutDir: true`. Import `design/gruvax-design-tokens.css` in `main.tsx` (the SINGLE place the
    token contract enters). Build `ShelfLabel.tsx` ("SHELF A"/"SHELF B", Barlow Condensed 900 24px via
    `--gruvax-font-display`), `Cube.tsx` (props unitId,row,col,state,address; `data-state` ∈ dim/lit/empty;
    address overlay top-left), and `ShelfGrid.tsx` (CSS Grid `repeat(4, var(--gruvax-cell-size-xl))`, gap
    `--gruvax-cell-gap-xl`). Author `kiosk.css` referencing ONLY `var(--gruvax-*)` for all colors, sizes,
    radii, and transitions (dim/lit/empty per UI-SPEC §Color; LED on 300ms spring / off 500ms smooth).
    Create `frontend/src/api/types.ts` (SearchResult, LocateResult, Unit) matching plan 03 shapes. Render
    a static 2×(4×4) grid in `App.tsx`/`KioskView.tsx` driven by `GET /api/units` (or a stub units array
    if API not running locally) so the grid is visible before search wiring. Empty cubes use `data-state="empty"`.
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -rInE "#[0-9A-Fa-f]{6}" src/ | grep -v "design-tokens" || echo "NO_HARDCODED_HEX"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0 and emits `../static/index.html`
    - `frontend/src/main.tsx` imports `gruvax-design-tokens.css`
    - `frontend/src/routes/kiosk/Cube.tsx` contains `data-state` and renders an address overlay
    - No 6-digit hex literal appears in any `frontend/src/*` file (grep over src excluding token import prints `NO_HARDCODED_HEX`)
    - `ShelfGrid` uses `var(--gruvax-cell-size-xl)` for column/row sizing (token-driven, not 80px literal)
    - `vite.config.ts` sets `build.outDir` to `../static`
  </acceptance_criteria>
  <done>The kiosk renders a 2×(4×4) token-styled grid with address overlays and empty-state cubes; build emits into static/.</done>
</task>

<task type="auto">
  <name>Task 2: SearchBox + ResultsList + highlight wiring (SRCH-01..06, SRCH-02→CUBE-02)</name>
  <read_first>
    - .planning/phases/01-first-search-cube-highlight/01-UI-SPEC.md (§Search Box Contract: debounce/clear-X/loading; §Results List Contract; §Top-result auto-highlight behavior; §No-results state; §Animation Contract)
    - .planning/research/ARCHITECTURE.md (§State shape: Zustand highlight + TanStack Query keys)
    - frontend/src/api/types.ts + frontend/src/routes/kiosk/Cube.tsx (Task 1)
    - design/gruvax-design-tokens.css (motion + color tokens)
  </read_first>
  <action>
    Create `frontend/src/api/client.ts` (fetch wrappers for `/api/search`, `/api/locate`, `/api/units`)
    and `frontend/src/state/store.ts` (Zustand: `query`, `selectedReleaseId`, `highlight.primaryCube`,
    `animationToken`). Build `SearchBox.tsx`: 56px input per UI-SPEC, `--gruvax-blue` border, yellow focus
    ring, placeholder "Type artist, title, label or catalog#", a clear-X (≥44×44px tap target, shown only
    when non-empty, aria-label "Clear search") that empties query + clears highlight, client debounce
    ~250ms before firing the TanStack Query `['search', q]`, and a loading indicator (3-dot pulse) shown
    ONLY after a request has been in flight >300ms (delayed-reveal timer) with aria-label "Searching…".
    Build `ResultsList.tsx` (Framer Motion AnimatePresence enter 200ms / exit 150ms, max 6 visible rows),
    `ResultRow.tsx` (artist·title / label / catalog# in DM Mono via `--gruvax-text-mono`; selected state
    = `--gruvax-blue-faint` bg + yellow left border), and `NoResultsRow.tsx` ("No records found" + body
    copy per UI-SPEC §No-results). On results arrival: auto-select the top result, call `/api/locate`,
    set `highlight.primaryCube`; the grid transitions that cube to `data-state="lit"` (and all others to
    dim) via the token transitions. Tapping a different row re-runs locate + re-highlights. Clear-X / empty
    query returns all cubes to dim. If `primary_cube` is null, no cube highlights (empty-state behavior).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/routes/kiosk/SearchBox.tsx` contains a clear-X with `aria-label` "Clear search" and a debounce timer
    - `frontend/src/routes/kiosk/ResultsList.tsx` references `api/locate` (selection triggers locate) and renders `NoResultsRow` when items is empty and query is non-empty
    - The Zustand store exposes `highlight.primaryCube`; setting it flips the matching `Cube` to `data-state="lit"`
    - Loading indicator logic uses a >300ms delay timer (SRCH-05) and debounce ~250ms (SRCH-06) — both present in source
    - `cd frontend && npm run build && npx tsc --noEmit` exits 0
    - No hardcoded hex in any new component file (token-only)
  </acceptance_criteria>
  <done>Typeahead search renders ranked results with debounce/clear-X/delayed-loading/no-results; top result auto-highlights its cube; tapping another result moves the highlight.</done>
</task>

<task type="auto">
  <name>Task 3: FastAPI StaticFiles serving + compose.yaml + mosquitto.conf (DEP-01)</name>
  <read_first>
    - .planning/research/01-RESEARCH.md (§Pattern 6 StaticFiles mount + cache headers; §Pattern 8 compose.yaml; Pitfall 3 mount order; Pitfall 10 down -v)
    - .planning/research/ARCHITECTURE.md (illustrative compose.yaml; §Deployment; §Frontend hosting StaticFiles recommended)
    - .planning/phases/01-first-search-cube-highlight/01-CONTEXT.md (DEP-01 gruvax-api + mosquitto; mosquitto no ports)
    - src/gruvax/app.py (the StaticFiles mount placeholder left in plan 03)
    - Dockerfile (plan 01 — add the frontend build COPY stage)
  </read_first>
  <action>
    In `src/gruvax/app.py`, fill the StaticFiles placeholder: AFTER all `/api` routers, if `static/`
    exists, `app.mount("/", StaticFiles(directory="static", html=True), name="spa")` and serve
    `index.html` with `Cache-Control: no-store` while hashed assets get `immutable` (Pitfall 19). Extend
    the `Dockerfile` with a frontend build stage (node base → `npm ci` → `npm run build` producing
    `static/`) and COPY `static/` into the runtime image. Write `compose.yaml` per ARCHITECTURE/RESEARCH
    §Pattern 8: `gruvax-api` (build ., env DATABASE_URL + OBSERVED_DISCOGSOGRAPHY_SCHEMA default
    `gruvax_dev` + MQTT_* + LOG_LEVEL, `ports: ["8000:8000"]`, networks [internal, discogsography_default],
    `depends_on` mosquitto healthy, healthcheck hitting `/api/health`, `restart: unless-stopped`) and
    `mosquitto` (`eclipse-mosquitto:2.1-alpine`, mounts conf + passwd + data/log volumes, NO `ports:`,
    networks [internal], healthcheck, `restart: unless-stopped`). Declare named volumes and the external
    `discogsography_default` network. Write `mosquitto/mosquitto.conf` (`persistence true`,
    `persistence_location /mosquitto/data/`, listener on 1883 bound to the internal network only,
    password_file). Update `README.md` with the demo runbook: `just seed-dev` then `docker compose up`,
    open the SPA, and the `docker compose down` (NOT `-v`) note. Add a one-line STACK.md reconciliation
    note (Vite 8 / aiomqtt 2.5 / sse-starlette 3.4).
  </action>
  <verify>
    <automated>docker compose config -q && grep -c "ports:" compose.yaml && uv run python -c "import yaml,sys; c=yaml.safe_load(open('compose.yaml')); m=c['services']['mosquitto']; sys.exit(0 if 'ports' not in m else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config -q` exits 0 (valid compose file)
    - `compose.yaml` has NO `ports:` key under the `mosquitto` service (python yaml check exits 0)
    - `compose.yaml` defines services `gruvax-api` and `mosquitto`, both with a `healthcheck` and `restart: unless-stopped`
    - `mosquitto/mosquitto.conf` contains `persistence true`
    - `src/gruvax/app.py` mounts `StaticFiles(directory="static", html=True)` AFTER all `include_router` calls
    - Dockerfile runs as non-root user `gruvax` and copies the built `static/` directory
    - `README.md` documents `docker compose up` and the `down` (no `-v`) caveat
  </acceptance_criteria>
  <done>FastAPI serves the built SPA; `docker compose config` validates; mosquitto has no host ports and both services are healthchecked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — Core Value end-to-end demo (docker compose up + SPA)</name>
  <action>
    No new code. Claude first runs the full end-to-end stack autonomously (`just seed-dev` then
    `docker compose up -d`, confirm `docker compose ps` healthy, `curl /api/health` 200) and reports the
    results, THEN pauses for the human to visually verify the kiosk in a browser per the steps below.
    This checkpoint validates the demoable definition (D-01); the ~200 ms feel is a design target, not a
    hard gate (D-02).
  </action>
  <what-built>
    A demoable kiosk: `docker compose up` brings up `gruvax-api` (serving the React SPA via FastAPI
    StaticFiles) + `mosquitto`, against the synthetic `gruvax_dev` seed. The SPA shows a 2×(4×4) cube
    grid with address overlays; typing a query returns ranked results; the top result auto-highlights its
    cube; tapping another result moves the highlight; empty cubes are desaturated. This is the Core Value
    end-to-end slice (D-01 demoable definition).
  </what-built>
  <how-to-verify>
    1. From the repo root run `just seed-dev` (loads the synthetic gruvax_dev collection + 32 boundaries),
       then `docker compose up -d`.
    2. Run `docker compose ps` — confirm `gruvax-api` and `mosquitto` are both healthy.
    3. `curl -s http://localhost:8000/api/health` — confirm HTTP 200 with `discogsography_view_check` ok.
    4. Open `http://localhost:8000/` in any browser. Confirm: a 2×(4×4) grid (32 cubes) renders, each cube
       shows its row+col address (A1..H4), and some cubes are desaturated/dashed (empty).
    5. Type a seeded artist/label/catalog (e.g. a "Blue Note" label or "BLP 4195"). Confirm a ranked
       results list appears, the top result is selected, and exactly one cube lights yellow with the LED glow.
    6. Tap a different result row — confirm the highlight moves to that result's cube.
    7. Click the clear-X — confirm the field empties, results disappear, and all cubes return to dim.
    8. Type "zzznomatch" — confirm the "No records found" state renders and no cube highlights.
    9. Confirm responsiveness feels ~instant locally (D-02 design target, not a hard gate).
  </how-to-verify>
  <verify>
    <human-check>Human confirms steps 4–8 in the browser; Claude confirms steps 1–3 via CLI before pausing.</human-check>
  </verify>
  <done>Operator confirms the typed-query → cube-highlight Core Value works end-to-end via docker compose.</done>
  <resume-signal>Type "approved" or describe what looked wrong (which step, what you saw vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → gruvax-api (StaticFiles + /api) | served bundle + API on one origin; same input-validation surface as plan 03 |
| compose internal network → mosquitto | broker reachable only inside the Compose network (no host port) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Spoofing/Tampering | mosquitto LAN exposure | mitigate | No `ports:` on mosquitto in v1; internal-only network (DEP-01); password_file in conf (acceptance criterion) |
| T-01-13 | Information Disclosure | stale SPA bundle served from cache | mitigate | `index.html` served `Cache-Control: no-store`; hashed assets immutable (Pitfall 19) |
| T-01-14 | Tampering | StaticFiles catch-all intercepts /api | mitigate | StaticFiles mounted AFTER all /api routers (Pitfall 3; source-order acceptance criterion) |
| T-01-15 | Information Disclosure | hardcoded hex / off-token UI drift | accept | Enforced by grep gate (no hex in components); design-token contract is the single source |
| T-01-SC | Tampering | npm/uv installs (frontend + image) | mitigate | All packages registry-verified per RESEARCH audit; none [SUS]/[SLOP]; lockfiles (`uv.lock`, `package-lock.json`) committed |
</threat_model>

<verification>
- `cd frontend && npm run build && npx tsc --noEmit` green; no hardcoded hex in components.
- `docker compose config -q` valid; mosquitto has no host ports; both services healthchecked.
- Human checkpoint: `docker compose up` → SPA in browser → type → cube lights → tap → highlight moves → clear → dim → no-results state.
</verification>

<success_criteria>
A user can run `docker compose up`, open the SPA in any browser, type a query, and see the right cube
highlight on a 2×(4×4) grid with address overlays and desaturated empty cubes — the Core Value end-to-end.
All UI consumes design tokens; the Compose stack runs gruvax-api + mosquitto (no broker ports, healthchecked).
</success_criteria>

<output>
Create `.planning/phases/01-first-search-cube-highlight/01-04-SUMMARY.md` when done.
</output>
