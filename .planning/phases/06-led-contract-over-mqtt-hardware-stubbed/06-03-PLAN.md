---
phase: 06-led-contract-over-mqtt-hardware-stubbed
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - tests/unit/test_led_admin_endpoints.py
  - src/gruvax/mqtt/publishers.py
  - src/gruvax/api/admin/leds.py
  - src/gruvax/api/admin/router.py
  - frontend/src/api/adminClient.ts
  - frontend/src/routes/admin/Settings.tsx
autonomous: true
requirements: [LED-06, LED-07, DEP-03]
nyquist_compliant: true

must_haves:
  truths:
    - "An admin All-off button publishes an empty retained payload (payload=b'', retain=True) to EVERY state/{u}/{r}/{c} topic (cubes enumerated from gruvax.units) plus a command on all/off, idempotently — safe to call repeatedly, clears retained ghosts (LED-06 / D-11, Pitfall 3)"
    - "An admin diagnostic endpoint returns a run_id immediately and cycles every cube one-at-a-time through the configured state colors (label-span → position → error → setup → off) in a background task, logging each publish (LED-07 / D-08, D-09)"
    - "During the diagnostic run the server transiently subscribes to status/# (the prefix), logs whatever arrives, and times out (expected: nothing in v1) — wiring the future hardware status seam (LED-07 / D-10)"
    - "Both admin LED endpoints require require_admin (session + CSRF); degraded mode (client=None) returns gracefully without raising (DEP-03 / V4 access control, Pitfall C)"
    - "Cube enumeration uses SELECT id, rows, cols FROM gruvax.units — never hardcoded N=2/4x4 (Don't-Hand-Roll, works at unit 3)"
    - "The All-off + Run Diagnostic buttons live in the LEDs section of the existing Settings page (D-19)"
  artifacts:
    - path: "src/gruvax/api/admin/leds.py"
      provides: "POST /api/admin/leds/off (idempotent) + POST /api/admin/leds/diagnostic (BackgroundTask run_id) — both admin-gated"
      exports: ["router"]
    - path: "src/gruvax/mqtt/publishers.py"
      provides: "publish_all_off(client, pool, settings_cache) + run_diagnostic(client, pool, settings_cache, run_id) appended to Plan 01 module"
      contains: "async def publish_all_off"
    - path: "tests/unit/test_led_admin_endpoints.py"
      provides: "Tests for all-off enumeration/idempotency, diagnostic run_id + sequence, admin-gating, degraded mode"
      contains: "def test_all_off"
  key_links:
    - from: "frontend/src/routes/admin/Settings.tsx"
      to: "/api/admin/leds/off and /api/admin/leds/diagnostic"
      via: "ledsAllOff() / ledsDiagnostic() in adminClient"
      pattern: "ledsAllOff"
    - from: "src/gruvax/api/admin/leds.py"
      to: "publishers.publish_all_off / publishers.run_diagnostic"
      via: "await / BackgroundTasks.add_task"
      pattern: "publish_all_off|run_diagnostic"
    - from: "src/gruvax/api/admin/router.py"
      to: "leds_router"
      via: "router.include_router(leds_router)"
      pattern: "leds_router"
    - from: "src/gruvax/mqtt/publishers.py"
      to: "gruvax.units (DB)"
      via: "SELECT id, rows, cols FROM gruvax.units"
      pattern: "FROM gruvax.units"
---

<objective>
Deliver the all-off + diagnostic admin vertical slice: an idempotent "All off"
button that clears retained state for every cube, and a diagnostic endpoint that
cycles every cube through the configured color sequence in the background while
transiently listening for (future) hardware status — all admin-gated, all
observable end-to-end via `mosquitto_sub` with zero hardware.

Purpose: Closes LED-06 (all-off clear-retained), LED-07 (diagnostic sequence +
status logging), and completes the retained-state hygiene story (DEP-03 / Pitfall
3). The all-off path is also the authoritative retained-cleanup mechanism given
Mosquitto's expiry-cleanup limitation (RESEARCH Pitfall B).

Output: New admin/leds.py router (off + diagnostic), publisher functions
publish_all_off + run_diagnostic, and the two admin buttons wired into the LEDs
settings section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-CONTEXT.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-RESEARCH.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-PATTERNS.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-01-SUMMARY.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-02-SUMMARY.md

<interfaces>
<!-- Extracted from codebase / Plan 01 — use directly. -->

From Plan 01 (src/gruvax/mqtt/publishers.py): `safe_publish(client, topic, payload, *, qos, retain, properties, timeout)`, `_make_expiry_props(seconds)`, `hex_to_rgb(hex_str)`, `clamp_brightness(val, ceiling)`, `fan_out_illuminate(...)`. From src/gruvax/mqtt/topics.py: `state_topic`, `all_off_topic`, `diagnostic_topic`, `status_wildcard`, `illuminate_topic`. From src/gruvax/settings.py: `MQTT_TOPIC_PREFIX`, `MQTT_STATE_EXPIRY_SECONDS`.

Admin endpoint analog (src/gruvax/api/admin/segments.py): `router = APIRouter(tags=[...])`; handlers take `request: Request, pool: Any = Depends(get_pool), _admin: dict[str,str] = Depends(require_admin)`; return dict or JSONResponse. require_admin lives in src/gruvax/api/deps.py (session + CSRF, Phase 3).

Admin router factory (src/gruvax/api/admin/router.py): imports sub-routers INSIDE create_admin_router() body then router.include_router(...). Add leds_router the same way (it gets the /admin prefix + /api from app.py).

units table: `SELECT id, rows, cols FROM gruvax.units ORDER BY ordering` (column is `id`, not unit_id). Rows/cols are 0-indexed grids; enumerate range(rows) x range(cols).

aiomqtt 2.5.x subscribe/messages pattern (RESEARCH §Pattern 6): `await client.subscribe(topic, qos=1)`; `async with asyncio.timeout(5.0): async for msg in client.messages: ...`; `except TimeoutError: pass`; `finally: await client.unsubscribe(topic)`.

Frontend admin client (frontend/src/api/adminClient.ts): `adminFetch(path, opts)` CSRF wrapper (lines 53-77). Add ledsAllOff() and ledsDiagnostic() (RESEARCH §Settings.tsx / PATTERNS gives exact bodies). Settings.tsx LEDs section + settings-actions container exist from Plan 02 — append the two buttons.
</interfaces>

<diagnostic_params>
Claude's-discretion values locked here (RESEARCH §Open Questions 4 + D-09):
- Inter-cube delay: 200 ms (`led_diagnostic.inter_cube_ms`, default 200; read via settings_cache.get with default — optional, no migration needed, hardcode default acceptable).
- Color sequence per cube: label-span → position → error → setup → off (5 states, D-09), each published as a state/* payload using the configured colors from settings_cache.
- Between cubes use `await asyncio.sleep(INTER_CUBE_DELAY_S)` to yield the loop (Anti-pattern: don't block).
- Status subscribe window: 5 s asyncio.timeout after the cube loop (D-10).
- run_id is cosmetic in v1 (log + return; no polling endpoint — RESEARCH Open Question 5).
</diagnostic_params>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 tests — all-off enumeration/idempotency + diagnostic sequence + admin-gating (RED)</name>
  <files>tests/unit/test_led_admin_endpoints.py</files>
  <read_first>
    - tests/conftest.py (admin_session fixture; client fixture; MEMORY note: re-seed test PIN / re-seed boundaries before client fixture if needed)
    - tests/unit/test_mqtt_publishers.py (Plan 01 — the stub aiomqtt client pattern to reuse; AsyncMock recording publish/subscribe/unsubscribe calls)
    - src/gruvax/api/admin/segments.py (admin endpoint test analog: require_admin gating → 401/403 without session)
    - .planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-RESEARCH.md §Patterns 5/6/7 + §Validation Architecture (LED-06, LED-07 rows)
  </read_first>
  <action>
    Create `tests/unit/test_led_admin_endpoints.py` (RED). Use a stub aiomqtt client (AsyncMock recording publish/subscribe/unsubscribe) and a mocked or test pool returning a small units set (e.g. one unit id=1 rows=2 cols=2 → 4 cubes):

    Publisher-level tests (call publishers directly with the stub):
      - test_all_off: publish_all_off enumerates cubes from gruvax.units and publishes payload=b'' retain=True qos=1 to every state/{id}/{r}/{c} topic (4 cubes → 4 state clears) PLUS one command on all/off (LED-06, D-11).
      - test_all_off_idempotent: calling publish_all_off twice produces the same set of clear publishes both times (no error, idempotent) (D-11).
      - test_all_off_uses_units_table: assert the SQL fetch hits gruvax.units (no hardcoded N) — verify via the mocked cursor receiving the units SELECT (Don't-Hand-Roll).
      - test_diagnostic_sequence: run_diagnostic publishes, for each cube, the 5-state color sequence (label-span→position→error→setup→off); assert publish call count == cubes × states (+ subscribe/unsubscribe to status/# once) (LED-07, D-09).
      - test_diagnostic_status_subscribe: run_diagnostic calls client.subscribe(status_wildcard) and client.unsubscribe(...) exactly once, and times out gracefully with no status messages (D-10).
      - test_publishers_degraded: publish_all_off / run_diagnostic with client=None return without raising (Pitfall C).

    Endpoint-level tests (httpx + admin_session, app.state.mqtt = AsyncMock):
      - test_off_endpoint_requires_admin: POST /api/admin/leds/off without a session returns 401/403 (V4 access control).
      - test_diagnostic_endpoint_returns_run_id: POST /api/admin/leds/diagnostic (with admin_session) returns 200 with {"run_id": ..., "started_at": ...} (D-08).
      - test_off_endpoint_degraded: with app.state.mqtt=None, POST /api/admin/leds/off returns 200 {"published": 0} without raising.

    All MUST fail (RED): publishers functions + admin/leds.py do not exist yet.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_led_admin_endpoints.py -q 2>&1 | tail -15; echo "EXPECT failures — RED (publish_all_off / run_diagnostic / admin leds router not implemented)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `tests/unit/test_led_admin_endpoints.py`
    - Contains `def test_all_off`, `def test_diagnostic_sequence`, `def test_off_endpoint_requires_admin`, `def test_diagnostic_endpoint_returns_run_id`
    - test_all_off asserts payload=b'' retain=True to every state/* topic + one all/off command
    - test_diagnostic_status_subscribe asserts subscribe/unsubscribe to the status wildcard
    - Running the file fails (RED) — target functions/router not implemented
  </acceptance_criteria>
  <done>One Wave-0 test file encodes LED-06 idempotent all-off enumeration, LED-07 diagnostic sequence + status subscribe, admin-gating, and degraded mode; it fails because the targets do not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: publish_all_off + run_diagnostic in publishers.py (GREEN)</name>
  <files>src/gruvax/mqtt/publishers.py</files>
  <read_first>
    - src/gruvax/mqtt/publishers.py (Plan 01 — append to this module; reuse safe_publish, _make_expiry_props, hex_to_rgb, clamp_brightness, topics builders)
    - src/gruvax/mqtt/topics.py (state_topic, all_off_topic, status_wildcard, illuminate_topic)
    - src/gruvax/api/units.py (DB pool pattern: SELECT id, rows, cols FROM gruvax.units ORDER BY ordering; short-lived connection)
    - src/gruvax/db/queries.py (psycopg async %s placeholder + pool.connection() pattern)
    - .planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-RESEARCH.md §Pattern 5/6/7 + §Pitfalls B/C/G + §Anti-Patterns (don't hold DB conn through loop; asyncio.sleep to yield; don't subscribe too broad)
    - .planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-PATTERNS.md §publishers.py all-off + diagnostic patterns
    - tests/unit/test_led_admin_endpoints.py (the RED publisher tests)
  </read_first>
  <action>
    Append two functions to `src/gruvax/mqtt/publishers.py` (do not alter Plan 01 functions):

    `async def publish_all_off(client: aiomqtt.Client | None, pool, settings_cache: dict) -> int`: short-circuit if client is None (log warning, return 0 — Pitfall C). Open a SHORT-LIVED DB connection only to fetch units: `SELECT id, rows, cols FROM gruvax.units ORDER BY ordering`; close it before publishing (Anti-pattern: don't hold conn through the loop). Build a clear task per cube: `safe_publish(client, state_topic(prefix, unit_id, r, c), b'', qos=1, retain=True, timeout=0.5)` for every (unit_id in ids) × range(rows) × range(cols) (D-11, Pitfall 3 — empty retained payload deletes the retained message). Run them via `asyncio.gather(*tasks, return_exceptions=True)`. Then `safe_publish(client, all_off_topic(prefix), b'{}', qos=1, retain=False, timeout=0.5)` (the command topic — NOT retained, anti-pattern guard). Return the count of cube clears published. Idempotent by construction. Log `"LED all-off: published %d clear-retained payloads"`.

    `async def run_diagnostic(client: aiomqtt.Client | None, pool, settings_cache: dict, run_id: str) -> None`: short-circuit if client is None (log warning, return). Read INTER_CUBE_DELAY_S = int(settings_cache.get("led_diagnostic.inter_cube_ms", 200)) / 1000. Resolve the 5 state colors from settings_cache (.get with the seeded defaults). Fetch units via a short-lived DB connection (same SELECT). For each unit/row/col: publish a state/* payload (qos=1, retain=True, expiry props) for each of the 5 states in order label-span → position → error → setup → off, using the resolved color + appropriate clamped brightness (ambient for span, active for the rest; off = brightness 0 / color #000000); log each publish; `await asyncio.sleep(INTER_CUBE_DELAY_S)` between cubes to yield the loop. After the cube loop: `await client.subscribe(status_wildcard(prefix), qos=1)`; within `async with asyncio.timeout(5.0): async for msg in client.messages: logger.info("LED status from firmware: topic=%s payload=%s", msg.topic, msg.payload)`; `except TimeoutError: pass` (expected — no hardware); `finally: await client.unsubscribe(status_wildcard(prefix))` (D-10, Pattern 6, Pitfall G — status/# is disjoint from illuminate/*). Log `"LED diagnostic run_id=%s complete"`.

    Use `settings.MQTT_TOPIC_PREFIX` via the topics.py builders throughout. Use `%`-style logging.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_led_admin_endpoints.py -k "all_off or diagnostic or degraded" -x -q && uv run python -c "from gruvax.mqtt.publishers import publish_all_off, run_diagnostic; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/gruvax/mqtt/publishers.py` contains `async def publish_all_off(` and `async def run_diagnostic(`
    - publish_all_off SQL contains `FROM gruvax.units` (no hardcoded unit count) and publishes `b''` with `retain=True` to state topics
    - run_diagnostic subscribes to `status_wildcard(...)`, iterates `client.messages` under `asyncio.timeout(5.0)`, and unsubscribes in `finally`
    - both functions short-circuit on `client is None` without raising
    - command topic all/off is published with `retain=False`; state clears with `retain=True`
    - `uv run pytest tests/unit/test_led_admin_endpoints.py -k "all_off or diagnostic or degraded" -x` exits 0
  </acceptance_criteria>
  <done>publish_all_off (idempotent, units-enumerated, retained-clear) and run_diagnostic (cube×state sequence + status subscribe) exist and their publisher-level tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: admin/leds.py router + registration + admin UI buttons (GREEN, end-to-end)</name>
  <files>src/gruvax/api/admin/leds.py, src/gruvax/api/admin/router.py, frontend/src/api/adminClient.ts, frontend/src/routes/admin/Settings.tsx</files>
  <read_first>
    - src/gruvax/api/admin/segments.py (exact admin endpoint analog: require_admin guard, BackgroundTasks usage, pool dep, JSONResponse/dict return)
    - src/gruvax/api/admin/router.py (create_admin_router — add leds_router import + include_router INSIDE the function body)
    - src/gruvax/api/deps.py (require_admin signature — session + CSRF)
    - src/gruvax/mqtt/publishers.py (publish_all_off / run_diagnostic from Task 2)
    - frontend/src/api/adminClient.ts (adminFetch wrapper lines 53-77; existing export style)
    - frontend/src/routes/admin/Settings.tsx (Plan 02 LEDs section + settings-actions container — append All-off + Run Diagnostic buttons)
    - .planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-PATTERNS.md §admin/leds.py + §router.py MODIFY + §Settings.tsx buttons + §adminClient ledsAllOff/ledsDiagnostic (exact bodies)
    - tests/unit/test_led_admin_endpoints.py (the RED endpoint tests)
  </read_first>
  <action>
    Wire the admin endpoints and UI to complete the slice.

    `src/gruvax/api/admin/leds.py` (new): `router = APIRouter(tags=["admin-leds"])`. Both handlers admin-gated via `_admin: dict[str,str] = Depends(require_admin)`.
      - `@router.post("/leds/off")` `async def leds_all_off(request, pool=Depends(get_pool), _admin=Depends(require_admin)) -> dict`: read client = getattr(app.state, "mqtt", None) + settings_cache; `published = await publishers.publish_all_off(client, pool, settings_cache)` (0 if client None); return `{"published": published}`. Degraded mode returns 200 {"published": 0} (don't raise — Pitfall C).
      - `@router.post("/leds/diagnostic")` `async def start_diagnostic(request, background_tasks: BackgroundTasks, pool=Depends(get_pool), _admin=Depends(require_admin)) -> dict`: `run_id = str(uuid4())`; read client + settings_cache; `background_tasks.add_task(publishers.run_diagnostic, client=client, pool=pool, settings_cache=settings_cache, run_id=run_id)`; return `{"run_id": run_id, "started_at": datetime.now(UTC).isoformat()}` (D-08 — instant ack).

    `src/gruvax/api/admin/router.py` (modify): inside create_admin_router(), add `from gruvax.api.admin.leds import router as leds_router` and `router.include_router(leds_router)` (keeps /admin + /api prefixes from app.py).

    `frontend/src/api/adminClient.ts` (modify): add `export async function ledsAllOff(): Promise<{published: number}>` calling `adminFetch('/api/admin/leds/off', { method: 'POST' })`, throw on !ok, return json; add `export async function ledsDiagnostic(): Promise<{run_id: string; started_at: string}>` calling `adminFetch('/api/admin/leds/diagnostic', { method: 'POST' })`, throw on !ok, return json. (CSRF handled by adminFetch.)

    `frontend/src/routes/admin/Settings.tsx` (modify): in the LEDs section's `settings-actions` container (from Plan 02), append an "ALL OFF" secondary button (onClick → `void handleLedsAllOff()`) and a "RUN DIAGNOSTIC" secondary button (onClick → `void handleLedsDiagnostic()`). Add `handleLedsAllOff` (calls ledsAllOff, shows a transient "All off sent." status) and `handleLedsDiagnostic` (calls ledsDiagnostic, shows "Diagnostic started ({run_id})." status). Reuse the SaveStatus reset pattern. Surface errors via the existing ledsError slot.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_led_admin_endpoints.py -x -q && cd frontend && npm run build 2>&1 | tail -6 && npx eslint src/api/adminClient.ts src/routes/admin/Settings.tsx 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `src/gruvax/api/admin/leds.py` contains `@router.post("/leds/off")` and `@router.post("/leds/diagnostic")`, both with `Depends(require_admin)`
    - diagnostic handler uses `BackgroundTasks.add_task(publishers.run_diagnostic, ...)` and returns `run_id`
    - `src/gruvax/api/admin/router.py` contains `leds_router` import + `include_router(leds_router)`
    - `frontend/src/api/adminClient.ts` contains `export async function ledsAllOff` and `export async function ledsDiagnostic`
    - `frontend/src/routes/admin/Settings.tsx` LEDs section contains an "ALL OFF" and a "RUN DIAGNOSTIC" button wired to the handlers
    - `uv run pytest tests/unit/test_led_admin_endpoints.py -x` exits 0 (all-off, diagnostic, admin-gating, degraded all green)
    - `npm run build` exits 0 and eslint clean on touched files
  </acceptance_criteria>
  <done>POST /api/admin/leds/off + /diagnostic are live, admin-gated, registered; the Settings LEDs section has working All-off + Run Diagnostic buttons; all endpoint tests are GREEN and the frontend builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → POST /api/admin/leds/{off,diagnostic} | Authenticated admin actions (session + CSRF) triggering broker fan-out |
| gruvax-api → Mosquitto (status/#) | Transient subscribe during diagnostic; future hardware publishes here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Elevation of Privilege | POST /api/admin/leds/diagnostic and /off | mitigate | `Depends(require_admin)` (session + CSRF) on both handlers — verified by test_off_endpoint_requires_admin (V4) |
| T-06-13 | CSRF | All-off / diagnostic POST | mitigate | adminFetch double-submit `X-CSRF-Token` cookie pattern (Phase 3) on ledsAllOff/ledsDiagnostic |
| T-06-14 | DoS | Diagnostic blocking the event loop | mitigate | BackgroundTask + `await asyncio.sleep(INTER_CUBE_DELAY_S)` yields between cubes (Anti-pattern guard); returns run_id instantly (D-08) |
| T-06-15 | DoS | Broker hiccup during all-off | mitigate | safe_publish native timeout + `asyncio.gather(return_exceptions=True)`; degraded mode returns published:0 (Pitfall C) |
| T-06-16 | Info Disclosure | Diagnostic subscribe too broad (sees illuminate traffic) | mitigate | Subscribe only to `status/#` (disjoint from illuminate/*); broker-side filter (Pitfall G) |
| T-06-17 | Tampering | Stale retained ghosts on future hardware boot | mitigate | All-off is the authoritative clear-retained (empty payload) given Mosquitto expiry-cleanup limit (Pitfall B, D-11) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: zero new deps) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_led_admin_endpoints.py -q` green
- `uv run pytest -q` full suite green (no regression)
- `cd frontend && npm run build` exits 0; eslint clean on touched files
- MANUAL (broker up): `mosquitto_sub -t 'gruvax/v1/dev/leds/#' -v`; click ALL OFF → observe empty retained payloads on every state/{id}/{r}/{c} + an all/off command; click RUN DIAGNOSTIC → observe the cube×state sequence cycle and check api logs for per-publish lines + the status-subscribe timeout
- Idempotency: click ALL OFF twice → no errors, retained topics cleared both times
</verification>

<success_criteria>
- All-off publishes empty retained to every state/* + a command on all/off, idempotently (SC3, LED-06, D-11)
- Diagnostic cycles every cube through the documented color sequence in the background and logs each publish + any status responses (SC3, LED-07, D-08/09/10)
- Both endpoints are admin-gated; degraded mode never raises (DEP-03, V4)
- All-off + Run Diagnostic live in the Settings LEDs section — no new route (D-19)
- Retained-state hygiene complete: all-off is the authoritative clear (DEP-03, Pitfall 3/B)
</success_criteria>

<output>
Create `.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-03-SUMMARY.md` when done.
</output>
