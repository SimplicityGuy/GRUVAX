---
phase: 06-led-contract-over-mqtt-hardware-stubbed
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/unit/test_led_color.py
  - tests/unit/test_admin_led_settings.py
  - src/gruvax/api/admin/settings.py
  - frontend/src/api/types.ts
  - frontend/src/api/adminClient.ts
  - frontend/src/components/ColorBlindPreview.tsx
  - frontend/src/routes/admin/Settings.tsx
  - frontend/src/routes/admin/admin.css
autonomous: true
requirements: [LED-04, LED-05]
nyquist_compliant: true

must_haves:
  truths:
    - "Admin can tune label-span, position, error, setup, and all-off LED colors via a new LEDs section in the existing Settings page; this LED palette is a SEPARATE surface from the kiosk UI's Nordic Grid colors (kiosk lit cell stays yellow); values persist to gruvax.settings and refresh the settings_cache so the next illuminate uses them (LED-05 / D-04, D-05, D-15, D-19)"
    - "Admin can set two brightness ceilings — ambient (label-span ~30-50%) and active (position 100%) — and the server clamps every payload's brightness to the relevant ceiling (LED-04 / D-07)"
    - "Defaults are accessibility-respecting: position gold #FFD700, span purple #7C3AED — NOT red/green for active/error (LED-05 / D-05, Pitfall 18)"
    - "A lightweight color-blind preview (deuteranopia/protanopia/tritanopia matrix simulation) renders next to each color picker, in-SPA with no new packages (LED-05 / D-18)"
    - "The LEDs section reuses the existing Settings shell, SaveStatus state machine, and admin.css patterns — no new route (D-19)"
    - "Transition styles ship as fixed per-state defaults (defined in Plan 06-01's payload schema) with NO admin transition editor in v1 — the writable settings key allow-list deliberately excludes transition keys (D-17)"
    - "Color settings are stored as JSON strings (#RRGGBB) consistent with auth.pin_hash storage; the publisher resolves hex→RGB at publish time (Pitfall E)"
  artifacts:
    - path: "frontend/src/components/ColorBlindPreview.tsx"
      provides: "In-SPA color-blind simulation preview (3x3 matrix multiply, zero deps)"
      contains: "simulateColorBlindness"
    - path: "src/gruvax/api/admin/settings.py"
      provides: "Extended _ALLOWED_SETTINGS_KEYS + key_map + GET/PUT covering LED color/brightness keys"
      contains: "led_color.position"
    - path: "tests/unit/test_led_color.py"
      provides: "Tests for color-blind simulation invariants and the gold/purple distinguishability"
      contains: "def test_colorblind_preview"
  key_links:
    - from: "frontend/src/routes/admin/Settings.tsx"
      to: "/api/admin/settings (PUT)"
      via: "putAdminSettings with LED keys"
      pattern: "led_color"
    - from: "src/gruvax/api/admin/settings.py"
      to: "app.state.settings_cache"
      via: "load_settings_cache(pool) after PUT"
      pattern: "load_settings_cache"
    - from: "frontend/src/routes/admin/Settings.tsx"
      to: "ColorBlindPreview"
      via: "JSX render next to each color input"
      pattern: "ColorBlindPreview"
---

<objective>
Deliver the admin LED color + brightness configuration vertical slice: a new
"LEDs" section inside the existing admin Settings page (no new route) with a
per-state color picker, two brightness sliders, accessibility-safe defaults, and
an in-SPA color-blind preview — backed by extending the existing
`/api/admin/settings` GET/PUT to persist the LED keys into `gruvax.settings` and
refresh the settings_cache so the publisher built in Plan 01 immediately uses the
tuned colors and clamped brightness.

Purpose: Closes LED-04 (brightness ceilings) and LED-05 (per-state colors with
accessibility defaults + Pitfall 18 color-blind preview). After this slice the
admin can change what the LEDs will look like and verify accessibility before any
hardware exists.

Output: An admin LEDs settings section, a reusable ColorBlindPreview component,
and an extended settings endpoint persisting + caching LED presentation knobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-CONTEXT.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-RESEARCH.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-PATTERNS.md
@.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-01-SUMMARY.md
@./.claude/skills/sketch-findings-gruvax/SKILL.md

<interfaces>
<!-- Extracted from codebase — use directly. -->

Existing admin/settings.py contract (src/gruvax/api/admin/settings.py):
- `_ALLOWED_SETTINGS_KEYS = frozenset({"cube.nominal_capacity", "session.idle_ttl_seconds"})` (line 28)
- GET /settings returns `{cube_nominal_capacity, session_idle_ttl_seconds}` (lines 31-52)
- PUT /settings uses `key_map` {body_key: db_key} (lines 71-74), UPDATEs gruvax.settings ::jsonb, then refreshes `request.app.state.settings_cache = await load_settings_cache(pool)` (lines 93-98)
- Colors stored as JSON strings: pin_hash uses `(f'"{new_hash}"',)` pattern (settings.py line 168). Use the same `'"#FFD700"'` JSON-string form.

Frontend admin settings client (frontend/src/api/adminClient.ts):
- `getAdminSettings(): Promise<AdminSettings>` (line 129), `putAdminSettings(payload): Promise<Partial<AdminSettings>>` (line 143) — both wrap `adminFetch` (CSRF-aware).
- AdminSettings / AdminSettingsPut types in frontend/src/api/types.ts (lines 103, 112).

Settings.tsx patterns: `type SaveStatus = 'idle'|'saving'|'saved'|'error'`; section template at lines 110-166; `setTimeout(() => setStatus('idle'), 2000)` reset; load on mount via getAdminSettings (lines 41-48).

Color-blind matrices + simulateColorBlindness function are given verbatim in RESEARCH.md §Code Examples (deuteranopia/protanopia/tritanopia 3x3 matrices) — copy exactly.
</interfaces>

<dependency_note>
Plan 01 created src/gruvax/mqtt/publishers.py with `fan_out_illuminate` reading
`settings_cache.get("led_color.position", ...)` etc., and migration 0006 seeded
the 11 LED keys. This plan makes those keys ADMIN-EDITABLE and adds the UI.
The publisher already consumes them — no publisher change needed here.
</dependency_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 tests — color-blind simulation + admin LED settings persistence (RED)</name>
  <files>tests/unit/test_led_color.py, tests/unit/test_admin_led_settings.py</files>
  <read_first>
    - tests/conftest.py (admin_session fixture; client fixture; note MEMORY: admin_session may need re-seed — follow integration test pattern that re-seeds the test PIN)
    - tests/unit/test_queries.py (mocked-pool style) and a settings-touching test for the GET/PUT shape
    - src/gruvax/api/admin/settings.py (GET/PUT contract to assert against)
    - .planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-RESEARCH.md §"Validation Architecture" (LED-05 color-blind test row) + §Code Examples (matrices)
  </read_first>
  <action>
    Create two RED test files.

    `tests/unit/test_led_color.py` — pure-function tests for the color math (the Python side mirrors the TS matrices for parity; implement a small `gruvax`-side helper OR test via a JS-independent assertion of the matrix math expectations):
      - test_colorblind_preview: simulating the seeded gold #FFD700 and purple #7C3AED under deuteranopia produces two DISTINCT results (the Pitfall 18 guarantee — the safe pair stays distinguishable) (LED-05).
      - test_hex_to_rgb: hex_to_rgb("#FFD700") == (255, 215, 0); tolerates missing "#".
      Note: hex_to_rgb already lives in gruvax.mqtt.publishers (Plan 01) — import and assert it here.

    `tests/unit/test_admin_led_settings.py` — httpx AsyncClient + admin_session, asserting the extended endpoint:
      - test_get_settings_includes_led_keys: GET /api/admin/settings returns led_color_position, led_color_label_span, led_color_error, led_color_setup, led_color_all_off, led_brightness_ambient, led_brightness_active (defaults from migration 0006) (LED-04, LED-05).
      - test_put_led_colors_persists_and_caches: PUT /api/admin/settings {led_color_position: "#00FF00", led_brightness_ambient: 64} → returns updated list; re-GET reflects the new values; assert app.state.settings_cache["led_color.position"] == "#00FF00" after the PUT (settings_cache refresh, D-15).
      - test_put_rejects_unknown_led_key: a non-whitelisted key is ignored (not written).

    Both files MUST fail (RED) — the endpoint does not yet return/accept LED keys.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_led_color.py tests/unit/test_admin_led_settings.py -q 2>&1 | tail -15; echo "EXPECT failures — RED phase (LED keys not yet wired)"</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `tests/unit/test_led_color.py`, `tests/unit/test_admin_led_settings.py`
    - `tests/unit/test_led_color.py` contains `def test_colorblind_preview` and asserts gold vs purple stay distinct under deuteranopia
    - `tests/unit/test_admin_led_settings.py` contains `def test_put_led_colors_persists_and_caches` and asserts `settings_cache["led_color.position"]`
    - Running both files fails (RED) because GET/PUT do not yet handle LED keys
  </acceptance_criteria>
  <done>Two Wave-0 test files exist encoding LED-04/LED-05 persistence + color-blind distinguishability, and they fail because the endpoint is not yet extended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend admin/settings.py GET/PUT for LED color + brightness keys (GREEN)</name>
  <files>src/gruvax/api/admin/settings.py</files>
  <read_first>
    - src/gruvax/api/admin/settings.py (the EXACT file to modify — _ALLOWED_SETTINGS_KEYS line 28, GET lines 31-52, PUT key_map lines 71-74 + cache refresh lines 93-98)
    - migrations/versions/0006_led_settings_seed.py (the seeded keys + their JSON-string/number storage form — Plan 01)
    - src/gruvax/db/queries.py (load_settings_cache — refreshed after PUT)
    - tests/unit/test_admin_led_settings.py (the RED tests this task turns GREEN)
    - .planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-PATTERNS.md §admin/settings.py MODIFY (the exact frozenset + key_map extension)
  </read_first>
  <action>
    Extend `src/gruvax/api/admin/settings.py` (do not change unrelated PIN logic):

    Extend `_ALLOWED_SETTINGS_KEYS` to add: `led_color.position`, `led_color.label_span`, `led_color.error`, `led_color.setup`, `led_color.all_off`, `led_brightness.ambient`, `led_brightness.active`. (Transition keys are NOT admin-editable in v1 per D-17 — exclude them from the writable set.)

    Extend GET /settings to include the LED keys in the response object, reading from the same `settings_map` already fetched via `WHERE key = ANY(%s)`. Return body adds: `led_color_position` (default "#FFD700"), `led_color_label_span` ("#7C3AED"), `led_color_error` ("#E63946"), `led_color_setup` ("#0077B6"), `led_color_all_off` ("#000000"), `led_brightness_ambient` (int 128), `led_brightness_active` (int 255). Colors come back as JSON strings — strip surrounding quotes if the JSONB returns a quoted string (use the same accessor the existing code uses; values stored as `'"#FFD700"'` deserialize to the Python str `"#FFD700"`).

    Extend the PUT `key_map` with: `led_color_position`→`led_color.position`, `led_color_label_span`→`led_color.label_span`, `led_color_error`→`led_color.error`, `led_color_setup`→`led_color.setup`, `led_color_all_off`→`led_color.all_off`, `led_brightness_ambient`→`led_brightness.ambient`, `led_brightness_active`→`led_brightness.active`. For color values, store as a JSON string (wrap in quotes, e.g. `f'"{value}"'`) so the stored form matches the migration seed and Pitfall E; validate the value matches `#RRGGBB` (6 hex digits) before writing — reject malformed hex with HTTP 422 (Tampering mitigation). For brightness values, store the bare integer (0..255 clamp server-side) as `::jsonb`. Keep the existing `load_settings_cache(pool)` refresh after the writes so the publisher (Plan 01) sees new values immediately (D-15).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_admin_led_settings.py tests/unit/test_led_color.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/gruvax/api/admin/settings.py` `_ALLOWED_SETTINGS_KEYS` contains `led_color.position` and `led_brightness.active`
    - GET /settings response includes `led_color_position` and `led_brightness_ambient` keys
    - PUT key_map includes `led_color_position` → `led_color.position`
    - Color hex values are validated against `#RRGGBB` and stored as JSON strings; brightness stored as integers
    - PUT still calls `load_settings_cache(pool)` to refresh `app.state.settings_cache`
    - `uv run pytest tests/unit/test_admin_led_settings.py tests/unit/test_led_color.py -x` exits 0
  </acceptance_criteria>
  <done>GET/PUT /api/admin/settings persist + return the 7 LED color/brightness keys, validate hex, refresh the cache, and the admin-settings + color tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Admin LEDs settings section + ColorBlindPreview component (GREEN, UI slice)</name>
  <files>frontend/src/components/ColorBlindPreview.tsx, frontend/src/api/types.ts, frontend/src/api/adminClient.ts, frontend/src/routes/admin/Settings.tsx, frontend/src/routes/admin/admin.css</files>
  <read_first>
    - frontend/src/routes/admin/Settings.tsx (the file to modify — section template lines 110-166; SaveStatus union line 21; useState + load-on-mount lines 24-48; save handler pattern lines 50-67)
    - frontend/src/api/adminClient.ts (getAdminSettings/putAdminSettings lines 129-151 — extend payload with LED keys; adminFetch CSRF wrapper)
    - frontend/src/api/types.ts (AdminSettings line 103, AdminSettingsPut line 112 — extend with LED fields)
    - frontend/src/routes/admin/admin.css (existing settings-section / settings-field / settings-color-input / settings-range-input class conventions — add any missing classes)
    - ./.claude/skills/sketch-findings-gruvax/SKILL.md (validated admin section structure, CSS patterns, ALL-CAPS Barlow labels, design-token usage)
    - .planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-PATTERNS.md §Settings.tsx MODIFY (the LEDs section JSX + ColorBlindPreview matrices verbatim)
    - design/gruvax-design-tokens.json (Nordic Grid token swatches offered as presets: blue #0051A2, yellow #FFDA00, off-white #F7F9FC)
  </read_first>
  <action>
    Add the LEDs configuration UI. Reuse the existing Settings shell — NO new route (D-19).

    `frontend/src/components/ColorBlindPreview.tsx` (new): export a `ColorBlindPreview({ hex }: { hex: string })` component that renders three small swatches (DEUTERANOPIA / PROTANOPIA / TRITANOPIA) computed via the `simulateColorBlindness(hex, type)` function and the three 3x3 `MATRICES` copied VERBATIM from RESEARCH.md §Code Examples. Pure math, zero new packages (D-18). Each swatch shows the simulated color and a small mono label. Guard against malformed hex (return original on parse failure).

    `frontend/src/api/types.ts` (modify): extend `AdminSettings` and `AdminSettingsPut` with optional fields: `led_color_position`, `led_color_label_span`, `led_color_error`, `led_color_setup`, `led_color_all_off` (string), `led_brightness_ambient`, `led_brightness_active` (number).

    `frontend/src/api/adminClient.ts` (modify): no new functions needed — `putAdminSettings` already accepts `AdminSettingsPut`; the extended type covers LED keys. Confirm getAdminSettings return type carries the new fields.

    `frontend/src/routes/admin/Settings.tsx` (modify): add a new `<section className="settings-section" aria-labelledby="leds-heading">` titled "LEDS" (ALL CAPS Barlow per design language) placed after CAPACITY & TIMEOUT and before SEGMENT OVERRIDES. Add useState for the 5 colors (seeded from getAdminSettings on mount, defaults gold/purple/error/setup/black) and 2 brightness ceilings; add a `ledsStatus: SaveStatus` + `ledsError`. For EACH of the 5 states render: an ALL-CAPS label, a `<input type="color">` bound to its state, the Nordic Grid token swatches as quick-pick preset buttons (#0051A2, #FFDA00, #F7F9FC) next to the picker, and a `<ColorBlindPreview hex={color} />`. For brightness render two `<input type="range" min=0 max=255>` (AMBIENT / LABEL SPAN, and ACTIVE / POSITION) with a mono `<span>` showing the value. Add a `handleSaveLeds` that calls `putAdminSettings({led_color_position, ..., led_brightness_ambient, led_brightness_active})`, sets `ledsStatus='saved'`, and resets via `setTimeout(...2000)`. Include a "SAVE LED SETTINGS" primary button. (All-off + diagnostic buttons are added in Plan 03 — leave a stable `settings-actions` container so Plan 03 can append.)

    `frontend/src/routes/admin/admin.css` (modify): add any missing classes used above — `settings-color-input`, `settings-range-input`, `settings-value-mono`, `.colorblind-preview` row + swatch styles. Consume design tokens (var(--gruvax-*)); never hardcode hex for chrome (the LED colors themselves are user data, not chrome).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -8 && npx eslint src/components/ColorBlindPreview.tsx src/routes/admin/Settings.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ColorBlindPreview.tsx` contains `simulateColorBlindness` and the three matrices (deuteranopia/protanopia/tritanopia)
    - `frontend/src/routes/admin/Settings.tsx` contains a "LEDS" section, five `type="color"` inputs, two `type="range"` brightness inputs, `<ColorBlindPreview`, and `handleSaveLeds`
    - `frontend/src/api/types.ts` AdminSettings/AdminSettingsPut include `led_color_position` and `led_brightness_ambient`
    - `npm run build` exits 0 and `eslint` reports no errors on the two touched files
    - admin.css adds the color/range/preview classes (no hardcoded chrome hex; design tokens used)
  </acceptance_criteria>
  <done>The admin Settings page has a LEDs section with per-state color pickers, token presets, brightness sliders, and a color-blind preview; the frontend builds and lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → PUT /api/admin/settings | Authenticated admin input (session + CSRF) writing LED color/brightness values |
| settings_cache → publishers.fan_out_illuminate | Admin-tuned colors flow into published payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Elevation of Privilege | PUT /api/admin/settings (LED keys) | mitigate | Existing `require_admin` (session + CSRF) on the settings router — inherited from Phase 3 |
| T-06-08 | Tampering | LED color hex value | mitigate | Server validates `#RRGGBB` (6 hex digits) before write → HTTP 422 on malformed; stored as JSON string (Pitfall E) |
| T-06-09 | Tampering | Brightness value out of range | mitigate | Stored as integer; server-side `clamp_brightness` (Plan 01) bounds 0..255 at publish |
| T-06-10 | Tampering | Non-whitelisted settings key write | mitigate | `_ALLOWED_SETTINGS_KEYS` frozenset + key_map allow-list; unknown keys ignored |
| T-06-11 | Info Disclosure | Inaccessible color pair chosen by admin | mitigate | Color-blind preview (D-18) surfaces deuteranopia/protanopia/tritanopia rendering at pick time (Pitfall 18) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: zero new deps; color-blind preview is zero-dep matrix math) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_led_color.py tests/unit/test_admin_led_settings.py -q` green
- `uv run pytest -q` full suite green (no regression to existing admin settings / PIN tests)
- `cd frontend && npm run build` exits 0; `eslint` clean on touched files
- MANUAL (admin UI): open /admin/settings, scroll to LEDS; verify five color pickers, token presets, two brightness sliders, and three color-blind swatches per color; change position color + save; confirm the next kiosk illuminate (Plan 01 path) uses the new color via mosquitto_sub
</verification>

<success_criteria>
- Admin can tune label-span, position, error, setup, all-off colors + two brightness ceilings via the LEDs section; values persist and refresh the cache (SC2, LED-04, LED-05, D-15)
- Defaults are accessibility-respecting (gold/purple, not red/green) (SC2, LED-05, Pitfall 18)
- Color-blind preview renders for each color in-SPA with no new packages (LED-05, D-18)
- The LEDs surface is a section in the existing Settings page — no new route (D-19)
</success_criteria>

<output>
Create `.planning/phases/06-led-contract-over-mqtt-hardware-stubbed/06-02-SUMMARY.md` when done.
</output>
