---
phase: 04-sync-polish-diagnostics
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/property/test_nightly_scheduler.py
  - tests/unit/test_nightly_scheduler.py
  - tests/unit/test_session.py
  - tests/integration/sync/test_purge.py
  - tests/integration/api/test_diagnostics.py
  - tests/integration/api/test_admin_settings.py
autonomous: true
requirements: [SYN-01, SYN-02]
must_haves:
  truths:
    - "Every Phase 4 behavior named in 04-VALIDATION.md has a RED test that imports the (not-yet-existing) production symbol or asserts the (not-yet-present) response field"
    - "tests/property/test_nightly_scheduler.py expresses the always-future + 22-26h-window + monotonic invariants for next_fire_after()"
  artifacts:
    - path: "tests/property/test_nightly_scheduler.py"
      provides: "Hypothesis invariants for next_fire_after()"
      contains: "next_fire_after"
    - path: "tests/unit/test_nightly_scheduler.py"
      provides: "cadence anchoring + skip policy + off-parking + _read_sync_cadence fallback unit tests"
      contains: "test_skip_policy"
    - path: "tests/integration/sync/test_purge.py"
      provides: "purge self-clearing + audit-lineage-untouched integration tests"
      contains: "profile_collection"
    - path: "tests/integration/api/test_diagnostics.py"
      provides: "per-profile diagnostics section test (extends existing file)"
      contains: "test_profiles_section"
  key_links:
    - from: "tests/property/test_nightly_scheduler.py"
      to: "gruvax.sync.nightly"
      via: "import next_fire_after, now_local"
      pattern: "from gruvax.sync.nightly import"
---

<objective>
Wave 0 test scaffolding for Phase 4. Create the six RED test files (or test additions) named in 04-VALIDATION.md so every subsequent task has an `<automated>` verify that fails until its production code lands.

Purpose: Nyquist compliance — no Phase 4 behavior ships without a test written first. These tests import production symbols that do not exist yet (`gruvax.sync.nightly`) or assert response fields not yet present (`profiles[]`, `needs_reauth`, `sync.cadence`); they are EXPECTED to fail (RED) at the end of this plan.
Output: Six test files in RED state, covering SYN-01 (scheduler, cadence, skip policy, off-parking, startup catch-up) and SYN-02 (needs_reauth, diagnostics profiles[], purge, rotate-clears-revoked).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sync-polish-diagnostics/04-VALIDATION.md
@.planning/phases/04-sync-polish-diagnostics/04-RESEARCH.md
@.planning/phases/04-sync-polish-diagnostics/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scheduler property + unit RED tests (SYN-01)</name>
  <files>tests/property/test_nightly_scheduler.py, tests/unit/test_nightly_scheduler.py</files>
  <read_first>
    - tests/property/test_estimator_props.py (EXACT analog per 04-PATTERNS.md — Hypothesis `@given`/`@settings` structure, import conventions)
    - tests/unit/test_admin_led_settings.py (role-match analog per 04-PATTERNS.md — unit test for a settings/service component; AsyncMock pattern)
    - 04-RESEARCH.md §"Hypothesis Invariants for next_fire_after()" (lines ~723-761 — the two invariant test bodies are authoritative) and §"Validation Architecture" Test Map
    - 04-VALIDATION.md §"Per-Task Verification Map" (the SYN-01 rows — exact test node IDs: test_skip_policy, test_cadence_off)
  </read_first>
  <action>
    Create tests/property/test_nightly_scheduler.py importing `from gruvax.sync.nightly import next_fire_after, now_local` (module does not exist yet — RED). Implement the two Hypothesis invariants from 04-RESEARCH §Hypothesis Invariants verbatim: `test_next_fire_always_future` (result > now across 2025-2027 epoch range, hour 0-23, max_examples=500) and `test_next_fire_interval_in_22_26h_window` (successive hour=3 firings always 22-26 wall hours apart). Per D4-01.

    Create tests/unit/test_nightly_scheduler.py with the four SYN-01 unit behaviors from the Test Map, mocking `pool` and `sync_profile` with `unittest.mock.AsyncMock`: (a) cadence fire-time anchoring — `next_fire_after` derived from fire-hour lists {"24h":[3], "12h":[3,15], "6h":[3,9,15,21]} produces the soonest correct hour (D4-03); (b) `test_skip_policy` — the loop's profile-selection SQL excludes `app_token_revoked=TRUE` and `last_sync_status='in_progress'` rows (assert against a stubbed cursor return, D4-04); (c) `test_cadence_off` — when `_read_sync_cadence` returns `"off"`, one loop iteration parks (sleeps) and never calls `sync_profile` (D4-06); (d) `_read_sync_cadence` fallback — absent settings row returns `"24h"` (D4-06 / Pitfall 6).

    Reference the to-be-built symbols `_sync_loop`, `_read_sync_cadence`, `next_fire_after` from `gruvax.sync.nightly`. Tests MUST fail at import or assertion (RED) — do not stub the production module.
  </action>
  <verify>
    <automated>uv run pytest tests/property/test_nightly_scheduler.py tests/unit/test_nightly_scheduler.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/property/test_nightly_scheduler.py tests/unit/test_nightly_scheduler.py --co -q` COLLECTS the tests (import-time collection of test functions succeeds OR fails only on the missing `gruvax.sync.nightly` import — both acceptable RED states; document which)
    - tests/property/test_nightly_scheduler.py contains the string `next_fire_after` and a `@settings(max_examples=500)` decorator
    - tests/unit/test_nightly_scheduler.py defines test functions named `test_skip_policy` and `test_cadence_off` (grep: `grep -c "def test_skip_policy\|def test_cadence_off" tests/unit/test_nightly_scheduler.py` returns 2)
    - Running the tests (not --co) FAILS — confirms RED: `uv run pytest tests/property/test_nightly_scheduler.py -x -q` exits non-zero
  </acceptance_criteria>
  <done>Both scheduler test files exist and are RED (fail on missing gruvax.sync.nightly symbols).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Session + diagnostics + cadence-persistence RED tests (SYN-01/SYN-02)</name>
  <files>tests/unit/test_session.py, tests/integration/api/test_diagnostics.py, tests/integration/api/test_admin_settings.py</files>
  <read_first>
    - tests/unit/test_sessions.py and tests/integration/test_session_bootstrap.py (existing session-test analogs — fixture + ASGITransport client conventions)
    - tests/integration/test_diagnostics.py (EXISTING diagnostics integration test — note: the planner-targeted file is tests/integration/api/test_diagnostics.py per VALIDATION.md; reuse the fixtures/patterns from the existing test_diagnostics.py)
    - tests/integration/api/test_admin_sync_endpoint.py + tests/integration/conftest.py (admin API integration fixtures — admin auth, db pool, second_profile fixture from Phase 2)
    - 04-RESEARCH.md §"Validation Architecture" Test Map (rows: test_needs_reauth, test_profiles_section, test_sync_cadence) and §Pattern 4/5/6
    - 04-CONTEXT.md D4-07 / D4-08 / D4-15 / D4-06
  </read_first>
  <action>
    Create tests/unit/test_session.py (or add to the appropriate session unit test home if test_session.py is the canonical new file per VALIDATION.md) with `test_needs_reauth`: assert `GET /api/session` returns `needs_reauth: true` when the bound profile has `app_token_revoked=TRUE`, and `false`/absent otherwise (D4-08). Use the existing session bootstrap fixture + ASGITransport AsyncClient pattern.

    Add `test_profiles_section` to tests/integration/api/test_diagnostics.py (create the file under tests/integration/api/ if it does not exist; mirror the existing tests/integration/test_diagnostics.py fixtures): assert `GET /api/admin/diagnostics` JSON includes a `profiles` array where each entry has keys `id`, `display_name`, `last_sync_at`, `last_sync_status`, `last_sync_item_count`, `last_sync_error`, `app_token_revoked` (D4-15). Assert deleted profiles are excluded.

    Add `test_sync_cadence` to tests/integration/api/test_admin_settings.py (create under tests/integration/api/ if absent): PUT `/api/admin/settings` with `{"sync_cadence": "12h"}` then GET returns `sync_cadence: "12h"`; PUT an invalid value (e.g. `"7h"`) returns 422 with `type: "invalid_cadence"` (D4-06). PIN-gated — use admin auth fixture.

    All three assert behavior not yet implemented (RED).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_session.py tests/integration/api/test_diagnostics.py tests/integration/api/test_admin_settings.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - All three files collect (or fail only because the asserted fields are absent): `uv run pytest ... --co -q` lists `test_needs_reauth`, `test_profiles_section`, `test_sync_cadence`
    - `grep -c "needs_reauth" tests/unit/test_session.py` ≥ 1; `grep -c "profiles" tests/integration/api/test_diagnostics.py` ≥ 1; `grep -c "invalid_cadence\|sync_cadence" tests/integration/api/test_admin_settings.py` ≥ 1
    - Running each test FAILS (RED): `uv run pytest tests/unit/test_session.py::test_needs_reauth -x -q` exits non-zero
  </acceptance_criteria>
  <done>Session, diagnostics, and cadence-persistence tests exist and are RED.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Purge + rotate-clears-revoked RED tests (SYN-02)</name>
  <files>tests/integration/sync/test_purge.py</files>
  <read_first>
    - tests/integration/sync/test_sync_profile.py + tests/integration/sync/test_sync_cache_refresh.py (sync integration analogs — db pool fixture, profile seeding, advisory-lock awareness)
    - tests/integration/test_profile_manager_api.py (existing profile CRUD + rotate integration test — the rotate flow + second_profile fixture; test_rotate_clears_revoked belongs here per VALIDATION.md but may be added to test_purge.py or the profiles integration test — pick and note)
    - migrations/versions/0009_v2_profiles_and_collection_cache.py lines 113-148 (profile_collection DDL: `profile_id UUID NOT NULL REFERENCES gruvax.profiles(id) ON DELETE CASCADE`; PK is (profile_id, release_id)) and lines 162-188 (the 7 v1 tables fan-out)
    - 04-CONTEXT.md D4-11 / D4-12 / D4-13 / D4-09; 04-RESEARCH.md §Pitfall 4 (FK direction) and §"Critical Factual Confirmations" (D4-09 already wired)
    - VERIFIED FK FACT (record in test docstring): no `change_log`/`change_sets` tables shipped (PROF-04 note); the only audit table that exists is `boundary_history`; nothing REFERENCES `profile_collection`. The purge DELETE has zero cascade risk into audit tables.
  </read_first>
  <action>
    Create tests/integration/sync/test_purge.py with three behaviors (all RED — purge code lands in 04-01):

    (1) Purge self-clearing predicate (D4-12): seed a soft-deleted profile (`deleted_at IS NOT NULL`) that still has `profile_collection` rows; run the startup purge sweep (`gruvax.sync.nightly._startup_purge_sweep` or the equivalent symbol 04-01 exposes); assert `profile_collection` rows for that profile are zero afterward AND the predicate no longer matches it (idempotent — a second sweep is a no-op).

    (2) Audit lineage untouched (D4-13): before purge, capture row counts of `boundary_history`, `cube_boundaries`, `segment_overrides`, `record_stats`, `settings` for the soft-deleted profile; after purge assert those counts are UNCHANGED and the `profiles` row still exists (`deleted_at` still set — never hard-deleted, D4-14). Assert ONLY `profile_collection` rows were removed.

    (3) `test_rotate_clears_revoked` (D4-09): set a profile `app_token_revoked=TRUE`; perform a rotate-PAT (against the in-process fake-discogsography fixture so the test sync returns 200); assert `app_token_revoked` is `FALSE` afterward. (This verifies the already-wired reset path; the test is RED only because the integration harness for it is new.)

    Use parameterized `%s` SQL in any direct cursor assertions. Reference the purge symbol that 04-01 will expose; do not implement it here.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/sync/test_purge.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/sync/test_purge.py --co -q` collects three test functions including one named `test_rotate_clears_revoked`
    - File docstring records the verified FK fact: no `change_log`/`change_sets` tables exist; nothing references `profile_collection`; purge has zero audit-cascade risk (grep: `grep -c "change_log\|change_sets\|profile_collection" tests/integration/sync/test_purge.py` ≥ 1)
    - Running the tests FAILS (RED): `uv run pytest tests/integration/sync/test_purge.py -x -q` exits non-zero (missing purge symbol or unimplemented behavior)
  </acceptance_criteria>
  <done>Purge + rotate-clears-revoked tests exist and are RED; FK-safety fact documented in the test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → dev Postgres | Tests seed/read the shared `gruvax_dev` schema (per MEMORY: dev DB schema is gruvax_dev); no untrusted external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Tampering | test SQL fixtures | accept | Test-only code; parameterized `%s` SQL still used in assertions for consistency; no production surface. |
| T-04-00-SC | Tampering | npm/pip installs | accept | Zero new packages (04-RESEARCH §Package Legitimacy Audit: none); pytest/Hypothesis/httpx already in lockfile. |
</threat_model>

<verification>
- All six test files/additions exist and COLLECT.
- Running the full new set is RED (every test fails — production code is not written yet).
- No new dependencies added (uv lockfile unchanged).
</verification>

<success_criteria>
- `uv run pytest tests/property/test_nightly_scheduler.py tests/unit/test_nightly_scheduler.py tests/unit/test_session.py tests/integration/sync/test_purge.py tests/integration/api/test_diagnostics.py tests/integration/api/test_admin_settings.py --co -q` collects all targeted tests.
- Every test is RED (fails) — confirmed by a non-`--co` run exiting non-zero.
- The FK-safety fact (no change_log/change_sets; nothing references profile_collection) is documented in test_purge.py.
</success_criteria>

<output>
Create `.planning/phases/04-sync-polish-diagnostics/04-00-SUMMARY.md` when done.
</output>
