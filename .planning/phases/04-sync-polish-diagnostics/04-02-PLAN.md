---
phase: 04-sync-polish-diagnostics
plan: 02
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - src/gruvax/api/admin/diagnostics.py
autonomous: true
requirements: [SYN-02]
must_haves:
  truths:
    - "GET /api/admin/diagnostics returns a profiles[] array with last_sync_at, last_sync_status, last_sync_item_count, last_sync_error, app_token_revoked, id, and display_name for each non-deleted profile"
    - "The per-profile diagnostics data is gated behind require_admin and excludes soft-deleted profiles"
  artifacts:
    - path: "src/gruvax/api/admin/diagnostics.py"
      provides: "profiles[] section appended to GET /diagnostics response dict"
      contains: "profiles"
  key_links:
    - from: "src/gruvax/api/admin/diagnostics.py"
      to: "gruvax.profiles"
      via: "SELECT per-profile sync metadata WHERE deleted_at IS NULL"
      pattern: "FROM gruvax.profiles WHERE deleted_at IS NULL"
---

<objective>
Extend `GET /api/admin/diagnostics` with a per-profile `profiles[]` section carrying each non-deleted profile's sync metadata (D4-15). This is the data source for the frontend diagnostics cards (Plan 04-03, Surface 1).

Purpose: SYN-02 closure — per-profile `/admin/diagnostics` data so the cards can report `last_sync_at`, `last_sync_status`, `last_sync_item_count`, `last_sync_error` accurately.
Output: One DB query block + one new `profiles` key in the existing `get_diagnostics` return dict. No new endpoint, no schema change. Disjoint from 04-01's files → runs in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sync-polish-diagnostics/04-RESEARCH.md
@.planning/phases/04-sync-polish-diagnostics/04-PATTERNS.md
@.planning/phases/04-sync-polish-diagnostics/04-CONTEXT.md

<interfaces>
<!-- Contract this plan produces — consumed by Plan 04-03 (frontend cards). -->
GET /api/admin/diagnostics response (existing keys unchanged) gains:
  "profiles": [
    {
      "id": str,
      "display_name": str,
      "last_sync_at": str | null,   // ISO 8601
      "last_sync_status": "ok" | "failed" | "in_progress" | null,
      "last_sync_item_count": int | null,
      "last_sync_error": str | null,   // tagged: pat_rejected|network|rate_limited|server_error|cancelled
      "app_token_revoked": bool
    }, ...
  ]
Behind require_admin. Soft-deleted profiles (deleted_at IS NOT NULL) excluded. ORDER BY created_at.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: profiles[] section on GET /api/admin/diagnostics</name>
  <files>src/gruvax/api/admin/diagnostics.py</files>
  <read_first>
    - src/gruvax/api/admin/diagnostics.py lines ~44-108 (get_diagnostics — the require_admin guard line ~48, the existing DB-query + dict-comprehension pattern lines ~88-107, and the final `return {}` dict to extend)
    - 04-PATTERNS.md §"src/gruvax/api/admin/diagnostics.py" (EXACT query + dict-comprehension excerpt to follow) and §Shared Patterns (require_admin guard, parameterized SQL)
    - 04-RESEARCH.md §Pattern 5 (the SELECT + profile_diagnostics list comprehension — authoritative shape)
    - 04-CONTEXT.md D4-15 (per-profile cards live on /admin/diagnostics; the cards read last_sync_at/status/item_count/error verbatim); 04-UI-SPEC.md Surface 1 (the consuming card layout — confirms field set)
    - tests/integration/api/test_diagnostics.py::test_profiles_section (from 04-00 — RED→GREEN target)
  </read_first>
  <behavior>
    - GET /api/admin/diagnostics includes a `profiles` array; each entry has the 7 fields in the interfaces block; last_sync_at is ISO 8601 or null; app_token_revoked is a bool.
    - Soft-deleted profiles are excluded; ordering is by created_at.
    - All existing diagnostics keys (sync_age_seconds, top_searched, slow_queries, mqtt, pool, phantom_boundary_count, recent_logs) remain unchanged.
  </behavior>
  <action>
    In `get_diagnostics`, before the final `return {...}`, add one DB block following the existing query+comprehension pattern: `SELECT id::text, display_name, last_sync_at, last_sync_status, last_sync_item_count, last_sync_error, app_token_revoked FROM gruvax.profiles WHERE deleted_at IS NULL ORDER BY created_at` (parameterized — no f-strings; this query has no params so it is a plain literal SQL string). Build `profile_diagnostics` as a list of dicts mapping the 7 columns, with `last_sync_at` rendered via `.isoformat() if row[...] else None` and `app_token_revoked` via `bool(...)`. Add `"profiles": profile_diagnostics` to the return dict (D4-15). Do NOT alter any existing key. The `require_admin` dependency (line ~48) already covers this data — no new auth logic.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/api/test_diagnostics.py::test_profiles_section -x -q && uv run ruff check src/gruvax/api/admin/diagnostics.py && uv run mypy --strict src/gruvax/api/admin/diagnostics.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/api/test_diagnostics.py::test_profiles_section -x -q` PASSES (RED→GREEN)
    - Response includes `profiles` key with the 7 fields per entry; soft-deleted excluded (asserted by the test)
    - SQL is a literal string with no f-string interpolation: `grep -c 'f"SELECT\|f.SELECT' src/gruvax/api/admin/diagnostics.py` returns 0
    - Existing diagnostics keys untouched — `uv run pytest tests/integration/test_diagnostics.py -x -q` (the pre-existing system-level test) still green
    - ruff + mypy --strict clean
  </acceptance_criteria>
  <done>GET /api/admin/diagnostics returns profiles[]; system-level diagnostics unchanged; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → GET /api/admin/diagnostics | PIN-gated read of per-profile sync metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | profiles[] sync metadata | mitigate | Behind existing `require_admin` guard; no PAT/secret in the payload (only sync status/timestamps/counts/error-tags); last_sync_error is a fixed tag enum, not free-form secret-bearing text. |
| T-04-02-02 | Information Disclosure | cross-profile data | accept | Admin endpoint intentionally lists ALL non-deleted profiles (single-owner admin context, OOS-04 does not restrict the admin view — it restricts the non-admin per-session reads). |
| T-04-02-SC | Tampering | npm/pip installs | accept | Zero new packages (04-RESEARCH §Package Legitimacy Audit: none). |
</threat_model>

<verification>
- New per-profile section test green; pre-existing system diagnostics test green (no regression).
- ruff + mypy --strict clean.
- No schema change (head stays 0011).
</verification>

<success_criteria>
- `uv run pytest tests/integration/api/test_diagnostics.py::test_profiles_section tests/integration/test_diagnostics.py -x -q` green.
- profiles[] contract matches the interfaces block exactly (Plan 04-03 depends on it).
</success_criteria>

<output>
Create `.planning/phases/04-sync-polish-diagnostics/04-02-SUMMARY.md` when done.
</output>
