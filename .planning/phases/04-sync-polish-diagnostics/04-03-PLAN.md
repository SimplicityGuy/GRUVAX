---
phase: 04-sync-polish-diagnostics
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/src/api/session.ts
  - frontend/src/api/adminClient.ts
  - frontend/src/api/settings.ts
  - frontend/src/routes/kiosk/ReauthBanner.tsx
  - frontend/src/routes/kiosk/ReauthBanner.css
  - frontend/src/routes/kiosk/KioskView.tsx
  - frontend/src/routes/admin/Diagnostics.tsx
  - frontend/src/routes/admin/Diagnostics.css
  - frontend/src/routes/admin/ProfileDiagnosticsCard.tsx
  - frontend/src/routes/admin/ProfileDrawer.tsx
  - frontend/src/routes/admin/ProfileStatusBadge.tsx
  - frontend/src/routes/admin/SyncProgressSection.tsx
  - frontend/src/routes/admin/Settings.tsx
autonomous: true
requirements: [SYN-02]
must_haves:
  truths:
    - "When a bound profile's PAT is revoked, the kiosk renders a non-blocking inline re-auth banner and cube-search keeps working off the cached collection"
    - "The Profiles admin list/drawer shows a RE-AUTH REQUIRED badge for profiles with app_token_revoked=true, and it auto-clears after a successful rotate"
    - "/admin/diagnostics shows a PROFILES section with one Nordic-Grid card per non-deleted profile reporting last_sync_at, status, item count, and last error, refetching every 30s"
    - "The Sync-now path shows an indeterminate spinner with elapsed seconds until terminal, then fires a completion toast"
    - "/admin/settings has a SYNC CADENCE select (24h/12h/6h/off) that auto-saves and persists"
  artifacts:
    - path: "frontend/src/routes/kiosk/ReauthBanner.tsx"
      provides: "non-blocking kiosk re-auth inline banner (Surface 5)"
      contains: "reauth-banner"
    - path: "frontend/src/routes/admin/ProfileDiagnosticsCard.tsx"
      provides: "per-profile diagnostics card (Surface 1)"
      contains: "diag-profile-card"
    - path: "frontend/src/routes/admin/Diagnostics.tsx"
      provides: "ProfilesDiagnosticsSection wired to GET /api/admin/diagnostics profiles[] with 30s refetch"
      contains: "ProfilesDiagnosticsSection"
    - path: "frontend/src/routes/admin/Settings.tsx"
      provides: "SYNC CADENCE select control with auto-save"
      contains: "sync_cadence"
  key_links:
    - from: "frontend/src/routes/kiosk/KioskView.tsx"
      to: "frontend/src/routes/kiosk/ReauthBanner.tsx"
      via: "conditional render when needs_reauth / bound profile app_token_revoked"
      pattern: "ReauthBanner"
    - from: "frontend/src/routes/admin/Diagnostics.tsx"
      to: "GET /api/admin/diagnostics"
      via: "TanStack Query refetchInterval 30_000 reading profiles[]"
      pattern: "refetchInterval"
    - from: "frontend/src/routes/admin/Settings.tsx"
      to: "PUT /api/admin/settings"
      via: "auto-save onChange of sync_cadence select"
      pattern: "sync_cadence"
    - from: "frontend/src/routes/admin/ProfileStatusBadge.tsx"
      to: "profile.app_token_revoked"
      via: "re-auth-required status derivation"
      pattern: "re-auth-required"
---

<objective>
Wire the five Phase 4 UI surfaces (04-UI-SPEC.md) to the backend contracts from Plans 04-01 (`needs_reauth`, `sync.cadence`) and 04-02 (diagnostics `profiles[]`): kiosk re-auth banner, admin re-auth badge, per-profile diagnostics cards, Sync-now spinner+elapsed+toast, and the cadence select.

Purpose: SYN-02 closure — the user-observable polish that makes sync autonomous AND visible. All surfaces extend existing Phase 2/3/8 components; zero new dependencies (04-UI-SPEC Registry Safety).
Output: 2 new components (ReauthBanner, ProfileDiagnosticsCard) + 11 in-place edits. Consumes Nordic Grid design tokens only (no hardcoded hex). sketch-findings-gruvax CSS patterns apply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sync-polish-diagnostics/04-UI-SPEC.md
@.planning/phases/04-sync-polish-diagnostics/04-PATTERNS.md
@.planning/phases/04-sync-polish-diagnostics/04-CONTEXT.md
@.claude/skills/sketch-findings-gruvax/SKILL.md
@design/gruvax-design-language.md
@design/gruvax-design-tokens.css

<interfaces>
<!-- Backend contracts this plan consumes (from 04-01 + 04-02). -->
GET /api/session → SessionData gains: needs_reauth: boolean (true iff bound profile app_token_revoked).
  ProfileSummary already has app_token_revoked: boolean (frontend/src/api/session.ts).
GET /api/admin/diagnostics → DiagnosticsData gains: profiles: ProfileDiagnosticEntry[]
  ProfileDiagnosticEntry = { id, display_name, last_sync_at: string|null, last_sync_status: 'ok'|'failed'|'in_progress'|null, last_sync_item_count: number|null, last_sync_error: string|null, app_token_revoked: boolean }
GET/PUT /api/admin/settings → gains sync_cadence: '24h'|'12h'|'6h'|'off' (PUT validates; 422 invalid_cadence).
ProfileStatusBadge already supports 're-auth-required' status (profile-status-badge--re-auth-required CSS exists).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: API client types + kiosk re-auth banner (Surfaces 2 + 5)</name>
  <files>frontend/src/api/session.ts, frontend/src/api/adminClient.ts, frontend/src/api/settings.ts, frontend/src/routes/kiosk/ReauthBanner.tsx, frontend/src/routes/kiosk/ReauthBanner.css, frontend/src/routes/kiosk/KioskView.tsx, frontend/src/routes/admin/ProfileStatusBadge.tsx</files>
  <read_first>
    - frontend/src/api/session.ts (SessionData + ProfileSummary — app_token_revoked already present; add needs_reauth?: boolean)
    - frontend/src/api/adminClient.ts (DiagnosticsData type — add ProfileDiagnosticEntry + profiles[]) and frontend/src/api/settings.ts (GET/PUT settings shapes — add sync_cadence)
    - frontend/src/routes/kiosk/KioskView.tsx (where StalenessBar renders; sessionStore profiles + boundProfileId access) and frontend/src/routes/kiosk/StalenessBar.css (the staleness-bar-enter keyframe + system-bar styling to mirror)
    - frontend/src/routes/admin/ProfileStatusBadge.tsx (the existing status switch — confirm 're-auth-required' branch + profile-status-badge--re-auth-required class)
    - 04-UI-SPEC.md Surface 5 (kiosk banner: --gruvax-yellow bg, --gruvax-blue-darker text, Space Grotesk 18px, 12px×16px padding, role=alert aria-live=polite, KeyRound/AlertCircle inline SVG, NOT dismissible, copy: "Shelf data may be outdated — ask the owner to update the connection." — NO "PAT"/"token"/"API key") + Surface 2 (badge data-wiring only) + §Color (yellow reserved-for list) + §Accessibility
    - 04-PATTERNS.md §"frontend/src/routes/kiosk/ReauthBanner.tsx" (template) + §"ProfileDrawer re-auth badge wiring"; .claude/skills/sketch-findings-gruvax (Nordic Grid CSS patterns)
    - 04-CONTEXT.md D4-07, D4-08, D4-10 (non-blocking — search keeps working)
  </read_first>
  <action>
    Types: add `needs_reauth?: boolean` to `SessionData` in session.ts (D4-08); add `ProfileDiagnosticEntry` interface + `profiles: ProfileDiagnosticEntry[]` to `DiagnosticsData` in adminClient.ts (matching 04-02's interfaces block exactly); add `sync_cadence: '24h'|'12h'|'6h'|'off'` to the GET/PUT settings shapes in settings.ts.

    ReauthBanner.tsx (NEW, Surface 5): a presentational component taking `profileName?: string`, rendering `<div className="reauth-banner" role="alert" aria-live="polite">` with an inline Lucide-pattern SVG icon (`stroke="currentColor"`, 18×18, aria-hidden) + the exact copy "Shelf data may be outdated — ask the owner to update the connection." (do NOT mention PAT/token/API key). ReauthBanner.css (NEW): `.reauth-banner` consuming tokens only — background `var(--gruvax-yellow)`, color `var(--gruvax-blue-darker)`, Space Grotesk 18px/1.5, padding `var(--gruvax-space-3) var(--gruvax-space-4)`, full-width `border-radius: 0`, mount via the shared `staleness-bar-enter` keyframe (reference or co-locate from StalenessBar.css). NO hardcoded hex.

    KioskView.tsx: derive `needsReauth` and render `{needsReauth && <ReauthBanner profileName={boundProfile?.display_name} />}` directly below (or stacked with) the existing StalenessBar. Prefer `session.needs_reauth` (the authoritative D4-08 field); fall back to deriving `boundProfile?.app_token_revoked` from sessionStore if needed. CRITICAL (D4-10): banner is non-blocking — the search input, cube grid, and all kiosk interactivity remain live; do not gate any of them on needsReauth. Ensure the kiosk re-reads `GET /api/session` on a low-frequency interval (≤5 min) so a freshly-revoked PAT surfaces the banner without a manual reload (per 04-RESEARCH §Pitfall 5 / Open Question 2 — add a useQuery refetchInterval on the session bootstrap if one is not already present).

    ProfileStatusBadge.tsx (Surface 2): confirm/wire the `re-auth-required` status to render from `app_token_revoked=true`. The badge class already exists — this is data-wiring only (no CSS). Auto-clears on next refetch after a successful rotate (D4-07/D4-09).
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit && npm test -- --run ReauthBanner ProfileStatusBadge 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` passes (types for needs_reauth, ProfileDiagnosticEntry, sync_cadence compile)
    - `cd frontend && npm run lint` clean (no eslint errors — per MEMORY: keep frontend eslint-clean)
    - ReauthBanner.tsx contains the exact copy "Shelf data may be outdated — ask the owner to update the connection." and `role="alert"`; `grep -c "PAT\|API key\|token" frontend/src/routes/kiosk/ReauthBanner.tsx` returns 0
    - No hardcoded hex in ReauthBanner.css: `grep -ciE '#[0-9a-f]{3,6}' frontend/src/routes/kiosk/ReauthBanner.css` returns 0 (tokens only)
    - KioskView renders ReauthBanner conditionally and does NOT disable search on needsReauth (D4-10 — banner is additive)
    - MANUAL (UI-SPEC fidelity, deferred to /gsd-verify-work): kiosk banner renders + search still works after revoking a bound PAT
  </acceptance_criteria>
  <done>Types added; ReauthBanner built + rendered non-blocking on the kiosk; re-auth badge wired; tsc + lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Per-profile diagnostics cards section (Surface 1)</name>
  <files>frontend/src/routes/admin/ProfileDiagnosticsCard.tsx, frontend/src/routes/admin/Diagnostics.tsx, frontend/src/routes/admin/Diagnostics.css</files>
  <read_first>
    - frontend/src/routes/admin/Diagnostics.tsx (SystemStatusSection lines ~297-369 + StalenessSection lines ~73-97 = card-with-status-dots analog; the formatRelativeTime helper lines ~43-52 and stalenessStatus lines ~36-41; the existing data-load pattern lines ~469-476 and main return lines ~520-543; the existing .diag-heading is 900 — DO NOT reuse it)
    - frontend/src/routes/admin/Diagnostics.css (existing diag-* classes: settings-section, diag-status-row, diag-badge--{ok|stale|outdated|warning}, diag-cell-mono, diag-skeleton/diag-shimmer)
    - frontend/src/routes/admin/ProfileDrawer.tsx lines ~128-136 (the useQuery + refetchInterval analog for D4-16 polling)
    - 04-UI-SPEC.md Surface 1 (card layout: header = name Barlow Condensed 16px 700 --gruvax-blue + ProfileStatusBadge; rows LAST SYNC/STATUS/ITEMS/LAST ERROR; refetchInterval 30_000; loading = diag-skeleton; empty = "No profiles yet..."; focal point = the status badge) + §Typography (4-size cap; 700-weight PROFILES heading, NOT the 900 .diag-heading) + §Copywriting (all row labels + empty state + "Never synced") + §Color (no yellow on cards)
    - 04-PATTERNS.md §"Diagnostics.tsx" (ProfilesDiagnosticsSection template) + §"ProfileDiagnosticsCard.tsx" (card template) + §Shared Patterns (Nordic Grid section component, 202+poll refetchInterval)
    - 04-CONTEXT.md D4-15, D4-16; 04-02's interfaces block (ProfileDiagnosticEntry shape — this is the data source)
    - .claude/skills/sketch-findings-gruvax (Nordic Grid card CSS)
  </read_first>
  <action>
    ProfileDiagnosticsCard.tsx (NEW, Surface 1): props `{ profile: ProfileDiagnosticEntry }`. Render `<div className="diag-profile-card">` with: header row = display name (Barlow Condensed 16px 700, ALL CAPS, --gruvax-blue) + `<ProfileStatusBadge>` (the focal point — status from app_token_revoked + last_sync_status); four `diag-status-row` rows — LAST SYNC (DM Mono 14px relative time via a shared formatRelativeTime; "Never synced" when null), STATUS (diag-badge--{ok|stale|outdated} computed from last_sync_at age via stalenessStatus), ITEMS (DM Mono 14px `{N,###}` via toLocaleString or "—"), LAST ERROR (Space Grotesk 14px muted, last_sync_error truncated to 60 chars or "none"). Extract formatRelativeTime + stalenessStatus to a shared lib or import from Diagnostics.tsx (do not duplicate divergently). No yellow on the card (§Color).

    Diagnostics.tsx: add a `ProfilesDiagnosticsSection` sub-component (`<section className="settings-section">` + a 700-weight PROFILES heading class — NOT .diag-heading which is 900) that maps `data?.profiles ?? []` to `ProfileDiagnosticsCard`s inside `<div className="diag-profiles-grid">`, with the diag-skeleton loading state and the "No profiles yet. Create a profile to see sync diagnostics." empty state. Render it after `<SystemStatusSection>` and before `<RecentLogsSection>`. Wire 30s polling per D4-16: convert the diagnostics fetch to a TanStack `useQuery({ queryKey: ['admin','diagnostics'], queryFn: getDiagnostics, refetchInterval: 30_000 })` (or add a scoped useQuery just for this section if a full refactor of the existing imperative load is risky — pick the lower-risk option and note it); background refetch shows NO spinner (UI-SPEC Interaction States).

    Diagnostics.css: add `.diag-profiles-grid` (CSS Grid, `gap: var(--gruvax-space-5)`), `.diag-profile-card` (settings-section-like: --gruvax-off-white bg, --gruvax-shadow-sm, --gruvax-radius-lg, --gruvax-space-4 padding), and a 700-weight profiles-heading rule (24px Barlow Condensed). Tokens only — no hardcoded hex.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` and `npm run lint` clean
    - ProfileDiagnosticsCard renders all four rows (LAST SYNC/STATUS/ITEMS/LAST ERROR) — `grep -c "LAST SYNC\|STATUS\|ITEMS\|LAST ERROR" frontend/src/routes/admin/ProfileDiagnosticsCard.tsx` ≥ 4
    - Diagnostics.tsx adds ProfilesDiagnosticsSection with `refetchInterval` set to 30000 — `grep -c "refetchInterval" frontend/src/routes/admin/Diagnostics.tsx` ≥ 1
    - PROFILES heading uses a 700-weight class, NOT .diag-heading (the 900 class): the new heading class is distinct (grep confirms a new className, not `diag-heading`)
    - No hardcoded hex in new Diagnostics.css rules: `grep -ciE '#[0-9a-f]{3,6}' frontend/src/routes/admin/Diagnostics.css` does not increase for the new rules (tokens only — verify added lines use var(--gruvax-*))
    - Empty state copy present: `grep -c "No profiles yet" frontend/src/routes/admin/Diagnostics.tsx` ≥ 1
    - MANUAL (UI-SPEC fidelity): cards match Nordic Grid typography at /gsd-verify-work
  </acceptance_criteria>
  <done>PROFILES diagnostics section + per-profile cards render with 30s polling, Nordic Grid styling, 4-size type cap; tsc + lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sync-now spinner+elapsed+toast + cadence select (Surfaces 3 + 4)</name>
  <files>frontend/src/routes/admin/SyncProgressSection.tsx, frontend/src/routes/admin/ProfileDrawer.tsx, frontend/src/routes/admin/Settings.tsx</files>
  <read_first>
    - frontend/src/routes/admin/SyncProgressSection.tsx (current component — takes only itemCount; add syncStartedAt?: number|null + elapsed counter)
    - frontend/src/routes/admin/ProfileDrawer.tsx (handleSyncNow ~line 257, the 202+poll refetchInterval ~128-136, SyncProgressSection usage, the SyncToast firing on terminal; where to thread syncStartedAt = Date.now() on trigger; where to place the re-auth badge above the PAT input ~before line 334)
    - frontend/src/routes/admin/Settings.tsx (existing GENERAL SETTINGS section + the auto-save-on-change pattern + the settings-select / settings input class) and frontend/src/api/settings.ts (sync_cadence GET/PUT — added in Task 1)
    - frontend/src/components/SyncToast.tsx (no change needed — confirm it fires on terminal with "Sync complete — {N,###} records")
    - 04-UI-SPEC.md Surface 3 (spinner 20px --gruvax-yellow ring on --gruvax-blue-faint; "Syncing…" Space Grotesk 14px; elapsed "(Ns)" DM Mono 14px muted; aria-live polite + aria-busy; toast --gruvax-success, 4s, role=status) + Surface 4 (cadence select: label "SYNC CADENCE" Barlow Condensed 16px 700; width min(200px,100%); options "Every 24 hours"/"Every 12 hours"/"Every 6 hours"/"Off" → "24h"/"12h"/"6h"/"off"; sub-label "Syncs run at 03:00, 15:00 (12h), 09:00/21:00 (6h) server time." Space Grotesk 14px muted; auto-save onChange + "Saved" fade; min-height 44px; label htmlFor/id) + §Copywriting + §Animation
    - 04-PATTERNS.md §"SyncProgressSection.tsx" (elapsed counter excerpt) + §"ProfileDrawer.tsx"; 04-CONTEXT.md D4-17, D4-06
  </read_first>
  <action>
    SyncProgressSection.tsx (Surface 3, D4-17): add `syncStartedAt?: number | null` prop; add a `useEffect`+`setInterval(1000)` elapsed counter that clears on unmount / when syncStartedAt clears; render `(Ns)` in the existing `sync-progress-count` slot (DM Mono 14px muted) after "Syncing…". Keep `aria-live="polite"` + `aria-busy="true"`. No new CSS class — sync-progress-count already exists.

    ProfileDrawer.tsx: thread `syncStartedAt` — record `Date.now()` in state/ref when `handleSyncNow()` succeeds (the 202 accepted), pass it to SyncProgressSection, clear it on terminal. Confirm the existing 202+poll still drives in_progress → terminal and that SyncToast fires once on terminal with the completion copy "Sync complete — {N,###} records" (do not regress the 02-08 poll-until-terminal fix). Add the re-auth badge block above the PAT input section: when the drawer's profile status is 're-auth-required', render the existing badge + helper text (data-wiring; no new CSS).

    Settings.tsx (Surface 4, D4-06): add a labeled `<select>` "SYNC CADENCE" (Barlow Condensed 16px 700, ALL CAPS, --gruvax-blue) with id + associated `<label htmlFor>`; options Every 24 hours/12 hours/6 hours/Off mapping to values 24h/12h/6h/off; initialize from the `sync_cadence` value fetched on mount; auto-save on onChange via PUT /api/admin/settings with optimistic local update + a transient "Saved" confirmation (Space Grotesk 14px --gruvax-success, fades after 2s) and a "Could not save. Try again." error state; min-height 44px on the select; sub-label "Syncs run at 03:00, 15:00 (12h), 09:00/21:00 (6h) server time." (Space Grotesk 14px muted — subordinate via muted color, NOT a 5th type size). Reuse the existing settings-select/input class pattern; tokens only, no hardcoded hex.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` and `npm run lint` clean
    - SyncProgressSection takes `syncStartedAt` and renders an elapsed `(Ns)` counter — `grep -c "syncStartedAt\|elapsed" frontend/src/routes/admin/SyncProgressSection.tsx` ≥ 2
    - Settings.tsx adds a sync_cadence select with the four options and auto-save — `grep -c "sync_cadence\|SYNC CADENCE" frontend/src/routes/admin/Settings.tsx` ≥ 1; the four option values 24h/12h/6h/off present
    - Cadence sub-label uses 14px muted (not a 12px / 5th size) — confirm via class, not inline font-size
    - ProfileDrawer threads syncStartedAt and preserves the poll-until-terminal toast (no regression of 02-08)
    - No hardcoded hex in any new/changed CSS: tokens only
    - MANUAL (UI-SPEC fidelity): Sync now → spinner+elapsed → completion toast; cadence change persists — verified at /gsd-verify-work
  </acceptance_criteria>
  <done>Sync-now shows spinner + elapsed + completion toast; cadence select auto-saves/persists; re-auth badge in drawer; tsc + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk browser → GET /api/session | needs_reauth consumed to render banner. |
| admin browser → GET /api/admin/diagnostics, PUT /api/admin/settings | per-profile data display + cadence write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | kiosk re-auth banner copy | mitigate | Copy is deliberately non-technical ("Shelf data may be outdated…") — never reveals PAT/token/profile internals to a public-facing kiosk (acceptance: zero "PAT"/"token"/"API key" in ReauthBanner). |
| T-04-03-02 | Tampering | sync_cadence PUT value | mitigate | Backend validates against _CADENCE_VALUES (04-01); frontend select constrains to four options; no free-text. |
| T-04-03-03 | Information Disclosure | diagnostics cards | accept | Behind require_admin (admin route); displays only sync metadata, no secrets. |
| T-04-03-SC | Tampering | npm installs | mitigate | Zero new packages (04-UI-SPEC Registry Safety: none). If execution surfaces any [ASSUMED]/[SUS] package install, STOP — blocking-human checkpoint required (none expected). |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run lint` clean.
- 2 new components (ReauthBanner, ProfileDiagnosticsCard); 11 in-place edits.
- No hardcoded hex anywhere in P4 CSS/TSX — design tokens only.
- Type scale stays at four sizes (14/16/18/24); two font weights (400/700).
- Kiosk banner is non-blocking (D4-10); 02-08 poll-until-terminal toast not regressed.
- MANUAL verifications (kiosk banner + search-still-works; diagnostics Nordic-Grid fidelity + Sync-now spinner→toast) deferred to /gsd-verify-work per 04-VALIDATION §Manual-Only.
</verification>

<success_criteria>
- All five UI surfaces wired to the 04-01/04-02 backend contracts; tsc + lint clean.
- Re-auth badge auto-clears after successful rotate; kiosk banner non-blocking; cadence persists; Sync-now shows spinner+elapsed+toast.
- Full backend+frontend suite green at wave close (`uv run pytest tests/ -q --tb=short` + frontend build) — confirms no v1.0 invariant regression.
</success_criteria>

<output>
Create `.planning/phases/04-sync-polish-diagnostics/04-03-SUMMARY.md` when done.
</output>
