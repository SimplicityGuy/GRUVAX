---
phase: 02-real-position-estimation
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/state/store.ts
  - frontend/src/routes/kiosk/SubCubeBar.tsx
  - frontend/src/routes/kiosk/SpanUnderlay.tsx
  - frontend/src/routes/kiosk/Cube.tsx
  - frontend/src/routes/kiosk/ShelfGrid.tsx
  - frontend/src/routes/kiosk/ResultsList.tsx
  - frontend/src/routes/kiosk/KioskView.tsx
  - frontend/src/routes/kiosk/kiosk.css
autonomous: false
requirements: [CUBE-04, CUBE-03, CUBE-10, CUBE-08]
nyquist_compliant: true

must_haves:
  truths:
    - "The primary cube shows a horizontal sub-cube position bar whose width and offset come from sub_cube_interval; intensity attenuates with confidence (CUBE-04, D-01)"
    - "A singleton-label record renders a faint full-cube band, not a tick mark or zero-width bar (CUBE-10/D-02 reconciled)"
    - "When the label spans multiple cubes, a connecting underlay is drawn behind the spanned cubes and never recolors the lit primary cell (CUBE-03, D-04)"
    - "Selecting a result choreographs span fade-in → primary pulse → bar slide-in within <=600 ms and a new selection hard-cancels the in-flight animation (CUBE-08, D-05, D-06)"
    - "A '~' cue appears only when confidence <= 0.50 (TEXT_CUE_THRESHOLD); above it, uncertainty is visual-only (D-03)"
  artifacts:
    - path: "frontend/src/routes/kiosk/SubCubeBar.tsx"
      provides: "confidence-attenuated horizontal position bar; singleton full-cube band; '~' cue"
      contains: "SubCubeBar"
    - path: "frontend/src/routes/kiosk/SpanUnderlay.tsx"
      provides: "connecting underlay beneath spanned cubes; row/unit wrap handling; z-index below cubes"
      contains: "SpanUnderlay"
    - path: "frontend/src/state/store.ts"
      provides: "labelSpan + subCubeInterval + confidence fields + setLocateResult() action"
      contains: "setLocateResult"
  key_links:
    - from: "frontend/src/routes/kiosk/ResultsList.tsx"
      to: "frontend/src/state/store.ts setLocateResult"
      via: "locate response → setLocateResult(result) (replaces setHighlightCube)"
      pattern: "setLocateResult"
    - from: "frontend/src/routes/kiosk/KioskView.tsx"
      to: "GSAP timeline"
      via: "useEffect keyed on animationToken — kill() + sequential fromTo"
      pattern: "animationToken"
    - from: "frontend/src/routes/kiosk/Cube.tsx"
      to: "frontend/src/routes/kiosk/SubCubeBar.tsx"
      via: "renders SubCubeBar inside primary cube when subInterval present"
      pattern: "SubCubeBar"
---

<objective>
Make the kiosk answer "where exactly" visually: render the sub-cube position bar inside the primary cube (CUBE-04), a label-span connecting underlay behind spanned cubes (CUBE-03), the singleton faint full-cube band (CUBE-10/D-02), and the selection-lands animation choreography (CUBE-08) — all consuming the now-populated `LocateResult` from Plan 01's `/api/locate`. After this plan, selecting a search result plays the span → pulse → bar sequence and shows the position bar with confidence-driven attenuation.

Purpose: Implements the UI half of CUBE-03, CUBE-04, CUBE-10, and the full CUBE-08, within the Nordic Grid design system (tokens only; lit-cell rule preserved).

Output: `SubCubeBar` + `SpanUnderlay` components, extended `Cube`/`ShelfGrid`/`KioskView`, the Zustand `setLocateResult` action, the TS type extension, the GSAP timeline, and CSS using design tokens only.

This slice depends on 02-01 because it consumes the populated `sub_cube_interval`/`confidence`/`label_span` shapes the backend dispatcher now returns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-position-estimation/02-CONTEXT.md
@.planning/phases/02-real-position-estimation/02-RESEARCH.md
@.planning/phases/02-real-position-estimation/02-PATTERNS.md
@.planning/phases/02-real-position-estimation/02-UI-SPEC.md
@.planning/phases/02-real-position-estimation/02-VALIDATION.md
@.planning/phases/02-real-position-estimation/02-01-SUMMARY.md

<interfaces>
<!-- Backend JSON shape from Plan 01 (what /api/locate now returns). -->
sub_cube_interval JSON: { start: number, end: number, crosses_boundary: boolean, next_cube: CubeRef | null }
  (NOTE: the JSON has NO `cube` field — the Python dataclass has one but the serializer omits it.)
confidence: number   (0.30 = cube-only floor / singleton; <=0.50 shows "~" cue; >0.50 visual-only)
label_span: CubeRef[]  (sorted by unit_id,row,col — may have >=2 entries)

<!-- Existing TS shapes to extend. From frontend/src/api/types.ts -->
LocateResult.sub_cube_interval is currently typed `null` — change to `SubInterval | null`.
CubeRef: { unit_id, row, col }

<!-- Existing store. From frontend/src/state/store.ts -->
animationToken: number (already exists); setHighlightCube increments it via set((s) => ({..., animationToken: s.animationToken + 1}))
clearSearch resets highlight + animationToken to 0.

<!-- Locate call site. From frontend/src/routes/kiosk/ResultsList.tsx -->
top-result effect + handleSelect call locateRelease(id).then((result) => setHighlightCube(result.primary_cube))
  → REPLACE setHighlightCube(result.primary_cube) with setLocateResult(result).

<!-- Cube + grid analogs. From frontend/src/routes/kiosk/Cube.tsx / ShelfGrid.tsx -->
Cube({unitId,row,col,state,address}) returns div.cube[data-state][data-unit-id][data-row][data-col] with span.cube__address
ShelfGrid({unit, shelfIndex, litCube, emptyCubes}) double-loops r,c, computes isLit, pushes <Cube .../>; returns div.shelf-grid

<!-- Design tokens (consume; never hardcode hex). From design/gruvax-design-tokens.css -->
--gruvax-yellow, --gruvax-yellow-faint (rgba(255,218,0,0.12)), --gruvax-yellow-glow (rgba(255,218,0,0.35)),
--gruvax-text-muted, --gruvax-radius-sm (4px), --gruvax-radius-pill (9999px),
--gruvax-cell-size-xl (80px), --gruvax-cell-gap-xl (12px), --gruvax-space-1/2/3,
--gruvax-ease-standard, --gruvax-ease-decelerate, --gruvax-text-label (11px)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TS types + Zustand setLocateResult + ResultsList wiring</name>
  <files>frontend/src/api/types.ts, frontend/src/state/store.ts, frontend/src/routes/kiosk/ResultsList.tsx</files>
  <read_first>
    - frontend/src/api/types.ts (LocateResult.sub_cube_interval currently `null`; CubeRef shape)
    - frontend/src/state/store.ts (animationToken increment pattern in setHighlightCube; clearSearch resets)
    - frontend/src/routes/kiosk/ResultsList.tsx (both locate call sites: top-result effect + handleSelect)
    - .planning/phases/02-real-position-estimation/02-UI-SPEC.md (TypeScript Type Extension block; Zustand Store Extension Contract table)
    - .planning/phases/02-real-position-estimation/02-PATTERNS.md (types.ts + store.ts Pattern Assignments; Pitfall D — token must increment even on same-record re-selection)
  </read_first>
  <action>
    In `types.ts` add `export interface SubInterval { start: number; end: number; crosses_boundary: boolean; next_cube?: CubeRef }` (no `cube` field — matches the backend JSON serializer from Plan 01). Change `LocateResult.sub_cube_interval` from `null` to `SubInterval | null`. (Do NOT re-add `did_you_mean` — Plan 02 already added it to `SearchResponse`.)
    In `store.ts` add fields `labelSpan: CubeRef[]` (init `[]`), `subCubeInterval: SubInterval | null` (init `null`), `confidence: number` (init `0`). Add action `setLocateResult: (result: LocateResult) => void` that in ONE `set((s) => ({...}))` call sets `highlight: { primaryCube: result.primary_cube }`, `labelSpan: result.label_span`, `subCubeInterval: result.sub_cube_interval`, `confidence: result.confidence`, and `animationToken: s.animationToken + 1`. The token MUST increment unconditionally even when the same cube is re-selected (Pitfall D). Extend `clearSearch` to also reset `labelSpan: []`, `subCubeInterval: null`, `confidence: 0`. Import `SubInterval` and `LocateResult` from `../api/types`.
    In `ResultsList.tsx` replace both `setHighlightCube(result.primary_cube)` / `setHighlightCube(located.primary_cube)` calls with `setLocateResult(result)` / `setLocateResult(located)`; on the `.catch` paths keep clearing highlight (call `setHighlightCube(null)` or a reset). Pull `setLocateResult` from the store instead of `setHighlightCube` where appropriate.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` exports `interface SubInterval` with `start`, `end`, `crosses_boundary`, `next_cube?` and NO `cube` field
    - `types.ts` `LocateResult.sub_cube_interval` typed `SubInterval | null`
    - `store.ts` has `labelSpan`, `subCubeInterval`, `confidence`, and `setLocateResult`
    - `setLocateResult` increments `animationToken` in the same `set` call (grep: `animationToken: s.animationToken + 1` inside setLocateResult)
    - `clearSearch` resets `labelSpan`, `subCubeInterval`, `confidence`
    - `ResultsList.tsx` calls `setLocateResult` (grep: `setLocateResult`), not `setHighlightCube(result.primary_cube)`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The store captures the full locate result and bumps the animation token; ResultsList feeds the locate response into setLocateResult; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: SubCubeBar + SpanUnderlay components + Cube/ShelfGrid integration + CSS</name>
  <files>frontend/src/routes/kiosk/SubCubeBar.tsx, frontend/src/routes/kiosk/SpanUnderlay.tsx, frontend/src/routes/kiosk/Cube.tsx, frontend/src/routes/kiosk/ShelfGrid.tsx, frontend/src/routes/kiosk/kiosk.css</files>
  <read_first>
    - frontend/src/routes/kiosk/Cube.tsx (component props/structure analog for SubCubeBar; where to nest the bar)
    - frontend/src/routes/kiosk/ShelfGrid.tsx (geometry double-loop; optional-prop `??` fallback pattern; where to render SpanUnderlay)
    - frontend/src/routes/kiosk/kiosk.css (existing `.cube`, `.shelf-grid`, `.shelf-area` styles — add z-index/position carefully)
    - design/gruvax-design-tokens.css (all bar/underlay tokens listed in <interfaces>)
    - .planning/phases/02-real-position-estimation/02-UI-SPEC.md (SubCubeBar visual spec table + confidence→opacity mapping + crosses-boundary rendering + "~" cue; SpanUnderlay geometry/wrap algorithm + z-order rule + band formula; Layout Changes; New Color Contracts)
    - .planning/phases/02-real-position-estimation/02-PATTERNS.md (SubCubeBar.tsx + SpanUnderlay.tsx + Cube.tsx + ShelfGrid.tsx Pattern Assignments; CSS Token Consumption shared pattern)
  </read_first>
  <action>
    Create `SubCubeBar.tsx`: `interface SubCubeBarProps { interval: SubInterval; confidence: number; isSingleton: boolean }`. Returns an absolutely-positioned `<div className="sub-cube-bar">` (variant class `sub-cube-bar--singleton` when `isSingleton`). Compute width/left from the interval (`(end-start)*100%` / `start*100%`) for normal records; full-width for singletons. Pass `style={{ '--confidence': confidence, '--bar-left': ..., '--bar-width': ... } as CSSProperties}` and let CSS apply the opacity formula `max(0.35, 0.35 + var(--confidence) * 0.65)` (singleton fixed `0.18`). `pointer-events: none`. Render the `~` cue `<span className="sub-cube-bar__cue">~</span>` only when `confidence <= 0.50` (TEXT_CUE_THRESHOLD); set `aria-label="approximate position"` on the parent when low-confidence. Tokens only — no hex.
    Create `SpanUnderlay.tsx`: `interface SpanUnderlayProps { labelSpan: CubeRef[]; primaryCube: CubeRef; cellSize: number; cellGap: number }`. Group `labelSpan` by `(unit_id, row)` into runs (UI-SPEC wrap-detection algorithm); for each run compute one absolutely-positioned `<div className="span-underlay__band">` using the band formula (`left`, `width`, `top` from `cellSize`/`cellGap` and the unit's offset) — coordinate math, NOT `getBoundingClientRect()`. Cross-unit/row spans render multiple bands (no connector across the physical unit gap). Each band: layout values via inline `style`, color/border via CSS. No GSAP here (KioskView owns the timeline).
    Extend `Cube.tsx`: add optional props `subInterval?: SubInterval | null` and `confidence?: number`. When `subInterval` is present and this cube is the primary, render `<SubCubeBar interval=... confidence=... isSingleton={subInterval.start===0 && subInterval.end===1} />` as an absolutely-positioned child below the address overlay. Handle the companion bar for `crosses_boundary` (this cube === `next_cube`) per UI-SPEC. No layout change to the address overlay.
    Extend `ShelfGrid.tsx`: add optional props `labelSpan?: CubeRef[]`, `subCubeInterval?: SubInterval | null`, `confidence?: number` (use the `?? []`/`?? null` fallback pattern). Pass `subInterval`/`confidence` to the `Cube` matching `litCube`. Render `<SpanUnderlay>` as a sibling of `.shelf-grid` (within a `position: relative` container) when `labelSpan && labelSpan.length > 1`, reading `cellSize`/`cellGap` from the XL tokens.
    In `kiosk.css`: add `.cube { position: relative; z-index: 1 }` (lit-cell stays above underlay — D-04) and `.span-underlay__band { position: absolute; z-index: 0; background: var(--gruvax-yellow-faint); border: 1px solid var(--gruvax-yellow-glow); border-radius: var(--gruvax-radius-pill); height: var(--gruvax-space-3); opacity: 0 }`. Add `.sub-cube-bar { position: absolute; height: 8px; border-radius: var(--gruvax-radius-sm); background: var(--gruvax-yellow); opacity: max(0.35, calc(0.35 + var(--confidence) * 0.65)); pointer-events: none; z-index: 1 }`, `.sub-cube-bar--singleton { width: 100%; left: 0; opacity: 0.18 }`, `.sub-cube-bar__cue { color: var(--gruvax-text-muted); font: var(--gruvax-text-label)/... }`. Ensure `.shelf-area` (or the per-unit container) has `position: relative` so absolute bands anchor correctly (add if missing). Tokens only.
    CUBE-10 / D-02 RECONCILIATION (comment in SubCubeBar.tsx): REQUIREMENTS CUBE-10 literal is "tick-mark indicator"; per D-02 the owner overrides to a faint full-cube band — singletons render full-width at opacity 0.18, never a tick or zero-width bar (Pitfall 21).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -RInE "#[0-9a-fA-F]{3,6}" src/routes/kiosk/SubCubeBar.tsx src/routes/kiosk/SpanUnderlay.tsx; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `SubCubeBar.tsx` exports `SubCubeBar`; renders `--singleton` variant; `~` cue gated on `confidence <= 0.50`; `pointer-events: none`
    - `SpanUnderlay.tsx` exports `SpanUnderlay`; groups by `(unit_id, row)`; uses `cellSize`/`cellGap` math (grep: no `getBoundingClientRect`)
    - `Cube.tsx` renders `SubCubeBar` for the primary cube when `subInterval` is present
    - `ShelfGrid.tsx` renders `SpanUnderlay` only when `labelSpan.length > 1`
    - `kiosk.css` sets `.cube { ... z-index: 1 }` and `.span-underlay__band { ... z-index: 0 }` (D-04 lit-cell-above-underlay)
    - Zero hardcoded hex in `SubCubeBar.tsx`/`SpanUnderlay.tsx` (the grep in verify must find nothing — `test $? -ne 0` passes)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The position bar and span underlay render from LocateResult data with confidence attenuation, singleton full-cube band, row/unit wrap handling, and z-order that never recolors a lit cell; types compile and no hex literals leak in.</done>
</task>

<task type="auto">
  <name>Task 3: GSAP selection-lands timeline (CUBE-08) in KioskView with hard-cancel</name>
  <files>frontend/src/routes/kiosk/KioskView.tsx, frontend/src/routes/kiosk/ShelfGrid.tsx, frontend/src/routes/kiosk/kiosk.css</files>
  <read_first>
    - frontend/src/routes/kiosk/KioskView.tsx (existing useRef/useEffect cleanup pattern at lines 25-31, 65-78; how ShelfGrid is rendered + props spread at lines 134-144; store consumption)
    - frontend/src/state/store.ts (animationToken; labelSpan/subCubeInterval/confidence from Task 1)
    - frontend/src/routes/kiosk/ShelfGrid.tsx (the props added in Task 2; where refs must attach)
    - design/gruvax-design-tokens.css (--gruvax-ease-decelerate, --gruvax-ease-standard for ease parity)
    - .planning/phases/02-real-position-estimation/02-UI-SPEC.md (Selection-Lands Animation: per-step timing budget table; GSAP hard-cancel pattern; new-search interruption D-06; Pi 5 perf constraints — transform/opacity only, no box-shadow; singleton cross-fade variant)
    - .planning/phases/02-real-position-estimation/02-PATTERNS.md (KioskView.tsx Pattern Assignment — GSAP timeline pattern + useRef + cleanup; animationToken useEffect dep)
  </read_first>
  <action>
    In `KioskView.tsx` consume `animationToken`, `labelSpan`, `subCubeInterval`, `confidence` from the store and pass `labelSpan`/`subCubeInterval`/`confidence` into `<ShelfGrid>` (additive to the existing spread). Add `import gsap from 'gsap'`. Add refs: `timelineRef`, `spanUnderlayRef`, `primaryCubeRef`, `barRef`. Wire these refs to the rendered DOM nodes (the primary lit cube, its SubCubeBar, and the SpanUnderlay band) — thread ref callbacks through `ShelfGrid` → `Cube`/`SpanUnderlay` props, or query the live `litCube` node via a ref forwarded from those components.
    Add a `useEffect(() => {...}, [animationToken])` implementing the UI-SPEC GSAP timeline: `timelineRef.current?.kill()` (hard-cancel — D-06), `gsap.set(spanUnderlayRef.current, { opacity: 0 })`, `gsap.set(barRef.current, { scaleX: 0 })`, then a `gsap.timeline()` with steps (1) span underlay `opacity 0→0.60` 0.15s `power2.out`; (2) primary cube `scale 1→1.04` 0.10s `back.out(1.7)`; (2b) `scale →1` 0.10s `power2.inOut`; (3) bar `scaleX 0→1` 0.20s `power2.out` with `transformOrigin: 'left center'`, overlapped `-=0.10`. Total <=600 ms (budget 500 ms). For singletons (`subCubeInterval.start===0 && end===1`), replace step 3 with a `opacity 0→0.18` cross-fade (no scaleX slide) per UI-SPEC. Store the timeline in `timelineRef`; cleanup `return () => { tl.kill() }`. Animate `transform`/`opacity` ONLY — never `box-shadow` (Pi 5 / Pitfall 16). Add `will-change: transform, opacity` on the animated elements in `kiosk.css` (scoped, removed acceptable via cleanup or a class).
    Verify D-06 interruption: a new selection (or `clearSearch`) increments `animationToken`, re-firing the effect → `kill()` + `gsap.set()` reset + fresh timeline (no cross-fade between selections).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `KioskView.tsx` imports `gsap` and has a `useEffect` keyed on `[animationToken]` (grep: `[animationToken]`)
    - The effect calls `timelineRef.current?.kill()` and `gsap.set(...)` before building the new timeline (D-06 hard-cancel)
    - The timeline animates only `opacity`/`scale`/`scaleX` (grep KioskView for `boxShadow`/`box-shadow` — must be absent in GSAP tweens)
    - Total timeline duration sums to <=0.60s (steps 0.15 + 0.10 + 0.10 + 0.20 with -0.10 overlap = 0.45–0.55s)
    - `KioskView.tsx` passes `labelSpan`/`subCubeInterval`/`confidence` to `ShelfGrid`
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>Selecting a result plays span fade-in → primary pulse → bar slide-in within <=600 ms using transform/opacity only; a new selection hard-cancels and restarts the sequence; the singleton variant cross-fades; build and type-check pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify the position-estimation kiosk experience on the Pi (CUBE-08 SC-3 + D-01 visual judgment)</name>
  <action>
    All automated work (Tasks 1-3) is complete: types, store, components, GSAP timeline, and CSS build and type-check clean. This checkpoint validates the perceptual/timing behaviors that are not unit-assertable: the ≤600 ms lands-animation budget on real hardware (SC-3), the confidence→bar attenuation read (D-01), the singleton full-cube band (D-02), hard-cancel interruption (D-06), and the lit-cell-above-underlay rule (D-04). The ≤600 ms feel is a design target validated by eye on the Pi, not a CI assertion.
  </action>
  <what-built>
    The full Phase 2 position-estimation kiosk UI: sub-cube position bar with confidence attenuation, label-span connecting underlay, singleton faint full-cube band, and the selection-lands GSAP choreography with hard-cancel on new search.
  </what-built>
  <how-to-verify>
    1. Run `docker compose up` on `lux` (or `cd frontend && npm run dev` with the API running) and open the kiosk URL in Chromium on the Pi 5 + 7" touchscreen.
    2. Search a dense multi-record label (e.g. "Blue Note") and select the top result. Confirm: span underlay fades in, the primary cube springs/pulses, the sub-cube bar slides in — total feels within ~600 ms and stays smooth (CUBE-08 / SC-3).
    3. Search a singleton-label record. Confirm the bar is a faint FULL-cube band (not a tick or thin bar) and a "~" cue appears (low confidence — D-02/D-03).
    4. Compare a high-confidence dense label vs the low-confidence singleton — the bar should read crisp/bright vs faint/wide (D-01 attenuation).
    5. Select result A, then immediately select result B mid-animation. Confirm B's sequence runs fresh with NO cross-fade or "behind" feel (D-06 hard-cancel).
    6. Find a label that spans two cubes; confirm the connecting underlay draws behind both cubes and the lit primary cube is still full-saturation yellow on top (D-04 — underlay never recolors the lit cell).
  </how-to-verify>
  <verify>
    Manual on-device verification per the steps above; no automated command (perceptual timing + visual-attenuation judgment on the Pi 5 + 7" touchscreen).
  </verify>
  <done>Operator confirms the lands animation completes ≤600 ms and is interruptible, the singleton renders a faint full-cube band, confidence attenuation reads correctly, and the span underlay never recolors the lit primary cell.</done>
  <resume-signal>Type "approved" or describe issues (timing, attenuation read, wrap geometry, lit-cell tint, interruption feel).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/locate JSON → kiosk render | locate response (numeric interval, confidence, cube list) crosses into client rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | sub_cube_interval bounds in client render | mitigate | Backend guarantees 0<=start<=end<=1 (Plan 01 property tests); client computes width/left from those bounds without eval/innerHTML; values bound to CSS custom props, not DOM injection |
| T-02-12 | Information Disclosure | rendered position data | accept | Shows only physical shelf-position of owned records; no PII; home-LAN kiosk |
| T-02-13 | Denial of Service | GSAP timeline churn on rapid typing | mitigate | Hard-cancel via `kill()` per animationToken (D-06) prevents stacked timelines; transform/opacity-only keeps Pi 5 on the compositor (Pitfall 16) |
| T-02-SC | Tampering | npm installs | accept | No new packages — GSAP/Framer/Zustand already installed (RESEARCH Standard Stack: no additions); no install task |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` and `npm run build` succeed.
- Manual (Task 4): the lands animation completes <=600 ms on the Pi, is interruptible, the singleton renders a faint full-cube band, the underlay never recolors the lit cell.
- No hardcoded hex in any Phase 2 component (grep across `src/routes/kiosk/SubCubeBar.tsx`, `SpanUnderlay.tsx`, `DidYouMean.tsx`).
</verification>

<success_criteria>
- CUBE-04: confidence-attenuated sub-cube bar inside the primary cube; crosses-boundary companion bar (D-01).
- CUBE-10/D-02: singletons render a faint full-cube band, not a tick mark (reconciled in code comment).
- CUBE-03/D-04: label-span connecting underlay behind spanned cubes; never recolors a lit cell; row/unit wrap handled.
- CUBE-08/D-05/D-06: span → pulse → bar choreography <=600 ms, hard-cancellable, transform/opacity-only.
- Tokens only — no hardcoded hex in any new component.
</success_criteria>

<output>
Create `.planning/phases/02-real-position-estimation/02-03-SUMMARY.md` when done.
</output>
