---
phase: 02-real-position-estimation
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/run_all_algorithms.py
  - fixtures/synth_collection.py
  - tests/integration/test_run_all_algorithms.py
autonomous: true
requirements: [POS-06]
nyquist_compliant: true

must_haves:
  truths:
    - "A developer can run `uv run python scripts/run_all_algorithms.py --ci` and see per-distribution-shape MAE, timing, and confidence for §4.1 vs §4.8"
    - "On every synthetic planted-truth shape, §4.1 MAE is <= §4.8 MAE — proving §4.1 is the right v1 default (D-08 reframed: §4.1 >= §4.8 on synthetic shapes + runtime budget)"
    - "The harness runs the gitignored local CSV when present and skips it gracefully under --ci or when the CSV is absent (repo-hygiene: CSV never committed)"
    - "The harness genuinely differentiates the algorithms — it does not score §4.1 = 0.00 MAE on every shape by construction (Pitfall F)"
  artifacts:
    - path: "scripts/run_all_algorithms.py"
      provides: "developer A/B harness CLI (§4.1 vs §4.8, per-shape MAE/timing/confidence, --ci flag)"
      contains: "run_all_algorithms"
    - path: "fixtures/synth_collection.py"
      provides: "planted-truth synthetic shapes (shared with property tests from Plan 01)"
      contains: "make_sparse_gappy"
  key_links:
    - from: "scripts/run_all_algorithms.py"
      to: "gruvax.estimator.algorithm.locate / locate_cube_only"
      via: "runs both estimators against planted-truth collections"
      pattern: "locate"
    - from: "scripts/run_all_algorithms.py"
      to: "fixtures/synth_collection.py"
      via: "imports planted-truth shape factories"
      pattern: "synth_collection"
---

<objective>
Build the developer-facing A/B harness (POS-06) that runs §4.1 (index) and §4.8 (cube-only) against the synthetic planted-truth shapes and the gitignored local CSV, emitting per-distribution-shape error metrics — proving §4.1 >= §4.8 before §4.1 is locked as the v1 default (D-07/D-08). After this plan, `uv run python scripts/run_all_algorithms.py --ci` prints a per-shape comparison table and a CI test asserts §4.1 MAE <= §4.8 MAE on every shape.

Purpose: Implements POS-06 — the only tool that can genuinely differentiate the two algorithms on this data (D-07: a rank-only proxy cannot, because §4.1 is rank-based).

Output: `scripts/run_all_algorithms.py` (a standalone CLI, not in the pytest collection), planted-truth shape factories in `fixtures/synth_collection.py` (extended/finalized from Plan 01's Wave-0 scaffold), and a CI integration test asserting the §4.1 >= §4.8 invariant.

This slice depends on 02-01 because it imports `locate`/`locate_by_index`/`locate_cube_only` and the `make_*` factories created there.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-position-estimation/02-CONTEXT.md
@.planning/phases/02-real-position-estimation/02-RESEARCH.md
@.planning/phases/02-real-position-estimation/02-PATTERNS.md
@.planning/phases/02-real-position-estimation/02-VALIDATION.md
@.planning/phases/02-real-position-estimation/02-01-SUMMARY.md

<interfaces>
<!-- Estimators from Plan 01. From src/gruvax/estimator/algorithm.py -->
def locate(release_id, label, catalog_number, cache, snapshot) -> LocateResult  # §4.1 primary + §4.8 fallback
def locate_cube_only(release_id, label, catalog_number, cache) -> LocateResult   # §4.8 only
LocateResult.sub_cube_interval may be None (§4.8 / fallback). For MAE, §4.8 null → worst-case 0.5 (center-of-cube).

<!-- Planted-truth factories from Plan 01. From fixtures/synth_collection.py -->
make_uniform_dense(...) / make_sparse_gappy(...) / make_multi_prefix(...) / make_singleton(...)
  each returns (BoundaryCache, CollectionSnapshot, dict[int, float])  # dict = planted truth position per release_id
Pitfall F: sparse_gappy planted truth is GAP-WEIGHTED (NOT idx/(k-1)) so §4.1 shows non-zero MAE and the harness differentiates.

<!-- Repo hygiene (CLAUDE.md). Local CSV is gitignored and NEVER committed. -->
CI path: synthetic shapes only. Local path: read the gitignored RWlodarczyk-collection-*.csv + fixtures/boundaries.yaml.
Guard: skip the local-CSV path under --ci or when the CSV file is absent.

<!-- No analog for a CLI script. From 02-PATTERNS.md "No Analog Found" -->
Use standard argparse + `if __name__ == "__main__":`; --ci flag skips local CSV.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize planted-truth shape factories in fixtures/synth_collection.py</name>
  <files>fixtures/synth_collection.py</files>
  <read_first>
    - fixtures/synth_collection.py (the Wave-0 scaffold created in Plan 01 — extend/finalize; do not duplicate)
    - src/gruvax/estimator/collection_snapshot.py (RecordRow, `_load_snapshot` seam)
    - src/gruvax/estimator/boundary_cache.py (BoundaryRow, `_load_rows` seam)
    - src/gruvax/estimator/normalize.py (parse_key — truth must reflect physical shelf order via the same comparator)
    - .planning/phases/02-real-position-estimation/02-RESEARCH.md (§POS-06 A/B Harness Design — shape factories + Pitfall F differentiation requirement)
    - .planning/phases/02-real-position-estimation/02-PATTERNS.md (synth_collection.py Pattern Assignment — plain module, triplet return, seams not DB)
  </read_first>
  <action>
    Finalize the four planted-truth factories in `fixtures/synth_collection.py` (repo-root `fixtures/`, matching conftest FIXTURE_DIR): `make_uniform_dense(n_records, cube_capacity)`, `make_sparse_gappy(n_records, max_gap)`, `make_multi_prefix(n_records, prefixes)`, `make_singleton()`. Each returns `(BoundaryCache, CollectionSnapshot, dict[int, float])` built via `_load_rows`/`_load_snapshot` (no DB). The truth dict maps `release_id → planted physical-shelf position fraction in [0,1]`.
    Pitfall F (critical): plant truth as the PHYSICAL shelf truth, not §4.1's own output. For `uniform_dense`, truth = `idx/(k-1)` (this IS correct for that shape — validates §4.1). For `sparse_gappy`, truth = gap-weighted cumulative fraction (the §4.10 formula) so §4.1 (which ignores gaps) shows non-zero MAE while §4.8 (null → 0.5) shows higher MAE. For `singleton`, truth = 0.5 (center). Add a `def all_shapes() -> dict[str, callable]` helper returning the named factories so both the harness and the property tests (Plan 01) can iterate them.
  </action>
  <verify>
    <automated>uv run python -c "from fixtures.synth_collection import all_shapes, make_uniform_dense, make_sparse_gappy; cache, snap, truth = make_sparse_gappy(); assert isinstance(truth, dict) and len(truth) >= 2; assert set(all_shapes()) >= {'uniform_dense','sparse_gappy','multi_prefix','singleton'}"</automated>
  </verify>
  <acceptance_criteria>
    - `fixtures/synth_collection.py` defines `make_uniform_dense`, `make_sparse_gappy`, `make_multi_prefix`, `make_singleton`, and `all_shapes`
    - Each factory returns a 3-tuple `(BoundaryCache, CollectionSnapshot, dict[int, float])` built via `_load_rows`/`_load_snapshot` (grep: `_load_snapshot`; no `pool`/`await` in this module)
    - `sparse_gappy` planted truth is gap-weighted, NOT `idx/(k-1)` (Pitfall F — document with a comment)
    - The verify command exits 0
  </acceptance_criteria>
  <done>The shared planted-truth generator exposes all four shapes plus an `all_shapes()` registry, with sparse_gappy truth distinct from §4.1's formula so the harness can differentiate algorithms.</done>
</task>

<task type="auto">
  <name>Task 2: A/B harness CLI + CI assertion test</name>
  <files>scripts/run_all_algorithms.py, tests/integration/test_run_all_algorithms.py</files>
  <read_first>
    - fixtures/synth_collection.py (the finalized factories + `all_shapes` from Task 1)
    - src/gruvax/estimator/algorithm.py (locate / locate_cube_only signatures from Plan 01)
    - src/gruvax/estimator/contract.py (LocateResult.sub_cube_interval may be None — §4.8 → MAE worst-case 0.5)
    - .planning/phases/02-real-position-estimation/02-RESEARCH.md (§POS-06 metrics output format; CI vs local split + CSV guard; §4.8 null → worst-case 0.5 MAE)
    - .planning/phases/02-real-position-estimation/02-VALIDATION.md (POS-06 verification command `uv run python scripts/run_all_algorithms.py --ci`)
    - CLAUDE.md (repo hygiene — CSV + background/ never committed; CI uses synthetic dataset)
  </read_first>
  <action>
    Create `scripts/run_all_algorithms.py` (the `scripts/` dir does not exist yet — create it). Standard `argparse` CLI with a `--ci` flag (skip local CSV) and `if __name__ == "__main__":`. Define `run_all_algorithms(ci: bool = False) -> dict` returning a per-shape results structure. For each shape in `all_shapes()`: build `(cache, snapshot, truth)`, run BOTH `locate(...)` (§4.1) and `locate_cube_only(...)` (§4.8) for every planted release_id, and compute per-shape: MAE of the predicted position fraction vs planted truth (predicted = midpoint of `sub_cube_interval`; for §4.8 / null interval use worst-case 0.5 center-of-cube), per-call timing (assert aggregate p95 < 50 ms — POS-03 budget), and mean confidence. Print a per-shape table (`§4.1 MAE=… p95_ms=… conf=…` / `§4.8 MAE=… …`) and an aggregate timing line.
    Local-CSV path (only when NOT `--ci` and the gitignored CSV exists): load the real collection from `RWlodarczyk-collection-*.csv` + `fixtures/boundaries.yaml`, run the same metrics. Guard with `if ci or not local_csv_path.exists(): skip` — NEVER fail when the CSV is absent (repo hygiene; CI has no CSV).
    Create `tests/integration/test_run_all_algorithms.py`: `test_index_beats_or_ties_cube_only_on_all_shapes` calls `run_all_algorithms(ci=True)` and asserts, for every synthetic shape, `result[shape]["index"]["mae"] <= result[shape]["cube_only"]["mae"]` (D-08 reframed: §4.1 >= §4.8 on planted-truth shapes). Add `test_harness_aggregate_under_budget` asserting aggregate p95 < 50 ms. This file IS collected by pytest (the script itself is not). Mark the local-CSV portion skipped under CI.
  </action>
  <verify>
    <automated>uv run python scripts/run_all_algorithms.py --ci && uv run pytest tests/integration/test_run_all_algorithms.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/run_all_algorithms.py` exists with `def run_all_algorithms` and an `argparse` `--ci` flag
    - The script runs both estimators and prints per-shape MAE/timing/confidence for §4.1 and §4.8
    - §4.8 null interval is scored as worst-case 0.5 MAE (grep: a `0.5` center-of-cube fallback)
    - The local-CSV path is guarded so `--ci` (and absent CSV) never reads or requires the gitignored CSV
    - `tests/integration/test_run_all_algorithms.py` asserts `index.mae <= cube_only.mae` for every shape
    - `uv run python scripts/run_all_algorithms.py --ci` exits 0 and the pytest run exits 0
  </acceptance_criteria>
  <done>The A/B harness runs §4.1 vs §4.8 across planted-truth shapes (and the local CSV when present), emits per-shape metrics, stays under the 50 ms budget, and a CI test proves §4.1 MAE <= §4.8 MAE on every shape — without ever committing or requiring the local CSV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CSV file → harness (dev only) | the gitignored owner collection CSV is read locally; never committed (repo hygiene) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Information Disclosure | local collection CSV in CI artifacts | mitigate | `--ci` and absent-file guards prevent the CSV from being read in CI; CLAUDE.md repo hygiene keeps it gitignored; harness output is aggregate metrics, not row data |
| T-02-15 | Tampering | harness imports production estimator | accept | Read-only use of `locate`/`locate_cube_only`; no DB, no mutation; in-memory seams only |
| T-02-16 | Repudiation | A/B result used to lock §4.1 | accept | Result is a developer decision aid (D-08); not a runtime control surface; CI test makes the §4.1>=§4.8 claim reproducible |
| T-02-SC | Tampering | pip installs | accept | No new packages — harness uses stdlib argparse + existing estimator (RESEARCH: no additions); no install task |
</threat_model>

<verification>
- `uv run python scripts/run_all_algorithms.py --ci` prints a per-shape §4.1 vs §4.8 table and exits 0.
- `uv run pytest tests/integration/test_run_all_algorithms.py -x -q` is green (§4.1 MAE <= §4.8 MAE on every shape; aggregate p95 < 50 ms).
- `git status` shows no collection CSV staged (repo hygiene preserved).
</verification>

<success_criteria>
- POS-06: developer A/B harness exists, runs §4.1 and §4.8 against synthetic planted-truth shapes (CI) and the local CSV (when present, gitignored), and emits per-distribution-shape MAE/timing/confidence.
- §4.1 >= §4.8 proven on every synthetic shape (D-07/D-08); the harness genuinely differentiates (Pitfall F avoided).
- The CSV is never committed and never required in CI.
</success_criteria>

<output>
Create `.planning/phases/02-real-position-estimation/02-04-SUMMARY.md` when done.
</output>
