---
phase: 04-realtime-live-updates
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - frontend/src/routes/kiosk/Cube.tsx
  - frontend/src/routes/kiosk/ShelfGrid.tsx
  - frontend/src/routes/kiosk/KioskView.tsx
  - frontend/src/routes/kiosk/kiosk.css
  - frontend/src/state/store.connectivity.test.ts
autonomous: true
requirements: [RTM-04]
must_haves:
  truths:
    - "D-01: While the owner has a cube editor open on another device, the affected cube range on the kiosk shows a subtle ambient shimmer (mid-edit, driven by admin_editing) (RTM-04)"
    - "D-02: The shimmer is ambient and opacity-only — it never recolors a lit cell and never alters the cube's data-state"
    - "D-03: The shimmer clears when boundary_changed commits the cubes, and auto-clears after ~60s of no editing activity (client TTL safety)"
  artifacts:
    - path: "frontend/src/routes/kiosk/Cube.tsx"
      provides: "shimmerActive prop renders a decorative .cube-shimmer-overlay sibling"
      contains: "shimmerActive"
    - path: "frontend/src/routes/kiosk/kiosk.css"
      provides: "Opacity-only shimmer overlay animation (GPU-composited, Pi frame budget)"
      contains: "cube-shimmer-overlay"
    - path: "frontend/src/state/store.connectivity.test.ts"
      provides: "Tests for shimmer state on admin_editing + 60s TTL + clear-on-commit"
      contains: "shimmerExpiresAt"
  key_links:
    - from: "frontend/src/routes/kiosk/KioskView.tsx"
      to: "ShelfGrid shimmerCubes"
      via: "Zustand selector store.shimmerCubes → Set<unit-row-col> → ShelfGrid → Cube.shimmerActive"
      pattern: "shimmerCubes"
---

<objective>
Render the "boundaries updating" shimmer on the kiosk. While the owner has a cube editor open on another device, the `admin_editing` event (already received and stored as `shimmerCubes` by Plan 01, produced by Plan 03's heartbeat) drives a subtle ambient overlay on the affected cube range. The shimmer is opacity-only, never recolors a lit cell, never touches `data-state`, clears on commit, and auto-clears after the ~60s client TTL.

Purpose: Completes RTM-04 — the visible "boundaries updating" cue from roadmap criterion 1. Honors D-01 (mid-edit, before commit), D-02 (ambient shimmer, no text, never recolor a lit cell, LED-physics), and D-03 (clears on commit + 60s idle safety).
Output: the `shimmerActive` prop on Cube, the `shimmerCubes` pass-through through ShelfGrid, the Zustand-selector-driven shimmer set + 60s TTL sweeper in KioskView, the `.cube-shimmer-overlay` CSS, and the connectivity/shimmer store test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-realtime-live-updates/04-CONTEXT.md
@.planning/phases/04-realtime-live-updates/04-RESEARCH.md
@.planning/phases/04-realtime-live-updates/04-PATTERNS.md
@.planning/phases/04-realtime-live-updates/04-UI-SPEC.md
@.planning/phases/04-realtime-live-updates/04-VALIDATION.md
@.planning/phases/04-realtime-live-updates/04-01-SUMMARY.md
@.planning/phases/04-realtime-live-updates/04-03-SUMMARY.md

<interfaces>
<!-- Contracts extracted from the codebase + Plan 01 additions. -->

Store shimmer state (added by Plan 01 — already present): shimmerCubes: {unit,row,col}[], shimmerExpiresAt: number, setShimmerCubes(cubes) (sets shimmerExpiresAt = Date.now()+60_000), clearShimmerCubes(cubes). The admin_editing SSE listener (Plan 01) already calls setShimmerCubes/clearShimmerCubes; the boundary_changed listener (Plan 01) already calls clearShimmerCubes on commit (D-03 primary clear). This plan adds the RENDERING + the 60s TTL sweeper.

Cube.tsx: existing optional-prop pattern `isCompanionBar?: boolean` (~L26) and conditional JSX render (~L98); FillBar rendered ~L107. Add shimmerActive?: boolean and render the overlay div after FillBar.

ShelfGrid.tsx: renders the grid of <Cube>; accept a new prop `shimmerCubes: Set<string>` (keys "unit-row-col") and pass shimmerActive={shimmerCubes.has(`${unit}-${row}-${col}`)} per cell. ShelfGrid is rendered twice in KioskView JSX (~L301 and ~L321) — pass the prop to both.

KioskView.tsx: read shimmerCubes reactively with a Zustand selector `useGruvaxStore((s) => s.shimmerCubes)` and useMemo it into a Set<string>; pass to ShelfGrid. Add a useEffect that, while shimmerCubes is non-empty, sets a setTimeout against shimmerExpiresAt to clear expired shimmer (D-03 60s safety) — read shimmerExpiresAt via selector; clean up the timer on change/unmount.

CSS contract (UI-SPEC Surface 1, LOCKED): .cube-shimmer-overlay { position:absolute; inset:0; z-index:3; border-radius:inherit; pointer-events:none; background: var(--gruvax-yellow-faint); border: 1px solid var(--gruvax-yellow-glow); animation: shimmer-sweep 2000ms var(--gruvax-ease-standard) infinite; will-change: opacity; } and @keyframes shimmer-sweep { 0%{opacity:0} 40%{opacity:1} 60%{opacity:1} 100%{opacity:0} }. Tokens --gruvax-yellow-faint, --gruvax-yellow-glow, --gruvax-ease-standard ALL confirmed present in design/gruvax-design-tokens.css. Animate ONLY opacity (Pi frame budget, Pitfall 16) — never background/box-shadow/layout props. NEVER set data-state or a solid background on the overlay (never recolor a lit cell — D-02).

kiosk.css analogs: absolute-overlay pattern of .cube__address / .sub-cube-bar; @keyframes style of led-glow-pulse; will-change:opacity on .span-underlay__band. Token-only — zero hardcoded hex (file header comment enforces this).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store shimmer test — admin_editing sets shimmer, boundary_changed clears, 60s TTL (RED→GREEN)</name>
  <files>frontend/src/state/store.connectivity.test.ts</files>
  <read_first>
    - frontend/src/state/store.ts (the connectivity slice + shimmerCubes + shimmerExpiresAt + setShimmerCubes/clearShimmerCubes added by Plan 01)
    - .planning/phases/04-realtime-live-updates/04-VALIDATION.md (RTM-04 row → `npm test --run -- store`; Wave 0: store.connectivity test for shimmer + D-03 TTL)
    - .planning/phases/04-realtime-live-updates/04-RESEARCH.md (Pattern 6 store shape; § Validation Wave 0 store test)
    - frontend/src/routes/kiosk/ShelfGrid.test.tsx (existing vitest harness style)
  </read_first>
  <action>
    Create frontend/src/state/store.connectivity.test.ts (vitest). Reset the store before each test. Assert: setShimmerCubes([{unit:1,row:0,col:0}]) populates shimmerCubes and sets shimmerExpiresAt to roughly Date.now()+60_000 (use vi.useFakeTimers / a tolerance window); clearShimmerCubes([{unit:1,row:0,col:0}]) removes that cube and leaves others intact; setSseConnected(true) sets connectivity.sseConnected true and updates lastSeenAt; setSseConnected(false) leaves lastSeenAt unchanged. Add a TTL-expiry assertion describing the intended 60s safety clear (the sweeper itself is wired in Task 3 — if asserting the sweeper here, mark it pending until Task 3, otherwise assert only the shimmerExpiresAt math here).
  </action>
  <verify>
    <automated>cd frontend && npm test --run -- store.connectivity</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/state/store.connectivity.test.ts exists; `grep -q "shimmerExpiresAt" frontend/src/state/store.connectivity.test.ts` and `grep -q "setShimmerCubes" frontend/src/state/store.connectivity.test.ts`
    - From frontend/: `npm test --run -- store.connectivity` exits 0 (store-level shimmer + connectivity behavior green; the Plan-01 store slice satisfies these)
  </acceptance_criteria>
  <done>The store test proves admin_editing-driven shimmer state, per-cube clear, the 60s shimmerExpiresAt math, and connectivity flag behavior.</done>
</task>

<task type="auto">
  <name>Task 2: shimmerActive prop on Cube + shimmerCubes pass-through on ShelfGrid + .cube-shimmer-overlay CSS</name>
  <files>frontend/src/routes/kiosk/Cube.tsx, frontend/src/routes/kiosk/ShelfGrid.tsx, frontend/src/routes/kiosk/kiosk.css</files>
  <read_first>
    - frontend/src/routes/kiosk/Cube.tsx (the isCompanionBar?: boolean optional-prop pattern ~L26, prop destructure ~L52, conditional JSX ~L98, FillBar render ~L107)
    - frontend/src/routes/kiosk/ShelfGrid.tsx (how cells are mapped and <Cube> is rendered; existing prop interface)
    - frontend/src/routes/kiosk/kiosk.css (absolute-overlay rules for .cube__address / .sub-cube-bar; @keyframes led-glow-pulse; will-change:opacity on .span-underlay__band; token-only header convention)
    - .planning/phases/04-realtime-live-updates/04-UI-SPEC.md (Surface 1 — full .cube-shimmer-overlay + @keyframes shimmer-sweep contract; composition-with-lit-cell table; "never recolor a lit cell"; Pi frame budget = opacity-only)
    - .planning/phases/04-realtime-live-updates/04-PATTERNS.md (Cube.tsx section L533-557; kiosk.css section L561-587)
    - design/gruvax-design-tokens.css (confirm --gruvax-yellow-faint, --gruvax-yellow-glow, --gruvax-ease-standard exist — they do)
  </read_first>
  <action>
    Cube.tsx: add `shimmerActive?: boolean` to the props interface (default false), with a JSDoc noting it renders a decorative overlay and never recolors the cube (D-02). Render `{shimmerActive && <div className="cube-shimmer-overlay" aria-hidden="true" />}` inside the .cube container, after the FillBar — do NOT change data-state or any existing class.
    ShelfGrid.tsx: add `shimmerCubes: Set<string>` to its props; for each rendered cell pass `shimmerActive={shimmerCubes.has(`${unit}-${row}-${col}`)}`. Default to an empty Set if undefined to keep existing callers safe.
    kiosk.css: add the .cube-shimmer-overlay rule and @keyframes shimmer-sweep exactly per UI-SPEC Surface 1, using var(--gruvax-yellow-faint), var(--gruvax-yellow-glow), var(--gruvax-ease-standard); animate only opacity; will-change: opacity; z-index 3; pointer-events none; border-radius inherit. No hardcoded hex.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test --run -- ShelfGrid && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "shimmerActive" frontend/src/routes/kiosk/Cube.tsx` and `grep -q "cube-shimmer-overlay" frontend/src/routes/kiosk/Cube.tsx`
    - `grep -q "aria-hidden" frontend/src/routes/kiosk/Cube.tsx` (overlay is decorative)
    - `grep -q "shimmerCubes" frontend/src/routes/kiosk/ShelfGrid.tsx`
    - `grep -q "cube-shimmer-overlay" frontend/src/routes/kiosk/kiosk.css` and `grep -q "shimmer-sweep" frontend/src/routes/kiosk/kiosk.css`
    - `grep -A12 "cube-shimmer-overlay" frontend/src/routes/kiosk/kiosk.css | grep -E "box-shadow|background:\s*#|data-state"` returns nothing (opacity-only; never a solid background; never data-state)
    - `grep -cE '#[0-9A-Fa-f]{3,6}' frontend/src/routes/kiosk/kiosk.css` does not increase from the shimmer block (no new hardcoded hex — overlay uses tokens)
    - From frontend/: `npm run lint` exits 0, `npm test --run -- ShelfGrid` exits 0, `npm run build` exits 0
  </acceptance_criteria>
  <done>Cube renders a decorative opacity-only shimmer overlay when shimmerActive; ShelfGrid threads a shimmerCubes Set to each cell; the CSS animates only opacity using design tokens, never recolors a lit cell, never touches data-state.</done>
</task>

<task type="auto">
  <name>Task 3: Wire shimmerCubes selector into KioskView + 60s TTL sweeper</name>
  <files>frontend/src/routes/kiosk/KioskView.tsx</files>
  <read_first>
    - frontend/src/routes/kiosk/KioskView.tsx (the SSE consumer + connectivity wiring from Plan 01; the boundary_changed re-locate from Plan 02; the two <ShelfGrid> renders ~L301 and ~L321; existing useMemo for emptyCubes/fillLevels ~L63-80; existing timer-based useEffect L96-110)
    - frontend/src/state/store.ts (shimmerCubes + shimmerExpiresAt selectors; clearShimmerCubes)
    - .planning/phases/04-realtime-live-updates/04-UI-SPEC.md (Surface 1 React integration — derive shimmerActive in ShelfGrid via Set.has; 60s client TTL via setTimeout/useEffect)
    - .planning/phases/04-realtime-live-updates/04-PATTERNS.md (KioskView shimmerCubes prop pass-through L521-529; Zustand selector for reactive updates)
    - .planning/phases/04-realtime-live-updates/04-CONTEXT.md (D-03 — clears on commit AND ~60s idle safety)
  </read_first>
  <action>
    In KioskView.tsx: subscribe reactively with `const shimmerCubes = useGruvaxStore((s) => s.shimmerCubes)` and `const shimmerExpiresAt = useGruvaxStore((s) => s.shimmerExpiresAt)`; useMemo a `shimmerSet = new Set(shimmerCubes.map((c) => `${c.unit}-${c.row}-${c.col}`))` keyed on [shimmerCubes]; pass `shimmerCubes={shimmerSet}` to BOTH <ShelfGrid> renders. Add a useEffect keyed on [shimmerCubes, shimmerExpiresAt] that, when shimmerCubes is non-empty, schedules a setTimeout for (shimmerExpiresAt - Date.now()) that calls `useGruvaxStore.getState().clearShimmerCubes(useGruvaxStore.getState().shimmerCubes)` to enforce the D-03 60s safety clear; clear the timeout in the cleanup. Do NOT re-add or duplicate the admin_editing listener (Plan 01 already sets shimmerCubes) — this task only renders and enforces the TTL.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "useGruvaxStore((s) => s.shimmerCubes)" frontend/src/routes/kiosk/KioskView.tsx` (reactive selector, not getState in render)
    - `grep -c "shimmerCubes=" frontend/src/routes/kiosk/KioskView.tsx` is ≥ 2 (passed to both ShelfGrid renders)
    - `grep -q "shimmerExpiresAt" frontend/src/routes/kiosk/KioskView.tsx` and `grep -q "clearShimmerCubes" frontend/src/routes/kiosk/KioskView.tsx` (60s TTL sweeper present)
    - `grep -c "new EventSource" frontend/src/routes/kiosk/KioskView.tsx` equals 1 (single consumer preserved)
    - The 60s TTL sweeper has an ACTIVE (non-pending) automated assertion: store.connectivity.test.ts asserts `shimmerExpiresAt ≈ Date.now()+60_000` math directly on store state with `vi.useFakeTimers()` and is NOT marked `it.todo`/`it.skip`/`pending` — verify with `grep -E "it\\.(todo|skip)|\\.skip\\(" frontend/src/state/store.connectivity.test.ts` returning no shimmer/TTL match (the TTL assertion runs and passes under `npm test --run`)
    - From frontend/: `npm run lint` exits 0, `npm test --run` exits 0 (full frontend suite), `npm run build` exits 0
  </acceptance_criteria>
  <done>KioskView reactively passes the shimmer Set to both grids, the affected cube range visibly shimmers while the owner is mid-edit, and the shimmer auto-clears via the 60s TTL (and via boundary_changed on commit from Plan 01). Full frontend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin_editing event → kiosk render | Public kiosk renders an ambient cue derived from a server-fanned event |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Information Disclosure | Shimmer reveals which cubes the owner is editing to a public visitor | accept | The shimmer exposes only "this cube range is being edited" — no record text, catalog#, or PII. Cube identity is already public via GET /api/cubes. Acceptable for a home-LAN kiosk with visiting friends (PROJECT.md). |
| T-04-13 | Denial of Service | Shimmer animation drops the Pi 5 below 60fps (Pitfall 16) | mitigate | Overlay animates ONLY opacity on a GPU-composited layer with will-change: opacity; rendered only on the affected cubes (≤8 typical), not all 32; 2000ms (~0.5Hz) keyframe. Manual hardware check (04-VALIDATION Manual-Only) confirms <16ms p95. |
| T-04-14 | Tampering | Shimmer recolors / overwrites a lit cell state (design-constraint breach) | mitigate | Overlay is a separate aria-hidden sibling div using rgba tokens only at low opacity; never sets data-state, never sets a solid background, never suppresses the LED glow. CSS gate asserts no box-shadow/solid-bg/data-state in the overlay rule. |
| T-04-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH § Package Legitimacy Audit). No install task → no checkpoint required. |
</threat_model>

<verification>
Run after all tasks (LOCAL — no CI), from frontend/:
- `npm run lint`, `npm run build`, `npm test --run` (full frontend suite green, including store.connectivity, ShelfGrid)
Plus the 04-VALIDATION Manual-Only check on the Pi: open a cube editor on mobile → confirm subtle ambient shimmer on the affected range with no lit-cell recolor; commit → shimmer clears and the cube re-glows.
Backend is unchanged by this plan.
</verification>

<success_criteria>
- RTM-04: while the owner has an editor open on another device, the affected cube range on the kiosk shows a subtle ambient shimmer (D-01 mid-edit)
- D-02: shimmer is ambient, no text, opacity-only, never recolors a lit cell, never alters data-state
- D-03: shimmer clears on boundary_changed commit (Plan 01) AND auto-clears after the ~60s client TTL (this plan's sweeper)
- Pi frame budget: opacity-only GPU-composited animation; single EventSource consumer preserved
</success_criteria>

<output>
Create `.planning/phases/04-realtime-live-updates/04-04-SUMMARY.md` when done.
</output>
