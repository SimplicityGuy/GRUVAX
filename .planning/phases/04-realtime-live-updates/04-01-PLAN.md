---
phase: 04-realtime-live-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gruvax/events/__init__.py
  - src/gruvax/events/bus.py
  - src/gruvax/api/events.py
  - src/gruvax/api/deps.py
  - src/gruvax/app.py
  - src/gruvax/api/admin/cubes.py
  - frontend/src/state/store.ts
  - frontend/src/routes/kiosk/KioskView.tsx
  - tests/unit/test_event_bus.py
  - tests/integration/test_sse.py
autonomous: true
requirements: [ADMN-11, RTM-01, RTM-02]
must_haves:
  truths:
    - "D-04: An admin boundary edit on one device causes the affected cube(s) to re-render on an open kiosk within ~500ms with no manual refresh (ADMN-11/RTM-01)"
    - "D-09: GET /api/events streams Server-Sent Events and holds no DB pool connection for the life of the stream (Pitfall 10)"
    - "Two simultaneous searches (kiosk + mobile) complete concurrently without server-side serialization (RTM-02)"
    - "D-10: The kiosk EventSource sets connectivity.sseConnected with no visible offline UI this phase, and D-11: re-syncs boundary-derived queries on every (re)connect"
    - "D-03: a boundary_changed commit clears the shimmer for the affected cubes (the primary on-commit clear; the 60s idle safety lives in Plan 04)"
  artifacts:
    - path: "src/gruvax/events/bus.py"
      provides: "EventBus class — asyncio.Queue-per-subscriber in-process fan-out"
      contains: "class EventBus"
    - path: "src/gruvax/api/events.py"
      provides: "GET /api/events SSE endpoint, no DB dependency"
      contains: "EventSourceResponse"
    - path: "tests/unit/test_event_bus.py"
      provides: "EventBus unit tests + SSE no-pool-dependency assertion"
      contains: "test_sse_no_pool_dep"
    - path: "tests/integration/test_sse.py"
      provides: "Latency, headers, and concurrency integration tests"
      contains: "test_boundary_changed_latency"
  key_links:
    - from: "src/gruvax/api/admin/cubes.py"
      to: "src/gruvax/events/bus.py"
      via: "bus.publish('boundary_changed', ...) after cache.load(pool)"
      pattern: "bus\\.publish\\(.boundary_changed."
    - from: "frontend/src/routes/kiosk/KioskView.tsx"
      to: "/api/events"
      via: "new EventSource('/api/events') in useEffect"
      pattern: "new EventSource\\(./api/events.\\)"
    - from: "src/gruvax/api/events.py"
      to: "src/gruvax/api/deps.py"
      via: "Depends(get_event_bus) — NEVER get_pool"
      pattern: "Depends\\(get_event_bus\\)"
---

<objective>
Build the realtime spine end-to-end as the thinnest vertical slice that proves live re-render: an in-process EventBus, a `GET /api/events` SSE endpoint with no DB dependency, the `bus.publish('boundary_changed', ...)` fan-out wired at the existing post-commit seam in `cubes.py`, and the kiosk `EventSource` consumer that turns `boundary_changed` into a cube re-render. After this plan, an admin PUT on one device re-renders the affected cube on an open kiosk within ~500ms with no manual refresh.

Purpose: This is the spine every other Phase 4 slice depends on (re-glow, shimmer, optimistic rollback all consume this channel). It satisfies ADMN-11 (live re-render), RTM-01 (SSE subscribe + re-render), and RTM-02 (concurrent searches, no serialization because the SSE endpoint pins zero pool slots).
Output: New `events/bus.py`, `api/events.py`, `get_event_bus` provider, lifespan wiring, two `cubes.py` publish seams, the kiosk `connectivity` + `shimmerCubes` Zustand slice, the kiosk SSE consumer, and the Wave-0 backend test scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-realtime-live-updates/04-CONTEXT.md
@.planning/phases/04-realtime-live-updates/04-RESEARCH.md
@.planning/phases/04-realtime-live-updates/04-PATTERNS.md
@.planning/phases/04-realtime-live-updates/04-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. No exploration required. -->

Event payload contract (one event per change-set — Claude's Discretion confirmed in RESEARCH Q2):
  boundary_changed → {"cube_ids": [{"unit": int, "row": int, "col": int}], "change_set_id": str}
  admin_editing    → {"cube_ids": [{"unit": int, "row": int, "col": int}], "editing": bool}
  server_hello     → {"version": str}
  server_shutdown  → {}

Existing post-commit seam in src/gruvax/api/admin/cubes.py:
  - put_cube_boundary  (async def at L210): cache.invalidate() at L321, await cache.load(pool) at L322; both handlers already carry `cache: BoundaryCache = Depends(get_boundary_cache)` and `pool` access.
  - bulk_write_cubes   (async def at L624): cache.invalidate() at L765, await cache.load(pool) at L766; the committed body dict is `response_body` with key "change_set_id" (built at L720-722); the parsed edits live on the request body model (the bulk handler iterates the validated updates).
  - File documents "Pitfall A — cache.invalidate() is NEVER called inside the transaction" at L636.

Existing deps.py provider shape (mirror exactly): get_boundary_cache at L41-59 — `getattr(request.app.state, "<key>", None)` → None-check raises `HTTPException(status_code=status.HTTP_503_SERVICE_UNAVAILABLE, ...)` → return typed value.

Existing app.py: lifespan() at L77; boundary_cache registered on app.state at L109; settings_cache at L131; teardown calls `await disconnect_mqtt(app)` (L143) then `await pool.close()` (L144). create_app() registers routers L176-184 BEFORE the StaticFiles mount (L193) — all include_router calls MUST precede StaticFiles. FastAPI app version constant is "0.1.0" (L164).

Existing kiosk query keys (CONFIRMED via grep — the ARCHITECTURE/RESEARCH sketch's `['cube', u, r, c]` key does NOT exist in this codebase):
  - ['units']                                  — KioskView units query
  - ['cubes']                                  — KioskView cubes-with-fill query
  - ['cube-contents', unit_id, row, col]       — CubeContentsPanel reverse-lookup query
  - ['search', debouncedQuery]                 — search query
  - ['admin', 'cubes'] / ['admin', 'history']  — admin queries
  Locate is IMPERATIVE, not a query: ResultsList calls `locateRelease(release_id)` then `setLocateResult(result)`. There is NO `['locate', id]` query key. (D-05 re-locate is handled in Plan 02, not here.)

Existing store.ts (frontend/src/state/store.ts): create<GruvaxStore>((set) => ({...})); has selectedReleaseId, highlight, setHighlightCube (L59-63 derived-state pattern), setLocateResult (L69, increments animationToken), animationToken. NO connectivity slice yet.

Existing KioskView.tsx: imports `useQuery` (L2); store destructure at L27-28; useQuery for units (L50) and cubes (L57); existing useEffect blocks L96-130; useLayoutEffect GSAP timeline keyed on [animationToken] at L157-257.

Existing tests/conftest.py: provides module-scoped `admin_session` fixture returning {"cookies": <cookies>, "csrf_token": <value>} after seeding PIN "0000" and POST /api/admin/login. Each integration test module provides its own `client` fixture (LifespanManager + ASGITransport + AsyncClient). See tests/integration/test_health.py L23-42 for the client fixture boilerplate to copy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 test scaffold — EventBus unit tests + SSE integration tests (RED)</name>
  <files>tests/unit/test_event_bus.py, tests/integration/test_sse.py</files>
  <read_first>
    - .planning/phases/04-realtime-live-updates/04-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — bind these tests to ADMN-11, RTM-01, RTM-02, D-09, Pitfall 8)
    - .planning/phases/04-realtime-live-updates/04-RESEARCH.md (§ Validation Architecture — Key Test Patterns: test_boundary_changed_latency, test_sse_headers, test_sse_no_pool_dep, EventBus tests)
    - .planning/phases/04-realtime-live-updates/04-PATTERNS.md (tests/unit/test_event_bus.py analog = tests/unit/test_algorithm.py; tests/integration/test_sse.py analog = tests/integration/test_health.py)
    - tests/integration/test_health.py (copy the module-scoped `client` fixture boilerplate: LifespanManager + ASGITransport + AsyncClient, L23-42)
    - tests/conftest.py (the `admin_session` fixture shape: {"cookies", "csrf_token"})
    - tests/unit/test_algorithm.py (mirror the @pytest.mark.asyncio decorator + asyncio.wait_for timeout style)
  </read_first>
  <action>
    Create tests/unit/test_event_bus.py with: test_subscribe_receive_publish (subscribe → publish 'boundary_changed' → assert event.name and that data round-trips; unsubscribe → assert queue removed from bus._subscribers); test_slow_subscriber_drops_silently (fill the queue to maxsize=64 then publish once more — must NOT raise, queue stays full); and test_sse_no_pool_dep (import gruvax.api.events.stream_events, use inspect.signature, assert "pool" not in param names and "get_pool" not in str(signature) — this binds D-09 / Pitfall 10).
    Create tests/integration/test_sse.py with a module-scoped `client` fixture copied from test_health.py, and: test_sse_headers (stream GET /api/events, assert status 200, header x-accel-buffering == "no", cache-control == "no-store" — binds Pitfall 8); test_boundary_changed_latency (use the RESEARCH § Validation recipe: open an SSE reader task on /api/events, wait ~50ms to connect, record t0, PUT /api/admin/cubes/1/0/0/boundary with admin_session cookies + X-CSRF-Token header, await an asyncio.Event set when "boundary_changed" appears in the stream with timeout 0.5, assert latency < 0.5 — this is the PRIMARY ADMN-11 gate); test_concurrent_searches (fire two concurrent httpx GET /api/search?q=... via asyncio.gather, assert both complete and total wall-time is not ~2× a single request — binds RTM-02). Use `@pytest.mark.asyncio(loop_scope="session")` per the Phase 1 DB-test convention (STATE.md). These tests are expected to FAIL until Tasks 2-4 land (RED phase).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_event_bus.py tests/integration/test_sse.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_event_bus.py tests/integration/test_sse.py --collect-only -q` lists at least: test_subscribe_receive_publish, test_slow_subscriber_drops_silently, test_sse_no_pool_dep, test_sse_headers, test_boundary_changed_latency, test_concurrent_searches (collection succeeds — no import/syntax errors)
    - `grep -c "test_boundary_changed_latency\|test_sse_headers\|test_sse_no_pool_dep" tests/integration/test_sse.py tests/unit/test_event_bus.py` is ≥ 3 across the two files
    - tests/integration/test_sse.py references `admin_session` and uses `X-CSRF-Token` in the PUT
  </acceptance_criteria>
  <done>Both test files exist, collect cleanly, and encode the ADMN-11 latency gate, Pitfall 8 headers, D-09 no-pool assertion, RTM-01 bus behavior, and RTM-02 concurrency. They are RED (implementation not yet present).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EventBus + get_event_bus provider + lifespan wiring (GREEN for unit tests)</name>
  <files>src/gruvax/events/__init__.py, src/gruvax/events/bus.py, src/gruvax/api/deps.py, src/gruvax/app.py</files>
  <behavior>
    - EventBus().subscribe() returns an asyncio.Queue(maxsize=64) and registers it; unsubscribe(q) discards it
    - await publish(name, data) fans out an Event(name, data) to every subscriber via put_nowait; on asyncio.QueueFull it drops silently (never raises) — drop-oldest backpressure (D-11 resync covers the gap)
    - get_event_bus(request) returns app.state.event_bus or raises HTTP 503 if not present
    - app lifespan instantiates EventBus, stores it on app.state.event_bus, publishes server_hello{version:"0.1.0"} on startup, and best-effort publishes server_shutdown{} before pool.close() on teardown
  </behavior>
  <read_first>
    - src/gruvax/estimator/boundary_cache.py (analog for EventBus class lifecycle, module docstring, __future__/dataclass/typing import block, "Phase N seam" docstring notation — PATTERNS L43-121)
    - src/gruvax/api/deps.py (mirror get_boundary_cache at L41-59 exactly — getattr/None-check/503/typed-return; note the local-import-inside-function pattern used by require_admin at L109-110 to avoid circular import)
    - src/gruvax/app.py (lifespan steps 3b/3c at L109-135 for the try/log/except pattern; teardown at L143-144; create_app router registration at L176-184)
    - .planning/phases/04-realtime-live-updates/04-PATTERNS.md (events/bus.py, deps.py, app.py sections — verbatim structural guidance)
    - .planning/phases/04-realtime-live-updates/04-RESEARCH.md (Pattern 1 EventBus, EventBus Dependency Provider code example, Lifespan wiring)
  </read_first>
  <action>
    Create src/gruvax/events/__init__.py as an empty file (mirror src/gruvax/mqtt/__init__.py). Create src/gruvax/events/bus.py with `from __future__ import annotations`, an `@dataclass Event(name: str, data: dict[str, Any])`, and `class EventBus` holding `self._subscribers: set[asyncio.Queue[Event]]`; implement subscribe() (asyncio.Queue(maxsize=64), add, return), unsubscribe(q) (discard), and `async def publish(self, name, data)` iterating `list(self._subscribers)` with `q.put_nowait(event)` wrapped in `try/except asyncio.QueueFull: pass`. Use the boundary_cache.py docstring style with the Phase-4-seam note ("call AFTER the DB transaction commits and AFTER cache.load(), same as cache.invalidate() — Pitfall A").
    Add get_event_bus(request: Request) -> "EventBus" to deps.py, structurally identical to get_boundary_cache, keyed on "event_bus", with the EventBus import placed inside the function body (local import) to avoid circular dependency.
    In app.py lifespan, after the settings_cache step (~L131-135) and before `yield`, add step 3d: `from gruvax.events.bus import EventBus`, instantiate, store on `app.state.event_bus`, then best-effort `await event_bus.publish("server_hello", {"version": "0.1.0"})` inside try/except-log. In teardown, before `await pool.close()`, best-effort `await event_bus.publish("server_shutdown", {})` in try/except pass.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_event_bus.py -x -q && ruff check src/gruvax/events/ src/gruvax/api/deps.py src/gruvax/app.py && mypy src/gruvax/events/ src/gruvax/api/deps.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_event_bus.py -x -q` exits 0 (all three unit tests green, including test_sse_no_pool_dep)
    - `grep -q "class EventBus" src/gruvax/events/bus.py` and `grep -q "maxsize=64" src/gruvax/events/bus.py`
    - `grep -q "except asyncio.QueueFull" src/gruvax/events/bus.py` (drop-oldest backpressure present)
    - `grep -q "def get_event_bus" src/gruvax/api/deps.py`
    - `grep -q "server_hello" src/gruvax/app.py` and `grep -q "server_shutdown" src/gruvax/app.py`
    - `ruff check` and `mypy` exit 0 for the touched files
  </acceptance_criteria>
  <done>EventBus exists with bounded queues + silent drop, get_event_bus mirrors the established provider pattern, the lifespan registers the bus and emits server_hello/server_shutdown, and the EventBus unit tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SSE endpoint + bus.publish at the cubes.py post-commit seam (GREEN for SSE integration tests)</name>
  <files>src/gruvax/api/events.py, src/gruvax/app.py, src/gruvax/api/admin/cubes.py</files>
  <behavior>
    - GET /api/events returns an EventSourceResponse with ping=15 and headers X-Accel-Buffering: no + Cache-Control: no-store
    - The SSE generator subscribes to the bus, yields each Event as a ServerSentEvent(event=name, data=json), polls request.is_disconnected(), and unsubscribes in a finally-block; the handler signature contains NO pool dependency
    - put_cube_boundary publishes boundary_changed{cube_ids:[{unit,row,col}], change_set_id} AFTER cache.load(pool)
    - bulk_write_cubes publishes boundary_changed listing all affected cubes from the validated updates + response_body["change_set_id"] AFTER cache.load(pool)
    - An admin PUT causes a connected SSE client to receive boundary_changed in < 500ms
  </behavior>
  <read_first>
    - src/gruvax/api/locate.py (analog for router declaration, async def signature with Depends(), typed return, docstring — PATTERNS L125-174)
    - src/gruvax/api/admin/cubes.py (the two seams: L320-322 put_cube_boundary and L764-766 bulk_write_cubes; the "Pitfall A" comment at L636; how response_body and change_set_id are built at L720-722; the existing import line at L42 and how Depends(get_boundary_cache) sits in both signatures)
    - .planning/phases/04-realtime-live-updates/04-PATTERNS.md (events.py + cubes.py sections — exact insertion points and the Pitfall A ordering rule)
    - .planning/phases/04-realtime-live-updates/04-RESEARCH.md (Pattern 2 SSE endpoint, Pattern 3 bus publish at seam, Complete SSE Endpoint code example, Anti-Patterns: no DB Depends, never publish inside the transaction)
  </read_first>
  <action>
    Create src/gruvax/api/events.py mirroring locate.py's structure: `from __future__ import annotations`, module logger, `router = APIRouter(tags=["events"])`, and `async def stream_events(request: Request, bus: EventBus = Depends(get_event_bus)) -> EventSourceResponse`. Inside, define an async `generator()` that calls `q = bus.subscribe()`, loops `while True` checking `await request.is_disconnected()` (break on disconnect), uses `await asyncio.wait_for(q.get(), timeout=1.0)` (continue on asyncio.TimeoutError so ping=15 keepalive runs), yields `ServerSentEvent(event=event.name, data=json.dumps(event.data))`, and calls `bus.unsubscribe(q)` in a finally-block. Return `EventSourceResponse(generator(), ping=15, headers={"X-Accel-Buffering": "no", "Cache-Control": "no-store"})`. The signature MUST NOT include any pool / get_pool dependency (D-09, Pitfall 10).
    Register the events router in create_app() after the existing /api routers and BEFORE the StaticFiles mount: `from gruvax.api.events import router as events_router; app.include_router(events_router, prefix="/api")` (deferred-import convention).
    In cubes.py: add `get_event_bus` to the existing deps import at L42 and `from gruvax.events.bus import EventBus`; add `bus: EventBus = Depends(get_event_bus)` to both put_cube_boundary (L210) and bulk_write_cubes (L624) signatures alongside the existing cache dep. Insert `await bus.publish("boundary_changed", {...})` immediately AFTER `await cache.load(pool)` in BOTH handlers (after L322 and after L766) — never inside the transaction. For put_cube_boundary use cube_ids=[{"unit": unit_id, "row": row, "col": col}] and the committed change_set_id for that write. For bulk_write_cubes use cube_ids built from the validated updates (one {unit,row,col} per edit) and response_body["change_set_id"].
  </action>
  <verify>
    <automated>python -m pytest tests/integration/test_sse.py -x -q && ruff check src/gruvax/api/events.py src/gruvax/api/admin/cubes.py src/gruvax/app.py && mypy src/gruvax/api/events.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/integration/test_sse.py -x -q` exits 0 (test_sse_headers, test_boundary_changed_latency < 500ms, test_concurrent_searches all green)
    - `grep -q "EventSourceResponse" src/gruvax/api/events.py` and `grep -q 'ping=15' src/gruvax/api/events.py`
    - `grep -q "X-Accel-Buffering" src/gruvax/api/events.py` and `grep -q "no-store" src/gruvax/api/events.py`
    - `grep -vE '^\s*#' src/gruvax/api/events.py | grep -c "get_pool"` equals 0 (no pool dependency on the SSE endpoint)
    - `grep -c "bus.publish(\"boundary_changed\"" src/gruvax/api/admin/cubes.py` equals 2 (both seams wired)
    - `grep -q "events_router" src/gruvax/app.py` (router registered before StaticFiles)
    - `ruff check` and `mypy` exit 0 for the touched files
  </acceptance_criteria>
  <done>GET /api/events streams events with the correct headers and no pool dependency; both admin write paths fan out boundary_changed after the cache reloads; the < 500ms ADMN-11 latency gate is GREEN.</done>
</task>

<task type="auto">
  <name>Task 4: Kiosk SSE consumer + connectivity/shimmer Zustand slice (live re-render end-to-end)</name>
  <files>frontend/src/state/store.ts, frontend/src/routes/kiosk/KioskView.tsx</files>
  <read_first>
    - frontend/src/state/store.ts (the create<GruvaxStore>((set) => ({...})) shape, the setHighlightCube derived-state pattern at L59-63, setLocateResult at L69, selectedReleaseId)
    - frontend/src/routes/kiosk/KioskView.tsx (store destructure L27-28; existing useEffect blocks L96-130 for cleanup-return idiom; the useLayoutEffect GSAP timeline at L157-257; the useQuery keys ['units'] L51 and ['cubes'] L58)
    - .planning/phases/04-realtime-live-updates/04-PATTERNS.md (store.ts connectivity slice section L376-428; KioskView SSE consumer section L432-518; the Zustand .getState()-in-event-handlers rule L809-813)
    - .planning/phases/04-realtime-live-updates/04-RESEARCH.md (Pattern 5 Kiosk SSE Consumer, Pattern 6 Zustand connectivity slice, Pitfall 4 never es.close() in onerror, Pitfall 5 stale closure → use .getState())
    - .planning/phases/04-realtime-live-updates/04-UI-SPEC.md (Component Inventory — store.ts gets connectivity + shimmerCubes + shimmerExpiresAt + setters)
  </read_first>
  <action>
    In store.ts add the full Phase-4 slice (one cohesive addition so later plans need not touch this file): an interface `ShimmerCube { unit: number; row: number; col: number }`; on GruvaxStore add `connectivity: { sseConnected: boolean; lastSeenAt: number; bannerVisible: false }` (bannerVisible is a stub for the deferred Offline slice — D-10), `shimmerCubes: ShimmerCube[]`, `shimmerExpiresAt: number`, and setters `setSseConnected(connected)`, `setShimmerCubes(cubes)`, `clearShimmerCubes(cubes)`. Initialize connectivity to {sseConnected:false,lastSeenAt:0,bannerVisible:false}, shimmerCubes [], shimmerExpiresAt 0. setSseConnected uses set((s)=>...) updating lastSeenAt=Date.now() only when connecting; setShimmerCubes sets shimmerExpiresAt=Date.now()+60_000 (D-03 TTL); clearShimmerCubes filters out the matching unit-row-col keys.
    In KioskView.tsx: import `useQueryClient` alongside `useQuery`; add `const queryClient = useQueryClient()`. Add ONE new useEffect (keyed on [queryClient]) that creates `new EventSource('/api/events')` and: defines a `resync()` that invalidates ['units'] and ['cubes'] and ['admin','cubes'] (D-11 — note the real kiosk keys are ['cubes'] and ['cube-contents',...], NOT ['cube',u,r,c]); sets es.onopen to `useGruvaxStore.getState().setSseConnected(true)` then resync(); sets es.onerror to setSseConnected(false) and does NOT call es.close() (Pitfall 4); adds a 'boundary_changed' listener that parses {cube_ids, change_set_id}, invalidates ['cubes'], ['units'], ['admin','cubes'], ['admin','history'], and per-cube ['cube-contents', unit, row, col] for each cube_id, then calls clearShimmerCubes(cube_ids) (D-03); adds an 'admin_editing' listener that calls setShimmerCubes(cube_ids) when editing else clearShimmerCubes(cube_ids); adds a 'server_hello' listener that runs resync() + invalidates ['admin','settings']; adds a 'server_shutdown' listener that sets sseConnected false; and returns `() => es.close()` (the ONLY place es.close() is called). Inside every listener read store state via `useGruvaxStore.getState()` (not the hook form) to avoid stale closures. Do NOT add the active-selection re-locate here — that is Plan 02 (D-05).
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run build && npm test --run -- store</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "new EventSource('/api/events')" frontend/src/routes/kiosk/KioskView.tsx`
    - `grep -q "setSseConnected" frontend/src/state/store.ts` and `grep -q "shimmerCubes" frontend/src/state/store.ts` and `grep -q "shimmerExpiresAt" frontend/src/state/store.ts`
    - `grep -q "boundary_changed" frontend/src/routes/kiosk/KioskView.tsx` and `grep -q "getState()" frontend/src/routes/kiosk/KioskView.tsx`
    - `grep -c "es.close()" frontend/src/routes/kiosk/KioskView.tsx` equals 1 (only in the cleanup return, never in onerror)
    - From frontend/: `npm run lint` exits 0, `npm run build` exits 0, `npm test --run -- store` exits 0
  </acceptance_criteria>
  <done>The kiosk opens a single EventSource, sets connectivity.sseConnected on open/error, re-syncs boundary-derived queries on every (re)connect, and re-renders affected cubes on boundary_changed using the REAL kiosk query keys. Opening an admin edit on another device now re-renders the kiosk cube live without refresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk browser → GET /api/events | Untrusted, cookieless public client reads the SSE stream over the home LAN |
| admin device → PUT /api/admin/cubes/... | Session + CSRF-gated write that triggers the fan-out |
| EventBus → all SSE subscribers | In-process fan-out; one slow/hostile subscriber must not stall others |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | GET /api/events (public, cookieless) | accept | Events carry only {cube_ids, change_set_id} — the same cube boundary data already public via GET /api/cubes. No PII, no record text, no session/admin data ever placed on the bus. Server constructs payloads from validated DB values, never from raw client input. |
| T-04-02 | Denial of Service | SSE endpoint pinning a DB pool slot (Pitfall 10) | mitigate | SSE endpoint depends ONLY on get_event_bus, never get_pool. Asserted by test_sse_no_pool_dep (inspect.signature). Pool (max=10) is never starved → two concurrent searches stay concurrent (RTM-02). |
| T-04-03 | Denial of Service | Slow/frozen SSE subscriber backing up the bus | mitigate | EventBus uses bounded asyncio.Queue(maxsize=64) per subscriber + put_nowait drop-on-QueueFull (never blocks the publisher); generator unsubscribes in a finally-block on disconnect/CancelledError so no subscriber leak. |
| T-04-04 | Tampering | boundary_changed payload injection / log injection | mitigate | Payload built server-side from validated DB values (unit_id/row/col ints, change_set_id str). psycopg %s placeholders only (no f-string SQL). No PIN/session value is logged on any new path. |
| T-04-05 | Tampering | Event published for an uncommitted/rolled-back write | mitigate | bus.publish() is called AFTER conn.commit() and AFTER cache.load() (Pitfall A ordering), never inside the transaction — verified by the latency integration test which only sees the event post-commit. |
| T-04-SC | Tampering | npm/pip installs | mitigate | No new packages this phase (RESEARCH § Package Legitimacy Audit — sse-starlette 3.4.4 / TanStack Query 5 / Zustand 5 already pinned and APPROVED). No install task → no [ASSUMED]/[SUS] checkpoint required. |
</threat_model>

<verification>
Run after all tasks (LOCAL — no CI):
- Backend: `python -m pytest tests/ -x` (full suite green, including tests/integration/test_sse.py::test_boundary_changed_latency)
- Backend static: `ruff check .` and `mypy src/`
- Frontend (from frontend/): `npm run lint`, `npm run build`, `npm test --run`
</verification>

<success_criteria>
- ADMN-11: an admin PUT → kiosk receives boundary_changed via SSE in < 500ms (test_boundary_changed_latency green)
- RTM-01: GET /api/events yields SSE events; the kiosk re-renders affected cubes on boundary_changed with no manual refresh; client disconnect unsubscribes the queue
- RTM-02: two concurrent searches complete without serialization (test_concurrent_searches green); SSE endpoint holds no pool connection (test_sse_no_pool_dep green)
- Pitfall 8: SSE response carries X-Accel-Buffering: no + Cache-Control: no-store + ping=15
- connectivity.sseConnected is set by EventSource onopen/onerror/server_shutdown; every (re)connect resyncs boundary-derived queries (D-11 foundation)
</success_criteria>

<output>
Create `.planning/phases/04-realtime-live-updates/04-01-SUMMARY.md` when done.
</output>
