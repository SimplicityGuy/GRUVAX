---
phase: 03-admin-loop-pin-manual-entry-undo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - migrations/versions/0004_admin_tables.py
  - src/gruvax/settings.py
  - src/gruvax/app.py
  - src/gruvax/db/queries.py
  - src/gruvax/estimator/boundary_math.py
  - tests/conftest.py
  - tests/unit/test_pin.py
  - tests/unit/test_sessions.py
  - tests/unit/test_boundary_validation.py
  - tests/unit/test_diff_preview.py
  - tests/unit/test_fill_level.py
  - tests/unit/test_cube_contents.py
  - tests/unit/test_midpoint.py
  - tests/integration/test_admin_auth.py
  - tests/integration/test_boundary_editor.py
  - tests/integration/test_change_set.py
  - tests/integration/test_cube_public.py
  - tests/property/test_fill_level_property.py
  - tests/property/test_midpoint_property.py
  - tests/property/test_boundary_validation_property.py
autonomous: true
requirements: [ADMN-01, ADMN-02, ADMN-09, ADMN-12, CUBE-07, CUBE-09]
user_setup:
  - service: gruvax-api (self-hosted, no external account)
    why: "Admin PIN is DB-seeded via CLI and SESSION_SECRET must be set before login works (D-02). These are bootstrap steps Claude cannot run for production; tests seed their own."
    env_vars:
      - name: SESSION_SECRET
        source: "Generate locally: python -c \"import secrets; print(secrets.token_urlsafe(32))\" — add to .env and docker-compose env for gruvax-api"

must_haves:
  truths:
    - "alembic upgrade head creates boundary_history, admin_sessions, settings, idempotency_keys in the gruvax schema and seeds settings defaults"
    - "alembic downgrade base then upgrade head round-trips clean (drops all four tables in reverse, re-creates them)"
    - "passlib[argon2] and slowapi are installed and importable"
    - "The full Wave-0 test suite exists and runs (tests fail RED against unimplemented code, but collect without import errors)"
    - "count_records_in_boundary, sample_records, and suggest_midpoint pure helpers compute correct values from a CollectionSnapshot with no DB access"
  artifacts:
    - path: "migrations/versions/0004_admin_tables.py"
      provides: "DDL for boundary_history, admin_sessions, settings, idempotency_keys + seeded settings defaults"
      contains: "CREATE TABLE gruvax.boundary_history"
    - path: "src/gruvax/estimator/boundary_math.py"
      provides: "count_records_in_boundary, sample_records, suggest_midpoint pure helpers"
      exports: ["count_records_in_boundary", "sample_records", "suggest_midpoint"]
    - path: "src/gruvax/settings.py"
      provides: "SESSION_SECRET + SESSION_TTL_SECONDS config fields"
      contains: "SESSION_SECRET"
    - path: "tests/conftest.py"
      provides: "admin_session fixture + test PIN seeding helper"
      contains: "admin_session"
  key_links:
    - from: "src/gruvax/app.py"
      to: "gruvax.settings (settings cache)"
      via: "lifespan step 3c loads settings into app.state.settings_cache"
      pattern: "settings_cache"
    - from: "migrations/versions/0004_admin_tables.py"
      to: "migrations/versions/0003_pg_trgm_indexes.py"
      via: "down_revision chaining"
      pattern: "down_revision.*0003"
---

<objective>
Lay the shared, no-auth-required foundation for the entire Phase 3 admin loop: the Alembic migration that creates all four admin tables, the new Python dependencies, the configuration fields, the lifespan settings cache, the COMPLETE Wave-0 test scaffold for the phase, and the pure boundary-math helpers (fill count, sampling, midpoint) that both the kiosk-reveal slice and the editor slice consume.

Purpose: Every downstream Phase 3 plan depends on these tables, helpers, and tests existing. Pulling them into one foundation plan keeps the auth, kiosk-reveal, editor, and undo slices free of cross-cutting setup and gives the Nyquist validation strategy (03-RESEARCH.md §Validation Architecture) its full failing-test surface up front. The within-plan task order is strict TDD: install/migrate (Task 1) → write the failing test scaffold (Task 2, RED) → implement the boundary-math helpers against that scaffold (Task 3, GREEN).

Output: Migration 0004, extended `settings.py`/`app.py`/`queries.py`, installed deps, 17 new/extended test files (all RED until later plans implement the code they target, except the boundary-math helper tests which go GREEN in Task 3), and `boundary_math.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-loop-pin-manual-entry-undo/03-CONTEXT.md
@.planning/phases/03-admin-loop-pin-manual-entry-undo/03-RESEARCH.md
@.planning/phases/03-admin-loop-pin-manual-entry-undo/03-PATTERNS.md

<interfaces>
<!-- Concrete contracts the executor consumes — no codebase exploration needed. -->

From src/gruvax/estimator/collection_snapshot.py:
```python
@dataclass(frozen=True)
class RecordRow:
    release_id: int
    label: str
    catalog_number: str

class CollectionSnapshot:
    def get_label_records(self, label: str) -> list[RecordRow]: ...   # casefold lookup, insertion order
    def _load_snapshot(self, by_label: dict[str, list[RecordRow]]) -> None: ...  # test seam (keys casefolded by caller)
```

From src/gruvax/estimator/boundary_cache.py:
```python
@dataclass(frozen=True)
class BoundaryRow:
    unit_id: int; row: int; col: int
    first_label: str | None; first_catalog: str | None
    last_label: str | None;  last_catalog: str | None
    is_empty: bool
```

From src/gruvax/estimator/normalize.py (the ONLY legal catalog comparison path — raw-string compare forbidden, Pitfall C):
```python
def parse_key(catalog: str | None) -> tuple[tuple[int, int | str], ...]: ...  # numeric-aware sort key
def catalog_in_range(catalog, first_catalog, last_catalog) -> bool: ...
def compare_catalogs(a, b) -> int: ...  # -1/0/1
# Labels are NOT catalog numbers: compare labels with str.casefold(), NEVER normalize_catalog().
```

From src/gruvax/settings.py (current):
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")
    DATABASE_URL: str
    OBSERVED_DISCOGSOGRAPHY_SCHEMA: str = "gruvax_dev"
    LOG_LEVEL: str = "INFO"
settings = Settings()
```

From tests/conftest.py (current fixtures): session-scoped `db_pool` (real pool), `boundary_cache` (yaml list). Integration tests use `LifespanManager(create_app())` + `AsyncClient(ASGITransport(...))` with `@pytest.mark.asyncio(loop_scope="session")` (see tests/integration/test_search.py).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps + Alembic migration 0004 (admin tables) + settings/app config</name>
  <files>pyproject.toml, migrations/versions/0004_admin_tables.py, src/gruvax/settings.py, src/gruvax/app.py, src/gruvax/db/queries.py</files>
  <read_first>
    - migrations/versions/0001_create_schema.py (EXACT analog: op.execute DDL, header revision/down_revision pattern, downgrade reverse-order drops)
    - migrations/versions/0003_pg_trgm_indexes.py (most recent migration — confirm its revision id is the down_revision for 0004)
    - migrations/env.py (DO NOT MODIFY — confirms version_table_schema="public" + connect-event search_path already handle new tables)
    - src/gruvax/settings.py (the file being modified — current BaseSettings shape)
    - src/gruvax/app.py (the file being modified — lifespan steps 1-4 and create_app router registration; see step 3b snapshot load as the analog for the 3c settings-cache load)
    - src/gruvax/db/queries.py (the file being modified — %s placeholder convention, async with pool.connection() pattern; add load_settings_cache here)
    - pyproject.toml (the file being modified — dependencies array + dependency-groups.dev)
  </read_first>
  <action>
    Add `passlib[argon2]` and `slowapi` to pyproject.toml `dependencies`; add `types-passlib` to `[dependency-groups].dev`. Run `uv sync` so the lockfile updates and packages are importable (these are [OK]-approved in 03-RESEARCH.md Package Legitimacy Audit — no human gate needed). Note on the "latest versions" convention (global memory): `passlib[argon2]` 1.7.4 IS the PyPI-latest passlib — there is no 2.x release — so pinning 1.7.4 satisfies the latest-versions rule (the rule was evaluated, not silently violated). The actively-maintained crypto primitive is the underlying `argon2-cffi`, pulled in via the `[argon2]` extra. A future migration to `pwdlib` is a one-function swap in `pin.py` and is deferred per 03-RESEARCH.md Open Question 1; the Argon2id PHC hash format is forward-compatible.

    Create `migrations/versions/0004_admin_tables.py` with `revision="0004"`, `down_revision` set to the actual revision id of 0003 (verify by reading 0003's `revision` value — do NOT assume the string). In `upgrade()`, use `op.execute()` for each: (1) `gruvax.boundary_history` — columns per 03-RESEARCH.md Pattern 4 DDL: `id BIGSERIAL PK`, `change_set_id UUID NOT NULL`, `unit_id/row/col SMALLINT NOT NULL`, `prev_first_label/prev_first_catalog/prev_last_label/prev_last_catalog TEXT`, `prev_is_empty BOOLEAN NOT NULL`, `new_first_label/new_first_catalog/new_last_label/new_last_catalog TEXT`, `new_is_empty BOOLEAN NOT NULL`, `changed_by TEXT NOT NULL DEFAULT 'admin'`, `changed_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `source TEXT NOT NULL CHECK (source IN ('manual','bulk','revert'))`; plus index `bh_changed_at_idx` on `(changed_at DESC)` and `bh_change_set_idx` on `(change_set_id)`. (2) `gruvax.admin_sessions` — `id UUID PK`, `created_at/last_seen_at TIMESTAMPTZ DEFAULT now()`, `expires_at TIMESTAMPTZ NOT NULL`, `hard_expires_at TIMESTAMPTZ NOT NULL`, `client_label TEXT`, `user_agent TEXT`, `revoked_at TIMESTAMPTZ`; partial index `admin_sessions_expires_idx` on `(expires_at) WHERE revoked_at IS NULL`. (3) `gruvax.settings` — `key TEXT PK`, `value JSONB NOT NULL`, `description TEXT`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`; then a seed INSERT for `cube.nominal_capacity`='95', `session.idle_ttl_seconds`='600', `session.hard_cap_seconds`='1800' (per D-13 and Claude's-discretion defaults). (4) `gruvax.idempotency_keys` — `key TEXT PK`, `response_json JSONB NOT NULL`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`; index `idempotency_keys_created_idx` on `(created_at)`. `downgrade()` drops all four in reverse order with `DROP TABLE IF EXISTS`. Do NOT touch migrations/env.py.

    Extend `src/gruvax/settings.py`: add `SESSION_SECRET: str` (NO default — missing value must crash boot, matching the DATABASE_URL pattern) and `SESSION_TTL_SECONDS: int = 600` (10-min idle, D-04).

    Extend `src/gruvax/db/queries.py`: add `async def load_settings_cache(pool) -> dict[str, Any]` that selects `key, value FROM gruvax.settings` and returns a `{key: value}` dict (value is already JSONB-decoded by psycopg). Use the existing `%s`-placeholder `async with pool.connection() as conn, conn.cursor() as cur` convention — zero f-string SQL.

    Extend `src/gruvax/app.py` lifespan: after the collection-snapshot load (step 3b), add step 3c that calls `load_settings_cache(pool)` inside a `try/except + logger.error + proceed` block (mirroring 3b) and assigns the result to `app.state.settings_cache` (empty dict on failure). This makes nominal_capacity / idle TTL available to later endpoints without per-request DB reads.
  </action>
  <verify>
    <automated>uv run python -c "import passlib.hash, slowapi; print('deps ok')" && uv run alembic upgrade head && uv run alembic downgrade base && uv run alembic upgrade head && uv run python -c "from gruvax.db.queries import load_settings_cache; from gruvax.settings import Settings; print('imports ok')" && uv run ruff check src/gruvax/settings.py src/gruvax/app.py src/gruvax/db/queries.py && uv run mypy src/gruvax/settings.py src/gruvax/db/queries.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import passlib.hash, slowapi"` exits 0
    - `migrations/versions/0004_admin_tables.py` contains `CREATE TABLE gruvax.boundary_history`, `CREATE TABLE gruvax.admin_sessions`, `CREATE TABLE gruvax.settings`, `CREATE TABLE gruvax.idempotency_keys`
    - `0004_admin_tables.py` `down_revision` equals the `revision` value found in `migrations/versions/0003_pg_trgm_indexes.py`
    - `alembic upgrade head && alembic downgrade base && alembic upgrade head` exits 0 (round-trip clean — OBS-03 convention)
    - `grep -c "SESSION_SECRET" src/gruvax/settings.py` ≥ 1 and there is no `= ` default on the SESSION_SECRET line
    - `src/gruvax/app.py` contains `settings_cache` assigned in the lifespan
    - `grep -v '^#' src/gruvax/db/queries.py | grep -c "f\"" ` for any SQL string is 0 (no f-string SQL added)
    - `uv run ruff check src/gruvax/settings.py src/gruvax/app.py src/gruvax/db/queries.py` exits 0 and `uv run mypy src/gruvax/settings.py src/gruvax/db/queries.py` exits 0
  </acceptance_criteria>
  <done>Deps installed; migration 0004 creates and round-trips all four admin tables with seeded settings; SESSION_SECRET/SESSION_TTL_SECONDS configured; settings cache loaded at startup; the files created/modified here lint + type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Complete Wave-0 test scaffold for the phase (RED for unimplemented code) — written BEFORE boundary_math (TDD)</name>
  <files>tests/conftest.py, tests/unit/test_pin.py, tests/unit/test_sessions.py, tests/unit/test_boundary_validation.py, tests/unit/test_diff_preview.py, tests/unit/test_fill_level.py, tests/unit/test_cube_contents.py, tests/unit/test_midpoint.py, tests/integration/test_admin_auth.py, tests/integration/test_boundary_editor.py, tests/integration/test_change_set.py, tests/integration/test_cube_public.py, tests/property/test_fill_level_property.py, tests/property/test_midpoint_property.py, tests/property/test_boundary_validation_property.py</files>
  <read_first>
    - tests/integration/test_search.py (EXACT analog: module-scoped client fixture, LifespanManager + AsyncClient, @pytest.mark.asyncio(loop_scope="session"))
    - tests/unit/test_normalize.py (EXACT analog for pure-function unit tests — plain def test_*, no async)
    - tests/unit/test_collection_snapshot.py (snapshot fixture via _load_snapshot, RecordRow construction)
    - tests/property/test_parser_props.py (Hypothesis @given/@settings usage)
    - tests/conftest.py (the file being modified — db_pool + boundary_cache fixtures; add admin_session)
    - .planning/phases/03-admin-loop-pin-manual-entry-undo/03-RESEARCH.md §Validation Architecture (the authoritative Req→Test map; every row below comes from it)
    - .planning/phases/03-admin-loop-pin-manual-entry-undo/03-PATTERNS.md §Test Patterns (analog snippets for each test file)
  </read_first>
  <action>
    Write the complete Wave-0 test surface from the 03-RESEARCH.md §Validation Architecture Req→Test map FIRST, before any implementation. This is the RED step of the phase TDD cycle. Most of these tests target code implemented in plans 02-05; they MUST collect without import errors and fail RED (assertion or expected-501/connection failure) — they go GREEN as later plans land. The boundary-math helper tests (test_fill_level, test_cube_contents, test_midpoint, test_fill_level_property, test_midpoint_property) target Task 3 of THIS plan and will go GREEN when Task 3 implements `boundary_math.py`. Use the exact test names from the Req→Test map so per-task `<verify>` commands in later plans resolve.

    Add an `admin_session` module-scoped fixture to `tests/conftest.py`: it must (a) ensure a test PIN hash and a `SESSION_SECRET` exist for the test DB (seed `gruvax.settings` `auth.pin_hash` via `hash_pin("0000")` and set a test SESSION_SECRET env var if absent), then (b) POST `/api/admin/login` with PIN "0000" and return `{"cookies": res.cookies, "csrf_token": <gruvax_csrf>}`. Use the `client` fixture pattern from test_search.py. Document that Argon2id `time_cost` should be reduced in tests for speed (note in a comment; the actual CryptContext lives in plan 02). The `hash_pin` import this fixture needs is provided by plan 02 — until then the fixture's seeding step may itself be RED, which is expected.

    Unit tests (pure functions, no async):
    - `test_pin.py` — `test_verify_correct_pin`, `test_verify_wrong_pin` against `gruvax.auth.pin` (import inside test or top — RED until plan 02).
    - `test_sessions.py` — `test_hard_cap_expired`, `test_idle_expired` (pure expiry-logic checks against `gruvax.auth.sessions` helpers).
    - `test_boundary_validation.py` — `test_first_gt_last` (a boundary with parse_key(first) > parse_key(last) is rejected by the validator helper).
    - `test_diff_preview.py` — `test_movement_counts` (record-movement count from collection_snapshot for a known boundary change; uses boundary_math + the diff helper from plan 04/05).
    - `test_fill_level.py` — `test_empty_cube` (fill 0.0 for is_empty), `test_overstuffed` (count/capacity > 1.0 possible). Targets boundary_math.count_records_in_boundary (Task 3).
    - `test_cube_contents.py` — `test_sample_subset` (sample is subset of in-range), `test_sample_size` (≤ n, index-stride). Targets boundary_math.sample_records (Task 3).
    - `test_midpoint.py` — `test_midpoint_is_real_record`, `test_midpoint_empty_range`. Targets boundary_math.suggest_midpoint (Task 3).

    Integration tests (LifespanManager + AsyncClient, `loop_scope="session"`):
    - `test_admin_auth.py` — `test_login_success`, `test_login_wrong_pin` (401), `test_rate_limit` (6th attempt → 429), `test_csrf_missing` (POST without X-CSRF-Token → 403), `test_cookie_flags` (gruvax_session is HttpOnly), `test_csrf_cookie_readable` (gruvax_csrf is NOT HttpOnly), `test_logout`, `test_change_pin_revokes_sessions`.
    - `test_boundary_editor.py` — `test_phantom_blocked` (400 + near_misses), `test_phantom_force_save` (force:true bypasses phantom but not comparator), `test_near_misses_returned`, `test_validate_no_db_write` (dry-run diff writes nothing).
    - `test_change_set.py` — `test_bulk_writes_history` (shared change_set_id), `test_idempotency_key_replay` (no double write), `test_revert_writes_inverse` (source='revert'), `test_revert_conflict_skip` (newer change-set → skip+report), `test_revert_is_undoable`.
    - `test_cube_public.py` — `test_cube_not_found` (404 for nonexistent cube), `test_cube_contents_shape` (returns total_count, fill_level, sample_records).

    Property tests (Hypothesis):
    - `test_fill_level_property.py` — count ≥ 0 for any valid boundary; monotone in capacity; labels compared with casefold not normalize_catalog. Targets Task 3.
    - `test_midpoint_property.py` — midpoint suggestion (when returned) is an element of the label's records and its index lies strictly between the two anchors. Targets Task 3.
    - `test_boundary_validation_property.py` — any boundary with parse_key(first_catalog) > parse_key(last_catalog) is rejected; any boundary with first_label.casefold() > last_label.casefold() is rejected.

    For tests targeting not-yet-implemented endpoints/modules, write the assertion as the FINAL intended behavior (do not stub to `assert True`); a clean RED collection + meaningful failures is the deliverable.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_pin.py tests/unit/test_sessions.py tests/unit/test_boundary_validation.py tests/unit/test_diff_preview.py tests/unit/test_fill_level.py tests/unit/test_cube_contents.py tests/unit/test_midpoint.py tests/integration/test_admin_auth.py tests/integration/test_boundary_editor.py tests/integration/test_change_set.py tests/integration/test_cube_public.py tests/property/test_fill_level_property.py tests/property/test_midpoint_property.py tests/property/test_boundary_validation_property.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` over all 14 new/extended test files exits 0 (no import/collection errors)
    - `tests/conftest.py` contains `admin_session` and seeds `auth.pin_hash`
    - Each test function name from the 03-RESEARCH.md §Validation Architecture Req→Test map exists (e.g. `grep -rl "def test_login_success" tests/integration/test_admin_auth.py` non-empty; same for test_rate_limit, test_csrf_missing, test_revert_conflict_skip, test_phantom_blocked)
    - `grep -rc "loop_scope=\"session\"" tests/integration/test_admin_auth.py tests/integration/test_boundary_editor.py tests/integration/test_change_set.py tests/integration/test_cube_public.py` each ≥ 1
    - The boundary-math helper tests (test_fill_level, test_cube_contents, test_midpoint, test_fill_level_property, test_midpoint_property) exist and fail RED at this point (their target, boundary_math.py, lands in Task 3); auth/editor/change-set/public tests also fail RED (their code lands in plans 02-05)
  </acceptance_criteria>
  <done>The complete phase Wave-0 test scaffold collects cleanly and fails RED across the board (no implementation exists yet); admin_session fixture and test PIN seeding are in place; this is the RED checkpoint before Task 3 implements the helpers GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure boundary-math helpers (fill count, sampling, midpoint) — implemented to make the Task 2 helper tests GREEN</name>
  <files>src/gruvax/estimator/boundary_math.py</files>
  <read_first>
    - tests/unit/test_fill_level.py, tests/unit/test_cube_contents.py, tests/unit/test_midpoint.py (the RED tests from Task 2 — the contract this task must satisfy)
    - tests/property/test_fill_level_property.py, tests/property/test_midpoint_property.py (the RED property tests from Task 2)
    - src/gruvax/estimator/collection_snapshot.py (RecordRow shape; get_label_records returns insertion-order list; casefold convention)
    - src/gruvax/estimator/normalize.py (parse_key, catalog_in_range, compare_catalogs — the ONLY legal catalog comparison; labels use .casefold() NEVER normalize_catalog, Pitfall C)
    - src/gruvax/estimator/boundary_cache.py (BoundaryRow shape — the input to count_records_in_boundary)
    - .planning/phases/03-admin-loop-pin-manual-entry-undo/03-RESEARCH.md Pattern 8 + Open Question 4 (multi-label boundary semantics)
  </read_first>
  <behavior>
    - count_records_in_boundary(boundary, snapshot) returns 0 when boundary.is_empty or first_label is None.
    - For a same-label boundary (first_label == last_label), counts records of that label whose catalog is within [first_catalog, last_catalog] using catalog_in_range (parse_key), not raw string.
    - For a multi-label boundary (first_label.casefold() < last_label.casefold()): records of the FIRST label with catalog >= first_catalog (parse_key); records of any MIDDLE label (first < label < last by casefold) fully included; records of the LAST label with catalog <= last_catalog (parse_key).
    - count is monotone non-decreasing as the catalog range widens; never negative.
    - sample_records([], n) == []; sample_records(xs, n) == xs when len(xs) <= n; otherwise returns exactly n records via index-stride, all members of the input.
    - suggest_midpoint returns a record strictly between the two anchor records' indices in the parse_key-sorted label list, or None when there is no record strictly between them.
  </behavior>
  <action>
    Create `src/gruvax/estimator/boundary_math.py` exporting three pure functions (no DB, no I/O), implemented so the helper tests written in Task 2 turn GREEN:

    `count_records_in_boundary(boundary: BoundaryRow, snapshot: CollectionSnapshot) -> int` — implement the multi-label range semantics from 03-RESEARCH.md Open Question 4. Compare labels with `str.casefold()`; compare catalogs ONLY with `parse_key`/`catalog_in_range` from normalize.py (Pitfall C — never `normalize_catalog()` on a label). Iterate the union of `snapshot.get_label_records(...)` for first/last and any middle labels; for same-label boundary use `catalog_in_range`. Return the integer count.

    `sample_records(records_in_range: list[RecordRow], n: int = 7) -> list[RecordRow]` — index-stride even sampling per 03-RESEARCH.md Pattern 8: `[]` for empty; identity when `len <= n`; else `step = len/n` and take `records[int(i*step)]` for i in range(n).

    `suggest_midpoint(label: str, first_anchor_release_id: int, last_anchor_release_id: int, snapshot: CollectionSnapshot) -> RecordRow | None` — sort the label's records by `parse_key(r.catalog_number)`, find the indices of the two anchor release_ids, compute `mid = (i_a + i_b)//2`, return `records[mid]` only if `i_a < mid < i_b` (strictly between — Pitfall 22: a real owned record, never a synthesized string). Return None if either anchor is missing or no record lies strictly between.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_fill_level.py tests/unit/test_cube_contents.py tests/unit/test_midpoint.py tests/property/test_fill_level_property.py tests/property/test_midpoint_property.py -q --tb=short && uv run ruff check src/gruvax/estimator/boundary_math.py && uv run mypy src/gruvax/estimator/boundary_math.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/gruvax/estimator/boundary_math.py` exports `count_records_in_boundary`, `sample_records`, `suggest_midpoint`
    - `pytest tests/unit/test_fill_level.py tests/unit/test_cube_contents.py tests/unit/test_midpoint.py` exits 0 (these were written RED in Task 2 and now go GREEN)
    - `pytest tests/property/test_fill_level_property.py tests/property/test_midpoint_property.py` exits 0
    - `grep -c "normalize_catalog" src/gruvax/estimator/boundary_math.py` is 0 (labels never use normalize_catalog — Pitfall C)
    - `grep -c "parse_key\|catalog_in_range" src/gruvax/estimator/boundary_math.py` ≥ 1 (catalog comparisons go through normalize.py)
    - `uv run ruff check src/gruvax/estimator/boundary_math.py` and `uv run mypy src/gruvax/estimator/boundary_math.py` exit 0
  </acceptance_criteria>
  <done>boundary_math.py provides the three pure helpers; their unit + property tests (authored RED in Task 2) now pass GREEN; lint + type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → build | New deps (passlib[argon2], slowapi) cross into the supply chain |
| migration → database | DDL alters the gruvax schema; a bad migration could corrupt or block startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | settings.py default secret | mitigate | SESSION_SECRET has NO default — boot crashes if unset, preventing an insecure shared default (Runtime State Inventory, 03-RESEARCH.md) |
| T-03-02 | Denial of Service | migration 0004 | mitigate | downgrade()/upgrade() round-trip verified in CI; IF EXISTS guards prevent partial-state failures |
| T-03-03 | Tampering | boundary_math catalog compare | mitigate | All catalog comparisons route through parse_key (normalize.py); labels via casefold — no raw-string compare (Pitfall C / POS-01) |
| T-03-SC | Tampering | passlib[argon2] / slowapi installs | accept | Both [OK] in 03-RESEARCH.md Package Legitimacy Audit (slopcheck 2026-05-20); no [ASSUMED]/[SUS] packages → no blocking human checkpoint required |
</threat_model>

<verification>
- `uv run alembic upgrade head && uv run alembic downgrade base && uv run alembic upgrade head` round-trips clean.
- `uv run pytest tests/unit/test_fill_level.py tests/unit/test_cube_contents.py tests/unit/test_midpoint.py tests/property/test_fill_level_property.py tests/property/test_midpoint_property.py -q` is GREEN.
- `uv run pytest tests/ --collect-only -q` reports no collection errors.
- `uv run ruff check src/gruvax/settings.py src/gruvax/app.py src/gruvax/db/queries.py src/gruvax/estimator/boundary_math.py && uv run mypy src/gruvax/settings.py src/gruvax/db/queries.py src/gruvax/estimator/boundary_math.py` pass.
</verification>

<success_criteria>
- Migration 0004 creates boundary_history, admin_sessions, settings (+seed), idempotency_keys and round-trips cleanly.
- passlib[argon2] + slowapi installed; SESSION_SECRET/SESSION_TTL_SECONDS configured; settings cache loads at startup.
- The full phase Wave-0 test scaffold is authored first (Task 2, RED), then boundary_math.py is implemented (Task 3) so its helper tests go GREEN; endpoint tests remain RED awaiting plans 02-05.
- boundary_math.py provides count_records_in_boundary, sample_records, suggest_midpoint with GREEN unit + property tests.
</success_criteria>

<output>
Create `.planning/phases/03-admin-loop-pin-manual-entry-undo/03-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
