---
phase: 09-tooling-and-docs-hardening
plan: 06
type: execute
wave: 4
depends_on: [09-05]
files_modified:
  - docs/ARCHITECTURE.md
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [D-08, D-09, D-10]
must_haves:
  truths:
    - "docs/ARCHITECTURE.md captures the final Phase 1-8 design (data model + v_collection, API surface, position estimation, LED-over-MQTT, SSE realtime, observability, deploy) with Mermaid-only diagrams"
    - "README.md and CLAUDE.md point at docs/ARCHITECTURE.md and reflect final reality (incl. pull-based GHCR deploy + structlog/orjson)"
    - "CLAUDE.md Architecture section no longer says 'Architecture not yet mapped'"
    - "no lux references remain in README.md or CLAUDE.md; nox appears nowhere (confirmed no-op)"
    - "Implements D-08 (new docs/ARCHITECTURE.md), D-09 (refresh README + CLAUDE.md to point at it) and D-10 (lux strip in README + CLAUDE.md)"
  artifacts:
    - path: "docs/ARCHITECTURE.md"
      provides: "Canonical Phase 1-8 architecture reference with Mermaid diagrams"
      contains: "v_collection"
    - path: "CLAUDE.md"
      provides: "Architecture section pointing at docs/ARCHITECTURE.md (placeholder replaced)"
      contains: "docs/ARCHITECTURE.md"
  key_links:
    - from: "README.md"
      to: "docs/ARCHITECTURE.md"
      via: "Architecture section links the canonical doc"
      pattern: "docs/ARCHITECTURE.md"
    - from: "CLAUDE.md"
      to: "docs/ARCHITECTURE.md"
      via: "Architecture section pointer replacing the 'not yet mapped' placeholder"
      pattern: "docs/ARCHITECTURE.md"
---

<objective>
Capture the final Phase 1-8 design in a new user-facing `docs/ARCHITECTURE.md` (D-08), refresh
`README.md` and the CLAUDE.md Architecture section to point at it and reflect final reality
(D-09 — endpoints, schema, the new pull-based GHCR deploy, structlog/orjson tooling), and strip
the remaining `lux` references from both docs (D-10). Diagrams are Mermaid-only (project convention).

This is the last plan and depends on Plan 05: the docs describe the new pull-based deploy model, so
they must be written AFTER the compose flip lands. The `nox` strip is a confirmed no-op (it appears
nowhere in GRUVAX) — do not invent references to remove.

Purpose: Fill the "Architecture not yet mapped" gap, make ARCHITECTURE.md the canonical reference,
and seed the v2.0 multi-user milestone. No product behavior change.
Output: docs/ARCHITECTURE.md (new), refreshed README.md + CLAUDE.md, zero lux references in docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tooling-and-docs-hardening/09-CONTEXT.md
@.planning/phases/09-tooling-and-docs-hardening/09-RESEARCH.md
@.planning/phases/09-tooling-and-docs-hardening/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/ARCHITECTURE.md (D-08) — Mermaid-only, verified against the live surface</name>
  <files>docs/ARCHITECTURE.md</files>
  <read_first>
    - .planning/phases/09-tooling-and-docs-hardening/09-RESEARCH.md "docs/ARCHITECTURE.md Minimal Outline" (lines 869-952 — the accurate outline) and the "Architecture Patterns" Mermaid diagrams (lines 207-248 — reuse the logging/CI/deploy flowcharts)
    - .planning/phases/09-tooling-and-docs-hardening/09-PATTERNS.md "docs/ARCHITECTURE.md" (lines 763-787 — structure convention + Mermaid rule)
    - docs/runbook-fresh-host.md (the Markdown structure convention: flat # title, ## sections, fenced commands, tables)
    - src/gruvax/app.py (the router registration — VERIFY the actual public + admin endpoint list against the outline; RESEARCH.md flags the endpoint list as MEDIUM confidence — confirm against create_app())
    - src/gruvax/api/ (router files — confirm /api/* + /api/admin/* paths) and the migrations dir (confirm gruvax schema tables incl. the Phase 5 cut-point model)
  </read_first>
  <action>
    Create docs/ARCHITECTURE.md per the RESEARCH.md outline (lines 874-952), VERIFYING each section
    against the live code (do not transcribe the outline blindly — RESEARCH.md marks the endpoint list
    MEDIUM confidence): (1) Data Model — gruvax schema tables + the gruvax.v_collection read-only
    contract (DEP-02) over discogsography; (2) API Surface — the actual public /api/* and PIN-gated
    /api/admin/* endpoints registered in create_app(); (3) Position Estimation — two-level segment-aware
    interpolation + the LocateResult contract + the §4.8 cube-only fallback; (4) LED Contract — MQTT
    topics, Pydantic payloads, HighlightRegistry TTL; (5) Realtime — SSE /api/events events + kiosk
    re-render; (6) Observability — /api/health subsystems + the in-memory log/slow-query rings (reset on
    restart) + /api/admin/diagnostics; (7) Deploy — Compose services, the POST-Phase-9 pull-based GHCR
    image (ghcr.io/simplicityguy/gruvax:latest) + StaticFiles SPA. Embed the three Mermaid flowcharts
    from RESEARCH.md (logging flow, CI orchestration, compose deploy). EVERY diagram is a ```mermaid```
    block (CONVENTIONS.md binding — no ASCII art / prose arrows). Use neutral host placeholders
    (your-server.local / the deployment host) — zero lux references.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && test -f docs/ARCHITECTURE.md && grep -q 'v_collection' docs/ARCHITECTURE.md && test $(grep -c '```mermaid' docs/ARCHITECTURE.md) -ge 1 && test $(grep -c 'lux' docs/ARCHITECTURE.md) -eq 0 && test $(grep -c 'nox' docs/ARCHITECTURE.md) -eq 0 && uv run pre-commit run check-yaml --files docs/ARCHITECTURE.md 2>/dev/null; true</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ARCHITECTURE.md` exists with all seven sections (Data Model, API Surface, Position Estimation, LED Contract, Realtime, Observability, Deploy)
    - `grep -c 'v_collection' docs/ARCHITECTURE.md` >= 1 (the discogsography contract documented)
    - `grep -c 'ghcr.io/simplicityguy/gruvax' docs/ARCHITECTURE.md` >= 1 (the post-Phase-9 deploy)
    - `grep -c '```mermaid' docs/ARCHITECTURE.md` >= 1 AND there is NO ASCII-art/prose-arrow diagram (Mermaid-only)
    - `grep -c 'lux' docs/ARCHITECTURE.md` returns 0 and `grep -c 'nox' docs/ARCHITECTURE.md` returns 0
    - the documented endpoint list matches the routers registered in src/gruvax/app.py (spot-verified, not invented)
  </acceptance_criteria>
  <done>docs/ARCHITECTURE.md is the canonical, code-verified Phase 1-8 reference with Mermaid-only diagrams, the pull-based GHCR deploy, and no host names.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh README.md + CLAUDE.md to point at ARCHITECTURE.md and reflect final reality (D-09 + D-10)</name>
  <files>README.md, CLAUDE.md</files>
  <read_first>
    - README.md (FULL — the banner/badge/nav structure to preserve, lines 1-60, plus the two lux occurrences and the Stack section per PATTERNS.md lines 791-813)
    - CLAUDE.md (the "## Architecture" placeholder section "Architecture not yet mapped" + the four lux occurrences + the Technology Stack section, per PATTERNS.md lines 816-838)
    - docs/ARCHITECTURE.md (Task 1 output — the doc both files now point at)
    - .planning/phases/09-tooling-and-docs-hardening/09-PATTERNS.md "README.md" (lines 791-813) and "CLAUDE.md" (lines 816-838) — the exact targeted edits
    - .planning/phases/09-tooling-and-docs-hardening/09-RESEARCH.md "lux Reference Classification" (lines 843-865 — README ×2, CLAUDE ×4 sites)
  </read_first>
  <action>
    README.md: PRESERVE the centered banner (theme-aware <picture>), shields, tagline, and nav line
    (do not restructure). Add a `## Architecture` section linking docs/ARCHITECTURE.md. Update the Stack
    section to reflect Phase 9 additions (structlog, orjson) and the pull-based GHCR deploy. Strip the two
    lux references (the description line and the hardware/stack table row -> "the home server" /
    "Deployment host"). Update the Python version string to current (3.14) if stale.

    CLAUDE.md: REPLACE the "## Architecture" placeholder ("Architecture not yet mapped. Follow existing
    patterns found in the codebase.") with a pointer to docs/ARCHITECTURE.md (per PATTERNS.md lines
    828-834 — a single pointer, no content duplication). Strip all four lux references (prose ->
    "the deployment host" / `your-server.local`). Reflect the new deploy/tooling reality in the relevant
    sections where it is already discussed (do not introduce new runtime config — D-10). Do NOT touch the
    GSD-managed sections (Developer Profile / Project Skills auto-managed blocks).

    Final repo-wide doc grep: confirm `lux`/`nox` survive ONLY where intended (genericized) across
    README.md, CLAUDE.md, docs/, compose*.yaml.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && grep -q 'docs/ARCHITECTURE.md' README.md && grep -q 'docs/ARCHITECTURE.md' CLAUDE.md && ! grep -q 'Architecture not yet mapped' CLAUDE.md && test $(grep -c 'lux' README.md) -eq 0 && test $(grep -c 'lux' CLAUDE.md) -eq 0 && test $(grep -rc 'nox' README.md CLAUDE.md docs/ | grep -vc ':0$') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'docs/ARCHITECTURE.md' README.md` >= 1 AND `grep -c 'docs/ARCHITECTURE.md' CLAUDE.md` >= 1
    - `grep -c 'Architecture not yet mapped' CLAUDE.md` returns 0 (placeholder replaced)
    - `grep -c 'lux' README.md` returns 0 AND `grep -c 'lux' CLAUDE.md` returns 0
    - `grep -rc 'nox' README.md CLAUDE.md docs/` returns 0 matches everywhere (confirmed no-op — none introduced)
    - the README banner/badge/nav block is intact (still present, unmodified structurally)
    - README Stack section mentions structlog/orjson and the GHCR pull-based deploy
  </acceptance_criteria>
  <done>README.md and CLAUDE.md both point at docs/ARCHITECTURE.md, reflect the final Phase 1-8 reality and the new deploy/tooling, the CLAUDE.md placeholder is gone, and no lux/nox references remain in any doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs → infrastructure disclosure | README/CLAUDE/ARCHITECTURE are committed to a public repo; concrete host names (lux) disclose internal infrastructure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-DOCLEAK | Information Disclosure | lux host names in public docs | mitigate | Strip all lux references to neutral placeholders (D-10); acceptance gates assert `grep -c lux` returns 0 across README/CLAUDE/ARCHITECTURE/docs. |
| T-9-DOCDRIFT | Repudiation | ARCHITECTURE.md endpoint list accuracy | mitigate | Endpoint/schema sections verified against the live routers in src/gruvax/app.py (RESEARCH.md flagged the list MEDIUM confidence — confirm, do not transcribe blindly). |
| T-9-DOCSECRET | Information Disclosure | docs content | accept | Docs describe public architecture only; no secrets/credentials are documented (PIN/session secrets remain in env/GitHub Secrets, never in docs). |
</threat_model>

<verification>
- `docs/ARCHITECTURE.md` exists, is Mermaid-only, and is code-verified against the live endpoint/schema surface.
- README.md and CLAUDE.md both link docs/ARCHITECTURE.md; the CLAUDE placeholder is replaced.
- `grep -rc 'lux' README.md CLAUDE.md docs/ compose.yaml` returns 0 across the board (compose handled in Plan 05; this plan closes the docs).
- `grep -rc 'nox' README.md CLAUDE.md docs/` returns 0 (confirmed no-op; none introduced).
</verification>

<success_criteria>
- docs/ARCHITECTURE.md captures the full Phase 1-8 design with Mermaid-only diagrams and the pull-based GHCR deploy (D-08).
- README.md + CLAUDE.md point at it and reflect final reality including structlog/orjson + pull-based deploy (D-09).
- The "Architecture not yet mapped" gap is filled; no lux references survive in any doc; nox appears nowhere (D-10).
</success_criteria>

<output>
Create `.planning/phases/09-tooling-and-docs-hardening/09-06-SUMMARY.md` when done.
</output>
