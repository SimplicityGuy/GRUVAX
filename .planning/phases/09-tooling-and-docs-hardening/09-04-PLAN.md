---
phase: 09-tooling-and-docs-hardening
plan: 04
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/gruvax/mqtt/lifecycle.py
  - tests/property/test_export_roundtrip.py
  - tests/property/test_import_roundtrip.py
autonomous: true
requirements: [D-06, D-07]
must_haves:
  truths:
    - "uv run ruff check src/ tests/ reports 0 errors (all 69 fixed: 48 auto + 21 manual)"
    - "uv run mypy --strict src/gruvax/ stays clean after the cleanup"
    - "the full test suite stays green — no behavior changed by the lint fixes"
    - "no continue-on-error remains anywhere (the gate is now honest)"
  artifacts:
    - path: "src/gruvax/mqtt/lifecycle.py"
      provides: "SIM105 fix: contextlib.suppress replacing try/except/pass"
      contains: "contextlib.suppress"
  key_links:
    - from: "ruff cleanup"
      to: "code-quality.yml pre-commit gate"
      via: "ruff check exits 0 so pre-commit run --all-files can be honest-green"
      pattern: "ruff"
---

<objective>
Resolve the entire pre-existing ruff lint debt — 69 errors (advisory `continue-on-error` in
Phase 8 CI) — so `code-quality` becomes a true blocking gate (D-06). Per D-07 this is ISOLATED
into its own plan/wave: it is the largest mechanical chunk, independent of the workflow YAML,
and touches `src/` + `tests/` broadly. None of the 69 fixes require a design decision (RESEARCH.md
Pitfall 6 breakdown: 48 auto-fixable via `ruff --fix --unsafe-fixes`, 21 manual across B905×5,
SIM105×1, B017×1, E402×1, RUF059×1).

Depends on Plan 01 (the structlog migration settles `src/gruvax/logging_config.py` +
`src/gruvax/app.py` first) so the broad `src/gruvax/` sweep here never collides with those edits.
No file overlap with Plan 03 (dotfiles/scripts/frontend) — parallel-safe in Wave 2.

Purpose: Make the honest-green CI gate real (a gate allowed to fail is not a gate).
Output: ruff check src/ tests/ exits 0; mypy --strict stays clean; full suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tooling-and-docs-hardening/09-CONTEXT.md
@.planning/phases/09-tooling-and-docs-hardening/09-RESEARCH.md
@.planning/phases/09-tooling-and-docs-hardening/09-PATTERNS.md
@.planning/phases/09-tooling-and-docs-hardening/09-VALIDATION.md

<interfaces>
<!-- Ruff config (pyproject.toml) the cleanup must satisfy — DO NOT relax it:
  [tool.ruff.lint]
  select = ["E", "F", "W", "I", "UP", "B", "C4", "SIM", "RUF"]
  ignore = ["E501"]

69-error breakdown (RESEARCH.md Pitfall 6 / Ruff Error Detail):
  AUTO (48, ruff --fix --unsafe-fixes): I001x17, F401x17, RUF002x15, RUF003x4,
    RUF005x2, UP037x2, RUF023x1, SIM118x1, C416x1
  MANUAL (21):
    B905 x5  — add strict=False (or True) to zip(): tests/property/test_export_roundtrip.py,
               tests/property/test_import_roundtrip.py, + 3 src locations
    SIM105 x1 — try/except/pass -> contextlib.suppress in src/gruvax/mqtt/lifecycle.py
    B017  x1 — pytest.raises(Exception) -> specific exception (e.g. yaml.YAMLError)
    E402  x1 — move module import to top of a test file
    RUF059 x1 — remove unused walrus variable `app` from a test file
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply all auto-fixable ruff fixes (48 errors)</name>
  <files>src/ (auto-fix sweep), tests/ (auto-fix sweep)</files>
  <read_first>
    - .planning/phases/09-tooling-and-docs-hardening/09-RESEARCH.md "Pitfall 6: 69 ruff errors" (lines 776-798) and "Ruff Error Detail — Wave Isolation" (lines 817-839) — the exact auto vs manual split
    - .planning/phases/09-tooling-and-docs-hardening/09-PATTERNS.md "pyproject.toml" (lines 194-211 — the ruff rule set; fix violations, do NOT change config)
    - justfile (the lint-fix recipe — `ruff check --fix --unsafe-fixes`)
  </read_first>
  <action>
    Run the auto-fixer across the source + test tree: `uv run ruff check --fix --unsafe-fixes src/ tests/`.
    This resolves the 48 auto-fixable errors (I001 import sorting x17, F401 unused imports x17, RUF002
    EN-DASH x15, RUF003 x4, RUF005 x2, UP037 x2, RUF023 x1, SIM118 x1, C416 x1). Then run
    `uv run ruff format src/ tests/` to keep formatting consistent. Do NOT edit pyproject.toml's ruff
    config (the rule set stays exactly as-is — fix violations, not the gate). After this step, the only
    remaining ruff errors must be the 21 manual ones (B905x5, SIM105x1, B017x1, E402x1, RUF059x1).
    Verify the diff did not touch src/gruvax/logging_config.py or app.py in a way that conflicts with
    Plan 01 (those should already be lint-clean from Plan 01; if ruff --fix touches them it is benign
    import-sorting only).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && uv run ruff check src/ tests/ 2>&1 | tail -5; uv run ruff format --check src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - After this task, `uv run ruff check src/ tests/` reports at most 21 remaining errors, and EVERY remaining error code is one of B905, SIM105, B017, E402, RUF059 (no auto-fixable codes left): verify with `uv run ruff check src/ tests/ --output-format=concise | grep -oE '[A-Z]+[0-9]+' | sort -u`
    - `uv run ruff format --check src/ tests/` exits 0 (formatting consistent)
    - `git diff --stat` shows changes confined to src/ and tests/ (no pyproject.toml ruff-config change)
  </acceptance_criteria>
  <done>All 48 auto-fixable ruff errors are resolved and formatting is consistent; only the 21 known manual errors remain.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the 21 manual ruff fixes (B905, SIM105, B017, E402, RUF059)</name>
  <files>src/gruvax/mqtt/lifecycle.py, tests/property/test_export_roundtrip.py, tests/property/test_import_roundtrip.py</files>
  <read_first>
    - .planning/phases/09-tooling-and-docs-hardening/09-RESEARCH.md "Ruff Error Detail — Wave Isolation" "Manual edits required" (lines 830-839) — the exact per-rule, per-file fixes
    - src/gruvax/mqtt/lifecycle.py (the SIM105 try/except/pass to replace with contextlib.suppress)
    - tests/property/test_export_roundtrip.py and tests/property/test_import_roundtrip.py (the B905 zip() calls needing strict=)
    - the output of `uv run ruff check src/ tests/ --output-format=concise` (run it to get the exact remaining file:line for B017, E402, RUF059, and the 3 src B905 sites)
  </read_first>
  <action>
    Resolve each remaining manual error guided by `ruff check --output-format=concise` (it names the
    exact file:line for all 21):
    - B905 x5: add `strict=False` to each flagged `zip(...)` call (use strict=False to preserve current
      behavior unless the two iterables are provably equal-length, in which case strict=True is safer) in
      tests/property/test_export_roundtrip.py, tests/property/test_import_roundtrip.py, and the 3 src
      locations ruff names. Choose strict=False where lengths may differ; do not change runtime behavior.
    - SIM105 x1: in src/gruvax/mqtt/lifecycle.py, replace the try/except/pass block with
      `with contextlib.suppress(<the exact exception being swallowed>):` (add `import contextlib`).
    - B017 x1: replace `pytest.raises(Exception)` with the specific exception (e.g. `pytest.raises(yaml.YAMLError)`)
      at the ruff-named test site; import the exception if needed.
    - E402 x1: move the flagged module import to the top of the test file (above any comment block / code).
    - RUF059 x1: remove the unused walrus-assigned variable `app` (replace `app := ...` with the bare
      expression, or delete the assignment) at the ruff-named test site.
    Re-run ruff after each edit cluster. These are mechanical; none change behavior.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && uv run ruff check src/ tests/ && uv run mypy --strict src/gruvax/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check src/ tests/` exits 0 (0 errors — D-06 satisfied)
    - `grep -c 'contextlib.suppress' src/gruvax/mqtt/lifecycle.py` returns >= 1
    - `grep -c 'pytest.raises(Exception)' tests/` (recursive) returns 0
    - `uv run mypy --strict src/gruvax/` exits 0 (no new type regressions)
  </acceptance_criteria>
  <done>All 21 manual ruff errors are fixed mechanically; `ruff check src/ tests/` exits 0 and mypy --strict stays clean.</done>
</task>

<task type="auto">
  <name>Task 3: Prove zero behavior change — full suite green</name>
  <files>tests/ (verification only — corrective edits if a manual fix altered test intent)</files>
  <read_first>
    - .planning/phases/09-tooling-and-docs-hardening/09-VALIDATION.md "Sampling Rate" + "Per-Task Verification Map" (the pre-commit honest-green row)
    - any test file edited in Task 2 (confirm the assertion intent is preserved after B017/E402/RUF059 edits)
  </read_first>
  <action>
    Run the full backend test suite twice back-to-back (project memory: the suite must stay
    order-independent on the shared dev Postgres) to prove the lint cleanup changed no behavior. If any
    test now fails because a manual fix altered intent (most likely the B017 specific-exception change or
    the RUF059 walrus removal), correct the test so it asserts the same behavior as before — do NOT
    weaken assertions to make them pass. Confirm `ruff check` is still 0 and `ruff format --check` is
    clean after any test edits.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && uv run pytest tests/ -q && uv run pytest tests/ -q && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q` exits 0 on two consecutive runs (order-independent, no regression)
    - `uv run ruff check src/ tests/` exits 0
    - `uv run ruff format --check src/ tests/` exits 0
    - `uv run mypy --strict src/gruvax/` exits 0
  </acceptance_criteria>
  <done>The full suite passes twice consecutively with the lint cleanup applied; ruff/format/mypy are all clean — the honest-green CI gate is now achievable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lint auto-fix → source behavior | `ruff --fix --unsafe-fixes` and manual edits could silently change runtime behavior if a "fix" alters intent (e.g. B017 specific-exception, RUF059 walrus removal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-FIXREGRESS | Tampering | unsafe-fixes + manual edits to src/tests | mitigate | Full suite run twice after the cleanup (Task 3); no assertion weakening allowed; mypy --strict re-run. A behavior change would surface as a test failure. |
| T-9-GATEHOLE | Repudiation | the honest-green gate | mitigate | ruff config is NOT relaxed (fix violations, not the gate); `continue-on-error` is dropped in Plan 02; this plan makes `ruff check src/ tests/` exit 0 so the Plan 02/03 pre-commit gate is genuinely blocking (D-06). |
| T-9-B608 | Information Disclosure | swallowed-exception SIM105 fix | accept | contextlib.suppress narrows to the exact exception being swallowed (no broader suppression than the original try/except). bandit B608 (SQL injection) remains gated separately. |
</threat_model>

<verification>
- `uv run ruff check src/ tests/` exits 0 (0 of the original 69 errors remain).
- `uv run ruff format --check src/ tests/` exits 0.
- `uv run mypy --strict src/gruvax/` exits 0.
- `uv run pytest tests/ -q` exits 0 twice consecutively (zero behavior change, order-independent).
</verification>

<success_criteria>
- All 69 ruff errors resolved (48 auto + 21 manual); ruff check src/ tests/ exits 0 (D-06).
- mypy --strict stays clean; full suite stays green (zero product-behavior change).
- The CI gate can now be honest (no continue-on-error needed) — D-06/D-07 satisfied in isolation.
</success_criteria>

<output>
Create `.planning/phases/09-tooling-and-docs-hardening/09-04-SUMMARY.md` when done.
</output>
