---
phase: 03-devices-pairing
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_fingerprint_cookie.py
  - tests/integration/test_devices.py
  - tests/integration/test_migrate_0011.py
  - tests/browser/__init__.py
  - tests/browser/test_reboot_persistence.py
  - frontend/src/routes/kiosk/PairView.tsx
  - frontend/src/routes/kiosk/PairView.test.tsx
  - frontend/src/routes/admin/DeviceDrawer.tsx
  - frontend/src/routes/admin/DeviceDrawer.test.tsx
  - pyproject.toml
  - uv.lock
autonomous: false
requirements: [DEV-01, DEV-02, DEV-03]
user_setup: []

must_haves:
  truths:
    - "Every Phase 3 requirement has a RED test that fails for the right reason before implementation lands"
    - "playwright + pytest-playwright are available in the dev dependency group and chromium is installed"
    - "Frontend logic (countdown auto-reroll, NumericKeypad auto-submit) has focused RED Vitest tests that run (assertion-RED, not import-error)"
  artifacts:
    - path: "tests/unit/test_fingerprint_cookie.py"
      provides: "Cookie attribute contract (HttpOnly + SameSite=Strict + max_age >= 30d)"
      contains: "def test_fingerprint_cookie_is_httponly"
    - path: "tests/integration/test_devices.py"
      provides: "Full pairing/bind/revoke/reassign/concurrency/session/poll/SSE suite (11 tests)"
      contains: "def test_bind_success"
    - path: "tests/integration/test_migrate_0011.py"
      provides: "Migration 0011 round-trip gate"
      contains: "upgrade"
    - path: "tests/browser/test_reboot_persistence.py"
      provides: "Playwright persistent-context reboot simulation scaffold"
      contains: "launch_persistent_context"
    - path: "frontend/src/routes/kiosk/PairView.test.tsx"
      provides: "Countdown M:SS + auto-reroll-on-expiry RED test"
      contains: "PairView"
    - path: "frontend/src/routes/admin/DeviceDrawer.test.tsx"
      provides: "NumericKeypad auto-submit-on-4th-digit RED test"
      contains: "DeviceDrawer"
  key_links:
    - from: "tests/integration/test_devices.py"
      to: "gruvax.api.admin.limiter"
      via: "reset_bind_rate_limit autouse fixture"
      pattern: "limiter\\.reset"
---

<objective>
Wave 0 test scaffolding for Phase 3. Create the RED test files that every later plan turns GREEN, and add the dev-only Playwright dependency. Every requirement (DEV-01/02/03) gets failing tests up front so the Nyquist sampling map in 03-VALIDATION.md has real `<automated>` targets — including focused frontend Vitest tests for the logic-heavy `/pair` countdown and DeviceDrawer keypad so 03-04 gets sub-10s feedback.

Purpose: Lock the behavioral contract before implementation. Backend tests fail because the code does not exist (import errors / 404 / missing tables). Frontend tests run against minimal placeholder stub components so they are RED on assertions (the proper RED→GREEN signal), not import errors — 03-04 replaces the stubs with the real implementations.
Output: 4 backend/browser test files + 2 frontend Vitest test files + 2 minimal frontend stub components + 1 test package marker, Playwright in the dev group, chromium installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-devices-pairing/03-RESEARCH.md
@.planning/phases/03-devices-pairing/03-PATTERNS.md
@.planning/phases/03-devices-pairing/03-VALIDATION.md

<interfaces>
Test contracts the later plans must satisfy (target identifiers — backend ones do NOT exist yet, that is why backend tests are RED):

Backend (Python):
- gruvax.auth.sessions.issue_fingerprint_cookie(response, secure=False) -> str
- gruvax.auth.sessions.get_fingerprint(request) -> str | None
- gruvax.auth.sessions.FINGERPRINT_COOKIE = "gruvax_device_fp"
- gruvax.auth.sessions.FINGERPRINT_MAX_AGE = 30 * 24 * 3600
- gruvax.api.admin.limiter._BIND_RATE
- POST /api/devices/pairing-codes -> {code, expires_at}; sets HttpOnly gruvax_device_fp cookie
- GET /api/devices/me -> {state: "unpaired"|"pending"|"paired"|"revoked", profile_id}
- POST /api/admin/devices/bind {code} -> 200 (PIN-gated, rate-limited); 404 {type:"code_not_found"} / {type:"code_expired"}; 429 {type:"rate_limited"}
- GET /api/admin/devices -> grouped list
- PATCH /api/admin/devices/{id} (rename / change-profile / unbind)
- POST /api/admin/devices/{id}/revoke ; reinstate ; DELETE
- GET /api/session -> adds device_id + is_device_paired when fingerprint maps to paired device
- migration 0011 creates gruvax.devices + gruvax.pairing_codes
- SSE: device_revoked on revoke; device_reassigned on change-profile (OLD profile channel)

Frontend (this plan creates minimal stubs; 03-04 implements them fully):
- PairView (default export from frontend/src/routes/kiosk/PairView.tsx) — /pair route; renders countdown M:SS; auto-rerolls on 0:00
- DeviceDrawer (named export from frontend/src/routes/admin/DeviceDrawer.tsx) — bind mode uses NumericKeypad; auto-submits on the 4th digit
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy for playwright + pytest-playwright before install</name>
  <what-built>Nothing installed yet — this gate precedes `uv add`. 03-RESEARCH.md Package Legitimacy Audit tagged both packages [ASSUMED] because slopcheck was unavailable at research time. Per the security threat model (T-03-SC), package installs require a blocking human checkpoint for [ASSUMED] packages.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/playwright/ — confirm it is the Microsoft-maintained official package (source: github.com/microsoft/playwright-python), latest 1.60.0.
    2. Open https://pypi.org/project/pytest-playwright/ — confirm it is the official Playwright pytest plugin from the same Microsoft repo, latest 0.8.0.
    3. Confirm both are dev-only additions (no runtime impact).
  </how-to-verify>
  <action>STOP. Do not run `uv add` until the human approves. Present the two PyPI pages above for verification of the [ASSUMED]-tagged packages (T-03-SC). This checkpoint is never auto-approvable regardless of workflow.auto_advance.</action>
  <verify>Human confirms both packages are the official Microsoft Playwright packages on pypi.org before any install proceeds.</verify>
  <resume-signal>Type "approved" to proceed with `uv add --group dev playwright pytest-playwright`, or describe concerns.</resume-signal>
  <done>Human has typed "approved" after verifying both packages on pypi.org.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright dev dependency + install chromium</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (the `[dependency-groups]` / dev group section and `[tool.pytest.ini_options]`)
    - .planning/phases/03-devices-pairing/03-RESEARCH.md (Latest Version Check — A4 note: playwright 1.60.0 has no 3.14 classifier; verify it imports on the 3.14 image)
  </read_first>
  <action>Run `uv add --group dev playwright pytest-playwright` to add playwright 1.60.0 and pytest-playwright 0.8.0 to the dev dependency group only. Then run `playwright install chromium`. Verify the wrapper imports under Python 3.14 by running `python -c "import playwright; from playwright.async_api import async_playwright"` — if it raises on 3.14 (RESEARCH A4 risk), note in the SUMMARY that browser tests must run on a python3.13 venv step in CI, but do NOT block this plan. Do NOT add either package to the runtime dependency list — they are dev/test only.</action>
  <verify>
    <automated>python -c "import playwright; from playwright.async_api import async_playwright; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml dev group contains `playwright` and `pytest-playwright`
    - `python -c "from playwright.async_api import async_playwright"` exits 0
    - Neither package appears in the runtime `dependencies` array
  </acceptance_criteria>
  <done>playwright + pytest-playwright in dev group; chromium installed; async API imports on the target Python.</done>
</task>

<task type="auto">
  <name>Task 3: Write RED backend + browser test files for DEV-01/02/03</name>
  <files>tests/unit/test_fingerprint_cookie.py, tests/integration/test_devices.py, tests/integration/test_migrate_0011.py, tests/browser/__init__.py, tests/browser/test_reboot_persistence.py</files>
  <read_first>
    - tests/unit/test_sessions.py (pure-function unit-test pattern + how it imports from gruvax.auth.sessions)
    - tests/integration/test_admin_auth.py (ASGI client fixture, LifespanManager, reset_login_rate_limit autouse fixture, loop_scope="session" assertions)
    - tests/integration/test_migrate_0010.py (migration round-trip test pattern — subprocess alembic upgrade/downgrade)
    - tests/integration/test_sse_per_profile.py (how SSE events are asserted against the per-profile EventBus; the `live_server` uvicorn-in-thread fixture pattern at lines ~44-79)
    - tests/integration/test_session_bootstrap.py (GET /api/session response-shape assertions)
    - .planning/phases/03-devices-pairing/03-RESEARCH.md (Pattern 5 Playwright verbatim; Validation Architecture → Phase Requirements → Test Map for the exact test names)
    - .planning/phases/03-devices-pairing/03-PATTERNS.md (test_devices.py + test_fingerprint_cookie.py analog blocks)
    - .planning/phases/03-devices-pairing/03-VALIDATION.md (Per-Task Verification Map — every test name + command listed there must exist)
  </read_first>
  <action>Create the backend + browser RED test files mirroring the analog patterns. They MUST fail today (import errors / 404 / missing tables), not on bad assertions.

tests/unit/test_fingerprint_cookie.py — `test_fingerprint_cookie_is_httponly` asserting issue_fingerprint_cookie sets httponly=True, samesite="strict", max_age >= 30*24*3600, and returns a value len >= 40 (secrets.token_urlsafe(32)); `test_clear_fingerprint_cookie_matches_attributes` asserting delete_cookie attributes match set_cookie (CR-04). Use MagicMock for Response per the analog.

tests/integration/test_devices.py — add a `reset_bind_rate_limit` autouse fixture calling `from gruvax.api.admin.limiter import limiter; limiter.reset()` AND `reset_login_rate_limit` for the PIN path. Module-scoped `client` ASGITransport fixture. Implement exactly these 11 tests from 03-VALIDATION.md (all `@pytest.mark.asyncio(loop_scope="session")`): test_generate_code, test_me_transitions_to_paired, test_bind_success, test_bind_rate_limit, test_revoke_guard, test_profile_soft_delete_detaches, test_concurrent_bind, test_session_returns_device, test_sse_device_revoked, test_sse_device_reassigned, test_expired_code. For test_sse_device_reassigned: pair a device to profile A, then admin PATCH change-profile to profile B, and assert a `device_reassigned` event carrying the device_id arrives on the OLD profile (A) SSE channel (reassign half of criterion #3 — use the per-profile EventBus assertion pattern from test_sse_per_profile.py). For the PIN-gated bind tests, authenticate via the existing admin-login flow used in test_admin_auth.py / test_profile_manager_api.py. For test_concurrent_bind use asyncio.gather on two bind calls with the same code and assert exactly one 200 + one 404.

tests/integration/test_migrate_0011.py — copy test_migrate_0010.py's subprocess round-trip pattern (upgrade head -> downgrade -1 -> upgrade head exits 0); assert gruvax.devices and gruvax.pairing_codes exist after upgrade and are gone after downgrade to 0010.

tests/browser/__init__.py — empty package marker.

tests/browser/test_reboot_persistence.py — RESEARCH.md Pattern 5 scaffold: `test_fingerprint_persists_across_reboot` using launch_persistent_context with a tmp user_data_dir, asserting the gruvax_device_fp cookie has httpOnly True, sameSite "Strict", expires > now + 86400, and survives a context close + relaunch from the same dir. Guard the module with `pytest.importorskip("playwright")`. (Plan 03-05 fleshes out the live-server wiring + creates tests/browser/conftest.py `live_server` fixture; this file may be RED/skip-pending until then.)

Do NOT implement any production code in this plan — only tests + the dependency add.</action>
  <verify>
    <automated>pytest tests/unit/test_fingerprint_cookie.py tests/integration/test_devices.py tests/integration/test_migrate_0011.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All five backend/browser files exist; `pytest --collect-only` succeeds (no syntax/collection errors)
    - `pytest tests/unit/test_fingerprint_cookie.py -x` FAILS with ImportError on gruvax.auth.sessions.issue_fingerprint_cookie (RED for the right reason)
    - `pytest tests/integration/test_devices.py::test_bind_success -x` FAILS with 404/Not Found (endpoint absent), not assertion mismatch
    - test_devices.py contains all 11 test function names from 03-VALIDATION.md Per-Task Verification Map (incl. test_sse_device_reassigned)
    - tests/browser/test_reboot_persistence.py contains `launch_persistent_context` and `pytest.importorskip("playwright")`
  </acceptance_criteria>
  <done>Backend + browser RED test files committed; all 11 backend test names present (incl. reassign); failures are due to missing implementation only.</done>
</task>

<task type="auto">
  <name>Task 4: Write RED frontend Vitest tests + minimal stubs for /pair countdown + DeviceDrawer keypad</name>
  <files>frontend/src/routes/kiosk/PairView.tsx, frontend/src/routes/kiosk/PairView.test.tsx, frontend/src/routes/admin/DeviceDrawer.tsx, frontend/src/routes/admin/DeviceDrawer.test.tsx</files>
  <read_first>
    - frontend/src/routes/kiosk/StalenessBar.test.tsx (Vitest + testing-library render/timer pattern; how fake timers are used)
    - frontend/src/routes/admin/ProfileDrawer.test.tsx (drawer Vitest pattern; how the drawer is mounted + interactions fired)
    - frontend/src/routes/kiosk/KioskView.EventSource.test.tsx (mocking fetch / EventSource in Vitest)
    - frontend/src/routes/admin/NumericKeypad.tsx (digit-key click contract the auto-submit test drives)
    - frontend/src/routes/admin/ProfileDrawer.tsx (export style + props shape for the DeviceDrawer stub signature)
    - .planning/phases/03-devices-pairing/03-UI-SPEC.md (Surface 1 countdown behavior + auto-reroll; Device Drawer bind auto-submit on 4th digit)
    - .planning/phases/03-devices-pairing/03-VALIDATION.md (frontend-unit rows)
  </read_first>
  <action>Create two minimal placeholder stub components plus two focused RED Vitest tests. The stubs exist ONLY so the tests resolve their imports, typecheck, and run RED on assertions (proper RED→GREEN). 03-04 replaces the stub bodies with the real implementations against these same tests.

frontend/src/routes/kiosk/PairView.tsx (STUB) — `export function PairView() { return null }` (or a trivial `<div data-testid="pair-view" />`). No countdown, no fetch — so the assertions below fail.

frontend/src/routes/kiosk/PairView.test.tsx — import `{ PairView } from './PairView'`. Use `vi.useFakeTimers()`. Mock global fetch so POST /api/devices/pairing-codes returns {code, expires_at ~5 min out} and GET /api/devices/me returns {state:"unpaired"}. Wrap render in QueryClientProvider + MemoryRouter as the analog tests do. Assert: (a) the countdown renders in M:SS format (e.g. "4:59"); (b) advancing fake timers to expiry (0:00) triggers a SECOND POST /api/devices/pairing-codes (auto-reroll) — assert the pairing-code fetch mock was called twice. Against the stub these assertions FAIL (RED).

frontend/src/routes/admin/DeviceDrawer.tsx (STUB) — `export function DeviceDrawer(_props: { device?: unknown; mode?: string; onClose?: () => void }) { return null }`. Match the prop names the real component will use (mirror ProfileDrawer's prop shape) so the test's props typecheck. No keypad — so the bind assertion fails.

frontend/src/routes/admin/DeviceDrawer.test.tsx — import `{ DeviceDrawer } from './DeviceDrawer'`. Render the drawer in bind/ADD-DEVICE mode; mock fetch for POST /api/admin/devices/bind. Query the NumericKeypad digit keys and click "1","2","3","4"; assert exactly one POST to /api/admin/devices/bind fires automatically after the 4th digit (no separate submit click) with body code "1234". Against the stub this FAILS (RED).

Do NOT implement the real PairView/DeviceDrawer logic here — stubs only. These give 03-04 sub-10s per-task feedback.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist; PairView.tsx and DeviceDrawer.tsx are minimal stubs exporting the named symbols the tests import
    - `cd frontend && npx tsc --noEmit` exits 0 (stubs + tests typecheck cleanly)
    - `cd frontend && npx vitest run src/routes/kiosk/PairView.test.tsx` runs and FAILS on assertions (countdown/auto-reroll absent) — RED, NOT an import error
    - `cd frontend && npx vitest run src/routes/admin/DeviceDrawer.test.tsx` runs and FAILS on assertions (no auto-submit) — RED, NOT an import error
    - PairView test asserts a second pairing-code fetch on expiry; DeviceDrawer test asserts a single bind POST on the 4th digit
  </acceptance_criteria>
  <done>Two RED Vitest tests + two minimal stubs committed; tsc clean; both tests run assertion-RED; 03-04 has focused frontend signals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → PyPI | pip install pulls third-party packages (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | playwright + pytest-playwright installs | mitigate | Blocking human checkpoint (Task 1) verifies both [ASSUMED] packages on pypi.org before `uv add`; both are official Microsoft packages; dev-group only (no runtime exposure); never auto-approvable |
</threat_model>

<verification>
- `pytest --collect-only` clean across the three pytest-collectable files; backend tests RED for missing-implementation reasons.
- `cd frontend && npx tsc --noEmit` clean (stubs + tests typecheck); both Vitest files run assertion-RED.
- playwright imports on the target Python.
</verification>

<success_criteria>
- 4 backend/browser test files + 2 frontend Vitest tests + 2 frontend stubs + tests/browser package marker exist.
- playwright + pytest-playwright in the dev group; chromium installed.
- Every requirement (DEV-01/02/03) has at least one RED test, including the reassign SSE path and the two frontend logic units.
</success_criteria>

<output>
Create `.planning/phases/03-devices-pairing/03-00-SUMMARY.md` when done.
</output>
