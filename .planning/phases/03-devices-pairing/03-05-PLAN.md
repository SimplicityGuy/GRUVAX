---
phase: 03-devices-pairing
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - deploy/kiosk/start-kiosk.sh
  - deploy/kiosk/gruvax-kiosk.service
  - deploy/kiosk/README.md
  - tests/browser/test_reboot_persistence.py
autonomous: true
requirements: [DEV-01]
user_setup: []

must_haves:
  truths:
    - "A committed kiosk launcher + systemd --user unit point Chromium at http://gruvax.lan/pair with a persistent --user-data-dir on the SD card"
    - "The fingerprint cookie survives a Chromium persistent-context close + relaunch (reboot simulation), proving DEV-01 reboot persistence"
    - "The deploy README documents the real-hardware reboot smoke test as the manual contract"
  artifacts:
    - path: "deploy/kiosk/start-kiosk.sh"
      provides: "Chromium kiosk launcher with persistent user-data-dir"
      contains: "user-data-dir"
    - path: "deploy/kiosk/gruvax-kiosk.service"
      provides: "systemd --user unit (Restart=always)"
      contains: "Restart=always"
    - path: "tests/browser/test_reboot_persistence.py"
      provides: "Playwright persistent-context reboot round-trip"
      contains: "launch_persistent_context"
  key_links:
    - from: "deploy/kiosk/start-kiosk.sh"
      to: "http://gruvax.lan/pair"
      via: "Chromium --app= URL"
      pattern: "/pair"
    - from: "tests/browser/test_reboot_persistence.py"
      to: "gruvax_device_fp cookie"
      via: "persistent user-data-dir survives relaunch"
      pattern: "gruvax_device_fp"
---

<objective>
The Pi provisioning artifacts (D3-08) and the reboot-persistence proof (D3-09). A committed `start-kiosk.sh` + `systemd --user` unit + README launch Chromium at `http://gruvax.lan/pair` with a persistent `--user-data-dir` on the SD card so the fingerprint cookie survives reboot. The Playwright persistent-context test is the closest faithful CI simulation of a reboot; the cookie-attribute unit test (03-01) is the contract, and the real-hardware reboot is documented as a manual smoke step.

Purpose: DEV-01 reboot persistence (criterion #2). Ties the provisioning URL (Area 4) to the routing decision (`/pair`, Area 1).
Output: 3 deploy artifacts + 1 implemented browser test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-devices-pairing/03-RESEARCH.md
@.planning/phases/03-devices-pairing/03-VALIDATION.md
@.planning/phases/03-devices-pairing/03-CONTEXT.md

<interfaces>
From 03-01: fingerprint cookie name `gruvax_device_fp`, HttpOnly + SameSite=Strict + max_age = 30 days.
From 03-02: POST /api/devices/pairing-codes issues the cookie; POST /api/admin/devices/bind binds.
From 03-04: /pair route renders and triggers the pairing-code request.
RESEARCH Pattern 5: full Playwright persistent-context test body (verbatim source).
RESEARCH Pattern 6: start-kiosk.sh + gruvax-kiosk.service verbatim source (CLAUDE.md §Raspberry Pi Kiosk).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Committed kiosk provisioning artifacts</name>
  <files>deploy/kiosk/start-kiosk.sh, deploy/kiosk/gruvax-kiosk.service, deploy/kiosk/README.md</files>
  <read_first>
    - .planning/phases/03-devices-pairing/03-RESEARCH.md (Pattern 6 — start-kiosk.sh full flag set, gruvax-kiosk.service unit, user-data-dir path recommendation, Pitfall 2 tmpfs warning, crash-recovery Preferences patch)
    - .planning/phases/03-devices-pairing/03-CONTEXT.md (D3-08 artifact scope — launcher + unit + README only, NOT a full provisioner)
    - .planning/phases/03-devices-pairing/03-VALIDATION.md (Manual-Only Verifications — real reboot smoke + <30s end-to-end stopwatch)
    - CLAUDE.md (§Recommended Stack — Raspberry Pi Kiosk: Chromium flags, --password-store=basic, labwc/Wayland, --ozone-platform=wayland)
  </read_first>
  <action>Create `deploy/kiosk/start-kiosk.sh` per RESEARCH Pattern 6: `set -euo pipefail`; `GRUVAX_URL="${GRUVAX_URL:-http://gruvax.lan/pair}"`; `USER_DATA_DIR="${USER_DATA_DIR:-${HOME}/.local/share/gruvax-kiosk}"` (SD-card path, NOT /tmp or tmpfs — Pitfall 2); mkdir -p the dir; patch Preferences exit_type Crashed->Normal to suppress the restore-tabs dialog; `exec chromium-browser --kiosk --noerrdialogs --disable-infobars --no-first-run --password-store=basic --ozone-platform=wayland --user-data-dir="$USER_DATA_DIR" --app="$GRUVAX_URL"`. Make it executable (chmod +x). Create `deploy/kiosk/gruvax-kiosk.service` per Pattern 6: [Unit] After=graphical-session.target; [Service] Type=simple, ExecStart=%h/.config/gruvax/start-kiosk.sh, Restart=always, RestartSec=3, Environment=GRUVAX_URL=http://gruvax.lan/pair; [Install] WantedBy=default.target. Create `deploy/kiosk/README.md` documenting: install steps (copy start-kiosk.sh to ~/.config/gruvax/, enable the systemd --user unit), the critical persistent-storage requirement (user-data-dir must be on the SD card, never tmpfs — cookies vanish on reboot otherwise, Pitfall 2), and the MANUAL reboot smoke test from 03-VALIDATION.md Manual-Only Verifications (pair the kiosk, `sudo reboot`, confirm it auto-loads the bound-profile search UI without re-pairing) plus the <30s end-to-end stopwatch check. Any diagram MUST be a Mermaid block (project convention — never ASCII art).</action>
  <verify>
    <automated>bash -n deploy/kiosk/start-kiosk.sh && grep -q "gruvax.lan/pair" deploy/kiosk/start-kiosk.sh && grep -q "Restart=always" deploy/kiosk/gruvax-kiosk.service && grep -q "user-data-dir" deploy/kiosk/start-kiosk.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n deploy/kiosk/start-kiosk.sh` exits 0 (valid shell syntax)
    - start-kiosk.sh contains `--app="$GRUVAX_URL"` defaulting to http://gruvax.lan/pair and `--user-data-dir` on a non-tmpfs path
    - gruvax-kiosk.service contains `Restart=always` and `ExecStart=%h/.config/gruvax/start-kiosk.sh`
    - README documents the tmpfs warning AND the manual reboot smoke test
    - start-kiosk.sh is chmod +x
  </acceptance_criteria>
  <done>Committed launcher + unit + README; URL is /pair; user-data-dir persistent; manual reboot test documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Playwright reboot-persistence test</name>
  <files>tests/browser/test_reboot_persistence.py</files>
  <read_first>
    - tests/browser/test_reboot_persistence.py (the RED scaffold from 03-00 — flesh out the body)
    - .planning/phases/03-devices-pairing/03-RESEARCH.md (Pattern 5 verbatim — launch_persistent_context, cookie assertions, second-launch persistence check; A4 Python 3.14 caveat; Pitfall 1 max_age; Environment Availability — chromium install + importorskip guard)
    - .planning/phases/03-devices-pairing/03-VALIDATION.md (browser test command + Wave 0 requirements)
    - tests/integration/test_devices.py (how the live app/server fixture is constructed; how bind is invoked)
  </read_first>
  <behavior>
    - First launch: persistent context with a tmp user_data_dir visits /pair, which issues the gruvax_device_fp cookie; assert the cookie is httpOnly True, sameSite "Strict", expires > now + 86400 (NOT a session cookie — Pitfall 1)
    - Pair the device (fetch the displayed code, call POST /api/admin/devices/bind as admin)
    - Close the context (simulates reboot), relaunch with the SAME user_data_dir
    - Second launch: the fingerprint cookie is present and its value is identical; GET /api/session returns a non-null bound_profile_id + device_id
    - Module guarded by pytest.importorskip("playwright") so CI without chromium skips cleanly
  </behavior>
  <action>Flesh out `tests/browser/test_reboot_persistence.py` from RESEARCH Pattern 5. Stand up a live server URL the persistent Chromium context can reach (use a live test server fixture — uvicorn on a free port against create_app, or the project's existing live-server pattern if one exists; if none exists, run uvicorn in a thread/subprocess bound to 127.0.0.1 and yield its base URL). `test_fingerprint_persists_across_reboot`: with a tempfile.TemporaryDirectory() as user_data_dir, async_playwright -> chromium.launch_persistent_context(user_data_dir, headless=True, args=["--no-sandbox"]); page.goto(f"{base}/pair"); read the code off the page (or call POST /api/devices/pairing-codes through the page context); perform an admin bind via the API; capture cookies, assert gruvax_device_fp exists with httpOnly True, sameSite "Strict", expires > time.time()+86400; context.close(); relaunch with the SAME user_data_dir; assert the cookie persists with an identical value; assert page.evaluate fetch('/api/session') returns bound_profile_id not null and device_id present. Guard the module with `pytest.importorskip("playwright")`. If playwright fails on Python 3.14 (RESEARCH A4), mark/skip with a clear reason and note it in the SUMMARY — do not let it red the whole suite.</action>
  <verify>
    <automated>pytest tests/browser/test_reboot_persistence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test asserts the gruvax_device_fp cookie has httpOnly True, sameSite "Strict", and expires > now + 86400
    - test relaunches from the same user_data_dir and asserts the cookie value is identical
    - test asserts GET /api/session returns a non-null bound_profile_id + device_id after the reboot simulation
    - module uses `pytest.importorskip("playwright")`
    - `pytest tests/browser/test_reboot_persistence.py -x -q` passes (or skips cleanly only if chromium/playwright unavailable on the CI Python)
  </acceptance_criteria>
  <done>Reboot-persistence proven via persistent-context round-trip; cookie survives close+relaunch with identical value; bound profile restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pi disk (user-data-dir) → reboot | cookie must persist on SD card across power cycle |
| systemd --user → Chromium | supervised kiosk process auto-restarts on crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Availability | session cookie lost on reboot (re-pair forced) | mitigate | max_age (30d) from 03-01 + persistent non-tmpfs --user-data-dir (Pitfall 1/2); proven by the persistent-context test |
| T-03-19 | Availability | kiosk process dies and stays down | mitigate | systemd --user unit Restart=always + RestartSec=3 |
| T-03-20 | Information Disclosure | fingerprint cookie readable off the Pi disk | accept | LAN-only deployment (OOS-06 no TLS yet); cookie is HttpOnly and revocable server-side; physical access to the SD card is out of the household threat model |
</threat_model>

<verification>
- `bash -n deploy/kiosk/start-kiosk.sh` clean; grep checks pass for /pair URL, Restart=always, user-data-dir.
- `pytest tests/browser/test_reboot_persistence.py -x -q` passes (cookie survives relaunch with identical value; bound profile restored).
- README documents the real-hardware manual reboot smoke test.
</verification>

<success_criteria>
- DEV-01 reboot persistence proven in CI via persistent-context round-trip.
- Committed provisioning artifacts launch Chromium at /pair with a persistent cookie store.
- Manual real-hardware reboot + <30s end-to-end documented for the deploy README.
</success_criteria>

<output>
Create `.planning/phases/03-devices-pairing/03-05-SUMMARY.md` when done.
</output>
