---
phase: 07-wizards-import-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/0007_wizard_source_labels.py
  - src/gruvax/api/admin/cubes.py
  - tests/conftest.py
  - tests/integration/test_wizard.py
  - tests/integration/test_import.py
  - tests/integration/test_export.py
  - tests/integration/test_settings_import.py
  - tests/unit/test_settings_export.py
  - tests/property/test_export_roundtrip.py
  - tests/property/test_import_roundtrip.py
autonomous: true
requirements: [ADMN-04, ADMN-05, ADMN-10]
must_haves:
  truths:
    - "boundary_history.source accepts 'wizard', 'reshuffle', 'csv', 'yaml' after migration 0007"
    - "Migration 0007 round-trips upgrade→downgrade→upgrade clean"
    - "A wizard/import commit through POST /api/admin/cubes/bulk records its origin in boundary_history.source (not hardcoded 'bulk')"
    - "All Phase 7 test files exist and run RED against unimplemented endpoints (Wave 0)"
  artifacts:
    - path: "migrations/versions/0007_wizard_source_labels.py"
      provides: "Extends boundary_history.source CHECK to add wizard/reshuffle/csv/yaml"
      contains: "boundary_history_source_check"
    - path: "src/gruvax/api/admin/cubes.py"
      provides: "BulkWriteRequest.source field + body.source passthrough to write_history_row"
      contains: "source"
    - path: "tests/integration/test_wizard.py"
      provides: "ADMN-04/ADMN-10 source-label + atomic-commit tests (Wave 0)"
      min_lines: 40
    - path: "tests/integration/test_import.py"
      provides: "ADMN-05 csv/yaml/phantom/contiguity/atomicity/partial-import tests (Wave 0)"
      min_lines: 60
    - path: "tests/unit/test_settings_export.py"
      provides: "D-14 hard-exclusion test (no pin_hash in export)"
      min_lines: 20
  key_links:
    - from: "src/gruvax/api/admin/cubes.py:bulk_write_cubes"
      to: "gruvax.db.queries.write_history_row"
      via: "source=body.source"
      pattern: "source=body\\.source"
    - from: "migrations/versions/0007_wizard_source_labels.py"
      to: "gruvax.boundary_history"
      via: "ALTER TABLE ... CHECK constraint"
      pattern: "CHECK \\(source IN"
---

<objective>
Lay the atomic backbone for every Phase 7 multi-cube operation: extend the
`boundary_history.source` CHECK constraint so wizard/import commits are legible in
History (D-04), thread a caller-supplied `source` through the existing atomic
`POST /api/admin/cubes/bulk` path (D-01, D-09, D-15), and author the full Wave-0 RED
test scaffold + synthetic fixtures that all later plans turn GREEN.

Purpose: Without migration 0007 the DB CHECK rejects `source='wizard'|'csv'|...`; without
the `source` field every wizard/import shows as "bulk" in History (Pitfall 1). The test
scaffold makes the Nyquist sampling contract executable from the first commit.

Output: migration 0007, a one-field extension to `BulkWriteRequest` + history write, and
all 7 Phase 7 test files (RED) plus synthetic 4-cube / 32-cube fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-wizards-import-export/07-CONTEXT.md
@.planning/phases/07-wizards-import-export/07-RESEARCH.md
@.planning/phases/07-wizards-import-export/07-PATTERNS.md
@.planning/phases/07-wizards-import-export/07-VALIDATION.md

<interfaces>
<!-- Verified from codebase — executor uses these directly, no exploration needed. -->

migrations/versions/0006_led_settings_seed.py revision chain:
  revision = "0006"; down_revision = "0005"
  → migration 0007 sets revision="0007", down_revision="0006"

migrations/versions/0005_segment_model.py current CHECK (the thing 0007 extends):
  CHECK (source IN ('manual', 'bulk', 'revert', 'cut_insert'))
  (two-step DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT pattern — copy it)

Alembic env (migrations/env.py): alembic_version lives in `public` schema;
  search_path set via connect event listener; gruvax schema is the target.

src/gruvax/api/admin/cubes.py (VERIFIED line numbers):
  - class BulkWriteRequest (line 113): currently only `updates: list[BoundaryEdit]`
  - bulk_write_cubes (line 662): hardcodes source="bulk" at the write_history_row
    call on line 780, inside `async with pool.connection() as conn, conn.transaction()`
  - write_history_row(conn, change_set_id, unit_id, row, col, prev, new_first_label,
    new_first_catalog, is_empty, source=...) — last positional/keyword arg is source

tests/conftest.py (VERIFIED): provides session-scoped `db_pool`; integration tests
  define their own module-scoped `client` fixture (LifespanManager + AsyncClient +
  ASGITransport on create_app()); login helper posts /api/admin/login {"pin":"0000"}
  and reads cookie `gruvax_csrf`. Test marker: @pytest.mark.asyncio(loop_scope="session").
  NOTE (project memory): the shared admin_session fixture is unreliable — copy the
  per-module `_login` helper from tests/integration/test_change_set.py instead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0007 — extend boundary_history.source CHECK (D-04)</name>
  <files>migrations/versions/0007_wizard_source_labels.py</files>
  <read_first>
    - migrations/versions/0005_segment_model.py (the exact DROP+ADD CONSTRAINT two-step to copy; lines 63–75 upgrade, 108–116 downgrade)
    - migrations/versions/0006_led_settings_seed.py (revision chain: revision/down_revision header)
    - migrations/env.py (search_path connect listener; alembic_version in public schema)
  </read_first>
  <action>
    Create migrations/versions/0007_wizard_source_labels.py with revision="0007",
    down_revision="0006", branch_labels=None, depends_on=None. upgrade() does the two-step:
    `op.execute("ALTER TABLE gruvax.boundary_history DROP CONSTRAINT IF EXISTS boundary_history_source_check")`
    then `op.execute` adding constraint named boundary_history_source_check with
    `CHECK (source IN ('manual','bulk','revert','cut_insert','wizard','reshuffle','csv','yaml'))`.
    downgrade() reverses to the Phase 5 set `('manual','bulk','revert','cut_insert')` via the same
    DROP-then-ADD two-step. Module docstring follows the 0005 convention header (Revision ID,
    Revises, Phase 7 note, conventions). All DDL via op.execute() with the explicit constraint name.
    Do NOT touch any other table or seed any rows.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && python -m alembic upgrade head && python -m alembic downgrade 0006 && python -m alembic upgrade head; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - migrations/versions/0007_wizard_source_labels.py exists with revision="0007" and down_revision="0006"
    - File contains the literal substring `'wizard'` and `'reshuffle'` and `'csv'` and `'yaml'` inside a CHECK
    - `python -m alembic upgrade head && python -m alembic downgrade 0006 && python -m alembic upgrade head` exits 0
    - After upgrade head, inserting a boundary_history row with source='wizard' does NOT raise a CHECK violation
  </acceptance_criteria>
  <done>Migration 0007 applies, round-trips clean, and the source CHECK admits all four new labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add source field to BulkWriteRequest + thread to history write (Pitfall 1)</name>
  <files>src/gruvax/api/admin/cubes.py</files>
  <read_first>
    - src/gruvax/api/admin/cubes.py lines 113–124 (BulkWriteRequest) and 661–818 (bulk_write_cubes; the hardcoded source="bulk" on line 780)
    - .planning/phases/07-wizards-import-export/07-PATTERNS.md §"cubes.py — EDIT: add source field"
  </read_first>
  <behavior>
    - Test: POST /api/admin/cubes/bulk with body {"updates":[...], "source":"wizard"} → the written boundary_history rows have source='wizard'
    - Test: POST with no source field → defaults to source='bulk' (backward compat; existing test_change_set.py / test_cubes_bulk.py stay green)
    - Test: POST with source='csv' → history rows source='csv'
    - Edge: source field is a plain str default "bulk"; the DB CHECK (migration 0007) is the value guard
  </behavior>
  <action>
    In src/gruvax/api/admin/cubes.py, add `source: str = "bulk"` to BulkWriteRequest (after
    `updates: list[BoundaryEdit]`). In bulk_write_cubes, change the hardcoded `source="bulk"`
    keyword in the write_history_row call (line ~780) to `source=body.source`. Do not change the
    phantom-validate loop, the idempotency short-circuit, the transaction structure, or the
    post-commit cache.invalidate()/SegmentCache.derive()/bus.publish() sequence. The default
    "bulk" preserves every existing caller's behavior.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && pytest tests/integration/test_change_set.py tests/integration/test_cubes_bulk.py tests/integration/test_wizard.py -x -q -k "not hypothesis"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "source: str = \"bulk\"" src/gruvax/api/admin/cubes.py returns a match in BulkWriteRequest
    - grep -n "source=body.source" src/gruvax/api/admin/cubes.py returns a match (no remaining `source="bulk"` in bulk_write_cubes)
    - tests/integration/test_wizard.py::test_source_label passes (commit with source='wizard' → boundary_history.source='wizard')
    - Existing tests/integration/test_change_set.py and test_cubes_bulk.py remain GREEN (default-source backward compat)
  </acceptance_criteria>
  <done>cubes/bulk accepts an optional source and records it in history; omitting it defaults to 'bulk'.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 RED test scaffold + synthetic fixtures for all Phase 7 requirements</name>
  <files>tests/conftest.py, tests/integration/test_wizard.py, tests/integration/test_import.py, tests/integration/test_export.py, tests/integration/test_settings_import.py, tests/unit/test_settings_export.py, tests/property/test_export_roundtrip.py, tests/property/test_import_roundtrip.py</files>
  <read_first>
    - .planning/phases/07-wizards-import-export/07-VALIDATION.md §"Per-Task Verification Map" + §"Wave 0 Requirements" (the exact test file → test name → command map)
    - tests/conftest.py (db_pool fixture; existing synthetic fixture path constants)
    - tests/integration/test_change_set.py lines 1–60 (the client fixture + _login helper pattern to copy)
    - tests/integration/test_segment_api.py (admin-authed integration pattern + how contiguity errors are asserted)
  </read_first>
  <action>
    Author all 7 Phase 7 test files as RED (failing-by-absence) per 07-VALIDATION.md, each using
    the module-scoped `client` fixture + `_login` helper from test_change_set.py (NOT the shared
    admin_session fixture). Test files and the named tests they MUST contain:
    (1) tests/integration/test_wizard.py — test_wizard_atomic_commit (one change_set_id via cubes/bulk
        source='wizard'), test_source_label (boundary_history.source='wizard'), test_reshuffle_source
        (source='reshuffle'), test_idempotency_replay (same Idempotency-Key → cached response, no dup history row).
    (2) tests/integration/test_import.py — test_csv_import, test_yaml_import, test_partial_import
        (16 of 32 cubes → rest become is_empty), test_phantom_row_rejected (validate 400, ZERO partial
        DB state), test_contiguity_violation (SEG-05 reject, no partial state), test_atomicity (failing
        row mid-import → zero partial state). Target endpoint: POST /api/admin/import/boundaries.
    (3) tests/integration/test_export.py — test_export_returns_yaml (GET /api/admin/export/boundaries.yaml
        → 200, media type application/x-yaml), test_overrides_in_export (per-label overrides serialized).
    (4) tests/integration/test_settings_import.py — test_unknown_key_rejected (422, no DB write),
        test_auth_key_rejected (auth.* → 422). Target: POST /api/admin/import/settings.
    (5) tests/unit/test_settings_export.py — test_no_pin_in_export (assert 'auth.pin_hash' not in export
        AND no key starts with 'auth.'), test_all_allowed_keys (every _ALLOWED_SETTINGS_KEYS present).
    (6) tests/property/test_export_roundtrip.py — @given Hypothesis strategy over synthetic cut sets
        (unit_id 1–2, row/col 0–3, sampled labels, unique by address); assert export→re-import→zero diff.
    (7) tests/property/test_import_roundtrip.py — YAML import round-trip distinct from export roundtrip.
    Add synthetic fixtures to tests/conftest.py: a 4-cube and a 32-cube cut-point set + matching
    v_collection rows, using ONLY made-up labels ("Atlantic","Blue Note","Columbia","Impulse") and
    synthetic catalog numbers — NEVER the real collection CSV (project repo-hygiene constraint).
    Tests that target not-yet-built endpoints must FAIL (RED), not skip silently — assert on the
    expected 200/400/422 so an unimplemented endpoint (404) fails the assertion.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && pytest tests/integration/test_import.py tests/integration/test_export.py tests/integration/test_settings_import.py tests/unit/test_settings_export.py -q --collect-only && echo "collected OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist: tests/integration/test_{wizard,import,export,settings_import}.py, tests/unit/test_settings_export.py, tests/property/test_{export_roundtrip,import_roundtrip}.py
    - `pytest --collect-only` on the new files exits 0 (no import/collection errors)
    - tests/conftest.py contains a synthetic 4-cube fixture and a 32-cube fixture using only made-up labels
    - grep -rL "discogsography\|/background/\|real.collection.csv" on the new test files confirms zero reference to real collection data
    - Running tests/integration/test_import.py shows RED failures (target endpoints unimplemented), not skips
  </acceptance_criteria>
  <done>Every Phase 7 requirement has an executable test that fails today and turns green as later plans land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → cubes/bulk | Authenticated admin supplies a `source` string crossing into a DB CHECK-guarded column |
| migration runner → DB DDL | Migration alters a CHECK constraint on a shared-Postgres table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | BulkWriteRequest.source free string | mitigate | DB CHECK constraint (migration 0007) is the value allowlist; an out-of-set source raises a CHECK violation inside the transaction → atomic rollback, no partial write |
| T-07-02 | Tampering | migration 0007 downgrade with pre-existing wizard/csv rows | accept | downgrade is a dev/CI round-trip op; production rows with new sources are not expected during a downgrade. Documented in migration docstring. |
| T-07-03 | Repudiation | wizard/import commits indistinguishable in audit log | mitigate | source=body.source makes every multi-cube op attributable in boundary_history (D-15 keystone) |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (composition-only, RESEARCH §No New Dependencies). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `python -m alembic upgrade head && python -m alembic downgrade 0006 && python -m alembic upgrade head` exits 0
- `pytest tests/integration/test_change_set.py tests/integration/test_cubes_bulk.py -x -q` GREEN (no regression)
- `pytest tests/integration/test_wizard.py::test_source_label -x` GREEN
- New test files collect without error; their endpoint-targeting tests are RED (await later plans)
</verification>

<success_criteria>
- boundary_history.source admits 'wizard','reshuffle','csv','yaml' and the migration round-trips clean
- cubes/bulk records the caller's source (default 'bulk'); no regression on existing bulk callers
- All 7 Phase 7 test files + synthetic fixtures exist; no real collection data referenced
</success_criteria>

<output>
Create `.planning/phases/07-wizards-import-export/07-01-SUMMARY.md` when done
</output>
