---
phase: 07-wizards-import-export
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/admin/AdminShell.tsx
  - frontend/src/routes/admin/Wizard.tsx
autonomous: true
gap_closure: true
requirements: [ADMN-10, ADMN-04]
must_haves:
  truths:
    - "From the admin UI, the owner can START a reshuffle without typing a URL or having a pre-existing draft"
    - "The WIZARD route offers a discoverable choice between fresh setup and reshuffle when no draft exists"
    - "Starting a reshuffle routes to /admin/wizard in reshuffle mode (existing cut points pre-loaded)"
  artifacts:
    - path: "frontend/src/routes/admin/Wizard.tsx"
      provides: "Mode-choice entry screen on /admin/wizard when no ?mode= and no draft"
      contains: "START RESHUFFLE"
    - path: "frontend/src/routes/admin/AdminShell.tsx"
      provides: "Discoverable reshuffle entry (nav or wizard landing)"
  key_links:
    - from: "frontend/src/routes/admin/Wizard.tsx"
      to: "reshuffle mode walk (pre-loaded cut points)"
      via: "START RESHUFFLE control sets mode=reshuffle"
      pattern: "mode.*reshuffle"
---

<objective>
Close gap G1 (UAT test 2, major): the reshuffle wizard ENGINE works (verified by the
user via the direct `/admin/wizard?mode=reshuffle` URL) but there is NO discoverable UI
entry point to start a reshuffle. The WIZARD nav tab links to `/admin/wizard`, which
mounts in SETUP mode only; reshuffle is reachable today solely via the `?mode=reshuffle`
query param or a pre-existing `reshuffleDraft` in the store. Add a discoverable control so
the owner can start a reshuffle from the admin UI.

Purpose: ADMN-10 / SC3 — "Owner can run a reshuffle wizard after a real-life shelf haul."
The feature is built; it is simply unreachable. This is a narrow frontend-only fix.

Output: A mode-choice landing on `/admin/wizard` (when there is no `?mode=` param and no
draft) presenting START SETUP WIZARD and START RESHUFFLE, plus reuse of the locked
07-UI-SPEC copy. No backend changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-wizards-import-export/07-UAT.md
@.planning/phases/07-wizards-import-export/07-CONTEXT.md
@.planning/phases/07-wizards-import-export/07-UI-SPEC.md

# Files being modified (read in full before editing):
@frontend/src/routes/admin/Wizard.tsx
@frontend/src/routes/admin/AdminShell.tsx

# Analog (entry-point + nav patterns):
@frontend/src/routes/admin/ReshuffleBanner.tsx

<interfaces>
<!-- Already-established contracts the executor uses directly — no exploration needed. -->

From frontend/src/routes/admin/Wizard.tsx (current behavior):
- `Wizard()` resolves mode ONCE on mount: `reshuffleDraft ? 'reshuffle' : (searchParams.get('mode') === 'reshuffle' ? 'reshuffle' : 'setup')`.
- In reshuffle mode it pre-loads existing cut points from `adminGetCubes()` into `cuts` (see the `useEffect` keyed on `mode === 'reshuffle'`).
- The `wizard-mode-badge` already renders SETUP / RESHUFFLE.

From 07-UI-SPEC.md Copywriting Contract (locked copy — use verbatim):
- `START SETUP WIZARD` — Primary CTA, wizard start (setup).
- `START RESHUFFLE` — Primary CTA, wizard start (reshuffle), shown when existing boundaries are present.
- Labels: ALL CAPS, Barlow Condensed 700. Body/instruction: sentence case, Space Grotesk 400.

From useAdminStore (frontend/src/state/adminStore.ts):
- `reshuffleDraft: ReshuffleDraft | null`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a mode-choice landing to the WIZARD route</name>
  <read_first>
    - frontend/src/routes/admin/Wizard.tsx (the file being modified — read the full mount/mode-resolution logic and render tree)
    - frontend/src/routes/admin/ReshuffleBanner.tsx (analog: how CONTINUE navigates to /admin/wizard?mode=reshuffle)
    - .planning/phases/07-wizards-import-export/07-UI-SPEC.md §"Surface 6: Nav Entry Points" + Copywriting Contract
  </read_first>
  <action>
    The WIZARD nav tab links to `/admin/wizard`, which currently mounts straight into SETUP
    mode — there is no way to choose reshuffle (D-01: "one wizard engine, two entry modes").
    Add a discoverable entry choice so reshuffle is reachable from the UI.

    Implement the entry choice INSIDE Wizard.tsx as a landing view that renders when there is
    NO explicit mode signal — i.e. when `searchParams.get('mode')` is null/empty AND
    `reshuffleDraft` is null. This preserves every existing deep-link path: a draft still
    auto-resumes in reshuffle mode, and `?mode=reshuffle` / `?mode=setup` still go straight
    into the walk (the ReshuffleBanner CONTINUE button and any existing links keep working).

    Behavior:
    - Add a small "entry" phase/branch (e.g. a `started` boolean or an `entryChoice` view flag)
      that short-circuits BEFORE the cubes-loading walk render when no mode signal is present.
    - The landing shows two ALL-CAPS primary controls using the locked copy:
        - `START SETUP WIZARD` → enters setup mode (set mode to 'setup' and proceed into the walk).
        - `START RESHUFFLE` → enters reshuffle mode (set mode to 'reshuffle' and proceed; the
          existing reshuffle pre-load effect populates `cuts` from `adminGetCubes()`).
    - Implement mode entry by navigating to the canonical URLs the engine already understands
      (`navigate('/admin/wizard?mode=setup')` / `navigate('/admin/wizard?mode=reshuffle')`) so
      there is ONE source of truth for mode and no drift vs. D-01. Re-resolving mode from the
      URL on the resulting render is the established pattern; do NOT add a second parallel mode
      setter that can diverge.
    - Add a short sentence-case instruction above the two buttons explaining the difference
      (setup = build from scratch; reshuffle = re-walk existing shelves after a haul). Keep it
      plain-language per the design language.
    - Reuse existing wizard CSS classes (`wizard-btn`, `wizard-btn--primary`,
      `wizard-btn--outline`, `wizard-route`); do NOT introduce hardcoded hex — all color via
      `--gruvax-*` tokens. All API/user strings via JSX `{}` interpolation; never innerHTML
      (CLAUDE.md / sketch-findings constraint).

    Do NOT remove or weaken the existing auto-resume-from-draft or `?mode=` deep-link paths.
    Do NOT touch any backend file or the reshuffle ENGINE logic — the engine is verified
    working; only the entry surface is missing.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0 (no type errors).
    - `cd frontend && npm run build` exits 0.
    - `grep -n "START RESHUFFLE" frontend/src/routes/admin/Wizard.tsx` returns ≥1 match.
    - `grep -n "START SETUP WIZARD" frontend/src/routes/admin/Wizard.tsx` returns ≥1 match.
    - `grep -nE "mode=reshuffle" frontend/src/routes/admin/Wizard.tsx` returns ≥1 match (the
      reshuffle choice routes into reshuffle mode).
    - `grep -c "innerHTML" frontend/src/routes/admin/Wizard.tsx` returns 0.
    - `grep -nE "#[0-9a-fA-F]{3,6}" frontend/src/routes/admin/Wizard.tsx` returns 0 (no hardcoded hex).
    - Existing deep-link behavior intact: with `?mode=reshuffle` OR a draft present, the
      landing is skipped and the walk renders (confirm by reading the mode-resolution branch —
      the landing only shows when BOTH the `?mode=` param is absent AND `reshuffleDraft` is null).
  </acceptance_criteria>
  <done>Visiting /admin/wizard (no query param, no draft) shows START SETUP WIZARD + START RESHUFFLE; START RESHUFFLE enters reshuffle mode with existing cut points pre-loaded.</done>
</task>

<task type="auto">
  <name>Task 2: Surface a RESHUFFLE entry point in admin nav chrome</name>
  <read_first>
    - frontend/src/routes/admin/AdminShell.tsx (the file being modified — read the nav block lines ~158-199)
    - .planning/phases/07-wizards-import-export/07-UI-SPEC.md §"Surface 6: Nav Entry Points"
  </read_first>
  <action>
    The single WIZARD nav tab is ambiguous — it only ever opens setup. Make reshuffle
    discoverable from the persistent admin chrome.

    Preferred minimal approach (keeps the locked nav order SETTINGS · CUBES · HISTORY · WIZARD ·
    IMPORT intact): the WIZARD NavLink continues to point at `/admin/wizard`, which now lands on
    the Task-1 mode choice. No additional nav tab is required IF the Task-1 landing is the
    discoverable choice. Verify the WIZARD tab reaches the landing (it links to `/admin/wizard`
    with no query param, so it will).

    If — and only if — the Task-1 landing alone does not make reshuffle obviously reachable
    (e.g. the WIZARD tab already carries a `?mode=` default somewhere), add a lightweight
    RESHUFFLE affordance that navigates to `/admin/wizard?mode=reshuffle`, styled with the
    existing `admin-nav-tab` class so it matches the locked nav visual language. Do NOT invent a
    new visual style or palette; do NOT reorder or rename the existing tabs.

    Constraints: all color via `--gruvax-*` tokens (no hardcoded hex); ALL-CAPS Barlow labels
    for any new control; strings via JSX `{}` only (never innerHTML).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0 and `npm run build` exits 0.
    - `grep -nE "to=\"/admin/wizard\"|/admin/wizard" frontend/src/routes/admin/AdminShell.tsx`
      confirms the WIZARD tab still links to `/admin/wizard` (the Task-1 landing).
    - `grep -c "innerHTML" frontend/src/routes/admin/AdminShell.tsx` returns 0.
    - `grep -nE "#[0-9a-fA-F]{3,6}" frontend/src/routes/admin/AdminShell.tsx` returns 0
      (no hardcoded hex introduced).
    - Nav order is unchanged: SETTINGS, CUBES, HISTORY, WIZARD, IMPORT still present
      (confirm by reading the nav block).
  </acceptance_criteria>
  <done>The admin chrome's WIZARD tab leads to a screen where the owner can clearly start a reshuffle; reshuffle is reachable without typing a URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin SPA route | Frontend navigation only; no new server input crosses here in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0706-01 | Tampering (XSS) | New entry-screen render in Wizard.tsx / AdminShell.tsx | mitigate | All strings rendered via JSX `{}` interpolation (textContent semantics); never innerHTML (CLAUDE.md / sketch-findings). |
| T-0706-02 | Elevation of Privilege | Reshuffle entry routes | accept | Entry control only navigates; the reshuffle commit still goes through `require_admin` (session + CSRF) on `POST /cubes/bulk` — no auth surface added here. |
| T-0706-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced (07-UI-SPEC Registry Safety: no new npm packages in Phase 7). Frontend-only edits to existing files. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0.
- `cd frontend && npm run build` exits 0.
- Manual (folded into the cross-cutting human-verify in 07-08, but checkable here): /admin/wizard
  with no query param presents both START SETUP WIZARD and START RESHUFFLE; tapping START
  RESHUFFLE shows the RESHUFFLE mode badge and pre-loaded cut points.
</verification>

<success_criteria>
The owner can start a reshuffle entirely from the admin UI (no URL typing, no pre-existing
draft required). The setup path and all existing deep-links (`?mode=`, auto-resume from draft,
ReshuffleBanner CONTINUE) remain unchanged. tsc + build are clean.
</success_criteria>

<output>
Create `.planning/phases/07-wizards-import-export/07-06-SUMMARY.md` when done.
</output>
