---
phase: 05-close-v2-0-integration-gaps-kiosk-collection-changed-listene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/kiosk/KioskView.EventSource.test.tsx
  - frontend/src/routes/kiosk/KioskView.tsx
autonomous: true
requirements: [SYN-01, SYN-02, API-02]
must_haves:
  truths:
    - "After a collection_changed SSE event, the kiosk invalidates the ['search'] query key so visible search results refetch (no manual reload)"
    - "After a collection_changed SSE event, the kiosk resyncs grid data (['units']/['cubes']) and re-locates the active selection"
    - "The kiosk search query does not fire (no /api/search fetch) while boundProfileId is null"
  artifacts:
    - path: "frontend/src/routes/kiosk/KioskView.tsx"
      provides: "collection_changed SSE listener + boundProfileId-gated search enabled flag"
      contains: "collection_changed"
    - path: "frontend/src/routes/kiosk/KioskView.EventSource.test.tsx"
      provides: "RED tests: collection_changed invalidates ['search']; search disabled when boundProfileId null"
      contains: "collection_changed"
  key_links:
    - from: "frontend/src/routes/kiosk/KioskView.tsx"
      to: "queryClient.invalidateQueries(['search'])"
      via: "es.addEventListener('collection_changed', ...)"
      pattern: "collection_changed"
    - from: "frontend/src/routes/kiosk/KioskView.tsx"
      to: "useQuery search enabled gate"
      via: "enabled gated on boundProfileId non-null"
      pattern: "!!boundProfileId"
---

<objective>
Close B-01 (kiosk consumes `collection_changed`) and the frontend half of B-02 (gate the search
query on `boundProfileId`). Frontend-only — the SSE publisher in `profile_sync.py` is already
correct and is NOT touched.

Purpose:
- B-01: After a nightly/manual "Sync now" completes, the backend publishes `collection_changed`,
  but `KioskView.tsx` never listens for it, so the kiosk shows pre-sync results until a manual
  reload. Add the listener so the visible results refetch live (restores SYN-01 Flow 4 and the
  SYN-02 staleness-refresh path).
- B-02 (frontend): gate the search `useQuery` `enabled` on `boundProfileId` being non-null so no
  request fires before session bootstrap resolves the bound profile (defense-in-depth against the
  422; the tolerant backend in Plan 01 is the authoritative fix).

Output: RED Vitest tests (written first) + the `collection_changed` listener and the `enabled`
gate in `KioskView.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-close-v2-0-integration-gaps-kiosk-collection-changed-listene/05-CONTEXT.md
@.planning/phases/05-close-v2-0-integration-gaps-kiosk-collection-changed-listene/05-PATTERNS.md

<interfaces>
<!-- Existing KioskView SSE + query idioms. Extracted from KioskView.tsx — use directly. -->

KioskView.tsx line 42 — bound profile read from session store:
```typescript
const boundProfileId = useSessionStore((s) => s.boundProfileId)
```

KioskView.tsx lines 169-171 — the search useQuery (target of the B-02 gate):
```typescript
queryKey: ['search', debouncedQuery, boundProfileId],
queryFn: () => searchCollection(debouncedQuery, 10, boundProfileId ?? undefined),
enabled: debouncedQuery.trim().length > 0,           // gate to extend
```

KioskView.tsx lines 245-255 — resync() (does NOT invalidate ['search']):
```typescript
const resync = () => {
  void queryClient.invalidateQueries({ queryKey: ['units'] })
  void queryClient.invalidateQueries({ queryKey: ['cubes'] })
  relocateActiveSelection()
}
```

KioskView.tsx lines 326-339 — the simple no-payload listener idiom + the single cleanup point:
```typescript
es.addEventListener('server_hello', () => { resync(); /* ... */ })
es.addEventListener('server_shutdown', () => { useGruvaxStore.getState().setSseConnected(false) })
return () => { es.close() }   // the ONLY es.close() — Pitfall 4
```
The SSE effect dep array is `[queryClient, boundProfileId]` (line 341).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RED Vitest tests for collection_changed invalidation and the boundProfileId gate</name>
  <files>frontend/src/routes/kiosk/KioskView.EventSource.test.tsx</files>
  <read_first>
    - frontend/src/routes/kiosk/KioskView.EventSource.test.tsx (lines 84-103 `renderKioskAndFlush`; 108-133 the beforeEach session-store seed; 155-169 the `invalidateQueries` spy pattern) — the existing `boundary_changed` dispatch + spy harness to mirror.
    - .planning/phases/05-close-v2-0-integration-gaps-kiosk-collection-changed-listene/05-PATTERNS.md (lines 258-347) — exact RED test bodies for both new cases + the MockEventSource.dispatchEvent idiom and the `useSessionStore.setState` unbound override.
  </read_first>
  <action>
    Add two tests to the existing `KioskView.EventSource.test.tsx`, mirroring the existing
    `boundary_changed` dispatch + `vi.spyOn(qc, 'invalidateQueries')` harness:

    (1) "collection_changed invalidates search query key (B-01)": render via `renderKioskAndFlush(qc)`
    (session store seeded bound, per the existing beforeEach), spy on `qc.invalidateQueries`, then
    `await act(async () => { es.dispatchEvent('collection_changed', {}) })`. Map the spy calls to
    their `queryKey` and assert `calledKeys` `toContainEqual(['search'])` (and `toContainEqual(['units'])`
    / `['cubes']` from the `resync()` call).

    (2) "search query is disabled when boundProfileId is null (B-02)": override the session store to
    the unbound state (`useSessionStore.setState({ profileCount: 0, boundProfileId: null, profiles: [] })`),
    spy on `global.fetch`, render via `renderKioskAndFlush(qc)`, and assert `fetchSpy` was NOT called
    with a URL containing `/api/search`.

    Run the tests now and confirm BOTH FAIL (RED): test (1) because there is no `collection_changed`
    listener yet; test (2) because the `enabled` gate does not yet include `boundProfileId`. Do NOT
    implement the component changes in this task.
  </action>
  <acceptance_criteria>
    - `KioskView.EventSource.test.tsx` contains a test dispatching `collection_changed` and asserting `toContainEqual(['search'])`.
    - It contains a test asserting no `/api/search` fetch fires when `boundProfileId` is null.
    - Running `cd frontend && npx vitest run src/routes/kiosk/KioskView.EventSource.test.tsx` shows both new tests FAILING (RED).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/routes/kiosk/KioskView.EventSource.test.tsx 2>&1 | grep -E "collection_changed|boundProfileId|FAIL"</automated>
  </verify>
  <done>Both new tests exist and fail (RED): collection_changed→['search'] not yet wired; search-disabled-when-null not yet gated.</done>
</task>

<task type="auto">
  <name>Task 2: Add collection_changed listener and gate search enabled on boundProfileId</name>
  <files>frontend/src/routes/kiosk/KioskView.tsx</files>
  <read_first>
    - frontend/src/routes/kiosk/KioskView.tsx (line 42 boundProfileId; lines 169-171 the search useQuery; lines 245-255 resync(); lines 326-339 the server_hello/server_shutdown listeners + cleanup return) — exact insertion points.
    - .planning/phases/05-close-v2-0-integration-gaps-kiosk-collection-changed-listene/05-PATTERNS.md (lines 23-98 the listener idiom + the explicit note that the handler must add `['search']` invalidation AND call resync(), because resync() alone does not bust ['search']; lines 334-347 the target enabled gate).
  </read_first>
  <action>
    B-01: Inside the SSE `useEffect`, immediately after the `server_shutdown` listener (line ~334)
    and BEFORE the cleanup `return () => { es.close() }` (line ~337), register a no-payload listener:
    `es.addEventListener('collection_changed', () => { ... })`. In the handler, call
    `void queryClient.invalidateQueries({ queryKey: ['search'] })` (prefix-key invalidation busts all
    `['search', q, profileId]` entries) AND call `resync()` (which invalidates `['units']`/`['cubes']`
    and re-locates the active selection). Use the simple no-`e` form like `server_hello` — the
    publisher (`profile_sync.py:356`) sends no payload. Do NOT add a second `es.close()`; the single
    cleanup return is the only teardown (Pitfall 4). Do NOT modify the effect dep array beyond what
    already exists (`[queryClient, boundProfileId]`).

    B-02 frontend: change the search `useQuery` `enabled` (line ~171) from
    `enabled: debouncedQuery.trim().length > 0` to `enabled: !!boundProfileId && debouncedQuery.trim().length > 0`
    so no `/api/search` request fires while `boundProfileId` is null. Keep the existing
    `queryFn` and `queryKey` unchanged. Do NOT touch `client.ts` (its `if (profileId) paramObj.profile_id = profileId`
    builder is now correct against the tolerant backend from Plan 01).
  </action>
  <acceptance_criteria>
    - `grep -c "collection_changed" frontend/src/routes/kiosk/KioskView.tsx` ≥ 1, inside the SSE useEffect before the cleanup return.
    - The collection_changed handler invalidates `['search']` and calls `resync()`.
    - `grep -c "!!boundProfileId && debouncedQuery" frontend/src/routes/kiosk/KioskView.tsx` ≥ 1 (the gated enabled flag).
    - Exactly one `es.close()` remains in the file (no new teardown added).
    - `cd frontend && npx vitest run src/routes/kiosk/KioskView.EventSource.test.tsx` exits 0 (both RED tests now GREEN).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/routes/kiosk/KioskView.EventSource.test.tsx</automated>
  </verify>
  <done>collection_changed listener busts ['search'] + resyncs; search useQuery disabled while boundProfileId null; both prior RED tests GREEN; no extra es.close() added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend SSE stream → KioskView EventSource | `collection_changed` server event crosses into the client; payload-less, triggers a cache refetch only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | search query firing before session bootstrap | mitigate | Gate the search `useQuery` `enabled` on `boundProfileId` non-null so no `/api/search` request is issued until the bound profile is resolved — prevents an unscoped/ambiguous query attempt from the client. Backend Plan 01 is the authoritative scope guard; this is defense-in-depth. |
| T-05-04 | Denial of Service | malformed/duplicate collection_changed frames | accept | `collection_changed` is payload-less and the handler only triggers TanStack `invalidateQueries` (debounced/coalesced by the query layer). Mirrors the existing `server_hello` pattern; no new parsing surface (no `JSON.parse`), so no IN-02 try/catch needed. Low risk. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs — edits to existing frontend source + test files only; no new dependencies. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/routes/kiosk/KioskView.EventSource.test.tsx` exits 0.
- `cd frontend && npx eslint src/routes/kiosk/KioskView.tsx` clean.
- `cd frontend && npx tsc --noEmit` clean.
- Exactly one `es.close()` in KioskView.tsx (cleanup-point invariant preserved).
</verification>

<success_criteria>
- B-01 closed: `collection_changed` invalidates `['search']` and resyncs grid/locate → kiosk results refresh live after sync (SYN-01 Flow 4 restored; SYN-02 staleness-refresh path restored).
- B-02 frontend half closed: search query gated on `boundProfileId`; no `/api/search` fetch while null.
- API-02 end-to-end refresh path restored on the kiosk (search results reflect the per-profile cache after sync).
- RED tests written before the fix; both GREEN after.
</success_criteria>

<output>
Create `.planning/phases/05-close-v2-0-integration-gaps-kiosk-collection-changed-listene/05-02-SUMMARY.md` when done.
</output>
