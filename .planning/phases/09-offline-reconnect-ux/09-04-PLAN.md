---
phase: 09-offline-reconnect-ux
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
gap_closure: true
files_modified:
  - frontend/src/routes/kiosk/KioskView.tsx
  - frontend/src/routes/kiosk/KioskView.EventSource.test.tsx
autonomous: true
requirements: [OFF-04]
must_haves:
  truths:
    - "resync() actively invalidates the ['search'] query key, so a reconnect (onopen) or server_hello refreshes stale search results, not only boundary data (ROADMAP SC4, user decision 2026-06-01)"
    - "The 'Back online' SyncToast onDismiss handler has a stable identity (useCallback) so background re-renders do not re-arm its 4s auto-dismiss timer (WR-01)"
    - "showBackOnlineToast is cleared on disconnect (es.onerror and server_shutdown) so the OfflineBanner and 'Back online' toast are never visible simultaneously (WR-02)"
  artifacts:
    - path: "frontend/src/routes/kiosk/KioskView.tsx"
      provides: "resync() includes search invalidation; stable onDismiss callback; toast cleared on disconnect"
      contains: "queryKey: ['search']"
  key_links:
    - from: "frontend/src/routes/kiosk/KioskView.tsx resync()"
      to: "['search'] query"
      via: "queryClient.invalidateQueries"
      pattern: "invalidateQueries({ queryKey: ['search'] })"
---

<objective>
Gap closure for Phase 9 verification (status human_needed). Applies three user-approved fixes
(decision recorded 2026-06-01) to the reconnect path in KioskView.tsx — all in one file plus its
EventSource test — to satisfy ROADMAP SC4 literally and resolve the two flaky-LAN reconnect
defects (WR-01, WR-02) found by code review and confirmed by the verifier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-offline-reconnect-ux/09-VERIFICATION.md
@.planning/phases/09-offline-reconnect-ux/09-REVIEW.md
@.planning/phases/09-offline-reconnect-ux/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC4 — actively invalidate ['search'] in resync()</name>
  <files>frontend/src/routes/kiosk/KioskView.tsx</files>
  <read_first>
    - frontend/src/routes/kiosk/KioskView.tsx lines ~300-410 (resync(), onopen, server_hello, collection_changed)
  </read_first>
  <action>
    In the `resync()` function (currently invalidating `['units']` and `['cubes']` then calling
    `relocateActiveSelection()`), add a line that invalidates the search query:
    `void queryClient.invalidateQueries({ queryKey: ['search'] })`.
    Place it alongside the existing `['units']`/`['cubes']` invalidations. Update the resync()
    comment to note that search is now actively refreshed on reconnect (ROADMAP SC4, supersedes the
    D-73/74 passive-staleTime note — user decision 2026-06-01). This makes both onopen and
    server_hello refresh search. Do NOT remove the existing `['search']` invalidation in the
    `collection_changed` handler (it is still correct).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX/frontend && grep -q "queryKey: \['search'\]" src/routes/kiosk/KioskView.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - resync() contains an `invalidateQueries({ queryKey: ['search'] })` call.
    - server_hello (which calls resync()) therefore refreshes search on server restart, matching
      ROADMAP SC4 wording.
  </acceptance_criteria>
  <done>resync() invalidates ['search']; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: WR-01 + WR-02 — stable toast onDismiss + clear toast on disconnect</name>
  <files>frontend/src/routes/kiosk/KioskView.tsx</files>
  <read_first>
    - frontend/src/routes/kiosk/KioskView.tsx line 1 (react import), line 61 (showBackOnlineToast),
      lines 339-342 (es.onerror), 398-401 (server_shutdown), 781-786 (SyncToast onDismiss)
  </read_first>
  <action>
    WR-01: Add `useCallback` to the React import on line 1. Define a stable handler near the other
    component state/hooks (after the `showBackOnlineToast` state and `queryClient`):
    `const handleBackOnlineDismiss = useCallback(() => setShowBackOnlineToast(false), [])`.
    Replace the inline `onDismiss={() => setShowBackOnlineToast(false)}` on the SyncToast (line ~784)
    with `onDismiss={handleBackOnlineDismiss}` so its identity is stable across re-renders and the
    SyncToast 4s auto-dismiss timer is not re-armed by background re-renders.

    WR-02: Add `setShowBackOnlineToast(false)` to BOTH `es.onerror` (after the existing
    `setSseConnected(false)`, line ~341) and the `server_shutdown` listener (after its
    `setSseConnected(false)`, line ~400) so a disconnect within 4s of a reconnect clears the
    "Back online" toast — never showing the OfflineBanner and the toast at the same time.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX/frontend && grep -q "useCallback" src/routes/kiosk/KioskView.tsx && grep -q "handleBackOnlineDismiss" src/routes/kiosk/KioskView.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `useCallback` imported; `handleBackOnlineDismiss` defined with empty deps and passed to SyncToast.
    - `setShowBackOnlineToast(false)` present in both `es.onerror` and the `server_shutdown` listener.
  </acceptance_criteria>
  <done>Stable onDismiss callback wired; toast cleared on both disconnect paths; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Tests for SC4 search refresh + toast-cleared-on-disconnect</name>
  <files>frontend/src/routes/kiosk/KioskView.EventSource.test.tsx</files>
  <read_first>
    - frontend/src/routes/kiosk/KioskView.EventSource.test.tsx (existing EventSource lifecycle tests
      — mirror their setup: mock EventSource, fire onopen/onerror/server_hello/server_shutdown)
  </read_first>
  <action>
    Add tests asserting:
    (a) On reconnect (server_hello, or onopen after a disconnect) the ['search'] query is invalidated
        — spy on queryClient.invalidateQueries and assert it was called with `{ queryKey: ['search'] }`.
    (b) After a reconnect shows the "Back online" toast, firing es.onerror (or server_shutdown)
        hides it again (showBackOnlineToast → false; toast no longer in the DOM).
    Follow the existing test file's patterns and helpers; keep the suite green.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX/frontend && npx vitest run src/routes/kiosk/KioskView.EventSource.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - New tests for SC4 search invalidation and toast-clear-on-disconnect pass.
    - Full `npm test` in frontend stays green.
  </acceptance_criteria>
  <done>New EventSource tests green; full frontend suite green.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` (tsc -b && vite build) clean.
- `cd frontend && npm test` all green.
- resync() invalidates ['search']; onDismiss is a stable useCallback; toast cleared in onerror + server_shutdown.
</verification>

<success_criteria>
ROADMAP SC4 satisfied structurally (search actively refreshed on reconnect); WR-01 and WR-02
resolved; frontend build + tests green.
</success_criteria>

<output>
Create `.planning/phases/09-offline-reconnect-ux/09-04-SUMMARY.md` when done.
</output>
