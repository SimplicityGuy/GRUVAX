---
phase: 09-offline-reconnect-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gruvax/api/events.py
autonomous: true
requirements: [OFF-03]
must_haves:
  truths:
    - "Each SSE connection receives a per-connection retry: directive in the 2000-8000 ms range"
    - "EventSource keepalive ping interval stays at 15s (unchanged)"
  artifacts:
    - path: "src/gruvax/api/events.py"
      provides: "SSE generator that emits a jittered retry: field on the initial frame"
      contains: "random.randint(2000, 8000)"
  key_links:
    - from: "src/gruvax/api/events.py generator()"
      to: "EventSource client reconnect timer"
      via: "ServerSentEvent(comment=..., retry=retry_ms) initial yield"
      pattern: "retry=retry_ms"
---

<objective>
Add per-connection reconnect jitter to the kiosk SSE endpoint so that when `gruvax-api`
restarts (Compose redeploy / nightly), all connected clients do NOT reconnect in lockstep at
the browser-default 3s — preventing the thundering-herd reconnect storm (PITFALLS 36).

Purpose: Satisfies the server-side half of OFF-03 (auto-reconnect with backoff + jitter, no
reconnect storm). This is the backend's only change for Phase 9 and is fully independent of the
frontend banner/degraded-mode work — it can run in parallel.

Output: `src/gruvax/api/events.py` emits a `retry:` field (random 2000-8000 ms) on the initial
SSE frame, leaving `ping=15` and all event handling untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-offline-reconnect-ux/09-CONTEXT.md
@.planning/phases/09-offline-reconnect-ux/09-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit jittered retry: on the SSE generator's initial frame</name>
  <files>src/gruvax/api/events.py</files>
  <read_first>
    - src/gruvax/api/events.py (the `generator()` async function, ~lines 58-86 — the
      `yield ServerSentEvent(comment="connected")` initial frame and the `while True` event loop)
    - .planning/phases/09-offline-reconnect-ux/09-PATTERNS.md §`src/gruvax/api/events.py`
      (exact before/after diff; `retry` is an `int | None` ms field on `ServerSentEvent`)
    - .planning/research/PITFALLS.md §Pitfall 36 (reconnect storm; server sends `retry:` jitter
      so each client gets a different reconnect interval over a 2-8s window)
  </read_first>
  <action>
    Add `import random` to the top-of-file imports (alongside the existing `asyncio`, `json`).
    Inside `generator()`, immediately before the first yield, compute
    `retry_ms = random.randint(2000, 8000)` and change the initial frame from
    `yield ServerSentEvent(comment="connected")` to
    `yield ServerSentEvent(comment="connected", retry=retry_ms)`. Do NOT touch `ping=15` on the
    `EventSourceResponse` — the keepalive ping and the per-connection reconnect interval are
    orthogonal (PITFALLS 8 vs 36). Do NOT add a `/healthz` reconnect probe — per the CONTEXT
    Claude's-Discretion decision this phase relies on EventSource auto-reconnect + this jitter
    alone; a probe is explicitly NOT added. Leave the `while True` loop, `request.is_disconnected()`
    check, `asyncio.wait_for(q.get(), timeout=1.0)`, and `bus.unsubscribe(q)` cleanup unchanged.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && uv run python -c "import ast,sys; src=open('src/gruvax/api/events.py').read(); assert 'random.randint(2000, 8000)' in src or 'random.randint(2000,8000)' in src, 'jitter call missing'; assert 'retry=retry_ms' in src, 'retry= not passed to ServerSentEvent'; assert 'ping=15' in src, 'ping=15 must remain'; assert 'import random' in src, 'random import missing'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `events.py` contains `import random`, `random.randint(2000, 8000)`, and
      `retry=retry_ms` passed into the initial `ServerSentEvent`.
    - Source assertion: `ping=15` is still present on the `EventSourceResponse` (keepalive
      unchanged) and there is exactly one `bus.unsubscribe(q)` in the `finally` block (no
      structural change to the loop).
    - Behavior assertion: existing SSE endpoint tests still pass —
      `cd /Users/Robert/Code/public/GRUVAX && uv run pytest tests -k events -q` exits 0.
    - Tie to ROADMAP success criterion 3: a restart no longer reconnects all clients at the
      identical browser-default interval; reconnects spread over the 2-8s jitter window.
  </acceptance_criteria>
  <done>
    `events.py` yields a jittered `retry:` (2000-8000 ms) on connect, `ping=15` retained, all
    event-bus subscribe/unsubscribe semantics unchanged, `pytest -k events` green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk browser → `/api/events/{profile_id}` SSE | Long-lived connection; untrusted client may reconnect repeatedly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Denial of Service | SSE reconnect after server restart | mitigate | Per-connection `retry:` jitter (2000-8000 ms) spreads reconnects across a window, preventing a synchronized thundering herd on the single Uvicorn worker (PITFALLS 36). This is the core deliverable of this plan. |
| T-09-02 | Denial of Service | A single client opening many SSE connections | accept | Home-LAN only, no public exposure (per CLAUDE.md Connectivity constraint); `bus.unsubscribe(q)` in `finally` reclaims per-connection resources on disconnect. Low-value internal target. |
| T-09-03 | Information Disclosure | `retry_ms` value observable by client | accept | The reconnect interval is non-sensitive; exposing it is by-design (the browser must read it). No PII or auth material in the frame. |
</threat_model>

<verification>
- `uv run pytest tests -k events -q` passes.
- `uv run ruff check src/gruvax/api/events.py` and `uv run mypy src/gruvax/api/events.py` clean.
- Manual: `curl -N http://localhost:PORT/api/events/<profile_id>` shows a `retry:` line in the
  first frame with a value between 2000 and 8000.
</verification>

<success_criteria>
SSE clients receive a jittered `retry:` interval on connect; no lockstep reconnect after a
server restart (ROADMAP success criterion 3, server side); `ping=15` and event delivery
semantics unchanged.
</success_criteria>

<output>
Create `.planning/phases/09-offline-reconnect-ux/09-01-SUMMARY.md` when done.
</output>
