---
phase: 09-offline-reconnect-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/state/store.ts
  - frontend/src/App.tsx
autonomous: true
requirements: [OFF-01, OFF-03]
must_haves:
  truths:
    - "connectivity.bannerVisible is true exactly when sseConnected is false"
    - "TanStack queries are NOT paused when navigator.onLine is false (networkMode 'always')"
  artifacts:
    - path: "frontend/src/state/store.ts"
      provides: "bannerVisible as a real boolean flipped by setSseConnected"
      contains: "bannerVisible: boolean"
    - path: "frontend/src/App.tsx"
      provides: "QueryClient defaultOptions.queries.networkMode = 'always'"
      contains: "networkMode: 'always'"
  key_links:
    - from: "frontend/src/state/store.ts setSseConnected"
      to: "connectivity.bannerVisible"
      via: "set() updater deriving bannerVisible from !connected"
      pattern: "bannerVisible: !connected"
    - from: "frontend/src/App.tsx QueryClient"
      to: "all kiosk queries"
      via: "defaultOptions.queries.networkMode"
      pattern: "networkMode: 'always'"
---

<objective>
Activate the connectivity store contract and the QueryClient network policy that the
banner/degraded-mode plan (09-03) builds on. This is the small, independent "contracts +
config" foundation: unlock `connectivity.bannerVisible` from a `false`-literal stub to a real
`boolean` derived from SSE connection state, and add `networkMode: 'always'` to the QueryClient
so queries are gated by SSE connectivity (not by `navigator.onLine`) — preventing the
reconnect-triggered refetch storm.

Purpose: OFF-01 (the store flag the banner renders from) + OFF-03 (the network policy that
keeps queries from pausing on a cosmetic `navigator.onLine=false` and stampeding on reconnect —
PITFALLS 35/36, locked decision D8).

Output: `store.ts` with a boolean `bannerVisible` flipped inside `setSseConnected`; `App.tsx`
QueryClient with `networkMode: 'always'`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-offline-reconnect-ux/09-CONTEXT.md
@.planning/phases/09-offline-reconnect-ux/09-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts the executor must produce/preserve. Extracted from 09-PATTERNS.md. -->

Current store.ts ConnectivityState (lines ~19-24) — `bannerVisible` is a literal `false` stub:
```ts
interface ConnectivityState {
  sseConnected: boolean
  lastSeenAt: number
  bannerVisible: false   // <- literal false (stub) -> change to boolean
}
```

Current setSseConnected action (store.ts ~lines 168-176):
```ts
setSseConnected: (connected) =>
  set((s) => ({
    connectivity: {
      ...s.connectivity,
      sseConnected: connected,
      lastSeenAt: connected ? Date.now() : s.connectivity.lastSeenAt,
    },
  })),
```

Current App.tsx QueryClient (lines ~24-31):
```ts
const queryClient = new QueryClient({
  defaultOptions: { queries: { retry: 1, refetchOnWindowFocus: false } },
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unlock connectivity.bannerVisible and flip it in setSseConnected</name>
  <files>frontend/src/state/store.ts</files>
  <behavior>
    - Test: after `setSseConnected(false)`, `getState().connectivity.bannerVisible === true`.
    - Test: after `setSseConnected(true)`, `getState().connectivity.bannerVisible === false`.
    - Test: `setSseConnected(true)` still updates `sseConnected=true` and bumps `lastSeenAt`;
      `setSseConnected(false)` preserves the previous `lastSeenAt` (existing behavior unchanged).
  </behavior>
  <read_first>
    - frontend/src/state/store.ts (the `ConnectivityState` interface ~lines 19-24, the initial
      `connectivity: { ... }` state ~line 166, and the `setSseConnected` action ~lines 168-176)
    - .planning/phases/09-offline-reconnect-ux/09-PATTERNS.md §`frontend/src/state/store.ts`
      (exact before/after for the interface, initial state, and action)
    - .planning/research/PITFALLS.md §Pitfall 35 (sseConnected is the authoritative offline
      signal; bannerVisible mirrors !sseConnected)
  </read_first>
  <action>
    Change `bannerVisible: false` in the `ConnectivityState` interface to `bannerVisible: boolean`
    (D-04 / OFF-01: this phase activates the deferred stub). The initial state literal
    `connectivity: { sseConnected: false, lastSeenAt: 0, bannerVisible: false }` stays as written
    (no type conflict once the interface is `boolean`). In `setSseConnected`, add
    `bannerVisible: !connected` to the `connectivity` updater object so the banner flag derives
    from disconnection. Do NOT change the `sseConnected` / `lastSeenAt` lines. If a co-located
    unit test file for the store exists, add the three behavior cases above; if none exists,
    create `frontend/src/state/store.test.ts` with them.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX/frontend && npx tsc --noEmit && npx vitest run src/state/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -v '^\s*\*' frontend/src/state/store.ts | grep -q 'bannerVisible: boolean'`
      (interface widened) and `grep -q 'bannerVisible: !connected' frontend/src/state/store.ts`
      (flip wired into the action).
    - Behavior assertion: vitest proves `setSseConnected(false)` → `bannerVisible===true` and
      `setSseConnected(true)` → `bannerVisible===false`, with `lastSeenAt` semantics unchanged.
    - `npx tsc --noEmit` exits 0 (no remaining `false`-literal type conflict anywhere reading
      `bannerVisible`).
  </acceptance_criteria>
  <done>
    `bannerVisible` is a real boolean tracking `!sseConnected`; tsc clean; store unit tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add networkMode 'always' to the QueryClient defaultOptions</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (the `new QueryClient({ defaultOptions: { queries: {...} } })` ~lines 24-31)
    - .planning/phases/09-offline-reconnect-ux/09-PATTERNS.md §`frontend/src/App.tsx`
      (exact addition + rationale)
    - .planning/research/PITFALLS.md §Pitfall 35 and §Pitfall 36 (why `'always'`, not
      `'offlineFirst'`: queries must not pause on `navigator.onLine=false`; SSE-derived gating
      does the pausing instead, and this prevents the mass refetch on reconnect)
  </read_first>
  <action>
    In the `QueryClient` `defaultOptions.queries` object, add the literal property
    `networkMode: 'always'` alongside the existing `retry: 1` and `refetchOnWindowFocus: false`.
    This is the locked decision D8 (PITFALLS reasoning overrides the STACK recommendation). Do
    NOT change `retry` or `refetchOnWindowFocus`. Per the CONTEXT lock (line 74), the kiosk search
    query `staleTime` stays at `30_000` — do NOT bump it to `60_000`; this plan touches only
    `App.tsx` (the search query lives in KioskView, unchanged).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX/frontend && grep -q "networkMode: 'always'" src/App.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -q "networkMode: 'always'" frontend/src/App.tsx` AND the existing
      `retry: 1` and `refetchOnWindowFocus: false` lines remain (grep both).
    - `npx tsc --noEmit` exits 0 (valid TanStack Query v5 `networkMode` value).
    - Tie to ROADMAP success criterion 3/4: queries no longer pause on `navigator.onLine=false`,
      so reconnect refetch is governed by SSE state + `staleTime`, not a `navigator.onLine` flip.
  </acceptance_criteria>
  <done>
    `App.tsx` QueryClient sets `networkMode: 'always'`; tsc clean; no other query option changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser network state → TanStack Query scheduler | `navigator.onLine` is an untrustworthy connectivity signal on a LAN (PITFALLS 35) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Denial of Service | Reconnect refetch storm via `refetchOnReconnect` | mitigate | `networkMode: 'always'` stops queries from being mass-paused-then-resumed on a `navigator.onLine` flip; combined with the 09-01 `retry:` jitter and unchanged `staleTime: 30_000`, reconnect refetches are spread and de-duplicated (PITFALLS 36). |
| T-09-05 | Spoofing | `navigator.onLine=true` while server is down (false connectivity) | mitigate | Connectivity truth is `connectivity.sseConnected` (set by SSE open/error), never `navigator.onLine`; `bannerVisible` derives from `!sseConnected` only (PITFALLS 35). `navigator.onLine` is consumed downstream (09-03) for cosmetic copy only. |
| T-09-06 | Tampering | Client-side store mutation of `bannerVisible` | accept | Client-side UX state, not a security boundary. Server-side authz (admin PIN, device binding from prior phases) remains the real control; a tampered banner flag cannot grant access. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean.
- `npx vitest run src/state/store.test.ts` green.
- `grep` confirms both `bannerVisible: boolean`/`bannerVisible: !connected` (store) and
  `networkMode: 'always'` (App.tsx).
</verification>

<success_criteria>
The store exposes a boolean `bannerVisible` that tracks `!sseConnected`, and the QueryClient
uses `networkMode: 'always'` — the contract + config the banner/degraded-mode plan consumes.
</success_criteria>

<output>
Create `.planning/phases/09-offline-reconnect-ux/09-02-SUMMARY.md` when done.
</output>
