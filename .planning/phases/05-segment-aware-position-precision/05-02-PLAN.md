---
phase: 05-segment-aware-position-precision
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/gruvax/estimator/segment_cache.py
  - src/gruvax/estimator/boundary_cache.py
  - fixtures/synth_collection.py
  - tests/unit/test_segment_cache.py
autonomous: true
requirements: [SEG-02, SEG-03, SEG-04, SEG-05]
user_setup: []

must_haves:
  truths:
    - "SegmentCache.derive() turns cut points + collection snapshot into ordered per-label SegmentBins with no DB access"
    - "Per-segment counts are row-counts of v_collection records (dupes + variants each counted), never catalog arithmetic"
    - "An admin width override wins over the count-derived fraction, and per-bin applied_fractions always sum to 1.0"
    - "mypy --strict passes on boundary_cache.py (the _overrides comprehension is index-safe) and on the new segment_cache.py"
    - "The full test suite stays green after this wave (no consumer of last_* is touched yet, so nothing regresses)"
  artifacts:
    - path: "src/gruvax/estimator/segment_cache.py"
      provides: "SegmentCache, SegmentBin, LabelSegment dataclasses + derive()/lookup API"
      contains: "class SegmentCache"
      min_lines: 150
    - path: "src/gruvax/estimator/boundary_cache.py"
      provides: "Type-safe _overrides comprehension (mypy --strict clean at line ~100)"
      contains: "_overrides"
    - path: "fixtures/synth_collection.py"
      provides: "make_multi_label_bin()/make_straddle() return a populated SegmentCache (not None)"
      contains: "SegmentCache"
    - path: "tests/unit/test_segment_cache.py"
      provides: "SEG-02/03/04/05 unit tests un-skipped and passing"
      contains: "def test_"
  key_links:
    - from: "src/gruvax/estimator/segment_cache.py"
      to: "src/gruvax/estimator/collection_snapshot.py"
      via: "get_label_records() + parse_key sort for row-rank derivation"
      pattern: "get_label_records"
    - from: "fixtures/synth_collection.py"
      to: "src/gruvax/estimator/segment_cache.py"
      via: "factories call SegmentCache().derive(...) and return it"
      pattern: "SegmentCache\\(\\)"
---

<objective>
Build the `SegmentCache` service: the in-memory derived structure that turns the cut-point
`BoundaryCache` plus the `CollectionSnapshot` into ordered per-label `SegmentBin`s (counts,
auto-fractions, applied-fractions with override renormalization, straddle `continues` flags).
Fix the one residual mypy `[index]` error in `boundary_cache.py`. Upgrade the two Phase-5
synth factories to return a populated `SegmentCache` instead of the `None` placeholder, then
un-skip and pass the SEG-02/03/04/05 unit tests stubbed in 05-01.

Purpose: SegmentCache is the seam every downstream wave reads from — the estimator (Wave 3),
the admin segment API (Wave 4), and the frontend editor (Wave 5). It must exist and be correct
before any consumer is rewired. This wave adds ONLY a new file + a typing fix + factory upgrades,
so it touches no `last_*` consumer and leaves the suite green.
Output: `segment_cache.py`, a mypy-clean `boundary_cache.py`, upgraded factories, passing SEG-02..05 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-segment-aware-position-precision/05-CONTEXT.md
@.planning/phases/05-segment-aware-position-precision/05-RESEARCH.md
@.planning/phases/05-segment-aware-position-precision/05-PATTERNS.md
@.planning/phases/05-segment-aware-position-precision/05-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase. Use directly. -->

From src/gruvax/estimator/boundary_cache.py (Phase 5 cut-point shape — 05-01 GREEN):
```python
@dataclass(frozen=True)
class BoundaryRow:
    unit_id: int; row: int; col: int
    first_label: str | None    # the cut point
    first_catalog: str | None  # the cut point
    is_empty: bool
class BoundaryCache:
    def get_boundaries(self) -> Sequence[BoundaryRow]: ...
    @property
    def overrides(self) -> dict[tuple[int, int, int, str], float]: ...
    def _load_rows(self, rows: list[BoundaryRow]) -> None: ...          # test seam
    def _load_overrides(self, o: dict[tuple[int,int,int,str], float]) -> None: ...  # test seam
```

From src/gruvax/estimator/collection_snapshot.py:
```python
@dataclass(frozen=True)
class RecordRow:
    release_id: int; label: str; catalog_number: str
class CollectionSnapshot:
    _by_label: dict[str, list[RecordRow]]      # keyed by label.casefold()
    def get_label_records(self, label: str) -> list[RecordRow]: ...   # O(1), unsorted within label
    def _load_snapshot(self, by_label: dict[str, list[RecordRow]]) -> None: ...  # test seam
```

From src/gruvax/estimator/normalize.py (the ONLY legal catalog compare path — POS-01):
```python
def parse_key(catalog_number: str) -> tuple: ...   # use as sort key; never raw-string compare
```

Comparison rules (Pitfall 1 / Pitfall C, binding):
  Labels  → `.casefold()` ONLY (never parse_key).
  Catalogs → `parse_key()` ONLY (never raw string).
</interfaces>

<!-- Authoritative algorithm + pattern sources -->
@.planning/notes/segment-aware-boundaries.md
@src/gruvax/estimator/segment_cache.py
@src/gruvax/estimator/boundary_cache.py
@src/gruvax/estimator/collection_snapshot.py
@fixtures/synth_collection.py
@tests/unit/test_segment_cache.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SegmentCache (dataclasses + derive + lookups) and fix boundary_cache mypy</name>
  <files>src/gruvax/estimator/segment_cache.py, src/gruvax/estimator/boundary_cache.py</files>
  <read_first>
    - src/gruvax/estimator/segment_cache.py (current 05-01 state — confirm whether 05-01 left a stub or empty file; if a skeleton exists, extend it)
    - 05-PATTERNS.md § "src/gruvax/estimator/segment_cache.py" (the full dataclass shapes, derive() algorithm steps 1-6, override-normalization Pitfall 2, lookup methods, label/catalog comparison rule — copy this structure)
    - 05-RESEARCH.md § "Pattern 1: SegmentCache" and "Pitfall 2: Segment fraction normalization" (the 4-step renormalization rule)
    - .planning/notes/segment-aware-boundaries.md (cut-point model, two invariants, the straddle/`continues` rule)
    - src/gruvax/estimator/boundary_cache.py lines 93-102 (the `_overrides` dict-comprehension that mypy --strict flags `[index]` because `fetchall()` rows are typed object/Any)
  </read_first>
  <behavior>
    - derive(): single-label single-bin → exactly 1 LabelSegment, auto_fraction == 1.0, applied_fraction == 1.0, first_rank_in_label == 0, offset_in_bin == 0.0.
    - derive() on make_multi_label_bin(): bin has 2 ordered LabelSegments (LabelA then LabelB by casefold), counts == row counts (LabelA=8, LabelB=6 including the LB 003 duplicate and LB 003-r variant), auto_fractions sum to 1.0.
    - Row-count rule (SEG-03): LabelB.segment_count counts BOTH the duplicate LB 003 owned copy AND the LB 003-r variant — i.e. len(records in range), never (parse_key(last)-parse_key(first)) arithmetic.
    - Override application (SEG-04): with an injected override for one label in a 2+ label bin, that label's applied_fraction == the override and the remaining labels' applied_fractions are renormalized proportionally by raw count so the bin sum == 1.0 (assert within 1e-6).
    - continues flag: in make_straddle(), the LabelS segment in bin (1,0,0) has continues == True (label runs into the next bin); the segment in the final bin (1,0,1) has continues == False.
    - get_segment_for_rank(label, rank) returns the (SegmentBin, LabelSegment) whose [first_rank_in_label, last_rank_in_label] contains rank; None if no segment matches.
  </behavior>
  <action>
    Implement `src/gruvax/estimator/segment_cache.py` per the 05-PATTERNS.md SegmentCache section.
    Define frozen dataclasses `LabelSegment(label, first_rank_in_label, last_rank_in_label,
    segment_count, auto_fraction, applied_fraction, offset_in_bin, is_override, continues)` and
    `SegmentBin(unit_id, row, col, cut_label, cut_catalog, segments: tuple[LabelSegment, ...])`.
    Define `class SegmentCache` with `_bins: list[SegmentBin]`, `_by_coord: dict[tuple[int,int,int], SegmentBin]`,
    and methods `derive(cache, snapshot, overrides)`, `_load_bins(bins)` (test seam mirroring
    BoundaryCache._load_rows), `get_bin(unit_id,row,col)`, `get_bins_for_label(label)`,
    `get_segment_for_rank(label, rank)`, and `invalidate()`.
    derive() algorithm (Pattern 1 steps): (1) sort cut points by (unit_id,row,col); (2) for each
    label, get records and sort by `parse_key(r.catalog_number)`; (3) assign each label's records
    to bins by comparing the record's row-rank against each bin's cut-point rank within that label's
    sorted run (a record belongs to bin B for that label if its rank >= B's first-rank-for-label and
    < the next bin's first-rank-for-label); (4) per bin, sum segment_counts → bin_total; auto_fraction
    = segment_count / bin_total; (5) apply overrides keyed (unit,row,col,label.casefold-match) then
    renormalize per Pitfall 2 — collect overridden fractions, remaining = 1.0 - sum(overridden),
    distribute remaining proportionally among non-overridden by raw count, then assert
    abs(sum(applied_fraction) - 1.0) < 1e-6; (6) compute offset_in_bin as the cumulative sum of
    preceding applied_fractions; set continues=True when a label's records extend past this bin's run.
    Labels compared by `.casefold()`; catalogs sorted by `parse_key()` — never raw string, never arithmetic.
    No DB access anywhere in this module (CPU-only seam).
    Then fix the mypy `[index]` in boundary_cache.py: type the cursor rows so `r[0..4]` is indexable
    under `--strict` (e.g. annotate `overrides_raw` / cast each row to a tuple, or use a typed
    psycopg row factory) and remove any now-unnecessary `# type: ignore`. Do not change the SQL or
    the public `overrides`/`_load_overrides`/`invalidate` API.
  </action>
  <verify>
    <automated>uv run mypy --strict src/gruvax/estimator/segment_cache.py src/gruvax/estimator/boundary_cache.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run mypy --strict src/gruvax/estimator/segment_cache.py src/gruvax/estimator/boundary_cache.py` exits 0 with no errors (the prior `boundary_cache.py:~100 [index]` error is gone).
    - `segment_cache.py` defines `class SegmentCache`, `class SegmentBin`, `class LabelSegment`; no `import psycopg` / no DB calls (grep: `grep -nE "psycopg|await .*\.execute|pool" src/gruvax/estimator/segment_cache.py` returns nothing).
    - derive() contains an explicit `assert` (or raise) that per-bin applied_fractions sum to 1.0 within 1e-6.
    - Catalog sort uses `parse_key`; label compare uses `.casefold()` (grep both present; grep that no raw `last_catalog`/`last_label` symbol appears).
    - `_done`: SegmentCache compiles, is mypy-strict clean, and boundary_cache.py is mypy-strict clean.
  </acceptance_criteria>
  <done>SegmentCache implemented per spec; boundary_cache.py mypy-strict clean; both files type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade synth factories to return a derived SegmentCache; un-skip SEG-02/03/04/05 unit tests</name>
  <files>fixtures/synth_collection.py, tests/unit/test_segment_cache.py</files>
  <read_first>
    - fixtures/synth_collection.py lines 239-343 (current make_multi_label_bin/make_straddle returning `(BoundaryCache, None, CollectionSnapshot)`) and lines 71-138 (_build_cache_multi, _build_snapshot, _build_snapshot_multi helpers)
    - tests/unit/test_segment_cache.py (the 6 skip-stubbed tests from 05-01: test_segment_cache_derive_single_label, test_segment_cache_segments_ordered, test_row_count_not_arithmetic, test_override_applied, test_override_renormalization_sums_to_one, test_contiguity_validation)
    - 05-PATTERNS.md § "Tests (all new test files)" (session-scoped fixture pattern + no-DB seam + the SegmentCache state pre-check pattern)
    - 05-VALIDATION.md Per-Requirement Verification Map rows SEG-02/03/04/05 (the exact `<automated>` commands these tests back)
  </read_first>
  <action>
    Change the return type of `make_multi_label_bin()` and `make_straddle()` from
    `tuple[BoundaryCache, None, CollectionSnapshot]` to
    `tuple[BoundaryCache, SegmentCache, CollectionSnapshot]`: after building the cache + snapshot,
    construct `SegmentCache()`, call `.derive(cache, snapshot, cache.overrides)`, and return it in
    the middle slot. Update the module docstring lines that say "SegmentCache is None here — Plan 02
    fills the derive() logic" to reflect the populated cache. For the override tests, expose a way to
    inject overrides (either accept an optional `overrides` arg defaulting to `{}` and call
    `cache._load_overrides(overrides)` before derive, OR add a sibling factory
    `make_multi_label_bin_with_override(label, fraction)`) — choose whichever keeps the existing
    stub call sites working with minimal edits; document the choice in the factory docstring.
    Then un-skip and implement the assertions in the five SEG-02/03/04 tests in test_segment_cache.py
    (leave `test_contiguity_validation`, which imports `validate_contiguity` from admin/validation,
    SKIPPED — it is Wave 4 scope; keep its skip reason but update it to "implemented in Plan 05-04").
    Each test must follow Pitfall 5 discipline where relevant: assert SegmentCache state
    (segment count, auto_fraction, first_rank) before asserting derived values.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_segment_cache.py -x -q && uv run mypy --strict src/gruvax/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_segment_cache.py -x -q` passes; the SEG-02/03/04 tests are no longer skipped (pytest report shows them as passed, not skipped).
    - `test_row_count_not_arithmetic` asserts LabelB count == 6 (includes the duplicate LB 003 and the LB 003-r variant) and would FAIL if counting used parse_key subtraction.
    - `test_override_renormalization_sums_to_one` asserts per-bin applied_fraction sum == 1.0 within 1e-6 with at least one override active.
    - `make_multi_label_bin()` / `make_straddle()` return a populated `SegmentCache` (grep: `SegmentCache()` and `.derive(` present in fixtures/synth_collection.py).
    - GREEN-SUITE GATE: `uv run mypy --strict src/gruvax/` exits 0.
    - Full suite gate (run once at wave end): `just lint && just typecheck && just test` all exit 0 — record the pass in the SUMMARY.
  </acceptance_criteria>
  <done>Factories return derived SegmentCache; SEG-02/03/04 tests pass un-skipped; mypy --strict clean; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB rows → SegmentCache derive | `segment_overrides.fraction` values flow into applied-fraction math; malformed values could break the sum-to-1.0 invariant |
| Collection records → row-rank math | record lists drive counts; a record with no parseable catalog could distort ordering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | override fraction in derive() | mitigate | DB CHECK `fraction > 0.0 AND fraction <= 1.0` (migration 0005, already applied) bounds inputs; derive() asserts per-bin sum == 1.0 within 1e-6 and renormalizes non-overridden segments |
| T-05-02-02 | Denial of Service | derive() over large snapshot | accept | CPU-only, O(records-per-label); ~3,000-record home-LAN scale; runs at load/invalidate, never on the hot path |
| T-05-02-03 | Information Disclosure | segment data | accept | No new endpoint in this wave; SegmentCache is in-process only |
| T-05-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this wave (RESEARCH.md: zero new deps); slopcheck N/A |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_segment_cache.py -x -q` — SEG-02/03/04 pass un-skipped.
- `just typecheck` (mypy --strict src/gruvax/) exits 0 — boundary_cache.py [index] fixed, segment_cache.py clean.
- `just lint` exits 0.
- `just test` (full suite, Postgres up) exits 0 — no regression (no last_* consumer touched this wave).
</verification>

<success_criteria>
- SegmentCache derives correct ordered per-label segments from cut points (SEG-02), counts by row-count incl. dupes/variants (SEG-03), applies overrides with renormalization summing to 1.0 (SEG-04).
- boundary_cache.py and segment_cache.py are mypy --strict clean.
- The full test suite (`just lint && just typecheck && just test`) is GREEN at wave end.
</success_criteria>

<output>
Create `.planning/phases/05-segment-aware-position-precision/05-02-SUMMARY.md` when done.
MUST record the green-suite gate result (`just lint && just typecheck && just test` all exit 0) and a
grep audit confirming `segment_cache.py` has no DB access and no `last_label`/`last_catalog` symbols.
</output>
