---
phase: 05-segment-aware-position-precision
plan: 02
type: tdd
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/gruvax/estimator/segment_cache.py
  - src/gruvax/estimator/algorithm.py
  - src/gruvax/estimator/boundary_math.py
  - src/gruvax/api/deps.py
  - src/gruvax/app.py
  - src/gruvax/api/locate.py
  - tests/unit/test_segment_cache.py
  - tests/unit/test_segment_estimator.py
  - tests/property/test_segment_props.py
  - tests/integration/test_locate.py
files_modified_note: "test_segment_cache.py / test_segment_estimator.py / test_segment_props.py are filled in here (created as stubs in Plan 01); no structural overlap with Plan 01's other files"
autonomous: true
requirements: [SEG-02, SEG-03, SEG-06, SEG-07]
must_haves:
  truths:
    - "SegmentCache.derive() produces ordered per-label LabelSegments from cut points + collection snapshot"
    - "Segment counts are row-counts of v_collection (dupes + variants counted), never catalog arithmetic"
    - "Per-bin applied_fraction values sum to 1.0; overrides win and non-overridden labels renormalize"
    - "locate_by_segment() two-level interpolation: a single-segment bin reproduces §4.1 exactly"
    - "A straddling label resolves to the correct adjacent bin by row-rank with no special-casing"
    - "/api/locate returns estimator_version 'segment-v1' for covered records; §4.8 cube-only fallback retained"
    - "/api/locate hot path stays CPU-only with no DB on the estimate path; p95 <= 50 ms"
  artifacts:
    - path: "src/gruvax/estimator/segment_cache.py"
      provides: "SegmentCache, SegmentBin, LabelSegment — derived segment structure, no DB on hot path"
      contains: "class SegmentCache"
    - path: "src/gruvax/estimator/algorithm.py"
      provides: "locate_by_segment() (replaces locate_by_index) + segment-aware locate_cube_only + updated locate() dispatcher"
      contains: "def locate_by_segment"
    - path: "src/gruvax/api/deps.py"
      provides: "get_segment_cache dependency provider"
      contains: "get_segment_cache"
    - path: "src/gruvax/app.py"
      provides: "SegmentCache instantiated + derived in lifespan after BoundaryCache + CollectionSnapshot load"
      contains: "SegmentCache"
  key_links:
    - from: "src/gruvax/api/locate.py"
      to: "locate_by_segment via locate()"
      via: "Depends(get_segment_cache) passed to locate()"
      pattern: "get_segment_cache"
    - from: "src/gruvax/estimator/algorithm.py"
      to: "SegmentCache.get_segment_for_rank"
      via: "two-level interpolation lookup"
      pattern: "get_segment_for_rank"
    - from: "src/gruvax/app.py"
      to: "SegmentCache.derive"
      via: "lifespan startup after snapshot.load()"
      pattern: "\\.derive\\("
---

<objective>
Build the segment-aware estimator backend end-to-end so `/api/locate` returns precise two-level
interpolation estimates. Create the new `SegmentCache` service that derives per-bin ordered
`LabelSegment`s (counts row-counted from `v_collection`, fractions = override ?? auto with
renormalization) from the cut points + collection snapshot. Replace `locate_by_index` with
`locate_by_segment` (two-level interpolation), update the §4.8 `locate_cube_only` fallback to use
`SegmentCache` for coverage (last_* no longer exists), rewrite `boundary_math` counting to use
`SegmentBin`, add the `get_segment_cache` dependency + lifespan wiring, and route `/api/locate`
through the segment estimator with `estimator_version = "segment-v1"`.

Purpose: This is the core accuracy slice (SEG-02/03/06/07). After this plan, a user querying a
record whose label shares a bin with other labels sees a correct sub-cube position — the visible
end-to-end capability MVP_MODE requires. §4.1 is retired (D-01); correctness rests on the
single-segment regression invariant + Hypothesis properties (D-02), not an A/B harness.

Output: `segment_cache.py` (new), refactored `algorithm.py` + `boundary_math.py`, `get_segment_cache`
in `deps.py`, lifespan wiring in `app.py`, `/api/locate` routed through the segment estimator, and
the unit/property test files (stubbed in Plan 01) now passing including the benchmark p95 ≤ 50 ms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-segment-aware-position-precision/05-CONTEXT.md
@.planning/phases/05-segment-aware-position-precision/05-RESEARCH.md
@.planning/phases/05-segment-aware-position-precision/05-PATTERNS.md
@.planning/phases/05-segment-aware-position-precision/05-VALIDATION.md
@.planning/notes/segment-aware-boundaries.md
@.planning/phases/05-segment-aware-position-precision/05-01-SUMMARY.md

<interfaces>
<!-- LocateResult / SubInterval contract — FROZEN (contract.py). Do NOT change shapes. -->
<!-- sub_cube_interval emits {start, end, crosses_boundary, next_cube}; no cube field (Phase 2 D). -->
<!-- Estimator constants: POSITION_HALF_WIDTH = 0.05; CUBE_ONLY_CONFIDENCE = 0.30; -->
<!--   NO_BOUNDARY_CONFIDENCE = 0.0; compute_confidence(k) in constants.py. -->
<!-- SEGMENT_ESTIMATOR_VERSION = "segment-v1" (added Plan 01). -->

<!-- SegmentCache dataclass shapes (PATTERNS Pattern 1 / segment_cache lines 187-260): -->
LabelSegment(label, first_rank_in_label, last_rank_in_label, segment_count,
             auto_fraction, applied_fraction, offset_in_bin, is_override, continues)  # frozen
SegmentBin(unit_id, row, col, cut_label, cut_catalog, segments: tuple[LabelSegment, ...])  # frozen

<!-- SegmentCache API surface to implement: -->
SegmentCache.derive(cache: BoundaryCache, snapshot: CollectionSnapshot,
                    overrides: dict[tuple[int,int,int,str], float]) -> None
SegmentCache._load_bins(bins)  # test seam (mirrors BoundaryCache._load_rows)
SegmentCache.get_bin(unit_id, row, col) -> SegmentBin | None
SegmentCache.get_bins_for_label(label) -> list[SegmentBin]
SegmentCache.get_segment_for_rank(label, rank) -> tuple[SegmentBin, LabelSegment] | None
SegmentCache.invalidate() -> None

<!-- Comparison rules (Pitfall C / POS-01): labels .casefold() ONLY; catalogs parse_key() ONLY. -->
<!-- CollectionSnapshot.get_label_records(label) -> list[RecordRow] (per-label owned items). -->
<!-- app.state holds: db_pool, boundary_cache, collection_snapshot, event_bus, settings_cache. -->
<!-- deps providers exist: get_pool, get_boundary_cache, get_collection_snapshot, get_event_bus, require_admin. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SegmentCache derivation (counts, fractions, override renormalization)</name>
  <files>src/gruvax/estimator/segment_cache.py, tests/unit/test_segment_cache.py, tests/property/test_segment_props.py</files>
  <read_first>
    - src/gruvax/estimator/boundary_cache.py (analog — module structure, frozen dataclass style, _load_rows seam, invalidate; the _overrides map added in Plan 01)
    - src/gruvax/estimator/collection_snapshot.py (get_label_records API; how per-label records are grouped/ordered; casefold grouping)
    - src/gruvax/estimator/normalize.py (parse_key + catalog_in_range — the ONLY legal catalog compare path; POS-01)
    - .planning/notes/segment-aware-boundaries.md (segment derivation invariants: label contiguity; counts = row-count not catalog arithmetic; the bin example)
    - .planning/phases/05-segment-aware-position-precision/05-PATTERNS.md (segment_cache.py Pattern lines 157-280 — dataclasses, cache class, override normalization Pitfall 2, label/catalog rule)
    - .planning/phases/05-segment-aware-position-precision/05-RESEARCH.md (Pattern 1; Pitfall 2 normalization; Pitfall 5; Code Examples segment derivation pseudocode)
    - tests/unit/test_segment_cache.py + tests/property/test_segment_props.py (the Wave 0 stubs from Plan 01 to fill in)
    - fixtures/synth_collection.py (make_multi_label_bin / make_straddle factories)
  </read_first>
  <behavior>
    - derive() on a single-label single cube → 1 LabelSegment with auto_fraction == 1.0, first_rank_in_label == 0, offset_in_bin == 0.0
    - derive() on a bin with 2 labels (counts 8 and 12) → fractions ≈ 0.40 and 0.60, summing to 1.0
    - A label with a duplicate owned copy + a `-r` variant in range → segment_count counts ALL rows (dupes + variants), not a catalog subtraction
    - Override 0.45 on one label of a 2-label bin → that label applied_fraction == 0.45, the other renormalized to 0.55, sum == 1.0
    - Two overrides in a 3-label bin → remaining non-overridden label fills (1.0 - sum overrides) proportionally; per-bin sum == 1.0 within 1e-6
    - get_segment_for_rank(label, rank) returns the bin+segment whose [first_rank, last_rank] contains rank; None when out of range
    - A straddling label has a LabelSegment in each of the two adjacent bins with continues=True on the earlier bin's segment
  </behavior>
  <action>
    Create `src/gruvax/estimator/segment_cache.py` mirroring `boundary_cache.py` module structure
    (module docstring, `from __future__ import annotations`, TYPE_CHECKING guard importing BoundaryCache/BoundaryRow/CollectionSnapshot).
    Define frozen `LabelSegment` and `SegmentBin` dataclasses exactly per the interfaces block.
    Implement `SegmentCache` with: `__init__` (empty `_bins` list + `_by_coord` dict), `derive()`, `_load_bins()` test seam,
    `get_bin()`, `get_bins_for_label()`, `get_segment_for_rank()`, `invalidate()`.
    derive() algorithm: (1) sort cut points by (unit_id,row,col) physical order; (2) for each label in the snapshot,
    get records sorted by parse_key(catalog_number) — labels compared with `.casefold()` ONLY, catalogs with parse_key() ONLY;
    (3) assign each label's records to bins by comparing the record's row-rank to the cut-point ranks (a record belongs to bin B
    if its rank >= B's first_rank for that label and < the next bin's first_rank for that label) — derive first_rank_in_label/last_rank_in_label/segment_count = (last_rank - first_rank + 1) by row-count;
    (4) per bin sum segment_counts to get bin_total and compute auto_fraction = segment_count / bin_total;
    (5) apply overrides then renormalize per Pitfall 2: collect overridden fractions, remaining = 1.0 - sum(overridden),
    distribute remaining proportionally among non-overridden by raw count, assert abs(sum(applied_fraction) - 1.0) < 1e-6;
    (6) compute offset_in_bin as cumulative sum of preceding segments' applied_fraction; set continues=True for any segment whose label extends past this bin's cut.
    NEVER do catalog arithmetic; NEVER touch the DB inside derive() (it receives cache/snapshot/overrides already in memory).
    Fill in `tests/unit/test_segment_cache.py` (test_row_count_not_arithmetic, test_override_applied, test_contiguity_validation may stay deferred to Plan 03 for the validator portion — keep the derivation half here, derivation correctness, multi-override renormalization)
    and the fraction-sum + straddle-derivation Hypothesis invariants in `tests/property/test_segment_props.py`
    (test_per_bin_fractions_sum_to_one). Use the make_multi_label_bin / make_straddle factories and the _load_bins / _load_rows no-DB seams.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && pytest tests/unit/test_segment_cache.py::test_row_count_not_arithmetic tests/unit/test_segment_cache.py::test_override_applied tests/property/test_segment_props.py::test_per_bin_fractions_sum_to_one -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `segment_cache.py` contains `class SegmentCache`, `class LabelSegment`, `class SegmentBin`, and `def get_segment_for_rank(`
    - `segment_cache.py` derive() uses `parse_key` for catalog sort and `.casefold()` for labels (grep both present)
    - `segment_cache.py` contains no catalog subtraction for counts (review: counts derive from `len()`/rank arithmetic on records, not catalog numbers)
    - `pytest tests/unit/test_segment_cache.py::test_row_count_not_arithmetic` exits 0
    - `pytest tests/unit/test_segment_cache.py::test_override_applied` exits 0
    - `pytest tests/property/test_segment_props.py::test_per_bin_fractions_sum_to_one` exits 0
  </acceptance_criteria>
  <done>SegmentCache derives correct ordered per-label segments with row-counted counts and override-renormalized fractions summing to 1.0; derivation + fraction-sum tests pass; no DB and no catalog arithmetic inside derive().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-level interpolation estimator + §4.8 fallback refactor + boundary_math</name>
  <files>src/gruvax/estimator/algorithm.py, src/gruvax/estimator/boundary_math.py, tests/unit/test_segment_estimator.py, tests/property/test_segment_props.py</files>
  <read_first>
    - src/gruvax/estimator/algorithm.py (the file being refactored — locate_cube_only lines 47-121 uses last_label/last_catalog; locate_by_index lines 124-268; locate() dispatcher lines 271-329)
    - src/gruvax/estimator/boundary_math.py (count_records_in_boundary / get_records_in_boundary use last_*; suggest_midpoint/sample_records unchanged)
    - src/gruvax/estimator/segment_cache.py (the SegmentCache built in Task 1 — get_segment_for_rank, SegmentBin/LabelSegment)
    - src/gruvax/estimator/contract.py (LocateResult / SubInterval frozen shapes; CUBE_ONLY_CONFIDENCE; NO_BOUNDARY_CONFIDENCE)
    - src/gruvax/estimator/constants.py (POSITION_HALF_WIDTH; compute_confidence; SEGMENT_ESTIMATOR_VERSION)
    - .planning/phases/05-segment-aware-position-precision/05-PATTERNS.md (algorithm.py Pattern lines 283-416 — locate_cube_only sig change, locate_by_segment, locate() dispatcher; boundary_math Pattern lines 420-438; D-02 regression invariant test lines 1106-1120)
    - .planning/phases/05-segment-aware-position-precision/05-RESEARCH.md (Pattern 2 two-level interpolation; Pitfall 1 §4.8 coverage via SegmentCache; Pitfall 6 boundary_math; two-level interpolation formula code example lines 691-710)
    - .planning/notes/segment-aware-boundaries.md (two-level interpolation algorithm + straddle case)
    - tests/property/test_estimator_props.py (the existing §4.1 locate_by_index is the equality target for the D-02 regression invariant — keep locate_by_index importable for the test, or assert against a known §4.1 formula inline)
  </read_first>
  <behavior>
    - locate_by_segment on a single-segment bin produces the SAME sub_cube_interval.start as the §4.1 formula idx/(k-1) (D-02 regression anchor, test_single_segment_bin_reproduces_v1_index)
    - locate_by_segment for a record in the 2nd of 3 labels offsets by the first label's applied_fraction before interpolating within its segment
    - A straddle: an early-rank record resolves to the first bin; a late-rank record (past the cut) resolves to the second bin (test_straddle_resolves_to_correct_bin)
    - segment_count == 1 → position = offset + applied_fraction * 0.5 (singleton midpoint, never zero-width)
    - sub_cube_interval bounds: 0 <= start <= end <= 1 (test_sub_cube_interval_bounds)
    - locate_cube_only with no snapshot records for the label → NO_BOUNDARY confidence, sub_cube_interval None
    - count_records_in_bin(SegmentBin) == sum of segment_counts
  </behavior>
  <action>
    In `algorithm.py`: add `from gruvax.estimator.segment_cache import SegmentCache` and `SEGMENT_ESTIMATOR_VERSION` import.
    Rewrite `locate_cube_only()` to accept `(release_id, label, catalog_number, segment_cache, snapshot)` and determine coverage
    via `segment_cache.get_segment_for_rank(label, rank)` where rank is the record's row-rank among snapshot.get_label_records(label) sorted by parse_key
    (Pitfall 1 — last_label/last_catalog are gone). Return primary_cube/label_span from the matched SegmentBin coordinates; NO_BOUNDARY when rank not found.
    Replace `locate_by_index()` with `locate_by_segment(release_id, label, catalog_number, segment_cache, snapshot)` implementing the two-level formula:
    sort label records by parse_key, find row-rank; get (bin, seg) = get_segment_for_rank(label, rank); if None fall back to locate_cube_only;
    offset = seg.offset_in_bin; rank_in_segment = rank - seg.first_rank_in_label;
    if seg.segment_count <= 1: f = offset + seg.applied_fraction * 0.5 else f = offset + (rank_in_segment / (seg.segment_count - 1)) * seg.applied_fraction;
    start = max(0.0, f - POSITION_HALF_WIDTH), end = min(1.0, f + POSITION_HALF_WIDTH);
    confidence = compute_confidence(len(sorted_recs)); estimator_version = SEGMENT_ESTIMATOR_VERSION ("segment-v1").
    Update the `locate()` dispatcher to take `segment_cache` (replacing the `cache: BoundaryCache` param), route to locate_by_segment when the snapshot has records,
    fall back to locate_cube_only (estimator_version "cube-only-v1") when no records OR confidence <= CUBE_ONLY_CONFIDENCE (strip sub_cube_interval to None in the low-confidence branch, mirroring current behavior).
    KEEP `locate_by_index` importable IF the D-02 regression test compares against it; otherwise the test may assert against the literal §4.1 formula — implementer chooses but the regression invariant MUST be a real comparison (Pitfall 5: pre-assert SegmentCache single-segment state first).
    In `boundary_math.py`: replace `count_records_in_boundary()` with `count_records_in_bin(bin_: SegmentBin) -> int` returning `sum(seg.segment_count for seg in bin_.segments)`; retire `get_records_in_boundary()` (uses last_*); leave `suggest_midpoint`/`sample_records` unchanged.
    Fill in `tests/unit/test_segment_estimator.py` (two-level interpolation unit cases + fallback) and the remaining Hypothesis invariants in `tests/property/test_segment_props.py`:
    test_single_segment_bin_reproduces_v1_index (with Pitfall 5 pre-check), test_straddle_resolves_to_correct_bin, test_primary_cube_in_label_span, test_sub_cube_interval_bounds, test_monotone_position_within_label, test_cosmetic_stability.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && pytest tests/unit/test_segment_estimator.py tests/property/test_segment_props.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `algorithm.py` contains `def locate_by_segment(` and the dispatcher `def locate(` accepts a `segment_cache` parameter
    - `algorithm.py` no longer references `b.last_label` / `b.last_catalog` (grep with comments stripped: `grep -v '^\s*#' src/gruvax/estimator/algorithm.py | grep -c 'last_label'` == 0)
    - `boundary_math.py` contains `def count_records_in_bin(` and no longer references `last_label`
    - `pytest tests/property/test_segment_props.py::test_single_segment_bin_reproduces_v1_index` exits 0 and its body pre-asserts `len(bin.segments) == 1` before the equality check
    - `pytest tests/property/test_segment_props.py::test_straddle_resolves_to_correct_bin` exits 0
    - `pytest tests/unit/test_segment_estimator.py tests/property/test_segment_props.py` exits 0
  </acceptance_criteria>
  <done>Two-level interpolation estimator replaces §4.1; single-segment bin reproduces §4.1 exactly; straddle resolves by rank with no special-casing; §4.8 fallback + boundary_math use SegmentCache; all segment unit + property tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire SegmentCache into lifespan + deps + /api/locate; benchmark p95 ≤ 50 ms</name>
  <files>src/gruvax/api/deps.py, src/gruvax/app.py, src/gruvax/api/locate.py, tests/integration/test_locate.py</files>
  <read_first>
    - src/gruvax/app.py (lifespan lines 77-141 — BoundaryCache + CollectionSnapshot instantiation/load, app.state assignments; add SegmentCache after both load)
    - src/gruvax/api/deps.py (get_boundary_cache lines 41-60, get_collection_snapshot lines 62-83 — mirror for get_segment_cache; 503-if-missing pattern)
    - src/gruvax/api/locate.py (locate_endpoint lines 62-124 — Depends wiring, the locate() call, the response dict including estimator_version)
    - src/gruvax/estimator/segment_cache.py (SegmentCache.derive signature; consumes BoundaryCache._overrides)
    - tests/integration/test_locate.py (existing benchmark + locate integration tests — extend assertion to estimator_version "segment-v1"; the --benchmark-only path for the p95 gate)
    - .planning/phases/05-segment-aware-position-precision/05-PATTERNS.md (deps/lifespan wiring referenced via Shared Patterns; the invalidate+derive sequence)
    - .planning/phases/05-segment-aware-position-precision/05-RESEARCH.md (Architecture diagram Load Path — derive() after both caches load; hot path CPU-only no DB)
  </read_first>
  <action>
    In `app.py` lifespan: after `app.state.collection_snapshot = snapshot` is set, instantiate `SegmentCache()`,
    call `segment_cache.derive(cache, snapshot, cache.overrides)` (the overrides map loaded by BoundaryCache in Plan 01),
    and assign `app.state.segment_cache = segment_cache`. Guard with the same try/except logging style used for the other caches so a derive failure logs but does not crash boot (set an empty SegmentCache on failure).
    In `deps.py`: add `get_segment_cache(request)` mirroring `get_collection_snapshot` — read `request.app.state.segment_cache`, return HTTP 503 if missing.
    In `locate.py`: add `segment_cache: SegmentCache = Depends(get_segment_cache)` to `locate_endpoint`, and change the `locate(...)` call to pass `segment_cache=` (replacing the boundary `cache=` argument per the new dispatcher signature). The response dict already emits `result.estimator_version`; no contract field change.
    In `tests/integration/test_locate.py`: extend the covered-record test to assert `estimator_version == "segment-v1"` and that a no-snapshot record still returns `"cube-only-v1"`; ensure the `--benchmark-only` benchmark exercises the segment path and that the hot path performs NO DB query during estimation (the estimate reads only SegmentCache + CollectionSnapshot, both in memory).
    Confirm p95 ≤ 50 ms via the existing benchmark harness against the cached structures.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && pytest tests/integration/test_locate.py -x -q && pytest tests/integration/test_locate.py --benchmark-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `deps.py` contains `def get_segment_cache(`
    - `app.py` lifespan contains `SegmentCache(` and a call matching `\.derive\(`
    - `locate.py` contains `Depends(get_segment_cache)` and passes `segment_cache` to `locate(`
    - `pytest tests/integration/test_locate.py` exits 0 and includes an assertion `estimator_version == "segment-v1"`
    - `pytest tests/integration/test_locate.py --benchmark-only` reports the segment-path locate p95 ≤ 50 ms (SEG-08 budget)
    - The locate integration test confirms no DB query occurs during the estimate (assert via the existing no-DB test pattern or by not passing a pool to the estimate path)
  </acceptance_criteria>
  <done>/api/locate is routed through the segment-aware estimator with estimator_version "segment-v1", §4.8 fallback retained; SegmentCache derived in lifespan + exposed via deps; benchmark proves p95 ≤ 50 ms CPU-only on the hot path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/locate | Public read endpoint; untrusted `release_id` query param crosses here |
| SegmentCache (in-memory) → estimate | Derived data must never trigger DB access on the hot path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of Service | `/api/locate` hot path | mitigate | Estimate is CPU-only from in-memory SegmentCache + CollectionSnapshot; benchmark gate p95 ≤ 50 ms; no per-request DB so a slow DB cannot stall locate (SEG-08) |
| T-05-05 | Tampering | `release_id` query param | mitigate | Existing locate.py validates release_id via FastAPI typing + get_release_for_locate lookup; unknown id returns a no-boundary LocateResult, not an error leak |
| T-05-06 | Information Disclosure | LocateResult contract | accept | Contract is frozen and emits only position metadata; no PII surfaced (collection CSV stays out of repo per PROJECT hygiene) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH Package Legitimacy Audit: none). No install tasks; no checkpoint required |
</threat_model>

<verification>
- `pytest tests/unit/test_segment_cache.py tests/unit/test_segment_estimator.py tests/property/test_segment_props.py -x -q` exits 0 (SEG-02, SEG-03, SEG-06, SEG-07 unit + property).
- `pytest tests/property/test_segment_props.py::test_single_segment_bin_reproduces_v1_index -x` exits 0 (D-02 regression anchor / SEG-07).
- `pytest tests/integration/test_locate.py --benchmark-only` p95 ≤ 50 ms (SEG-08 budget preserved).
- `grep -v '^\s*#' src/gruvax/estimator/algorithm.py | grep -c last_label` returns 0 (Pitfall 1 resolved).
</verification>

<success_criteria>
- SegmentCache derives ordered per-label segments with row-counted counts (dupes + variants) and override-renormalized fractions summing to 1.0 (SEG-02, SEG-03, SEG-04 derivation half).
- locate_by_segment two-level interpolation: single-segment bin reproduces §4.1 exactly; straddle resolves by rank with no special-casing (SEG-06, SEG-07).
- /api/locate returns estimator_version "segment-v1"; §4.8 cube-only retained as fallback (SEG-07).
- Hot path CPU-only, no DB; p95 ≤ 50 ms (SEG-08 budget).
</success_criteria>

<output>
Create `.planning/phases/05-segment-aware-position-precision/05-02-SUMMARY.md` when done.
Record the final SegmentCache public API and the locate() dispatcher signature so Plan 03/04 wire against it.
</output>
