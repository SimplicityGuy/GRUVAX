---
phase: 05-segment-aware-position-precision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/0005_segment_model.py
  - src/gruvax/estimator/boundary_cache.py
  - src/gruvax/estimator/constants.py
  - fixtures/boundaries.yaml
  - fixtures/synth_collection.py
  - tests/conftest.py
  - tests/unit/test_segment_cache.py
  - tests/unit/test_segment_estimator.py
  - tests/property/test_segment_props.py
  - tests/integration/test_segment_api.py
  - tests/integration/test_migrate_0005.py
autonomous: true
requirements: [SEG-01]
must_haves:
  truths:
    - "Migration 0005 upgrade drops last_label/last_catalog from cube_boundaries and creates gruvax.segment_overrides"
    - "Migration 0005 round-trips clean: upgrade head -> downgrade base -> upgrade head exits 0"
    - "D-05: bin is 1:1 with a Kallax cube; cube_boundaries.first_* is the cut point, last_* is dropped/derived, durable identity stays (unit_id, row, col); the SEG-01 migration round-trips clean"
    - "BoundaryRow no longer has last_label/last_catalog fields; every construction site compiles"
    - "BoundaryCache.load() selects only cut-point columns and loads segment_overrides into a dict"
    - "Wave 0 test files exist and import cleanly so downstream plans can fill them"
  artifacts:
    - path: "migrations/versions/0005_segment_model.py"
      provides: "Cut-point migration: drop last_*, add segment_overrides, extend boundary_history source CHECK"
      contains: "segment_overrides"
    - path: "src/gruvax/estimator/boundary_cache.py"
      provides: "Refactored BoundaryRow (no last_*) + overrides map in BoundaryCache"
      contains: "_overrides"
    - path: "src/gruvax/estimator/constants.py"
      provides: "SEGMENT_ESTIMATOR_VERSION constant"
      contains: "SEGMENT_ESTIMATOR_VERSION"
    - path: "fixtures/synth_collection.py"
      provides: "make_multi_label_bin() + make_straddle() factories using cut-point BoundaryRow"
      contains: "make_multi_label_bin"
    - path: "tests/integration/test_migrate_0005.py"
      provides: "Alembic up/down round-trip test for SEG-01"
      contains: "def test_"
  key_links:
    - from: "src/gruvax/estimator/boundary_cache.py"
      to: "gruvax.segment_overrides"
      via: "BoundaryCache.load() second SELECT into _overrides"
      pattern: "segment_overrides"
    - from: "migrations/versions/0005_segment_model.py"
      to: "migrations/versions/0004_admin_tables.py"
      via: "down_revision = '0004'"
      pattern: "down_revision"
---

<objective>
Land the foundational data-model slice for the segment-aware boundary model: Migration 0005
transforms `gruvax.cube_boundaries` from the one-span-per-cube model to the cut-point model
(drops `last_label`/`last_catalog`), adds the `gruvax.segment_overrides` table, and extends the
`boundary_history.source` CHECK with `'cut_insert'`. Refactor the `BoundaryRow` dataclass and
`BoundaryCache.load()` to match, fix every `BoundaryRow(...)` construction site, update the seed
fixture, add the `SEGMENT_ESTIMATOR_VERSION` constant, and create all Wave 0 test stubs +
multi-label/straddle synth factories so downstream plans can implement against them.

Purpose: Every other Phase 5 plan depends on the new `BoundaryRow` shape and the
`segment_overrides` table. This is the foundational vertical slice (data model + cache + test
scaffold) that MVP_MODE sequencing places first — without it the segment estimator and admin
editor cannot compile.

Output: Migration 0005, refactored `boundary_cache.py`, extended `constants.py`, updated
`fixtures/boundaries.yaml` + `fixtures/synth_collection.py`, updated `tests/conftest.py`, and
five new Wave 0 test files (segment cache, segment estimator, segment props, segment API,
migration round-trip) — all importing cleanly with at least the SEG-01 migration test passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-segment-aware-position-precision/05-CONTEXT.md
@.planning/phases/05-segment-aware-position-precision/05-RESEARCH.md
@.planning/phases/05-segment-aware-position-precision/05-PATTERNS.md
@.planning/phases/05-segment-aware-position-precision/05-VALIDATION.md
@.planning/notes/segment-aware-boundaries.md

<interfaces>
<!-- Current BoundaryRow shape (boundary_cache.py lines 22-37) — DROP last_label/last_catalog -->
Current:
  BoundaryRow(unit_id, row, col, first_label, first_catalog, last_label, last_catalog, is_empty)
Phase 5 target:
  BoundaryRow(unit_id, row, col, first_label, first_catalog, is_empty)

<!-- Migration conventions confirmed: 0001-0004 use op.execute() with explicit constraint names; -->
<!-- alembic_version in public; search_path via connect listener in env.py (unchanged). -->
<!-- down_revision chains: 0005 -> 0004. boundary_history.source CHECK currently: -->
<!--   CHECK (source IN ('manual', 'bulk', 'revert')) — extend with 'cut_insert'. -->
<!-- cube_boundaries CHECK currently named 'empty_or_complete' — replace with 'cut_point_complete'. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0005 — cut-point model + segment_overrides + round-trip test</name>
  <files>migrations/versions/0005_segment_model.py, tests/integration/test_migrate_0005.py</files>
  <read_first>
    - migrations/versions/0004_admin_tables.py (analog — copy module conventions, op.execute() style, IF EXISTS guards, downgrade reverse order)
    - migrations/versions/0001_create_schema.py (cube_boundaries DDL + the exact `empty_or_complete` CHECK constraint name being replaced)
    - .planning/phases/05-segment-aware-position-precision/05-PATTERNS.md (lines 453-559 — Migration 0005 upgrade()/downgrade() reference)
    - .planning/phases/05-segment-aware-position-precision/05-RESEARCH.md (Pattern 3 — Migration 0005; Runtime State Inventory — boundary_history decision: keep last_* columns nullable)
    - tests/integration/test_locate.py (analog for integration test fixture/db_pool patterns, `@pytest.mark.asyncio(loop_scope="session")`)
  </read_first>
  <action>
    Create `migrations/versions/0005_segment_model.py` with `revision = "0005"`, `down_revision = "0004"`,
    `branch_labels = None`, `depends_on = None`, matching the module docstring/convention style of 0004.
    upgrade(): (1) `ALTER TABLE gruvax.cube_boundaries DROP COLUMN IF EXISTS last_label, DROP COLUMN IF EXISTS last_catalog`;
    (2) drop CHECK `empty_or_complete` and add CHECK `cut_point_complete` = `is_empty OR (first_label IS NOT NULL AND first_catalog IS NOT NULL)`;
    (3) drop `boundary_history_source_check` and re-add it as `CHECK (source IN ('manual', 'bulk', 'revert', 'cut_insert'))`
    (KEEP the existing prev_last_*/new_last_* columns on boundary_history as nullable historical artifact — do NOT drop them, per Runtime State Inventory A1);
    (4) `CREATE TABLE gruvax.segment_overrides` with columns unit_id SMALLINT NOT NULL REFERENCES gruvax.units(id) ON DELETE RESTRICT,
    row SMALLINT NOT NULL, col SMALLINT NOT NULL, label TEXT NOT NULL, fraction REAL NOT NULL CHECK (fraction > 0.0 AND fraction <= 1.0),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), PRIMARY KEY (unit_id, row, col, label),
    and FOREIGN KEY (unit_id, row, col) REFERENCES gruvax.cube_boundaries (unit_id, row, col) ON DELETE CASCADE.
    Use `op.execute()` with `%`-free literal SQL (DDL, no user params) and explicit constraint names exactly as listed.
    downgrade(): reverse order with IF EXISTS guards — DROP segment_overrides; restore boundary_history_source_check to the 3-value set;
    DROP cut_point_complete; restore empty_or_complete CHECK (the full first_*+last_* form); ADD COLUMN IF NOT EXISTS last_label TEXT and last_catalog TEXT (nullable).
    Create `tests/integration/test_migrate_0005.py` proving the round-trip: assert segment_overrides exists and the fraction CHECK rejects fraction>1.0;
    assert cube_boundaries has no last_label column post-upgrade; run `alembic upgrade head -> downgrade base -> upgrade head` programmatically (or via subprocess against the test DB) and assert exit 0 + final schema state (segment_overrides present, last_label absent). The fraction>1.0 rejection test is the SEG-01 + V5 security control proof.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && python -m alembic upgrade head && python -m alembic downgrade base && python -m alembic upgrade head && pytest tests/integration/test_migrate_0005.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `migrations/versions/0005_segment_model.py` contains `revision: str = "0005"` and `down_revision: str | None = "0004"`
    - `0005_segment_model.py` contains `CREATE TABLE gruvax.segment_overrides` and `CHECK (fraction > 0.0 AND fraction <= 1.0)`
    - `0005_segment_model.py` upgrade contains `DROP COLUMN IF EXISTS last_label` and `cut_point_complete`
    - `0005_segment_model.py` source CHECK contains `'cut_insert'`
    - `python -m alembic upgrade head && python -m alembic downgrade base && python -m alembic upgrade head` exits 0
    - `pytest tests/integration/test_migrate_0005.py -x` exits 0 and includes a test asserting an INSERT with fraction = 1.5 is rejected by the DB CHECK
  </acceptance_criteria>
  <done>Migration 0005 round-trips clean; segment_overrides exists with the fraction bound CHECK; last_* dropped from cube_boundaries; boundary_history.source accepts 'cut_insert'; round-trip integration test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor BoundaryRow + BoundaryCache; fix all construction sites; add estimator-version constant</name>
  <files>src/gruvax/estimator/boundary_cache.py, src/gruvax/estimator/constants.py, fixtures/synth_collection.py, fixtures/boundaries.yaml, tests/conftest.py</files>
  <read_first>
    - src/gruvax/estimator/boundary_cache.py (the file being refactored — current BoundaryRow lines 22-37, load() lines 57-73, _load_rows lines 75-81, invalidate lines 87-99)
    - src/gruvax/estimator/constants.py (the file being extended — add one constant)
    - fixtures/synth_collection.py (lines 31-54 _build_cache constructs BoundaryRow; lines 79-97 make_uniform_dense factory shape to mirror)
    - fixtures/boundaries.yaml (seed fixture — has last_label/last_catalog entries to remove)
    - tests/conftest.py (boundary fixture loader that constructs BoundaryRow from boundaries.yaml)
    - .planning/phases/05-segment-aware-position-precision/05-PATTERNS.md (Pattern 4 BoundaryCache refactor lines 73-155; synth_collection lines 728-767; Pitfall Summary P2)
    - .planning/phases/05-segment-aware-position-precision/05-RESEARCH.md (Pattern 4; Pitfall 4 — stale BoundaryRow references; Common Pitfalls)
  </read_first>
  <behavior>
    - BoundaryRow constructed without last_label/last_catalog raises no TypeError
    - BoundaryRow has no attribute last_label (AttributeError on access)
    - BoundaryCache._load_overrides({(1,0,0,"BLUE NOTE"): 0.45}) then accessor returns that dict
    - make_multi_label_bin() returns (BoundaryCache, SegmentCache-placeholder/None, CollectionSnapshot) with two labels in one cube
    - make_straddle() returns one label spanning two adjacent bins
    - constants.SEGMENT_ESTIMATOR_VERSION == "segment-v1"
  </behavior>
  <action>
    In `boundary_cache.py`: remove `last_label` and `last_catalog` from the frozen `BoundaryRow` dataclass
    (target shape: unit_id, row, col, first_label, first_catalog, is_empty). Change the `load()` SELECT to
    `SELECT unit_id, row, col, first_label, first_catalog, is_empty FROM gruvax.cube_boundaries ORDER BY unit_id, row, col`.
    Add a second SELECT in `load()` reading `SELECT unit_id, row, col, label, fraction FROM gruvax.segment_overrides`
    into `self._overrides: dict[tuple[int, int, int, str], float]` keyed `(int, int, int, str) -> float`.
    Initialize `self._overrides = {}` in `__init__`. Add `_load_overrides(self, overrides)` test seam mirroring `_load_rows`.
    Add an `overrides` read accessor (property or method) returning `self._overrides`. Extend `invalidate()` to also clear `self._overrides = {}`.
    In `constants.py`: add `SEGMENT_ESTIMATOR_VERSION: str = "segment-v1"`.
    In `fixtures/synth_collection.py`: drop `last_label`/`last_catalog` from the `_build_cache()` BoundaryRow construction,
    and add `make_multi_label_bin()` and `make_straddle()` factories (mirror `make_uniform_dense()` structure) returning
    `(BoundaryCache, SegmentCache, CollectionSnapshot)` — the SegmentCache may be left empty/None at this stage (Plan 02 fills derivation);
    the key requirement is realistic per-label record lists in the snapshot (include a duplicate owned copy and a `-r` variant so SEG-03 row-count tests have data) and cut-point-only BoundaryRows.
    In `fixtures/boundaries.yaml`: remove all `last_label`/`last_catalog` keys (keep first_label/first_catalog/is_empty).
    In `tests/conftest.py`: update the boundary fixture loader to construct `BoundaryRow` without last_* and to ignore any stale last_* keys.
    Grep for every `BoundaryRow(` construction site and fix each — confirmed sites: `fixtures/synth_collection.py`, `tests/conftest.py`; `api/admin/cubes.py` `_compute_movement_counts()` and `algorithm.py`/`boundary_math.py` are owned by Plan 02/03 and out of scope for this task except as a grep audit (note any others in the SUMMARY for Plan 02).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && python -c "from gruvax.estimator.boundary_cache import BoundaryRow; from gruvax.estimator.constants import SEGMENT_ESTIMATOR_VERSION; r=BoundaryRow(1,0,0,'A','001',False); assert not hasattr(r,'last_label'); assert SEGMENT_ESTIMATOR_VERSION=='segment-v1'; from fixtures.synth_collection import make_multi_label_bin, make_straddle; make_multi_label_bin(); make_straddle(); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `boundary_cache.py` BoundaryRow has no `last_label`/`last_catalog` (grep `last_label` in file with comments stripped returns 0: `grep -v '^\s*#' src/gruvax/estimator/boundary_cache.py | grep -c last_label` == 0)
    - `boundary_cache.py` contains `_overrides` and a `_load_overrides` method
    - `constants.py` contains `SEGMENT_ESTIMATOR_VERSION: str = "segment-v1"`
    - `fixtures/synth_collection.py` contains `def make_multi_label_bin(` and `def make_straddle(`
    - `grep -c last_label fixtures/boundaries.yaml` == 0
    - The python import-and-construct one-liner in verify prints `ok`
  </acceptance_criteria>
  <done>BoundaryRow is cut-point-only; BoundaryCache loads + holds the overrides map with a test seam; estimator-version constant exists; seed fixture + synth factories + conftest construct cut-point BoundaryRows; multi-label + straddle factories exist.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test stubs (segment cache, estimator, props, API)</name>
  <files>tests/unit/test_segment_cache.py, tests/unit/test_segment_estimator.py, tests/property/test_segment_props.py, tests/integration/test_segment_api.py</files>
  <read_first>
    - tests/property/test_estimator_props.py (analog — session-scoped fixtures lines 26-42; @given/@settings invariant pattern lines 173-193; no-DB seam usage)
    - tests/integration/test_segment_api.py reference does not exist yet — use tests/integration/test_cubes_bulk.py and tests/integration/test_boundary_editor.py as the admin-API integration analog
    - fixtures/synth_collection.py (the make_multi_label_bin / make_straddle factories added in Task 2)
    - .planning/phases/05-segment-aware-position-precision/05-VALIDATION.md (Per-Requirement Verification Map — exact test names per requirement; Extended Hypothesis Invariants table)
    - .planning/phases/05-segment-aware-position-precision/05-PATTERNS.md (Tests section lines 1066-1120 — D-02 regression invariant pattern with Pitfall 5 pre-check)
    - .planning/phases/05-segment-aware-position-precision/05-RESEARCH.md (Phase Requirements -> Test Map; Extended Hypothesis Invariants code examples lines 712-739)
  </read_first>
  <action>
    Create four test files as Wave 0 scaffolds. Each test referenced by name in 05-VALIDATION.md MUST exist
    (defined, importable, collected by pytest) but MAY be marked `@pytest.mark.skip(reason="Wave 0 stub — implemented in Plan 02/03")`
    until its production code lands; do NOT mark them xfail and do NOT leave bodies that error on collection.
    `tests/unit/test_segment_cache.py`: define `test_row_count_not_arithmetic`, `test_override_applied`, `test_contiguity_validation`,
    plus a derivation correctness test and a multi-override renormalization test (Pitfall 2). Use session-scoped fixtures wrapping make_multi_label_bin / make_straddle.
    `tests/unit/test_segment_estimator.py`: define unit tests for `locate_by_segment` two-level interpolation and the §4.8 fallback coverage path (SEG-06 unit).
    `tests/property/test_segment_props.py`: define every invariant from the Extended Hypothesis Invariants table —
    `test_per_bin_fractions_sum_to_one`, `test_single_segment_bin_reproduces_v1_index`, `test_straddle_resolves_to_correct_bin`,
    `test_primary_cube_in_label_span`, `test_sub_cube_interval_bounds`, `test_monotone_position_within_label`, `test_cosmetic_stability`.
    For `test_single_segment_bin_reproduces_v1_index`, include the Pitfall 5 pre-check comment block (assert len(bin.segments)==1, auto_fraction==1.0, first_rank==0 BEFORE the estimator equality assertion) so the implementer cannot write a vacuous test.
    `tests/integration/test_segment_api.py`: define API integration test names for the new endpoints (get segments, set override, edit cut, insert cut) and a require_admin 401/403 test — all skip-stubbed.
    Each stub body should contain a one-line docstring naming the requirement it covers and a `pytest.skip(...)` call (or skip marker) so the test name is collectible for the Nyquist per-requirement verify commands.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX && pytest tests/unit/test_segment_cache.py tests/unit/test_segment_estimator.py tests/property/test_segment_props.py tests/integration/test_segment_api.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` lists `tests/property/test_segment_props.py::test_single_segment_bin_reproduces_v1_index`
    - `pytest --collect-only` lists `tests/property/test_segment_props.py::test_per_bin_fractions_sum_to_one` and `test_straddle_resolves_to_correct_bin`
    - `pytest --collect-only` lists `tests/unit/test_segment_cache.py::test_row_count_not_arithmetic`, `::test_override_applied`, `::test_contiguity_validation`
    - All four files import without ImportError and collect 0 errors
    - `test_single_segment_bin_reproduces_v1_index` body contains the Pitfall 5 pre-check comment referencing `len(bin.segments) == 1`
  </acceptance_criteria>
  <done>All Wave 0 test files exist, import cleanly, and every test name referenced by 05-VALIDATION.md is collectible (skip-stubbed where production code is not yet present), so downstream plans satisfy Nyquist Dimension 8 by filling them in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → Postgres DDL | Schema-shaping SQL; must not introduce an unconstrained writable surface |
| segment_overrides INSERT/UPDATE → DB | fraction value must be bounded at the storage layer regardless of caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `segment_overrides.fraction` | mitigate | DB CHECK `fraction > 0.0 AND fraction <= 1.0` created in Migration 0005; test_migrate_0005 asserts a fraction=1.5 INSERT is rejected (V5 input validation at storage) |
| T-05-02 | Tampering | `segment_overrides` FK | mitigate | FOREIGN KEY (unit_id,row,col) REFERENCES cube_boundaries ON DELETE CASCADE prevents orphan override rows pointing at non-existent bins |
| T-05-03 | Repudiation | `boundary_history` audit trail | accept | Keep prev_last_*/new_last_* columns as nullable historical artifact (Runtime State Inventory A1); no audit data destroyed by the migration |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH Package Legitimacy Audit: none). No install tasks; no checkpoint required |
</threat_model>

<verification>
- `python -m alembic upgrade head && python -m alembic downgrade base && python -m alembic upgrade head` exits 0 (SEG-01 round-trip, OBS-03 convention).
- `pytest tests/integration/test_migrate_0005.py -x -q` exits 0 including the fraction-bound rejection test.
- `pytest tests/ --collect-only -q` reports 0 collection errors (every callsite + Wave 0 stub imports cleanly).
- `grep -v '^\s*#' src/gruvax/estimator/boundary_cache.py | grep -c last_label` returns 0.
</verification>

<success_criteria>
- Migration 0005 exists, round-trips clean, creates segment_overrides with the fraction CHECK and FK, and extends boundary_history.source with 'cut_insert' (SEG-01).
- BoundaryRow is cut-point-only and BoundaryCache loads + holds segment_overrides via a test seam.
- SEGMENT_ESTIMATOR_VERSION = "segment-v1" exists.
- Seed fixture + synth factories + conftest construct cut-point BoundaryRows; multi-label + straddle factories exist.
- All Wave 0 test files exist, import cleanly, and every 05-VALIDATION.md test name is collectible.
</success_criteria>

<output>
Create `.planning/phases/05-segment-aware-position-precision/05-01-SUMMARY.md` when done.
Record in the SUMMARY any additional `BoundaryRow(...)` construction sites found via grep that Plan 02/03 must fix.
</output>
