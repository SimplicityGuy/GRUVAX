---
phase: 05-segment-aware-position-precision
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - frontend/src/routes/admin/CutPointEditor.tsx
  - frontend/src/routes/admin/SegmentEditorPanel.tsx
  - frontend/src/routes/admin/LocatorHeader.tsx
  - frontend/src/routes/admin/DiffPreviewSheet.tsx
  - frontend/src/routes/admin/Settings.tsx
  - frontend/src/App.tsx
  - frontend/src/routes/admin/admin.css
autonomous: false
requirements: [SEG-04, SEG-05, SEG-06, SEG-08]
must_haves:
  truths:
    - "Admin can open the cut-point editor and see a bin-card list with mini segment strips + insert-cut dividers"
    - "Admin can edit a bin's cut point and add a new cut via the slide-up record picker"
    - "Admin can drag width overrides in the full segment editor panel and stage them for preview"
    - "DiffPreviewSheet renders the new change types: cut-point, insert/new-bin, override set, orphaned-override removed"
    - "A contiguity violation blocks PREVIEW CHANGES with a plain-language error"
    - "The kiosk SubCubeBar reflects the improved two-level interpolation accuracy (no UI change, backend-driven)"
    - "The /admin/cubes/:unit/:row/:col route renders CutPointEditor (replaces the Phase 3 CubeEditor)"
  artifacts:
    - path: "frontend/src/routes/admin/CutPointEditor.tsx"
      provides: "Bin-card list + insert-cut dividers + NEW badge + renumber hint; replaces CubeEditor"
      contains: "CutPointEditor"
    - path: "frontend/src/routes/admin/SegmentEditorPanel.tsx"
      provides: "Full 88px SegmentStrip + SegmentLegend + PREVIEW CHANGES per bin"
      contains: "PREVIEW CHANGES"
    - path: "frontend/src/routes/admin/LocatorHeader.tsx"
      provides: "Compact 4x4 mini-Kallax, edited bin lit yellow"
      contains: "LocatorHeader"
    - path: "frontend/src/App.tsx"
      provides: "Route swap: cut-point editor replaces CubeEditor at the cubes detail route"
      contains: "CutPointEditor"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "CutPointEditor"
      via: "Route element for /admin/cubes/:unit/:row/:col"
      pattern: "CutPointEditor"
    - from: "frontend/src/routes/admin/SegmentEditorPanel.tsx"
      to: "SegmentStrip + SegmentLegend"
      via: "panel composition"
      pattern: "SegmentStrip"
    - from: "frontend/src/routes/admin/DiffPreviewSheet.tsx"
      to: "cut-point / insert / override / orphan change types"
      via: "new diff-row renderers"
      pattern: "OVERRIDE REMOVED"
---

<objective>
Assemble the primitives from Plan 04 into the full admin cut-point + width-override editor and wire
it in. Build `CutPointEditor` (bin-card list with mini strips, insert-cut dividers, NEW badge,
renumber hint), `SegmentEditorPanel` (full 88px strip + legend + PREVIEW CHANGES), `LocatorHeader`
(compact 4×4 mini-Kallax with the edited bin lit yellow), extend `DiffPreviewSheet` with the new
change types (cut-point, insert/new-bin, override set, orphaned-override removed) and the contiguity
hard-block, add the Settings "Segment Overrides" drift-threshold section, and swap the route so
`/admin/cubes/:unit/:row/:col` renders `CutPointEditor` instead of the Phase 3 `CubeEditor`.

Purpose: This is the user-facing completion of the phase (SEG-04, SEG-05, SEG-08). After this plan
the owner can view/edit/add cut points and set width overrides with diff-preview + change-set undo
covering the new mutations — ROADMAP success criterion 5. The drag UX feel and drift highlight are
the one manual-only verification in 05-VALIDATION.md, so a human-verify checkpoint closes the phase.

Output: four assembled components, extended DiffPreviewSheet + Settings, the route swap, and a
human-verify checkpoint confirming the end-to-end edit → preview → commit → undo flow on a device.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-segment-aware-position-precision/05-CONTEXT.md
@.planning/phases/05-segment-aware-position-precision/05-UI-SPEC.md
@.planning/phases/05-segment-aware-position-precision/05-PATTERNS.md
@.planning/phases/05-segment-aware-position-precision/05-VALIDATION.md
@.planning/phases/05-segment-aware-position-precision/05-04-SUMMARY.md
@.claude/skills/sketch-findings-gruvax/references/boundary-editing.md
@design/gruvax-design-tokens.css

<interfaces>
<!-- Plan 04 component prop signatures (from 05-04-SUMMARY): -->
<!-- SegmentStrip({ segments, onDragSetOverride?, isReadOnly? }) -->
<!-- SegmentLegend({ segments, onResync }) — DRIFT_THRESHOLD = 0.03 internal -->
<!-- RecordPickerSheet({ mode: 'edit'|'insert', binNumber, onCommit, onCancel }) -->
<!-- adminClient: getUnitSegments, setSegmentOverrides, putCutPoint, insertCut, validateBoundary -->

<!-- CutPointEditor analog (PATTERNS lines 773-873): CubeEditor.tsx — route params lines 155-165, -->
<!--   useQuery lines 170-178, editing heartbeat lines 308-329, setPendingChangeSet merge lines 357-399, -->
<!--   phantom warning lines 469-509, PREVIEW CHANGES CTA lines 613-624. -->
<!-- Route to swap (App.tsx line 40): /admin/cubes/:unit/:row/:col currently -> CubeEditor; -> CutPointEditor. -->
<!--   UI-SPEC names the segments sub-route /admin/cubes/:unit/:row/:col/segments (SegmentEditorPanel inline or sub-route). -->
<!-- DiffPreviewSheet (PATTERNS lines 1039-1062): new change-type <tr> renderers; orphan warning row. -->
<!-- LocatorHeader: reuse kiosk ShelfGrid/Cube at --gruvax-cell-size-sm 28px; edited bin --gruvax-cell-lit + shadow-led. -->
<!-- Settings (UI-SPEC section H): drift threshold integer field 1-20, default 3; REVIEW OVERRIDES button. -->
<!-- Contiguity hard-block (UI-SPEC contiguity state machine): PREVIEW CHANGES disabled on contiguity_violation; -->
<!--   error copy "This cut would split {LABEL} across non-adjacent bins. Reposition the cut or remove the existing boundary." -->
<!-- Kiosk SubCubeBar: UNCHANGED component contract — accuracy improves from the backend estimator only. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LocatorHeader + SegmentEditorPanel (full strip + legend + PREVIEW CHANGES)</name>
  <files>frontend/src/routes/admin/LocatorHeader.tsx, frontend/src/routes/admin/SegmentEditorPanel.tsx, frontend/src/routes/admin/admin.css</files>
  <read_first>
    - frontend/src/routes/kiosk/ShelfGrid.tsx + frontend/src/routes/kiosk/Cube.tsx (reuse at compact 28px scale for the LocatorHeader mini-Kallax)
    - frontend/src/routes/kiosk/gridGeometry.ts (cube coordinate -> grid position helper)
    - frontend/src/routes/admin/SegmentStrip.tsx + SegmentLegend.tsx (Plan 04 components composed by the panel)
    - frontend/src/routes/admin/CubeEditor.tsx (PREVIEW CHANGES CTA lines 613-624; editing heartbeat lines 308-329; setPendingChangeSet merge lines 357-399)
    - frontend/src/state/adminStore.ts (pendingChangeSet store used to stage edits)
    - .planning/phases/05-segment-aware-position-precision/05-UI-SPEC.md (section A Locator Header; section E SegmentEditorPanel — heading, straddle multi-cut chain, PREVIEW CHANGES, no-change state)
    - .planning/phases/05-segment-aware-position-precision/05-PATTERNS.md (SegmentEditorPanel Pattern lines 1013-1037)
  </read_first>
  <action>
    Create `frontend/src/routes/admin/LocatorHeader.tsx`: a compact 4x4 mini-Kallax reusing the kiosk ShelfGrid/Cube at --gruvax-cell-size-sm 28px;
    the edited bin (by unit/row/col) renders lit yellow (--gruvax-cell-lit + --gruvax-shadow-led), all others dim (--gruvax-cell-dim); non-interactive.
    Show "SHELF A / BIN n" labels (Barlow Condensed 700 16px ALL CAPS, --gruvax-blue) where BIN n is display order.
    Create `frontend/src/routes/admin/SegmentEditorPanel.tsx`: heading "BIN {n} SEGMENTS" (Barlow 900 36px), the LocatorHeader, the full 88px draggable `SegmentStrip`,
    the `SegmentLegend`, and a "PREVIEW CHANGES" primary CTA (disabled until a change is staged; "No changes made yet." otherwise).
    Wire the strip's `onDragSetOverride` to update local segment fractions and stage an override edit into the pendingChangeSet (adminStore) — call setSegmentOverrides on commit-to-pending, mirroring the CubeEditor setPendingChangeSet merge logic.
    Wire the legend's `onResync` to set the override fraction to the current auto fraction and re-stage. Render the straddle multi-cut chain (N-1 `↪` captions) for labels spanning multiple bins.
    Reuse the editing heartbeat pattern (signal editing on dirty, clear on unmount). Add panel + locator CSS to `admin.css` (tokens only).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX/frontend && npx tsc --noEmit && grep -q "LocatorHeader" src/routes/admin/LocatorHeader.tsx && grep -q "PREVIEW CHANGES" src/routes/admin/SegmentEditorPanel.tsx && grep -q "SegmentStrip" src/routes/admin/SegmentEditorPanel.tsx && ! grep -qE "#[0-9A-Fa-f]{6}" src/routes/admin/LocatorHeader.tsx src/routes/admin/SegmentEditorPanel.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `LocatorHeader.tsx` reuses the kiosk grid and lights the edited bin with `--gruvax-cell-lit` (grep present)
    - `SegmentEditorPanel.tsx` composes `SegmentStrip` and `SegmentLegend` and renders the `PREVIEW CHANGES` CTA
    - The PREVIEW CHANGES CTA is disabled until a change is staged (source review of the `disabled` prop)
    - Neither file contains a hardcoded hex color (grep -E `#[0-9A-Fa-f]{6}` returns 0)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>LocatorHeader shows the lit edited bin; SegmentEditorPanel composes the full draggable strip + legend + gated PREVIEW CHANGES, staging overrides/resyncs into the pending change-set with the editing heartbeat.</done>
</task>

<task type="auto">
  <name>Task 2: CutPointEditor (bin-card list + insert-cut) + DiffPreviewSheet/Settings extensions + route swap</name>
  <files>frontend/src/routes/admin/CutPointEditor.tsx, frontend/src/routes/admin/DiffPreviewSheet.tsx, frontend/src/routes/admin/Settings.tsx, frontend/src/App.tsx, frontend/src/routes/admin/admin.css</files>
  <read_first>
    - frontend/src/routes/admin/CubeEditor.tsx (the component being replaced — copy route params, useQuery, heartbeat, setPendingChangeSet merge, phantom warning, navigation patterns)
    - frontend/src/routes/admin/SegmentEditorPanel.tsx + RecordPickerSheet.tsx (composed by CutPointEditor)
    - frontend/src/routes/admin/DiffPreviewSheet.tsx (the file being extended — existing diff-cube-card BEFORE/AFTER <tr> lines 298-367; add cut-point/insert/override/orphan renderers)
    - frontend/src/routes/admin/Settings.tsx (the file being extended — add Segment Overrides section)
    - frontend/src/App.tsx (the route table — line 40 swaps CubeEditor -> CutPointEditor; keep /preview and /history)
    - .planning/phases/05-segment-aware-position-precision/05-UI-SPEC.md (section D CutPointEditor bin-card + insert-cut divider + renumber hint; section G DiffPreviewSheet extensions exact copy; section H Settings; interaction state machines: insert cut, edit cut, contiguity validation, override drift)
    - .planning/phases/05-segment-aware-position-precision/05-PATTERNS.md (CutPointEditor Pattern lines 773-873; DiffPreviewSheet Pattern lines 1039-1062)
    - frontend/src/api/adminClient.ts (getUnitSegments, putCutPoint, insertCut, validateBoundary)
  </read_first>
  <action>
    Create `frontend/src/routes/admin/CutPointEditor.tsx` (replaces CubeEditor at /admin/cubes/:unit/:row/:col): a vertical list of bin cards, one per cube,
    each showing the bin-number chip (40x40 --gruvax-blue), the "STARTS AT {label} · {catalog#}" cut point, a read-only mini `SegmentStrip`, and an "EDIT SEGMENTS" action that opens the `SegmentEditorPanel` (inline expand or the /segments sub-route).
    Render insert-cut dividers between cards (44px tap target, dashed, "+ insert cut"); tapping opens `RecordPickerSheet` in insert mode; on commit, create a NEW bin card with the "NEW" badge and show the renumber hint banner (display-only; the real renumber happens at diff-preview commit per D-06).
    Tapping the "STARTS AT" row opens `RecordPickerSheet` in edit mode (putCutPoint on stage). Reuse the editing heartbeat + setPendingChangeSet merge from CubeEditor.
    Before PREVIEW CHANGES navigates to /preview, call validateBoundary (dry-run); on a contiguity_violation response, BLOCK with the error banner copy "This cut would split {LABEL} across non-adjacent bins. Reposition the cut or remove the existing boundary." and keep PREVIEW CHANGES disabled until resolved.
    Extend `DiffPreviewSheet.tsx` with the four new change-type renderers (copy the existing BEFORE/AFTER <tr> structure): cut-point change ("BEFORE/AFTER label · catalog#"), insert/new-bin ("NEW: BIN {n} created · starts at ..."), override set ("OVERRIDE SET: LABEL {name} in BIN {n} -> {N}% (was auto {M}%)"), and orphaned-override removed ("OVERRIDE REMOVED: LABEL {name} in BIN {n} — label no longer in this bin" with --gruvax-warning tint, D-04). Keep COMMIT CHANGE SET / BACK TO EDITOR.
    Extend `Settings.tsx` with a "SEGMENT OVERRIDES" section: a drift-threshold integer field (1-20, default 3, DM Mono) labeled "DRIFT ALERT THRESHOLD (% POINTS)" + hint, and a "REVIEW OVERRIDES" secondary button.
    Swap the route in `App.tsx`: replace the `CubeEditor` element at `/admin/cubes/:unit/:row/:col` with `CutPointEditor` (import CutPointEditor, remove the CubeEditor import if now unused). Add the bin-card / divider / NEW-badge / renumber-hint CSS to `admin.css` (tokens only).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/GRUVAX/frontend && npx tsc --noEmit && grep -q "CutPointEditor" src/App.tsx && grep -q "OVERRIDE REMOVED" src/routes/admin/DiffPreviewSheet.tsx && grep -q "SEGMENT OVERRIDES\|DRIFT ALERT" src/routes/admin/Settings.tsx && ! grep -qE "#[0-9A-Fa-f]{6}" src/routes/admin/CutPointEditor.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `App.tsx` route `/admin/cubes/:unit/:row/:col` renders `CutPointEditor` (not `CubeEditor`)
    - `CutPointEditor.tsx` renders bin cards with a read-only mini `SegmentStrip` and insert-cut dividers, and opens `RecordPickerSheet` in both edit and insert modes
    - `CutPointEditor.tsx` blocks PREVIEW CHANGES on a `contiguity_violation` validate response with the exact UI-SPEC error copy
    - `DiffPreviewSheet.tsx` contains the `OVERRIDE REMOVED` renderer and the cut-point/insert/override-set renderers
    - `Settings.tsx` contains the `SEGMENT OVERRIDES` section with a default-3 drift-threshold field
    - `CutPointEditor.tsx` contains no hardcoded hex (grep -E `#[0-9A-Fa-f]{6}` returns 0); `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>CutPointEditor replaces CubeEditor with a bin-card list + insert-cut + edit-cut flows and contiguity hard-block; DiffPreviewSheet renders all new change types incl. orphaned-override; Settings has the drift section; the route is swapped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the segment editor end-to-end (drag UX + drift + contiguity + undo)</name>
  <what-built>
    The full segment-aware admin editor: cut-point bin-card list, full-size draggable segment strip with
    sum-conserving width overrides, AUTO/OVERRIDE chips with the 3pp drift review highlight + resync,
    slide-up record picker for editing/inserting cuts, diff-preview of cut/insert/override/orphan changes,
    contiguity hard-block, and the change-set undo path. The kiosk SubCubeBar now reflects two-level
    interpolation accuracy with no visual change. This is the one manual-only verification in 05-VALIDATION.md
    (drag feel + visual drift cue are tactile/visual, not unit-assertable).
  </what-built>
  <how-to-verify>
    1. Start the stack (e.g. `docker compose up` on the dev box) and open `/admin` on a phone or the kiosk; sign in with the PIN.
    2. Open the cut-point editor for a multi-label bin (`/admin/cubes/:unit/:row/:col`). Confirm the bin-card list shows mini segment strips and insert-cut dividers, and the LocatorHeader lights the edited bin yellow.
    3. Tap EDIT SEGMENTS on a multi-label bin. In the full strip, drag a handle: confirm only the adjacent pair resizes, the sum stays 100%, neither segment goes below 5%, and the active handle turns yellow.
    4. Confirm the legend shows the OVERRIDE chip; verify a drift "review" highlight appears when the override deviates more than 3 percentage points from auto, and "reset to {N}%" resyncs it.
    5. Tap a STARTS AT row -> record picker (edit mode); confirm two-step label->catalog autocomplete, phantom-block on a non-existent record, and visible duplicate/variant rows. Set a cut and confirm the change stages.
    6. Tap an insert-cut divider -> record picker (insert mode); insert a cut and confirm the NEW badge + renumber hint appear.
    7. Try a cut that would scatter a label across non-adjacent bins; confirm PREVIEW CHANGES is blocked with the plain-language contiguity error.
    8. PREVIEW CHANGES -> confirm the diff-preview shows cut-point/insert/override rows (and an "OVERRIDE REMOVED" row if you orphaned one). COMMIT CHANGE SET.
    9. Open the kiosk, search a record in the edited bin; confirm the sub-cube bar position looks correct for a multi-label bin.
    10. Go to History -> REVERT the change set; confirm the editor and kiosk return to the prior state.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. drag jitter, drift threshold wrong, contiguity not blocking, undo incomplete)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /api/admin/cubes/* | Editor commits cross to PIN-gated endpoints; validate dry-run gates every commit |
| derived label/catalog strings → DOM | Diff-preview + bin-card + picker render untrusted-shaped strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Tampering | CutPointEditor commit path | mitigate | Every commit gated by validateBoundary dry-run (contiguity + phantom + overflow) before the atomic bulk POST; PREVIEW CHANGES disabled on contiguity_violation |
| T-05-17 | Tampering | DiffPreviewSheet / bin-card rendering | mitigate | Strings rendered via React text children (auto-escaped) or el()/createTextNode; no raw-HTML React escape hatch |
| T-05-18 | Repudiation | change-set commit | mitigate | Reuse Phase 3 boundary_history change-set + Idempotency-Key + undo path (D-07); every multi-cube mutation is one auditable, revertible change-set |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH Package Legitimacy Audit: none). No install tasks; no checkpoint required |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 (full editor typechecks).
- `App.tsx` route renders `CutPointEditor` at the cubes detail route (CubeEditor retired from routing).
- `DiffPreviewSheet.tsx` renders all four new change types incl. `OVERRIDE REMOVED` (SEG-08, D-04, D-06).
- Human-verify checkpoint approved: drag feel, 3pp drift highlight, contiguity hard-block, and change-set undo all confirmed on a device (the manual-only verification from 05-VALIDATION.md).
- `cd frontend && npm run lint` (if present) exits 0.
</verification>

<success_criteria>
- Admin can view/edit/add cut points and set per-label width overrides via the new editor; diff-preview + change-set undo cover the new mutations (SEG-04, SEG-05, SEG-08 — ROADMAP criterion 5).
- Contiguity violations hard-block PREVIEW CHANGES with a plain-language error (SEG-05).
- DiffPreviewSheet renders cut-point/insert/override-set/orphaned-override change types (D-04, D-06).
- The kiosk SubCubeBar reflects two-level interpolation accuracy with no UI change (SEG-06).
- CutPointEditor replaces CubeEditor at the cubes detail route.
</success_criteria>

<output>
Create `.planning/phases/05-segment-aware-position-precision/05-05-SUMMARY.md` when done.
Record the human-verify outcome and any deviations from the UI-SPEC discovered during assembly.
</output>
