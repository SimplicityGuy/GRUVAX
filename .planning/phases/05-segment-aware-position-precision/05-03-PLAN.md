---
phase: 05-segment-aware-position-precision
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/gruvax/estimator/algorithm.py
  - src/gruvax/estimator/boundary_math.py
  - src/gruvax/api/deps.py
  - src/gruvax/app.py
  - src/gruvax/api/locate.py
  - src/gruvax/api/units.py
  - src/gruvax/db/seed_boundaries.py
  - scripts/run_all_algorithms.py
  - tests/unit/test_segment_estimator.py
  - tests/property/test_segment_props.py
  - tests/unit/test_algorithm.py
  - tests/unit/test_fill_level.py
  - tests/property/test_fill_level_property.py
  - tests/integration/test_run_all_algorithms.py
autonomous: true
requirements: [SEG-06, SEG-07]

must_haves:
  truths:
    - "/api/locate returns a sub-cube interval from two-level interpolation behind the unchanged LocateResult contract"
    - "A straddling label resolves to the correct bin by row-rank with no special-casing (D-08)"
    - "A single-segment bin reproduces §4.1 exactly (the regression invariant that replaces the dropped A/B gate, D-02)"
    - "estimator_version is 'segment-v1' for the segment path; §4.8 cube-only stays the fallback ('cube-only-v1')"
    - "Every read-path consumer of last_label/last_catalog (algorithm, boundary_math, units.py, seed_boundaries, run_all_algorithms) compiles and the suite is green"
  artifacts:
    - path: "src/gruvax/estimator/algorithm.py"
      provides: "locate_by_segment() two-level interpolation; locate() dispatcher segment-aware; §4.8 fallback rewired via SegmentCache"
      contains: "def locate_by_segment"
    - path: "src/gruvax/estimator/boundary_math.py"
      provides: "count_records_in_bin(SegmentBin); get_records_in_boundary retired or segment-backed"
      contains: "SegmentBin"
    - path: "src/gruvax/api/units.py"
      provides: "GET /api/cubes + /cubes/{u}/{r}/{c} fill/sample computed via SegmentCache (no last_* construction, no last_* SELECT)"
      contains: "segment_cache"
    - path: "src/gruvax/db/seed_boundaries.py"
      provides: "INSERT/UPSERT writes only cut-point columns (no last_label/last_catalog)"
      contains: "first_catalog"
  key_links:
    - from: "src/gruvax/api/locate.py"
      to: "src/gruvax/estimator/algorithm.py"
      via: "locate(..., segment_cache=...) call with the app-state SegmentCache"
      pattern: "segment_cache"
    - from: "src/gruvax/app.py"
      to: "src/gruvax/estimator/segment_cache.py"
      via: "lifespan constructs SegmentCache and derive()s after boundary cache + snapshot load"
      pattern: "SegmentCache\\(\\)"
    - from: "src/gruvax/api/deps.py"
      to: "app.state.segment_cache"
      via: "get_segment_cache() dependency provider"
      pattern: "get_segment_cache"
---

<objective>
Replace §4.1 with the segment-aware two-level-interpolation estimator and wire the SegmentCache
through the read hot path, then heal every read-path orphan of the dropped `last_*` fields so the
suite goes green.

Backend: implement `locate_by_segment()` in `algorithm.py` (retire `locate_by_index`), rewire the
§4.8 `locate_cube_only` coverage check to use SegmentCache, update the `locate()` dispatcher to
`segment-v1`; replace `boundary_math.count_records_in_boundary` with `count_records_in_bin(SegmentBin)`
and retire `get_records_in_boundary`; add `get_segment_cache` to `deps.py`; construct + `derive()`
the SegmentCache in the `app.py` lifespan; pass it through `locate.py`. Heal the orphans:
`units.py` (drop last_* BoundaryRow construction + last_* SELECTs + switch fill/sample to SegmentCache),
`seed_boundaries.py` (drop last_* from INSERT/UPSERT), `scripts/run_all_algorithms.py`
(fix `locate` calls + `BoundaryRow(last_*=...)` construction — NOT extended with the segment estimator
per D-01, just made to compile and pass its CI test).

Tests: fill the SEG-06/07 segment-estimator + property stubs (D-02 regression invariant with the
Pitfall-5 pre-check); rewrite the §4.1-era semantic tests (`test_algorithm.py`,
`test_fill_level_property.py`, `test_run_all_algorithms.py`) for the segment model; mechanically heal
`test_fill_level.py`.

Purpose: this is the cohesive unit that makes the read path segment-aware and the suite green.
Output: a working `/api/locate` on `segment-v1` + green estimator/fill-level/harness tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-segment-aware-position-precision/05-CONTEXT.md
@.planning/phases/05-segment-aware-position-precision/05-RESEARCH.md
@.planning/phases/05-segment-aware-position-precision/05-PATTERNS.md
@.planning/phases/05-segment-aware-position-precision/05-VALIDATION.md
@.planning/phases/05-segment-aware-position-precision/05-REPLAN-NOTES.md
@.planning/phases/05-segment-aware-position-precision/05-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase. -->

From src/gruvax/estimator/segment_cache.py (built in 05-02 — read it fully first):
```python
@dataclass(frozen=True)
class LabelSegment:
    label: str; first_rank_in_label: int; last_rank_in_label: int
    segment_count: int; auto_fraction: float; applied_fraction: float
    offset_in_bin: float; is_override: bool; continues: bool
@dataclass(frozen=True)
class SegmentBin:
    unit_id: int; row: int; col: int
    cut_label: str | None; cut_catalog: str | None
    segments: tuple[LabelSegment, ...]
class SegmentCache:
    def derive(self, cache, snapshot, overrides) -> None: ...
    def get_bin(self, unit_id, row, col) -> SegmentBin | None: ...
    def get_bins_for_label(self, label) -> list[SegmentBin]: ...
    def get_segment_for_rank(self, label, rank) -> tuple[SegmentBin, LabelSegment] | None: ...
    def invalidate(self) -> None: ...
```

From src/gruvax/estimator/contract.py (FROZEN — do not change shapes; SEG-06):
```python
@dataclass(frozen=True)
class CubeRef: unit_id: int; row: int; col: int
@dataclass(frozen=True)
class SubInterval:
    cube: CubeRef; start: float; end: float
    crosses_boundary: bool = False; next_cube: CubeRef | None = None
class LocateResult:
    release_id: int; primary_cube: CubeRef | None; label_span: list[CubeRef]
    sub_cube_interval: SubInterval | None; confidence: float
    generated_at: datetime; estimator_version: str = "cube-only-v1"
CUBE_ONLY_CONFIDENCE = 0.30 ; NO_BOUNDARY_CONFIDENCE = 0.0
```

From src/gruvax/estimator/constants.py:
```python
POSITION_HALF_WIDTH: float            # band half-width (never zero-width — Pitfall 21)
def compute_confidence(k: int) -> float: ...
SEGMENT_ESTIMATOR_VERSION: str = "segment-v1"   # added in 05-01
```

From src/gruvax/api/deps.py (existing providers — add get_segment_cache mirroring these):
```python
def get_boundary_cache(request) -> BoundaryCache       # reads app.state.boundary_cache; 503 if missing
def get_collection_snapshot(request) -> CollectionSnapshot  # app.state.collection_snapshot; 503
```
</interfaces>

@.planning/notes/segment-aware-boundaries.md
@src/gruvax/estimator/algorithm.py
@src/gruvax/estimator/boundary_math.py
@src/gruvax/estimator/segment_cache.py
@src/gruvax/api/deps.py
@src/gruvax/app.py
@src/gruvax/api/locate.py
@src/gruvax/api/units.py
@src/gruvax/db/seed_boundaries.py
@scripts/run_all_algorithms.py
@tests/unit/test_segment_estimator.py
@tests/property/test_segment_props.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement locate_by_segment + segment-aware boundary_math; wire SegmentCache through deps/app/locate</name>
  <files>src/gruvax/estimator/algorithm.py, src/gruvax/estimator/boundary_math.py, src/gruvax/api/deps.py, src/gruvax/app.py, src/gruvax/api/locate.py</files>
  <read_first>
    - src/gruvax/estimator/algorithm.py (locate_cube_only lines 47-121 using b.first_label/b.last_label coverage; locate_by_index lines 124-268 — the §4.1 layout to copy; locate dispatcher lines 271-329)
    - 05-PATTERNS.md § "src/gruvax/estimator/algorithm.py" (the verbatim locate_cube_only rewrite, locate_by_segment algorithm steps 1-8, dispatcher pattern) and § "Pattern 2: Two-Level Interpolation Algorithm"
    - 05-RESEARCH.md § "Pitfall 1" (locate_cube_only must use segment_cache.get_segment_for_rank, not last_*) and § "Code Examples → Two-Level Interpolation Formula"
    - src/gruvax/estimator/boundary_math.py (get_records_in_boundary lines 37-110 + count_records_in_boundary lines 113-129 to retire/replace; sample_records + suggest_midpoint lines 132-222 stay)
    - src/gruvax/api/deps.py lines 41-82 (get_boundary_cache / get_collection_snapshot pattern to mirror)
    - src/gruvax/app.py lines 101-122 (lifespan steps 3/3b where boundary cache + snapshot load — add a 3c SegmentCache derive step after both)
    - src/gruvax/api/locate.py lines 30-35, 62-109 (imports + locate() call to thread segment_cache through)
  </read_first>
  <behavior>
    - locate_by_segment on a single-segment bin returns the SAME sub_cube_interval.start/.end as the
      retired §4.1 locate_by_index for the same record (within 1e-6) — the D-02 regression invariant.
    - On make_straddle(): a record in the first half of LabelS resolves to primary_cube (1,0,0); a
      record in the second half resolves to (1,0,1) — no special-casing.
    - segment_count == 1 (singleton) → midpoint position offset + applied_fraction*0.5, banded, never zero-width.
    - locate() dispatcher: snapshot has no records for label → cube-only fallback, estimator_version "cube-only-v1";
      else segment path with estimator_version "segment-v1"; confidence <= CUBE_ONLY_CONFIDENCE → strip
      sub_cube_interval and downgrade to "cube-only-v1".
    - LocateResult shape unchanged (SEG-06 contract frozen).
  </behavior>
  <action>
    In `algorithm.py`: rewrite `locate_cube_only()` to take `(release_id, label, catalog_number,
    segment_cache, snapshot)` and determine coverage via `segment_cache.get_segment_for_rank(label,
    rank)` where rank is the record's index in the parse_key-sorted label records (drop all
    `b.last_label`/`b.last_catalog` use). Replace `locate_by_index` with `locate_by_segment(release_id,
    label, catalog_number, segment_cache, snapshot)` implementing the two-level interpolation:
    sort label records by `parse_key`, find row-rank, `get_segment_for_rank` → (bin, seg); fall back to
    `locate_cube_only` when rank is None or no segment matches; offset = seg.offset_in_bin;
    rank_in_segment = rank - seg.first_rank_in_label; if seg.segment_count <= 1 → f = offset +
    seg.applied_fraction*0.5 else f = offset + (rank_in_segment/(seg.segment_count-1))*seg.applied_fraction;
    band start=max(0,f-POSITION_HALF_WIDTH), end=min(1,f+POSITION_HALF_WIDTH); confidence =
    compute_confidence(k); estimator_version = SEGMENT_ESTIMATOR_VERSION. Set `crosses_boundary`/`next_cube`
    on the SubInterval when seg.continues is True (straddle display). Update `locate()` dispatcher to call
    `locate_by_segment` and thread `segment_cache`; keep the §4.8 fallback semantics. Update `__all__`
    (replace `locate_by_index` with `locate_by_segment`); keep `locate_by_index` importable ONLY if a
    test still needs it for the regression comparison — otherwise remove it (the regression test may
    import it from git history is NOT allowed; if the D-02 test needs §4.1, keep a private
    `_locate_by_index_v1` reference implementation in the test file or in algorithm.py annotated as the
    frozen §4.1 baseline). Decide and document which approach in the module docstring.
    In `boundary_math.py`: add `count_records_in_bin(bin_: SegmentBin) -> int` returning
    `sum(seg.segment_count for seg in bin_.segments)`; retire `get_records_in_boundary` +
    `count_records_in_boundary` (or keep a thin snapshot-based `get_records_in_bin(bin_, snapshot)` ONLY
    if units.py still needs real RecordRow lists for sample_records — see Task 2). Update the module
    docstring + TYPE_CHECKING imports (import SegmentBin under TYPE_CHECKING).
    In `deps.py`: add `get_segment_cache(request) -> SegmentCache` mirroring `get_boundary_cache`
    (read app.state.segment_cache; 503 if missing).
    In `app.py` lifespan: after the boundary cache (step 3) and snapshot (3b) load, construct
    `SegmentCache()`, call `segment_cache.derive(cache, snapshot, cache.overrides)` in a try/except that
    logs + proceeds (mirror the existing step-3 error handling), and set `app.state.segment_cache`.
    In `locate.py`: import `get_segment_cache`, add the `segment_cache` Depends param, and call
    `locate(release_id=..., label=..., catalog_number=..., segment_cache=segment_cache, snapshot=snapshot)`.
    Keep the JSON serialization unchanged (contract frozen).
  </action>
  <verify>
    <automated>uv run mypy --strict src/gruvax/ && uv run pytest tests/unit/test_segment_estimator.py -x -q 2>/dev/null; uv run python -c "import gruvax.app"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run mypy --strict src/gruvax/` exits 0 (algorithm.py, boundary_math.py, deps.py, app.py, locate.py all clean).
    - `grep -n "last_label\|last_catalog" src/gruvax/estimator/algorithm.py src/gruvax/estimator/boundary_math.py` returns nothing.
    - `grep -n "def locate_by_segment" src/gruvax/estimator/algorithm.py` matches; `SEGMENT_ESTIMATOR_VERSION` is used as the segment-path estimator_version.
    - `grep -n "def get_segment_cache" src/gruvax/api/deps.py` matches; `grep -n "SegmentCache()" src/gruvax/app.py` and `grep -n "segment_cache.derive" src/gruvax/app.py` both match.
    - `grep -n "segment_cache" src/gruvax/api/locate.py` matches; the LocateResult JSON body keys are unchanged (contract frozen).
    - `uv run python -c "import gruvax.app"` exits 0 (no import-time errors after rewiring).
  </acceptance_criteria>
  <done>Segment estimator implemented; SegmentCache wired through deps/app/locate; algorithm + boundary_math + wiring mypy-strict clean and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Heal read-path orphans — units.py, seed_boundaries.py, run_all_algorithms.py</name>
  <files>src/gruvax/api/units.py, src/gruvax/db/seed_boundaries.py, scripts/run_all_algorithms.py</files>
  <read_first>
    - src/gruvax/api/units.py (lines 30-32 imports get_records_in_boundary; lines 87-90 last_* SELECT; lines 103-112 BoundaryRow(last_*=...) construction; lines 113-114 get_records_in_boundary fill calc; lines 159-162 + 180-189 the per-cube SELECT + BoundaryRow + get_records_in_boundary + sample_records)
    - src/gruvax/db/seed_boundaries.py lines 63-85 (_upsert_cubes INSERT column list, ON CONFLICT SET, value tuple — all carry last_label/last_catalog)
    - scripts/run_all_algorithms.py lines 42-45 imports; line 98-100 + 255 the `locate(...)` calls (signature changed to segment_cache); lines 198-213 the `BoundaryRow(last_label=..., last_catalog=...)` construction in the local-CSV path
    - 05-PATTERNS.md § "src/gruvax/estimator/boundary_math.py" (count_records_in_bin contract) and § "Pitfall Summary" rows P2/P3
    - tests/integration/test_run_all_algorithms.py lines 20-31, 123 (run_all_algorithms(ci=True) contract the script must still satisfy — D-01: NOT extended with segment estimator, just kept compiling/passing)
  </read_first>
  <action>
    `units.py`: replace the `from gruvax.estimator.boundary_math import get_records_in_boundary,
    sample_records` import with `count_records_in_bin` (and `sample_records` if still used). Drop the
    `last_label, last_catalog` columns from BOTH SELECT statements (lines 87-90 and 159-162) so they
    fetch only `unit_id, row, col, first_label, first_catalog, is_empty`. Remove the
    `last_label=`/`last_catalog=` kwargs from BOTH `BoundaryRow(...)` constructions (lines 103-112 and
    180-189). Switch the fill-level + count compute to SegmentCache: add a `segment_cache:
    SegmentCache = Depends(get_segment_cache)` param to `get_cubes_bulk` and `get_cube`, look up
    `segment_cache.get_bin(unit_id,row,col)`, compute `count = count_records_in_bin(seg_bin) if seg_bin
    else 0` and `fill_level = count / max(nominal_capacity, 1)`. For `get_cube`'s `sample_records`: if
    boundary_math retains a snapshot-based record enumerator use it; otherwise derive sample release_ids
    by slicing the bin's per-segment record ranges from the snapshot (use `snapshot.get_label_records`
    for each segment label and slice [first_rank, last_rank+1]). Keep the response JSON keys the kiosk
    expects (`first_label, first_catalog, is_empty, total_count, fill_level, sample_records`); the
    `last_label`/`last_catalog` keys may be omitted from the response (they no longer exist) — confirm
    no kiosk test asserts them (grep tests for `last_label` in units coverage).
    `seed_boundaries.py`: in `_upsert_cubes`, remove `last_label, last_catalog` from the INSERT column
    list (line 64-65), remove `last_label = EXCLUDED.last_label, last_catalog = EXCLUDED.last_catalog`
    from the ON CONFLICT SET (lines 71-72), reduce the VALUES placeholder count, and drop
    `cube.get("last_label")`/`cube.get("last_catalog")` from the value tuple (lines 82-83). The YAML
    fixture already omits these keys (05-01).
    `scripts/run_all_algorithms.py`: update the `locate(...)` calls (lines ~98-100 and ~255) to pass a
    `segment_cache` (construct one via `SegmentCache().derive(cache, snapshot, cache.overrides)` from the
    same cache/snapshot the script already builds); remove the `last_label=`/`last_catalog=` kwargs from
    the `BoundaryRow(...)` construction (lines ~206-213) in the local-CSV path. Per D-01 do NOT add the
    segment estimator as a new compared algorithm — keep the harness shape; the only goal is that the
    script imports/runs and `run_all_algorithms(ci=True)` still returns its existing result structure so
    `test_run_all_algorithms.py` passes (Task 3 may adjust that test's `index`-keyed assertions).
  </action>
  <verify>
    <automated>uv run mypy --strict src/gruvax/api/units.py src/gruvax/db/seed_boundaries.py && uv run python -c "import scripts.run_all_algorithms" && grep -L "last_label\|last_catalog" src/gruvax/api/units.py src/gruvax/db/seed_boundaries.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "last_label\|last_catalog" src/gruvax/api/units.py src/gruvax/db/seed_boundaries.py scripts/run_all_algorithms.py` returns nothing.
    - `uv run mypy --strict src/gruvax/api/units.py src/gruvax/db/seed_boundaries.py` exits 0.
    - `uv run python -c "import scripts.run_all_algorithms"` exits 0 (signature-compatible `locate` calls).
    - units.py SELECTs fetch exactly `unit_id, row, col, first_label, first_catalog, is_empty` (no last_*).
    - seed_boundaries INSERT column list and value tuple have equal arity (no leftover %s placeholder).
  </acceptance_criteria>
  <done>units.py, seed_boundaries.py, and run_all_algorithms.py compile with no last_* references; fill/count via SegmentCache.</done>
</task>

<task type="auto">
  <name>Task 3: Fill SEG-06/07 estimator + property tests; rewrite §4.1-era semantic tests; mechanically heal fill-level tests</name>
  <files>tests/unit/test_segment_estimator.py, tests/property/test_segment_props.py, tests/unit/test_algorithm.py, tests/unit/test_fill_level.py, tests/property/test_fill_level_property.py, tests/integration/test_run_all_algorithms.py</files>
  <read_first>
    - tests/unit/test_segment_estimator.py + tests/property/test_segment_props.py (the 05-01 skip-stubs for SEG-06/07 + the D-02 invariants table; un-skip and implement)
    - 05-PATTERNS.md § "Tests (all new test files)" — esp. the D-02 regression invariant pattern (Pitfall 5 pre-check) and the Hypothesis @given/@settings pattern
    - 05-VALIDATION.md "Extended Hypothesis Invariants (D-02)" table (the 7 invariant test names that MUST exist and pass)
    - tests/unit/test_algorithm.py (the ~31 §4.1 cases using locate_by_index/last_*/count_records_in_boundary — rewrite for the segment estimator) — read fully
    - tests/unit/test_fill_level.py (4 BoundaryRow constructions + count_records_in_boundary usage — mechanical heal to SegmentBin/count_records_in_bin) and tests/property/test_fill_level_property.py (get_records_in_boundary/count usage — rewrite for SegmentCache)
    - tests/integration/test_run_all_algorithms.py (the `index`-keyed CI assertions — adjust to the harness's current result keys; do NOT assert a segment algorithm was added — D-01)
  </read_first>
  <action>
    Un-skip and implement the SEG-06 unit tests in `test_segment_estimator.py` (two-level interpolation,
    straddle resolution to correct bin, singleton midpoint, fallback when rank missing) and the SEG-07 /
    D-02 invariants in `test_segment_props.py`: `test_per_bin_fractions_sum_to_one`,
    `test_single_segment_bin_reproduces_v1_index` (with the mandatory Pitfall-5 pre-check:
    assert len(bin.segments)==1, auto_fraction≈1.0, first_rank_in_label==0 BEFORE comparing estimator
    outputs; compare locate_by_segment to the frozen §4.1 reference — use whatever §4.1 handle Task 1
    preserved), `test_straddle_resolves_to_correct_bin`, plus the carried §7.3 invariants
    (`test_primary_cube_in_label_span`, `test_sub_cube_interval_bounds`,
    `test_monotone_position_within_label`, `test_cosmetic_stability`). Use the make_multi_label_bin /
    make_straddle / single-segment fixtures from synth_collection (now returning a populated SegmentCache).
    Rewrite the semantic §4.1 tests in `test_algorithm.py`: replace `locate_by_index` calls with
    `locate_by_segment`, replace `BoundaryRow(last_*=...)` constructions with cut-point fixtures + a
    derived SegmentCache, assert against the segment estimator's behavior; preserve the singleton
    full-band and never-zero-width assertions (Pitfall 21). Rewrite `test_fill_level_property.py` and
    heal `test_fill_level.py` to use `count_records_in_bin(SegmentBin)` (via a derived SegmentCache)
    instead of `count_records_in_boundary`/`get_records_in_boundary`, and drop last_* from any
    BoundaryRow construction. Adjust `test_run_all_algorithms.py` so its assertions match the harness's
    current result keys (the harness was NOT extended with the segment estimator — D-01); keep the
    p95-budget and no-`local_csv`-in-CI assertions.
    Where a §4.1 numeric expectation no longer holds under the segment model, update the expected value
    to the segment estimator's correct output — do NOT delete coverage; the test count for behaviors
    must not shrink (a semantic rewrite keeps the assertion, changes the expectation).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_segment_estimator.py tests/property/test_segment_props.py tests/unit/test_algorithm.py tests/unit/test_fill_level.py tests/property/test_fill_level_property.py tests/integration/test_run_all_algorithms.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The six listed test files all pass; SEG-06/07 tests in test_segment_estimator.py + test_segment_props.py are no longer skipped.
    - `test_single_segment_bin_reproduces_v1_index` contains the Pitfall-5 pre-check assertions (len==1, auto_fraction≈1.0, first_rank==0) BEFORE the estimator-equality assertion.
    - All 7 D-02 invariant test names from 05-VALIDATION.md exist and pass.
    - `grep -n "last_label\|last_catalog\|locate_by_index\|count_records_in_boundary\|get_records_in_boundary" tests/unit/test_algorithm.py tests/unit/test_fill_level.py tests/property/test_fill_level_property.py` returns nothing (or only the preserved §4.1 reference handle if Task 1 kept one — note it explicitly).
    - GREEN-SUITE GATE: `just lint && just typecheck && just test` all exit 0; record the result in the SUMMARY.
    - Latency check (record in SUMMARY): `uv run pytest tests/integration/test_locate.py --benchmark-only` shows p95 ≤ 50 ms (if the benchmark test exists; if not, note absence).
  </acceptance_criteria>
  <done>SEG-06/07 + D-02 invariants pass; §4.1-era semantic tests rewritten for the segment model; fill-level tests healed; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/locate | release_id is an int path/query param; resolved against v_collection before estimate |
| client → GET /api/cubes, /api/cubes/{u}/{r}/{c} | public read endpoints (D-15); coordinates are typed ints |
| SegmentCache → estimator hot path | derived in-memory structure; must be populated before serving (no DB on hot path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | release_id / coordinate params | mitigate | FastAPI typed int params return 422 on non-int (existing convention T-01-09); coordinates validated `ge=`; no string interpolation into SQL (all %s) |
| T-05-03-02 | Denial of Service | locate hot path | mitigate | CPU-only two-level interpolation; SegmentCache pre-derived at startup/invalidate; p95 ≤ 50 ms gate preserved (verify via benchmark) |
| T-05-03-03 | Information Disclosure | /api/cubes public endpoints | accept | Public by design (D-15); home-LAN only, no PII; returns cube metadata + fill level only |
| T-05-03-04 | Tampering | seed_boundaries CLI writes | accept | Dev/CI-only CLI; %s placeholders; YAML fixture is synthetic (no PII, gitignored CSV never used) |
| T-05-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this wave (RESEARCH.md: zero new deps) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_segment_estimator.py tests/property/test_segment_props.py -q` — SEG-06/07 + D-02 invariants pass.
- `uv run python -c "import gruvax.app; import scripts.run_all_algorithms"` exits 0.
- `grep -rn "last_label\|last_catalog" src/gruvax/estimator/ src/gruvax/api/units.py src/gruvax/db/seed_boundaries.py scripts/run_all_algorithms.py` returns nothing.
- `just lint && just typecheck && just test` all exit 0 (full suite GREEN — the read path is now segment-aware end to end).
</verification>

<success_criteria>
- /api/locate returns a two-level-interpolation sub-cube interval behind the unchanged contract; straddle resolves by rank (SEG-06).
- estimator_version is "segment-v1"; §4.8 cube-only retained as fallback; single-segment bin reproduces §4.1 exactly (SEG-07 / D-02).
- All read-path last_* orphans (algorithm, boundary_math, units.py, seed_boundaries, run_all_algorithms) compile.
- Full suite GREEN at wave end.
</success_criteria>

<output>
Create `.planning/phases/05-segment-aware-position-precision/05-03-SUMMARY.md` when done.
MUST record: the green-suite gate result (`just lint && just typecheck && just test` exit 0); a grep
audit proving zero `last_label`/`last_catalog` in src/gruvax/estimator/, units.py, seed_boundaries.py,
run_all_algorithms.py; whether a frozen §4.1 reference handle was preserved for the D-02 test (and where);
and the locate p95 benchmark result (or note its absence).
</output>
