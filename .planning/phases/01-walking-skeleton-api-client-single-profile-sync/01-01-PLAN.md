---
phase: 01-walking-skeleton-api-client-single-profile-sync
plan: 01
type: execute
wave: 1
depends_on: ["01-00"]
files_modified:
  - migrations/versions/0009_v2_profiles_and_collection_cache.py
  - src/gruvax/settings.py
  - src/gruvax/db/pool.py
  - compose.yaml
  - .github/workflows/build.yml
  - justfile
  - pyproject.toml
requirements: [API-03, PROF-03]
autonomous: true
tags: [migration, schema, profiles, settings, fernet]

must_haves:
  truths:
    - "D-01: gruvax.profiles schema lands in P1 with app_token_encrypted BYTEA (Fernet ciphertext via GRUVAX_SECRET_KEY); `alembic upgrade head` creates gruvax.profiles + gruvax.profile_collection + adds nullable profile_id to all 7 v1 tables + drops gruvax.v_collection."
    - "D-02: `alembic upgrade head` seeds exactly one row in gruvax.profiles with deterministic id='00000000-0000-0000-0000-000000000001' and display_name='Default' (with empty `'\x'::bytea` Fernet placeholder + app_token_revoked=TRUE)."
    - "D-03: gruvax.profile_collection PRIMARY KEY is the composite (profile_id, release_id, folder_id) — preserves dupes-on-purpose (same release in main + wantlist-archive folder)."
    - "D-04: release_id stored as BIGINT, folder_id as INT; profile_collection has fts_vector (weighted A=catalog_number/B=title/C=artist||' '||label) + GIN(fts) + composite (profile_id,label,catalog_number) + GIN trgm(artist,title) indexes per spec; instance_id NOT included (omitted from envelope)."
    - "D-05: profiles.last_sync_status CHECK constraint enforces values ∈ {ok, failed, in_progress}; app_token_revoked is the 4th independent state signal (not encoded into last_sync_status)."
    - "D-11: nullable profile_id column is added to all 7 v1 tables (cube_boundaries, segments, change_log, change_sets, settings, record_stats, ambient_baseline) in P1 with default-UUID backfill; NOT NULL promotion deferred to P2."
    - "D-12: OBSERVED_DISCOGSOGRAPHY_SCHEMA is removed from src/gruvax/settings.py and compose.yaml; pool._configure_connection is simplified to single literal search_path = 'gruvax, public' (no schema branch)."
    - "D-18: Boot fails with a clear pydantic ValidationError when DISCOGSOGRAPHY_BASE_URL is missing/malformed (one env var per integration target convention); same fail-fast for GRUVAX_SECRET_KEY (Fernet validator)."
    - "D-19: Migration 0009 is fully round-trippable — `alembic upgrade head → downgrade base → upgrade head` is clean; downgrade recreates gruvax.v_collection verbatim from migration 0002 (+ restores grants) and drops the new profiles/profile_collection tables + the 7-table profile_id columns; CI round-trip gate enforces."
  artifacts:
    - path: "migrations/versions/0009_v2_profiles_and_collection_cache.py"
      provides: "Single-atomic migration creating profiles + profile_collection + 7-table profile_id fanout + drop v_collection"
      contains: "DEFAULT_PROFILE_UUID = \"00000000-0000-0000-0000-000000000001\""
    - path: "src/gruvax/settings.py"
      provides: "DISCOGSOGRAPHY_BASE_URL + GRUVAX_SECRET_KEY (SecretStr, validated) settings; OBSERVED_DISCOGSOGRAPHY_SCHEMA removed"
    - path: "src/gruvax/db/pool.py"
      provides: "_configure_connection simplified to single search_path"
    - path: "compose.yaml"
      provides: "DISCOGSOGRAPHY_BASE_URL + GRUVAX_SECRET_KEY env on api service; OBSERVED_DISCOGSOGRAPHY_SCHEMA removed"
    - path: ".github/workflows/build.yml"
      provides: "Alembic round-trip CI gate (upgrade head → downgrade base → upgrade head)"
  key_links:
    - from: "migrations/versions/0009_v2_profiles_and_collection_cache.py::downgrade"
      to: "gruvax.v_collection (recreated from migration 0002 verbatim)"
      via: "op.execute SET LOCAL search_path = gruvax, gruvax_dev, public + CREATE VIEW"
      pattern: "SET LOCAL search_path"
    - from: "src/gruvax/settings.py::Settings.GRUVAX_SECRET_KEY"
      to: "Fernet validation in field_validator"
      via: "_validate_fernet_key classmethod"
      pattern: "Fernet\\(v\\.get_secret_value"
---

<objective>
Land the single-atomic Alembic migration 0009 that creates the new schema (`gruvax.profiles` + `gruvax.profile_collection`), adds nullable `profile_id` to all 7 v1 tables with default-UUID backfill, and drops `gruvax.v_collection` — with a clean upgrade/downgrade round-trip. Simultaneously add the two boot-fail-if-missing env vars (`DISCOGSOGRAPHY_BASE_URL`, `GRUVAX_SECRET_KEY`), simplify the connection pool's search_path, and wire the CI round-trip gate. This is the foundation Plans 02-06 build on; nothing else compiles until profiles/profile_collection exist.

Depends on Plan 01-00 (Wave 0 scaffolding) — package markers and the pre-moved legacy seed at `tests/fixtures/legacy/synth_collection.sql` are in place BEFORE this plan's downgrade test references them.

Purpose: Cleanly retire the v1 `v_collection` cross-schema dependency (D-12 + D-19) and establish the per-profile schema for P2-P4 fanout, in one migration that satisfies the v1.0 CI round-trip invariant.

Output: Migration file, settings additions, pool simplification, compose updates, CI gate, justfile recipe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-VALIDATION.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-00-PLAN.md
@migrations/versions/0002_v_collection_view.py
@migrations/versions/0008_record_stats.py
@migrations/env.py
@src/gruvax/settings.py
@src/gruvax/db/pool.py
@compose.yaml
@pyproject.toml
@justfile

<interfaces>
<!-- D-01 / D-02 / D-11 / D-12 / D-19 — exact schema constants the executor must use verbatim -->

DEFAULT_PROFILE_UUID = "00000000-0000-0000-0000-000000000001"  (D-02)
DEFAULT_PROFILE_DISPLAY_NAME = "Default"

7 v1 tables (per D-11) that receive nullable profile_id with default-UUID backfill:
  cube_boundaries, segments, change_log, change_sets, settings, record_stats, ambient_baseline

profiles table column order (per D-01):
  id UUID PK DEFAULT gen_random_uuid(),
  display_name TEXT NOT NULL,
  discogs_username TEXT,
  discogsography_user_id UUID,
  app_token_encrypted BYTEA NOT NULL,
  app_token_revoked BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  last_sync_at TIMESTAMPTZ,
  last_sync_status TEXT CHECK (... IN ('ok','failed','in_progress')) [D-05]
  last_sync_error TEXT CHECK (... IN ('pat_rejected','network','rate_limited','server_error','cancelled') OR NULL) [D-06]
  last_sync_item_count BIGINT,
  deleted_at TIMESTAMPTZ

Partial-unique indexes (D-01):
  uq_profiles_display_name_active ON (LOWER(display_name)) WHERE deleted_at IS NULL
  uq_profiles_dgs_user_id_active ON (discogsography_user_id) WHERE deleted_at IS NULL AND discogsography_user_id IS NOT NULL

profile_collection table (D-03/D-04) — PK is composite (profile_id, release_id, folder_id):
  profile_id UUID NOT NULL REFERENCES gruvax.profiles(id) ON DELETE CASCADE,
  release_id BIGINT NOT NULL,
  folder_id INT,
  artist/title/label/catalog_number TEXT,
  year INT,
  fts_vector TSVECTOR GENERATED ALWAYS AS (weighted A=catalog_number, B=title, C=artist||' '||label) STORED,
  synced_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  PRIMARY KEY (profile_id, release_id, folder_id)

Indexes:
  ix_profile_collection_fts GIN(fts_vector)
  ix_profile_collection_label_catalog (profile_id, label, catalog_number)
  ix_profile_collection_artist_trgm GIN(artist gin_trgm_ops)
  ix_profile_collection_title_trgm  GIN(title  gin_trgm_ops)

Settings additions (per RESEARCH §Common operation 1):
  DISCOGSOGRAPHY_BASE_URL: str                  # no default → boot-fail-if-missing
  GRUVAX_SECRET_KEY: SecretStr                  # boot-fail; field_validator constructs Fernet(v) — raises on malformed key

Removed:
  OBSERVED_DISCOGSOGRAPHY_SCHEMA                # D-12 — drop from settings.py AND compose.yaml env block

Pool simplification (db/pool.py::_configure_connection per D-12):
  search_path = "gruvax, public"  (single value, no branch on OBSERVED_DISCOGSOGRAPHY_SCHEMA)
  Preserve the autocommit-on-for-SET-config-then-restore pattern (lines 68-77).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add settings + simplify pool + add dep packages</name>
  <files>
    pyproject.toml,
    src/gruvax/settings.py,
    src/gruvax/db/pool.py,
    tests/unit/test_settings_boot.py
  </files>
  <read_first>
    src/gruvax/settings.py,
    src/gruvax/db/pool.py,
    pyproject.toml,
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-RESEARCH.md (§Common operation 1, §Standard Stack — additions only),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md (§Modified files — settings.py + db/pool.py rows, §1. set_pat scaffold for SecretStr pattern reference),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md (D-01, D-12, D-18)
  </read_first>
  <behavior>
    - Test 1: instantiating Settings without DISCOGSOGRAPHY_BASE_URL set in env raises pydantic.ValidationError mentioning the field.
    - Test 2: instantiating Settings without GRUVAX_SECRET_KEY raises pydantic.ValidationError mentioning the field.
    - Test 3: instantiating Settings with GRUVAX_SECRET_KEY="not-a-valid-fernet-key" raises ValidationError (Fernet construction fails inside field_validator).
    - Test 4: instantiating Settings with GRUVAX_SECRET_KEY set to a valid Fernet.generate_key() value succeeds and settings.GRUVAX_SECRET_KEY is SecretStr (repr does NOT leak the key value).
    - Test 5: settings.GRUVAX_SECRET_KEY.get_secret_value() round-trips through Fernet(key).encrypt(b"x")/decrypt back to b"x".
    - Test 6: attribute `OBSERVED_DISCOGSOGRAPHY_SCHEMA` does NOT exist on the Settings class.
  </behavior>
  <action>
    Add three runtime dependencies to pyproject.toml [project] dependencies block (alphabetical insertion to match project convention): `cryptography>=48`, `stamina>=26`. Add `pytest-httpx>=0.36` to [dependency-groups] dev list. Run `uv sync` (executor must verify lockfile updates).

    In src/gruvax/settings.py: import `SecretStr` and `field_validator` from `pydantic` (mirror existing imports). Add two new fields below DATABASE_URL/SESSION_SECRET section using the project's ASCII section-header comment convention (per PATTERNS.md §Modified files row for settings.py):
      - `DISCOGSOGRAPHY_BASE_URL: str` (no default — mirrors DATABASE_URL convention at line 21 of settings.py per D-18).
      - `GRUVAX_SECRET_KEY: SecretStr` (no default — mirrors SESSION_SECRET at line 45 per D-01).
    Add classmethod `_validate_fernet_key` decorated with `@field_validator("GRUVAX_SECRET_KEY")` that constructs `Fernet(v.get_secret_value().encode())` — Fernet's constructor raises ValueError on malformed key, surfacing precise boot-error message. The import of `cryptography.fernet.Fernet` happens INSIDE the validator to avoid module-import order issues.

    DELETE the `OBSERVED_DISCOGSOGRAPHY_SCHEMA` field declaration and any reference in the settings.py docstring per D-12. After editing src/, the grep gate in `<verify>` will catch any residual references in source files; migrations/ legitimately references the constant in 0009's downgrade body (Task 2 owns) and is excluded by the gate.

    In src/gruvax/db/pool.py::`_configure_connection`: REMOVE the conditional that reads `settings.OBSERVED_DISCOGSOGRAPHY_SCHEMA` and switches between dual and single search_path values (current lines 68-77 region). REPLACE the SET search_path call body with a single hardcoded value `"gruvax, public"`. PRESERVE the autocommit-on-then-restore wrap pattern verbatim (the only change is the literal search_path string). Add a one-line comment citing D-12 + Pitfall 5 (the gruvax_dev path is re-introduced inside migration 0009's downgrade only).

    Create tests/unit/test_settings_boot.py implementing Tests 1-6 above using `monkeypatch.setenv`/`delenv` per test. Use `pytest.raises(ValidationError)` for Tests 1-3.
  </action>
  <verify>
    <automated>uv sync &amp;&amp; uv run pytest tests/unit/test_settings_boot.py -x -q &amp;&amp; (! grep -rn OBSERVED_DISCOGSOGRAPHY_SCHEMA src/ tests/ --include='*.py')</automated>
  </verify>
  <done>
    - cryptography/stamina/pytest-httpx are in pyproject.toml and resolved by uv sync.
    - tests/unit/test_settings_boot.py exists and all 6 tests pass.
    - `grep -rn OBSERVED_DISCOGSOGRAPHY_SCHEMA src/ tests/ --include='*.py'` returns no hits (migrations/ legitimately references the var in 0009's downgrade body — Task 2 owns that file).
    - db/pool.py's _configure_connection uses literal `"gruvax, public"` search_path with the autocommit wrap preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0009 — profiles + profile_collection + 7-table fanout + drop v_collection (with round-trip)</name>
  <files>
    migrations/versions/0009_v2_profiles_and_collection_cache.py,
    tests/integration/test_migrate_0009.py,
    justfile,
    .github/workflows/build.yml
  </files>
  <read_first>
    migrations/versions/0002_v_collection_view.py (downgrade target to recreate verbatim),
    migrations/versions/0008_record_stats.py (current latest revision; canonical migration shape),
    migrations/env.py (alembic env config — verify transaction_per_migration + version_table_schema='public'),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-RESEARCH.md (§Common operation 2 — full migration template lines 681-829; §Pitfall 5 + §Pitfall 7 + §Pitfall 8),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md (§8 migration analog),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-11, D-12, D-19),
    tests/fixtures/legacy/synth_collection.sql (existing dev-DB seed pre-moved by Plan 01-00 — informs the downgrade re-create context),
    tests/conftest.py,
    justfile
  </read_first>
  <behavior>
    - Test 1: after `alembic upgrade head`, the gruvax.profiles table exists with all D-01 columns including the two CHECK constraints (last_sync_status, last_sync_error tag values).
    - Test 2: exactly one row exists in gruvax.profiles with id='00000000-0000-0000-0000-000000000001' and display_name='Default' and app_token_revoked=TRUE and app_token_encrypted is non-NULL bytes.
    - Test 3: both partial-unique indexes exist (uq_profiles_display_name_active, uq_profiles_dgs_user_id_active) and a SECOND row INSERT with display_name='DEFAULT' (case-collision) raises unique-violation; a second row with discogsography_user_id=NULL succeeds (NULL not constrained).
    - Test 4: gruvax.profile_collection PK is the composite (profile_id, release_id, folder_id) — query pg_constraint and assert contype='p' with those three columns; inserting two rows with same release_id but different folder_id under same profile_id succeeds; inserting a duplicate (profile_id, release_id, folder_id) triplet raises unique-violation.
    - Test 5: every one of the 7 v1 tables (cube_boundaries, segments, change_log, change_sets, settings, record_stats, ambient_baseline) now has a column `profile_id UUID` that is NULLABLE; any pre-existing rows have been backfilled with the default UUID.
    - Test 6: gruvax.v_collection does NOT exist (psycopg raises UndefinedTable on SELECT * FROM gruvax.v_collection).
    - Test 7: `alembic upgrade head → downgrade base → upgrade head` completes without error and produces an identical schema state (re-run Tests 1-6 after the final upgrade head).
    - Test 8 (Pitfall 5): the downgrade step's CREATE VIEW gruvax.v_collection succeeds because the migration explicitly issues `SET LOCAL search_path = gruvax, gruvax_dev, public` before the CREATE VIEW (assert by inspecting the rendered downgrade SQL or by running the round-trip in a CI DB with both schemas present).
    - Test 9 (Pitfall 7): no column-level UNIQUE constraint is added on profiles.discogsography_user_id — only the partial-unique INDEX. Verify by querying pg_constraint contype='u' on the table (must be empty).
    - Test 10 (Wave 0 legacy seed reachability): the round-trip CI fixture loads `tests/fixtures/legacy/synth_collection.sql` (pre-moved by Plan 01-00) for the downgrade phase; the legacy path resolves; the seed populates the gruvax_dev schema that the downgrade's CREATE VIEW depends on.
  </behavior>
  <action>
    Create migrations/versions/0009_v2_profiles_and_collection_cache.py copying the full body from RESEARCH.md §Common operation 2 (lines 681-829) — that excerpt is project-conventions-aware and ready to drop in. Verify exact preservation of:
      - revision = "0009", down_revision = "0008" (PATTERNS §8 convention; 0008 is current latest)
      - `CREATE EXTENSION IF NOT EXISTS pgcrypto` at top of upgrade (needed for gen_random_uuid())
      - Module-level `_CREATE_*` string constants, then `op.execute(_NAME)` inside upgrade()/downgrade() (PATTERNS §8 — never inline triple-quoted strings)
      - DEFAULT_PROFILE_UUID constant exactly "00000000-0000-0000-0000-000000000001" (D-02)
      - Seed INSERT uses sentinel `'\x'::bytea` placeholder + `app_token_revoked = TRUE` (D-02 + Pitfall 8 — the revoked flag blocks all consumers until the CLI rewrites)
      - 7-table fanout loop with `ADD COLUMN profile_id UUID REFERENCES gruvax.profiles(id) ON DELETE CASCADE` + `UPDATE ... SET profile_id = '<uuid>'::uuid WHERE profile_id IS NULL` (D-11) — interpolated UUID literal is safe because it's a constant, not user input
      - All four profile_collection indexes (fts, label_catalog, artist_trgm, title_trgm) per D-04
      - Both partial-unique indexes on profiles using the EXACT shape from D-01 — NEVER `UNIQUE` as a column-level constraint (Pitfall 7)
      - Downgrade body: `op.execute("SET LOCAL search_path = gruvax, gruvax_dev, public")` BEFORE the CREATE VIEW (Pitfall 5); CREATE VIEW body must match migration 0002's view body verbatim (re-read 0002_v_collection_view.py and copy the SELECT exactly); then drop profile_id columns in reverse order of _V1_TABLES; then drop indexes then tables (drop-children-before-parents).
      - Docstring "GRANT NOTE (for operator, after upgrading)" prose block per PATTERNS §8 second excerpt — REVOKE on upgrade, GRANT on downgrade.
      - `pg_trgm` extension: profile_collection's GIN trgm indexes require pg_trgm. Migration 0001 already creates it (verify); if not present in 0001, add `CREATE EXTENSION IF NOT EXISTS pg_trgm` to 0009's upgrade.

    Create tests/integration/test_migrate_0009.py implementing all 10 behavior tests above. Use the existing `tests/conftest.py` db_pool fixture to obtain a connection; for the round-trip test (Test 7), invoke alembic programmatically via `alembic.config.Config` + `alembic.command.upgrade/downgrade` against the test DB.

    Add a justfile recipe `migrate-roundtrip` (mirroring existing recipes — read justfile for current shape) that runs: `uv run alembic upgrade head && uv run alembic downgrade base && uv run alembic upgrade head`. This is what CI invokes.

    Add a CI job to .github/workflows/build.yml (read existing workflow first to understand matrix/services shape) that:
      - Spins up postgres:18 service
      - Loads `tests/fixtures/legacy/synth_collection.sql` (pre-moved by Plan 01-00) BEFORE the round-trip downgrade phase — this creates the `gruvax_dev` schema that the downgrade's CREATE VIEW depends on (Pitfall 5).
      - Runs migrations from `base` to `head`, downgrades back to `base`, then up to `head` again
      - Fails the build on any non-zero alembic exit
    Use the existing v1.0 CI gate's Postgres service pattern as the template (build.yml already has one — extend, do not duplicate).

    Set `OBSERVED_DISCOGSOGRAPHY_SCHEMA=gruvax_dev` in the CI test job's environment (downgrade test requires it — see Pitfall 5 + assumption A8 in RESEARCH.md). Document in the migration's docstring that this env var is no longer consumed by application code (Task 1 removed it from settings.py) but the downgrade requires gruvax_dev schema to be present for v_collection re-creation.

    For the downgrade-CI scenario where gruvax_dev schema may not exist: the CI test fixture must ensure `tests/fixtures/legacy/synth_collection.sql` is loaded against the test DB BEFORE the round-trip downgrade phase (creates gruvax_dev). Plan 01-00 has already moved the file to that path; Plan 01 just references it.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_migrate_0009.py -x -q &amp;&amp; just migrate-roundtrip</automated>
  </verify>
  <done>
    - migrations/versions/0009_v2_profiles_and_collection_cache.py exists with revision="0009" / down_revision="0008".
    - All 10 tests in test_migrate_0009.py pass.
    - `just migrate-roundtrip` exits 0 (clean round-trip).
    - .github/workflows/build.yml contains a step running `just migrate-roundtrip` after seeding `tests/fixtures/legacy/synth_collection.sql`, failing the job on non-zero exit.
    - gruvax.profiles is seeded with exactly the default-UUID row.
    - All 7 v1 tables have nullable profile_id backfilled to default UUID.
    - gruvax.v_collection is dropped after `alembic upgrade head`; re-created identically on downgrade.
  </done>
</task>

<task type="auto">
  <name>Task 3: Compose env wiring + bootstrap docs for GRUVAX_SECRET_KEY</name>
  <files>
    compose.yaml,
    README.md,
    .env.example
  </files>
  <read_first>
    compose.yaml,
    README.md,
    .env.example (if present — otherwise create),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-RESEARCH.md (§Runtime State Inventory — Secrets/env vars row),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md (D-18)
  </read_first>
  <behavior>
    - Behavior 1: compose.yaml's `api` service `environment:` block contains DISCOGSOGRAPHY_BASE_URL and GRUVAX_SECRET_KEY entries using the `${VAR:-default}` substitution pattern.
    - Behavior 2: compose.yaml no longer contains OBSERVED_DISCOGSOGRAPHY_SCHEMA env entry (search yields zero hits except in retention comments labeled with date 2026-05-27 noting removal).
    - Behavior 3: README.md has a one-paragraph "First-time secrets bootstrap" section that documents the exact `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"` command to generate GRUVAX_SECRET_KEY, and instructs operators to add it to `.env`.
  </behavior>
  <action>
    In compose.yaml `api:` service `environment:` block: REMOVE the `OBSERVED_DISCOGSOGRAPHY_SCHEMA:` line. ADD `DISCOGSOGRAPHY_BASE_URL: "${DISCOGSOGRAPHY_BASE_URL:-http://fake-discogsography:8004}"` (default points at the Plan 05 sibling service). ADD `GRUVAX_SECRET_KEY: "${GRUVAX_SECRET_KEY:?GRUVAX_SECRET_KEY must be set in .env}"` (the `:?` substitution form fails compose-up with a clear error rather than silently passing empty).

    Update README.md "Environment" / "Getting Started" section (or create a "Secrets bootstrap" subsection if none exists). Add the Fernet key generation one-liner and a `.env.example` snippet. Re-read README.md first to determine the right insertion point — the existing discogsography pattern + design-language conventions apply.

    Create or update .env.example at the repo root with placeholder lines for: DATABASE_URL, SESSION_SECRET, MQTT_PASSWORD, DISCOGSOGRAPHY_BASE_URL, GRUVAX_SECRET_KEY. Comment lines describe each var; the GRUVAX_SECRET_KEY line includes the generator command inline.

    Do NOT add the `fake-discogsography` compose service in this plan — Plan 05 owns that addition. This plan only adds the env-var wiring so that Plan 05's service slots in cleanly.
  </action>
  <verify>
    <automated>grep -c "GRUVAX_SECRET_KEY" compose.yaml | grep -v 0 &amp;&amp; grep -c "DISCOGSOGRAPHY_BASE_URL" compose.yaml | grep -v 0 &amp;&amp; (! grep -v '^#' compose.yaml | grep -c "OBSERVED_DISCOGSOGRAPHY_SCHEMA" | grep -v 0) &amp;&amp; grep -c "GRUVAX_SECRET_KEY" README.md | grep -v 0</automated>
  </verify>
  <done>
    - compose.yaml has DISCOGSOGRAPHY_BASE_URL + GRUVAX_SECRET_KEY env entries on the api service.
    - compose.yaml has no live OBSERVED_DISCOGSOGRAPHY_SCHEMA references.
    - README.md documents Fernet key generation.
    - .env.example exists with the new var placeholders.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → settings | Operator-controlled GRUVAX_SECRET_KEY (Fernet key) read at process boot |
| migration → DB | DDL operations run with elevated DB role privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-PAT-rest | Information Disclosure | gruvax.profiles.app_token_encrypted column | mitigate | BYTEA column stores Fernet ciphertext only (per D-01); plaintext never reaches the DB. Seed row uses `'\x'::bytea` empty placeholder + app_token_revoked=TRUE (Pitfall 8). |
| T-01-supply-chain | Tampering | New runtime deps cryptography, stamina | mitigate | RESEARCH §Package Legitimacy Audit verified both packages as `[OK]` via slopcheck; both are from established maintainers (PyCA, Hynek Schlawack). No blocking checkpoint needed. |
| T-01-migration-partial | Tampering | Alembic upgrade/downgrade partial-failure | mitigate | alembic env.py uses `transaction_per_migration=True` (existing convention); CI round-trip gate catches partial-failure modes early. Pitfall 7 (column-level UNIQUE) explicitly tested. |
| T-01-secret-leak-via-repr | Information Disclosure | settings.GRUVAX_SECRET_KEY accidentally logged | mitigate | Declared as `SecretStr` — pydantic represents as `**********` in repr/str; only `.get_secret_value()` exposes plaintext. Test 4 in Task 1 asserts. |
| T-01-malformed-key-boot | Denial of Service | Operator pastes invalid GRUVAX_SECRET_KEY | accept | field_validator calls Fernet(key) which raises ValueError immediately at boot — operator sees the error and re-generates. Same fail-fast convention as DATABASE_URL. |
| T-01-downgrade-failure | Tampering | Downgrade can't re-create v_collection (Pitfall 5) | mitigate | Migration 0009 downgrade explicitly issues `SET LOCAL search_path = gruvax, gruvax_dev, public` before CREATE VIEW; CI round-trip gate verifies. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_settings_boot.py tests/integration/test_migrate_0009.py -x -q` is green.
- `just migrate-roundtrip` exits 0.
- `grep -rn OBSERVED_DISCOGSOGRAPHY_SCHEMA src/ tests/ --include='*.py'` returns nothing (only acceptable hit is migration 0009's downgrade body in migrations/ — Task 2 owns).
- `grep -v '^#' compose.yaml | grep OBSERVED_DISCOGSOGRAPHY_SCHEMA` returns nothing.
- CI build.yml contains `just migrate-roundtrip` step.
</verification>

<success_criteria>
- Migration 0009 creates the full new schema and drops v_collection in one atomic operation.
- Round-trip (upgrade → downgrade → upgrade) is clean and CI-gated.
- Default profile UUID is seeded.
- Pool simplified; OBSERVED_DISCOGSOGRAPHY_SCHEMA fully removed from runtime code and compose.
- Settings boot-fails clearly on missing/malformed DISCOGSOGRAPHY_BASE_URL or GRUVAX_SECRET_KEY.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-01-SUMMARY.md` when done, listing:
- Migration revision number (0009), schema entities created/dropped, default profile UUID confirmed seeded
- Round-trip CI gate path
- All file paths modified
- Any deviation from D-01..D-19 (should be none)
</output>
</content>
</invoke>