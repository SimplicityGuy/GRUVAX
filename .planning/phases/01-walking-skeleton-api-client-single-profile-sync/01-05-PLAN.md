---
phase: 01-walking-skeleton-api-client-single-profile-sync
plan: 05
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/gruvax/api/health.py
  - src/gruvax/app.py
  - frontend/src/api/types.ts
  - services/fake-discogsography/Dockerfile
  - services/fake-discogsography/server.py
  - services/fake-discogsography/seed.yaml
  - compose.yaml
  - tests/integration/api/test_health.py
requirements: [SYN-02]
autonomous: true
tags: [health, lifespan, compose, fake-discogsography, init-sync]

must_haves:
  truths:
    - "/api/health response includes `discogsography_api_check` field with value strictly in {'ok','failed','stale'} (D-13)."
    - "/api/health response does NOT include the legacy `discogsography_view_check` field."
    - "`discogsography_api_check = 'ok'` iff last_sync_status='ok' AND app_token_revoked=FALSE."
    - "`discogsography_api_check = 'failed'` iff last_sync_status='failed' OR app_token_revoked=TRUE."
    - "`discogsography_api_check = 'stale'` iff last_sync_at IS NULL OR now() - last_sync_at > 24h."
    - "Overall `status` field is 'degraded' when db_ok=FALSE OR discogsography_api_check != 'ok' (preserves v1 contract semantics)."
    - "`sync_age_seconds` field is now derived from `now() - profiles.last_sync_at` for the default profile (NOT max(v_collection.synced_at))."
    - "Lifespan startup probe targets `gruvax.profile_collection` (NOT `gruvax.v_collection`); never crashes — log + flip flag + continue (CARRIES OVER v1 pattern)."
    - "`fake-discogsography` Compose sibling service serves the contract endpoints; `init-sync` one-shot container runs `gruvax-sync default` after `gruvax-api` is healthy (D-16, idempotent)."
    - "TypeScript HealthResponse type updated: discogsography_view_check renamed to discogsography_api_check, union widened to 'ok'|'failed'|'stale'."
  artifacts:
    - path: "src/gruvax/api/health.py"
      provides: "/api/health handler with renamed field + state widening per D-13"
    - path: "src/gruvax/app.py"
      provides: "Lifespan probe targets profile_collection + background task reads profiles.last_sync_at"
    - path: "services/fake-discogsography/Dockerfile"
      provides: "Minimal single-stage fake-discogsography container image"
    - path: "services/fake-discogsography/server.py"
      provides: "Uvicorn entry script importing tests/fixtures/fake_discogsography:create_fake_app with seed loaded from seed.yaml"
    - path: "services/fake-discogsography/seed.yaml"
      provides: "~3000-row synthetic seed for compose-time fake (separate from tests/ seed)"
    - path: "compose.yaml"
      provides: "fake-discogsography sibling service + init-sync container; api depends on fake-discogsography healthy"
  key_links:
    - from: "src/gruvax/api/health.py"
      to: "app.state.default_profile_last_sync_at + default_profile_last_sync_status + default_profile_app_token_revoked"
      via: "Background-task-populated cached attributes (no live DB probe — preserves v1 D-13 invariant)"
      pattern: "default_profile_last_sync"
    - from: "src/gruvax/app.py::_refresh_default_profile_state"
      to: "gruvax.profiles.last_sync_at"
      via: "60-second polling background task"
      pattern: "gruvax\\.profiles"
    - from: "compose.yaml::init-sync"
      to: "POST /api/admin/profiles/{default-uuid}/sync"
      via: "uv run gruvax-sync --profile default invocation"
      pattern: "gruvax-sync"
---

<objective>
Finish the wire-side of the data-source swap from v_collection → profile_collection: rename the `/api/health` field per D-13 and re-derive its three states from cached profile state; replace the lifespan v_collection probe with a profile_collection probe; replace the 60s `_refresh_sync_age` background task body with reads against `gruvax.profiles.last_sync_at`; rename the TypeScript HealthResponse field; add the Compose `fake-discogsography` sibling + `init-sync` one-shot per D-16 so `docker compose up` does a real end-to-end sync.

Purpose: Land all "edge of the system" rewires in one focused plan — health endpoint, lifespan probes, frontend type, and Compose. Plan 06 still has to rewire the queries.py / collection_snapshot.py read paths; that's the heaviest rewire and gets its own plan to keep this one focused.

Output: 4 modified Python files + 1 TypeScript file + 3 new infra files + compose.yaml updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-UI-SPEC.md
@.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-VALIDATION.md
@src/gruvax/api/health.py
@src/gruvax/app.py
@tests/fixtures/fake_discogsography.py
@compose.yaml

<interfaces>
<!-- /api/health response shape per D-13 + UI-SPEC -->

GET /api/health  (always 200)
Response JSON:
  {
    "status": "ok" | "degraded",
    "db": "ok" | "error",
    "discogsography_api_check": "ok" | "failed" | "stale",   # RENAMED + WIDENED (D-13)
    "mqtt": "ok" | "error",
    "sync_age_seconds": float | null,                         # source swapped; field name unchanged
    "started_at": "<iso8601>",
    "uptime_seconds": float,
    ... (any other v1 fields preserved as-is)
  }

<!-- app.state attributes for the new background task to populate -->

app.state.default_profile_last_sync_at: datetime | None
app.state.default_profile_last_sync_status: str | None   # 'ok' | 'failed' | 'in_progress' | None
app.state.default_profile_app_token_revoked: bool
app.state.sync_age_seconds: float | None                 # derived from default_profile_last_sync_at (kept as v1 attr name)

<!-- Lifespan probe replacement -->

REMOVE (in app.py lifespan step 2):
  SELECT 1 FROM gruvax.v_collection LIMIT 1
  app.state.discogsography_view_ok = True/False

ADD:
  SELECT count(*) FROM gruvax.profile_collection WHERE profile_id = %s::uuid LIMIT 1   # using DEFAULT_PROFILE_UUID
  app.state.profile_collection_ready = True/False
  (Failure path: same "log + flip flag + continue" pattern as v1's v_collection probe — never crash.)

<!-- Background task replacement (app.py step 1c — _refresh_sync_age) -->

OLD body:
  SELECT EXTRACT(EPOCH FROM (now() - max(synced_at))) FROM gruvax.v_collection

NEW body (per D-13 + PATTERNS §Shared Patterns "Background-task lifecycle"):
  SELECT last_sync_at, last_sync_status, app_token_revoked
  FROM gruvax.profiles WHERE id = %s::uuid AND deleted_at IS NULL
Then:
  app.state.default_profile_last_sync_at = row[0]
  app.state.default_profile_last_sync_status = row[1]
  app.state.default_profile_app_token_revoked = bool(row[2])
  app.state.sync_age_seconds = (now - row[0]).total_seconds() if row[0] else None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /api/health field rename + state-widening derivation</name>
  <files>
    src/gruvax/api/health.py,
    frontend/src/api/types.ts,
    tests/integration/api/test_health.py
  </files>
  <read_first>
    src/gruvax/api/health.py (full file — current discogsography_view_check derivation lines 34-66),
    frontend/src/api/types.ts (HealthResponse type definition; if missing, grep frontend/src for the type),
    tests/integration/api/test_health.py (existing v1 health tests — extend, do not rewrite),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md (D-13 verbatim),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-UI-SPEC.md (§Scope row 1 + §Color §/api/health state mapping — the three-state contract),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md (§12 health.py modification)
  </read_first>
  <behavior>
    - Test 1 (field rename): GET /api/health → response.json() has key "discogsography_api_check" and does NOT have key "discogsography_view_check".
    - Test 2 (value enum): the value is one of {"ok","failed","stale"} only (assert with `in` against the set).
    - Test 3 (ok derivation): app.state.default_profile_last_sync_status='ok' AND default_profile_app_token_revoked=FALSE → discogsography_api_check == 'ok'.
    - Test 4 (failed by status): default_profile_last_sync_status='failed' AND revoked=FALSE → 'failed'.
    - Test 5 (failed by revoked): default_profile_last_sync_status='ok' AND revoked=TRUE → 'failed'.
    - Test 6 (stale by null): default_profile_last_sync_at=None (regardless of status) → 'stale' (per D-13 order: `last_sync_at IS NULL` checked BEFORE status comparison when sync hasn't happened).  Verify the precedence per UI-SPEC: if status='failed' the field is 'failed', not 'stale'. So precedence is: failed > stale > ok. Re-verify against UI-SPEC table at lines 98-103 and CONTEXT.md D-13.
    - Test 7 (stale by age): default_profile_last_sync_at=now() - 25h → 'stale'.
    - Test 8 (overall degraded propagation): discogsography_api_check='failed' → top-level status='degraded'; api_check='ok' AND db='ok' AND mqtt='ok' → status='ok'.
    - Test 9 (sync_age_seconds source swap): app.state.sync_age_seconds is populated from `now() - default_profile_last_sync_at` (NOT max(v_collection.synced_at)). Assert by setting last_sync_at = now() - 60s; sync_age_seconds is in [55, 65] range.
    - Test 10 (TypeScript type renamed): `cat frontend/src/api/types.ts` shows `discogsography_api_check: 'ok' | 'failed' | 'stale'` and does NOT contain `discogsography_view_check`.
  </behavior>
  <action>
    Modify src/gruvax/api/health.py per PATTERNS §12. Preserve:
      - Module docstring "No live probe" rule (lines 35-43).
      - JSONResponse always-200 envelope shape.
      - Existing fields (db, mqtt, sync_age_seconds, started_at, uptime_seconds).

    Replace the `discogsography_view_check` derivation (lines around the existing view_ok read) with the D-13 three-state derivation per UI-SPEC §/api/health state mapping table:
      ```
      last_sync_at = getattr(request.app.state, "default_profile_last_sync_at", None)
      last_sync_status = getattr(request.app.state, "default_profile_last_sync_status", None)
      token_revoked = getattr(request.app.state, "default_profile_app_token_revoked", True)
      now = datetime.now(UTC)

      # Precedence per D-13 + UI-SPEC table: failed > stale > ok (failed bypasses stale check)
      if last_sync_status == 'failed' or token_revoked:
          api_check = 'failed'
      elif last_sync_at is None or (now - last_sync_at) > timedelta(hours=24):
          api_check = 'stale'
      elif last_sync_status == 'ok':
          api_check = 'ok'
      else:
          api_check = 'stale'  # default conservative for unknown intermediate states
      ```
    Rename the response body field from `discogsography_view_check` to `discogsography_api_check`.

    Update overall-status derivation: `overall = "ok" if db_ok and api_check == "ok" else "degraded"`.

    Locate `frontend/src/api/types.ts` (or the actual HealthResponse type location — grep `discogsography_view_check` across frontend/src/). Rename the field type member from `discogsography_view_check` to `discogsography_api_check` and widen the union from `'ok' | 'failed'` to `'ok' | 'failed' | 'stale'`. If no consumer in the v1.0 frontend reads this field (UI-SPEC §Component Inventory line 140 verified zero consumers), the rename is type-only and no further frontend code change is needed.

    Extend tests/integration/api/test_health.py with Tests 1-10. Use the existing health test fixture pattern (probably `client: AsyncClient` against the in-process app via LifespanManager — read the file first). For Tests 3-9, monkeypatch `app.state.default_profile_*` attributes inside the test before calling GET /api/health.

    For Test 10, do a `Read` of the modified types.ts file and assert via test (`assert "discogsography_api_check" in content` etc.) OR run `tsc --noEmit` in CI (not part of pytest). Simplest: do a Python-level open+read of the TS file inside the test.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/api/test_health.py -x -q &amp;&amp; (! grep -r discogsography_view_check src/gruvax/api/health.py)</automated>
  </verify>
  <done>
    - /api/health returns discogsography_api_check with three-state value per D-13.
    - The legacy discogsography_view_check field is gone from health.py and types.ts.
    - All 10 tests pass.
    - sync_age_seconds derives from default_profile_last_sync_at.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lifespan rewire — probe profile_collection + populate default_profile_* state</name>
  <files>
    src/gruvax/app.py
  </files>
  <read_first>
    src/gruvax/app.py (full file — lifespan steps 2 + 1c at lines 127-139 and 209-239),
    src/gruvax/db/pool.py (post-Plan-01 — search_path = "gruvax, public"),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md (D-13 + D-14),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md (§Modified files row for app.py; §Shared Patterns "Background-task lifecycle" + "Lifespan-startup probe")
  </read_first>
  <behavior>
    - Test 1 (startup probe success): with profile_collection populated for default UUID, lifespan startup logs "profile_collection probe: OK" and `app.state.profile_collection_ready` is True.
    - Test 2 (startup probe failure non-fatal): with profile_collection empty (or migration not run — drop the table to simulate), lifespan continues; logger.error fired; `app.state.profile_collection_ready` is False. App still serves /api/health.
    - Test 3 (background task populates default_profile_*): after lifespan startup with default-profile row present (last_sync_at set), `app.state.default_profile_last_sync_at`, `default_profile_last_sync_status`, `default_profile_app_token_revoked` are all set within ~1 second (allow asyncio.sleep(60) initial delay is bypassed for tests — see action note on initial fire).
    - Test 4 (background task refresh on schedule): manipulate gruvax.profiles.last_sync_at via direct SQL; after the next 60-second tick, app.state.default_profile_last_sync_at reflects the new value. (Skip the literal 60s sleep in tests via monkeypatching the sleep duration or asserting on the first iteration only.)
    - Test 5 (legacy view_ok attribute removed): no code path in app.py sets app.state.discogsography_view_ok any more (grep returns 0).
  </behavior>
  <action>
    In src/gruvax/app.py lifespan:

    Step 2 (around lines 127-139) — REPLACE the v_collection probe:
      ```
      # ── 2. profile_collection startup probe (P1 D-13) ────────────────────────
      try:
          async with pool.connection() as conn, conn.cursor() as cur:
              await cur.execute(
                  "SELECT 1 FROM gruvax.profile_collection WHERE profile_id = %s::uuid LIMIT 1",
                  (DEFAULT_PROFILE_UUID,),
              )
              # row may be None on first boot (no sync yet); that's OK — probe success means the
              # table exists and is queryable, not that it has rows.
              await cur.fetchone()
          app.state.profile_collection_ready = True
          logger.info("profile_collection probe: OK")
      except Exception as exc:
          app.state.profile_collection_ready = False
          logger.error(
              "profile_collection probe FAILED — search will return 503 until resolved. "
              "Run `alembic upgrade head` and `gruvax-sync --profile default`. Details: %s",
              exc,
          )
      ```
    DELETE the `app.state.discogsography_view_ok = True/False` assignments (Test 5).

    Define `DEFAULT_PROFILE_UUID = "00000000-0000-0000-0000-000000000001"` as a module-top constant (mirrors migration 0009).

    Step 1c (around lines 209-239) — REPLACE the _refresh_sync_age body per PATTERNS §Shared Patterns "Background-task lifecycle":
      ```
      app.state.default_profile_last_sync_at = None
      app.state.default_profile_last_sync_status = None
      app.state.default_profile_app_token_revoked = True
      app.state.sync_age_seconds = None

      async def _refresh_default_profile_state() -> None:
          # Fire once immediately so app.state.default_profile_* are populated before
          # /api/health is first served — then enter the 60s polling loop.
          while True:
              try:
                  async with pool.connection() as conn, conn.cursor() as cur:
                      await cur.execute(
                          "SELECT last_sync_at, last_sync_status, app_token_revoked "
                          "FROM gruvax.profiles WHERE id = %s::uuid AND deleted_at IS NULL",
                          (DEFAULT_PROFILE_UUID,),
                      )
                      row = await cur.fetchone()
                  if row is not None:
                      app.state.default_profile_last_sync_at = row[0]
                      app.state.default_profile_last_sync_status = row[1]
                      app.state.default_profile_app_token_revoked = bool(row[2])
                      now = datetime.now(UTC)
                      app.state.sync_age_seconds = (
                          (now - row[0]).total_seconds() if row[0] else None
                      )
              except Exception as exc:
                  logger.warning("default profile state refresh failed: %s", exc)
                  # Do NOT clobber known-good cached values on transient failures.
              await asyncio.sleep(60)

      _state_task = asyncio.create_task(_refresh_default_profile_state())
      # CR-01: strong reference per existing convention
      app.state.background_tasks.add(_state_task)
      _state_task.add_done_callback(app.state.background_tasks.discard)
      def _log_state_task_exc(t: asyncio.Task) -> None:
          if not t.cancelled() and t.exception() is not None:
              logger.warning("default_profile_state background task exited: %s", t.exception())
      _state_task.add_done_callback(_log_state_task_exc)
      logger.info("default_profile_state background refresh task scheduled (60s cadence)")
      ```
    DELETE the old `_refresh_sync_age` function entirely (its work is now the new task's responsibility).

    Verify no other lifespan steps reference `discogsography_view_ok` (grep). If any do, update them or remove (e.g., metrics that read view_ok would now read profile_collection_ready).

    Extend or write tests in `tests/integration/api/test_health.py` (Task 1's file) for Tests 1-5 here — they overlap heavily with Task 1's testing surface and consolidating prevents fixture duplication. Use `LifespanManager + AsyncClient + db_pool` to bring up the real app against the test DB.

    For Test 2 (startup-probe failure), the simplest approach: run the test with a clean DB that has the schema but no profiles row OR temporarily DROP the table inside a fixture; assert startup proceeds and /api/health returns degraded with profile_collection_ready=False.

    For Tests 3-4, the background task's first iteration must fire WITHOUT a 60s wait — modify the loop to execute the query before the first `await asyncio.sleep(60)` (the skeleton above already does this — the while loop body runs once before the sleep). For deterministic test timing, optionally accept a TEST-only env var or function param to shrink the sleep duration; OR assert against the first-iteration state and skip Test 4's timing-sensitive case (assert via direct invocation of the inner query function).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/api/test_health.py -x -q &amp;&amp; (! grep -n discogsography_view_ok src/gruvax/app.py)</automated>
  </verify>
  <done>
    - Lifespan startup probe targets profile_collection.
    - Background task populates default_profile_last_sync_at/status/app_token_revoked.
    - sync_age_seconds derives from default_profile_last_sync_at.
    - Old _refresh_sync_age function removed.
    - app.state.discogsography_view_ok no longer assigned anywhere in app.py.
    - All 5 lifespan tests pass (in addition to Task 1's health response tests).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose fake-discogsography sibling service + init-sync one-shot + seed.yaml</name>
  <files>
    services/fake-discogsography/Dockerfile,
    services/fake-discogsography/server.py,
    services/fake-discogsography/seed.yaml,
    compose.yaml,
    tests/integration/test_compose_smoke.py
  </files>
  <read_first>
    compose.yaml (full file — mosquitto + api service blocks for the sibling pattern),
    tests/fixtures/fake_discogsography.py (Plan 02 — create_fake_app factory),
    Dockerfile (root — for the multi-stage uv reference; new fake-discogsography Dockerfile is MINIMAL),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-CONTEXT.md (D-15, D-16, D-17, D-18),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-RESEARCH.md (§Recommended Project Structure — services/fake-discogsography path; §Pattern 4),
    .planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-PATTERNS.md (§11 services/fake-discogsography analog),
    fixtures/synth_collection.sql (existing synth dataset — adapt for seed.yaml schema)
  </read_first>
  <behavior>
    - Behavior 1 (Dockerfile builds): `docker build services/fake-discogsography` produces a runnable image; the image's CMD launches uvicorn against `server:app`.
    - Behavior 2 (seed.yaml has ~3000 rows): the YAML file has >=2900 release records following the contract envelope schema (`id`, `title`, `year`, `catalog_number`, `artist`, `label`, `genres`, `styles`, `rating`, `date_added`, `folder_id`).
    - Behavior 3 (compose includes fake-discogsography): `docker compose config` shows the `fake-discogsography` service block with the right image/build context, healthcheck against /api/user/collection?limit=1 + Bearer dscg_dev_seed, and `networks: [internal]`.
    - Behavior 4 (compose api depends on fake healthy): the `api` service's `depends_on:` adds `fake-discogsography: condition: service_healthy`.
    - Behavior 5 (init-sync container present): the `init-sync` one-shot service exists; depends on api + fake-discogsography healthy; runs `uv run gruvax-sync --profile default` with `GRUVAX_BASE_URL=http://api:8000`; restart: "no" (one-shot).
    - Behavior 6 (init-sync idempotency): on second `docker compose up`, init-sync re-runs but is idempotent — the sync_profile staging-swap unconditionally replaces rows, so re-running on already-populated DB is safe (D-16: "idempotent — skips if profile_collection is already populated for the default profile" is approximate; the actual implementation re-syncs cleanly which is also fine).
  </behavior>
  <action>
    Create services/fake-discogsography/Dockerfile (MINIMAL — PATTERNS §11 last paragraph):
      ```
      # syntax=docker/dockerfile:1.7
      FROM python:3.14-slim AS runtime
      WORKDIR /app
      RUN pip install --no-cache-dir fastapi==0.136.1 uvicorn[standard]==0.32.* pyyaml==6.0.* pydantic==2.13.*
      COPY server.py seed.yaml ./
      EXPOSE 8004
      HEALTHCHECK --interval=30s --timeout=5s --retries=5 \
          CMD curl -fsS -H "Authorization: Bearer dscg_dev_seed" http://127.0.0.1:8004/api/user/collection?limit=1 || exit 1
      CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8004"]
      ```

    Create services/fake-discogsography/server.py:
      ```
      """Compose-sibling fake discogsography. Reuses the test-time fake factory."""
      from __future__ import annotations
      from pathlib import Path
      import yaml
      from fake_discogsography import create_fake_app  # shipped via build context

      _SEED_PATH = Path(__file__).parent / "seed.yaml"
      _seed = yaml.safe_load(_SEED_PATH.read_text())["releases"]
      app = create_fake_app(seed=_seed, user_id="00000000-0000-0000-0000-DEFAULTDEVUSER")
      ```
    HOWEVER — `fake_discogsography` module is in `tests/fixtures/`. Either:
      (a) Copy the factory file into `services/fake-discogsography/` (duplication; trivial to keep in sync since the file is small), OR
      (b) Move the factory to a shared module that both test fixtures and the compose service can import (e.g., `src/gruvax/_internal/fake_discogsography.py` and re-export from tests/fixtures).
    Choose (a) for build-isolation simplicity. The Dockerfile copies `server.py + seed.yaml + fake_discogsography.py` (a copy of the test file). Document the duplication + add a CI lint check (or a justfile recipe) `just sync-fake` that copies tests/fixtures/fake_discogsography.py → services/fake-discogsography/fake_discogsography.py and fails CI if they drift. Implementor must add this recipe to justfile.

    Create services/fake-discogsography/seed.yaml. Adapt from fixtures/synth_collection.sql's existing ~200-row synthetic data, padded out to ~3000 rows. Use a quick generator script (one-time, can live in tests/fixtures/generate_seed.py and produce the YAML deterministically) — generate per the contract item schema (id as string, title, catalog_number from label-aware prefix generators, artist, label, year, folder_id=1). Aim for >=2900 entries to satisfy the "v1 baseline ~3,000 items" success criterion (ROADMAP §Phase 1 success #4). YAML shape:
      ```
      releases:
        - id: "1"
          title: "Track 1"
          year: 1975
          catalog_number: "BLP 4001"
          artist: "Artist 1"
          label: "Blue Note"
          genres: ["jazz"]
          styles: []
          rating: 0
          date_added: "2026-01-01"
          folder_id: 1
        - id: "2"
          ...
      ```
    The generator script can produce this from the same shape-variety contract as fixtures/synth_collection.sql (label prefix table, catalog# format mix, etc.). Document in the generator's docstring.

    Modify compose.yaml — ADD the `fake-discogsography` service block (mirror the mosquitto block's shape per PATTERNS §11):
      ```yaml
      fake-discogsography:
        image: gruvax/fake-discogsography:dev
        build:
          context: ./services/fake-discogsography
        container_name: gruvax-fake-discogsography
        networks: [internal]
        healthcheck:
          test: ["CMD", "curl", "-fsS", "-H", "Authorization: Bearer dscg_dev_seed", "http://127.0.0.1:8004/api/user/collection?limit=1"]
          interval: 30s
          timeout: 5s
          retries: 5
          start_period: 10s
        restart: unless-stopped
        logging:
          driver: "json-file"
          options: { max-size: "10m", max-file: "3" }
      ```
    Add to the existing `api` service's `depends_on`:
      ```yaml
      fake-discogsography:
        condition: service_healthy
      ```

    ADD the `init-sync` one-shot service block:
      ```yaml
      init-sync:
        image: ghcr.io/simplicityguy/gruvax:latest   # reuses main gruvax image
        depends_on:
          api: { condition: service_healthy }
          fake-discogsography: { condition: service_healthy }
        command: ["uv", "run", "gruvax-sync", "--profile", "default"]
        environment:
          GRUVAX_BASE_URL: "http://api:8000"
          DATABASE_URL: "${DATABASE_URL:-postgresql+psycopg://gruvax:gruvax@gruvax-dev-pg:5432/gruvax}"
          # PIN for init-sync sourced from .env (operator provides; the dev-default
          # is whatever gruvax-set-pin was last run with on this deployment).
          GRUVAX_ADMIN_PIN: "${GRUVAX_ADMIN_PIN:?GRUVAX_ADMIN_PIN must be set in .env for init-sync}"
        restart: "no"
        networks: [internal]
      ```
    NOTE: the `gruvax-sync` CLI in Plan 04 reads the PIN via getpass interactively. For init-sync (non-TTY) it needs to read from env / stdin. The simplest cross-cut: have `gruvax-sync` accept the PIN via stdin pipe when not on a TTY. The compose `command:` becomes `["sh", "-c", "echo $GRUVAX_ADMIN_PIN | uv run gruvax-sync --profile default"]`. This is a small extension to Plan 04's CLI — the CLI's getpass branch should already be `if sys.stdin.isatty(): getpass.getpass(...) else: sys.stdin.read().strip()` for symmetry with set_pat. Plan 04's task 3 owns this if a gap is found during execution; this plan's compose entry should use the `sh -c` form above.

    Create tests/integration/test_compose_smoke.py with Behavior 3-5 assertions: parse compose.yaml via PyYAML and assert the service blocks exist with the expected fields. (NO `docker compose up` invocation in pytest — too slow/heavy. Smoke test is structural only.)

    For Behavior 1-2 (Dockerfile builds + seed.yaml row count), add a justfile recipe `just smoke-fake` that runs `docker build services/fake-discogsography` and `python -c "import yaml; print(len(yaml.safe_load(open('services/fake-discogsography/seed.yaml'))['releases']))"`. Document in plan summary; not a pytest gate but invokable.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_compose_smoke.py -x -q &amp;&amp; python3 -c "import yaml,sys; n=len(yaml.safe_load(open('services/fake-discogsography/seed.yaml'))['releases']); sys.exit(0 if n>=2900 else 1)"</automated>
  </verify>
  <done>
    - services/fake-discogsography/{Dockerfile, server.py, seed.yaml} exist; seed has >=2900 releases.
    - compose.yaml has the fake-discogsography service + init-sync one-shot + api dependency.
    - justfile has `just sync-fake` (drift guard) and `just smoke-fake` (build + count) recipes.
    - tests/integration/test_compose_smoke.py asserts compose structure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/health response → external clients | Cached state; no PII; no PAT |
| compose-time init-sync → admin endpoint | GRUVAX_ADMIN_PIN in env (operator-provided) |
| fake-discogsography container ↔ api container | Internal compose network; not exposed to LAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-health-leak | Information Disclosure | /api/health reveals last_sync_at timestamps | accept | LAN-only deployment (OOS-06); timestamps reveal sync cadence but not PAT or user data; matches v1 contract. |
| T-01-init-sync-pin | Information Disclosure | GRUVAX_ADMIN_PIN in compose env | mitigate | Sourced from `.env` (gitignored per project conv); compose uses `${GRUVAX_ADMIN_PIN:?...}` substitution that fails compose-up clearly if missing; not stored in image. |
| T-01-fake-svc-exposure | Spoofing | fake-discogsography exposed externally | mitigate | Bound to `internal` network only; no `ports:` mapping; only reachable via Compose DNS as `fake-discogsography:8004`. |
| T-01-fake-leak-prod | Tampering | fake-discogsography accidentally runs in prod | mitigate | Distinct compose service name + only built/run in local dev; prod overrides DISCOGSOGRAPHY_BASE_URL to point at the real discogsography. Documented in compose.yaml header comment. |
| T-01-lifespan-crash | Denial of Service | profile_collection probe failure crashes startup | mitigate | "try/except + logger.error + proceed" pattern preserved (PATTERNS §Shared Patterns "Lifespan-startup probe"); Test 2 verifies. |
| T-01-fake-token-pollution | Tampering | Production code accidentally uses `dscg_dev_seed` | accept | The token is hard-coded only in the fake service's container; production GRUVAX never has this token in its profile rows. |
</threat_model>

<verification>
- `uv run pytest tests/integration/api/test_health.py tests/integration/test_compose_smoke.py -x -q` is green.
- `grep -rn discogsography_view_check src/gruvax/api/health.py` returns nothing.
- `grep -rn discogsography_view_ok src/gruvax/app.py` returns nothing.
- `grep -rn discogsography_view_check frontend/src/` returns nothing.
- compose.yaml has both `fake-discogsography` and `init-sync` service blocks.
- services/fake-discogsography/seed.yaml has at least 2900 releases.
</verification>

<success_criteria>
- /api/health field rename + three-state derivation per D-13.
- Lifespan probe + background task rewired to use profile_collection / profiles.
- Compose stack delivers a real end-to-end sync on `docker compose up` (D-16).
- TypeScript types updated.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-api-client-single-profile-sync/01-05-SUMMARY.md` when done, listing:
- /api/health response shape post-rewire (sample JSON)
- Lifespan startup log lines (probe + background task scheduled)
- Compose services added (fake-discogsography, init-sync) + dependency wiring
- Seed row count
- Frontend type rename location
</output>
