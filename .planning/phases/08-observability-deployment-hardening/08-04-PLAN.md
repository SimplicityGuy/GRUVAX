---
phase: 08-observability-deployment-hardening
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/gruvax/api/admin/diagnostics.py
  - src/gruvax/api/admin/router.py
  - frontend/src/routes/admin/Diagnostics.tsx
  - frontend/src/routes/admin/Diagnostics.css
  - frontend/src/routes/admin/AdminShell.tsx
  - frontend/src/api/adminClient.ts
  - frontend/src/App.tsx
  - tests/integration/test_diagnostics.py
autonomous: false
requirements: [OBS-05, OBS-06, OBS-07]
must_haves:
  truths:
    - "GET /api/admin/diagnostics requires an admin session + CSRF and returns the 7 SC#2 rows"
    - "POST /api/admin/diagnostics/reset-stats requires admin session + CSRF and clears record_stats"
    - "An unauthenticated request to either endpoint is rejected (401/403)"
    - "The /admin/diagnostics route renders sync staleness, top-N searched, slow-query log, MQTT status, pool size_used/size_min, phantom-boundary count, and recent logs"
    - "Diagnostics data loads on open and on an explicit Refresh; there is no polling and no SSE"
    - "The Reset stats action uses an inline confirm (RESET STATS -> CONFIRM RESET? -> YES, RESET / KEEP STATS) and shows Stats cleared on success"
  artifacts:
    - path: "src/gruvax/api/admin/diagnostics.py"
      provides: "admin-gated diagnostics GET + reset-stats POST"
      exports: ["router"]
    - path: "frontend/src/routes/admin/Diagnostics.tsx"
      provides: "the /admin/diagnostics page (7 sections)"
      min_lines: 80
  key_links:
    - from: "src/gruvax/api/admin/diagnostics.py"
      to: "require_admin"
      via: "Depends(require_admin) on GET and POST"
      pattern: "require_admin"
    - from: "frontend/src/routes/admin/Diagnostics.tsx"
      to: "/api/admin/diagnostics"
      via: "getDiagnostics() in useEffect on mount + Refresh"
      pattern: "getDiagnostics"
    - from: "src/gruvax/api/admin/router.py"
      to: "diagnostics router"
      via: "router.include_router(diagnostics_router)"
      pattern: "diagnostics"
---

<objective>
Build the SC#2 surface: the admin-gated `/api/admin/diagnostics` read endpoint (the 7 rows: sync staleness, top-N most-searched, slow-query ring buffer, MQTT status, Postgres pool size_used/size_min, phantom-boundary count, recent log lines) + the PIN-gated reset-stats action (D-06), then the `/admin/diagnostics` React page per the UI-SPEC contract (D-10, D-11) — manual refresh only, no polling, no SSE.

Depends on Plan 02 (query functions: staleness, top-N, phantom, reset) and Plan 03 (counters populate, slow_query_ring fills). Owns the diagnostics endpoint + admin router enrich + the new frontend route + adminClient/AdminShell/App enrich — disjoint from Plan 05 (kiosk banner).

Purpose: a real admin opens the page and sees live operational state; this ties OBS-05/06/07 together into one screen.
Output: diagnostics.py, router.py enrich, Diagnostics.tsx/.css, AdminShell nav tab, adminClient functions, App route, integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-observability-deployment-hardening/08-CONTEXT.md
@.planning/phases/08-observability-deployment-hardening/08-RESEARCH.md
@.planning/phases/08-observability-deployment-hardening/08-PATTERNS.md
@.planning/phases/08-observability-deployment-hardening/08-UI-SPEC.md
@.planning/phases/08-observability-deployment-hardening/08-VALIDATION.md
@.planning/phases/08-observability-deployment-hardening/08-02-SUMMARY.md
@.planning/phases/08-observability-deployment-hardening/08-03-SUMMARY.md
@design/gruvax-design-tokens.css

<interfaces>
<!-- From Plan 02 (queries), codebase (admin patterns), UI-SPEC. -->

From Plan 02 (db/queries.py):
- `get_sync_staleness_seconds(pool) -> float | None`
- `get_top_searched(pool, limit=10) -> list[dict]` (keys: release_id, title, primary_artist, search_count, search_count_7d, selection_count, selection_count_7d)
- `get_phantom_boundary_count(pool) -> int`
- `reset_record_stats(pool) -> None`

From src/gruvax/api/admin/leds.py (analog controller — admin-gated GET + background POST):
- imports `from gruvax.api.deps import get_pool, require_admin`; `router = APIRouter(tags=["..."])`; handlers take `request: Request, pool: Any = Depends(get_pool), _admin: dict[str, str] = Depends(require_admin)`.
- `require_admin` enforces signed-cookie session AND CSRF double-submit on mutating methods (401 missing/invalid session, 403 CSRF fail). No extra auth logic needed in the body.

From app.state (Plan 01) for the diagnostics reads:
- `app.state.slow_query_ring: deque` (entries: {path, total_ms, db_ms, threshold_ms, ts}); `app.state.log_ring_buffer: deque` (entries: {ts, level, logger, msg}); `app.state.sync_age_seconds: float | None`; `app.state.mqtt_ok: bool`; `app.state.db_pool` (psycopg AsyncConnectionPool).
- psycopg pool stats: `stats = pool.get_stats()`; `size_used = stats["pool_size"] - stats["pool_available"]`; `size_min = stats["pool_min"]` (RESEARCH Open Question 7; get_stats is sync, non-blocking).

From src/gruvax/api/admin/router.py:
- `create_admin_router()` imports sub-routers inside the function body and calls `router.include_router(<r>)`; add diagnostics after `import_router`.

From frontend/src/routes/admin/Settings.tsx (analog page) + adminClient.ts:
- `adminFetch(path, opts)` wraps fetch, auto-attaches `X-CSRF-Token` for mutating methods (adminClient.ts lines 58-85). GET pattern: `getAdminSettings`; POST pattern: `ledsAllOff`.
- Page uses `useEffect(() => { void load() }, [])` on mount; explicit Refresh button re-calls load(). Section card class = `.settings-section`, heading = `.settings-heading`, primary button = `.settings-btn-primary`.

From frontend/src/routes/admin/AdminShell.tsx:
- existing `<NavLink to=... className={({isActive}) => ...}>LABEL</NavLink>` nav-tab pattern; append a DIAGNOSTICS tab after IMPORT.

From frontend/src/App.tsx (route registration — read to find the admin route block) and frontend/src/lib/dom.ts:
- `el(tag, props, ...children)` helper for imperative DOM (use `replaceChildren()`, never innerHTML) — for the dark recent-logs terminal section.

UI-SPEC contract (08-UI-SPEC.md) — render to it, do NOT re-decide:
- Page sections, copy, typography (only 24/18/16/14 px), color tokens, the inline reset-confirm flow (no modal), and the exact empty-state/error strings. PIN is NOT re-prompted (session already covers auth). All hex via tokens.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnostics backend — admin-gated GET (7 rows) + reset-stats POST + router registration</name>
  <files>src/gruvax/api/admin/diagnostics.py, src/gruvax/api/admin/router.py, tests/integration/test_diagnostics.py</files>
  <read_first>
    - .planning/phases/08-observability-deployment-hardening/08-PATTERNS.md (section diagnostics.py lines 134-192; section admin/router.py lines 195-217; Shared Patterns: require_admin, app.state getattr, psycopg %s, pool.connection)
    - .planning/phases/08-observability-deployment-hardening/08-RESEARCH.md (Open Question 7 pool stats + phantom; Security Domain lines 1023-1037)
    - src/gruvax/api/admin/leds.py (analog: admin-gated GET + POST)
    - src/gruvax/api/admin/router.py (current include_router block)
    - tests/integration/test_health.py + tests/unit/test_led_admin_endpoints.py (analog harness; note dependency_overrides[require_admin] pattern from STATE Phase 06-04 decision for auth bypass in tests)
    - .planning/phases/08-observability-deployment-hardening/08-VALIDATION.md (test_diagnostics requirements line 56)
  </read_first>
  <action>
    Create `src/gruvax/api/admin/diagnostics.py` with `router = APIRouter(tags=["admin-diagnostics"])`.

    `GET /diagnostics` — handler signature `async def get_diagnostics(request: Request, pool: Any = Depends(get_pool), _admin: dict[str, str] = Depends(require_admin))`. Build and return a dict with the 7 SC#2 rows:
    - `sync_age_seconds`: `getattr(request.app.state, "sync_age_seconds", None)` (or `await get_sync_staleness_seconds(pool)` if app.state is None).
    - `top_searched`: `await get_top_searched(pool, 10)` (OBS-07).
    - `slow_queries`: `list(getattr(request.app.state, "slow_query_ring", deque()))` reversed (newest-first) (OBS-05).
    - `mqtt`: `"connected" if getattr(request.app.state, "mqtt_ok", False) else "disconnected"`.
    - `pool`: `{"size_used": stats["pool_size"] - stats["pool_available"], "size_min": stats["pool_min"]}` from `request.app.state.db_pool.get_stats()`.
    - `phantom_boundary_count`: `await get_phantom_boundary_count(pool)`.
    - `recent_logs`: `list(getattr(request.app.state, "log_ring_buffer", deque()))` reversed (newest-first), capped to last 20 (D-12).

    `POST /diagnostics/reset-stats` — `async def reset_stats(request: Request, pool, _admin = Depends(require_admin))` -> calls `await reset_record_stats(pool)`, logs `"record_stats truncated by admin"`, returns `{"reset": True}`. CSRF is enforced by require_admin on the POST (V4 access control, T-08-13).

    SECURITY: the diagnostics body must NOT include connection strings, env dumps, the PIN, or raw query text. Top-searched rows carry release_id + title/artist display fields only. Recent logs carry pre-formatted messages from the ring buffer (the codebase logs counts/timing, not query text).

    In `src/gruvax/api/admin/router.py`, add inside `create_admin_router()`: `from gruvax.api.admin.diagnostics import router as diagnostics_router` and `router.include_router(diagnostics_router)` after the `import_router` include.

    Write `tests/integration/test_diagnostics.py` (Wave-0): assert GET /api/admin/diagnostics with a valid admin session (use `app.dependency_overrides[require_admin]` stub per Phase 06-04 canonical pattern) returns the 7 keys above; assert an UNAUTHENTICATED GET (no override, no cookie) returns 401; assert reset-stats clears stats (seed via increment_search_count, confirm top_searched non-empty, POST reset, confirm empty); assert reset-stats without a session is 401/403. Use `test_staleness` and `test_counters` named tests to satisfy the VALIDATION map.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_diagnostics.py -x -q && uv run mypy --strict src/gruvax/api/admin/diagnostics.py</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/admin/diagnostics (authenticated) returns JSON with keys: `sync_age_seconds`, `top_searched`, `slow_queries`, `mqtt`, `pool`, `phantom_boundary_count`, `recent_logs`
    - `pool` value has `size_used` and `size_min`
    - Unauthenticated GET returns 401; unauthenticated POST reset-stats returns 401/403
    - `grep -n "require_admin" src/gruvax/api/admin/diagnostics.py` shows it on BOTH the GET and the POST
    - `grep -n "diagnostics" src/gruvax/api/admin/router.py` shows the include_router
    - Diagnostics body contains none of: `session_secret`, `database_url`, `pin`, raw query text
    - `uv run pytest tests/integration/test_diagnostics.py -x -q` exits 0; `uv run mypy --strict src/gruvax/api/admin/diagnostics.py` exits 0
  </acceptance_criteria>
  <done>Both endpoints live, admin+CSRF gated, returning the 7 rows / clearing stats; no secrets; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/diagnostics React page + adminClient + AdminShell nav + App route</name>
  <files>frontend/src/routes/admin/Diagnostics.tsx, frontend/src/routes/admin/Diagnostics.css, frontend/src/api/adminClient.ts, frontend/src/routes/admin/AdminShell.tsx, frontend/src/App.tsx</files>
  <read_first>
    - .planning/phases/08-observability-deployment-hardening/08-UI-SPEC.md (FULL — page structure, sections, typography 24/18/16/14, color tokens, reset-confirm flow, copy table, accessibility)
    - .planning/phases/08-observability-deployment-hardening/08-PATTERNS.md (section Diagnostics.tsx lines 642-757; section AdminShell.tsx lines 760-777; section adminClient.ts lines 780-808; Shared Patterns: el()/replaceChildren, useEffect-on-mount, token-only CSS)
    - frontend/src/routes/admin/Settings.tsx (analog page: state, load-on-mount, inline-confirm, section cards)
    - frontend/src/routes/admin/admin.css (.settings-section/.settings-heading/.settings-btn-primary classes)
    - frontend/src/api/adminClient.ts (adminFetch GET + POST CSRF patterns)
    - frontend/src/routes/admin/AdminShell.tsx (NavLink nav-tab pattern)
    - frontend/src/App.tsx (admin route registration block)
    - frontend/src/lib/dom.ts (el() helper)
    - design/gruvax-design-tokens.css (token names — never hardcode hex)
    - .claude/skills/sketch-findings-gruvax/SKILL.md (Nordic Grid: blue=structure, yellow=active/changed, DM Mono for data, ALL-CAPS Barlow labels)
  </read_first>
  <action>
    Add to `frontend/src/api/adminClient.ts`: `getDiagnostics(): Promise<DiagnosticsData>` (GET `/api/admin/diagnostics` via adminFetch) and `resetStats(): Promise<{reset: boolean}>` (POST `/api/admin/diagnostics/reset-stats` via adminFetch — CSRF auto-attached). Define the `DiagnosticsData` TS type mirroring the 7 backend keys.

    Create `frontend/src/routes/admin/Diagnostics.tsx` rendering EXACTLY the UI-SPEC page structure: DiagnosticsToolbar (REFRESH button + last-refreshed timestamp), StalenessSection (single row + OK/STALE/OUTDATED badge with thresholds 3d/14d per D-01), TopSearchedSection (top-10 table with the 5 count columns + empty state + the inline RESET STATS -> CONFIRM RESET? -> YES, RESET / KEEP STATS flow per D-06; PIN NOT re-prompted), SlowQuerySection (5-column table, newest-first, the SLO sub-label + empty state), SystemStatusSection (MQTT BROKER dot, POSTGRES POOL size_used/size_min, PHANTOM BOUNDARIES count), RecentLogsSection (dark terminal, newest-first, level color-coded, built via `el()`/`replaceChildren()` per the imperative-DOM rule). Data loads in `useEffect(() => { void load() }, [])` on mount + explicit Refresh; NO polling, NO SSE (D-11). Use the exact copy strings, badges, and error/empty-state text from the UI-SPEC copy table.

    Create `frontend/src/routes/admin/Diagnostics.css` consuming ONLY design tokens (never hardcode hex): section cards, staleness yellow/red tints (`--gruvax-yellow-faint`, `--gruvax-warning`, `--gruvax-error`), dark logs terminal (`--gruvax-blue-darker` bg), DM Mono data, Barlow ALL-CAPS labels, shimmer skeleton keyframes, 44px touch targets, focus ring per UI-SPEC accessibility.

    In `frontend/src/routes/admin/AdminShell.tsx`, append a `<NavLink to="/admin/diagnostics">DIAGNOSTICS</NavLink>` tab after the IMPORT tab (UI-SPEC nav order: SETTINGS -> CUBES -> HISTORY -> WIZARD -> IMPORT -> DIAGNOSTICS).

    In `frontend/src/App.tsx`, register the `/admin/diagnostics` route pointing to the Diagnostics page (same admin-shell gating as the other /admin/* routes).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0 and `npm run build` succeeds
    - `grep -n "getDiagnostics\|resetStats" frontend/src/api/adminClient.ts` returns both
    - `grep -n "/admin/diagnostics" frontend/src/App.tsx` and `frontend/src/routes/admin/AdminShell.tsx` each return a match
    - `grep -niE "#[0-9a-f]{3,6}" frontend/src/routes/admin/Diagnostics.css` returns 0 (no hardcoded hex; tokens only)
    - `grep -n "innerHTML" frontend/src/routes/admin/Diagnostics.tsx` returns 0 (el()/replaceChildren only)
    - Diagnostics.tsx renders all 7 UI-SPEC sections and the inline reset-confirm pair "YES, RESET" / "KEEP STATS" (no generic Cancel)
  </acceptance_criteria>
  <done>The /admin/diagnostics page renders the 7 rows per UI-SPEC, manual-refresh only, Nordic Grid tokens, inline reset-confirm; frontend builds + typechecks clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify /admin/diagnostics page renders per UI-SPEC</name>
  <what-built>The /admin/diagnostics admin page: SYNC STATUS, TOP SEARCHED (with Reset stats), SLOW QUERIES, SYSTEM (MQTT/pool/phantom), and RECENT LOGS — plus the manual Refresh and the inline reset-confirm flow, all in Nordic Grid styling.</what-built>
  <how-to-verify>
    1. Rebuild the stack: `docker compose up -d --build api` (dev PIN = 0000).
    2. Open the admin SPA on a phone-width viewport, sign in with PIN 0000, and tap the new DIAGNOSTICS nav tab.
    3. Confirm all 5 section cards render with Nordic Grid styling (blue headings, DM Mono numbers, dark logs terminal). Run a couple of kiosk searches first so TOP SEARCHED and RECENT LOGS have data.
    4. Tap REFRESH — data reloads; "Last refreshed" updates. Confirm there is NO continuous polling (network tab shows requests only on open + each Refresh tap).
    5. Tap RESET STATS -> CONFIRM RESET? appears inline -> tap KEEP STATS (no change) then RESET STATS -> YES, RESET -> "Stats cleared." appears and TOP SEARCHED empties.
    6. Confirm typography uses only the 4 sizes (24/18/16/14) and labels are ALL-CAPS Barlow.
  </how-to-verify>
  <action>Pause for human verification. The executor has built the /admin/diagnostics page; the human must follow the how-to-verify steps and approve before the plan completes. Do not auto-advance.</action>
  <verify>
    <human-check>Admin signs in (PIN 0000), opens DIAGNOSTICS, confirms all 5 section cards render per UI-SPEC, REFRESH reloads with no polling, and the RESET STATS inline-confirm flow works (YES, RESET clears TOP SEARCHED; KEEP STATS is a no-op).</human-check>
  </verify>
  <done>Human types "approved" after confirming the page renders per the UI-SPEC and the reset flow works.</done>
  <resume-signal>Type "approved" or describe issues (e.g., contrast, copy, layout)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN client -> /api/admin/diagnostics | Admin-only; exposes internal pool stats, timings, log lines |
| LAN client -> /api/admin/diagnostics/reset-stats | Admin-only destructive write (truncates counters) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Elevation of Privilege | reset-stats POST | mitigate | `Depends(require_admin)` enforces session + CSRF double-submit; unauthenticated POST returns 401/403. Test asserts rejection. |
| T-08-14 | Information Disclosure | diagnostics GET body | mitigate | Admin-gated; body excludes connection strings/env/PIN/raw query text; only release_id-keyed counts + display fields + ring-buffer messages. Test asserts no secret keys. |
| T-08-15 | Information Disclosure | recent_logs row | mitigate | Log ring buffer holds messages the codebase already emits (counts/timing) — no query text or PIN; admin-gated read only. |
| T-08-16 | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: none); stdlib + installed deps + existing frontend deps only. |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_diagnostics.py -q` green (auth gating + 7 rows + reset)
- `uv run mypy --strict src/gruvax/` exits 0
- `cd frontend && npx tsc --noEmit && npm run build` succeed
- Human-verify checkpoint approved (page renders per UI-SPEC; reset flow works)
</verification>

<success_criteria>
- /api/admin/diagnostics returns the 7 SC#2 rows, admin+CSRF gated (OBS-05, OBS-06, OBS-07; SC2)
- reset-stats clears counters, admin+CSRF gated (D-06)
- /admin/diagnostics page renders per UI-SPEC, manual refresh only (D-10, D-11)
- No secrets in payload; destructive action rejected when unauthenticated
</success_criteria>

<output>
Create `.planning/phases/08-observability-deployment-hardening/08-04-SUMMARY.md` when done.
</output>
