---
phase: 8
slug: observability-deployment-hardening
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-24
---

# Phase 8 — UI Design Contract

> Visual and interaction contract for the two user-facing surfaces in Phase 8.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Scope constraint:** Phase 8 is mostly backend/ops. This spec covers ONLY the two
> genuine user-facing surfaces:
>
> 1. `/admin/diagnostics` — new admin route (sibling to Settings), PIN/session + CSRF gated
> 2. **Kiosk sync-staleness banner** — persistent kiosk banner when `sync_age > 14d`
>
> All other Phase 8 work (health/version endpoints, structured logging, CI, Compose log
> limits, benchmarks) is non-UI and out of scope for this spec.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (vanilla DOM — `el()` / `replaceChildren()`, no shadcn) |
| Preset | not applicable |
| Component library | none (project-native components throughout) |
| Icon library | Inline SVG (Lucide subset, hand-authored per Settings.tsx pattern) |
| Token source | `design/gruvax-design-tokens.css` — consume only; never hardcode hex |
| Fonts | Barlow Condensed (Google Fonts, wght 700 + 900) · Space Grotesk (400/500/700) · DM Mono (400/500) |

**Build constraint:** All DOM built via `el()` helper + `replaceChildren()`. Never `innerHTML`. Matches every prior admin phase.

---

## Spacing Scale

Source: `--gruvax-space-*` tokens in `gruvax-design-tokens.css`. All values are multiples of 4.

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| `--gruvax-space-1` | 4px | Inline icon gaps; status-dot + label gap |
| `--gruvax-space-2` | 8px | Row internal padding; badge padding |
| `--gruvax-space-3` | 12px | Cell padding for diagnostic table rows |
| `--gruvax-space-4` | 16px | Default element spacing; section gap |
| `--gruvax-space-5` | 24px | Section padding (matches `.admin-content` pattern) |
| `--gruvax-space-6` | 32px | Between diagnostic sections |
| `--gruvax-space-7` | 48px | Page-level vertical rhythm (matches `.settings-page` gap) |

Exceptions: kiosk banner horizontal padding uses `--gruvax-space-4` (16px) left/right to avoid hugging the grid edges on the 7" screen; banner vertical padding uses `--gruvax-space-3` (12px) to keep it subtle.

---

## Typography

Three-font system — Barlow Condensed / Space Grotesk / DM Mono. Roles are fixed; no ad-hoc assignments.

**Exactly 4 distinct pixel sizes are used across both surfaces: 24 / 18 / 16 / 14.**

| Role | Font | Size token | Weight | Line Height | Usage |
|------|------|-----------|--------|-------------|-------|
| Section heading | Barlow Condensed | `--gruvax-text-display-md` (24px) | 900 | `--gruvax-leading-tight` (1.1) | Diagnostics page section headings (e.g. "SYNC STATUS") |
| Kiosk banner | Space Grotesk | `--gruvax-text-body-lg` (18px) | 400 | `--gruvax-leading-normal` (1.5) | Kiosk sync-staleness banner message ONLY — 18px is the contrast-safe floor for yellow-on-dark-blue (~3.1:1 = WCAG large text) |
| Field label / column header + large data | Barlow Condensed (labels) · DM Mono (data) | `--gruvax-text-display-sm` / `--gruvax-text-mono-lg` (16px) | 700 (labels) · 500 (data) | 1.1 labels (`--gruvax-leading-tight`) · 1.5 data (`--gruvax-leading-normal`) | Row labels, table column headers (ALL CAPS, tracked); staleness age readout (DM Mono 16px 500) |
| Body / data / log readout | Space Grotesk (body) · DM Mono (data + logs) | `--gruvax-text-body-sm` / `--gruvax-text-mono` (14px) | 400 (body) · 400/500 (data) | `--gruvax-leading-normal` (1.5) | Hint text, row descriptions, status messages, empty-state copy, error copy, sub-labels, log-line timestamps (Space Grotesk 14px); search/selection counts, timing values (ms), pool stats, recent log lines (DM Mono 14px) |

**Enforcement:**
- Barlow Condensed is ALL CAPS only — never sentence case for display labels.
- DM Mono is reserved for numeric data: counts, timing values (ms), pool sizes, catalog IDs, and the dark-terminal log readout. Never for instructions or copy.
- Body copy and instructions are Space Grotesk sentence case.
- **Size discipline:** the only sizes permitted in this phase are 24px, 18px, 16px, and 14px. 18px is reserved exclusively for the kiosk banner (its accessibility floor). Everything previously specced at 12px (captions / sub-labels / log timestamps) and 11px (log lines) folds into 14px. No 5th or 6th size appears anywhere in this spec.

---

## Color

Source: `gruvax-design-tokens.css`. No hex values anywhere in component CSS.

| Role | Token | Hex ref (for context) | Usage |
|------|-------|-----------------------|-------|
| Dominant surface (60%) | `--gruvax-off-white` | #F7F9FC | Diagnostics page background (matches `.admin-shell` body bg) |
| Secondary surface (30%) | `--gruvax-white` | #FFFFFF | Diagnostic section cards (matches `.settings-section` bg pattern) |
| Accent (10%) | `--gruvax-blue` | #0051A2 | Section headings, field labels, primary button bg, active nav tab underline |
| Staleness yellow | `--gruvax-yellow` | #FFDA00 | Staleness > 3d warning row background tint; kiosk banner bg when > 14d |
| Staleness red | `--gruvax-error` | #C0392B | Staleness > 14d row text color on diagnostics page |
| Destructive | `--gruvax-error` | #C0392B | "Reset stats" destructive button text + border |
| Success | `--gruvax-success` | #1A7A4A | Confirmation text after successful reset ("Stats cleared.") |
| Muted | `--gruvax-text-muted` | #777777 | Recent log lines, secondary data labels |
| Faint border | `--gruvax-border-faint` | #E5EEF7 | Diagnostic table row dividers |

**Accent reserved for:** section headings, ALL-CAPS field labels, primary CTA button background, active nav tab yellow underline (yellow `--gruvax-yellow` only on tab underline per existing pattern).

**Staleness state encoding:**
- `sync_age` 0–3d: no special treatment (normal data color, `--gruvax-text-secondary`)
- `sync_age` > 3d (admin diagnostics only): row background `--gruvax-yellow-faint` (rgba(255,218,0,0.12)), label text `--gruvax-warning` (#E6A800)
- `sync_age` > 14d (admin diagnostics): row background `--gruvax-yellow-faint`, label text `--gruvax-error` (#C0392B)
- `sync_age` > 14d (kiosk banner): banner background `--gruvax-yellow`, text `--gruvax-blue-darker` (#002855) — 18px text meets 3.1:1 contrast minimum per design-language accessibility note

**Yellow-on-blue / blue-on-yellow accessibility rule:** Yellow is 3.1:1 on blue — large text (18px+) or decoration only, never body copy. The kiosk banner uses 18px Space Grotesk to satisfy this. The admin yellow-tint row background uses `--gruvax-text-secondary` (#555555) for data values to maintain ≥ 4.5:1 contrast.

---

## Component Inventory

### Surface 1: `/admin/diagnostics` page

**Route:** `/admin/diagnostics` — new NavLink added to `AdminShell.tsx` topbar nav as "DIAGNOSTICS"
**Auth:** same `AdminShell` PIN/session gate as all `/admin/*` routes; no extra gating
**Layout:** mirrors `.settings-page` — `display: flex; flex-direction: column; gap: --gruvax-space-7`

#### Page structure

```
DiagnosticsPage
  ├── DiagnosticsToolbar          (REFRESH button + last-refreshed timestamp)
  ├── StalenessSection            (section card — sync staleness row)
  ├── TopSearchedSection          (section card — top-N table)
  ├── SlowQuerySection            (section card — ring buffer log)
  ├── SystemStatusSection         (section card — MQTT + pool + phantom rows)
  └── RecentLogsSection           (section card — log ring buffer tail)
```

Each section is a `.settings-section` card (white bg, `--gruvax-radius-lg`, `--gruvax-shadow-sm`, padding `--gruvax-space-5`).

#### DiagnosticsToolbar

- `REFRESH` button: primary button style (`.settings-btn-primary` class reused) — label "REFRESH", full-width on mobile, max-width 160px on desktop
- "Last refreshed: {relative time}" — Space Grotesk 14px, `--gruvax-text-muted`, to the right of button on ≥ 480px, below on mobile
- State: `loading` → button shows "REFRESHING…" with `disabled` + opacity 0.6 (matches existing `.settings-btn-primary:disabled` pattern)
- Data loads on page open (single `useEffect` on mount) + explicit Refresh tap. No polling, no SSE (D-11 locked).

#### StalenessSection

- Heading: "SYNC STATUS" (Barlow Condensed 900 24px `--gruvax-blue`)
- Single data row showing:
  - Label (ALL CAPS, Barlow Condensed 700 16px): "DISCOGSOGRAPHY LAST SYNC"
  - Value: DM Mono 16px 500 — formatted as "Xd Yh ago" or "< 1h ago"
  - Status badge: pill badge right-aligned — text "OK" / "STALE" / "OUTDATED"
    - OK (≤ 3d): `--gruvax-success` bg tint + text
    - STALE (> 3d): `--gruvax-warning` (#E6A800) bg tint (`--gruvax-yellow-faint`) + text
    - OUTDATED (> 14d): `--gruvax-error` bg tint + text
- Full row background: see color contract above (transparent for OK, `--gruvax-yellow-faint` for STALE/OUTDATED)

#### TopSearchedSection

- Heading: "TOP SEARCHED" (Barlow Condensed 900 24px `--gruvax-blue`)
- Table layout — four columns:
  - Artist / Title (Space Grotesk 14px `--gruvax-text-secondary`) — derived from release_id lookup (backend provides display text)
  - SEARCHES ALL-TIME (DM Mono 14px)
  - SEARCHES 7-DAY (DM Mono 14px)
  - SELECTED ALL-TIME (DM Mono 14px)
  - SELECTED 7-DAY (DM Mono 14px)
- Column headers: Barlow Condensed 700 16px ALL CAPS, `--gruvax-text-primary`, `--gruvax-border-faint` bottom border
- Row dividers: 1px `--gruvax-border-faint`
- Empty state: "No search data yet. Stats accumulate as records are searched." (Space Grotesk 14px `--gruvax-text-muted`, centered, `--gruvax-space-5` vertical padding)
- N = 10 rows maximum (display top 10 by all-time search count)

**"Reset stats" destructive action:**
- Positioned below the table, right-aligned
- Button style: outlined destructive — `border: 1px solid --gruvax-error`, text `--gruvax-error`, bg transparent, Barlow Condensed 700 16px ALL CAPS, min-height 44px
- Label: "RESET STATS"
- On tap: inline confirmation replaces the button text — "CONFIRM RESET?" with two inline buttons: "YES, RESET" (destructive filled: bg `--gruvax-error`, white text) and "KEEP STATS" (secondary outlined — names the preserved outcome, never a generic "Cancel")
- PIN is NOT re-prompted (the admin is already authenticated in the current session). The confirmation step is the only gate. (Decision: session PIN already covers auth; a second PIN re-entry would be redundant for a home-LAN single-admin product.)
- After reset: "Stats cleared." (Space Grotesk 14px `--gruvax-success`, auto-hides after 3s, role="status")
- After "KEEP STATS": returns to "RESET STATS" button state, no data touched

#### SlowQuerySection

- Heading: "SLOW QUERIES" (Barlow Condensed 900 24px)
- Sub-label: "Requests exceeding SLO threshold (search > 200 ms · locate > 50 ms)" — Space Grotesk 14px `--gruvax-text-muted`
- Table layout — five columns:
  - ENDPOINT (Barlow Condensed 700 16px ALL CAPS for value too, since it's a path token like `/api/search`)
  - TOTAL (ms) — DM Mono 14px — request-total time
  - DB (ms) — DM Mono 14px — DB-time component
  - THRESHOLD (ms) — DM Mono 14px `--gruvax-text-muted`
  - TIME — Space Grotesk 14px `--gruvax-text-muted` — relative timestamp ("2 min ago")
- Rows are shown newest-first (ring buffer tail = most recent)
- Empty state: "No slow queries logged. The ring buffer resets on restart." (Space Grotesk 14px `--gruvax-text-muted`, centered)
- Ring buffer cap: last 50 entries shown (backend decides buffer size; frontend renders all returned)

#### SystemStatusSection

- Heading: "SYSTEM" (Barlow Condensed 900 24px)
- Three status rows, each following a fixed layout:
  - Left: status indicator dot (8px circle, filled) + ALL-CAPS label (Barlow Condensed 700 16px)
  - Right: DM Mono 14px value

  Row 1 — MQTT BROKER
  - Dot: `--gruvax-success` green when connected; `--gruvax-error` red when disconnected
  - Value: "Connected" / "Disconnected" (Space Grotesk 14px)

  Row 2 — POSTGRES POOL
  - Dot: always `--gruvax-success` (no warning state for pool stats; data is informational)
  - Value: "{size_used} / {size_min}" — DM Mono 14px (e.g. "2 / 2")
  - Sub-label: "connections used / min pool size" — Space Grotesk 14px `--gruvax-text-muted`

  Row 3 — PHANTOM BOUNDARIES
  - Dot: `--gruvax-success` when 0; `--gruvax-warning` when > 0
  - Value: "{count}" — DM Mono 14px
  - Sub-label: "boundaries referencing records not in v_collection" — Space Grotesk 14px `--gruvax-text-muted`

- Row dividers: 1px `--gruvax-border-faint`, `--gruvax-space-3` vertical padding per row

#### RecentLogsSection

- Heading: "RECENT LOGS" (Barlow Condensed 900 24px)
- Display: monospace log lines, newest-first, max 20 lines visible (scrollable within a fixed-height container: `max-height: 300px; overflow-y: auto`)
- Container bg: `--gruvax-blue-darker` (#002855) — dark terminal aesthetic; text `--gruvax-white`
- Font: DM Mono 14px (`--gruvax-text-mono`), line-height `--gruvax-leading-relaxed` (1.7)
- Log level color coding (foreground text only, no bg):
  - ERROR: `--gruvax-error` (#C0392B)
  - WARNING: `--gruvax-warning` (#E6A800)
  - INFO: `--gruvax-white`
  - DEBUG: `--gruvax-text-muted` (#777777)
- Timestamp prefix: DM Mono 14px `--gruvax-text-faint` (#999999)
- Empty state (dark bg): "No log entries in buffer." — DM Mono 14px `--gruvax-text-faint`, centered, padding `--gruvax-space-4`

---

### Surface 2: Kiosk sync-staleness banner

**Trigger:** `sync_age_seconds > 1_209_600` (14 days, D-01 locked threshold)
**Location:** persistent banner rendered below the kiosk top bar (or fixed bottom of screen), above the grid. Never overlays the grid.
**Persistence:** shown continuously while condition is true; not dismissible (owner needs to act, not hide it)
**No polling source:** banner data comes from the same `/api/health` response the kiosk already consumes. If health is stale (kiosk offline), banner hides silently — the offline banner takes precedence.

#### Banner anatomy

```
┌─────────────────────────────────────────────────────────┐
│  ⚠  Collection data may be outdated — last synced 18d ago  │
└─────────────────────────────────────────────────────────┘
```

- Background: `--gruvax-yellow` (#FFDA00)
- Text color: `--gruvax-blue-darker` (#002855)
- Font: Space Grotesk 18px weight 400 — sentence case (18px meets accessibility minimum for yellow-on-dark contrast of 3.1:1 per design language note; this is the only 18px in the phase and it is reserved for this banner)
- Icon: inline SVG warning triangle (Lucide `AlertTriangle`, 18×18), `--gruvax-blue-darker`, `aria-hidden="true"` — decorative, not load-bearing
- Padding: `--gruvax-space-3` (12px) vertical × `--gruvax-space-4` (16px) horizontal
- Full-width, no border-radius (flush edge-to-edge like a system notification bar)
- `role="alert"` on mount (fires once); thereafter `role="status"` aria-live="polite" (not re-announcing on every render cycle)
- No dismiss button — this is a persistent operational signal

#### Banner copy (exact)

"Collection data may be outdated — last synced {Xd} ago"

Where `{Xd}` is formatted as: `18d`, `22d`, `30d` etc. (whole days only; no hours suffix on the kiosk banner).

Rules:
- Sentence case, Space Grotesk — NOT Barlow Condensed (banner is a notification, not a label)
- "Collection data may be outdated" — not "Discogsography sync is stale" (no jargon, no app internals)
- "last synced" — lowercase, consistent with natural language
- Em dash separator (—) between condition and detail, matching the voice/tone guide
- Never: "SYNC STALE", "sync_age_seconds", "collection_items", or any technical term

---

## Admin Navigation Update

`AdminShell.tsx` gains one new `<NavLink>`:

```tsx
<NavLink
  to="/admin/diagnostics"
  className={({ isActive }) =>
    `admin-nav-tab${isActive ? ' admin-nav-tab--active' : ''}`
  }
>
  DIAGNOSTICS
</NavLink>
```

Position: appended after "IMPORT" in the existing nav sequence (SETTINGS → CUBES → HISTORY → WIZARD → IMPORT → **DIAGNOSTICS**).

On narrow screens the topbar nav wraps; no special treatment needed — existing `.admin-topbar-nav` handles wrap via flex.

---

## Interaction States

### Diagnostics page load

| State | Visual |
|-------|--------|
| Loading (initial mount or Refresh tap) | Each section card shows a `--gruvax-border-faint` pulsing shimmer skeleton at the card's approximate height; button shows "REFRESHING…" disabled |
| Loaded | All section cards render with data |
| Network error | Error message replaces card content: "Could not load diagnostics. Check that the API is reachable." (Space Grotesk 14px `--gruvax-error`, role="alert") + retry link "Try again" (underlined, `--gruvax-blue`) |

Shimmer skeleton: single `div` per card with `background: linear-gradient(90deg, --gruvax-border-faint 25%, --gruvax-blue-faint 50%, --gruvax-border-faint 75%)` animated via `@keyframes shimmer` (0% → 100% bg-position, 1.2s linear infinite). No JS dependency.

### Reset stats flow

```
idle → confirm → (success | error) → idle
```

- Confirm state: inline — the "RESET STATS" button is replaced in-place by the two confirm buttons ("YES, RESET" + "KEEP STATS"); no modal, no overlay. Keeps the user's scroll position.
- "KEEP STATS" path: returns to idle, no network call, no data change.
- Network error on reset: "Could not reset stats. Try again." (Space Grotesk 14px `--gruvax-error`, role="alert", auto-hides after 4s) — returns to "RESET STATS" state.

### Kiosk banner mount/unmount

- Banner mounts: `opacity: 0 → 1`, `max-height: 0 → 48px`, both over `--gruvax-duration-base` (250ms) `--gruvax-ease-decelerate`. No spring (this is an informational notice, not a LED event).
- Banner unmounts (condition clears): reverse fade, `--gruvax-duration-fast` (150ms).
- Never animates on every page render — keyed to the boolean state, not the poll cycle.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — Refresh | "REFRESH" (idle) · "REFRESHING…" (loading) |
| Destructive action — Reset | "RESET STATS" (idle) · "CONFIRM RESET?" (confirm) · "YES, RESET" (confirm-primary) · "KEEP STATS" (confirm-secondary) |
| Post-reset success | "Stats cleared." |
| Empty state — top searched | "No search data yet. Stats accumulate as records are searched." |
| Empty state — slow queries | "No slow queries logged. The ring buffer resets on restart." |
| Empty state — recent logs | "No log entries in buffer." |
| Kiosk staleness banner | "Collection data may be outdated — last synced {Xd} ago" |
| Diagnostics load error | "Could not load diagnostics. Check that the API is reachable." |
| Reset error | "Could not reset stats. Try again." |
| Staleness badge — OK | "OK" |
| Staleness badge — stale (> 3d) | "STALE" |
| Staleness badge — outdated (> 14d) | "OUTDATED" |
| Nav tab label | "DIAGNOSTICS" |
| MQTT status — connected | "Connected" |
| MQTT status — disconnected | "Disconnected" |
| Phantom boundaries — zero | "0" (no additional copy; count alone is clear) |

**Voice rules applied:**
- ALL CAPS labels: Barlow Condensed — section headings, column headers, button labels, nav tabs
- Sentence case: Space Grotesk — hints, descriptions, kiosk banner text, status messages
- No generic action labels: the destructive confirm pair is "YES, RESET" / "KEEP STATS" — both name their outcome; "Cancel" / "OK" generic labels are not used.
- Plain language: no "sync_age_seconds", no "v_collection", no "ring buffer" in user-visible strings (exception: the empty-state copy for slow queries, which is admin-facing and benefits from the technical context)
- Error messages: problem description + explicit recovery action ("Check that the API is reachable", "Try again")

---

## Spacing Patterns (per component)

### Diagnostics page

```
admin-content padding:     --gruvax-space-5 (24px) — inherited from .admin-content
page gap between sections: --gruvax-space-7 (48px) — matches .settings-page gap
section card padding:      --gruvax-space-5 (24px) — matches .settings-section
section inner row gap:     --gruvax-space-4 (16px)
table row vertical pad:    --gruvax-space-3 (12px) top + bottom
column header bottom gap:  --gruvax-space-2 (8px)
status dot + label gap:    --gruvax-space-2 (8px)
toolbar button + timestamp gap: --gruvax-space-4 (16px)
```

### Kiosk banner

```
banner height (approx):    48px (auto — set by padding + font)
banner vertical padding:   --gruvax-space-3 (12px)
banner horizontal padding: --gruvax-space-4 (16px)
icon + text gap:           --gruvax-space-2 (8px)
```

---

## Accessibility

- All interactive elements have `aria-label` or visible label
- Refresh button: `aria-label="Refresh diagnostics"` when no icon text
- Destructive confirm buttons: `aria-label="Confirm reset stats"` (YES, RESET) / `aria-label="Keep stats, cancel reset"` (KEEP STATS)
- Status dots: `aria-hidden="true"` (color alone; text value is always present alongside)
- Kiosk banner: `role="alert"` on first mount; subsequent renders use `role="status"` to avoid re-announcing
- Recent logs scroll container: `aria-label="Recent log entries"` + `tabindex="0"` (keyboard scroll)
- Touch targets: all buttons minimum 44px height (matches existing `.settings-btn-primary { min-height: 48px }` and `.admin-icon-btn { height: 32px }` patterns; confirm row buttons use 44px min)
- Focus ring: `outline: 2px solid --gruvax-blue; outline-offset: 2px` on all buttons (matches existing admin CSS pattern)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

No third-party component registry is used. All UI is built from project-native `el()` DOM helpers and existing admin CSS class patterns established in Phases 3–7.

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Staleness thresholds (3d/14d) | 08-CONTEXT.md D-01 — LOCKED |
| Kiosk banner only (not no-results hint) | 08-CONTEXT.md D-02 — LOCKED |
| Route path `/admin/diagnostics` | 08-CONTEXT.md D-10 — LOCKED |
| No polling / manual refresh | 08-CONTEXT.md D-11 — LOCKED |
| Two metrics per record (search + selection counts, all-time + 7-day) | 08-CONTEXT.md D-04/D-05 — LOCKED |
| PIN-gated Reset stats action | 08-CONTEXT.md D-06 — LOCKED (note: "PIN-gated" means admin session, not a re-prompt; see destructive action spec above) |
| Slow-query breakdown (request-total + DB time) | 08-CONTEXT.md D-07 — LOCKED |
| In-memory ring buffer (reset on restart) | 08-CONTEXT.md D-08 — LOCKED |
| Per-endpoint SLO thresholds (search > 200 ms, locate > 50 ms) | 08-CONTEXT.md D-09 — LOCKED |
| Recent logs from in-memory log ring buffer | 08-CONTEXT.md D-12 — LOCKED |
| Nordic Grid design language | design/gruvax-design-language.md — project-wide |
| Token consumption (never hardcode hex) | design/gruvax-design-tokens.css + CLAUDE.md |
| el() / replaceChildren() DOM pattern | existing frontend codebase (all prior phases) |
| AdminShell nav tab pattern | frontend/src/routes/admin/AdminShell.tsx |
| `.settings-section` / `.settings-heading` / `.settings-btn-primary` | frontend/src/routes/admin/admin.css |
| Three-font system | design/gruvax-design-language.md + design/gruvax-design-tokens.css |
| Spacing token scale | design/gruvax-design-tokens.css `--gruvax-space-*` |
| Accessibility focus ring pattern | frontend/src/routes/admin/admin.css |

---

## Out of Scope (not in this UI-SPEC)

- `/api/health` and `/api/version` endpoint response shape — backend contract, no frontend
- Structured JSON logging — backend, no frontend
- Alembic CI round-trip — backend CI, no frontend
- Compose log limits and healthchecks — ops, no frontend
- `pytest-benchmark` SLO gate — backend CI, no frontend
- `just demo` smoke script — ops tooling, no frontend
- No-results page staleness hint — explicitly descoped by D-02; do not implement

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
