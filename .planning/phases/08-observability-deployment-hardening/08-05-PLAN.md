---
phase: 08-observability-deployment-hardening
plan: 05
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - frontend/src/routes/kiosk/StalenessBar.tsx
  - frontend/src/routes/kiosk/StalenessBar.css
  - frontend/src/routes/kiosk/KioskView.tsx
  - frontend/src/routes/kiosk/StalenessBar.test.tsx
autonomous: false
requirements: [OBS-06]
must_haves:
  truths:
    - "The kiosk shows a persistent staleness banner when sync_age_seconds > 14 days (1,209,600s)"
    - "The banner is hidden when sync_age_seconds is below 14 days or unknown/null"
    - "The banner reads sync_age_seconds from the /api/health response — no new endpoint"
    - "Search continues to work fully regardless of staleness; the no-results page stays generic (no staleness hint)"
    - "Banner copy is Nordic Grid: Space Grotesk 18px, plain language, no jargon, not dismissible"
  artifacts:
    - path: "frontend/src/routes/kiosk/StalenessBar.tsx"
      provides: "kiosk sync-staleness banner (>14d)"
      contains: "STALE_THRESHOLD_SECONDS"
  key_links:
    - from: "frontend/src/routes/kiosk/KioskView.tsx"
      to: "/api/health"
      via: "useQuery health -> StalenessBar syncAgeSeconds prop"
      pattern: "StalenessBar"
---

<objective>
Add the kiosk sync-staleness banner (OBS-06, SC3, D-01/D-02): a persistent, non-dismissible Nordic Grid banner that appears above the grid when `sync_age_seconds > 14 days`, reading the value from the existing `/api/health` response the kiosk consumes. Search keeps working at any staleness; the no-results page stays generic.

D-02 is honored exactly: this plan ships the kiosk BANNER only. It MUST NOT add any no-results staleness hint — that clause is deliberately descoped. Depends on Plan 03 (health.py now serves `sync_age_seconds`). Frontend-only; disjoint from Plan 04's files.

Purpose: the owner gets a visible nudge to re-sync discogsography when collection data drifts, without breaking the Core Value flow.
Output: StalenessBar.tsx/.css, KioskView wiring, a component-logic test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-observability-deployment-hardening/08-CONTEXT.md
@.planning/phases/08-observability-deployment-hardening/08-RESEARCH.md
@.planning/phases/08-observability-deployment-hardening/08-PATTERNS.md
@.planning/phases/08-observability-deployment-hardening/08-UI-SPEC.md
@.planning/phases/08-observability-deployment-hardening/08-03-SUMMARY.md
@design/gruvax-design-tokens.css

<interfaces>
<!-- From codebase + UI-SPEC. -->

From Plan 03 (/api/health response):
- now includes `sync_age_seconds: number | null` (seconds since last discogsography sync; null when unknown/empty view).

From frontend/src/routes/kiosk/KioskView.tsx (current):
- `export function KioskView()` orchestrates SearchBox <-> ResultsList <-> ShelfGrid; already uses `useQuery` from @tanstack/react-query (e.g. units/cubes queries). Add a `health` useQuery and render `<StalenessBar syncAgeSeconds={healthData?.sync_age_seconds ?? null} />` ABOVE the ShelfGrid (never overlaying it).

From frontend/src/routes/admin/ReshuffleBanner.tsx (role analog — persistent conditional banner) and 08-PATTERNS.md section StalenessBar.tsx lines 812-899:
- conditional-render (return null when not stale), inline-SVG Lucide AlertTriangle (18x18, aria-hidden), token-only CSS, `role="alert"` on mount / `aria-live="polite"`.

UI-SPEC Surface 2 (08-UI-SPEC.md lines 225-261) — render to it, do NOT re-decide:
- Trigger `sync_age_seconds > 1_209_600`; bg `--gruvax-yellow`, text `--gruvax-blue-darker`, Space Grotesk 18px (the ONLY 18px in the phase, accessibility floor), padding 12px x 16px, full-width no radius, NOT dismissible. Exact copy: "Collection data may be outdated — last synced {Xd} ago" (whole days only; em dash; sentence case; never "SYNC STALE"/"sync_age_seconds"/"collection_items").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StalenessBar component + threshold logic + Nordic Grid CSS, with a logic test</name>
  <files>frontend/src/routes/kiosk/StalenessBar.tsx, frontend/src/routes/kiosk/StalenessBar.css, frontend/src/routes/kiosk/StalenessBar.test.tsx</files>
  <read_first>
    - .planning/phases/08-observability-deployment-hardening/08-UI-SPEC.md (Surface 2: Kiosk sync-staleness banner — full anatomy, copy, color, accessibility)
    - .planning/phases/08-observability-deployment-hardening/08-PATTERNS.md (section StalenessBar.tsx lines 812-899 — verbatim component + CSS token pattern)
    - frontend/src/routes/admin/ReshuffleBanner.tsx (role analog: persistent conditional banner)
    - frontend/src/routes/kiosk/KioskView.EventSource.test.tsx (analog for the kiosk frontend test harness)
    - design/gruvax-design-tokens.css (token names — never hardcode hex)
    - .claude/skills/sketch-findings-gruvax/SKILL.md (yellow = active/changed; Space Grotesk for body; plain language)
  </read_first>
  <behavior>
    - StalenessBar({syncAgeSeconds: null}) renders nothing (returns null).
    - StalenessBar({syncAgeSeconds: 100}) (< 14d) renders nothing.
    - StalenessBar({syncAgeSeconds: 1_209_601}) (> 14d) renders the banner with text containing "Collection data may be outdated" and "14d" (Math.floor(seconds/86400)).
    - The banner has `role="alert"` (or status with aria-live) and contains an aria-hidden warning SVG.
    - The displayed days are whole numbers only (no "h" suffix on the kiosk banner).
  </behavior>
  <action>
    Create `frontend/src/routes/kiosk/StalenessBar.tsx` with `const STALE_THRESHOLD_SECONDS = 14 * 24 * 60 * 60` (1,209,600; D-01) and `interface Props { syncAgeSeconds: number | null }`. Return `null` when `syncAgeSeconds === null || syncAgeSeconds < STALE_THRESHOLD_SECONDS`. Otherwise render a full-width `.staleness-bar` div with `role="alert"` + `aria-live="polite"`, an inline 18x18 Lucide AlertTriangle SVG (`aria-hidden="true"`), and the EXACT copy `Collection data may be outdated — last synced ${Math.floor(syncAgeSeconds / 86400)}d ago` (em dash, sentence case). Copy the pattern from 08-PATTERNS.md section StalenessBar.tsx.

    Create `frontend/src/routes/kiosk/StalenessBar.css` consuming ONLY tokens: `.staleness-bar { background: var(--gruvax-yellow); color: var(--gruvax-blue-darker); font-family: var(--gruvax-font-body); font-size: 18px; font-weight: 400; padding: var(--gruvax-space-3) var(--gruvax-space-4); display: flex; align-items: center; gap: var(--gruvax-space-2); width: 100%; border-radius: 0; }` plus the mount/unmount transition per UI-SPEC (opacity + max-height over `--gruvax-duration-base`). No hardcoded hex.

    Write `StalenessBar.test.tsx` covering the behavior block (RED first, then GREEN).

    DO NOT add or modify any no-results component or copy (D-02: no-results staleness hint is DESCOPED).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/routes/kiosk/StalenessBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/routes/kiosk/StalenessBar.test.tsx` exits 0
    - `grep -n "STALE_THRESHOLD_SECONDS" frontend/src/routes/kiosk/StalenessBar.tsx` shows the value resolves to 1209600 (14 * 24 * 60 * 60)
    - `grep -niE "#[0-9a-f]{3,6}" frontend/src/routes/kiosk/StalenessBar.css` returns 0 (tokens only)
    - The banner copy matches "Collection data may be outdated — last synced {N}d ago" exactly (em dash, sentence case)
    - No file under frontend/src/routes/kiosk references a no-results staleness hint (D-02 descope honored)
  </acceptance_criteria>
  <done>StalenessBar renders only above 14d with the exact Nordic Grid copy; logic test green; no no-results hint added.</done>
</task>

<task type="auto">
  <name>Task 2: Wire StalenessBar into KioskView via a /api/health query (above the grid)</name>
  <files>frontend/src/routes/kiosk/KioskView.tsx</files>
  <read_first>
    - .planning/phases/08-observability-deployment-hardening/08-PATTERNS.md (section StalenessBar.tsx — KioskView integration point lines 887-899)
    - .planning/phases/08-observability-deployment-hardening/08-UI-SPEC.md (Surface 2: location = below top bar / above grid, never overlays the grid; offline banner takes precedence)
    - frontend/src/routes/kiosk/KioskView.tsx (current useQuery usage + ShelfGrid render location, lines 54-67 + the JSX around line 447)
  </read_first>
  <action>
    In `frontend/src/routes/kiosk/KioskView.tsx`, add a `health` `useQuery` (`queryKey: ['health']`, `queryFn: () => fetch('/api/health').then(r => r.json())`, `staleTime: 60_000`, `refetchInterval: 60_000`). NOTE: this 60s health refetch is the banner-data source and is allowed — D-11's no-polling rule applies to the ADMIN telemetry page, not the kiosk banner (which reuses the health endpoint the kiosk already needs for connectivity). Render `<StalenessBar syncAgeSeconds={healthData?.sync_age_seconds ?? null} />` ABOVE the `<ShelfGrid />` (below the top bar), so it never overlays the grid. Import StalenessBar + its CSS.

    Per UI-SPEC: if health is unavailable (kiosk offline), `sync_age_seconds` is null/absent -> banner hides; the existing offline banner takes precedence. Do not change search behavior or the no-results page (D-02).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "StalenessBar" frontend/src/routes/kiosk/KioskView.tsx` returns a match
    - `grep -n "sync_age_seconds" frontend/src/routes/kiosk/KioskView.tsx` returns a match (reading from health data)
    - The StalenessBar renders above ShelfGrid (verify placement by reading the JSX)
    - `cd frontend && npx tsc --noEmit` exits 0 and `npm run build` succeeds
    - Search / no-results behavior is unchanged (no staleness hint added to NoResultsRow)
  </acceptance_criteria>
  <done>Kiosk reads sync_age_seconds from /api/health and renders the banner above the grid when >14d; build + typecheck clean; no-results unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify kiosk staleness banner (>14d) + generic no-results</name>
  <what-built>The kiosk sync-staleness banner: a full-width yellow Nordic Grid banner above the grid that appears only when collection data is older than 14 days, reading from /api/health.</what-built>
  <how-to-verify>
    1. Rebuild the stack: `docker compose up -d --build api`.
    2. Force a stale state for testing: in the dev DB, set the discogsography sync timestamp old enough that `max(v_collection.synced_at)` is > 14 days ago (e.g. update the underlying collection_items.updated_at in the dev schema, or temporarily stub app.state.sync_age_seconds to 1_300_000 in a dev build) and wait up to 60s for the health query to refetch (or reload the kiosk).
    3. Open the kiosk URL in Chromium (or a phone-width browser). Confirm the yellow banner appears ABOVE the grid (not overlaying it) with copy "Collection data may be outdated — last synced {N}d ago", Space Grotesk 18px, blue-darker text, a warning triangle icon, and no dismiss button.
    4. Run a search — confirm search still works fully and the no-results page (search a nonsense term) stays GENERIC with NO staleness hint (D-02).
    5. Restore the sync timestamp to recent; reload — confirm the banner disappears.
  </how-to-verify>
  <action>Pause for human verification. The executor has built the kiosk staleness banner; the human must force a >14d stale state, confirm the banner renders above the grid with the exact Nordic Grid copy, confirm the no-results page stays generic (D-02), then approve. Do not auto-advance.</action>
  <verify>
    <human-check>With sync_age forced > 14d, the kiosk shows the yellow banner above the grid with copy "Collection data may be outdated — last synced {N}d ago"; search still works; the no-results page has NO staleness hint; the banner disappears when sync is recent.</human-check>
  </verify>
  <done>Human types "approved" after confirming the banner threshold behavior and the generic no-results page.</done>
  <resume-signal>Type "approved" or describe issues (e.g., contrast, placement, copy)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kiosk SPA -> /api/health | Public health read; the only datum used is sync_age_seconds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Information Disclosure | banner copy | mitigate | Copy is plain-language, no jargon, no app internals (no "sync_age_seconds"/"collection_items"); shows only whole-day age. |
| T-08-18 | Denial of Service | kiosk health refetch | accept | 60s interval against a cached app.state read (no per-request DB hit); LAN-local; negligible. |
| T-08-19 | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: none); existing frontend deps only. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/routes/kiosk/StalenessBar.test.tsx` green
- `cd frontend && npx tsc --noEmit && npm run build` succeed
- Human-verify checkpoint approved (banner shows >14d, hides otherwise, no-results stays generic)
</verification>

<success_criteria>
- Kiosk banner appears only when sync_age_seconds > 14d, reading /api/health (OBS-06, SC3, D-01)
- Banner is Nordic Grid, plain-language, non-dismissible (D-02 copy rules)
- No no-results staleness hint added (D-02 descope honored)
- Search + no-results behavior unchanged
</success_criteria>

<output>
Create `.planning/phases/08-observability-deployment-hardening/08-05-SUMMARY.md` when done.
</output>
