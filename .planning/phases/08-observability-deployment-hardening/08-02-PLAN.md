---
phase: 08-observability-deployment-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/0008_record_stats.py
  - src/gruvax/db/queries.py
  - tests/unit/test_stats.py
autonomous: true
requirements: [OBS-07]
must_haves:
  truths:
    - "Migration 0008 creates gruvax.record_stats and round-trips clean (upgrade head → downgrade base → upgrade head)"
    - "increment_search_count and increment_selection_count upsert counters keyed by release_id only — no query text column exists or is written"
    - "All-time counters always increase; the 7-day bucket resets when the last event is older than 7 days"
    - "get_sync_staleness_seconds returns max(v_collection.synced_at) age in seconds via the view only"
    - "get_phantom_boundary_count counts non-empty boundaries whose first record is absent from v_collection"
    - "get_top_searched returns up to N rows joined to v_collection display fields, ordered by all-time search count"
    - "reset/truncate clears all record_stats rows"
  artifacts:
    - path: "migrations/versions/0008_record_stats.py"
      provides: "gruvax.record_stats table + ix_record_stats_search_count index"
      contains: "record_stats"
    - path: "src/gruvax/db/queries.py"
      provides: "stats + staleness + phantom + top-N query functions"
      contains: "increment_search_count"
  key_links:
    - from: "src/gruvax/db/queries.py"
      to: "gruvax.record_stats"
      via: "ON CONFLICT (release_id) DO UPDATE upsert"
      pattern: "record_stats"
    - from: "src/gruvax/db/queries.py"
      to: "gruvax.v_collection"
      via: "max(synced_at) staleness read + phantom NOT EXISTS + top-N JOIN"
      pattern: "v_collection"
---

<objective>
Build the durable counters substrate for OBS-07: the single new `gruvax` schema table (migration `0008` — the only persistent storage this phase adds, D-06) plus all the read/write query functions in `db/queries.py` that the search/locate hooks (Plan 03) and the diagnostics endpoint (Plan 04) consume.

This plan is the privacy keystone: counters are keyed by `release_id` ONLY. No column or write path ever stores raw query text (OBS-07 hard constraint, PRIV-02). Two metrics (search count, selection count), each with an all-time total and a rolling 7-day bucket (D-04, D-05).

Purpose: the DB contract must exist before any path increments it or any page reads it.
Output: migration `0008_record_stats.py`, six new query functions in `queries.py`, and `tests/unit/test_stats.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-observability-deployment-hardening/08-CONTEXT.md
@.planning/phases/08-observability-deployment-hardening/08-RESEARCH.md
@.planning/phases/08-observability-deployment-hardening/08-PATTERNS.md
@.planning/phases/08-observability-deployment-hardening/08-VALIDATION.md

<interfaces>
<!-- Extracted from codebase + research — executor uses these directly. -->

From migrations/versions/0007_wizard_source_labels.py (structural analog) + 0002_v_collection_view.py (CREATE style):
- Module attributes: `revision: str`, `down_revision: str | None`, `branch_labels`, `depends_on`; `from alembic import op`; DDL strings executed via `op.execute(...)`.
- Convention (carried 0001-0007): all DDL via `op.execute()` with explicit constraint/index names; `downgrade()` fully reverses `upgrade()`; `alembic_version` lives in `public`; search_path set via connect-event listener in env.py.

From migrations/versions/0002_v_collection_view.py:
- `gruvax.v_collection` already selects `ci.updated_at AS synced_at` — the `synced_at` column EXISTS (no view change needed, D-03 satisfied). Display columns available on the view include `release_id`, `title`, `primary_artist`, `label`, `catalog_number`.

From src/gruvax/db/queries.py (the file to enrich — existing patterns to mirror):
- Every function takes `pool: AsyncConnectionPool`; uses `async with pool.connection() as conn, conn.cursor() as cur:`; all SQL uses `%s` placeholders (psycopg DB-API, NOT `$1`); fetches via `await cur.fetchone()` / `await cur.fetchall()`; dict rows built with `cols = [desc[0] for desc in (cur.description or [])]` then `dict(zip(cols, row, strict=True))`.
- Analog functions to copy from: `get_sync_staleness_seconds`←`did_you_mean_query` shape; `increment_search_count`←`store_idempotency` upsert; `get_phantom_boundary_count`←`cube_exact_match` NOT EXISTS pattern; `get_top_searched`←`list_change_sets` JOIN+dict pattern.

Target table (migration 0008) gruvax.record_stats columns:
`release_id BIGINT PRIMARY KEY`, `search_count BIGINT NOT NULL DEFAULT 0`, `search_count_7d BIGINT NOT NULL DEFAULT 0`, `selection_count BIGINT NOT NULL DEFAULT 0`, `selection_count_7d BIGINT NOT NULL DEFAULT 0`, `last_searched_at TIMESTAMPTZ`, `last_selected_at TIMESTAMPTZ`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`. Index: `ix_record_stats_search_count ON gruvax.record_stats (search_count DESC)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0008 — gruvax.record_stats table (release_id-keyed, no query text)</name>
  <files>migrations/versions/0008_record_stats.py</files>
  <read_first>
    - .planning/phases/08-observability-deployment-hardening/08-PATTERNS.md (§`migrations/versions/0008_record_stats.py` lines 577-638 — header, revision chain, DDL strings, upgrade/downgrade)
    - .planning/phases/08-observability-deployment-hardening/08-RESEARCH.md (Open Question 6 — table shape + 7-day rolling-bucket rationale; "Migration 0008 skeleton" lines 894-924)
    - migrations/versions/0007_wizard_source_labels.py (structural analog)
    - migrations/versions/0002_v_collection_view.py (CREATE-style + synced_at confirmation)
  </read_first>
  <action>
    Create `migrations/versions/0008_record_stats.py` with `revision = "0008"`, `down_revision = "0007"`, `branch_labels = None`, `depends_on = None`. Define `_CREATE_TABLE` creating `gruvax.record_stats` with EXACTLY the eight columns named in the interfaces block above (release_id PK; search_count, search_count_7d, selection_count, selection_count_7d all `BIGINT NOT NULL DEFAULT 0`; last_searched_at, last_selected_at `TIMESTAMPTZ`; updated_at `TIMESTAMPTZ NOT NULL DEFAULT now()`). Define `_CREATE_IDX = "CREATE INDEX ix_record_stats_search_count ON gruvax.record_stats (search_count DESC)"`. `upgrade()` runs both via `op.execute`. `downgrade()` runs `DROP INDEX IF EXISTS gruvax.ix_record_stats_search_count` then `DROP TABLE IF EXISTS gruvax.record_stats`.

    PRIVACY GATE (OBS-07): the table MUST NOT contain any text/query column. There is intentionally NO `query`, `query_text`, `term`, or `search_text` column. Add a docstring line stating "Counters are release_id-keyed aggregates; no query text is ever stored (OBS-07)".
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic downgrade base && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head && uv run alembic downgrade base && uv run alembic upgrade head` exits 0
    - After `upgrade head`, `gruvax.record_stats` exists with a `release_id` PRIMARY KEY (verify via `psql -c "\d gruvax.record_stats"` or an `information_schema` query)
    - `grep -niE "query|term|search_text" migrations/versions/0008_record_stats.py | grep -v '^#' | grep -ivc "no query text"` returns 0 (no query-text column)
    - `uv run alembic heads` shows a single head at revision 0008
  </acceptance_criteria>
  <done>0008 round-trips clean; record_stats exists with release_id PK; no query-text column anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Counter + staleness + phantom + top-N query functions in queries.py with failing tests</name>
  <files>src/gruvax/db/queries.py, tests/unit/test_stats.py</files>
  <read_first>
    - .planning/phases/08-observability-deployment-hardening/08-PATTERNS.md (§`src/gruvax/db/queries.py` lines 467-573 — verbatim function patterns for all six functions; §Shared Patterns: psycopg %s + pool.connection())
    - .planning/phases/08-observability-deployment-hardening/08-RESEARCH.md (Open Question 6 UPSERT shape; Open Question 7 phantom + pool stats; Open Question 1 staleness query; Pitfall 1 search_path; Pitfall 2 fire-and-forget exception logging)
    - src/gruvax/db/queries.py (existing analog functions: did_you_mean_query, store_idempotency, cube_exact_match, list_change_sets)
    - .planning/phases/08-observability-deployment-hardening/08-VALIDATION.md (test_stats requirements line 55)
    - tests/integration/test_search.py (analog for db_pool-backed integration-style tests, if test needs a live pool)
  </read_first>
  <behavior>
    - increment_search_count(pool, release_id): first call inserts row with search_count=1, search_count_7d=1, last_searched_at set; second call → search_count=2; after simulating last_searched_at older than 7 days, the next call resets search_count_7d to 1 while search_count keeps climbing.
    - increment_selection_count(pool, release_id): same semantics for selection_count / selection_count_7d / last_selected_at.
    - No query text is ever passed to or stored by either function (signature takes only pool + int release_id).
    - get_top_searched(pool, limit=10): returns up to `limit` dicts with keys release_id, title, primary_artist, search_count, search_count_7d, selection_count, selection_count_7d, ordered by search_count DESC; empty list when no stats.
    - get_sync_staleness_seconds(pool): returns a non-negative float for a seeded view, None when the view is empty.
    - get_phantom_boundary_count(pool): returns an int ≥ 0; counts non-empty boundaries whose (first_label, first_catalog) is absent from v_collection.
    - reset path: a TRUNCATE/clear leaves get_top_searched returning [].
  </behavior>
  <action>
    Add six async functions to `src/gruvax/db/queries.py`, each taking `pool: AsyncConnectionPool` first, all SQL using `%s` placeholders (never f-strings), all reading discogsography data ONLY through `gruvax.v_collection` (Pitfall 5):

    1. `get_sync_staleness_seconds(pool) -> float | None` — `SELECT EXTRACT(EPOCH FROM (now() - max(synced_at))) FROM gruvax.v_collection`; return `None` when row/value is None (OBS-06).
    2. `increment_search_count(pool, release_id: int) -> None` — the INSERT … ON CONFLICT (release_id) DO UPDATE upsert from 08-PATTERNS.md/RESEARCH OQ6: search_count += 1; search_count_7d = CASE WHEN last_searched_at > now() - INTERVAL '7 days' THEN search_count_7d + 1 ELSE 1; set last_searched_at = now(), updated_at = now() (D-04/D-05).
    3. `increment_selection_count(pool, release_id: int) -> None` — same shape for selection_count / selection_count_7d / last_selected_at.
    4. `get_top_searched(pool, limit: int = 10) -> list[dict[str, Any]]` — JOIN record_stats to v_collection for display fields, ORDER BY search_count DESC LIMIT %s (D-05).
    5. `get_phantom_boundary_count(pool) -> int` — COUNT of `cube_boundaries WHERE is_empty = FALSE AND NOT EXISTS (SELECT 1 FROM gruvax.v_collection v WHERE lower(v.label)=lower(cb.first_label) AND v.catalog_number = cb.first_catalog)` (Open Question 7).
    6. `reset_record_stats(pool) -> None` — `TRUNCATE gruvax.record_stats` (used by Plan 04's reset endpoint; placed here for a single SQL home).

    Write `tests/unit/test_stats.py` covering the behavior block. Tests that need a live pool use the existing `db_pool` fixture (session-scoped) with `@pytest.mark.asyncio(loop_scope="session")` (per STATE decision). To exercise the 7-day reset deterministically, directly UPDATE `last_searched_at` to `now() - INTERVAL '8 days'` between increments. Write tests RED first (they fail until 0008 + functions exist), then GREEN.

    Privacy assertion in the test: inspect `information_schema.columns` for `gruvax.record_stats` and assert no column name matches `query`/`term`/`text` (OBS-07).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_stats.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_stats.py -x -q` exits 0
    - `grep -c "increment_search_count\|increment_selection_count\|get_top_searched\|get_sync_staleness_seconds\|get_phantom_boundary_count\|reset_record_stats" src/gruvax/db/queries.py` returns ≥ 6
    - A test asserts `gruvax.record_stats` has no column matching `query`/`term`/`text` (OBS-07 privacy)
    - A test asserts the 7-day bucket resets to 1 after an 8-day-old last event while the all-time count still increments
    - `uv run mypy --strict src/gruvax/db/queries.py` exits 0
  </acceptance_criteria>
  <done>All six query functions exist with %s-parameterized SQL reading v_collection only; counters keyed by release_id with working 7-day rolling bucket; no query text anywhere; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search/locate path → record_stats | Server-side counter writes (Plan 03 calls these); release_id is the only datum crossing |
| Query functions → gruvax.v_collection | Read-only discogsography contact surface (Pitfall 5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | record_stats schema | mitigate | Table is release_id-keyed aggregates only; NO query/term/text column exists. Test asserts schema absence of such columns (OBS-07, PRIV-02). |
| T-08-06 | Injection (SQL) | new queries.py functions | mitigate | All SQL uses `%s` placeholders bound via psycopg; no f-string interpolation of inputs (project rule). |
| T-08-07 | Information Disclosure | discogsography data access | mitigate | All reads go through `gruvax.v_collection` only; no direct `collection_items` read (Pitfall 5, D-03). |
| T-08-08 | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: none); stdlib + installed deps only. |
</threat_model>

<verification>
- `uv run alembic upgrade head && uv run alembic downgrade base && uv run alembic upgrade head` exits 0
- `uv run pytest tests/unit/test_stats.py -q` green
- `uv run mypy --strict src/gruvax/db/queries.py` exits 0
- `uv run ruff check src/ tests/` exits 0
</verification>

<success_criteria>
- record_stats table created via 0008; clean Alembic round-trip (OBS-07, OBS-03 substrate)
- Two metrics × (all-time + 7-day) per release_id; no query text persisted (OBS-07, D-04/D-05)
- Staleness, phantom-count, top-N, and reset query functions ready for Plan 03/04 consumption
- All SQL reads discogsography via v_collection only (Pitfall 5)
</success_criteria>

<output>
Create `.planning/phases/08-observability-deployment-hardening/08-02-SUMMARY.md` when done.
</output>
