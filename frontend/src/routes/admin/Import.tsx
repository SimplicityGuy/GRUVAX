/**
 * Import — /admin/import route (stub for 07-04).
 *
 * This is a MINIMAL stub that returns null so the App route reference
 * compiles cleanly in plan 07-04.  Plan 07-05 (next wave) overwrites
 * this file with the real Import page implementation.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-function
export default function Import() {
  return null
}
